# 교체 파후르 모델 외형 동기화 아트 샘플

## 상태

- 승인 상태: `PENDING_USER_REVIEW`
- 재작업 상태: `2026-07-30_face_surface_fitted_angry_eyes_rework_20`
- Unity 적용 승인: `false`
- Unity 런타임 적용: `false`
- 기준 모델: `Assets/_Project/Art/Enemies/Pahur/Models/Pahur.fbx`
- 기준 모델 SHA-256:
  `9D376E7BD5262E9DF347D8CCC37675DDBCF647D063011C0A71E80CDDA4A9DEA3`
- 기준 이미지: `image/pāḫḫur(파후르).png`
- 검토 문서: `index.html`
- 요약 문서: `summary.html`

## 검토 방법

- `index.html`에서 기준 이미지와 정면 생성 렌더를 직접 비교합니다.
- `summary.html`은 같은 HTML 표현 형식으로 반영 내용·보존 범위·한계를
  요약합니다.
- `renders/03_reference_side_by_side_overview.png`는 기준 이미지와
  교체 FBX 아트 샘플의 정면 비교본입니다.
- `renders/08_head_detail_comparison.png`는 두건·화염 문양·청색
  광학부·매끄러운 얼굴을 기준 이미지·정면·3/4로 나란히 놓고,
  좌우 눈이 각 안면판의 경사와 원근을 따르는지 확인하는 확대
  비교본입니다.
- `renders/11_shoulders_left_arm_comparison.png`는 흉부·어깨·팔 장갑과
  관절 표면의 확대 비교본입니다.
- `renders/13_torso_detail_comparison.png`는 기준 이미지와 전용 몸통
  렌더를 나란히 놓은 흉갑·복부·허리 구조 비교본입니다.
- `blender/Pahur_Appearance_ReferenceSync.blend`와
  `exports/Pahur_Appearance_ReferenceSync.fbx`가 승인된 상단 판 삭제
  범위를 확인하는 검토본입니다.
- `exports/Pahur_Appearance_ReferenceSync.glb`는 범용 시각 검토본입니다.
- `EYE_SURFACE_ANALYSIS.json`은 기존 얼굴 `208`면의 정적 좌표와
  렌더 프레임에서 평가된 좌표·법선·UV·인접 관계를 기록한
  읽기 전용 표면 분석 자료입니다.

## 적용한 외형

- 교체 FBX에 이미 있는 묶은 두건 표면은 짙은 남색 비금속 천으로,
  얼굴은 매끄러운 중암도 건메탈로 분리했습니다. 머리 높이값으로
  나누지 않고 정면·3/4·측면 토폴로지에서 확정한 기존 머리
  폴리곤 `126`개를 두건 표면으로 고정했습니다.
- 두건에는 기준 이미지에서 직접 자른 주황 화염 문양과 어두운
  청색 코어를 적용했습니다. 얼굴에는 두건 바로 아래에서 서로
  떨어진 청색 광학부와 기존 금속면 위의 어두운 소켓만 표시했습니다.
- 눈은 얼굴 전체의 공통 X/Y 평면 투영에서 분리했습니다. 렌더
  프레임에서 평가된 기존 좌우 상단 안면판에 각각 접선축을 맞춘
  독립 투영을 사용하므로, 정면과 3/4에서 가까운 눈과 먼 눈이
  각 표면의 경사와 원근을 따릅니다.
- 두 눈은 하나의 렌즈 원본을 좌우 표면에 독립 배치하며 중앙 금속,
  관자놀이, 두건, 얼굴 하단을 가로지르지 않습니다. 이마 문양은
  기존 머리 로컬 표면 범위에 그대로 고정했습니다.
- 기준 이미지처럼 두 눈의 바깥쪽 눈꼬리는 높고 중앙 쪽 끝은 낮게
  보이도록, 기존 접선 평면 안에서 화면 왼쪽 눈을 `-16°`, 오른쪽
  눈을 `-14°` 회전했습니다. 정면 발광 영역 주축은 각각 `+8.9°`,
  `-11.6°`로 서로 중앙을 향해 내려갑니다.
- 얼굴 알베도에 남아 있던 반복 가로선을 제거하고, 러프니스·
  메탈릭 변화도 평탄화했습니다. 얼굴 노멀과 높이 맵은 평탄하며
  입·환기구·코·턱·추가 패널 문양은 없습니다.
- 몸통의 색상 경계를 만든 2D 투영 오버레이와 발광·금속성·거칠기
  투영 맵 연결을 모두 제거했습니다.
- 원본 몸통 `442`면에서 실제로 분리된 `7`개 연결 표면을 확인했고,
  검토 샘플에서는 중앙 상단의 독립 연결 표면 `97`만 삭제했습니다.
  남은 몸통 `424`면과 `6`개 연결 표면에는 청회색 외장, 중간 명도의
  중앙·골반 갑주, 짙은 내부 기계부를 배정했습니다.
- 법선 각도로 세분하면 `32°` 기준 `122`개 조각으로 쪼개져 다시
  삼각 얼룩이 되므로 해당 방식은 사용하지 않았습니다.
- 상단 중앙의 작은 연결 표면 `97`은 실제 중심 갑주보다 위에 따로
  끼어 있는 독립 판이므로 검토 샘플에서 정점 `11`, 에지 `27`,
  면 `18`, 루프 `54`를 실제 삭제했습니다. 재질로 숨기거나 위치를
  옮긴 결과가 아닙니다.
- 허벅지와 정강이는 어두운 청회색 강철을 기본으로 하고 국소적인
  밝은 판만 남겼습니다.
- 화염방사기는 짙은 건메탈, 후방 탱크는 매끄러운 마모 강철, 호스는
  거친 검은 고무로 분리했습니다. 황동·열변색은 작은 탱크 피팅에만
  제한했습니다.
- 새 모델의 기존 판금 형상을 우선하며 알베도에는 저주파 명도 변화,
  짧고 제한된 긁힘과 미세 수평 금속결만 넣었습니다. UV마다 늘어나던
  굵은 격자·대각선·볼트 무늬와 강한 패널 노멀은 제거했습니다.
- 몸통의 각 연결 표면은 전용 알베도·금속성·거칠기·미세 노멀만
  사용하며, 임의 패널 선·상태 표시·복부 무늬를 그리지 않았습니다.
- 팔과 다리의 재질 배정 및 텍스처는 이번 작업에서 변경하지
  않았습니다.
- 몸통·가스통·무기 재질 배정과 텍스처도 이번 얼굴 수정 전 상태를
  유지했습니다.
- 중앙 하단 연결 표면 `63·84`의 재질 배정도 이번 수정 전 상태를
  유지했습니다.
- 기준 이미지에 없는 입·세로 환기구·얼굴 패널·후면 문양·추가
  부품·화염 VFX는 만들지 않았습니다.

## 보존 범위

- 프로젝트 원본 FBX는 정점 `2,445`, 에지 `6,575`, 면 `4,348`,
  루프 `13,044` 상태로 수정하지 않았습니다.
- 검토 Blend와 FBX는 승인된 연결 표면 `97`만 삭제한 정점 `2,434`,
  에지 `6,548`, 면 `4,330`, 루프 `12,990`입니다.
- 삭제 대상을 제외한 정점 좌표·본 가중치·에지·면·UV 해시는
  삭제 전후 일치하며, UV 레이어 `1`, 본 `24`, 내장 보행 액션을
  유지합니다.
- 다른 메시를 생성·삭제·이동·변형하지 않았습니다.
- 두건·얼굴·탱크·화염방사기·호스의 실루엣과 부품 수는 교체 FBX
  원형 그대로입니다.

## Unity 적용 기준

- 이 결과는 사용자 검토용 아트 샘플이며 Unity에는 적용하지 않았습니다.
- 사용자 승인 후에도 Unity 적용은 별도 묶음 승인을 받아야 합니다.
- 적용 시 교체 FBX 임포트 설정·리그·애니메이션을 유지하고 전용
  텍스처와 머티리얼만 현재 Renderer에 연결합니다.
- `Approved Pahur Enemy Placement` 아래 10개 인스턴스에 같은 외형을
  적용하되 모델 메시와 애니메이션은 바꾸지 않습니다.
