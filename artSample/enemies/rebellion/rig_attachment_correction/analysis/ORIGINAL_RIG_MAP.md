# 리벨리온 원본 리그 구성 분석

## 분석 기준

- 원본: `appearance_reference_sync/source/Rebellion_Unity_Source_Unmodified.glb`
- 원본 리그 본 수: `29`
- 원본 스킨드 메시: `Mesh1.0`
- 원본 별도 메시: `Icosphere`
- 원본 GLB의 Armature와 본 계층은 승인 외형 GLB에서도 유지됩니다.

## 전체 계층

```text
Bone_000
└─ Bone_001
   ├─ Bone_003
   │  └─ Bone_002
   │     ├─ Bone_013 → Bone_012 → Bone_011 → Bone_010 → Bone_009
   │     ├─ Bone_018 → Bone_017 → Bone_016 → Bone_015 → Bone_014
   │     ├─ Bone_023 → Bone_022 → Bone_021 → Bone_020 → Bone_019
   │     └─ Bone_028 → Bone_027 → Bone_026 → Bone_025 → Bone_024
   ├─ Bone_005
   │  └─ Bone_004
   └─ Bone_008
      └─ Bone_007
         └─ Bone_006
```

## 역할 분리

### 루트와 몸통 중심

- `Bone_000`은 전체 리그 루트입니다.
- `Bone_001`은 몸통 내부의 상위 구조 본입니다.
- `Bone_003 → Bone_002`는 네 다리 체인이 갈라지는 중심 분기입니다.
- `Bone_005 → Bone_004`는 몸통 내부의 보조 체인입니다. 지배 정점은 없지만
  몸통 정점의 보조 가중치를 가집니다.

### 무장 계열

- `Bone_008 → Bone_007 → Bone_006`은 네 다리와 분리된 독립 분기입니다.
- `Bone_008`은 원본 정면 원반·무장 주변의 넓은 영역을 지배합니다.
  지배 정점은 `361`개이고 범위는 Blender 원본 좌표 기준
  `X=-1.0246~1.0246`, `Y=-1.1300~-0.0033`, `Z=1.3508~1.7000`입니다.
- `Bone_007`은 정면의 더 작고 돌출된 부위를 지배합니다.
  지배 정점은 `122`개이고 범위는
  `X=0.2471~0.3921`, `Y=-1.2816~-1.2421`, `Z=1.4628~1.5550`입니다.
- `Bone_006`은 `Bone_007`의 끝 본이며 지배 정점은 없습니다.
- 원본 별도 메시 `Icosphere`는 Armature 본의 자식이 아니라 독립 오브젝트입니다.

### 네 다리 계열

네 다리는 모두 `Bone_002`에서 갈라지며 서로 독립된 5본 체인입니다.
Blender 원본 좌표에서 정면은 `-Y` 방향입니다.

- 후면 `-X` 다리:
  `Bone_013 → Bone_012 → Bone_011 → Bone_010 → Bone_009`
- 후면 `+X` 다리:
  `Bone_018 → Bone_017 → Bone_016 → Bone_015 → Bone_014`
- 정면 `-X` 다리:
  `Bone_023 → Bone_022 → Bone_021 → Bone_020 → Bone_019`
- 정면 `+X` 다리:
  `Bone_028 → Bone_027 → Bone_026 → Bone_025 → Bone_024`

## 기존 승인 외형 생성 오류

- 원본 리그 자체에서 무장 계열과 다리 계열은 분리되어 있습니다.
- 기존 승인 외형 생성 스크립트는 `Z>0.88`인 모든 정점의 가중치를 합산해
  몸통 본을 자동 선택했습니다.
- 이 영역에는 원반뿐 아니라 네 다리의 상부가 함께 들어가므로
  후면 `+X` 다리의 두 번째 관절인 `Bone_017`이 잘못 선택됐습니다.
- 그 결과 새 무장 세부 오브젝트와 수납부 파생 정점이 `Bone_017`에
  잘못 귀속됐습니다.
- 따라서 수정 대상은 원본 29본 리그의 계층이 아니라, 승인 외형 생성 과정에서
  새로 추가된 오브젝트와 파생 정점의 잘못된 귀속입니다.

## 실제 변형 범위 확인

원본 GLB에서 각 후보 본을 로컬 X축으로 `15도` 진단 회전하고 실제 변형 메시를
평가했습니다.

- `Bone_008`: `1882`정점이 움직입니다. 영향 범위는 원반 정면과 상부 몸통을
  넓게 포함합니다. 무장 계열의 시작 본입니다.
- `Bone_007`: `162`정점만 움직입니다. 정면의 작고 돌출된 무장 부위에
  국한됩니다. 무장 계열의 가동 본입니다.
- `Bone_006`: 움직이는 정점이 없습니다. 무장 계열의 끝점입니다.
- `Bone_023`: `4767`정점이 움직이며 정면 `-X` 다리 한 체인과 그 접합부에
  해당합니다.
- `Bone_017`: `3607`정점이 움직이며 후면 `+X` 다리의 중간 관절 이하에
  해당합니다. 영향 범위의 최대 Z는 `1.3903`으로 정면 무장 높이보다 낮습니다.
- `Bone_002`: 네 다리 체인의 공통 상위 본이므로 회전 시 `17982`정점이
  움직입니다. 개별 다리 관절이나 무장 본이 아니라 다리 전체 분기 허브입니다.

이 결과로 원본 무장 분기와 네 다리 분기가 직접 연결되지 않았음을 확인했습니다.
