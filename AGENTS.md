# Bellerophon 공통 작업 규칙

이 문서는 저장소 전체에 적용되는 공통 규칙의 원본이다. 에이전트는 응답과 도구 실행 전에 이 문서를 확인하고, 응답·도구 실행이 규칙을 위반하지 않는지 점검한다. 이 규칙은 모든 후속 작업에 적용한다.

분야별 작업은 해당 문서를 추가로 읽는다.

| 작업 분야 | 필수 규칙 문서 |
|---|---|
| 게임 기획, UI, VFX, 사운드, 비애니메이션 아트 | `docs/DESIGN_RULES.md` |
| 모델, 텍스처, 머티리얼, 적대 개체 샘플, 모델 교체 | `docs/MODELING_RULES.md` |
| Unity 애니메이션, 적대 개체 상태, 물리 기반 모션 | `docs/ANIMATION_RULES.md` |

기존 모델링을 수정하는 작업은 `artSample/` 제작이 필요하지 않다. 신규 모델 제작, 텍스처·머티리얼 제작과 새로운 시각 디자인 작업은 분야별 규칙의 샘플 승인 절차를 따른다.

`.codex/hooks.json`의 Codex 훅 하네스가 세션 시작 규칙 주입, 승인 상태 기록, 도구 호출 전 승인 범위 차단을 기계적으로 강제한다.

## 지시 수신 점검

지시를 받을 때마다 아래 순서로 점검한다.

1. 작업 분야를 판단하고 필요한 분야별 규칙 문서를 읽는다.
2. 검증 명령 금지 목록에 걸리는 항목이 있는지 확인하고, 있으면 해당 항목을 계획에서 제외한다.
3. 읽기 외에 명령, 수정, Unity 반영, 문서, Git이 필요하면 승인 요청을 먼저 출력하고 멈춘다.

이 점검을 건너뛰고 도구를 호출하지 않는다.

## 승인

- 사용자가 명시한 단일 작업 범위만 수행한다.
- 파일 읽기, 명령 실행, 파일 수정, Unity 반영, 검증, 문서 갱신, Git 커밋·푸시 전에 승인을 받는다.
- 승인은 쪼개지 않는다. 작업에 필요한 읽기, 명령, 수정, Unity 반영, 검증, 문서, Git 범위를 최대한 모두 파악해 한 번에 묶어 한국어로 제시한다.
- 승인 요청은 아래 형식을 그대로 지킨다. 해당 내용이 없으면 `없음`이라고 쓰고 생략하지 않는다.

```
작업 승인 요청
작업 목표:
읽을 파일/범위:
수정할 파일/범위:
실행할 명령:
검증 범위:
실행하지 않을 항목:
```

- 승인 요청은 여섯 섹션의 의미와 순서를 명확히 유지한다. 빈 줄 위치와 불릿 사용은 가독성을 위한 형식이며, 내용과 승인 범위를 식별할 수 있으면 사소한 형식 차이만으로 요청을 무효화하지 않는다.
- 훅이 승인 경계를 기계적으로 비교할 수 있도록 `읽을 파일/범위`, `수정할 파일/범위`, `실행할 명령`에는 `없음` 또는 `- 없음`이 아닌 경우 섹션별로 최소 하나 이상의 구체적인 경로나 명령을 백틱으로 감싸서 적는다. 설명용 불릿까지 모두 백틱을 포함할 필요는 없다.
- 승인 요청의 `실행하지 않을 항목`에는 `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업을 항상 명시한다. 이 고정 표기는 실행 제안이나 예외 승인 요청으로 보지 않는다.
- `진행해`, `승인할게`처럼 직전 승인 요청 범위를 명확히 승인한 경우에만 그 범위 안에서 작업한다.
- 승인된 파일, 경로, 명령, 대상, 검증, Unity 반영, Git 범위 안이면 세부 작업 내용, 순서, 구현 방식, 중간 결과가 달라져도 추가 승인을 요청하지 않는다.
- 수치 조정, 캡처 횟수처럼 승인 범위 안의 사유로 다시 묻지 않는다.
- 승인 범위에 없던 파일, 경로, 명령, 대상, 검증, Unity 반영, Git 작업이 필요해 기존 경계를 벗어날 때만 즉시 중단하고 추가 승인을 요청한다.
- 사용자가 특정 명령이나 파일 수정을 직접 지시해도 그 지시 범위 밖의 파일, 명령, 검증은 추가 승인 없이 수행하지 않는다.
- 승인 요청 없이 실행한 작업은 규칙 위반이다. 해당 변경을 파기하고 사용자가 승인한 기존 상태로 되돌리는 것을 우선한다.

## 검증 명령 선제 금지 (2026-07-15)

이 절은 다른 모든 검증·완료·승인 규칙보다 우선한다.

사용자가 자신의 메시지에서 에이전트의 승인 요청보다 먼저 명령명과 대상 범위를 직접 지정하지 않았다면 다음을 수행하지 않는다.

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- `./scripts/Run-EditModeTests.ps1` 및 EditMode 테스트 관련 작업
- `./scripts/Run-PlayModeTests.ps1` 및 PlayMode 테스트 관련 작업
- `./scripts/Build-WindowsDev.ps1` 및 Windows Build 관련 작업

금지 범위는 명령 실행, 재실행, 실패 조사, 결과 파일·로그 확인, 결과 문서화, 관련 생성물 작성과 해당 항목을 승인 요청, 작업 계획, 권장 검증, 후속 단계, 완료 조건에 선제적으로 포함하는 행위까지 모두 포함한다. 에이전트가 먼저 제안한 목록에 대한 일반 승인은 직접 요청이나 예외 승인이 아니다.

예외 조건을 충족한 경우에만 검증 순서는 Harness, EditMode, PlayMode, Windows Build를 참고한다. 빌드, 씬 구성, 패키지, 프로젝트 설정을 바꿨다면 4단계까지 확인한다. 이 사다리는 기본 실행·제안·승인 요청 대상이 아니다.

## 추측과 위반 처리

- 사용자 지시가 명확한 범위 안에서만 작업한다. 애매하거나 모르는 부분은 추측하거나 임의로 구현하지 말고 질문 후 진행한다.

## 모델링·애니메이션 수정의 질문과 직접 검증 우선순위

- 모델링과 애니메이션 수정 작업을 진행할 때 대상, 부위, 의도, 기준 이미지, 동작 또는 완료 기준 중 하나라도 모르거나 애매하면 절대 추론하지 않는다. 승인 요청을 작성하기 전에 사용자에게 구체적으로 질문하고 답을 받은 뒤 진행한다.
- 모델링·애니메이션 수정 승인 요청의 `검증 범위`에는 `모호성 확인: 없음` 또는 `모호성 확인: 사용자 답변으로 해소됨`을 명시한다. 이 표기가 없거나 모호성이 남아 있는 상태이면 훅은 승인 요청 작성을 차단하고 사용자에게 먼저 질문하도록 강제한다.
- 모델링과 애니메이션 수정 작업의 검증은 수치와 스크립트보다 실제 모델링 또는 애니메이션을 직접 확인하는 것을 항상 1순위로 한다. 수치, 로그, 스크립트와 자동 검증은 직접 확인 뒤의 2순위 보조 근거로만 사용하며 직접 확인 결과를 뒤집을 수 없다.
- 모델링·애니메이션 수정 승인 요청의 `검증 범위`에는 `검증 우선순위: 1순위 직접 모델링·애니메이션 확인, 2순위 수치·스크립트 보조 검증`을 명시한다. 이 순서가 누락되거나 뒤바뀌면 훅은 승인 요청을 차단하고 직접 확인을 1순위로 고친 뒤에만 작업을 계속하게 한다.
- 직접 확인은 사용자가 표시하거나 지정한 대상과 같은 부위, 구도, 시점, 포즈 및 필요한 확대 수준에서 수정 전후를 실제로 보고 판정하는 것을 뜻한다. 이미지를 열었다는 사실, 렌더러·정점·삼각형 수, 파일 존재 또는 스크립트 성공만으로 직접 확인을 대체하지 않는다.
- 모델링·애니메이션 완료 응답에서 직접 확인 근거가 없거나 수치·스크립트 근거를 먼저 제시하면 Stop 훅이 완료 응답을 차단하고 직접 확인을 먼저 수행·보고하도록 강제한다.

## 경과 보고

- 작업 시작, 원인 확인, 첫 수정 완료, 검증 결과 확인 시점마다 현재 상태와 확인된 결과를 즉시 보고한다.
- 보고에는 현재 적용 중인 규칙 절과 현재 작업이 승인 범위 안인지 한 줄로 함께 적는다.
- 새 결과가 없어도 5분마다 수행 단계, 지연 원인, 다음 행동을 보고한다.
- 장시간 실행되는 명령이나 도구 호출은 60초 이내 간격으로 상태를 확인한다. 실행 중이면 진행 명령, 경과 시간, 대기 사유를 보고한 뒤 계속 기다린다.
- 같은 검증 실패가 두 번 연속 반복되면 추가 조정 전에 두 실패의 핵심 수치, 반복 원인에 대한 현재 판단, 다음 수정 방향을 먼저 보고한다.
- 최종 캡처는 관련 검증을 통과한 뒤 한 번만 실행한다. 여러 캡처나 반복 비교는 사용자가 명시적으로 승인한 경우에만 한다.
- 연결·스트리밍 문제로 중간 보고 미전달이 의심되면 새 변경을 계속 누적하지 말고, 실행 중인 명령 상태와 마지막 완료 단계를 먼저 확인해 보고한다.

## 기본 운영과 Unity

- 대화는 항상 한국어 존댓말로 한다. 문서를 읽을 때 한글 인코딩이 깨지지 않도록 먼저 온전히 변환한다.
- 코드 식별자, 네임스페이스, 파일명은 영어를 쓴다.
- 이 저장소는 Unity `6000.3.x LTS` 기반 Steam 출시용 게임 프로젝트이며, 현재 기준 버전은 `6000.3.16f1`이다.
- 기능 구현보다 프로젝트의 구조적 일관성과 반복 가능한 검증 루프를 우선한다.
- 새 변수를 추가하기 전 기존 코드·설정·데이터 정의에 같은 용도의 변수가 있는지 확인한다. 새 변수가 필요하면 역할을 작업 메모, 관련 문서, 코드 근처 최소 주석 중 적절한 위치에 기록한다.
- `Library`, `Temp`, `Logs`, `UserSettings`, `Builds`, `TestResults`는 생성물로 취급하며 직접 편집하지 않는다.
- 작업 목표 달성에 파일 삭제나 생성물 정리가 필요하면 대상 경로와 작업 범위를 확인한 뒤 진행할 수 있다. 단 작업 범위 밖의 사용자 변경, 저장소 이력 조작, `git reset --hard` 같은 복구 불가능한 작업은 명시 요청 없이 금지한다.
- 사용자가 직접 편집한 내용을 스크립트에 반영할 때는 그 편집값 반영만 수행하며 무관한 수정, 정리, 리팩터링, 검증 확장, 재생성을 하지 않는다.
- 사용자가 Unity에서 편집한 뒤 스크립트 반영을 요청하면 반영 후 상태를 최종 복구 시점으로 저장한다.
- 런타임 게임 로직은 가능한 한 `MonoBehaviour`에서 분리해 EditMode 테스트가 가능하게 만든다.
- Steam, 저장소, 업적, 클라우드 같은 플랫폼 기능은 인터페이스 뒤에 둔다. 게임 로직이 Steamworks SDK를 직접 참조하지 않게 한다.
- 새 기능은 설계상 최소 하나 이상의 검증 경로를 가져야 한다. 순수 로직은 EditMode 테스트, 씬·입력·물리·UI는 PlayMode 테스트를 우선한다. 실제 실행은 검증 명령 선제 금지 규칙을 따른다.
- Unity 에디터를 열거나 재시작할 때 `Unity.exe`, `.unity` 씬 파일, 임의의 `Start-Process` 명령을 직접 쓰지 않는다. 에디터 실행은 `./scripts/Open-UnityProject.ps1`를 쓴다.
- 코드·에셋 변경을 열린 에디터에 반영할 때는 재시작보다 `./scripts/Refresh-UnityProject.ps1`를 먼저 쓴다.
- stale 컴파일, 잘못 열린 에디터, `Temp/UnityLockfile`, AssetImportWorker 잔여 프로세스가 의심되어도 먼저 리프레시와 열린 에디터 브리지 재시도를 시도한다. `./scripts/Open-UnityProject.ps1 -Restart -ValidateCargoRunScene`는 리프레시 실패 또는 잘못된 프로젝트가 열린 경우의 최후 수단이다.
- `Open-UnityProject.ps1`는 `ProjectSettings/ProjectVersion.txt`와 `Assets/_Project/Scenes/CargoRunMvp.unity`의 존재를 확인하고, `-projectPath D:\Bellerophon2\Bellerophon`으로 열린 실제 프로젝트 에디터만 정상으로 인정한다.
- 에디터가 기본 `Untitled` 씬만 보여주는 상태는 정상 실행이 아니다. `Open-UnityProject.ps1`는 열린 에디터 브리지에 `OpenCargoRunMvpScene` 명령을 보내 `CargoRunMvp`가 활성 씬이 되게 해야 한다.
- Unity 복원, 재생성, Ensure, Validate, Smoke, Test, Build 계열 명령은 사용자가 명령명과 대상 범위를 명시적으로 승인한 경우에만 실행한다.

## 완료 기준과 알림

- 컴파일 에러와 새 Unity 콘솔 에러가 없어야 한다.
- 관련 테스트가 통과해야 한다. 단 검증 명령 선제 금지 범위를 넘어 실행하지 않는다. 사용자가 직접 요구하지 않은 검증은 완료 조건에서 제외하고, 실행하지 않았다는 사실을 보고한다.
- 하네스 규칙을 바꾸면 `docs/HARNESS.md`와 관련 스크립트를 함께 갱신한다.
- 아트·모델링·텍스처링은 해당 분야 문서에 정의된 나란한 시각 비교를 통과해야 한다.
- 승인된 작업을 실제로 완료하고 최종 응답을 보내기 직전에 `powershell -NoProfile -ExecutionPolicy Bypass -File ./.scripts/Notify-TaskCompletion.ps1`를 실행한다.
- 승인 요청, 사용자 확인 대기, 실패 또는 미완료 보고를 위해 최종 응답을 보내기 직전에는 `powershell -NoProfile -ExecutionPolicy Bypass -File ./.scripts/Notify-TaskPaused.ps1`를 실행한다.
- 완료 알림과 중단 알림은 한 응답에 하나만 실행한다. 알림이 실패해도 완료한 작업을 되돌리지 않고, 실패 사실을 최종 응답에 알린다.

## Codex 규칙 하네스

- `.codex/rule-sources.json`은 필수 규칙 문서, 규칙 파일 검색 패턴, 제외 경로, 보호할 훅 파일을 정의한다. 하네스는 저장소 안의 `AGENTS.md`·`AGENTS.override.md`, `docs/**/*_RULES.md`, `docs/agent-rules/**/*.md`, `docs/**/RULES.md`, `.codex/rules/**/*.md`를 동적으로 찾고 `Backups`, Unity 생성물, 샘플 아트 경로는 제외한다.
- 중첩 `AGENTS.md`·`AGENTS.override.md`는 해당 문서가 놓인 디렉터리 트리에만 적용하고, 작업 대상에 더 가까운 문서를 우선한다. 하네스는 올바른 경로별 판단을 위해 발견된 전문 전체를 주입한다.
- `Invoke-RuleHook.ps1`은 Windows PowerShell 5.1에서도 한글 훅 소스를 UTF-8로 명시적으로 읽는 ASCII 부트스트랩이다. 모든 명령 훅은 이 부트스트랩을 거쳐 실행한다.
- `SessionStart`와 `SubagentStart` 훅은 `startup`, `resume`, `clear`, `compact`를 포함한 모든 시작 경로에서 발견된 규칙 전문을 개발자 컨텍스트로 주입하고, 규칙과 하네스 파일의 SHA-256 매니페스트를 `.codex/state/`에 세션 ID별로 기록한다. 시작 훅이 건너뛰어져 매니페스트가 없으면 `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`이 현재 세션 매니페스트를 복구한다.
- `Stop`과 `UserPromptSubmit` 훅은 여섯 섹션 승인 요청을 직후의 `진행해` 또는 `승인할게` 응답과 연결한다. 승인 표현은 전체 길이가 100자 이하일 때만 인식하며, 저장된 직전 승인 요청이 없으면 승인 토큰을 만들지 않는다.
- `PreToolUse`는 매처 `*`로 훅이 지원하는 모든 로컬 도구 호출을 검사한다. 규칙 매니페스트, 현재 턴 승인 토큰, 백틱으로 명시된 파일·명령 범위, 검증 명령 선제 금지를 확인하고 위반 호출을 `deny`로 즉시 차단한다. `PermissionRequest`도 같은 정책으로 허용 또는 거부한다.
- `PostToolUse`는 규칙 또는 하네스 파일이 실행 뒤 바뀌었는지 다시 비교한다. 승인 범위 밖 변경은 후속 진행을 차단하고, 승인된 변경은 새 매니페스트와 재시작 필요 상태를 기록한다. 사후 훅은 이미 실행된 도구 결과를 자동 복구하지 못하므로 사전 훅과 함께 사용한다.
- `Stop`과 `SubagentStop`은 구조화된 승인 요청과 최종 응답의 기계 판별 가능한 규칙을 검사한다. 정상 최종 응답도 도구 사용 여부와 무관하게 첫 Stop에서 한 번 더 자체 점검하게 하며, 위반이 있으면 원래 승인 범위 안에서 해당 부분만 교정한 응답을 다시 생성한다. 같은 검증 실패가 반복되어도 세션을 중단 상태로 고정하지 않는다.
- Codex 도구 훅을 거치지 않는 호스티드 도구에는 실행 전 기계적 차단을 적용할 수 없다. 해당 도구도 주입된 규칙과 승인 범위를 동일하게 적용하고, `Stop`의 최종 자체 점검에서 응답과 수행 내용을 다시 대조한다. 이미 발생한 외부 부작용은 훅이 자동으로 되돌릴 수 없으므로 되돌렸다고 가장하지 않고 즉시 보고한다.
- `Notify-TaskCompletion.ps1`과 `Notify-TaskPaused.ps1`의 단독 `-File` 호출은 승인 토큰 없이 허용한다.
- 상태 파일은 `.codex/state/`에 세션 ID별로 저장하며 Git에 올리거나 직접 편집하지 않는다.
- 프로젝트 훅은 신뢰된 프로젝트에서만 로드된다. 훅 또는 규칙 문서가 바뀌면 현재 유지보수 작업을 마친 뒤 새 Codex 세션을 시작하고 `/hooks`에서 변경된 훅을 검토·신뢰해 다시 로드한다.
- 훅에 차단당하면 우회하지 않는다. 훅이 지적한 사항은 사용자에게 즉시 보고하고, 현재 승인 범위 안에서 추가 승인을 요청하지 않은 채 작업을 정정한 뒤 계속한다. 정정에 승인 범위 밖 파일, 경로, 명령, 대상, 검증, Unity 반영 또는 Git 작업이 필요할 때만 기존 승인 규칙에 따라 추가 승인을 요청한다.
