# Bellerophon Agent Harness

## 분산 규칙의 효력과 필수 확인

- 이 파일과 `docs/agent-rules/*.md`는 하나의 규칙 집합이며, 분산 파일의 규칙도 `AGENTS.md`와 동일한 효력을 가진다. 이 규칙은 현재 세션뿐 아니라 모든 후속 작업에 적용한다.
- 에이전트는 응답 전에 현재 적용되는 규칙을 확인하고 위반 여부를 점검한다. 규칙 위반 작업은 파기하고 사용자가 승인한 기존 상태로 되돌리는 것을 우선한다.
- 아래 라우팅 요약은 승인 전 안전 판단용이며 전문을 대체하지 않는다. 파일 읽기·명령·수정 등이 필요한 작업은 첫 묶음 승인에 적용 규칙 파일 열람을 포함하고, 승인 후 다른 작업보다 먼저 전문을 읽는다.
- 적용 파일을 읽지 않았거나 찾지 못했으면 해당 작업을 시작하지 않는다. 승인된 범위의 경계를 벗어나는 적용 파일이나 작업이 필요해질 때만 즉시 중단하고 새 묶음 승인을 받는다.
- 우선순위 표기가 있는 규칙은 그 표기를 유지한다. 특히 `docs/agent-rules/03-validation.md`의 2026-07-15 절대 규칙은 다른 검증·완료·승인 규칙보다 우선하며, `docs/agent-rules/04-art-and-modeling.md`의 2026-06-12 Override는 충돌하는 아트 규칙보다 우선한다.

## 작업별 규칙 라우팅

- 모든 작업: `docs/agent-rules/01-scope-and-approval.md`, `docs/agent-rules/02-progress-and-unity.md`
- 검증, 테스트, 빌드, 완료 판정: `docs/agent-rules/03-validation.md`
- 아트, UI, 모델링, 텍스처, 머티리얼, VFX, 사운드, 모델 교체: `docs/agent-rules/04-art-and-modeling.md`
- 적대 개체 샘플·Unity 적용·애니메이션 또는 물리 기반 모션: `docs/agent-rules/05-enemy-animation-and-physics.md`
- 둘 이상의 유형이 섞이면 해당 파일을 모두 적용한다.

## 승인 전 반드시 적용할 핵심 규칙

- 사용자가 명시한 단일 작업만 수행한다. 필요한 읽기·명령·수정·Unity 반영·검증·문서·Git 범위를 가능한 한 모두 파악해 한 번에 묶어 한국어로 제시하고 명시 승인을 받는다.
- 파일 읽기, 명령 실행, 파일 수정, Unity 브리지/검증/스모크, 문서 갱신, Git 작업 전에 승인받아야 한다. 직접 지시받은 범위 밖 행동도 추가 승인 없이는 수행하지 않는다.
- 승인 요청은 반드시 `작업 승인 요청`으로 시작하고 `작업 목표` → `읽을 파일/범위` → `수정할 파일/범위` → `실행할 명령` → `검증 범위` → `실행하지 않을 항목` 순서를 지킨다. 해당 내용이 없으면 `없음`이라고 쓴다.
- `진행해`, `승인`, `좋아`, `계속 진행`처럼 직전 요청을 명확히 승인한 뒤에만 그 범위에서 작업한다. 승인된 파일·경로·명령·대상·검증·Unity·Git 범위 안이면 세부 작업 내용, 순서, 구현 방식 또는 중간 결과가 달라져도 추가 승인을 요청하지 않는다.
- 승인된 범위에 없던 파일·경로·명령·대상·검증·Unity 반영·Git 작업이 필요해질 때만 즉시 중단하고 추가 묶음 승인을 받는다.
- 사용자가 지시한 작업을 승인범위 밖 외에는 수치 조정, 캡쳐 횟수와 같은 이유로 추가 승인 요청을 하지 않는다.
- 사용자가 직접 편집한 값의 스크립트 반영은 그 값만 반영하며 정리·리팩터링·검증 확장·재생성을 하지 않는다. Unity 편집값 반영 뒤에는 그 상태를 최종 복구 시점으로 저장한다.
- Unity 복원, 재생성, Ensure, Validate, Smoke, Test, Build 계열 명령은 사용자가 명령명과 대상 범위를 명시적으로 승인한 경우에만 실행한다.

## 선제 제안도 금지되는 검증 - 절대 규칙 (2026-07-15)

- 사용자가 에이전트의 승인 요청보다 먼저 자신의 메시지에서 명령명과 대상 범위를 직접 지정하지 않았다면 `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 각각에 관련된 실행·재실행·조사·로그·문서·생성물 작업을 수행하지 않는다.
- 위 항목을 승인 요청, 계획, 권장 검증, 후속 단계, 완료 조건에 선제적으로 포함하지 않는다. 에이전트가 먼저 제안한 뒤 받은 일반 승인은 예외가 아니다.
- 검증 사다리와 일반 품질 확인 필요성으로 예외를 추론하지 않는다. 상세 조건은 `docs/agent-rules/03-validation.md` 전문을 따른다.

## 추측 금지 및 사용자 확인 규칙 - 절대 규칙 (2026-07-18)

- 에이전트는 쓸데없이 추론하지 않고 사용자가 설명한 명확한 지시대로만 작업한다.
- 애매모호하거나 모르는 부분이 존재하는 건 추측해서 작업하지 말고 사용자한테 해당 사항을 물어보고 답을 받은 뒤 진행한다.

## 기본 운영 원칙

- 대화는 항상 한국어 존댓말로 한다. 한글 문서는 인코딩을 온전히 변환한 뒤 파악하고 코드 식별자·네임스페이스·파일명은 영어를 쓴다.
- 이 저장소는 Unity `6000.3.x LTS` 기반 Steam 출시용 게임 프로젝트이며 현재 기준은 `6000.3.16f1`이다. 기능보다 구조적 일관성과 반복 가능한 검증 루프를 우선한다.
- 애매하거나 없던 사항은 임의 구현하지 않고 사용자에게 설명을 요청한다. 계획 뒤에는 원본 기획서와 대조한다.
- 새 변수 전에는 기존 코드·설정·데이터의 동일 용도 변수를 확인하고, 새 변수의 위치와 역할을 작업 메모·문서·최소 코드 주석 중 적절한 곳에 기록한다.
- `Library`, `Temp`, `Logs`, `UserSettings`, `Builds`, `TestResults`는 생성물이므로 직접 편집하지 않는다.
- 목표 달성에 필요한 파일 삭제·생성물 정리는 대상 경로와 범위를 확인한 뒤 별도 삭제 승인을 요구하지 않고 진행할 수 있다. 작업 밖 사용자 변경, 이력 조작, `git reset --hard` 같은 복구 불가능 작업은 명시 요청 없이는 금지한다.
- 런타임 로직은 가능한 한 `MonoBehaviour`에서 분리하고, Steam·저장소·업적·클라우드 기능은 인터페이스 뒤에 둬 게임 로직이 Steamworks SDK를 직접 참조하지 않게 한다.
- 작업 시작·원인 확인·첫 수정·검증 결과 시점에 즉시 보고한다. 새 결과가 없어도 5분마다, 장시간 명령은 60초 이내마다 상태를 확인해 보고한다.
