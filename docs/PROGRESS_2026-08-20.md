# 2026-08-20 진행 기록

## 이슈판트 5번 장검 오른손 파지 보정

- 사용자가 승인한 모델링 수정 전 5번 장검의 91프레임 칼날·롤 궤적은 유지하고, 칼자루의 오른손 파지 위치만 수정했다.
- 수정 전 오른손 가중 정점 전체 평균은 손 로컬 `(-0.005946, 0.111559, 0.006808)`로 손목·건틀릿 가중 정점이 섞여 파지가 얕게 보였다.
- 손 가중 범위의 손가락·주먹 쪽 상위 절반 정점 중심 `(-0.013192, 0.169377, 0.019140)`을 새 파지점으로 사용했다. 검 메시의 폼멜 기준 파지 비율과 장검 회전은 변경하지 않았다.
- 최종 전체 검사 결과 칼자루 최대 오차는 `0.000021663m`, 기존 칼날 궤적 최대 오차는 `0°`, 기존 롤 궤적 최대 오차는 `0°`, 오른팔 곡선 오차는 `0`이다.
- Unity Edit Mode에서 Slash `7회`, Running 하체 `15회`를 반복재생한 뒤 씬 상태 복원과 상·하체 자세 오차 `0`을 확인했다.
- 최종 Unity 캡처는 수치·반복 검사를 통과한 뒤 한 번만 생성했다. 위 행의 GIF 15프레임과 아래 행의 현재 5번 15개 시점을 직접 대조하고, 같은 최종 PNG의 손 부위를 확대 판독했다.
- 직접 판독 결과 오른손 주먹이 폼멜과 가드 사이의 손잡이를 감싸며 가드와 폼멜이 주먹 양쪽 바깥에 남는다. 손목 경계의 얕은 파지는 사라졌고 전체 구간에서 같은 주먹 내부 고정점을 유지한다.
- 수치 기록: `docs/validation/ispant_slash_legacy_sword_grip_2026-08-20/Ispant_05_LegacySwordGrip_Inspection.txt`
- 직접 판독: `docs/validation/ispant_slash_legacy_sword_grip_2026-08-20/Ispant_05_LegacySwordGrip_Visual_Inspection.md`
- 최종 비교: `docs/validation/ispant_slash_legacy_sword_grip_2026-08-20/Ispant_05_LegacySwordGrip_Comparison.png`
- 최종 씬 SHA-256: `992F41CEE2BAE2238A5AD720E72F43CA327798476E18A92F7311CE90100C7758`
- 최종 비교 SHA-256: `170C6312690956A05EA99DE6766ED5A1A9274623183B963717B3F896720C8656`

## 이번 장검 파지 보정에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 기존 91프레임 장검 칼날·롤 궤적 수정
- 오른팔·오른손목 애니메이션 곡선 수정
- 장검·캐릭터 모델링, 메시, 본, 웨이트, 머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 가드 자세 또는 별도 복귀 모션 구현
- Git 작업

## 이슈판트 5번 Slash 전체 몸 + Running Hips·하체 합성

- 사용자 지정 `enemies model/išpant-new slash.fbx`와 `enemies model/išpant-new running.fbx`를 각각 `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_New_Slash_Source.fbx`, `Ispant_New_Running_Source.fbx`로 원본 그대로 복사했다. 공급 원본과 Unity 복사본 SHA-256은 Slash `AB3346A9FA93A0FC6045D5155E60BBB65A095460A444E124D236B987F899FCDE`, Running `A8471D0B2F1DF84D589A7BE3D54A171DF05E056915FD358FBEE0F74B5E1D77CB`로 각각 일치한다.
- 두 FBX의 여러 Take 중 사용자 지정대로 `mixamo.com`만 선택했다. Slash는 1–91프레임, Running은 1–43프레임이며 현재 `Ispant_New_Direct_Source.fbx`와 Generic 24본 이름·부모 계층이 정확히 일치한다.
- `Ispant_05_Slash_FullBody.anim`에 Slash 전체 Transform 곡선을 적용했다. `Ispant_05_Running_LowerBody_InPlace.anim`에는 Running의 `Hips`, 좌우 `UpLeg`, `Leg`, `Foot`, `ToeBase` 곡선만 적용했다.
- Running `Hips`의 로컬 X/Y 전진축만 고정해 제자리 반복하도록 했고 골반 회전·로컬 Z 수직 바운스와 다리 자세는 원본 그대로 유지했다.
- `Ispant_05_Running_LowerBody.mask`와 2개 Layer의 `Ispant_New_SlashRunning.controller`를 만들었다. Base Layer는 Slash 전체 몸, Override Layer는 Running `Hips`·하체이며 Root Motion은 비활성화했다.
- 현재 배치된 `Approved Ispant Enemy Placement/Ispant_05_RunningOneHandedSwordAttack/Ispant_New_Direct_Model`의 Animator에만 새 Controller를 연결했다.
- 곡선과 자세 대조 결과 Slash 상체·Running 하체의 위치·각도 최대 오차가 모두 0이고 Running 상체 곡선은 0개다. BakeMesh 정점은 전부 유한하며 기존 본체 8,755정점·9,798삼각형과 메시·웨이트·바인드포즈·머티리얼을 유지했다.
- Unity Edit Mode에서 Slash 4주기와 Running 하체 8주기를 실제 반복 재생한 뒤 중지했고 Transform과 씬 상태 복구를 통과했다.
- 수치 검사 통과 뒤 최종 캡처 명령을 한 번 실행했다. Slash 원본, Running 원본, 최종 합성을 시작·25%·50%·75%·종료의 15개 패널로 같은 모델·카메라에서 직접 대조했다.
- 직접 확인 결과 최종 상체는 Slash 원본의 준비·베기·회수 움직임과 같고, 최종 골반·하체는 Running 원본의 좌우 교대·착지 움직임과 같다. 늘어짐·찢어짐·부유 조각·비정상 관통은 보이지 않았다.
- 최종 비교: `docs/validation/ispant_slash_running_2026-08-20/captures/Ispant_05_Source_Composite_Comparison.png`
- 상세 기록: `docs/validation/ispant_slash_running_2026-08-20/Ispant_05_SlashRunning_Visual_Inspection.md`
- 최종 씬 SHA-256은 `5A550CFD50E3EE74CB8E7DBF272181E0044173979D5A9AA2AA8A2F29E6834B21`이다. 다른 이슈판트 11개 슬롯과 배치 밖 씬 루트는 변경하지 않았다.

## 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- `artSample/` 제작
- 공급 Slash·Running FBX 수정·재저장·변환
- 현재 이슈판트 메시 토폴로지·본 웨이트·바인드포즈·텍스처·머티리얼 수정
- 장검·머스켓 배치·교체·추종 방식 수정
- 다른 이슈판트 슬롯 및 배치 밖 씬 루트 수정
- AI·내비게이션·전투 판정·물리 이동 구현
- Root Motion과 슬롯 전진 이동
- 최종 1회 외 추가 Unity 캡처
- 일반 스모크·테스트·빌드
- Git 작업

## 이슈판트 5번 모델링 수정 전 장검 움직임 복원

- 모델링 수정 전 5번 씬 상태를 Git LFS의 과거 `CargoRunMvp.unity`에서 읽기 전용으로 확인했다. 당시 `Ispant_05_RunningOneHandedSwordAttack`은 GUID `acba52e14f712864989f5cd0f53d427c`의 `Ispant_RunningSwordAttack.fbx`를 사용했다.
- 당시 실제 장검 움직임은 `Ispant_05_RunningSwordAttack_InPlace.anim`의 오른손 아래 `Ispant_ApprovedLongSword` 회전 커브 91프레임에 남아 있었다. 모델링 교체 뒤에는 이 움직임이 현재 직접 모델로 옮겨지지 않았고, 별도 GIF 추종 방식이 사용되고 있었다.
- 원본 91프레임에서 당시 오른손 회전과 장검 로컬 회전을 함께 샘플링해 칼날·롤 방향을 원본 모델 공간으로 변환했다. 현재 5번은 이 방향을 그대로 재생하되, 위치는 현재 오른손 파지점에 유지한다.
- 기존 GIF 실제 좌표 방식은 현재 적용 기준에서 `SUPERSEDED` 처리했다. 오른팔·오른손목 애니메이션은 수정하지 않았다.
- 최종 수치 결과는 원본 칼날 궤적 최대 오차 `0°`, 원본 롤 궤적 최대 오차 `0°`, 오른팔 곡선 오차 `0`, 칼자루 최대 오차 `0.00013733m`다.
- Slash 상체와 Running Hips·하체 원본 자세 오차는 모두 `0`이며, 상체 좌우·높이 오차는 각각 `0.00000974m`, `0.000000536m`다. 온전한 `8,755`정점·`9,798`삼각형 메시와 머스켓 연속성, 얼굴·왼팔 안정 자세, Root Motion 비활성 및 제자리 반복을 유지했다.
- Unity Edit Mode에서 Slash `15회`, Running 하체 `32회` 반복재생 뒤 씬 상태 복원을 확인했다.
- 최종 비교 이미지의 위 행은 모델링 수정 전 5번, 아래 행은 현재 5번의 `0%`, `25%`, `50%`, `75%`, `100%`다. 직접 판독 결과 `좌하단 → 수직 상향 → 우하단 내려베기 → 오른쪽 후속 → 좌하단 복귀` 순서가 일치했다.
- 수치 기록: `docs/validation/ispant_slash_legacy_sword_motion_2026-08-20/Ispant_05_LegacySwordMotion_Inspection.txt`
- 직접 판독: `docs/validation/ispant_slash_legacy_sword_motion_2026-08-20/Ispant_05_LegacySwordMotion_Visual_Inspection.md`
- 최종 비교: `docs/validation/ispant_slash_legacy_sword_motion_2026-08-20/Ispant_05_LegacySwordMotion_Comparison.png`
- 최종 씬 SHA-256: `344245796C5AF4162726AF268BBE7E5E641C7F347E558869743A60574446DC5B`
- 최종 비교 SHA-256: `2091DA4C8D6FB329D44B2A6214FB428DF8142A0701D5F66936F3D966EFE61FBA`

## 이번 기존 장검 움직임 복원에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 오른팔·오른손목 애니메이션 곡선 수정
- 공급 FBX, 모델링, 메시, 머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 가드 및 별도 복귀 모션 구현
- Git 커밋·푸시·체크아웃·이력 변경

## 이슈판트 5번 얼굴·오른손 장검·왼팔 변형 수정

- 사용자 영상 `2026-08-20 17-07-36.mp4` 8.0231초 전체를 0.5초 간격 16프레임과 접촉 시트로 직접 확인했다.
- 수정 전 진단에서 Slash 시작 머리가 직접 모델 정면 자세와 `63.3831°`, 왼팔 상완이 `9.360298°` 어긋났고, 5번은 보정 전 메시 및 장검 추종 컴포넌트 0개 상태였다.
- Slash 클립의 `Head`를 직접 모델 정면 휴식 회전으로 고정했다. `LeftShoulder`, `LeftArm`, `LeftForeArm`도 안정 회전으로 고정했다.
- 새 메시를 만들지 않고 기존 승인된 `Ispant_New_DrawSword_Body.asset`을 5번 `char1`에 재사용해 머스켓 쪽으로 붙어 늘어나는 왼팔 변형을 제거했다.
- `IspantRigidSwordFollower`에 기존 4번 발검 방식을 보존하는 별도 손 기준 추종 모드를 추가했다. 5번 장검은 시작 칼날이 위를 향하고 칼자루가 오른손에 붙은 채 `LateUpdate`에서 오른손 위치·회전을 실시간 추종한다.
- 수치 결과는 칼자루 최대 오차 `0.000138183m`, 시작 칼날 상향 오차 `0°`, 손 기준 장검 상대 회전 오차 `0°`, 얼굴·왼팔 안정 회전 오차 `0°`다.
- 수정하지 않은 Slash 상체와 Running 하체의 원본 대비 위치·각도 오차는 모두 0이고, 제자리 반복 및 Root Motion 비활성 상태를 유지했다.
- Unity Edit Mode에서 Slash `10회`, Running 하체 `21회` 반복 후 씬 복원을 통과했다.
- 실제 Slash FBX, 실제 Running FBX, 수정된 최종 합성을 5개 시점으로 직접 대조했다. 얼굴 정면 정렬, 오른손 장검 상향 시작 및 팔 추종, 왼팔-머스켓 변형 제거를 모두 직접 확인했다.
- 검증 기록: `docs/validation/ispant_slash_running_fix_2026-08-20/Ispant_05_SlashRunning_Fix_Inspection.txt`
- 시각 기록: `docs/validation/ispant_slash_running_fix_2026-08-20/Ispant_05_SlashRunning_Fix_Visual_Inspection.md`
- 최종 비교: `docs/validation/ispant_slash_running_fix_2026-08-20/Ispant_05_SlashRunning_Fix_Comparison.png`
- 최종 씬 SHA-256: `147E6BA36A171C89F8DAB56F42609A19DC8992752DF4A78EF11E29FD7A72FDE2`

## 이번 수정에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 공급 FBX, 새 메시 토폴로지·본 웨이트·바인드포즈 수정
- 장검·머스켓 모델 형상·UV·머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- AI·내비게이션·전투·물리 이동·Root Motion 수정
- Git 작업

## 이슈판트 5번 상체 중심·정면 베기·머스켓 연속성 후속 수정

- 사용자 영상 `2026-08-20 17-45-19.mp4`의 16.4288초 전체를 2fps 33프레임과 4fps 접촉 시트로 직접 확인했다. 상체의 지속적인 왼쪽 치우침, 손 기준 고정 회전 때문에 위쪽에 머무는 장검, 기존 보정 메시의 머스켓 일부 누락을 재현했다.
- 수정 전 상체 어깨 중심의 좌우 편차 범위는 `-0.07191468m .. -0.000122075m`였다. Slash 자세 자체는 바꾸지 않고 런타임에 `Spine02` 이하 상체를 직접 모델 휴식 자세의 어깨 중심 좌우 오프셋으로 정렬했다. 수정 후 최대 좌우 오차는 `0.00000974m`다.
- 장검은 시작 칼날을 화면상 위 방향으로 유지하고, Slash 초반 `20%` 동안 오른팔 아래팔-손 방향으로 전환한 뒤 그 위치와 방향을 실시간 추종하도록 바꿨다. 칼자루 최대 오차는 `0.000140716m`, 시작 상향 오차는 `0°`, 혼합 후 팔 방향 오차는 `0.01978234°`, 상향 시작 대비 베기 회전은 `170.6153°`다.
- 기존 `9,678`삼각형 보정 메시 대신 직접 원본 `char1`의 온전한 `9,798`삼각형 메시를 복원해 개머리판·총신·총구가 끊기지 않도록 했다. 왼쪽 어깨·상완·아래팔은 직접 모델 휴식 회전을 유지해 왼팔이 머스켓으로 늘어나는 변형을 제거했다. 새 토폴로지·웨이트·바인드포즈는 만들지 않았다.
- Slash 상체 및 Running Hips·하체의 공급 원본 대비 위치·각도 오차는 모두 0이며, Root Motion 비활성·제자리 반복을 유지했다. Unity Edit Mode에서 Slash `9회`, Running 하체 `21회` 실제 반복 후 씬 복원을 통과했다.
- 수치 검사 통과 뒤 최종 비교 캡처를 한 번만 생성했다. 실제 Slash, 실제 Running, 완성 합성의 `0%`, `25%`, `50%`, `75%`, `100%`를 직접 대조하고 완성 합성 프레임을 확대 판독했다.
- 직접 판독에서 상체의 지속적 왼쪽 밀림이 사라졌고, 장검은 상향 시작 뒤 오른팔을 따라 내려와 정면 가로 베기 각도를 형성했다. 머스켓은 전체 길이가 연속적이며 왼팔과 서로 붙어 늘어나는 변형이 없다.
- 수치 기록: `docs/validation/ispant_slash_running_revision_2026-08-20/Ispant_05_SlashRunning_Revision_Inspection.txt`
- 시각 기록: `docs/validation/ispant_slash_running_revision_2026-08-20/Ispant_05_SlashRunning_Revision_Visual_Inspection.md`
- 최종 비교: `docs/validation/ispant_slash_running_revision_2026-08-20/Ispant_05_SlashRunning_Revision_Comparison.png`
- 최종 씬 SHA-256: `CB9665DBF2D19C15DCE68068ED819A1295C092A20599CE04EDFC65F724792354`
- 비교 캡처 SHA-256: `C46F99D97CE8A1060FD36DD92E8E7C4371FF8BA4499D81F4627E613FAE154438`

## 이번 후속 수정에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 공급 FBX, 새 메시 토폴로지·본 웨이트·바인드포즈 수정
- 장검·머스켓 모델 형상·UV·머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- AI·내비게이션·전투·물리 이동·Root Motion 수정
- 최종 1회 외 추가 Unity 캡처
- Git 작업

## 이슈판트 5번 상체 전체 정렬·몸 바깥 정면 베기 수정

- 사용자 영상 `2026-08-20 19-52-31.mp4` 7.367267초 전체를 4fps 30개 시점과 확대 프레임으로 확인했다. 머스켓 연속성은 유지됐지만 상체 전체가 왼쪽 아래에 남고, 준비·베기 구간의 칼날이 오른팔 안쪽과 흉곽 앞으로 들어오는 현상을 재현했다.
- 기존 `Spine02` 좌우 이동은 어깨 위쪽만 맞춰 복부·가슴의 치우침을 숨겼다. 하체의 자식인 상체 시작 본 `Spine` 전체를 직접 모델 휴식 자세의 골반 기준 좌우·높이 오프셋에 맞추도록 변경했다. 하체와 개체 루트는 건드리지 않았다.
- 장검은 아래팔 연장선 고정 대신 매 프레임 `상체 중심 → 오른손`의 정면 평면상 방사 방향으로 칼끝이 뻗는다. 칼자루는 오른손 Palm을 계속 추종하고, 장검 폭축은 모델 정면 방향으로 맞춰 정면 대상을 베는 궤적을 만든다.
- 수치 결과는 칼자루 오차 `0.00016256m`, 시작 화면 위 방향 오차 `0°`, 혼합 후 외측 방향 오차 `0°`, 정면 베기 폭축 오차 `0°`, 칼끝 최소 외측 증가 거리 `0.6607018m`다.
- 상체 전체 좌우·높이 최대 오차는 각각 `0.000009739m`, `0.000000536m`다. Slash 상체 및 Running Hips·하체의 원본 대비 위치·각도 오차는 모두 0이고 제자리 반복과 Root Motion 비활성을 유지했다.
- 최종 Unity 비교 캡처를 한 번 생성해 실제 Slash·Running과 완성 개체의 5개 시점을 대조했다. 25%·50%·75%·100% 확대 프레임에서 칼날이 오른손으로부터 몸 바깥으로 뻗고 흉곽·오른팔을 침범하지 않으며, 정면 베기 폭축을 유지하는 것을 직접 확인했다. 상체 전체 정렬과 머스켓 연결도 함께 확인했다.
- 1회 캡처의 0%에서 기존 화면 위축 부호 오류를 발견해 수정했다. 규칙에 따라 캡처를 추가하지 않았으며 현재 카메라·모델 위축 일치와 시작 각도 `0°`로 확인했다. 최종 현재 상태는 Unity Edit Mode에서 Slash `5회`, Running 하체 `11회` 반복 후 씬 복원을 통과했다.
- 수치 기록: `docs/validation/ispant_slash_running_outward_fix_2026-08-20/Ispant_05_SlashRunning_OutwardFix_Inspection.txt`
- 시각 기록: `docs/validation/ispant_slash_running_outward_fix_2026-08-20/Ispant_05_SlashRunning_OutwardFix_Visual_Inspection.md`
- 최종 비교: `docs/validation/ispant_slash_running_outward_fix_2026-08-20/Ispant_05_SlashRunning_OutwardFix_Comparison.png`
- 최종 씬 SHA-256: `687E6BD7E490B482820199DC632D2F7A0E541443FBFF095666E7F459B0192ED2`
- 비교 이미지 SHA-256: `219C7D5161AE058461BA34C8089359C1B65F4FCD5A1BDF340B659DDC63E2121D`

## 이번 수정에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 공급 FBX 수정
- 메시 토폴로지·본 웨이트·바인드포즈 수정
- 장검·머스켓 모델 형상·UV·머티리얼 수정
- 하체 애니메이션·개체 루트·Root Motion 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 최종 1회 외 추가 Unity 캡처
- Git 작업

## 이슈판트 5번 GIF 기준 장검 파지·전체 궤적 수정

- 사용자 기준 GIF `C:/Users/gus68/OneDrive/바탕 화면/animated.gif`를 220×260, 10fps, 1.5초, 15프레임으로 확인하고 전체 프레임을 순서대로 직접 대조했다.
- 기존 `상체 중심 → 오른손` 방사 방향 계산을 제거하고, GIF의 `수직 준비 → 머리 위 대각선 준비 → 정면 내려베기 → 낮은 팔로스루 → 수직 복귀` 15개 방향표본을 정규화 시간에 맞춰 반복하는 장검 전용 추종 모드로 교체했다.
- 오른쪽 어깨·상완·아래팔·손 애니메이션 곡선은 수정하지 않았다. 공급 Slash 대비 오른팔 4본 곡선 최대 오차는 `0`이다.
- 손바닥 고정점은 손 웨이트 경계 내부 중앙을 유지했다. 장검 파지점은 GIF에서 손 아래 폼멜이 전체 길이의 약 13%를 차지하는 비율에 맞춰 로컬 X `+0.000069`에서 `-0.000058`로 이동해 손등을 길게 관통하던 형태를 제거했다.
- 수치 결과는 GIF 15프레임 궤적 최대 각도 오차 `0°`, 손바닥-칼자루 최대 오차 `0.000171688m`, 정면 베기 폭축 최대 오차 `0°`, 상체 좌우·높이 오차 각각 `0.00000974m`, `0.000000536m`다.
- Slash 원본 상체와 Running Hips·하체 자세 오차는 모두 `0`이고, 온전한 `8,755`정점·`9,798`삼각형 본체 메시와 머스켓 연결, 얼굴·왼팔 안정 자세, Root Motion 비활성 및 제자리 반복을 유지했다.
- Unity Edit Mode에서 Slash `5주기`, Running 하체 `12주기`를 실제 반복 재생한 뒤 씬 상태 복원을 통과했다.
- 수치와 반복 검증 통과 뒤 최종 Unity 캡처를 한 번만 생성했다. 위 행의 GIF 전체 15프레임과 아래 행의 최종 5번 개체 동일 시점 15프레임을 확대해 직접 대조했고, 준비·머리 위 전환·내려베기·팔로스루·복귀 방향과 손 파지가 일치함을 확인했다.
- 수치 기록: `docs/validation/ispant_slash_gif_trajectory_2026-08-20/Ispant_05_SlashGifTrajectory_Inspection.txt`
- 직접 판독: `docs/validation/ispant_slash_gif_trajectory_2026-08-20/Ispant_05_SlashGifTrajectory_Visual_Inspection.md`
- 최종 비교: `docs/validation/ispant_slash_gif_trajectory_2026-08-20/Ispant_05_SlashGifTrajectory_Comparison.png`
- 최종 씬 SHA-256: `4075E756AA08EBB29249738FEC82D64EE196B19434116F29A016CABC90C93DFA`
- 비교 이미지 SHA-256: `BB438AEAE27DF5AB7FE7E6939D912A9472BF8B7E60A0D4AF8DC560582CF6258A`

## 이번 GIF 궤적 수정에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 오른팔·오른손목 애니메이션 곡선 수정
- 가드 자세·가드 상태·가드 전환 구현
- 공급 FBX 수정
- 메시 토폴로지·본 웨이트·바인드포즈 수정
- 장검·머스켓 모델 형상·UV·머티리얼 수정
- 하체 애니메이션·개체 루트·Root Motion 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 최종 1회 외 추가 Unity 캡처
- Git 작업

## 이슈판트 5번 GIF 장검 궤적 Model Cam 기준 재수정

- 사용자 실제 영상 `2026-08-20 21-00-08.mp4` 전체를 8fps로 추출하고, 실제 약 1.5초 반복 구간을 10fps·15프레임으로 위상별 재추출해 기준 GIF 15프레임과 직접 대조했다.
- 영상에서는 검의 머리 위 수평 전환과 내려베기·낮은 후속 구간이 GIF보다 늦고 좌우 방향도 어긋났다. 원인은 GIF의 화면 좌우·상하 방향표본을 모델 로컬 축으로 해석한 데 있었고, 이전 비교 카메라도 실제 `Model Cam`이 아닌 별도 축을 사용해 이 오류를 가렸다.
- GIF 15프레임의 X/Y를 화면 오른쪽/위쪽, Z를 카메라에서 장면 안쪽으로 정의하고, 실제 활성 `Model Cam`의 `right/up/forward` 축으로 변환하도록 `IspantRigidSwordFollower`를 수정했다. 검날의 베기 평면은 이슈판트 정면축을 유지한다.
- `Model Cam` 참조는 비직렬화 장면 캐시로 두어 슬롯 5 이외 개체에 빈 카메라 필드가 기록되지 않게 했다. 최종 장면 YAML에는 해당 캐시 필드가 직렬화되지 않았다.
- 오른쪽 어깨·상완·아래팔·손 곡선은 수정하지 않았고 Slash 원본 대비 오른팔 곡선 최대 오차는 `0`이다. 손바닥 결합 최대 오차 `0.000155797m`, GIF 화면 궤적 최대 각도 오차 `0.01978234°`, 정면 베기 평면 오차 `0°`다.
- 상체 좌우·높이 오차는 각각 `0.00000974m`, `0.000000477m`이며, Slash 상체·Running 하체 원본 자세 오차 `0`, 유한 본체 메시 `8,755`정점·`9,798`삼각형, 머스켓 연속성, 제자리 반복을 유지했다.
- Unity Edit Mode에서 최종 상태를 Slash `5주기`, Running 하체 `12주기` 반복재생하고 장면 복원을 확인했다.
- 수치·반복 검토 통과 뒤 최종 Unity 비교 캡처는 한 번만 생성했다. GIF와 슬롯 5의 15개 대응 프레임을 전체표 및 확대 핵심 프레임으로 직접 대조해 수직 준비, 머리 위 전환, 정면 깊이 전환, 내려베기, 낮은 후속 궤적, 직립 복귀의 화면상 검 방향이 일치함을 확인했다.
- 수치 기록: `docs/validation/ispant_slash_gif_trajectory_revision_2026-08-20/Ispant_05_SlashGifTrajectoryRevision_Inspection.txt`
- 직접 판독: `docs/validation/ispant_slash_gif_trajectory_revision_2026-08-20/Ispant_05_SlashGifTrajectoryRevision_Visual_Inspection.md`
- 최종 비교: `docs/validation/ispant_slash_gif_trajectory_revision_2026-08-20/Ispant_05_SlashGifTrajectoryRevision_Comparison.png`
- 최종 씬 SHA-256: `A5591B5EF924C6697D3AFA6D280CD13F1053145890805B25FC9484DB5F58A563`
- 비교 이미지 SHA-256: `6C6222C7D873EB77207ACC6AF7209F1EDD37937D9F4E7ED6329DC98C4195A318`

## 이번 GIF 궤적 재수정에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 오른팔·오른손목 애니메이션 곡선 수정
- 가드 자세·가드 상태·가드 전환 구현
- 공급 Slash·Running FBX 수정
- 메시 토폴로지·본 웨이트·바인드포즈 수정
- 장검·머스켓 모델 형상·UV·머티리얼 수정
- 하체 애니메이션·개체 루트·Root Motion 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 최종 1회 외 추가 Unity 캡처
- Git 작업

## 이슈판트 5번 GIF 장검 상향 궤적 재수정

- **이 절의 기존 PASS 판정은 폐기했다.** GIF 실제 좌표가 아닌 임의 방향표를 같은 기대값으로 검사한 순환 검증이었으며, 아래 `GIF 실제 장검 궤적 대입` 결과가 이를 대체한다.

- 사용자 실제 영상 `2026-08-20 21-32-46.mp4` 8.572134초 전체와 기준 GIF 15프레임을 직접 대조했다. 영상의 장검은 오른손을 기준으로 바깥쪽에서 아래로 진행해, GIF처럼 손 위와 머리 위를 지나는 방향과 반대였다.
- 원인은 GIF 화면 오른쪽을 `Model Cam.right`로 변환한 것이었다. 이슈판트 정면에서는 모델 로컬 오른쪽이 화면 왼쪽이므로 카메라에 의존하지 않는 고정 기준인 GIF 오른쪽=`-model.right`, 위=`model.up`, 깊이=`model.forward`로 수정했다.
- 오른팔·오른손목 애니메이션은 변경하지 않았다. 공급 Slash 대비 오른팔 4본 곡선 최대 오차는 `0`이다.
- GIF 상향 핵심 프레임 01·02·03·05·06·14·15에서 칼끝이 칼자루보다 위로 증가하는 최소 거리는 `0.08160949m`이며, 전체 15프레임 방향 최대 오차는 `0°`다.
- 손바닥-칼자루 최대 오차는 `0.000138452m`, 정면 베기 평면 최대 오차는 `0°`다. 상체 좌우·높이 오차는 각각 `0.00000974m`, `0.000000536m`다.
- Slash 상체·Running Hips 및 하체 원본 자세 오차는 모두 `0`이며 얼굴·왼팔 안정 자세, 온전한 `8,755`정점·`9,798`삼각형 본체 메시와 머스켓, Root Motion 비활성 및 제자리 반복을 유지했다.
- Unity Edit Mode에서 Slash `5주기`, Running 하체 `12주기`를 실제 반복재생한 뒤 장면 복원을 확인했다.
- 수치·반복 검토 통과 뒤 최종 Unity 비교 캡처를 한 번만 생성했다. GIF 위 행과 슬롯 5 아래 행의 전체 15프레임을 직접 확대 대조해 프레임 01~04와 14~15에서 칼날이 오른손 위와 머리 위로 진행하고, 중간 내려베기·낮은 팔로스루의 방향 전환과 오른손 파지가 일치함을 확인했다.
- 수치 기록: `docs/validation/ispant_slash_gif_upward_trajectory_2026-08-20/Ispant_05_SlashGifUpwardTrajectory_Inspection.txt`
- 직접 판독: `docs/validation/ispant_slash_gif_upward_trajectory_2026-08-20/Ispant_05_SlashGifUpwardTrajectory_Visual_Inspection.md`
- 최종 비교: `docs/validation/ispant_slash_gif_upward_trajectory_2026-08-20/Ispant_05_SlashGifUpwardTrajectory_Comparison.png`
- 최종 씬 SHA-256: `BCB2AC2C8E107808D2874A3C3404861F0CC559137ABCC95800890D190702BF79`
- 비교 이미지 SHA-256: `56B285CA0FBC78F601C4C75592C6D777BAC97A4756135CA039A4B80B0FC41EF4`

## 이번 GIF 상향 궤적 재수정에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 오른팔·오른손목 애니메이션 곡선 수정
- 가드 자세·가드 상태·가드 전환 구현
- 공급 Slash·Running FBX 수정
- 메시 토폴로지·본 웨이트·바인드포즈 수정
- 장검·머스켓 모델 형상·UV·머티리얼 수정
- 하체 애니메이션·개체 루트·Root Motion 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 최종 1회 외 추가 Unity 캡처
- Git 작업

## 이슈판트 5번 GIF 실제 장검 궤적 대입

- 기준 GIF 220×260, 10fps, 15프레임마다 오른손 파지점과 보이는 칼끝을 직접 표시해 원본 픽셀 좌표 15쌍을 추적했다. 좌표표는 `docs/validation/ispant_slash_gif_actual_trace_2026-08-20/Ispant_05_SlashGifActualTrace_Pixels.csv`에 저장했다.
- 기존 임의 `ReferenceBladeScreenDirections` 배열을 제거하고 실제 `(gripX, gripY, tipX, tipY)` 좌표를 런타임 입력으로 교체했다. 가장 긴 가시 표본 `152px`보다 짧은 표본은 남은 길이를 정면 깊이 성분으로 바꿔 5·7·9·10·11프레임의 원근 축소를 재현한다.
- 확대 대조에서 GIF 6~11프레임의 손과 현재 Slash 오른손이 몸의 반대쪽에 있는 사실을 확인했다. 오른팔을 수정하지 않고 두 손의 몸 기준 화면 측면이 다를 때만 추적 X를 대칭시켜 GIF의 바깥쪽→안쪽 이동 순서를 현재 오른손 측면에 맞췄다.
- 직접 판독에서 01~04 수직·머리 위 준비, 05 깊이 전환, 06~07 몸 바깥쪽 짧은 전개, 08~10 몸 안쪽 낮은 전환, 11 깊이 정렬, 12~13 긴 대각선 후속, 14~15 수직 복귀가 GIF 순서와 일치했다.
- 오른팔 곡선 오차 `0`, GIF 실제 좌표 기반 궤적 적용 오차 `0°`, 칼자루 최대 오차 `0.000172934m`, 정면 베기면 오차 `0.01978234°`다.
- 상체 좌우·높이 오차는 `0.00000974m`, `0.000000656m`이며 온전한 `8,755`정점·`9,798`삼각형 메시와 머스켓, 얼굴·왼팔 안정 상태를 유지했다.
- 최종 보정 상태를 Unity Edit Mode에서 Slash `13주기`, Running 하체 `29주기` 반복재생한 뒤 씬 복원을 확인했다.
- 실제 좌표표: `docs/validation/ispant_slash_gif_actual_trace_2026-08-20/Ispant_05_SlashGifActualTrace_Pixels.csv`
- 수치 기록: `docs/validation/ispant_slash_gif_actual_trace_2026-08-20/Ispant_05_SlashGifActualTrace_Inspection.txt`
- 직접 판독: `docs/validation/ispant_slash_gif_actual_trace_2026-08-20/Ispant_05_SlashGifActualTrace_Visual_Inspection.md`
- 확대 비교: `docs/validation/ispant_slash_gif_actual_trace_2026-08-20/Ispant_05_SlashGifActualTrace_EnlargedComparison.png`
- 최종 씬 SHA-256: `3203E4534F8E1E66BAB279D5DAC1FEDA53239A5C0E0D6AF86F69924EFBB58CD5`
- 실제 좌표표 SHA-256: `C83088C80BB1FD5A715C3BAAD69DE4D34BF0BE35C3BF1B5C4058F328D21A5518`
- 확대 비교 SHA-256: `74FE7599A9075EE21BF74371A08B88CBBB8250FAFEEFD619CF82202E582931C4`

## 이번 GIF 실제 궤적 대입에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 오른팔·오른손목 애니메이션 곡선 수정
- 가드 자세·상태·전환 구현
- 공급 Slash·Running FBX 수정
- 메시 토폴로지·본 웨이트·바인드포즈·텍스처·머티리얼 수정
- 장검·머스켓 모델 형상 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- Git 작업
