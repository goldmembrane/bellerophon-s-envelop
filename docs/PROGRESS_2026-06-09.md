# Progress 2026-06-09

## 다음 세션 시작 지점

- 이어서 작업할 때는 `docs/PROGRESS_2026-06-02.md`, `docs/PROGRESS_2026-06-08.md`, 그리고 이 문서를 먼저 읽는다.
- 세부 구현 단계 13단계부터 21단계까지는 완료 상태를 유지한다.
- 22단계는 멀티플레이 설계 게이트다. 별도 사용자 승인 없이 Steam 로비, 실제 네트워크 패키지, 최대 5인 온라인 동기화 구현을 시작하지 않는다.
- 최신 이어받기 지점은 제작 로드맵 2단계 ship interior runtime integration 검토다.
- 사용자는 최종 확인에서 복도/구역 이음새가 어느 정도 메워진 것을 확인했다. 다만 내일 작업은 실제 플레이 검토 결과를 기준으로 남은 시각 구멍, 막힘, 낙하 가능 지점이 있는지 다시 확인하는 것부터 시작한다.
- 2026-06-10 사용자 확인으로 `Control Room -> Armory` 복도는 다시 정의된 물리 복도다. 통제실 남쪽 `Cargo Hold` 출입구 바로 옆에 `Armory` 출입구를 두고, 무기고 북쪽에는 기존 `Cargo Hold` 램프를 막지 않는 3-segment 경로로 연결한다. 중간 랜딩은 세그먼트 바닥과 같은 높이로 맞춰 턱이 생기지 않게 한다.
- 최근 주요 수정은 경사 복도 endpoint 실링이다. Cargo Hold에서 Cockpit, Engine Room, Control Room, Armory, Supply Room으로 올라가는 5개 경사 복도 양 끝에 `Sloped Endpoint Seal`을 생성하고, 방 벽 평면 패치, 3.2m 복도 방향 슬리브, `Left/Right Side Wedge Fill` 전환 메쉬를 붙였다.
- 특히 사용자 지적 위치였던 `Cargo Hold -> Control Room`의 Cargo Hold 쪽, `Cargo Hold -> Engine Room`의 Engine Room 쪽, `Cargo Hold -> Armory` 양쪽, `Cargo Hold -> Supply Room` 양쪽 입구 옆 틈은 side wedge mesh로 메우는 기준을 추가했다.
- 검증은 `Phase4CargoShipGrayboxEditorValidation`과 `PostDetailedStage2ShipInteriorEditorValidation`에서 경사 endpoint마다 side wedge mesh와 collider가 없으면 실패하도록 강화했다.
- 정의되지 않은 `Control Room -> Supply Room`, `Supply Room -> Control Room` 물리 복도는 계속 생성하지 않는다.
- Unity Editor는 `.\scripts\Open-UnityProject.ps1`로 열고, 잘못 열린 `Untitled` 씬이나 임시 세션처럼 보이면 `.\scripts\Open-UnityProject.ps1 -Restart -ValidateCargoRunScene`로 다시 연다.
- 최신 검증은 `Run-Phase4CargoShipGrayboxSmoke.ps1` 기준 `Corridors=10`, `ValidatedRoutes=10`이며, `Run-PostDetailedStage2ShipInteriorSmoke.ps1`, `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`, `git diff --check` 모두 통과 상태다.
- Unity-generated `Assets/_Recovery` 및 `_Recovery.meta`는 생성물이므로 생기면 정리한다. `CargoRunMvp.unity`와 Phase20 material YAML의 줄 끝 공백은 Unity 재저장 뒤 다시 생길 수 있으므로 `git diff --check`로 확인한다.

## 오늘 작업

### 상점 구매 후 장비 슬롯 동기화

- 상점에서 다른 무기를 구매한 뒤 막대기로 전환하면 구매한 무기로 다시 전환되지 않을 수 있던 문제를 수정했다.
- `FirstPersonHandInventory`가 슬롯 선택 변경을 노출하고, 직접 슬롯 선택/마우스 휠 순환을 지원하며, 권위 장비 상태에서 활성 슬롯을 동기화할 수 있게 했다.
- `PlayerEquipmentController`가 손 장비 슬롯 선택을 `ShipDeviceState`로 전달하고 `ActiveHandSlotIndex` 기준으로 손 인벤토리를 갱신하게 했다.
- 구매한 무기와 손 인벤토리 선택이 계속 동기화되는지 확인하는 EditMode 테스트를 추가했다.

### 운송 중 ESC와 수동 포탑 프롬프트

- 운송 진행 중 ESC를 눌렀을 때 커서 해제 상태를 통해 수동 포탑 상호작용 모드/프롬프트가 노출될 수 있던 문제를 수정했다.
- `FirstPersonPlayerInput`이 ESC로 커서가 해제된 상태를 추적하고, 그 상태에서는 게임플레이 액션을 억제하게 했다.
- `FirstPersonInteractionController`가 ESC 커서 해제 중에는 월드 타겟 갱신을 건너뛰어 오래된 포탑 상호작용 프롬프트가 뜨지 않게 했다.
- ESC 기반 게임플레이 액션 억제와 운송 시작 시 이전 수동 포탑 패널 상태 초기화를 확인하는 EditMode 테스트를 추가했다.

### 21단계 테스트 기대값 보정

- 21단계 총손실 정산 테스트 기대값을 `1100`에서 `1000`으로 보정했다.
- 정산 규칙은 변경하지 않았다. 총손실로 실패한 운송은 의뢰 보상을 지급하지 않고, 수리 보험 청구는 보류 상태로 남는다.

## 검증

- `.\scripts\Run-HarnessValidation.ps1` passed.
- `.\scripts\Run-EditModeTests.ps1` passed.
- `.\scripts\Run-PlayModeTests.ps1` passed.
- `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1` passed.
- `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1` passed.

## Unity Editor 메모

- 오래된 컴파일 산출물 때문에 EditMode 테스트가 이전 결과를 계속 사용하는 상태가 있어 Unity Editor를 재시작했다.
- 재시작 전에 오래된 생성물 `Temp/UnityLockfile`을 제거했다.
- 재시작 후 생성된 `Assets/_Recovery`가 에디터 복구 산출물임을 확인하고 제거했다.

## 22단계 주의 사항

- 22단계는 `온라인/멀티플레이 설계 게이트`다.
- 필요한 작업은 구현이 아니라 설계와 검토다. 권위 모델, 상호작용 소유권, 권위 동기화 범위, Steam 로비/초대/재접속/중도 참가/연결 해제/마이그레이션 정책, 로컬 협동 상태와 온라인 메시지 매핑, 네트워크 테스트 전략을 정한다.
- 완료 조건은 멀티플레이 설계에 대한 별도 사용자 승인이다.

## 22단계 설계 게이트 실행 결과

- `docs/MULTIPLAYER_DESIGN_GATE.md`를 추가했다.
- 설계 결정:
  - 온라인 MVP는 최대 5인, host-authority 방식으로 진행한다.
  - Steam lobby는 발견/초대/접속 부트스트랩 용도이며 canonical gameplay state를 저장하지 않는다.
  - 위험 구간, 침입자, 정산, 사망자 수, 화물 손실, 방 손상, 지갑, 계약, 장비/상점 상태는 host authoritative session에서만 확정한다.
  - 조종석, 무기실 수동 포탑, 통제실, 동력실, 비품창고는 exclusive device ownership으로 처리한다.
  - 정비/상점/의뢰/화물 보급/업그레이드 UI의 상태 변경은 초기 온라인 MVP에서 host only로 제한한다.
  - late join은 행성/출발 전 상태에서만 허용하고, 운송 중 새 참가자는 차단한다.
  - 같은 플랫폼 identity의 reconnect는 허용하지만, 장치 소유권은 자동 복구하지 않는다.
  - host migration은 초기 온라인 MVP 범위에서 제외한다.
- 실제 Steamworks SDK, 네트워크 패키지, 로비 생성, invite 처리, max-5 sync 구현은 시작하지 않았다.
- 다음 구현 단계로 넘어가려면 `docs/MULTIPLAYER_DESIGN_GATE.md` 설계에 대한 사용자 구현 승인 확인이 필요하다.
- 사용자 확인: 네트워크 관련 구현은 게임을 다 만든 뒤 진행한다. 22단계 완료는 출시 준비 완료나 출시 승인 의미가 아니다.
- 22단계 문서 검증:
  - `.\scripts\Run-HarnessValidation.ps1` passed.
  - `git diff --check -- docs/MULTIPLAYER_DESIGN_GATE.md docs/PROGRESS_2026-06-09.md docs/DETAILED_IMPLEMENTATION_PLAN.md` passed with CRLF warnings only.
  - Repository-wide `git diff --check` still reports pre-existing trailing whitespace in modified Unity scene/material files outside the 22단계 문서 변경 범위.

## 상세 구현 이후 제작 로드맵

- `docs/POST_DETAILED_IMPLEMENTATION_ROADMAP.md`를 추가했다.
- 계획의 기준:
  - 상세 구현 22단계 완료는 출시 준비 완료가 아니라 시스템 기반 완료로 본다.
  - 다음 작업은 모델링, 아트 디렉션, 선박 내부 제작, 장비/소품, 적대 개체, 사운드, VFX, UI/UX, 콘텐츠 완성, QA, 최적화, 패키징 순서로 진행한다.
  - 네트워크 작업은 비네트워크 게임이 완성된 뒤 `docs/MULTIPLAYER_DESIGN_GATE.md`를 기준으로 별도 진행한다.
- 확인한 현재 상태:
  - `image/` 원본 적대 개체 레퍼런스는 존재한다.
  - Unity 런타임 자산용 `Assets/_Project/Art`와 `Assets/_Project/Audio` 폴더는 아직 없다.
  - 따라서 다음 실제 작업은 asset inventory, 자산 폴더/명명 규칙, art direction bible, 첫 모델링 slice 확정부터 시작하는 것이 적절하다.

## 아트 샘플 승인 규칙 추가

- `AGENTS.md`에 아트/에셋 승인 규칙을 추가했다.
- 모델링, UI, 애니메이션, 머티리얼, VFX, 사운드처럼 아트와 연관이 깊은 작업은 실제 게임에 붙이기 전에 저장소 루트의 `artSample/`에 사용자가 검사할 수 있는 샘플 파일을 먼저 만든다.
- 사용자가 샘플을 검사하고 승인한 뒤에만 해당 결과물을 실제 게임 씬, 프리팹, 런타임 자산, UI 흐름에 연결한다.
- 같은 규칙을 `docs/HARNESS.md`의 제약 목록에도 연결했다.

## 제작 로드맵 1단계 실행

- `docs/POST_DETAILED_IMPLEMENTATION_ROADMAP.md`의 1단계 `Art Direction And Asset Bible` 산출물을 준비했다.
- 추가한 문서:
  - `docs/ART_DIRECTION_BIBLE.md`
  - `docs/ASSET_PRODUCTION_LIST.md`
- 새 아트 승인 규칙에 맞춰 검사용 샘플을 추가했다:
  - `artSample/README.md`
  - `artSample/art_direction_reference_board.html`
- 1단계 내용:
  - 저채도 산업/폐쇄 공간, 어두운 조명, 거친 표면, 제한 시야, 불확실한 위협을 핵심 시각 기둥으로 정리했다.
  - 낡은 도장 금속, 어두운 노출 금속, 화물 스트랩/고무, 조종석 유리, 경고 페인트, 콘솔 화면, 손상/그을린 표면, 유기체 침입 재질을 material bible로 정리했다.
  - 정상 선박, 손상 방, 저시야 복도, 위험 경보, 침입자 경보, 행성 허브, 게임오버 조명 상태를 lighting bible로 정리했다.
  - 적대 개체 실루엣 우선순위는 Parvum, Transfer/player body, 외부 목표 3종, 나머지 seed entities, alien lifeforms, Cargo Freedom League, space pirates 순으로 잡았다.
  - 첫 production slice는 cargo hold, main corridor kit, cockpit helm/frame, armory turret handle, first-person arms/stick/musket, Parvum으로 잡았다.
- 실제 Unity 씬, 프리팹, 런타임 자산, UI 흐름에는 아무것도 연결하지 않았다.
- Bulk modeling과 runtime integration은 사용자가 `artSample/art_direction_reference_board.html`을 검토하고 승인하기 전까지 대기한다.

## 제작 로드맵 1단계 승인 및 2단계 샘플 준비

- 사용자가 `artSample/art_direction_reference_board.html`을 검토하고 "이대로 진행"으로 승인했다.
- `docs/ART_DIRECTION_BIBLE.md`의 상태를 stage 1 production target approved로 갱신했다.
- 2단계 `Ship Interior Modeling Pass`로 넘어가기 위한 검사용 샘플을 준비했다.
- 추가한 문서:
  - `docs/SHIP_INTERIOR_MODELING_SAMPLE.md`
- 추가/갱신한 샘플:
  - `artSample/ship_interior_modeling_sample.html`
  - `artSample/README.md`
- 갱신한 문서:
  - `docs/POST_DETAILED_IMPLEMENTATION_ROADMAP.md`
  - `docs/ASSET_PRODUCTION_LIST.md`
- 2단계 샘플 범위:
  - Cargo hold shell and strap kit.
  - Main corridor straight/curve/ramp/threshold kit.
  - Cockpit forward frame and helm direction.
  - Armory turret station direction.
  - Visual scale proposal.
  - Normal/warning/offline/heavily damaged/total-loss damage-state layer direction.
- 사용자 추가 확인 사항:
  - 복도는 최대 2명이 동시에 이동 가능한 폭으로 설정한다.
  - 각 구역은 사람 3명만 있어도 꽉 차 보이게 크기를 설정한다.
  - 화물선 내부는 고급적인 느낌보다 전체적으로 많이 사용한 투박한 느낌이 주가 되어야 한다.
- 실제 Unity 씬, 프리팹, 런타임 머티리얼, 콜라이더, 상호작용 앵커, UI 흐름에는 아직 연결하지 않았다.
- 다음 실제 ship interior runtime integration은 사용자가 `artSample/ship_interior_modeling_sample.html`을 검토하고 승인한 뒤에만 진행한다.

## 제작 로드맵 2단계 체크리스트 답변 반영

- 사용자 체크리스트 답변을 반영했다:
  - 화물칸은 첫 production room으로 진행한다.
  - 현재 곡선 복도는 임시방편이므로 production에서는 제거한다.
  - 복도 2명 동시 이동은 폭 기준이다. 앞뒤로 여러 명이 이동하는 것은 제한하지 않는다.
  - 방은 사람 3명만 있어도 꽉 차 보이는 기준 그대로 진행한다.
  - damage-state layer 방향은 그대로 진행한다.
- 3번 `Cockpit helm/frame and armory turret station as the first device props`는 사용자가 상세 설명을 요청해 설명했고, 이후 그대로 진행하도록 승인했다.
- `docs/SHIP_INTERIOR_MODELING_SAMPLE.md`와 `artSample/ship_interior_modeling_sample.html`에 3번 항목의 의미를 설명했다:
  - Cockpit helm/frame은 운송 시작 상호작용 주변의 낡은 전면 프레임, 더러운 유리, 조종면, 손잡이, 버튼, 상태 화면 실루엣을 뜻한다.
  - Armory turret station은 수동 포탑 상호작용 주변의 포탑 손잡이, 마운트, 지지 프레임, 경고 마킹 실루엣을 뜻한다.
  - 이는 운송/포탑 gameplay rule 변경이나 Unity runtime attachment가 아니다.
- 실제 Unity 씬, 프리팹, 런타임 머티리얼, 콜라이더, 상호작용 앵커, UI 흐름에는 아직 연결하지 않았다.

## 제작 로드맵 2단계 샘플 승인

- 사용자가 3번 장치 prop 항목도 그대로 진행하도록 승인했다.
- 이로써 `artSample/ship_interior_modeling_sample.html`의 2단계 샘플 체크리스트는 승인 완료 상태다.
- 승인된 2단계 방향:
  - 화물칸을 첫 production room으로 진행.
  - 곡선 복도 제거.
  - straight/angled/ramp/threshold corridor kit 우선.
  - 복도는 2명이 나란히 이동 가능한 폭 기준.
  - 방은 3명만 있어도 꽉 차 보이는 밀도 기준.
  - 화물선 내부는 고급감보다 투박하고 많이 사용한 작업선 느낌.
  - Cockpit helm/frame과 armory turret station을 첫 device prop art sample 대상으로 진행.
  - Damage-state layer는 제안대로 진행.
- 추가 사용자 확인: 2단계 production 적용 시 현재처럼 벽 없는 열린 상태가 아니라, 방과 복도에 실제 벽, 문틀/문턱, 천장 구조를 세워 닫힌 화물선 내부로 구성해야 한다.
- 실제 Unity runtime integration은 아직 시작하지 않았다.

## 제작 로드맵 2단계 적용 완료

- 승인된 2단계 선박 내부 모델링 방향을 실제 `CargoRunMvp` 생성 경로에 적용했다.
- 변경 범위:
  - `Phase4CargoShipGrayboxBootstrap`이 기존 바닥 중심 graybox 대신 production ship interior shell을 생성한다.
  - 6개 방 모두 바닥, 벽, 문틀/문턱, 천장 구조를 가진 닫힌 내부로 생성된다.
  - 복도 폭은 2명이 나란히 이동 가능한 기준인 `2.6m`로 설정했다.
  - Cargo Hold to Armory 임시 곡선 복도는 제거하고 3-segment angled/ramp route로 교체했다.
  - 화물칸 고정 프레임/스트랩, cockpit forward frame/helm 표면, armory turret station support frame을 실제 씬 생성 경로에 연결했다.
  - 투박하고 많이 사용한 화물선 느낌을 위해 rough wall/floor/ceiling, worn door frame, cable tray, warning/damage material을 `Assets/_Project/Art/Ship/Materials` 아래에 생성했다.
  - Phase16 low-saturation atmosphere pass가 새 production material도 함께 어둡고 저채도로 보정하도록 연결했다.
- 새 검증 경로:
  - `Assets/_Project/Editor/Validation/PostDetailedStage2ShipInteriorEditorValidation.cs`
  - `scripts/Run-PostDetailedStage2ShipInteriorSmoke.ps1`
- 검증 결과:
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
    - `Rooms=6; CorridorWidth=2.6; ArmoryCargoSegments=3; ClosedShell=True; RuntimeIntegration=True`
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed

## 2026-06-09 경사 복도 입구 옆 틈 추가 수정

- 사용자 플레이 검토에서 다음 경사 복도 endpoint의 입구 옆 틈에 외부 공간이 보이는 문제가 확인되었다:
  - `Cargo Hold -> Control Room`의 Cargo Hold 쪽 입구
  - `Cargo Hold -> Engine Room`의 Engine Room 쪽 입구
  - `Cargo Hold -> Armory`의 양쪽 입구
  - `Cargo Hold -> Supply Room`의 양쪽 입구
- 원인:
  - 이전 `Sloped Endpoint Seal`은 방 벽 평면 패치와 복도 방향 슬리브를 각각 만들었지만, 서로 다른 각도로 만나는 옆면 전환부를 하나의 연결 메쉬로 잇지 않았다.
  - 대각선 경사 복도에서는 그 사이가 입구 옆 삼각형 틈으로 남아 외부 공간이 보일 수 있었다.
- 수정 내용:
  - 모든 경사 복도 endpoint에 `Left Side Wedge Fill`과 `Right Side Wedge Fill`을 추가했다.
  - side wedge는 방 벽 평면의 문 옆 edge와 복도 슬리브의 문 옆 edge를 직접 잇는 수직 prism mesh다.
  - side wedge에는 renderer와 mesh collider를 모두 붙여 시각 구멍과 옆 틈 진입을 같이 막는다.
- 검증 강화:
  - `Phase4CargoShipGrayboxEditorValidation`과 `PostDetailedStage2ShipInteriorEditorValidation`은 경사 endpoint마다 side wedge mesh와 collider가 없으면 실패한다.
- 검증 결과:
  - `.\scripts\Run-Phase4CargoShipGrayboxSmoke.ps1`: passed
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed

## 2026-06-09 복도 외부 노출 구멍 최종 수정

- 사용자 플레이 검토에서 방-복도 이음새와 경사 복도 상단에 외부 검은 공간이 보이는 문제가 다시 확인되었다.
- 원인은 방 문 개구부가 실제 2.6m 복도보다 넓게 뚫려 있고, Cargo Hold처럼 낮은 데크에서 올라가는 경사 복도는 방 천장보다 복도 천장이 높아 문 위쪽에 빈 수직 공간이 남는 구조였다.
- 수정 내용:
  - 각 복도 시작/끝 threshold에 `Left/Right Mouth Closure Wall`을 추가해 넓은 문 개구부와 복도 폭 사이의 좌우 빈 공간을 막았다.
  - 각 threshold에 `Upper Bulkhead Wall`을 추가해 문 높이 위부터 복도 천장까지 이어지는 검은 외부 노출 공간을 막았다.
  - threshold `Ceiling Cap` 폭을 4.4m mouth 폭으로 넓혀 방-복도 접합부 상단이 복도 벽보다 좁게 끊기지 않도록 했다.
  - `Floor Lip`은 시각 실링으로만 유지하고 collider를 제거했다. 경사 램프 끝에서 수평 립 콜라이더가 캐릭터 이동을 막는 문제가 있었기 때문이다.
  - `Engine Room -> Control Room` 전면 우회 복도는 정의된 기존 복도만 유지하고, 90도 코너 내부 벽을 더 짧게 끊어 CharacterController 회전 공간을 확보했다.
  - 정의되지 않은 `Control Room -> Armory`, `Control Room -> Supply Room`, `Supply Room -> Control Room` 복도는 계속 생성하지 않는다.
- 검증 강화:
  - Post-detailed 2단계 검증에서 모든 threshold의 mouth closure wall, upper bulkhead wall, 넓어진 ceiling cap 존재와 최소 폭/높이를 확인한다.
  - Phase4 PlayMode smoke에서 9개 정의 복도를 모두 CharacterController로 실제 통과 검증한다.
- 현재 검증 결과:
  - `.\scripts\Run-Phase4CargoShipGrayboxSmoke.ps1`: passed
    - `Corridors=9; ValidatedRoutes=9; ArmoryCargoRoute=7.77; ControlCargoRoute=10.29; SupplyCargoRoute=7.77; SupplyArmoryRoute=19.56`
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
    - `Rooms=6; CorridorWidth=2.6; ArmoryCargoSegments=1; SupplyCargoSegments=1; SupplyArmorySegments=1; ClosedShell=True; RuntimeIntegration=True`

## 2026-06-09 경사 복도 endpoint 외부 노출 추가 수정

- 사용자 플레이 검토에서 경사 복도와 방을 잇는 양 끝 접합부에 외부 검은 공간이 계속 보이는 문제가 다시 확인되었다.
- 원인:
  - 기존 threshold seal은 복도 방향으로 회전된 보강물이라, 대각선 경사 복도에서는 축정렬된 방 벽 개구부와 복도 외피 사이에 삼각형 틈이 남을 수 있었다.
  - 기존 검증은 복도 중앙 통행 가능 여부, threshold seal, 중간 joint seal만 확인했고, 경사 복도 endpoint 전체가 방 벽 평면과 복도 방향 슬리브로 닫혔는지는 확인하지 않았다.
- 수정 내용:
  - Cargo Hold에서 Cockpit, Engine Room, Control Room, Armory, Supply Room으로 올라가는 5개 경사 복도 양 끝에 `Sloped Endpoint Seal`을 추가했다.
  - 각 endpoint에는 방 벽 평면 기준 `Room Plane Left/Right Closure Wall`, `Room Plane Upper Bulkhead Wall`, `Room Plane Ceiling Cap`을 생성해 방 벽 개구부와 복도 외피 사이의 삼각형 틈을 막는다.
  - 각 endpoint에는 복도 방향으로 3.2m 깊이의 `Sleeve Left/Right Closure Wall`, `Sleeve Ceiling Cap`을 생성해 입구 바로 뒤쪽 측면/천장 노출을 막는다.
  - 정의되지 않은 `Control Room -> Armory`, `Control Room -> Supply Room`, `Supply Room -> Control Room` 복도는 계속 생성하지 않는다.
- 검증 강화:
  - `Phase4CargoShipGrayboxEditorValidation`과 `PostDetailedStage2ShipInteriorEditorValidation`은 5개 경사 복도의 시작/끝 총 10개 endpoint에 위 실링 오브젝트가 없으면 실패한다.
- 검증 결과:
  - `.\scripts\Run-Phase4CargoShipGrayboxSmoke.ps1`: passed
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed

## 2026-06-09 Unity 에디터 실행 경로 고정

- 문제 원인:
  - 검증 후 Unity를 다시 열 때 손으로 `Start-Process` 명령을 조합하면서 프로젝트 락, AssetImportWorker 잔여 프로세스, 작업 디렉터리, 실제 `-projectPath` 검증을 한 번에 처리하지 못했다.
  - 이 상태에서는 Unity가 사용자가 보기에는 임시 파일/잘못된 세션처럼 열린 것처럼 보일 수 있다.
- 재발 방지:
  - `scripts/Open-UnityProject.ps1`를 추가했다.
  - interactive Unity 에디터는 앞으로 이 스크립트로만 연다.
  - stale editor, 남은 lockfile, AssetImportWorker 잔여 상태가 의심되면 `.\scripts\Open-UnityProject.ps1 -Restart -ValidateCargoRunScene`를 사용한다.
  - 스크립트는 `ProjectSettings\ProjectVersion.txt`, `Assets\_Project\Scenes\CargoRunMvp.unity`, Unity 실행 파일, 열린 에디터의 `-projectPath` 명령줄을 검증한다.
  - `UnityEditorValidationBridge`에 `OpenCargoRunMvpScene` 명령을 추가해 프로젝트가 열렸더라도 기본 `Untitled` 씬에 머무르면 실패하도록 했다.
  - `.\scripts\Open-UnityProject.ps1 -Restart` 실행 결과 `CargoRunMvp scene opened.` 로그를 확인했다.
- 문서 반영:
  - `AGENTS.md`에 Unity 에디터 실행 규칙을 추가했다.
  - `docs/HARNESS.md`에 Unity Editor Launch 절차를 추가했다.

## 2026-06-09 복도 조인트 외부 노출 차단 추가 수정

- 사용자 재검토에서 방-복도 이음새뿐 아니라 `Engine Room -> Control Room`처럼 방 바깥에서 꺾이는 복도 조인트에 큰 구멍이 남는 문제가 확인되었다.
- 원인:
  - 이전 수정은 각 복도 시작/끝 threshold를 막는 데 집중했다.
  - `Engine Room -> Control Room` 전면 우회 복도는 중간에 90도 회전하는 route point가 있으며, 이 조인트의 바깥쪽 코너에는 별도 폐쇄 벽과 천장 캡이 없었다.
- 수정:
  - 모든 중간 corridor route point에 `Joint Seal` 생성 단계를 추가했다.
  - 조인트마다 `Ceiling Cap`을 덮고, 실제로 열려 있어야 하는 두 방향을 제외한 바깥쪽 방향에는 `Closure Wall`을 생성한다.
  - 특히 `Corridor - Engine Room to Control Room Joint 1/2`는 각 조인트에 최소 2개 이상의 closure wall과 천장 캡을 가진다.
- 검증 강화:
  - `Phase4CargoShipGrayboxEditorValidation`이 중간 조인트별 ceiling cap 크기와 closure wall 개수를 확인한다.
  - `PostDetailedStage2ShipInteriorEditorValidation`이 `Engine Room -> Control Room` 조인트 1/2의 실링 오브젝트를 직접 확인한다.
- 검증 결과:
  - `.\scripts\Open-UnityProject.ps1 -Restart -ValidateCargoRunScene`: passed
  - `.\scripts\Run-Phase4CargoShipGrayboxSmoke.ps1`: passed
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed

## 2026-06-09 2단계 복도 구조 재수정 최종 기록

- 사용자 검토에서 확인된 통제실-무기고 우회 복도는 원본 연결 정의에 없는 임의 연결이므로 제거했다.
- 최종 생성 허용 복도는 9개로 고정한다. `Control Room -> Armory`, `Control Room -> Supply Room`, `Supply Room -> Control Room` 복도 오브젝트는 생성하지 않는다.
- 통제실에서 무기고로 직접 이어지는 물리 복도는 없다. 필요 이동은 정의된 물리 복도인 통제실-화물칸, 화물칸-무기고 경로를 통해 이뤄져야 한다.
- HUD 맵에서도 `Phase 16 Map Corridor - ControlRoom to Armory` 직통 선을 제거했다. 물리 복도와 UI 표시가 같은 연결 정의를 따라야 한다.
- 화물칸-무기고와 화물칸-비품실은 각각 2-segment 짧은 램프 경로로 분리했고, 비품실-무기고는 사용자가 승인한 정의 연결로만 유지한다.
- 복도 바닥과 landing에는 실제 collider를 유지해 구멍이나 추락 지점이 생기지 않게 검증한다.
- 검증 결과:
  - `.\scripts\Run-Phase4CargoShipGrayboxSmoke.ps1`: passed
    - `Rooms=6; Corridors=9; ArmoryCargoRoute=7.80; ControlCargoRoute=10.68; SupplyCargoRoute=7.80; SupplyArmoryRoute=19.56`
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
    - `Rooms=6; CorridorWidth=2.6; ArmoryCargoSegments=2; SupplyCargoSegments=2; SupplyArmorySegments=1; ClosedShell=True; RuntimeIntegration=True`

## 2026-06-09 복도 바닥/벽 재구성 최종 수정

- 사용자 플레이 검토에서 복도 중간 구멍, 막힘, 비스듬히 통로를 침범하는 벽/천장 문제가 확인되었다.
- 원인은 경사 복도에서 바닥, 벽, 천장, threshold frame을 같은 pitch 회전으로 생성해 벽과 천장이 통로 내부로 기울어지는 구조였다.
- 수정 기준:
  - 바닥은 경사를 따라가는 연속 ramp floor collider로 생성한다.
  - 벽, 천장, threshold frame은 경사 pitch를 쓰지 않고 수직/수평 기준으로 생성한다.
  - 화물칸-무기고와 화물칸-비품실은 중간 꺾임 없는 1-segment 직선 ramp로 만든다.
  - endpoint landing은 시각 덮개만 두고 collider를 제거해 ramp 끝에 턱이 생기지 않게 한다.
  - 중간 꺾임이 필요한 엔진룸-통제실 북쪽 우회 복도는 실제 북쪽 문 opening과 endpoint를 일치시킨다.
- 검증 강화:
  - `Phase4CargoShipGrayboxEditorValidation`이 복도 벽/천장 pitch 침범과 centerline 시야 침범을 검사한다.
  - `Phase4CargoShipGrayboxPlayModeSmoke`가 9개 정의 복도를 모두 `CharacterController`로 통과 검증한다.
- 검증 결과:
  - `.\scripts\Run-Phase4CargoShipGrayboxSmoke.ps1`: passed
    - `Corridors=9; ValidatedRoutes=9; ArmoryCargoRoute=7.80; ControlCargoRoute=10.32; SupplyCargoRoute=7.80; SupplyArmoryRoute=19.56`
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
    - `Rooms=6; CorridorWidth=2.6; ArmoryCargoSegments=1; SupplyCargoSegments=1; SupplyArmorySegments=1; ClosedShell=True; RuntimeIntegration=True`

## 2026-06-09 복도 접합부 구멍 및 천장 틈 수정

- 사용자 플레이 검토에서 방-복도 이음새의 검은 구멍과 복도 벽-천장 사이 틈이 확인되었다.
- 원인은 방 wall opening이 문 위쪽 header wall 없이 전체 높이로 비워져 있었고, 복도 ceiling bottom이 corridor wall top보다 높게 배치되어 있었기 때문이다.
- 수정 내용:
  - 모든 방 door opening 위에 `Door Header Wall - ...` bulkhead를 생성해 문 위쪽 외부 노출을 막았다.
  - 각 복도 시작/끝 threshold에 visual-only floor lip, left/right reveal wall, ceiling cap을 추가해 접합부 측면과 상단 틈을 덮었다.
  - 복도 ceiling bottom이 corridor wall top에 직접 닿도록 높이 계산을 변경했다.
  - 새 검증에서 corridor ceiling과 wall 사이 gap이 있으면 실패하도록 했다.
  - Stage2 검증에서 주요 door header wall과 threshold seal 존재를 확인하도록 했다.
- 검증 결과:
  - `.\scripts\Run-Phase4CargoShipGrayboxSmoke.ps1`: passed
    - `Corridors=9; ValidatedRoutes=9`
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
    - `Rooms=6; CorridorWidth=2.6; ArmoryCargoSegments=1; SupplyCargoSegments=1; SupplyArmorySegments=1; ClosedShell=True; RuntimeIntegration=True`

## 제작 로드맵 2단계 검토 피드백 추가 수정 완료

- 1차 수정 뒤 실제 PlayMode 이동 검증을 추가하면서 남은 통행 문제를 다시 확인했다.
- 최종 수정 내용:
  - `Cargo Hold -> Armory`는 화물창고 남서쪽에서 외곽으로 빠지는 4-segment route로 유지해 통제실/화물창고 경로와 겹치지 않게 했다.
  - `Control Room -> Armory`는 더 남쪽 외곽의 4-segment perimeter route로 이동해 `Cargo Hold -> Supply Room` 하부 램프와도 충분히 떨어지게 했다.
  - `Cargo Hold -> Supply Room`은 비품실에서 먼저 남쪽 하부 램프로 내려간 뒤 화물창고로 이어지는 2-segment route로 바꿨다.
  - `Supply Room -> Armory` 문과 endpoint를 비품실 북쪽으로 분리해 비품실-화물창고 램프 위를 막지 않게 했다.
  - 복도 landing은 시각 접합부로 유지하고, 경사 복도 바닥/확장 floor collider가 실제 발판 역할을 맡게 했다.
  - 경사 복도 벽은 시각 벽과 물리 가드 콜라이더를 분리했다. 단, `Cargo Hold -> Supply Room` 하부 램프는 좁은 코너 통행을 막지 않도록 별도 side guard collider를 두지 않고 시각 벽만 유지한다.
- 검증 강화:
  - `Phase4CargoShipGrayboxPlayModeSmoke`가 실제 `CharacterController`로 `Armory -> Cargo Hold`, `Control Room -> Cargo Hold`, `Control Room -> Armory`, `Supply Room -> Cargo Hold` 경로 이동을 검증한다.
  - 경사 구간 검증은 평면 도착뿐 아니라 route point 높이까지 내려왔는지 확인한다.
- 최종 검증 결과:
  - `.\scripts\Run-Phase4CargoShipGrayboxSmoke.ps1`: passed
    - `ArmoryCargoRoute=28.20; ControlCargoRoute=9.96; ControlArmoryRoute=87.84; SupplyCargoRoute=17.76`
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
    - `Rooms=6; CorridorWidth=2.6; ArmoryCargoSegments=4; SupplyCargoSegments=2; ControlArmorySegments=4; ClosedShell=True; RuntimeIntegration=True`
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed
- 주의 사항:
  - 열린 Unity Editor가 새 bridge command를 즉시 reload하지 않아 기존 editor process를 정상 종료한 뒤 batchmode 검증으로 재실행했다.
  - `Builds/`, `Logs/`, `TestResults/`는 검증 생성물이다.
  - 2단계는 이제 적용까지 완료되었으며, 다음 진행 전 사용자가 실제 Unity 씬을 검토해야 한다.

## 제작 로드맵 2단계 검토 피드백 수정

- 사용자가 플레이 검토 중 2단계 선박 내부 연결 문제를 확인했다:
  - 통제실에서 무기고로 가는 복도와 통제실에서 화물창고로 가는 복도가 겹쳐 통행할 수 없음.
  - 비품실에서 화물창고로 가는 복도 통행 불가.
  - 구역과 복도가 연결된 부분이 부자연스러워 잘못하면 떨어질 수 있음.
- 수정 내용:
  - `Control Room -> Armory` route를 `Control Room -> Cargo Hold` route와 분리해 동쪽/남쪽 외곽을 도는 4-segment perimeter route로 변경했다.
  - `Cargo Hold -> Supply Room` route를 별도 3-segment ramp route로 재배치했다.
  - `Control Room` armory 출입구를 동쪽 벽으로 옮기고, `Armory` control 출입구를 남쪽 벽으로 옮겼다.
  - `Supply Room` cargo 출입구를 남서쪽으로 옮겨 cargo route와 armory route가 같은 입구를 쓰지 않게 했다.
  - 모든 corridor route point에 landing plate를 추가해 방-복도 접합부와 꺾임 지점에서 발판이 끊기지 않게 했다.
- 검증 강화:
  - `PostDetailedStage2ShipInteriorEditorValidation`이 `Cargo Hold -> Supply Room` 3-segment route, `Control Room -> Armory` 4-segment route, 주요 landing/door frame 존재, route 간 최소 평면 거리까지 확인한다.
- 검증 결과:
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
    - `Rooms=6; CorridorWidth=2.6; ArmoryCargoSegments=3; SupplyCargoSegments=3; ControlArmorySegments=4; ClosedShell=True; RuntimeIntegration=True`
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed

## 2026-06-09 복도 외부 노출 구멍 최신 기준

- 이전 `Control Room -> Armory` 물리 복도 기록은 폐기한다. 원본 기획 및 사용자 확인에 없는 복도이므로 생성하지 않는다.
- 최종 허용 복도는 9개다: Cargo Hold에서 Cockpit/Engine Room/Control Room/Armory/Supply Room, Supply Room에서 Armory, Cockpit에서 Engine Room/Control Room, Engine Room에서 Control Room.
- 방 문 개구부가 복도보다 넓어 남던 좌우 틈은 threshold `Left/Right Mouth Closure Wall`로 막는다.
- 낮은 Cargo Hold에서 높은 데크로 올라가는 경사 복도 위쪽 틈은 threshold `Upper Bulkhead Wall`과 넓어진 `Ceiling Cap`으로 막는다.
- `Floor Lip`은 시각용으로만 사용한다. collider를 넣으면 경사 램프 끝에서 캐릭터 이동을 막는다.
- `Engine Room -> Control Room` 전면 우회 복도는 정의된 기존 복도로 유지하되, 코너 내부 벽 길이를 줄여 CharacterController가 막히지 않게 한다.
- 최신 확인:
  - `.\scripts\Run-Phase4CargoShipGrayboxSmoke.ps1`: passed
    - `Corridors=9; ValidatedRoutes=9; ArmoryCargoRoute=7.77; ControlCargoRoute=10.29; SupplyCargoRoute=7.77; SupplyArmoryRoute=19.56`
  - `.\scripts\Run-PostDetailedStage2ShipInteriorSmoke.ps1`: passed
    - `Rooms=6; CorridorWidth=2.6; ArmoryCargoSegments=1; SupplyCargoSegments=1; SupplyArmorySegments=1; ClosedShell=True; RuntimeIntegration=True`
