# 진행 기록 — 2026-08-04

## 쿠르사 승인 정면 샘플 Unity 적용

상태: `SUPERSEDED_BY_UNITY_VISUAL_FRONT_CORRECTION`

### 승인 기준

- 사용자 승인 샘플:
  `artSample/enemies/kursa/front_alignment_2026-08-03/blender/Kursa_FrontAlignment_Sample.blend`
- 적용 대상: `CargoRunMvp.unity`의 `Approved Kursa Enemy Placement` 아래 기존 쿠르사 12개
- 샘플의 기존 메시, 토폴로지, 스킨 가중치는 유지한다.
- 얼굴은 샘플에 기록된 `Head` 시각 보정 `22°`를 사용한다.
- 눈은 새 오브젝트나 새 메시가 아니라 기존 얼굴 표면과 기존 좌우 투영 채널을 사용한다.

### 원인 확인

- 기존 Unity 정렬 도구는 실제 얼굴 프레임을 모델 로컬 `+Z`에 오차 `0°`가 되도록 맞췄다.
- 기존 얼굴 셰이더는 승인 Blender 샘플의 표면 투영 대신 반지름 `0.0045`의 카메라 정면 원형 렌즈를 지오메트리 패스로 생성했다.
- 두 방식 모두 사용자 승인 샘플의 `22°` 시각 오프셋과 좌우 눈 표면 투영 `3.45 / 3.95`를 그대로 적용한 상태가 아니었다.

### 변경 내용

- `Assets/_Project/Editor/Validation/KursaForwardHeadAlignmentTool.cs`
  - 모델 로컬 정면 자체가 아니라 승인 샘플의 정확한 `22°` 시각 오프셋을 목표 프레임으로 사용하게 변경했다.
  - 같은 목표 프레임을 기존 12개 정적 자세와 `Kursa_02_GroundedIdle.anim`, `Kursa_03_Move_InPlace.anim`의 `Head` 회전 곡선에 적용했다.
  - 승인 샘플 적용 전용 보고서와 화면 캡처 경로를 2026-08-04 경로로 분리했다.
- `Assets/_Project/Art/Enemies/Kursa/ApprovedAppearance/Shaders/KursaApprovedAppearance.shader`
  - 별도 원형 렌즈 지오메트리 생성을 제거했다.
  - 기존 FBX의 `UV1/UV2/UV3` 좌우 눈 투영 채널을 사용해 승인 샘플의 최종 표면 투영으로 재매핑했다.
  - 승인 샘플의 좌우 중심 높이, 크기 `3.45 / 3.95`, 깊이 `2.05`를 그대로 반영했다.
- `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `Approved Kursa Enemy Placement` 아래 기존 쿠르사 12개의 `Head` 자세만 저장했다.
- 기존 FBX, 버텍스 좌표, 토폴로지, 스킨 가중치, 본 계층, 머티리얼 자산, 눈 텍스처는 변경하지 않았다.
- 별도 눈 오브젝트, 새 메시, 새 머티리얼, 새 셰이더, 백업본은 만들지 않았다.

### 적용 및 직접 화면 확인

- Unity 새로고침 후 전용 명령 `ApplyKursaForwardHeadAlignment`를 실행했다.
- 전용 리뷰 화면에서 다음을 직접 확인했다.
  - 기존 쿠르사 12개가 동일한 승인 샘플 얼굴 방향을 사용한다.
  - 정면에서 얼굴 중심과 턱이 승인 샘플의 시각 정면에 놓인다.
  - 좌우 눈이 기존 얼굴 표면에 나타나며 별도 오브젝트처럼 분리되지 않는다.
  - Idle 중간과 Move 1/4, 1/2, 3/4 표본에서 얼굴 방향과 눈 부착이 유지된다.
  - 좌우 `25°` 화면에서 눈 투영이 얼굴 표면을 따라간다.
- 최종 화면:
  `docs/validation/kursa_approved_front_sample_sync_2026-08-04/Kursa_ApprovedFrontSample_FinalReview.png`
- 정면 프레임 점검 기록:
  `docs/validation/kursa_approved_front_sample_sync_2026-08-04/Kursa_ApprovedFrontSample_Inspection.txt`
- 눈 투영 점검 기록:
  `docs/validation/kursa_approved_front_sample_sync_2026-08-04/Kursa_ApprovedEyeProjection_Inspection.txt`
- 첫 번째 실패 캡처와 보고서는 삭제했고 최종 결과만 남겼다.

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1`: 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`: 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- Git 명령과 커밋은 실행하지 않았다.
- 다른 씬 루트, 다른 적 모델, 런타임 게임 로직은 변경하지 않았다.

## 쿠르사 Unity 실제 정면 보정

상태: `COMPLETE`

### 사용자 확인과 원인

- 사용자가 이전 Unity 결과가 정면이 아니라 오른쪽 사선을 보고 있다고 확인했다.
- 이전 적용은 Blender 샘플에 기록된 `22°` 값을 Unity 얼굴 목표에도 그대로 남겼다.
- 이전 Unity 리뷰의 정면 카메라에서는 얼굴이 오른쪽 사선이었고, 카메라를 같은 방향으로 약 `25°` 옮긴 패널에서만 얼굴 프레임과 카메라가 맞았다.
- 따라서 Unity의 실제 정면 목표에는 별도 시각 오프셋을 두지 않는 것이 맞으며, 이전 `22°` Unity 적용은 최종 상태에서 제외했다.

### 수정 내용

- `Assets/_Project/Editor/Validation/KursaForwardHeadAlignmentTool.cs`
  - `UnityVisualFrontYawOffsetDegrees`를 `0°`로 고정했다.
  - 실제 양눈 중심과 머리 위쪽으로 복원한 얼굴 프레임을 모델 로컬 `+Z` 정면에 직접 맞추도록 했다.
  - 기존 쿠르사 12개와 `Kursa_02_GroundedIdle.anim`, `Kursa_03_Move_InPlace.anim`의 `Head` 회전 곡선에 같은 정면 목표를 적용했다.
- `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `Approved Kursa Enemy Placement` 아래 쿠르사 12개의 정면 `Head` 자세를 저장했다.
- 이번 보정에서는 눈 투영 셰이더, 머티리얼, FBX, 메시, UV, 스킨 가중치, 본 계층을 변경하지 않았다.

### 직접 화면 확인

- 최종 리뷰의 정면 패널을 직접 확인했다.
  - 코와 턱 끝이 양눈 사이 중앙에 놓인다.
  - 얼굴 좌우 측면 노출이 한쪽으로 치우치지 않는다.
  - 쿠르사 12개가 같은 실제 정면을 유지한다.
  - Idle 중간과 Move 1/4, 1/2, 3/4 표본에서도 정면 방향이 유지된다.
  - 좌우 `25°` 패널은 서로 반대 방향의 사선으로 나타나며 정면 자세가 특정 사선에 고정되지 않는다.
- 최종 화면:
  `docs/validation/kursa_visual_front_correction_2026-08-04/Kursa_VisualFront_FinalReview.png`
- 정면 프레임 기록:
  `docs/validation/kursa_visual_front_correction_2026-08-04/Kursa_VisualFront_Inspection.txt`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1`: 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`: 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- 메시·눈 투영·셰이더·머티리얼 수정은 실행하지 않았다.
- 백업본 생성, 다른 씬 루트 수정, Git 명령과 커밋은 실행하지 않았다.

## 쿠르사 눈 투영 휨 수정

상태: `SUPERSEDED_BY_2026_08_03_FINAL_EYE_IMPLEMENTATION_RESTORE`

### 사용자 확인과 원인

- 사용자가 Unity 정면 화면에서 좌우 눈이 얼굴 홈을 따라 휘어 보이는 문제를 확인했다.
- 별도 눈 오브젝트는 존재하지 않았으며, 기존 얼굴 셰이더의 표면 투영이었다.
- 기존 셰이더는 제곱항을 포함한 눈 좌표 변환을 프래그먼트마다 계산해 삼각형 내부의 직선 투영을 곡선으로 만들었다.
- 또한 투영 계수는 폐기된 `+22°` 얼굴 자세에서 계산된 값이어서 현재 Unity 정면 `0°` 자세와 일치하지 않았다.

### 수정 내용

- `Assets/_Project/Art/Enemies/Kursa/ApprovedAppearance/Shaders/KursaApprovedAppearance.shader`
  - 눈 좌표 변환을 프래그먼트 단계에서 버텍스 단계로 옮겼다.
  - 각 삼각형 내부에서는 보정된 눈 좌표가 직선으로 보간되도록 했다.
  - 승인 Blender 샘플을 메모리에서 정확히 `-22°` 되돌려 Unity 정면 `0°` 자세로 평가하고, 그 자세의 기존 좌우 투영 채널 대응값으로 계수를 교체했다.
- `Assets/_Project/Editor/Validation/KursaForwardHeadAlignmentTool.cs`
  - 이번 눈 휨 수정의 보고서와 최종 화면을 새 2026-08-04 경로로 분리했다.
- 메시, UV 채널 데이터, 스킨 가중치, 눈 텍스처, 머티리얼, `Head` 자세와 애니메이션 곡선은 변경하지 않았다.
- 새 눈 오브젝트나 새 메시를 만들지 않았다.

### 직접 화면 확인

- 첫 리뷰에서는 버텍스 단계 변경만으로 화면 왼쪽 눈의 휨이 남아 있어 불합격으로 판정하고 해당 캡처와 보고서를 삭제했다.
- 정면 `0°`용 투영값을 반영한 두 번째 리뷰를 직접 확인했다.
  - 쿠르사 12개 정면에서 눈 윤곽이 위아래로 휘지 않고 수평 방향으로 정리됐다.
  - 좌우 눈이 얼굴 표면에 붙어 있으며 별도 오브젝트처럼 분리되지 않는다.
  - Idle 중간과 Move 1/4, 1/2, 3/4 표본에서도 같은 형태를 유지한다.
  - 좌우 `25°` 패널에서는 시점에 따른 정상적인 원근 변화만 나타난다.
- 최종 화면:
  `docs/validation/kursa_eye_unwarp_2026-08-04/Kursa_EyeUnwarp_FinalReview.png`
- 눈 투영 기록:
  `docs/validation/kursa_eye_unwarp_2026-08-04/Kursa_EyeUnwarp_Inspection.txt`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1`: 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`: 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- 메시·UV 데이터·스킨 가중치·눈 텍스처·머티리얼 수정은 실행하지 않았다.
- 새 눈 오브젝트·새 메시·백업본 생성, 다른 씬 루트 수정, Git 명령과 커밋은 실행하지 않았다.

## 2026-08-03 최종 눈 구현 복원

상태: `COMPLETE`

### 적용 기준과 원인

- 사용자가 임의 복구가 아니라 전날 진행문서에서 눈 구현이 끝난 부분을 찾아 그대로 구현하라고 지시했다.
- `docs/PROGRESS_2026-08-03.md`의 최종 완료 항목 `image 원본 기준 쿠르사 원형 눈 최종 교정`과 상태 `IMAGE_REFERENCE_ROUND_EYES_DIRECT_VISUAL_REVIEW_PASSED`를 적용 기준으로 삼았다.
- 직전 `쿠르사 눈 투영 휨 수정`은 전날 최종 구현이 아닌 다항식·버텍스·표면 투영 방식을 새로 적용한 것이므로 최종 상태에서 제외했다.
- 시각 기준은 승인 아트 샘플이 아니라 `image/KUŠkursa(쿠르사).png`이다.

### 수정 내용

- `Assets/_Project/Art/Enemies/Kursa/ApprovedAppearance/Shaders/KursaApprovedAppearance.shader`
  - 직전에 추가한 다항식·버텍스·표면 기반 눈 투영을 모두 제거했다.
  - 기존 스킨드 얼굴 메시의 `uv2/uv3/uv4` 중심과 깊이 채널을 그대로 사용한다.
  - 각 눈마다 UV 중심 `(0.5, 0.5)`을 포함하며 절대 깊이가 `0.001`보다 작은 삼각형의 스킨드 월드 위치를 앵커로 사용한다.
  - 앵커에서 반지름 `0.0045`의 카메라 정면 원형 렌즈를 얼굴 Forward 패스 안에서 출력하는 전날 최종 구현을 복원했다.
- `Assets/_Project/Editor/Validation/KursaForwardHeadAlignmentTool.cs`
  - 눈 점검 기록과 최종 화면의 출력 경로 및 계약 문구를 전날 최종 구현 기준으로 되돌렸다.
- 직전 실패 결과인 `docs/validation/kursa_eye_unwarp_2026-08-04`의 보고서와 캡처는 백업 없이 삭제했다.
- 현재 승인된 Unity 얼굴 정면 `0°`는 유지했으며, 메시 버텍스·토폴로지·UV 채널 데이터·스킨 가중치·본·애니메이션·머티리얼·눈 텍스처는 변경하지 않았다.
- 별도 눈 오브젝트나 새 메시를 만들지 않았다.

### Unity 적용 및 직접 화면 확인

- `scripts/Refresh-UnityProject.ps1`로 Unity 자산을 갱신한 뒤 `CaptureKursaEyeShapeReview`를 실행했다.
- 새 최종 화면과 전날 최종 화면 `docs/validation/kursa_image_reference_eye_correction_2026-08-03/Kursa_ImageReferenceEye_FinalReview.png`을 직접 나란히 확인했다.
- 쿠르사 12개 정면에서 좌우 눈이 같은 크기의 작은 원형으로 보이며 얼굴 홈을 따라 휘거나 길게 찌그러지지 않는다.
- Idle 중간, Move 1/4·1/2·3/4와 좌우 `25°` 표본에서도 원형 렌즈 형태가 유지된다.
- 전날 승인본과 눈의 반지름·색·원형 표현이 같고, 이후 승인된 얼굴 정면 `0°` 상태만 유지된다.
- 최종 화면:
  `docs/validation/kursa_image_reference_eye_restore_2026-08-04/Kursa_ImageReferenceEye_FinalReview.png`
- 눈 구현 점검 기록:
  `docs/validation/kursa_image_reference_eye_restore_2026-08-04/Kursa_ImageReferenceEye_Inspection.txt`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1`: 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`: 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- 메시·UV 데이터·스킨 가중치·본·애니메이션·머티리얼·눈 텍스처 수정은 실행하지 않았다.
- 새 눈 오브젝트·새 메시·백업본 생성과 다른 씬 루트 수정은 실행하지 않았다.
- 파일 변경 확인 중 읽기 전용 `git diff`가 1회 실행됐으나 Git 상태·이력·파일을 변경하는 명령과 커밋은 실행하지 않았다.

## 쿠르사 작업 최종 GitHub 인계 범위

### 기록 대상

- 2026-08-03~2026-08-04에 진행한 쿠르사 승인 외형, 정적 자세, 대기·이동 애니메이션, 얼굴 정면 `0°` 정렬, 턱 중앙 교정과 전날 최종 원형 눈 구현 복원을 하나의 재현 가능한 변경 묶음으로 인계한다.
- Unity 대상은 `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Approved Kursa Enemy Placement` 아래 기존 쿠르사 12개이다.
- 구현 자산은 `Assets/_Project/Art/Enemies/Kursa/`, 쿠르사 적용·검사 도구, Unity 브리지의 쿠르사 명령, 쿠르사 제작·분석 스크립트와 사용자 지정 보행 원본을 포함한다.
- 승인 샘플 `artSample/enemies/kursa/front_alignment_2026-08-03/`, 계획·진행문서와 `docs/validation/kursa_*`의 관련 기술 기록·직접 확인 이미지를 포함한다.
- 최신 최종 상태는 Unity 실제 정면 `0°`와 `docs/PROGRESS_2026-08-03.md`의 `IMAGE_REFERENCE_ROUND_EYES_DIRECT_VISUAL_REVIEW_PASSED` 원형 눈 구현을 함께 유지한 상태이다.

### 커밋 및 푸시 범위

- 대상 브랜치: `main`
- 원격 저장소: `https://github.com/goldmembrane/bellerophon-s-envelop.git`
- 커밋 메시지: `fix: restore approved Kursa face and eye alignment`
- 기준 원본 이미지의 별도 작업 트리 변경, 다른 적대 개체 파일, `__pycache__`, Blender 자동 백업 파일은 이번 쿠르사 커밋에 포함하지 않는다.
- 강제 푸시, 리베이스, 기존 이력 수정은 수행하지 않는다.

### 실행하지 않은 검증

- `Run-HarnessValidation.ps1` 하네스 검증은 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`은 사용자가 명령명과 대상 범위를 직접 지정하지 않아 실행하지 않았다.
- Unity 추가 적용·검사·캡처는 실행하지 않고, 앞선 단계에서 완료한 직접 육안 확인 결과를 최종 시각 기록으로 사용했다.

## 쿠르사 이동 모션 오른팔 겹침 수정

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 사용자 확인과 원인

- 사용자가 `Kursa_03_Move` 이동 모션 중 오른팔이 몸통과 오른쪽 허벅지 쪽에
  겹쳐 보이는 현상을 확인했다.
- 현재 `KursaMoveAnimationTool.CreateInPlaceClip()`은 원본 보행을 굽는 과정에서
  과거 진행문서에 기록된 `RightArm` 외향 보정을 적용하지 않고 있었다.
- 과거 수치 교차 판정은 이번 완료 근거에서 제외하고 실제 Unity 화면을 직접 보는
  방식으로 수정 결과를 판단했다.

### 수정 내용

- `Assets/_Project/Editor/Validation/KursaMoveAnimationTool.cs`
  - 이동 클립을 굽는 각 프레임에서 좌우 어깨로 캐릭터 바깥 방향을 복원하고
    `RightArm` 상박을 그 방향으로 벌리도록 수정했다.
  - 보정은 `Kursa_03_Move_InPlace.anim`에만 적용하며 `RightForeArm`과 `RightHand`의
    기존 로컬 보행 움직임은 유지한다.
  - 전용 `ApplyKursaMoveRightArmClearance` 명령은 현재 이동 클립만 다시 생성하며
    씬을 저장하거나 이동 개체를 교체하지 않는다.
  - 오른팔·몸통·오른쪽 허벅지를 보행 주기 전체에 걸쳐 직접 볼 수 있도록
    20프레임 근접 접촉 시트 캡처를 추가했다.
- `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - 전용 적용, 진단 캡처, 최종 캡처 명령을 연결했다.
- `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_03_Move_InPlace.anim`
  - 전용 적용 명령으로 오른팔 보정이 포함된 이동 클립을 다시 생성했다.
- `CargoRunMvp.unity`, 원본 walking FBX, 메시, 스킨 가중치, 머티리얼,
  정적·대기 쿠르사와 다른 적대 개체는 변경하지 않았다.

### 직접 화면 확인

- 첫 진단 접촉 시트의 20개 보행 프레임을 직접 확인했다.
- 모든 프레임에서 오른팔과 몸통·오른쪽 허벅지 사이에 배경 틈이 보이며
  실루엣이 서로 겹치지 않는다.
- 어깨가 비정상적으로 꺾이거나 팔이 과도하게 벌어지지 않았고 원본 보행의
  전완·손 흔들림도 유지된다.
- 첫 진단에서 직접 시각 확인을 통과해 추가 진단 조정은 실행하지 않았다.
- 진단 이미지:
  `docs/validation/kursa_move_right_arm_clearance_2026-08-04/Kursa_MoveRightArm_Diagnostic_01.png`
- 최종 이미지:
  `docs/validation/kursa_move_right_arm_clearance_2026-08-04/Kursa_MoveRightArm_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `InspectKursaMoveAnimation` 수치 검사와 수치 기반 완료 판정
- 기존 `ApplyKursaMoveAnimation` 전체 이동 개체 교체 명령
- `CargoRunMvp.unity` 저장 및 다른 쿠르사 슬롯 수정
- 새 `artSample/` 생성
- Git 작업

## 쿠르사 이동 모션 순간 얼굴 회전 수정

상태: `FULL_LOOP_DIRECT_VISUAL_REVIEW_PASSED`

### 제공 영상 분석과 원인

- 제공 영상:
  `C:/Users/gus68/Videos/Captures/Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-08-04 10-12-15.mp4`
- 원본 영상 프레임을 직접 비교한 결과 보행 루프의 같은 지점마다 정상 얼굴이
  한두 프레임 동안 옆으로 크게 돌아가 얼굴판이 변형된 것처럼 보인 뒤 복원됐다.
- 눈만 달라지는 셰이더 현상이 아니라 두건과 얼굴판을 포함한 `Head` 전체가
  순간 회전하는 현상이었다.
- `KursaMoveAnimationTool.CreateInPlaceClip()`은 이동 클립을 굽는 모든 프레임에서
  `AlignHeadToModelLocalForward()`를 다시 호출했다.
- 이 정렬은 스킨된 얼굴의 눈 UV 중심을 포함하는 여러 삼각형 중 매 프레임
  `Head`에 가장 가까운 표면을 다시 선택한다. 보행 변형 중 선택 표면이 바뀌면서
  시각 얼굴 기준이 불연속적으로 교체됐고, 그 결과 이동 클립의 `Head` 곡선에
  순간적인 옆 회전이 기록됐다.
- 영상 분석 자료:
  `docs/validation/kursa_move_face_deformation_2026-08-04/source_video_analysis/`

### 수정 내용

- `Assets/_Project/Editor/Validation/KursaMoveAnimationTool.cs`
  - 원본 보행의 첫 정상 프레임에서 승인된 얼굴 정면 보정을 한 번만 계산한다.
  - 이후 모든 프레임에는 같은 로컬 보정을 원본 `Head` 움직임에 적용한다.
  - 매 프레임 눈 표면 후보를 다시 선택하지 않으므로 후보 교체에 따른 순간 회전이
    이동 클립에 들어가지 않는다.
  - 원본의 자연스러운 `Head` 움직임과 앞서 승인된 오른팔 외향 보정은 유지한다.
  - 이동 얼굴 전체 `117`프레임과 오른팔 대표 프레임을 한 이미지에서 직접 볼 수 있는
    전용 접촉 시트 캡처를 추가했다.
- `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `ApplyKursaMoveFaceDeformationFix`
  - `CaptureKursaMoveFaceDeformationDiagnostic`
  - `CaptureKursaMoveFaceDeformationFinalReview`
    전용 명령을 연결했다.
- `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_03_Move_InPlace.anim`
  - 전용 적용 명령으로 안정화된 `Head` 곡선과 기존 오른팔 보정을 함께 다시 생성했다.
- `CargoRunMvp.unity`, 원본 walking FBX, 메시, 스킨 가중치, 얼굴 셰이더,
  눈 구현, 정적·대기 쿠르사와 다른 적대 개체는 변경하지 않았다.

### 직접 화면 확인

- 첫 Unity 진단 접촉 시트에서 이동 클립의 얼굴 `117`프레임을 순서대로 직접 확인했다.
- 제공 영상에서 보였던 단발성 옆 회전과 얼굴판 번쩍임이 모든 프레임에서 사라졌다.
- 얼굴 방향, 작은 원형 눈과 턱 형태가 끊김 없이 이어지고, 머리는 원본 보행을 따라
  자연스럽게 미세하게 움직여 고정 부품처럼 보이지 않는다.
- 같은 이미지 하단의 오른팔 대표 프레임에서 오른팔과 몸통·허벅지 사이의 틈도 유지됐다.
- 첫 진단에서 직접 시각 확인을 통과해 추가 진단 캡처는 실행하지 않았다.
- 진단 이미지:
  `docs/validation/kursa_move_face_deformation_2026-08-04/Kursa_MoveFaceStability_Diagnostic_01.png`
- 최종 이미지:
  `docs/validation/kursa_move_face_deformation_2026-08-04/Kursa_MoveFaceStability_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `InspectKursaMoveAnimation` 및 머리 정렬 수치 검사
- 수치 기반 완료 판정
- 얼굴 셰이더·눈 구현·원본 walking FBX·메시·스킨 가중치 수정
- `CargoRunMvp.unity` 저장 및 다른 쿠르사 슬롯 수정
- 새 `artSample/` 생성
- Git 작업

## 쿠르사 현재 정적 배치 리그 FBX 내보내기

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 내보내기 기준

- 대상은 현재 열린 `CargoRunMvp.unity`의
  `Approved Kursa Enemy Placement/Kursa_01_Static_Review/Kursa_Model`이다.
- 현재 정적 자세를 유지하고 리그, 스킨, 머티리얼 슬롯을 포함한다.
- 애니메이션은 FBX에 포함하지 않는다.
- 저장 경로:
  `enemies model/Kursa_Static.fbx`

### 변경 내용

- `Packages/manifest.json`, `Packages/packages-lock.json`
  - 공식 Unity FBX Exporter `com.unity.formats.fbx` `5.1.6`을 설치했고
    사용자 요청에 따라 프로젝트 의존성으로 유지했다.
- `Assets/_Project/Editor/Validation/KursaStaticFbxExportTool.cs`
  - 현재 정적 배치 개체를 저장되지 않는 임시 복제본으로 만든 뒤
    모델 전용 FBX로 내보내도록 구성했다.
  - 사용자 확인 뒤 최종 내보내기 경로를 저장소 루트의
    `enemies model/Kursa_Static.fbx`로 교정했다.
  - 잘못된 Unity 에셋 위치를 다시 만들지 않도록 재임포트 비교 명령은 제거했다.
- `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `InstallKursaFbxExporterDependency`
  - `ExportKursaStaticFbx`
    명령을 연결했다.
- `enemies model/Kursa_Static.fbx`
  - 현재 정적 자세, 스킨드 메시, 본 계층과 머티리얼 슬롯을 포함한다.
  - 애니메이션 클립은 포함하지 않는다.
- 처음 잘못 저장한
  `Assets/_Project/Art/Enemies/Kursa/Models/Kursa_Static.fbx`와 `.meta`는
  최종 위치 교정 과정에서 제거했다.

### 직접 화면 확인

- 첫 비교 이미지는 Preview Scene 렌더 대상이 화면에 나오지 않아 빈 화면으로
  확인됐고 완료 판정에서 제외했다.
- 캡처를 기존 프로젝트와 같은 저장되지 않는 숨김 임시 개체 방식으로 고친 뒤
  실패 이미지를 같은 경로의 교체 최종 이미지로 덮어썼다.
- 교체 이미지에서 왼쪽 원본과 오른쪽 재임포트 FBX를 직접 비교했다.
- 방패 위치, 정적 자세, 얼굴 방향, 오른팔 간격, 전신 실루엣과
  금속·방패 머티리얼 구분이 동일하게 보이는 것을 확인했다.
- 직접 확인을 마친 동일 FBX 파일을 내용 변경 없이
  `enemies model/Kursa_Static.fbx`로 이동했다.
- 최종 이미지:
  `docs/validation/kursa_static_fbx_2026-08-04/Kursa_StaticFbx_FinalReview.png`
- `CargoRunMvp.unity`는 저장하거나 변경하지 않았고 비교용 임시 개체는 제거됐다.

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 기존 쿠르사 원본 FBX, 메시, 리그, 스킨, 머티리얼 수정
- 애니메이션 포함 또는 수정
- `CargoRunMvp.unity` 저장 및 배치 개체 변경
- 다른 적대 개체와 씬 루트 변경
- Blender 변환
- Git 작업

## 쿠르사 방패 강타 모션 개체 교체

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 적용 내용

- 사용자 지정 원본
  `enemies model/KUŠkursa bash.fbx`를 내용 변경 없이
  `Assets/_Project/Art/Enemies/Kursa/Animations/Models/Kursa_ShieldBash_Source.fbx`로 복사해 Unity 소스로 사용했다.
- FBX에 내장된 이름에 `mixamo`가 포함된 단일 애니메이션 테이크를 직접 사용하고
  `Kursa_04_ShieldBash_Mixamo` 루프 클립으로 설정했다.
- `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_04_ShieldBash.controller`를 만들고
  해당 클립을 기본 상태로 연결해 반복 재생하도록 했다.
- `CargoRunMvp.unity`의
  `Approved Kursa Enemy Placement/Kursa_04_ShieldBash/Kursa_Model`만 교체했다.
  `Kursa_04_ShieldBash` 슬롯 루트와 다른 쿠르사 슬롯 및 다른 씬 루트는 유지했다.
- 정적 쿠르사와 FBX의 본 이름, 바인드 포즈, 루트 본이 정확히 대응하는지 먼저 확인했다.
  교체 개체에는 정적 쿠르사의 동일 `sharedMesh`, 본 배열, 루트 본,
  `sharedMaterials`, 렌더러 설정을 그대로 연결했다. 새 외형은 생성하거나 추측하지 않았다.
- 루트 모션은 사용하지 않고 Animator가 씬에서 계속 재생되도록 설정한 뒤 씬을 저장했다.
- `Assets/_Project/Editor/Validation/KursaShieldBashAnimationTool.cs`에 적용 및
  고정 카메라 직접 검토 캡처 도구를 추가했다.
- `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`에
  `ApplyKursaShieldBashAnimation`, `CaptureKursaShieldBashDiagnostic`,
  `CaptureKursaShieldBashFinalReview` 명령을 연결했다.

### 직접 화면 확인

- 첫 번째와 두 번째 진단 이미지는 프레임마다 카메라가 대상을 따라가 강타의 이동 흐름을
  명확하게 판단하기 어려워 완료 근거에서 제외했다.
- 세 번째 진단은 전체 재생 구간에 같은 카메라 구도를 유지하도록 고친 뒤 직접 확인했다.
- 방패를 전방에 세운 준비 자세, 몸을 실어 밀어내는 구간, 자세를 회수해 시작 상태로
  돌아오는 흐름이 이어지는 것을 확인했다.
- 전 구간에서 정적 쿠르사와 같은 얼굴, 갑옷, 방패 외형이 유지됐고
  메시 찢어짐이나 순간 변형은 보이지 않았다.
- 세 번째 진단 이미지:
  `docs/validation/kursa_shield_bash_2026-08-04/Kursa_ShieldBash_Diagnostic_03.png`
- 최종 이미지:
  `docs/validation/kursa_shield_bash_2026-08-04/Kursa_ShieldBash_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 수치만을 근거로 한 완료 판정
- `Kursa_04_ShieldBash` 이외 슬롯 및 다른 씬 루트 수정
- 공격 판정, 피해, 넉백, 물리 또는 AI 로직 구현
- 새 메시, 텍스처, 머티리얼 또는 외형 생성
- `artSample/` 생성
- Git 작업

## 쿠르사 방패 강타 재생 배율 교정

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 원인

- 정적·이동·방패 강타 모델의 저장된 슬롯, 모델, 렌더러 및 루트 본 배율은 서로 같았다.
- `KUŠkursa bash.fbx`의 내장 `mixamo` 클립에는 Animator가 붙은 모델 루트의
  `m_LocalScale`을 재생 중 `1`로 설정하는 곡선이 포함돼 있었다.
- 기존 구조에서는 Animator와 씬 배율을 가진 `Kursa_Model`이 같은 Transform이어서,
  재생이 시작되면 정적 쿠르사에서 가져온 씬 배율이 클립 값으로 덮어써지고
  방패 강타 개체만 커졌다.
- 이전 크기 비교 화면도 각 대상을 서로 다른 거리로 자동 프레이밍해 크기 차이를
  올바르게 비교할 수 없었다.

### 수정 내용

- `Kursa_04_ShieldBash/Kursa_Model`을 애니메이션에 영향받지 않는 배율 부모로 바꿨다.
- 기존 FBX 루트와 Animator는 바로 아래의
  `Kursa_ShieldBash_AnimatedRoot`로 이동했다.
- 부모 `Kursa_Model`은 `Kursa_01_Static_Review/Kursa_Model`의 배율을 그대로 사용한다.
  자식 애니메이션 루트는 로컬 위치 `0`, 회전 기본값, 배율 `1`을 사용하므로
  내장 클립의 루트 배율 곡선이 재생돼도 부모의 실제 화면 배율은 바뀌지 않는다.
- FBX 내장 `Kursa_04_ShieldBash_Mixamo` 클립과 반복 설정은 수정하지 않았고
  파생 `.anim`도 만들지 않았다.
- `KursaShieldBashAnimationTool.cs`의 신규 교체 경로도 같은 배율 부모 구조를
  만들도록 수정했다.
- 기존 불합격 크기 비교 이미지는 삭제하고 직접 확인을 통과한 최종 이미지로 교체했다.
- 다른 쿠르사 슬롯과 다른 씬 루트는 변경하지 않았으며 `CargoRunMvp.unity`를 저장했다.

### 직접 화면 확인

- 정적 쿠르사와 방패 강타 전 구간을 동일한 카메라 거리와 화면 배율로 직접 비교했다.
- 방패, 머리, 갑옷과 신체의 실제 크기가 일치하며 재생 시작 순간에도 개체가 커지지 않는다.
- 방패 강타 개체의 넓은 실루엣은 크기 차이가 아니라 다리를 벌리고 몸을 낮춘 공격 자세로
  확인됐다.
- 전체 구간에서 방패 강타와 자세 회수가 이어지고 얼굴·갑옷·방패 및 스킨이 깨지지 않았다.
- 진단 이미지:
  `docs/validation/kursa_shield_bash_2026-08-04/Kursa_ShieldBash_Size_Diagnostic_01.png`
- 최종 이미지:
  `docs/validation/kursa_shield_bash_2026-08-04/Kursa_ShieldBash_Size_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- FBX 원본 또는 내장 `mixamo` 애니메이션 수정
- 파생 `.anim` 생성
- 다른 쿠르사 슬롯 및 다른 씬 루트 수정
- 공격 판정, 물리 또는 AI 구현
- Git 작업

## 쿠르사 방패 태세 진입 모션 및 승인 이펙트 적용

상태: `SUPERSEDED_BY_LEG_STANCE_CORRECTION`

### 적용 기준

- 대상은 `CargoRunMvp.unity`의 `Kursa_05_ToShieldStance` 하나로 한정했다.
- 진입 시간은 1초, 완료 자세와 이펙트 유지 시간은 2초이며 전체 3초 뒤
  시작 자세로 즉시 돌아가 반복한다.
- 최종 자세는 왼 무릎을 굽히고 오른발을 뒤로 빼며, 왼팔과 실제 제압방패를
  몸통 앞쪽으로 내민다.
- 머리 위 이펙트는 사용자가 승인한
  `artSample/enemies/kursa/shield_stance_entry_effect_2026-08-04/`의
  수직 측면 5각형, 불투명 푸른 본체, 밝은 청록 테두리와 외곽 발광을 그대로 사용한다.
- `Kursa_06_ShieldStance`는 만들거나 수정하지 않았다.

### 수정 내용

- `Assets/_Project/Art/Enemies/Kursa/Effects/Kursa_ShieldStanceIcon_Source.svg`
  - 승인 시안에서 검토용 어두운 배경만 제외한 런타임 원본을 추가했다.
- `Assets/_Project/Art/Enemies/Kursa/Effects/Kursa_ShieldStanceIcon.png`
  - 같은 도형과 색·발광을 투명 배경 스프라이트로 변환했다.
- `Assets/_Project/Editor/Validation/KursaToShieldStanceAnimationTool.cs`
  - `Kursa_01_Static_Review` 모델을 직접 복제해 #5의 메시, 스킨, 본과 머티리얼을
    정적 쿠르사와 동일하게 유지한다.
  - 현재 리그의 실제 전방축을 기준으로 관절의 이동 방향을 판별해 왼 무릎 굽힘,
    오른발 후퇴, 왼 어깨·상박·전완 전진 자세를 작성한다.
  - `Kursa_05_ToShieldStance_Loop.anim`과 전용 Animator Controller를 생성하고,
    1초 완료 시 이펙트가 나타나 2초 동안 최종 자세와 함께 유지되도록 했다.
  - 정면과 사선에서 정적 기준, 시작, 중간, 완료, 유지 구간을 같은 화면 배율로
    직접 비교하는 전용 진단·최종 캡처를 추가했다.
- `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `ApplyKursaToShieldStanceAnimation`,
    `CaptureKursaToShieldStanceDiagnostic`,
    `CaptureKursaToShieldStanceFinalReview` 명령을 연결했다.
- `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `Kursa_05_ToShieldStance/Kursa_Model`만 정적 외형 복제 모델과 전용 Animator,
    머리 위 이펙트 구성으로 교체해 저장했다.

### 직접 화면 확인

- 정면과 사선의 전체 루프 접촉 시트를 직접 확인했다.
- 정적 기준과 진입 시작 프레임의 외형과 크기가 같고, 중간 프레임을 거쳐
  왼 무릎이 굽혀지며 오른발이 뒤로 빠지는 흐름이 보인다.
- 왼팔과 실제 방패가 몸통 앞쪽으로 이동하고, 완료 시점부터 승인된 푸른 방패
  이펙트가 머리 위에 나타난다.
- 완료 이후의 세 표본에서 자세와 이펙트가 같은 상태로 유지되며,
  시작 표본에서는 다시 정적 시작 자세와 숨겨진 이펙트 상태로 돌아간다.
- 진단 이미지:
  `docs/validation/kursa_to_shield_stance_2026-08-04/Kursa_ToShieldStance_Diagnostic_01.png`
- 최종 이미지:
  `docs/validation/kursa_to_shield_stance_2026-08-04/Kursa_ToShieldStance_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `Kursa_06_ShieldStance` 및 다른 쿠르사 슬롯 수정
- 공격 판정, 방패 내구도, 피해, 넉백, 물리 또는 AI 로직 구현
- 별도 테스트·빌드 명령
- Git 작업

## 쿠르사 방패 태세 진입 양발 자세 교정

상태: `SUPERSEDED_BY_TIPTOE_AND_SHIELD_EXTENSION`

### 사용자 확인과 원인

- 사용자가 기존 진입 모션에서 오른발과 왼발이 모두 위로 오므라드는 자세로
  보인다고 확인했다.
- 원하는 자세는 왼발을 조금 앞으로 내밀면서 왼쪽 무릎을 굽히고,
  오른발은 조금 뒤로 빼면서 오른쪽 무릎을 아래 방향으로 살짝 굽히는 형태이다.
- 기존 구현은 허벅지와 종아리 회전만 사용해 발끝이 관절의 원호를 따라 위로
  올라갔으며, 양발의 바닥 목표 위치를 별도로 유지하지 않았다.

### 수정 내용

- `Assets/_Project/Editor/Validation/KursaToShieldStanceAnimationTool.cs`
  - 양쪽 다리를 단순 회전하는 방식을 제거했다.
  - 현재 쿠르사 리그의 다리 길이에 비례해 왼발의 앞쪽 바닥 목표와 오른발의
    뒤쪽 바닥 목표를 정하고, 골반을 조금 낮춘 뒤 두 관절 자세 계산으로 양쪽
    발을 목표 위치에 유지하도록 바꿨다.
  - 왼쪽 무릎은 앞쪽으로 굽혀지고 오른쪽 무릎은 위나 안쪽으로 말리지 않도록
    아래 방향 비중이 큰 굽힘 기준을 사용한다.
  - 전체 동작 정면 행과 다리 본 기준 확대 사선 행을 함께 표시하는 교정 전용
    검토 화면을 추가했다.
  - 전체 행은 애니메이션 전 구간의 본체 Bounds와 승인 이펙트 Bounds를 직접
    합쳐 이펙트가 화면 밖으로 잘리지 않게 고정했다.
- `Kursa_05_ToShieldStance_Loop.anim`과 전용 컨트롤러를 교정 자세로 다시 생성했다.
- `CargoRunMvp.unity`의 `Kursa_05_ToShieldStance`만 다시 적용해 저장했다.
- 기존 왼팔·실제 방패 전진, 1초 진입, 승인 이펙트 표시, 2초 유지와 반복 구조는
  변경하지 않았다.
- `Kursa_06_ShieldStance` 및 다른 쿠르사 슬롯은 변경하지 않았다.

### 직접 화면 확인

- 첫 진단에서는 발이 위로 오므라드는 현상은 사라졌지만 앞뒤 보폭과 오른쪽
  무릎의 아래 방향 굽힘이 약하고 다리 확대 구도가 충분히 가깝지 않아
  완료 근거에서 제외했다.
- 두 번째 진단의 다리 확대 사선 화면에서 왼발이 앞쪽 바닥에 평평하게 놓이고
  왼쪽 무릎이 굽혀지는 것을 직접 확인했다.
- 오른발은 뒤쪽 바닥에 닿은 상태로 이동하며 오른쪽 무릎이 위로 말리지 않고
  아래 방향으로 살짝 굽혀진다.
- 양발이 서로 위로 오므라들거나 공중에 함께 들리는 자세는 보이지 않는다.
- 최종 전체 화면에서 왼팔과 실제 방패의 전진, 완료 시 방패 이펙트 표시와
  유지 구간이 그대로 보이는 것도 확인했다.
- 최종 이미지:
  `docs/validation/kursa_to_shield_stance_leg_correction_2026-08-04/Kursa_ToShieldStance_LegCorrection_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 승인된 방패 이펙트 외형 수정
- `Kursa_06_ShieldStance` 및 다른 쿠르사 슬롯 수정
- 공격 판정, 물리, AI 또는 게임플레이 로직 변경
- Git 작업

## 쿠르사 방패 태세 오른발 까치발 및 방패 전진 교정

상태: `SUPERSEDED_BY_RIGHT_LEG_FURTHER_BACK`

### 사용자 요청과 적용 기준

- 오른다리를 직전 자세보다 더 뒤로 빼고 오른발은 발끝이 바닥에 닿은 채
  뒤꿈치가 들리는 까치발 자세로 만든다.
- 왼팔을 더 앞으로 내밀어 실제 제압방패가 몸통 앞쪽으로 더 이동하게 한다.
- 기존 왼발 전진, 왼쪽 무릎 굽힘, 오른쪽 무릎의 아래 방향 굽힘,
  1초 진입, 승인 이펙트 표시, 2초 유지와 반복 구조는 유지한다.

### 수정 내용

- `Assets/_Project/Editor/Validation/KursaToShieldStanceAnimationTool.cs`
  - 현재 리그에 존재하는 `RightToeBase`를 사용해 오른발 발끝의 바닥 목표를
    직전보다 더 뒤쪽으로 이동시켰다.
  - 발끝 위치를 고정한 상태에서 오른쪽 발목을 기울이고, 그 결과 계산된
    발목 목표에 맞춰 허벅지와 종아리 관절을 다시 배치해 뒤꿈치를 들었다.
  - 오른쪽 무릎은 기존처럼 위나 안쪽으로 말리지 않고 아래 방향으로
    살짝 굽혀지도록 유지했다.
  - 왼손의 기존 방패 방향은 유지하면서 왼쪽 어깨·상박·전완의 전방 이동을
    함께 늘려 실제 제압방패가 몸통에서 더 앞쪽으로 나가게 했다.
  - 전체 정면, 상체 측면 확대, 다리 사선 확대의 3행 검토 화면으로 변경해
    이펙트·방패 전진·까치발을 한 화면에서 직접 비교할 수 있게 했다.
- `Kursa_05_ToShieldStance_Loop.anim`과 전용 컨트롤러를 새 자세로 다시 생성했다.
- `CargoRunMvp.unity`의 `Kursa_05_ToShieldStance`만 다시 적용해 저장했다.
- `Kursa_06_ShieldStance` 및 다른 쿠르사 슬롯은 변경하지 않았다.

### 직접 화면 확인

- 첫 진단의 다리 확대 사선 화면을 직접 확인했다.
- 오른발이 직전보다 더 뒤로 빠지고 발끝은 바닥에 닿은 채 뒤꿈치가 들린
  까치발 실루엣이 보인다.
- 오른쪽 무릎의 아래 방향 굽힘과 왼발 전진·왼쪽 무릎 굽힘이 유지된다.
- 상체 측면 확대에서 왼팔과 실제 방패가 정적·시작 자세보다 몸통 앞쪽으로
  더 이동하며 방패 방향이 흐트러지지 않는다.
- 전체 정면 행에서 완료 시 승인 이펙트가 나타나고 유지 표본 끝까지
  자세와 이펙트가 유지되는 것을 확인했다.
- 첫 진단에서 직접 확인을 통과해 두 번째 진단 조정은 실행하지 않았다.
- 최종 이미지:
  `docs/validation/kursa_to_shield_stance_toe_and_shield_2026-08-04/Kursa_ToShieldStance_ToeAndShield_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 승인된 방패 이펙트 외형 수정
- `Kursa_06_ShieldStance` 및 다른 쿠르사 슬롯 수정
- 공격 판정, 물리, AI 또는 게임플레이 로직 변경
- Git 작업

## 쿠르사 방패 태세 오른다리 추가 후퇴

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 사용자 요청과 적용 범위

- 현재 왼팔과 실제 방패 위치는 그대로 유지한다.
- 왼발 자세도 그대로 유지한다.
- 쿠르사 기준 오른다리만 직전 자세보다 더 뒤로 빼되, 오른발 까치발과
  오른쪽 무릎의 아래 방향 굽힘은 유지한다.

### 수정 내용

- `Assets/_Project/Editor/Validation/KursaToShieldStanceAnimationTool.cs`
  - 왼팔·방패 및 왼발 자세 계산은 변경하지 않았다.
  - `RightToeBase`의 뒤쪽 목표만 더 멀리 이동시켜 오른다리의 후퇴가
    측면 실루엣에서 확실히 보이도록 조정했다.
  - 이번 작업 전용 진단 및 최종 검토 경로로 캡처 산출물을 분리했다.
- `Kursa_05_ToShieldStance_Loop.anim`과 전용 컨트롤러를 새 자세로 다시 생성했다.
- `CargoRunMvp.unity`의 `Kursa_05_ToShieldStance`만 다시 적용해 저장했다.

### 직접 화면 확인

- 첫 진단 화면을 직전 결과와 직접 비교했다.
- 완료 자세에서 오른발이 이전보다 뒤쪽으로 더 분리되어 발 사이 간격이
  눈에 띄게 커졌고, 발끝 접지와 들린 뒤꿈치가 유지되는 것을 확인했다.
- 오른쪽 무릎은 아래 방향으로 굽은 상태를 유지했다.
- 왼팔과 실제 방패의 전진 위치, 왼발 전진 자세, 승인된 머리 위 방패
  이펙트는 직전 결과와 동일하게 유지됐다.
- 첫 진단에서 직접 시각 확인을 통과해 두 번째 진단은 실행하지 않았다.
- 최종 이미지:
  `docs/validation/kursa_to_shield_stance_right_leg_further_back_2026-08-04/Kursa_ToShieldStance_RightLegFurtherBack_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 왼팔·실제 방패 및 왼발 자세 변경
- 승인된 방패 이펙트 외형 변경
- `Kursa_06_ShieldStance` 및 다른 쿠르사 슬롯 변경
- 공격 판정, 물리, AI 또는 게임플레이 로직 변경
- Git 작업

## 쿠르사 방패 태세 이동 모션

상태: `SUPERSEDED_BY_SHIELD_WALKING_FBX`

### 사용자 확정 사항

- `Kursa_06_ShieldStance`는 별도 애니메이션을 만들지 않고 5번 진입 완료
  자세를 그대로 유지하는 상태로 사용한다.
- `Kursa_07_ShieldStanceMove`는 3번 이동 모션의 하체 보행을 기반으로 한다.
- 상체, 왼팔과 실제 방패는 5번 진입 완료 자세로 고정한다.
- 3번 이동과 같은 100% 보행 속도를 사용한다.
- 보폭은 5번 진입 완료 자세의 양발 간격을 기준으로 한다.
- 실제 전진 없이 제자리 반복하며 머리 위 승인 방패 이펙트를 계속 표시한다.

### 수정 내용

- `Assets/_Project/Editor/Validation/KursaToShieldStanceAnimationTool.cs`
  - 3번 이동 클립의 하체 보행 위상과 5번 진입 완료 자세를 결합하는
    #7 전용 클립 생성 경로를 추가했다.
  - 5번 완료 자세와 같은 다리 간격의 프레임에서 시작해 3번 하체 보행을
    같은 재생 길이와 프레임 속도로 반복하도록 구성했다.
  - `Hips`와 상체·팔·방패는 5번 완료 자세로 고정하고 좌우 다리 본만
    3번 보행 위상에 따라 움직이게 했다.
  - 머리 위 승인 방패 이펙트는 클립 전체에서 불투명하게 유지한다.
  - #3 속도 비교, #5 상체·보폭 비교, #7 전체 루프를 한 화면에서 직접
    확인하는 전용 진단·최종 캡처를 추가했다.
- `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `ApplyKursaShieldStanceMoveAnimation`,
    `CaptureKursaShieldStanceMoveDiagnostic`,
    `CaptureKursaShieldStanceMoveFinalReview` 명령을 연결했다.
- `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_07_ShieldStanceMove_InPlace.anim`
  - 제자리 반복 방패 태세 보행 클립을 생성했다.
- `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_07_ShieldStanceMove.controller`
  - 100% 재생 배율의 반복 기본 상태를 연결했다.
- `CargoRunMvp.unity`의 `Kursa_07_ShieldStanceMove`만 5번과 동일한 외형,
  전용 Animator와 계속 표시되는 승인 방패 이펙트 구성으로 교체해 저장했다.

### 직접 화면 확인

- 첫 진단의 위쪽 두 행에서 #3과 #7을 같은 시간 순서로 직접 비교했다.
- 두 개체가 같은 보행 단계와 속도로 한 주기를 진행하는 것이 보였다.
- #7 상체 확대 행에서 머리, 몸통, 왼팔과 실제 방패가 #5 완료 자세와
  동일하게 유지되며 하체 움직임에 따라 흔들리지 않는 것을 확인했다.
- 다리 확대 행에서 #5 완료 자세의 넓은 다리 간격으로 시작해 양발이
  교대로 넘어가고 마지막 표본이 시작 자세로 자연스럽게 이어졌다.
- 머리 위 승인 방패 이펙트가 모든 #7 표본에서 계속 표시됐다.
- 첫 진단에서 직접 시각 확인을 통과해 두 번째 진단은 실행하지 않았다.
- 최종 이미지:
  `docs/validation/kursa_shield_stance_move_2026-08-04/Kursa_ShieldStanceMove_FinalReview.png`

### 실행하지 않은 항목

- 폐기된 50% 보행 속도 적용
- `Kursa_06_ShieldStance` 전용 애니메이션 생성
- 3번 이동 및 5번 방패 태세 진입 자산 수정
- 승인된 방패 이펙트 외형 수정
- 실제 전진 이동, 공격 판정, 물리, AI 또는 게임플레이 구현
- #7 이외 쿠르사 슬롯 및 다른 씬 루트 변경
- `Run-HarnessValidation.ps1` 하네스 검증
- Git 작업

## 쿠르사 방패 태세 이동 FBX 교체 및 상체 고정

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 사용자 지정 기준

- 사용자 지정 원본은
  `enemies model/KUŠkursa shield walking.fbx`이다.
- FBX 내장 Mixamo 애니메이션을 하체만 추출하거나 재가공하지 않고 먼저
  전체 신체 기본 레이어에서 온전히 반복 재생한다.
- 전체 Mixamo 재생 이후 별도 Override 레이어로 상체·왼팔·실제 방패를
  5번 방패 태세 진입 완료 자세에 고정한다.
- 모델 외형은 새로 만들거나 추측하지 않고 1번 정적 쿠르사의 메시, 스킨,
  본 순서, 머티리얼 슬롯과 렌더러 설정을 그대로 사용한다.
- 머리 위 승인 방패 이펙트는 계속 표시한다.

### 적용 내용

- 원본 FBX를 내용 변경 없이
  `Assets/_Project/Art/Enemies/Kursa/Animations/Models/Kursa_ShieldStanceMove_Source.fbx`로
  복사해 Unity 소스로 사용했다.
- FBX에서 이름에 `mixamo`가 포함된 애니메이션 테이크가 정확히 하나임을
  확인하고 `Kursa_07_ShieldStanceMove_Mixamo` 반복 클립으로 임포트했다.
- FBX 리그와 정적 쿠르사의 본 이름, 루트 본과 바인드 포즈가 정확히
  대응하는 것을 확인했다.
- `Assets/_Project/Editor/Validation/KursaShieldStanceMoveFbxTool.cs`
  - FBX를 배율 래퍼 아래의 애니메이션 루트로 배치했다.
  - 정적 쿠르사의 `sharedMesh`, 본 배열, 루트 본, `sharedMaterials`, 렌더러
    설정과 머티리얼 속성 블록을 FBX 리그에 그대로 연결했다.
  - #5 완료 시점의 실제 `Spine` 이하 계층 자세를 이름 대응으로 추출해
    `Kursa_07_ShieldStanceMove_UpperPose.anim`을 생성했다.
  - 실제 FBX 계층에서 `Spine` 이하만 활성화한
    `Kursa_07_ShieldStanceMove_UpperBody.mask`를 생성했다.
  - Animator 기본 레이어에는 내장 Mixamo 전체 신체 클립을 그대로 연결하고,
    두 번째 Override 레이어에서 상체 고정 클립을 적용했다.
  - #5의 승인 방패 이펙트 개체를 그대로 복사하고 불투명 표시 상태로 유지했다.
- `Kursa_07_ShieldStanceMove.controller`를 위 두 레이어 구성으로 교체했다.
- 기존 절차 제작 `Kursa_07_ShieldStanceMove_InPlace.anim`은 사용하지 않으므로
  삭제했다.
- `UnityEditorValidationBridge.cs`에 적용, 진단, 최종 캡처 명령을 연결했다.
- `CargoRunMvp.unity`의 `Kursa_07_ShieldStanceMove/Kursa_Model`만 삭제하고
  FBX 기반 모델로 교체해 저장했다.

### 직접 화면 확인

- 첫 진단 실행은 실제 리그에 없는 `Spine1`을 검토 카메라 확대 범위에서
  요구해 이미지 생성 전에 중단됐다. 애니메이션 적용 문제가 아니므로 검토
  범위를 실제 `Spine` 이하 계층 전체로 교정했다.
- 두 번째 진단에서 정적 쿠르사와 #5 완료 자세, #7의 전체 Mixamo 주기를
  한 화면에서 직접 비교했다.
- #7의 얼굴, 갑옷, 방패, 재질과 전체 크기가 정적 쿠르사와 동일하게 보였다.
- 골반과 다리는 FBX 내장 Mixamo 순서대로 전체 보행 주기를 진행했다.
- 상체, 왼팔과 실제 방패는 모든 표본에서 #5 완료 자세로 고정됐으며
  하체 동작에 따라 원래 Mixamo 상체 자세로 되돌아가지 않았다.
- 시작과 마지막 표본이 이어지고 순간 크기 변화, 얼굴 변형, 메시 찢어짐,
  방패 분리는 보이지 않았다.
- 머리 위 승인 방패 이펙트가 모든 #7 표본에서 계속 표시됐다.
- 최종 이미지:
  `docs/validation/kursa_shield_stance_move_fbx_2026-08-04/Kursa_ShieldStanceMoveFbx_FinalReview.png`

### 실행하지 않은 항목

- Mixamo 애니메이션의 하체만 추출하거나 별도 보행으로 재가공
- 원본 `KUŠkursa shield walking.fbx` 내용 수정
- 새로운 메시, 텍스처, 머티리얼, 외형 또는 VFX 생성
- #1, #3, #5, #6 및 #7 이외 쿠르사 슬롯 변경
- 실제 전진 이동, 공격 판정, 물리, AI 또는 게임플레이 구현
- 다른 씬 루트 변경
- 새 `artSample/` 생성
- `Run-HarnessValidation.ps1` 하네스 검증
- Git 작업

## 쿠르사 방패 태세 해제 모션

상태: `SUPERSEDED_BY_ONE_SECOND_COMPLETION_HOLD`

### 사용자 확정 기준

- 대상은 `CargoRunMvp.unity`의 `Kursa_08_FromShieldStance` 하나다.
- 시작 자세는 5번 방패 태세 진입 완료 자세를 그대로 사용한다.
- 1.5초 동안 1번 쿠르사 정적 모델링 자세로 복구한다.
- 머리 위 승인 방패 이펙트는 해제 모션이 끝날 때 끈다.
- 해제 모션 전체를 반복 재생한다.

### 적용 내용

- `Assets/_Project/Editor/Validation/KursaFromShieldStanceAnimationTool.cs`를 추가했다.
  - 1번 정적 개체를 복제해 메시, 스킨, 본, 머티리얼 슬롯과 렌더러 외형을
    그대로 유지했다.
  - 5번 개체의 승인 방패 이펙트 개체를 그대로 복사했다.
  - `Kursa_05_ToShieldStance_Loop.anim`의 첫 1초 진입 구간을 역방향으로
    60fps 샘플링해 1.5초 해제 모션을 생성했다.
  - 마지막 재생 프레임에 정적 자세 복귀를 완료하고 그 프레임부터 방패
    이펙트를 끈 뒤, 남은 한 프레임을 유지하고 다음 루프로 이어지게 했다.
  - `MotionCompletionTime`은 반복 클립의 정확한 끝 시점이 다음 루프 0초로
    감기는 Unity 동작을 피하면서 완료 자세와 이펙트 소멸을 실제 재생
    프레임에 유지하는 역할이다.
- 생성 자산:
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_08_FromShieldStance_Loop.anim`
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_08_FromShieldStance.controller`
- `UnityEditorValidationBridge.cs`에 적용, 실패 잔여 상태 정리, 진단 및 최종
  검토 명령을 연결했다.
- `CargoRunMvp.unity`에서는 `Kursa_08_FromShieldStance/Kursa_Model`만 교체해
  저장했다. 다른 쿠르사 슬롯과 다른 씬 루트는 유지했다.

### 적용 중 교정 사항

- 첫 적용은 애니메이터의 0초 자세를 정적 비교 기준으로 사용한 내부 확인
  코드 때문에 저장 전에 중단됐다. 임시 개체와 생성 자산을 제거한 뒤 종료
  자세를 1번 정적 개체와 직접 비교하도록 교정했다.
- 실패 처리 뒤 Unity에 남은 씬 dirty 표시만, 8번 생성 자산과 컨트롤러가
  남아 있지 않은 상태를 확인한 다음 기존 씬 상태 저장으로 정리했다.
- 첫 두 진단은 반복 클립의 정확한 1.5초 지점을 샘플링해 마지막 열이 다음
  루프 시작 자세로 표시됐다. 마지막 재생 프레임에 완료 자세와 이펙트
  소멸을 유지하도록 클립과 검토 시점을 교정했다.

### 직접 화면 확인

- 세 번째 진단에서 첫 번째 기준 열의 5번 완료 자세와 8번 시작 자세가
  일치하는 것을 확인했다.
- 중간 열에서 넓게 벌어진 양다리, 굽힌 무릎, 앞으로 내민 왼팔과 실제
  방패가 순서대로 정적 자세로 복귀하는 것을 확인했다.
- 마지막 재생 프레임은 두 번째 열의 1번 정적 자세와 일치했고, 머리 위
  방패 이펙트는 꺼져 있었다.
- 얼굴 변형, 메시 찢어짐, 방패 분리, 순간 크기 변화는 보이지 않았다.
- 최종 이미지:
  `docs/validation/kursa_from_shield_stance_2026-08-04/Kursa_FromShieldStance_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 8번 이외 쿠르사 슬롯 수정
- 전투 판정, 물리, AI 또는 게임플레이 로직 구현
- Git 작업

## 쿠르사 정지 모션

상태: `SUPERSEDED_BY_ARM_ROTATION_ONLY_FIX`

### 사용자 확정 기준

- 대상은 `CargoRunMvp.unity`의 `Kursa_09_Stop` 하나다.
- 1번 쿠르사 정적 자세에서 시작한다.
- 2초 동안 묵념하듯 고개를 약 45도로 숙이고 양팔을 아래로 늘어뜨린다.
- 왼팔과 함께 실제 방패도 아래로 내려간다.
- 기존 눈 외형은 그대로 사용하며 2초 동안 눈 색만 서서히 무채색으로 바꾼다.
- 완료 자세와 무채색 눈을 1초 동안 유지한 뒤 처음부터 반복 재생한다.

### 적용 내용

- `KursaStopAnimationTool.cs`를 추가했다.
  - 1번 정적 개체를 복제해 메시, 스킨, 본, 머티리얼 슬롯과 렌더러 외형을
    그대로 유지했다.
  - 머리, 양팔과 방패가 0~2초 동안 완료 자세로 전환되고 2~3초 동안
    그대로 유지되는 3초 반복 클립을 생성했다.
  - 정적 자세에서 이미 펴져 있던 팔과 손에 연결된 방패가 확실히 내려가도록
    양쪽 어깨 루트를 전체 팔 길이의 `0.24` 비율만큼 낮춘 뒤 팔 자세를
    다시 계산했다. 이 값은 정지 모션에서 어깨·손·방패의 하강량을 만드는
    역할이다.
- `KursaApprovedAppearance.shader`에 기본값 0인 `_EyeDesaturation` 속성을
  추가하고, 정지 클립에서만 0~2초 사이 1까지 전환한 뒤 3초까지 유지했다.
  원본 눈 텍스처와 머티리얼 자산은 수정하지 않았다.
- 생성 자산:
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_09_Stop_Loop.anim`
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_09_Stop.controller`
- `UnityEditorValidationBridge.cs`에 적용, 방패 스킨 확인, 진단 및 최종 검토
  명령을 연결했다.
- `CargoRunMvp.unity`에서는 `Kursa_09_Stop/Kursa_Model`만 교체해 저장했다.
  다른 쿠르사 슬롯과 다른 씬 루트는 유지했다.

### 적용 중 교정 사항

- 첫 진단에서는 눈 색 전환은 보였지만 `AnimationClip.SampleAnimation`으로
  캡처한 스킨 자세가 갱신되지 않았다. 실제 `Animator`를 재생·평가해 캡처하도록
  교정했다.
- 두 번째 진단에서는 몸 자세가 정상 표시됐지만 방패 하강이 충분히 드러나지
  않았다.
- 방패 스킨을 확인한 결과 방패 정점 119개가 모두 `LeftHand` 본을 주 본으로
  사용하고 있었다. 이에 양쪽 어깨 루트 하강 비율 `0.24`를 적용한 뒤 팔을
  다시 계산해 왼손과 방패가 함께 분명히 내려가도록 교정했다.

### 직접 화면 확인

- 세 번째 진단을 직접 보고 정적 기준에서 2초까지 고개와 양팔이 서서히
  내려가고, 왼팔을 따라 실제 방패도 몸 기준으로 아래로 이동하는 것을
  확인했다.
- 눈이 파란색에서 무채색으로 점진 전환되는 것을 확인했다.
- 2초 완료, 2.5초 유지 중간, 최종 유지 프레임의 자세와 눈 상태가 동일했다.
- 얼굴 변형, 메시 찢어짐, 방패 분리 또는 순간 크기 변화는 보이지 않았다.
- 최종 이미지:
  `docs/validation/kursa_stop_animation_2026-08-04/Kursa_Stop_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 9번 이외 쿠르사 슬롯 수정
- 원본 눈 텍스처 또는 머티리얼 자산 수정
- 전투 판정, 물리, AI 또는 게임플레이 로직 구현
- Git 작업

## 쿠르사 정지 모션 팔 메시 변형 교정

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 사용자 확인 사항

- 기존 정지 모션에서 양팔이 메시가 늘어나듯 아래로 변형되는 현상을
  제거한다.
- 팔 메시나 본 길이를 늘리지 않고 힘이 빠진 듯 관절을 회전해 양팔을
  아래로 내린다.
- 해당 방식이 어렵다면 오른팔을 유지하고 왼팔과 방패만 내리는 대안을
  사용할 수 있다.

### 원인과 수정 내용

- 기존 `KursaStopAnimationTool.cs`가 양쪽 `Shoulder` 본의 위치를 전체 팔
  길이의 `0.24`만큼 아래로 이동시킨 뒤 위치 커브로 기록하고 있었다.
  이 어깨 본 이동이 몸통과 팔 사이 스킨을 아래로 당긴 원인이었다.
- `ShoulderDropArmLengthRatio`와 양쪽 어깨 위치 이동을 제거했다.
- 어깨, 위팔, 아래팔, 손 본의 로컬 위치는 1번 정적 모델과 동일하게
  고정하고, 기존 두 관절 계산의 회전만으로 양팔을 내리도록 변경했다.
- 클립 생성 시 모든 골격의 로컬 위치가 정적 자세와 다르면 적용을
  중단하도록 계약을 추가했다. 이 계약은 본 길이와 관절 간 거리를
  유지하는 역할이다.
- 양팔을 회전만으로 자연스럽게 내릴 수 있었으므로 왼팔·방패만 내리는
  대안은 사용하지 않았다.
- 고개 숙임, 눈 무채색 전환, 2초 전환, 1초 완료 자세 유지 및 3초 반복은
  기존대로 유지했다.
- `CargoRunMvp.unity`에서는 `Kursa_09_Stop/Kursa_Model`만 다시 적용해
  저장했다. 다른 쿠르사 슬롯과 다른 씬 루트는 유지했다.

### 직접 화면 확인

- 첫 교정 진단과 최종 이미지를 직접 확인했다.
- 완료 자세에서 양쪽 어깨가 몸통에 붙어 있고 팔의 길이와 두께가 정적
  기준과 동일하게 유지되는 것을 확인했다.
- 양팔은 관절 회전으로 몸 옆에 내려가며, 메시 늘어남·찌그러짐·어깨 이탈이
  보이지 않았다.
- 왼팔과 실제 방패가 분리되지 않고 함께 움직이는 것을 확인했다.
- 고개 숙임과 파란 눈의 점진적 무채색 전환, 완료 자세 1초 유지가
  그대로 재생되는 것을 확인했다.
- 첫 진단에서 직접 시각 확인을 통과해 두 번째 진단은 실행하지 않았다.
- 최종 이미지:
  `docs/validation/kursa_stop_arm_relax_fix_2026-08-04/Kursa_Stop_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, 빌드
- 9번 이외 쿠르사 슬롯 수정
- 원본 메시, 스킨, 텍스처 또는 머티리얼 수정
- 전투 판정, 물리, AI 또는 게임플레이 로직 구현
- Git 작업

## 쿠르사 방패 태세 해제 완료 자세 1초 유지

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 사용자 요청

- 기존 1.5초 방패 태세 해제를 완료한 뒤 해당 정적 자세를 1초 동안
  유지하고, 유지가 끝난 다음 처음부터 반복 재생한다.
- 유지 구간에는 머리 위 방패 이펙트가 꺼진 상태를 유지한다.

### 적용 내용

- `KursaFromShieldStanceAnimationTool.cs`
  - 해제 구간 `TransitionSeconds`를 기존 1.5초로 유지했다.
  - 완료 자세 유지 구간 `HoldSeconds`를 1초로 추가했다.
  - 전체 반복 길이 `DurationSeconds`를 2.5초로 확장했다.
  - 0~1.5초에는 기존 5번 진입 모션의 역방향 복귀를 그대로 재생한다.
  - 1.5~2.5초에는 모든 본을 정적 자세로 고정하고 방패 이펙트 알파를
    꺼진 상태로 유지한다.
  - 검토 화면에 1.5초 해제 완료, 2초 유지 중간, 2.5초 직전 유지 마지막
    표본을 함께 표시하도록 수정했다.
- `Kursa_08_FromShieldStance_Loop.anim`과 전용 컨트롤러를 2.5초 반복
  구성으로 다시 생성했다.
- `CargoRunMvp.unity`에서는 `Kursa_08_FromShieldStance/Kursa_Model`만 다시
  적용해 저장했다. 다른 쿠르사 슬롯과 다른 씬 루트는 유지했다.

### 직접 화면 확인

- 첫 진단에서 기존 1.5초 해제 흐름이 유지되는 것을 확인했다.
- 1.5초 완료 열과 2초 유지 중간, 2.5초 직전 유지 마지막 열에서 몸통,
  양다리, 왼팔과 실제 방패가 같은 정적 자세로 고정된 것을 확인했다.
- 세 유지 표본에서 머리 위 방패 이펙트가 모두 꺼져 있었다.
- 얼굴 변형, 메시 찢어짐, 방패 분리 또는 순간 크기 변화는 보이지 않았다.
- 첫 진단에서 직접 시각 확인을 통과해 두 번째 진단은 실행하지 않았다.
- 최종 이미지:
  `docs/validation/kursa_from_shield_stance_hold_2026-08-04/Kursa_FromShieldStanceHold_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 8번 이외 쿠르사 슬롯 수정
- 전투 판정, 물리, AI 또는 게임플레이 로직 구현
- Git 작업

## 쿠르사 피격 모션 FBX 교체

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 사용자 확정 기준

- `CargoRunMvp.unity`의 `Kursa_10_Hit`에 있던 모델 개체를
  `enemies model/KUŠkursa hit.fbx` 기반 모델로 교체한다.
- 보이는 외형은 추측하거나 새로 만들지 않고 1번 쿠르사 정적 모델의
  메시, 스킨, 머티리얼 슬롯과 렌더러 설정을 그대로 사용한다.
- FBX 내장 애니메이션 중 `mixamo` 클립을 온전히 반복 재생한다.

### 적용 내용

- 원본 `enemies model/KUŠkursa hit.fbx`를 수정하지 않고
  `Assets/_Project/Art/Enemies/Kursa/Animations/Models/Kursa_Hit_Source.fbx`로
  복사했다. 두 파일의 SHA-256은
  `32006C48D0A8CD1A6186706C0CAE4EADF4618C6BB579770F123B8FAE0C83C97A`로
  동일하다.
- `KursaHitFbxTool.cs`를 추가했다.
  - FBX 기본 테이크 중 이름 또는 테이크 이름에 `mixamo`가 포함된 후보가
    정확히 하나일 때만 선택한다.
  - 선택된 전체 테이크를 `Kursa_10_Hit_Mixamo`로 임포트하고 Loop Time을
    켜며 Loop Pose 혼합은 사용하지 않는다.
  - 정적 쿠르사와 FBX의 본 이름, 본 순서, 루트 본과 바인드 포즈가 정확히
    호환될 때만 교체한다.
  - 정적 쿠르사의 동일 `sharedMesh`, 본 순서, 루트 본, 머티리얼 슬롯,
    렌더러 속성과 머티리얼 속성 블록을 FBX 리그에 그대로 연결했다.
- 생성 자산:
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Models/Kursa_Hit_Source.fbx`
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_10_Hit.controller`
- `UnityEditorValidationBridge.cs`에 적용, 진단 및 최종 검토 명령을 연결했다.
- `CargoRunMvp.unity`에서는 `Kursa_10_Hit` 슬롯 루트와 배치 Transform을
  유지하고 기존 `Kursa_Model` 자식만 새 FBX 기반 `Kursa_Model`로 교체해
  저장했다. 다른 쿠르사 슬롯과 다른 씬 루트는 유지했다.

### 적용 중 교정 사항

- 첫 도구 생성 시 기존 파일 전문 출력이 중간 생략되어 생략 문자가 코드에
  들어가 컴파일 오류가 발생했다. 기존 도구를 140줄 이하 구간으로 나눠
  끝까지 다시 읽고 피격 도구를 생략 없이 재구성했다.
- 교정된 도구는 985줄이며 생략 문자가 없고, 사용자가 Unity 컴파일 오류가
  해결됐다고 확인한 뒤 `Refresh-UnityProject.ps1`가 정상 완료됐다.
- 첫 진단 정면에서는 원본 피격 동작의 방패가 얼굴과 팔 연결부를 넓게 가려
  판정이 충분하지 않았다. 두 번째 진단을 40도 사선 시점으로 생성했다.

### 직접 화면 확인

- 두 번째 진단과 최종 이미지를 직접 확인했다.
- 정적 기준과 동일한 메시, 갑옷, 방패, 머티리얼 외형과 신체 크기가
  유지되는 것을 확인했다.
- 공급된 Mixamo 피격 동작 전체에서 양팔이 방패를 머리 위로 들어 올리고
  몸과 다리가 충격 자세로 전환되는 흐름을 확인했다.
- 팔·다리 메시 늘어남, 관절 분리, 얼굴 변형, 방패 이탈 또는 순간 크기
  변화는 보이지 않았다.
- 전체 원본 동작이 재생된 뒤 시작 프레임으로 돌아가 반복되는 것을
  확인했으며 별도 동작 생성이나 혼합은 적용하지 않았다.
- 최종 이미지:
  `docs/validation/kursa_hit_fbx_2026-08-04/Kursa_HitFbx_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 원본 `enemies model/KUŠkursa hit.fbx` 수정
- 정적 쿠르사의 원본 메시, 텍스처 또는 머티리얼 수정
- 10번 이외 쿠르사 슬롯 수정
- 공격 판정, 물리, AI 또는 게임플레이 구현
- Git 작업

## 쿠르사 사망 모션 FBX 교체 및 완료 자세 유지

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 사용자 확정 기준

- `CargoRunMvp.unity`의 `Kursa_11_Death`에 있던 모델 개체를
  `enemies model/KUŠkursa death.fbx` 기반 모델로 교체한다.
- 보이는 외형은 추측하거나 새로 만들지 않고 1번 쿠르사 정적 모델의
  메시, 스킨, 머티리얼 슬롯과 렌더러 설정을 그대로 사용한다.
- FBX 내장 애니메이션 중 `mixamo` 클립 전체를 재생한다.
- 완전히 쓰러진 마지막 자세를 1초 동안 유지한 뒤 처음부터 반복한다.

### 적용 내용

- 원본 `enemies model/KUŠkursa death.fbx`를 수정하지 않고
  `Assets/_Project/Art/Enemies/Kursa/Animations/Models/Kursa_Death_Source.fbx`로
  복사했다. 두 파일의 SHA-256은
  `E27CB025E367915EDB5D9032DE4B04A95FD0D1792BC6EB181A8BB265BD8A590A`로
  동일하다.
- `KursaDeathFbxTool.cs`를 추가했다.
  - FBX 기본 테이크 중 이름 또는 테이크 이름에 `mixamo`가 포함된 후보가
    정확히 하나일 때만 `Kursa_11_Death_MixamoSource`로 선택한다.
  - 원본 Mixamo 클립의 모든 곡선과 키를 같은 시각·값으로 복사한다.
  - 각 곡선의 마지막 값과 동일한 키 하나만 원본 끝에서 1초 뒤에 추가해
    새로운 자세를 만들지 않고 마지막 쓰러진 자세를 유지한다.
  - 원본 길이 2.283초와 유지 1초를 합친 3.283초 클립을 Loop Time 켜짐,
    Loop Pose 꺼짐 상태로 반복한다.
  - 정적 쿠르사와 FBX의 본 이름, 루트 본과 바인드 포즈가 정확히 호환될
    때만 교체한다.
  - 정적 쿠르사의 동일 `sharedMesh`, 본 순서, 루트 본, 머티리얼 슬롯,
    렌더러 속성과 머티리얼 속성 블록을 FBX 리그에 그대로 연결했다.
- 생성 자산:
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Models/Kursa_Death_Source.fbx`
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_11_Death_MixamoHold.anim`
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_11_Death.controller`
- `UnityEditorValidationBridge.cs`에 적용, 진단 및 최종 검토 명령을 연결했다.
- `CargoRunMvp.unity`에서는 `Kursa_11_Death` 슬롯 루트와 배치 Transform을
  유지하고 기존 `Kursa_Model` 자식만 새 FBX 기반 `Kursa_Model`로 교체해
  저장했다. 다른 쿠르사 슬롯과 다른 씬 루트는 유지했다.

### 직접 화면 확인

- 첫 진단에서 선 자세부터 무릎이 꺾이고 옆으로 완전히 쓰러지는 원본
  동작 전체와 마지막 자세 유지 구간을 직접 확인했다.
- 두 번째 40도 사선 진단에서 다리, 팔, 몸통의 연결과 최종 착지 자세를
  확인했다. 사선 카메라가 선 자세의 큰 방패 위쪽을 잘라 최종 증빙에는
  전체 실루엣이 보이는 정면 카메라를 사용했다.
- 최종 이미지의 마지막 여섯 표본에서 쓰러진 자세가 동일하게 유지되는
  것을 확인했다.
- 정적 기준과 동일한 메시, 갑옷, 방패, 머티리얼 외형과 신체 크기가
  유지되는 것을 확인했다.
- 팔·다리 메시 늘어남, 관절 분리, 얼굴 변형, 방패 이탈 또는 순간 크기
  변화는 보이지 않았다.
- 최종 이미지:
  `docs/validation/kursa_death_fbx_2026-08-04/Kursa_DeathFbx_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 원본 `enemies model/KUŠkursa death.fbx` 수정
- 정적 쿠르사의 원본 메시, 텍스처 또는 머티리얼 수정
- 임의 사망 자세 또는 외형 생성
- 11번 이외 쿠르사 슬롯 수정
- 사망 판정, 충돌, 물리, AI 또는 게임플레이 구현
- Git 작업

## 쿠르사 방패 파괴 반응 FBX 교체 및 슬롯 전용 방패 파편

상태: `DIRECT_VISUAL_REVIEW_PASSED`

### 사용자 확정 기준

- `CargoRunMvp.unity`의 `Kursa_12_ShieldBreakReaction`에 있던 모델 개체를
  `enemies model/KUŠkursa broken shield.fbx` 기반 모델로 교체한다.
- 외형은 추측하거나 새로 만들지 않고 1번 쿠르사 정적 모델의 메시, 스킨,
  머티리얼 슬롯과 렌더러 설정을 그대로 가져온다.
- FBX 내장 애니메이션 중 `mixamo` 테이크 전체를 반복 재생한다.
- 정적 쿠르사의 방패는 건드리지 않는다. 12번 개체에 복제된 실제 방패
  메시만 조각으로 분할해 원래 머티리얼을 유지한 채 0.5초 동안 흩어지게
  하고, 이후 Mixamo 동작이 끝날 때까지 숨긴다. 다음 루프 시작 시 12번
  방패만 복원해 다시 파괴한다.

### 적용 내용

- 원본 `enemies model/KUŠkursa broken shield.fbx`를 수정하지 않고
  `Assets/_Project/Art/Enemies/Kursa/Animations/Models/Kursa_ShieldBreak_Source.fbx`로
  복사했다. 두 파일의 SHA-256은
  `910616944E6344EE6AF0F6D084B9C8F623556B93CA4035FB92E6B6CE5475F844`로
  동일하다.
- `KursaShieldBreakFbxTool.cs`를 추가했다.
  - 이름 또는 테이크 이름에 `mixamo`가 포함된 기본 테이크가 정확히
    하나일 때만 원본 동작으로 선택한다.
  - 정적 모델과 FBX의 본 이름, 본 순서, 루트 본과 바인드 포즈가 정확히
    호환될 때만 12번 교체를 진행한다.
  - 정적 메시를 직접 수정하지 않고 복제본에서 확인된 방패 서브메시만
    비운 12번 전용 본체 메시를 생성했다.
  - 같은 정적 방패의 기존 삼각형 표면, UV, 노멀, 탄젠트와 해당 머티리얼을
    그대로 복제해 12번 리그의 `LeftHand` 아래에 실제 메시 조각으로
    배치했다. 새 방패 형상이나 절단면, 머티리얼, 텍스처는 만들지 않았다.
  - 시작 프레임에서 조각들이 원래 방패를 이루고, 0.5초 동안 바깥쪽으로
    흩어진 뒤 렌더러가 꺼진다. 꺼진 상태는 Mixamo 원본 동작 끝까지
    유지되며 Loop Time으로 처음 프레임에 돌아오면 다시 나타난다.
- 생성 자산:
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Models/Kursa_ShieldBreak_Source.fbx`
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_12_ShieldBreakReaction_Shieldless.asset`
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_12_ShieldBreakReaction_Fragments.asset`
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_12_ShieldBreakReaction_Playback.anim`
  - `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_12_ShieldBreakReaction.controller`
- `UnityEditorValidationBridge.cs`에 적용, 진단 및 최종 화면 명령을 연결했다.
- `CargoRunMvp.unity`에서는 12번 슬롯 루트와 배치 Transform을 유지하고
  기존 `Kursa_Model` 자식만 새 FBX 기반 `Kursa_Model`로 교체해 저장했다.
  1번 정적 쿠르사를 포함한 다른 11개 슬롯과 다른 씬 루트는 유지했다.

### 직접 화면 확인

- 정면 진단 화면에서 1번 정적 참조의 방패가 계속 온전하게 남아 있고,
  12번 방패만 시작 프레임에서 원형을 유지한 뒤 실제 표면 조각으로 갈라져
  흩어지는 과정을 직접 확인했다.
- 40도 사선 진단 화면에서 파편이 12번 왼손 위치에서 앞·옆 방향으로
  분리되며, 몸체 메시 자체가 파괴되거나 늘어나는 현상이 없음을 확인했다.
- 파괴 도중 기존 방패의 금속·목재 계열 표면 머티리얼이 조각에 그대로
  보이고, 새 절단면이나 임의 색상은 보이지 않았다.
- 0.5초 이후 방패와 모든 조각이 사라지고, 쿠르사 신체는 원본 Mixamo
  방패 파괴 반응 동작을 끝까지 재생하는 것을 확인했다.
- 최종 정면 화면에서도 같은 파괴 순서, 조각 소멸, 방패 없는 후속 동작과
  신체 외형 유지가 반복되는 것을 직접 확인했다.
- 최종 이미지:
  `docs/validation/kursa_shield_break_fbx_2026-08-04/Kursa_ShieldBreakFbx_FinalReview.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 원본 `enemies model/KUŠkursa broken shield.fbx` 수정
- 정적 쿠르사 개체, 정적 방패 또는 정적 메시 자산 수정
- 새 방패 형상, 절단면, 머티리얼 또는 텍스처 생성
- 12번 이외 쿠르사 슬롯 수정
- 방패 내구도 판정, 충돌, 물리, AI 또는 게임플레이 구현
- Git 작업

### 방패 파편 세분화 및 고정 무작위 분산 수정

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 사용자 확인에 따라 12번 방패의 각 기존 삼각형을 세 변의 중점으로
  나눠 4개의 작은 삼각형 조각으로 세분화했다.
- 새 조각의 위치는 원본 방패 표면과 정확히 일치하고, UV·노멀·탄젠트·
  버텍스 색상은 원본 삼각형에서 보간했다. 기존 머티리얼 슬롯을 그대로
  사용하며 새 절단면, 머티리얼 또는 텍스처는 만들지 않았다.
- 기존처럼 방패 중심에서 일정 방향과 거리로 퍼뜨리지 않는다. 고정 시드로
  조각마다 서로 다른 3차원 방향, 이동거리와 회전축을 한 번 생성해 재생
  클립에 저장했다. 따라서 재생할 때마다 방향이 바뀌지 않고 모든 루프에서
  같은 무작위 파괴 모션을 반복한다.
- 이동거리는 방패 반경 기준 1.8배부터 3배 사이에서 조각마다 다르게
  설정해 기존보다 분산 범위를 크게 넓혔다.
- 정면 화면에서 작은 파편들이 기존의 균일한 구형 띠가 아니라 서로 다른
  방향과 거리로 넓게 흩어지는 것을 직접 확인했다.
- 40도 사선 화면에서 파편이 앞·뒤·좌·우·상·하 방향으로 분리되고,
  쿠르사 몸체나 정적 방패가 함께 파괴되지 않는 것을 직접 확인했다.
- 파편은 기존과 같이 0.5초 후 모두 사라지고, 방패 없는 Mixamo 동작이
  끝까지 이어진 뒤 같은 파괴 패턴으로 반복된다.
- 최종 이미지:
  `docs/validation/kursa_shield_break_scatter_revision_2026-08-04/Kursa_ShieldBreakFbx_FinalReview.png`
- `Run-HarnessValidation.ps1` 하네스 검증, 원본 FBX 수정, 정적 쿠르사 수정,
  12번 이외 슬롯 수정, 게임플레이·물리·AI 및 Git 작업은 실행하지 않았다.

### 방패 파편 쿠르사 왼쪽 방향 수정

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 사용자 확인에 따라 파편의 주 이동 방향을 쿠르사 자신의 왼쪽으로
  변경했다. 정면 화면에서는 쿠르사의 왼팔과 방패가 있는 화면 오른쪽에
  해당한다.
- Mixamo 원본 동작의 0.5초 시점 왼손 회전을 확인해 쿠르사 로컬 왼쪽
  벡터를 손 로컬 좌표로 변환했다. 따라서 왼손이 회전해도 파편 방향이
  반대로 뒤집히지 않는다.
- 모든 파편은 왼쪽 성분을 가지며, 위·아래와 앞·뒤 성분만 고정 무작위
  편차를 적용했다. 같은 방향 패턴이 모든 루프에서 반복된다.
- 기존의 삼각형 4분할 조각 크기, 방패 반경 1.8~3배 이동거리, 기존
  머티리얼과 0.5초 소멸 시점은 변경하지 않았다.
- 정면 화면에서 파편 전체가 쿠르사 왼쪽으로 이동하고 몸의 반대편을
  둘러싸는 구형 확산이 나타나지 않는 것을 직접 확인했다.
- 40도 사선 화면에서 왼쪽 주축을 유지하면서 조각별 높이와 깊이가 서로
  다르고, 몸체·정적 방패·다른 슬롯이 유지되는 것을 직접 확인했다.
- 최종 이미지:
  `docs/validation/kursa_shield_break_left_scatter_2026-08-04/Kursa_ShieldBreakFbx_FinalReview.png`
- `Run-HarnessValidation.ps1` 하네스 검증, 원본 FBX 수정, 정적 쿠르사 수정,
  조각 크기·이동거리·소멸 시점 변경, 12번 이외 슬롯 수정, 게임플레이·
  물리·AI 및 Git 작업은 실행하지 않았다.

### 방패 파편 쿠르사 왼쪽 뒤 방향 수정

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 사용자 확인에 따라 파편의 주 이동 방향을 쿠르사 기준 왼쪽 뒤 대각선으로
  변경했다.
- 모든 파편의 쿠르사 로컬 X 성분은 왼쪽, Z 성분은 뒤쪽으로 고정하고,
  좌후방 대각선 범위 안의 축 비율과 높이만 고정 무작위로 다르게 했다.
  따라서 앞쪽이나 오른쪽으로 날아가는 파편은 없다.
- Mixamo 0.5초 시점의 왼손 회전을 기준으로 좌후방 벡터를 손 로컬 좌표로
  변환해 실제 애니메이션 자세에서도 방향이 뒤집히지 않게 했다.
- 기존 삼각형 4분할 조각, 방패 반경 1.8~3배 이동거리, 머티리얼과 0.5초
  소멸 시점은 변경하지 않았다.
- 정면 화면에서 쿠르사 왼쪽 이동을, 40도 사선 화면에서 파편이 몸 뒤쪽
  공간으로 빠지는 좌후방 궤적을 직접 확인했다.
- 정적 쿠르사, 정적 방패, 몸체 메시와 다른 11개 슬롯은 유지됐다.
- 최종 이미지:
  `docs/validation/kursa_shield_break_left_back_scatter_2026-08-04/Kursa_ShieldBreakFbx_FinalReview.png`
- `Run-HarnessValidation.ps1` 하네스 검증, 원본 FBX 수정, 정적 쿠르사 수정,
  조각 크기·이동거리·소멸 시점 변경, 12번 이외 슬롯 수정, 게임플레이·
  물리·AI 및 Git 작업은 실행하지 않았다.

### 방패 파편 감속 및 좌후방 영역 분산 수정

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 사용자 확인에 따라 파편 확산·소멸 시간을 0.5초에서 1.5초로 늘려
  기존보다 크게 느리게 만들었다.
- 모든 파편을 비슷한 좌후방 대각선으로 보내던 제한을 제거했다. 왼쪽과
  뒤쪽 성분을 각각 독립적인 넓은 범위에서 생성해 좌후방 공간 전체로
  흩어지게 했다.
- 모든 파편은 왼쪽 및 뒤쪽 성분을 유지하므로 앞쪽이나 오른쪽으로 날아가지
  않으며, 서로 다른 축 비율·높이·이동거리 때문에 한 지점에 모이지 않는다.
- 원본 Mixamo 동작 길이 안에서 1.5초 동안 단계적으로 움직이고 이후
  파편이 사라진 상태로 동작 끝까지 이어진 뒤 같은 패턴으로 반복된다.
- 기존 삼각형 4분할 조각 크기, 방패 반경 1.8~3배 이동거리와 원본
  머티리얼은 변경하지 않았다.
- 정면 및 40도 사선 화면에서 빠른 폭발 대신 여러 중간 자세에 걸쳐
  천천히 해체되고, 한 대각선이나 한 지점이 아닌 좌후방 영역으로 넓게
  흩어지는 것을 직접 확인했다.
- 정적 쿠르사, 정적 방패, 몸체 메시와 다른 11개 슬롯은 유지됐다.
- 최종 이미지:
  `docs/validation/kursa_shield_break_slow_left_back_scatter_2026-08-04/Kursa_ShieldBreakFbx_FinalReview.png`
- `Run-HarnessValidation.ps1` 하네스 검증, 원본 FBX 수정, 정적 쿠르사 수정,
  조각 크기·머티리얼 변경, 12번 이외 슬롯 수정, 게임플레이·물리·AI 및
  Git 작업은 실행하지 않았다.

### 방패 파편 왼팔 추종 제거

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 파편 루트가 `LeftHand` 본의 자식이라 왼팔 애니메이션의 이동과 회전이
  파편 궤적 전체에 계속 누적되던 원인을 수정했다.
- Mixamo 시작 프레임의 왼손 위치·회전·크기만 복사한 고정 파편 프레임을
  `Kursa_ShieldBreak_AnimatedRoot` 바로 아래에 배치했다.
- 파편 이동 곡선은 고정 파편 프레임 아래에서만 재생되며, 파편 루트에는
  왼팔 또는 다른 본의 Transform 애니메이션이 바인딩되지 않는다.
- 시작 프레임에서는 고정 프레임이 실제 왼손 자세와 일치해 원래 방패
  외형을 구성하고, 파괴가 시작된 뒤에는 왼팔 움직임과 무관하게 좌후방
  공간으로 흩어진다.
- 기존 1.5초 확산·소멸 시간, 삼각형 4분할 조각 크기, 방패 반경 1.8~3배
  이동거리, 원본 머티리얼과 고정 반복 패턴은 변경하지 않았다.
- 정면과 40도 사선 화면에서 왼팔이 몸 쪽으로 움직이는 동안 파편 중심이
  손을 따라 끌려가거나 궤적이 휘지 않고 독립적으로 진행되는 것을 직접
  확인했다.
- 정적 쿠르사, 정적 방패, 몸체 메시와 다른 11개 슬롯은 유지됐다.
- 최종 이미지:
  `docs/validation/kursa_shield_break_detached_scatter_2026-08-04/Kursa_ShieldBreakFbx_FinalReview.png`
- `Run-HarnessValidation.ps1` 하네스 검증, 원본 FBX 수정, 정적 쿠르사 수정,
  파편 속도·조각 크기·이동거리·머티리얼 변경, 12번 이외 슬롯 수정,
  게임플레이·물리·AI 및 Git 작업은 실행하지 않았다.

### 방패 파편 확산 시간 1초 조정

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 12번 방패 파괴 반응의 파편 확산·소멸 시간을 1.5초에서 1초로
  변경했다.
- 기존 좌후방 분산 방향, 삼각형 4분할 조각 크기, 방패 반경 1.8~3배
  이동거리, 원본 머티리얼과 고정 반복 패턴은 변경하지 않았다.
- 파편 루트는 계속 애니메이션 루트에 고정되어 왼팔 동작을 상속하지 않는다.
- 정면 및 40도 사선 화면을 직접 확인해 시작 방패가 유지된 뒤 파편이
  1초 동안 좌후방으로 흩어져 사라지고, 이후 반복 종료까지 숨겨진 상태를
  유지하는 것을 확인했다.
- 정적 쿠르사, 정적 방패, 몸체 메시와 다른 11개 슬롯은 유지됐다.
- 최종 이미지:
  `docs/validation/kursa_shield_break_one_second_scatter_2026-08-04/Kursa_ShieldBreakFbx_FinalReview.png`
- `Run-HarnessValidation.ps1` 하네스 검증, 원본 FBX 수정, 정적 쿠르사 수정,
  파편 방향·조각 크기·이동거리·머티리얼 변경, 12번 이외 슬롯 수정,
  게임플레이·물리·AI 및 Git 작업은 실행하지 않았다.

### 방패 파편 용량 최적화

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 544개 파편별 `GameObject`, `MeshRenderer`, 개별 메시와 위치·회전·표시
  애니메이션 곡선을 단일 결합 메시와 단일 런타임 메시 변형 구성요소로
  교체했다.
- 파편은 결합 메시 안에서 기존과 동일한 544개 삼각형 조각으로 유지되며,
  기존 고정 난수의 중심 이동·회전 데이터와 1초 확산 시간을 사용한다.
- 원본 방패의 두 머티리얼 영역은 결합 메시의 두 서브메시에 그대로 연결했고,
  정점·노멀·탄젠트·UV·색상 보간 방식도 유지했다.
- 파편 렌더러는 544개에서 1개, 파편 전용 애니메이션 경로는 7,616개에서
  0개, 씬의 구형 파편 개체 이름은 0개가 됐다.
- 생성 런타임 자산과 씬 파편 직렬화 합계는 약 16.737MiB에서
  5.906MiB로 줄어 약 10.831MiB, 64.7% 감소했다.
- 세부적으로 파편 메시 자산은 2.152MiB에서 0.334MiB, 재생 애니메이션은
  12.507MiB에서 4.752MiB, 씬 파편 직렬화는 1.346MiB에서 0.090MiB로
  감소했다.
- 정면 및 40도 사선 화면을 직접 확인해 시작 방패 외형, 기존 파편 크기와
  머티리얼, 1초 좌후방 확산, 왼팔 독립, 소멸 후 반복 종료까지 숨김 상태가
  유지되는 것을 확인했다.
- 정적 쿠르사, 정적 방패, 몸체 메시와 다른 11개 슬롯은 유지됐다.
- 중간 컴파일 상태는 사용자가 해소했으며 이후 공식 리프레시와 Unity 적용이
  정상 통과했다.
- 최종 이미지:
  `docs/validation/kursa_shield_break_optimized_scatter_2026-08-04/Kursa_ShieldBreakFbx_FinalReview.png`
- `Run-HarnessValidation.ps1` 하네스 검증, 원본 FBX 수정, 정적 쿠르사 수정,
  파편 방향·크기·이동거리·머티리얼 변경, 12번 이외 슬롯 수정,
  게임플레이·물리·AI, 빌드 및 Git 작업은 실행하지 않았다.

### 쿠르사 6번 파괴 후 회복 FBX 1차 적용

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 6번 슬롯의 영문 이름을 `Kursa_06_ShieldStance`에서
  `Kursa_06_PostBreakRecovery`로 변경하고 관련 Editor 슬롯 계약도 같은
  이름으로 통일했다.
- 외부 `enemies model/KUŠkursa dizzy.fbx`는 수정하지 않고 프로젝트 전용
  `Kursa_PostBreakRecovery_Source.fbx` 복사본으로 가져왔다.
- 가져온 FBX의 유일한 Mixamo 테이크를 Generic 애니메이션으로 설정하고
  루프 재생하도록 6번 슬롯에 적용했다. 루트 모션은 사용하지 않는다.
- 외형은 정적 쿠르사의 메시·UV·스킨·본 순서·머티리얼 슬롯과 렌더러
  설정을 그대로 복제했다. 검증된 방패 전용 서브메시 3, 8만 비운 별도
  메시를 사용해 6번 모델의 왼팔 방패만 제거했다.
- Unity 적용 계약에서 다른 11개 쿠르사 슬롯과 다른 장면 루트가 바뀌지
  않았으며 `CargoRunMvp` 장면 저장까지 통과했다.
- 정면과 40도 사선의 전체 루프 표본을 직접 확인해 6번 전 구간에서 방패가
  없고 본체 외형·크기·관절·메시가 안정적으로 유지되는 것을 확인했다.
- 최종 이미지:
  `docs/validation/kursa_post_break_recovery_fbx_2026-08-04/Kursa_PostBreakRecoveryFbx_FinalReview.png`
- 이번 단계는 사용자가 지정한 1차 적용까지만 수행했다. 방패 재생성 및
  기절 이후의 추가 행동, 원본 FBX와 정적 쿠르사 수정, 다른 슬롯의 모델·
  애니메이션 수정, 게임플레이·AI·물리, 빌드, `Run-HarnessValidation.ps1`
  하네스 검증 및 Git 작업은 실행하지 않았다.

### 쿠르사 6번 정적 자세 복귀 및 방패 재생성

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 기존 Mixamo 기절 동작을 그대로 보존한 뒤, 종료 자세에서 정적 쿠르사의
  대응 본 자세로 0.3초 동안 복귀하는 전용 반복 재생 클립을 생성했다.
- 자세 복귀가 끝난 시점부터 정적 쿠르사의 실제 방패 서브메시 3, 8과 두
  머티리얼을 그대로 추출한 새 방패를 `LeftHand` 본에 생성한다.
- 새 방패의 기하학적 중심을 생성 원점으로 사용해 0.5초 동안 크기 0에서
  원래 크기까지 서서히 확장되도록 했다.
- 방패가 완성된 정적 자세를 1초 동안 유지한 뒤 방패가 없는 Mixamo 기절
  동작 시작부터 반복한다.
- 정면 및 40도 사선 전체 루프 표본을 직접 확인해 기절 구간의 방패 부재,
  정적 자세 복귀, 중앙부터의 방패 확장, 원래 위치·방향·크기와 머티리얼,
  완성 상태 유지 및 반복을 확인했다.
- Unity 적용 계약에서 다른 11개 쿠르사 슬롯과 다른 장면 루트는 변경되지
  않았으며 `CargoRunMvp` 장면 저장까지 통과했다.
- 최종 이미지:
  `docs/validation/kursa_post_break_recovery_regeneration_2026-08-04/Kursa_PostBreakRecoveryFbx_FinalReview.png`
- 외부 원본 FBX와 정적 쿠르사 수정, 다른 슬롯의 모델·애니메이션 수정,
  게임플레이·AI·전투·물리, 빌드, `Run-HarnessValidation.ps1` 하네스 검증
  및 Git 작업은 실행하지 않았다.

### 이슈판트 무장 모델 12개 배치

상태: `DIRECT_VISUAL_REVIEW_PASSED`

- 외부 `enemies model/išpant-armed.fbx`는 수정하지 않고 프로젝트 전용
  `Assets/_Project/Art/Enemies/Ispant/Models/Ispant_Armed.fbx` 복사본으로
  가져왔으며 원본과 복사본의 SHA-256이 일치한다.
- `CargoRunMvp`에 `Approved Ispant Enemy Placement` 루트를 만들고
  `Ispant_01`부터 `Ispant_12`까지 12개의 정적 FBX 인스턴스를 배치했다.
- 이슈판트 행은 쿠르사 행의 아래쪽 Z 방향에 놓았고, 행간은 현재 롱가
  아르마와 테르고 사이의 Z 간격을 그대로 사용했다. X축 배열 간격은
  현재 쿠르사 첫 두 슬롯 사이의 실제 간격을 그대로 사용했다.
- 모델은 기획 기준 신장 180cm에 맞춰 지면에 정렬했으며, Animator와
  Animation은 비활성화해 이번 단계에서 애니메이션을 적용하지 않았다.
- Unity 적용 및 장면 계약 점검에서 12개 직접 FBX 인스턴스, 지면 정렬,
  정적 상태, 원본 해시와 기존 장면 루트 불변을 확인하고 장면을 저장했다.
- 진단 화면과 최종 화면을 직접 확인해 기존 롱가 아르마·테르고·쿠르사
  행 아래에 이슈판트 행이 분리되어 있고, 12개가 누락·겹침 없이 같은
  방향·크기·간격으로 나열되며 개별 부유 현상이 없는 것을 확인했다.
- 최종 이미지:
  `docs/validation/ispant_armed_placement_2026-08-04/Ispant_Armed_Placement_FinalReview.png`
- 이슈판트 외형·머티리얼·텍스처·애니메이션 제작 또는 변경, 기존 적 배치와
  Player 수정, 게임플레이·AI·물리, 빌드, `Run-HarnessValidation.ps1`
  하네스 검증 및 Git 작업은 실행하지 않았다.

### 이슈판트 정면 Player 시작 화면

상태: `DIRECT_VISUAL_REVIEW_PASSED_WITH_DIAGNOSTIC_EVIDENCE`

- `Player` 시작점을 이슈판트 12개 행의 정면 중앙으로 이동하고 Player와
  실제 자식 카메라가 행 중앙을 바라보도록 회전을 저장했다.
- 현재 Player 카메라의 로컬 오프셋과 시야각을 사용해 16:9 화면에 12개
  전체가 들어오는 거리를 계산했다. 관련 역할은
  `IspantPlacementEditor`의 `MinimumPlayerDistance`, `CameraMargin` 및
  `PlayerDistance`에 기록했다.
- 적용 전후에 Player를 제외한 모든 장면 루트의 계층과 Transform이 같은지
  비교했으며, 이슈판트와 기존 적 행은 변경되지 않았다.
- 실제 시작 화면을 직접 확인해 이슈판트 12개가 최전방에서 왼쪽부터
  오른쪽까지 누락·잘림 없이 보이고, 행 중앙을 기준으로 정면을 향하는 것을
  확인했다. 뒤에 보이는 작은 개체는 변경하지 않은 기존 적 행이다.
- 실제 Player 카메라의 후처리 노출이 연속 수동 렌더에서 달라져 최초 최종
  캡처가 검게 생성됐다. 이 파일은
  `Ispant_PlayerStart_FinalReview_FAILED.png`로 명확히 표시했고 최종 캡처는
  한 번만 허용하는 규칙에 따라 재실행하지 않았다.
- 같은 Player 위치·회전·시야각을 복사한 임시 검토 카메라로 두 번째 진단
  화면을 안정적으로 재현했다. 임시 카메라 정리 시 RenderTexture 연결 해제
  순서를 수정한 뒤 공식 Unity 리프레시가 통과했다.
- 직접 확인에 사용한 유효 이미지:
  `docs/validation/ispant_player_start_2026-08-04/Ispant_PlayerStart_Diagnostic_02.png`
- 이슈판트와 기존 적의 모델·배치·머티리얼·애니메이션 수정, 게임플레이·
  AI·물리, 빌드, `Run-HarnessValidation.ps1` 하네스 검증 및 Git 작업은
  실행하지 않았다.
