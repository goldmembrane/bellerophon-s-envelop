# PROGRESS 2026-07-16

## 세션 종료 인계 상태 - Grave 공격 모션 미승인 및 재작업 필요

- 현재 `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`은 사용자가 승인했던 공격 모션이 아니다. 사용자가 현재 결과와 두 차례 롤백 결과를 모두 명시적으로 거부했으므로 승인 완료 상태로 취급하면 안 된다.
- 승인된 공격 클립을 별도 복사하거나 전체 커브 스냅샷으로 고정하지 않은 채 생성 코드가 같은 `.anim`에 `clip.ClearCurves()`를 실행하고 49개 커브를 반복 재생성했다. 실제 화면의 Animator는 이전 커브를 캐시한 동안 저장 클립과 다른 모션을 보여줬고, Unity 재시작 뒤 캐시가 사라지면서 승인 모션 유실이 드러났다.
- 기존 Apply·Validate는 저장 클립을 별도로 샘플링했을 뿐 실제 공격 슬롯 Animator의 재생 상태와 저장 상태가 같은지 확인하지 않았다. 이후 `AnimatorController` 참조 해제·재할당, `Rebind`, 상태 재진입, 즉시 평가 및 `LiveAnimatorBound` 검사를 추가했으나 이는 캐시 불일치 재발 방지이며 유실된 승인 모션 복원 완료를 의미하지 않는다.
- 현재 저장된 공격 모션은 `22:03 RestoredCurtainCall` 기록을 기반으로 재구성한 상태이고 Apply·Validate 수치는 통과하지만 사용자가 시각적으로 승인하지 않았다. 다음 작업에서는 이 클립을 승인본으로 간주하지 말고 공격 모션을 다시 작업해야 한다.
- 재작업 시에는 기존 본 클립을 직접 덮어쓰지 않고 별도 작업 클립을 사용해야 한다. 사용자 시각 승인 뒤 승인본 `.anim` 복사본과 전체 커브 기준을 고정하고, 실제 Animator 재생 상태와 저장 클립 일치를 확인한 뒤에만 본 클립으로 교체해야 한다.
- Grave 피격 모션은 `Grave_Hit_Recoil` 반복 클립과 지정 슬롯 적용·검증까지 진행됐다. 공격 모션 유실 문제와 별개이며 오늘 문서의 해당 절에 수치와 범위가 기록돼 있다.
- `ScriptableSingleton already exists` 오류는 Unity 재시작 뒤 신규 발생 0건으로 확인했으며, 반복된 punctual light shadow atlas 항목은 오류가 아니라 별도 경고로 남겼다.
- 이 인계 기록 뒤 추가 애니메이션 수정, Unity 실행·재시작·검증, 새 캡처는 수행하지 않는다.
- 실행하지 않은 항목: 하네스 관련 테스트 검증, EditMode·PlayMode 테스트, 빌드, 강제 푸시, Git 이력 재작성.

## Grave 공격 슬롯 Animator 캐시 재바인딩

- 공격 클립 생성·적용 뒤 화면의 모션과 저장된 `.anim`이 달라질 수 있었던 원인을 확인했다. Apply는 같은 `AnimatorController` 참조를 다시 대입하고 저장했지만 `Animator.Rebind`, 상태 재진입, 즉시 평가를 하지 않았고, Validate는 실제 Animator 대신 클립을 `SampleAnimation`으로 별도 측정했다.
- `ApplyApprovedGraveCurtainCallAttack`이 지정 공격 슬롯 Animator를 비활성화하고 컨트롤러 참조를 해제한 뒤 저장 컨트롤러를 다시 할당하도록 변경했다. 이어서 `Rebind`, `CurtainCallAttack` 0초 재진입, `Update(0)`을 실행하고 실제 현재 클립이 저장된 공격 클립인지 검사한다.
- 첫 Apply는 재바인딩 직후에는 성공했지만 내부 측정 함수가 Animator를 비활성화했다가 복구한 뒤 live state가 초기화되어 최종 검사에서 중단됐다. Validate 시작과 모든 측정 종료 뒤에도 재바인딩하도록 수정했다.
- 최종 `ApplyApprovedGraveCurtainCallAttack`과 독립 `ValidateApprovedGraveCurtainCallAttack`이 모두 `LiveAnimatorBound=True`로 통과했다. 지정 공격 슬롯의 실제 Animator 상태와 저장된 공격 클립 참조가 일치한다.
- 공격 커브 수치는 현재 복구 기준을 유지했다. 최고 속도 `9.047625m/s`, 평균 `6.453618m/s`, 팔꿈치 굽힘 `69.85064도`, 최종 커튼콜 자세 오차 `0`, 몸통과 겹친 오른팔 표본 `0개`다.
- Unity C# 컴파일 오류는 `0건`이다.
- 결과 로그:
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ApplyLiveAnimatorRebind_Final.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ValidateLiveAnimatorRebind_Final.log`
- 실행하지 않은 항목: 새 캡처, Unity 재시작, 하네스 관련 테스트 검증, EditMode·PlayMode 테스트, 빌드, 다른 Grave 모션·모델·메시·머티리얼·전투 로직 변경, Git 작업.

## Grave 실제 재시작 전 승인 공격 모션 롤백

- 대상은 `Approved Grave Enemy Placement/Grave_03_Attack_RightArm_GiantSweep/Grave_Model` 하나로 한정했다.
- Unity 재시작은 `22:31`에 실행됐다. 사용자가 재시작 전에 공격 작업을 끝내도 된다고 확인한 마지막 Apply·Validate는 `22:02~22:03`의 `RestoredCurtainCall`이었다. 앞선 롤백에서 잘못 기준으로 삼은 `DirectAcceleratedCurtainCall`은 재시작 뒤인 `22:56~22:57`에 생성된 상태이므로 롤백 기준에서 제외했다.
- `22:03` 승인 상태의 `1.65초`, `2.00초` 몸통·목·머리·양어깨·양팔 중간 키를 복원했다. 오른팔은 `1.20초` 옆 전개 뒤 `1.28~1.58초`에 기존 하강 가이드를 거쳐 굽힌 커튼콜 자세로 이어진다.
- 해당 시점의 오른팔 2구간 보간과 속도 균일도 검증을 복구하고, 재시작 뒤 추가됐던 직접 세제곱 보간은 제거했다.
- 재시작 뒤 사용자가 별도로 승인한 “마지막에 오른팔이 솟지 않고 커튼콜 자세로 끝남”은 유지했다. 따라서 공격 본동작은 `22:03` 상태와 일치하면서 마지막 프레임은 커튼콜 자세를 유지한다.
- 낫 BlendShape는 옆 전개 완료부터 공격 마지막 프레임까지 `100`을 유지하고 공격 평가 종료 뒤 `0`으로 복구된다.
- `Refresh-UnityProject.ps1`, `ApplyApprovedGraveCurtainCallAttack`, `ValidateApprovedGraveCurtainCallAttack`을 실행했고 Apply와 저장 상태 Validate가 모두 통과했다.
  - 재시작 전 승인 로그와 동일한 중간 하강 `0.142262m`, 하강 `0.2129m`, 안쪽 이동 `1.133095m`, 전방 이동 `0.53817m`.
  - 재시작 전 승인 로그와 동일한 최저 속도 `4.282364m/s`, 평균 `6.453618m/s`, 최고 `9.047625m/s`, 속도 균일도 `0.66356`.
  - 경로 효율 `0.593161`, 팔꿈치 굽힘 `69.85064도`, 최종 커튼콜 자세 오차 `0`.
  - 몸통과 겹친 오른팔 표본 `0개`(`TorsoFrontClearance=1`), 낫 정면 방향 최솟값 `0.426015`.
  - 팔 스케일 오차 `0`, 모델·슬롯 root motion `0`, Unity C# 컴파일 오류 `0건`.
- 결과 로그:
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ApplyRollbackToAcceptedPreRestartCurtainCall.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ValidateRollbackToAcceptedPreRestartCurtainCall.log`
- 실행하지 않은 항목: 새 캡처, Unity 재시작, 하네스 관련 테스트 검증, EditMode·PlayMode 테스트, 빌드, 다른 Grave 모션·모델·메시·머티리얼·전투 로직 변경, Git 작업.

## Grave 옆 전개 후 직접 가속 커튼콜 공격

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-16 22-48-23.mp4`는 `1920x1000`, 약 `4.254초`였다. 영상에서 오른팔 옆 전개 뒤 동작이 커튼콜이 아니라 정면을 후려치는 스윕처럼 보이는 문제를 확인했다.
- 원인은 오른팔 가속 커브가 기존 `1.65초` 하강 자세를 중간 경유점으로 사용하고, 상체·머리·왼팔은 `2.00초` 정면 스윕 자세까지 계속 움직이던 점이었다. 따라서 옆 전개에서 커튼콜로 한 번에 이어지지 않았다.
- `1.65초`와 `2.00초`의 12개 공격 본 자세를 기존 `2.35초` 커튼콜 자세와 같게 만들어 잔여 스윕과 반동을 제거했다. 오른팔은 `1.20초`에 옆으로 완전히 전개된 뒤 `1.28초`까지 유지하고, `1.28~1.58초` 동안 옆 전개 자세에서 커튼콜 자세로 직접 이동한다.
- 중간 하강 가이드 없이 옆 전개 회전과 커튼콜 회전을 직접 보간한다. 보간 진행률에 세제곱 가속을 적용해 초반 움직임을 억제하고 하강 후반에 속도가 확실히 붙도록 했다.
- 전용 확인 기준은 최고/평균 속도 비 `1.8배` 이상, 직접 경로 효율 `0.65` 이상을 요구한다. 이전의 일정 속도 비율 기준은 가속 동작에 맞지 않아 제거했다.
- `Refresh-UnityProject.ps1`, 지정 공격 슬롯 대상 `ApplyApprovedGraveCurtainCallAttack`, 저장 상태 `ValidateApprovedGraveCurtainCallAttack`을 실행했고 Apply와 Validate가 동일하게 통과했다.
  - 베기 최고속도 `11.43243m/s`, 평균속도 `5.88149m/s`, 최고/평균 속도 비 `1.943798`.
  - 직접 경로 효율 `0.71182`, 커튼콜 자세 유지 편차 `0`, 마지막 커튼콜 회전 오차 `0`.
  - 옆 전개 거리 `0.666412m`, 전개 상승 `0.715041m`, 커튼콜 하강 `0.245955m`, 안쪽 이동 `1.158791m`.
  - 팔꿈치 굽힘 `69.85027도`, 몸통과 겹친 오른팔 표본 `0개`(`TorsoFrontClearance=1`).
  - 낫 BlendShape는 옆 전개 완료·가속 하강·마지막 프레임 `100`, 공격 평가 종료 뒤 `0`; 모델·슬롯 root motion `0`.
- Unity 로그에서 C# 컴파일 오류 `0건`을 확인했다.
- 주요 결과물:
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 지정 공격 슬롯 저장
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ApplyDirectAcceleratedCurtainCall_Strong.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ValidateDirectAcceleratedCurtainCall_Final.log`
- 캡처, Unity 재시작, 피격 모션·다른 Grave 애니메이션, 낫 메시 형상·모델·머티리얼·전투 로직, 하네스 관련 테스트 검증, EditMode·PlayMode 테스트, 빌드, Git은 실행하거나 변경하지 않았다.

## Grave 공격 종료의 커튼콜 자세 유지

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-16 22-33-49.mp4`를 확인했다. 영상 중앙 개체는 피격 슬롯이 아니라 `Grave_03_Attack_RightArm_GiantSweep`였으며, 커튼콜 도착 뒤 오른팔이 다시 위로 복귀하는 종료 구간이 보였다.
- 원인은 `Grave_Attack_CurtainCall_Sweep`의 `2.35초` 커튼콜 키 뒤에 `2.65초` 중간 복귀 키와 `3.00초` 기본 자세 키가 남아 있던 점이었다.
- `Hips`, 상체, 머리, 양팔을 포함한 기존 12개 공격 본의 `2.65초`와 `3.00초` 값을 `2.35초` 커튼콜 값과 동일하게 변경했다. 급강하·베기 구간, 낫 BlendShape 형상과 전개 타이밍은 변경하지 않았다.
- 새 `curtainCallRotations` 기준값은 `2.35초`에 샘플링한 12개 본 회전을 저장하며, `finalCurtainCallPoseError`는 마지막 프레임이 이 기준에서 벗어나는지를 확인한다. 기존 시작·종료 동일 검사는 이 커튼콜 종료 유지 검사로 교체했다.
- `Refresh-UnityProject.ps1`, 지정 공격 슬롯 대상 `ApplyApprovedGraveCurtainCallAttack`, 저장 상태 `ValidateApprovedGraveCurtainCallAttack`을 실행했다. Apply와 Validate가 동일하게 통과했다.
  - 마지막 프레임 커튼콜 회전 오차 `0`, 커튼콜 위치 유지 편차 `0.000002m`.
  - 팔꿈치 굽힘 `69.85064도`, 몸통 투영 범위에 들어온 오른팔 표본 `0개`(`TorsoFrontClearance=1`).
  - 베기 속도 최저 `4.282364m/s`, 평균 `6.453618m/s`, 최고 `9.047625m/s`로 기존 급강하를 유지했다.
  - 낫 BlendShape는 마지막 프레임 `100`, 공격 평가 종료 뒤 `0`; 모델·슬롯 root motion `0`.
- Unity 로그에서 C# 컴파일 오류 `0건`을 확인했다. 캡처, Unity 재시작, 피격 모션·다른 Grave 애니메이션, 모델·메시·머티리얼·전투 로직, 하네스 관련 테스트 검증, EditMode·PlayMode 테스트, 빌드, Git은 실행하거나 변경하지 않았다.
- 수정 범위:
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 지정 공격 슬롯 저장
  - `docs/PROGRESS_2026-07-16.md`

## Unity EditorToolsSettingsData ScriptableSingleton 중복 오류 복구

- 사용자 콘솔 이미지의 빨간 오류 `ScriptableSingleton already exists. Did you query the singleton in a constructor?`를 조사했다. 함께 반복된 `Reduced additional punctual light shadows resolution...` 항목은 별개의 그림자 아틀라스 경고이며 이번 수정 대상에서 제외했다.
- 재시작 전 현재 Unity Editor 로그에는 동일 Singleton 오류가 `56회` 기록돼 있었다. 프로젝트 Editor 코드에는 `ScriptableSingleton`, `EditorToolsSettingsData`, `EditorToolManager` 직접 조회가 없었다.
- 최초 전체 스택은 Animation Rigging `1.4.1`의 `BoneRendererUtils` 정적 생성자 `BoneRendererUtils.cs:121`이 도메인 리로드 중 `Tools.visibleLayers`를 조회하면서 `Tools → EditorPivotManager → EditorToolsSettingsData.instance`를 중첩 초기화하는 경로였다.
- 코드나 패키지 캐시를 수정하지 않고 승인된 최후 복구 경로 `scripts/Open-UnityProject.ps1 -Restart`를 사용해 Unity를 재시작했다. 새 프로세스에서 `D:\Bellerophon2\Bellerophon` 프로젝트와 `Assets/_Project/Scenes/CargoRunMvp.unity`가 정상적으로 다시 열렸다.
- 재시작 직후 `ScriptableSingleton already exists` 신규 발생 `0회`, C# 컴파일 오류 `0회`를 확인했다. 이어서 `scripts/Refresh-UnityProject.ps1`을 실행한 뒤에도 Singleton 오류 `0회`, 컴파일 오류 `0회`를 다시 확인했다.
- Animation Rigging 패키지, `Library`, `Temp`, `Logs`, `UserSettings`, 프로젝트 코드, Grave 애니메이션, 씬 내용, 조명·그림자 설정은 수정하지 않았다.
- 하네스 관련 테스트·검증, EditMode·PlayMode 테스트, Windows 빌드, Git은 실행하지 않았다.

## Grave 한 대 맞는 상체 후방 피격 모션

- 대상은 `Approved Grave Enemy Placement/Grave_04_Hit_Recoil/Grave_Model` 하나로 한정했다. 기존에는 슬롯 이름만 있고 피격용 클립·컨트롤러·전용 적용 명령이 없었다.
- `Grave_Hit_Recoil`은 `1.1초` 반복 클립이다. `0.07초`부터 반응해 `0.18초`에 최대 후방 반동에 도달하고, 짧은 복원 오버슈트와 작은 잔여 흔들림을 거쳐 `1.1초`에 기본 자세로 완전히 복귀한 뒤 다시 재생된다.
- `Spine02`, `Spine01`, `Spine`, `neck`, `Head` 5개 본의 Quaternion 회전 커브 20개만 사용했다. 몸통은 먼저 뒤로 밀리고 목·머리는 약간 늦게 따라와 한 대 맞은 충격처럼 보이도록 구성했다.
- `Hips`, 양쪽 다리·발·발가락, 모든 본 스케일, 모델 및 슬롯 루트는 움직이지 않는다. 다른 Grave 슬롯과 기존 공격 모션은 변경하지 않았다.
- 전용 `ApplyApprovedGraveHitRecoil`, `ValidateApprovedGraveHitRecoil`, `CaptureApprovedGraveHitRecoil` 명령과 열린 `CargoRunMvp` 씬의 지정 슬롯만 대상으로 하는 검증을 추가했다.
- Apply 내부 확인과 저장 상태 독립 Validate가 동일 수치로 통과했다.
  - 최대 반동 시각 `0.18초`.
  - 몸통 후방 이동 `0.103388m`, 머리 후방 이동 `0.16323m`, 상체 기울기 `22.29972도`.
  - 하체 회전 오차 `0`, 본 스케일 오차 `0`, 지면 관통 `0`.
  - 종료 자세 회전 오차 `0`, 종료 위치 오차 `0`, 모델·슬롯 root motion `0`.
  - 클립 길이 `1.1초`, `WrapMode.Loop`, 반복·Loop Blend 활성, 커브 `20`.
- 첫 적용 뒤 사용자가 재생되지 않는다고 확인한 원인은 `WrapMode.ClampForever`, `LoopTime=0`, `LoopBlend=0`으로 한 번 재생 후 마지막 프레임에 정지하던 설정이었다. 모션 키와 타이밍은 유지하고 세 반복 설정을 활성화해 `HitRecoil` 기본 상태에서 계속 재생되도록 재적용했다. 반복 상태 독립 검증은 `LoopTime=True`, `LoopBlend=True`, 시작·종료 회전 및 위치 오차 `0`으로 통과했다.
- 관련 확인 통과 뒤 최종 캡처를 승인된 한 번만 실행했다. `Grave_Hit_Recoil_Review.png`는 정면 직교 시트여서 후방 깊이 이동은 크게 드러나지 않지만 하체 고정과 시작·종료 자세 일치를 확인할 수 있다. 카메라 변경이나 추가 캡처는 실행하지 않았다.
- 생성·수정 범위:
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Hit_Recoil.anim`
  - `Assets/_Project/Art/Enemies/Grave/Controllers/Grave_Hit_Recoil.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 지정 피격 슬롯 Animator 참조
  - `docs/validation/grave_hit_recoil_2026-07-16/`
  - `docs/PROGRESS_2026-07-16.md`
- Unity 재시작, 하네스 관련 테스트·검증, EditMode·PlayMode 테스트, Windows 빌드, 다른 Grave 애니메이션, AI·이동·물리·피해·전투 로직, FBX·Blender·리그·본 가중치·메시·UV·머티리얼·텍스처·VFX, 실제 카메라 설정, Git은 실행하거나 변경하지 않았다.

## Grave 기존 커튼콜 자세 복원 및 오른팔 겹침 방지

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-16 21-54-13.mp4`를 확인했다. 영상은 `1920x1000`, 약 `5.301초`, H.264이며 `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/user_capture_215413_contact_01.jpg` 접촉 시트에서 커튼콜 마무리 때 오른팔 전체가 곧게 뻗는 문제를 확인했다.
- 원인은 이전 뒤틀림 방지 작업에서 커튼콜 위팔과 아래팔 목표 방향을 각각 `(-0.58,-0.64,0.50)`, `(-0.65,-0.55,0.52)`로 거의 평행하게 정렬해 원래의 팔꿈치 굽힘을 없앤 점이었다.
- 커튼콜 목표 방향을 기존 값인 위팔 `(-0.45,-0.86,0.24)`, 아래팔 `(-0.82,0.18,0.54)`로 정확히 복원했다. 메시 뒤틀림의 원인이던 프레임별 자동 축 비틀기 코드는 다시 추가하지 않았고, `1.28~1.58초` 연속 고속 베기와 `1.58~2.35초` 커튼콜 자세 유지도 보존했다.
- 직선 자세가 다시 생성되지 않도록 공격 구간 최대 팔꿈치 굽힘을 `55~90도`로 요구하고, 몸통 좌우·상하 투영 범위에 들어오는 아래팔·손 정점은 몸통 앞면과 최소 `0.015m` 이상 떨어지도록 함께 검사한다.
- Apply 내부 확인과 저장 상태 독립 `ValidateApprovedGraveCurtainCallAttack`가 동일 수치로 통과했다.
  - 커튼콜 팔꿈치 굽힘 `69.85064도`, 최종 팔꿈치 몸통 기준 좌우 `0.046371m`·앞쪽 `0.127831m`.
  - 최종 손 몸통 기준 왼쪽 `0.231796m`·앞쪽 `0.311012m`·아래쪽 `0.225776m`.
  - 몸통 투영 범위에 아래팔·손 정점이 들어온 표본 `0개`(`TorsoFrontClearance=1` sentinel).
  - 베기 속도 최저 `4.282364m/s`, 평균 `6.453618m/s`, 최고 `9.047625m/s`, 속도 균일도 `0.66356`.
  - 낫날 정면 정렬 최저 `0.426015`, 평균 `0.586559`; BlendShape는 옆 전개부터 마지막 프레임까지 `100`, 공격 뒤 `0` 복구.
  - 커브 `49`, 본 스케일 오차 `0`, 반복 자세 오차 `0`, 모델·슬롯 root motion `0`, 비오른팔 정점 변화 `0`.
- 컴파일 오류는 발생하지 않았다. 관련 확인 통과 뒤 최종 캡처를 승인된 한 번만 실행했으나 기존 `Model Cam` 구도에서 대상이 매우 작고 가려져 시각 품질 근거로 사용하지 않는다. 카메라 변경이나 추가 캡처는 실행하지 않았다.
- 주요 결과물:
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ApplyRestoredCurtainCall_FirstPass.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ValidateRestoredCurtainCall_Final.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/CaptureRestoredCurtainCall_Final.log`
- Unity 재시작, 하네스 관련 테스트·검증, 다른 테스트·빌드, 다른 Grave 슬롯, AI·이동·물리·피해·전투 로직, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX, 승인된 낫 형상·메시 변형, 지정 슬롯 밖 씬 오브젝트, 실제 카메라 설정, Git은 실행하거나 변경하지 않았다.

## Grave 공격 오른팔 아래쪽 반달 칼날 변형

- 대상은 `Approved Grave Enemy Placement/Grave_03_Attack_RightArm_GiantSweep/Grave_Model`의 기존 `Grave_Attack_CurtainCall_Sweep` 공격 하나로 한정했다.
- 기존 3초 공격 동작, 12개 본의 Quaternion Transform 커브 48개, 타이밍, 반복 설정, 타격 궤적은 유지했다. 적용 직전 커브의 키·값·접선·가중치를 저장하고 재생성 뒤 모두 같음을 코드에서 비교하도록 구성했다.
- 승인된 `Grave_Body` 메시를 기반으로 `Grave_Attack_CrescentBlade_Body.asset`을 생성하고 `GraveRightArmLowerCrescentBlade` BlendShape 한 개를 추가했다.
- 오른팔·오른쪽 전완·오른손의 스킨 가중치 합이 `0.45` 이상인 정점만 변형 대상으로 삼았다. 옆 전개 자세에서 팔 중심선과 실제 단면의 상단·하단을 계산해 위쪽은 칼등으로 유지하고 아래쪽만 반달 곡선으로 확장했다.
- BlendShape 커브는 `0초=0`, `0.35초=0`, `0.85초=55`, `1.20초=100`, `3.00초=100`이다. 오른팔을 옆으로 뻗는 동안 형성되고 공격 마지막 프레임까지 유지된다.
- 공격 전용 Inspect/Apply/Validate/Capture 명령은 열려 있는 `CargoRunMvp` 활성 씬만 사용하도록 변경했다. `OpenSceneMode.Single`로 씬 전체를 다시 열지 않고 지정 공격 슬롯만 읽고 수정한다.
- 첫 컴파일에서 곡선 비교용 `Keyframe` API 이름 오류를 확인해 이번 추가 코드 안에서 `inTangent/outTangent`로 수정했다.
- 칼날 최대 깊이 적용 시도는 저장 전 두 번 중단됐다.
  - 1차: 대상 정점 `665`, 최대 깊이 `0.129889m`.
  - 2차: 대상 정점 `665`, 최대 깊이 `0.279524m`.
  - 팔 단면 정규화와 깊이 상수 `0.52m` 적용 뒤 최종 형상을 만들었다.
- Apply 내부 확인과 저장 상태 Validate가 동일한 수치로 연속 통과했다.
  - 전체 커브 `49`: 기존 Transform `48` + 새 BlendShape `1`.
  - 칼날 아래쪽 최대 이동 `0.33018m`, 위쪽 돌출 `0m`.
  - 비오른팔 정점 변화 `0m`, 실제 변형 정점 `660`.
  - BlendShape 값 시작 `0`, 완전 전개 `100`, 마지막 프레임 `100`.
  - 기존 동작 수치: 옆 전개 `0.666412m`, 전개 상승 `0.715041m`, 중간 하강 `0.466215m`, 전체 하강 `0.657076m`, 안쪽 이동 `0.522812m`, 정면 변화 `0.173431m`, 하강 우세 비율 `3.788684`.
  - 기존 마무리 수치: 손 좌우 `-0.231796m`, 정면 `0.320219m`, 전완 왼쪽 도달 `0.278168m`, 왼쪽 우세 비율 `1.430983`.
  - 본 스케일 오차 `0`, 루프 본 자세 오차 `0`, 모델·슬롯 root motion `0`, 지면 관통 `0.00336m`.
- 전용 확인 통과 뒤 최종 캡처를 승인된 한 번만 실행했다. 다만 생성된 `Grave_CurtainCall_Attack_Review.png`는 렌더 직전 스킨 행렬 갱신 문제가 남아 9개 프레임이 기본 자세처럼 기록됐으므로 이번 반달 칼날의 시각 근거로 사용하지 않는다. 추가 캡처는 실행하지 않았다.
- 생성·수정 범위:
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Grave/Models/Grave_Attack_CrescentBlade_Body.asset`
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 지정 공격 슬롯 메시 참조
  - `docs/validation/grave_crescent_blade_attack_2026-07-16/`
  - `docs/PROGRESS_2026-07-16.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `InspectApprovedGraveAttackRig`, `ApplyApprovedGraveCurtainCallAttack`, 저장 상태 `ValidateApprovedGraveCurtainCallAttack`, 통과 뒤 `CaptureApprovedGraveCurtainCallAttack` 1회.
- 다른 공격 동작·클립·컨트롤러·Grave 슬롯·적대 개체·씬 루트, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX·AI·Collider·Rigidbody·피해·전투 로직·프로젝트 설정·Git은 수정하거나 실행하지 않았다.

## Grave 공격 오른팔 위쪽 갈고리·아래 칼날 및 순간 하강 가속

- 대상은 `Approved Grave Enemy Placement/Grave_03_Attack_RightArm_GiantSweep/Grave_Model`의 기존 `Grave_Attack_CurtainCall_Sweep` 공격 하나로 한정했다.
- 사용자의 방향 정정에 따라 갈고리 끝은 몸통 안쪽이 아니라 오른팔의 위쪽으로 말리게 했고, 팔의 앞·뒤가 아니라 위·아래 두께만 늘렸다. 칼날은 오른팔 아래쪽에 유지했다.
- 새 공격 전용 메시 `Grave_Attack_UpperHookBlade_Body.asset`과 BlendShape `GraveRightArmUpperHookLowerBlade`를 생성했다. 4본 가중 스키닝 행렬을 이용해 변형을 원본 정점 공간으로 역변환하여 앞·뒤 방향 오차를 제거했다.
- BlendShape는 기존과 같이 팔이 옆으로 완전히 전개되는 `1.20초`에 `100`이 되고 공격 종료 `3.00초`까지 `100`을 유지한다.
- 전체 공격 자세와 타이밍은 유지하되 `RightArm`과 `RightForeArm`의 `1.20~1.65초` 구간에만 `1.32초`, `1.42초` 가속 키를 추가했다. 다른 Transform 커브는 적용 전후 키·값·접선·가중치가 같음을 비교했다.
- 저장 전 검증은 아래 칼날 깊이 하나 때문에 두 번 중단됐다.
  - 1차: 아래 칼날 `0.196294m`, 앞·뒤 오차 `0.013585m`.
  - 2차: 4본 가중 역변환 적용 뒤 아래 칼날 `0.221774m`, 앞·뒤 오차 `0m`.
  - 반복 원인을 보고한 뒤 아래쪽 칼날 깊이만 `0.52m`에서 `0.78m`로 조정했다.
- Apply 내부 확인과 저장 상태 Validate가 통과했다.
  - 전체 커브 `49`: Transform `48` + BlendShape `1`.
  - 아래 칼날 `0.338003m`, 위쪽 두께 `0.088485m`, 위쪽 갈고리 상승 `0.345074m`.
  - 앞·뒤 변화 `0m`, 비오른팔 정점 변화 `0m`, 변형 정점 `771`.
  - 하강 최고속도 `3.424129m/s`, 평균속도 `1.036034m/s`, 순간/평균 속도 비 `3.305036`.
  - 본 스케일 오차 `0`, 루프 본 자세 오차 `0`, 모델·슬롯 root motion `0`, 지면 관통 `0.00336m`.
- 검증 통과 뒤 승인된 최종 캡처를 한 번만 실행했다. `docs/validation/grave_upper_hook_blade_accelerated_attack_2026-07-16/Grave_CurtainCall_Attack_Review.png`에서 9개 공격 시점의 서로 다른 실제 자세와 오른팔 갈고리·아래 칼날 유지, 하강 흐름을 확인했다.
- 생성·수정 범위:
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Grave/Models/Grave_Attack_UpperHookBlade_Body.asset`
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 지정 공격 슬롯 메시 참조
  - `docs/validation/grave_upper_hook_blade_accelerated_attack_2026-07-16/`
  - `docs/PROGRESS_2026-07-16.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `InspectApprovedGraveAttackRig`, `ApplyApprovedGraveCurtainCallAttack`, `ValidateApprovedGraveCurtainCallAttack`, 검증 통과 뒤 `CaptureApprovedGraveCurtainCallAttack` 1회.
- `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 로그·결과 확인·문서화는 실행하지 않았다.
- 다른 공격 클립·컨트롤러·Grave 슬롯·씬 루트, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX·AI·물리·피해·전투 로직·프로젝트 설정·Git은 수정하거나 실행하지 않았다.

### 공격 자세 Unity 표시 보완

- 사용자가 열린 Unity에서 변화가 보이지 않는다고 확인했다. 지정 슬롯에는 새 메시와 BlendShape가 연결돼 있었지만 에디터가 `EditMode`여서 공격 클립의 전개 자세가 화면에 평가되지 않은 것이 원인이었다.
- `InspectApprovedGraveAttackRig`를 지정 공격 전용 미리보기 겸 점검 명령으로 보완했다. 실행 시 공격 클립 `1.20초`를 대상 모델에 샘플링하고, 대상 오브젝트를 선택한 뒤 Scene View에 포커스한다.
- 최종 표시 상태에서 `Mesh=Grave_Attack_UpperHookBlade_Body`, `BladeIndex=0`, `BladeWeight=100`, `Controller=Grave_Attack_CurtainCall_Sweep`, `PreviewTime=1.2`를 확인했다.
- 미리보기는 씬에 저장하지 않았고 `PreviewSaved=False`, `SceneDirtyPreserved=True`를 확인했다.
- 미리보기 코드의 `ClearSceneDirtiness` API가 Unity 6.3에 없어 발생한 컴파일 오류 한 건은 해당 호출만 제거해 수정했다.
- 정체된 컴파일 상태를 해소하기 위해 승인 뒤 `Open-UnityProject.ps1 -Restart -ValidateCargoRunScene`를 한 번 실행했으나, 이후 사용자가 재시작 금지를 지시했다. 이후 작업에서는 Unity를 다시 재시작하지 않았다.
- 추가 캡처와 Play Mode, Harness/EditMode/PlayMode/Windows Build 검증은 실행하지 않았다.

### 오른팔 실제 갈고리 변형 및 순간 하강 가속 재수정

- 기존 구현은 팔 끝을 위로 이동시키기만 해 중심선이 되말리지 않았고, 하강은 `1.32초`에 이미 목표 자세의 `72%`에 도달해 순간 가속 전후가 시각적으로 분리되지 않았다. 사용자의 Unity 확인 결과를 기준으로 기존 완료 판단을 취소하고 재수정했다.
- 갈고리 변형은 안정된 모델 정면축을 기준으로 전완 진행축을 계산하고, 팔 끝을 위로 상승시킨 뒤 진행축 반대 방향인 위팔 쪽으로 되말도록 변경했다. 앞·뒤 축 성분은 투영에서 제거했다.
- 최종 형상 수치:
  - 아래 칼날 `0.338003m`
  - 위쪽 상승·두께 `0.312277m`
  - 갈고리 상승 `0.442085m`
  - 위팔 방향 되말림 최대 `0.449528m`
  - 갈고리 끝 진행축 `-0.131899m`: 끝이 위팔 방향으로 실제 역전됨
  - 기준 팔 대비 되말림 거리 `0.403946m`
  - 갈고리 끝 상승 `0.330903m`
  - 앞·뒤 변화 `0m`, 비오른팔 정점 변화 `0m`
- 하강은 `1.20~1.30초` 느린 준비, `1.30~1.36초` 급강하, `1.36~1.50초` 감속으로 분리했다. 급강하 최고속도 `7.752739m/s`, 직전 평균 `0.166209m/s`, 이후 평균 `0.115888m/s`, 전후 대비 급강하 비율 `46.64455`를 확인했다.
- BlendShape는 공격 시작 `0`, 옆 전개 완료 `100`, 급강하 `100`, 하강 완료 `100`, 공격 후반 `100`, 마지막 프레임 `100`이다. 다음 반복 또는 공격 종료 후 상태 전환에서 시작값 `0`으로 돌아간다.
- 정적 미리보기 자세가 다음 생성의 기준 자세로 섞이지 않도록 Apply 시작 시 승인된 Grave 리그의 정상 회전 자세를 복원한다. 비오른팔 커브 보존 검사는 하강 키 추가 직전과 직후의 생성 커브를 직접 비교한다.
- 편집 모드 미리보기는 3초 공격을 네 번 반복 재생하고 종료 뒤 정상 팔로 복구한다. `RestoredNormalArm=True`, `PreviewSaved=False`, `SceneDirtyPreserved=True`를 확인했다.
- 승인된 교체 캡처 1회에서는 되말림 상수 `0.98m`의 과보정으로 거대한 각진 고리가 확인됐다. 해당 상태를 완료로 처리하지 않고 최종 상수를 `0.46m`로 줄였으며, 추가 캡처는 실행하지 않았다. 따라서 현재 캡처 이미지는 최종 균형 형상의 시각 근거로 사용하지 않는다.
- `Refresh-UnityProject.ps1`, 공격 전용 Apply·Validate, 편집 모드 반복 미리보기와 승인된 교체 캡처 1회만 실행했다. Unity 재시작은 이번 재수정 범위에서 실행하지 않았다.
- `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 각 검증의 실행·재실행·실패 조사·로그 확인·결과 문서화·관련 생성물 작성은 실행하지 않았다.
- 다른 Grave 슬롯·공격 클립·컨트롤러, 지정 슬롯 밖의 씬 오브젝트, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX·AI·물리·피해·전투 로직·프로젝트 설정·Git은 변경하지 않았다.

## Grave 공격 오른팔 낫날 변형

- 사용자의 변경 지시에 따라 기존 위팔 방향 갈고리 되말림을 제거하고, 오른팔 진행 방향으로 이어지는 단일 낫날 형태로 교체했다.
- 새 공격 전용 메시 `Grave_Attack_ScytheBlade_Body.asset`과 BlendShape `GraveRightArmScytheBlade`를 생성해 지정 공격 슬롯에만 연결했다.
- 낫날은 기존 오른팔 길이를 주축으로 사용한다. 중간 아래쪽은 깊은 초승달형 절삭날, 위쪽은 두꺼운 칼등, 끝부분은 위로 들리면서 세로 단면이 가늘어지는 형태다. 위팔 방향 되말림은 적용하지 않았다.
- 최종 형상 수치:
  - 아래 절삭날 깊이 `0.339647m`
  - 위쪽 칼등 두께 `0.054986m`
  - 날끝 최대 상승 `0.285452m`
  - 진행축 추가 연장 `0.108458m`
  - 최종 진행축 길이 `0.391485m`: 갈고리처럼 역전되지 않고 바깥쪽으로 진행
  - 기준 팔 대비 연장 `0.099912m`
  - 날끝 평균 상승 `0.239998m`
  - 날끝 두께 비 `0.454271`: 끝으로 갈수록 가늘어짐
  - 앞·뒤 변화 `0m`, 비오른팔 정점 변화 `0m`, 변형 정점 `771`
- 첫 적용은 날끝 두께 비 `3.048007`, 지면 관통 `0.288092m`로 중단됐다. 추가 연장을 줄이고 날끝 구간의 절삭 깊이와 칼등 두께를 함께 감쇠했다.
- 두 번째 적용은 지면 관통이 `0.00336m`로 복구됐지만 날끝 판정이 `2.344378`로 반복 실패했다. 두 실패 수치와 원인을 보고한 뒤, 곡선 상승까지 두께로 합산하던 넓은 판정 구간을 실제 끝단 `progress ≥ 0.92`로 좁히고 테이퍼를 앞당겼다.
- 최종 Apply와 저장 상태 Validate가 동일한 수치로 통과했다. 지면 관통 `0.00336m`, 본 스케일 오차 `0`, 모델·슬롯 root motion `0`이다.
- 기존 변형 타이밍과 순간 하강 가속은 유지했다. BlendShape는 공격 시작 `0`, 전개 완료 `100`, 급강하 `100`, 하강 완료 `100`, 공격 후반 `100`, 마지막 프레임 `100`이다. 급강하 최고속도 `7.752739m/s`, 직전 `0.166209m/s`, 이후 `0.115888m/s`다.
- 검증 통과 뒤 승인된 최종 캡처를 한 번 실행했다. `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/Grave_CurtainCall_Attack_Review.png`에서 정상 팔이 전개 중 낫날로 변하고, 아래 절삭 곡선과 가늘게 치켜올라간 날끝이 형성되며 공격 마지막까지 유지되는 것을 확인했다.
- 실행한 명령은 `Refresh-UnityProject.ps1`, 공격 전용 Apply·Validate, 편집 모드 미리보기, 최종 캡처 1회와 승인 범위 diff 확인이다. Unity 재시작은 실행하지 않았다.
- `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 각 검증의 실행·재실행·실패 조사·로그 확인·결과 문서화·관련 생성물 작성은 실행하지 않았다.
- 다른 Grave 슬롯·공격 클립·컨트롤러, 지정 슬롯 밖 씬 오브젝트, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX·AI·물리·피해·전투 로직·프로젝트 설정·Git은 변경하지 않았다.

### 사용자 캡처 영상 기준 낫날 평면 재구성

- 사용자 지정 MP4 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-16 17-05-18.mp4`를 확인했다. 영상은 `1920x1000`, 약 `6.85초`, H.264이며 원본은 수정하지 않았다.
- `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/user_capture_review/`에 4fps 접촉 시트와 전개·하강 구간 확대 프레임을 검토 자료로 추출했다.
- 영상에서 기존 결과가 낫날이 아니라 둥근 팔 부피와 굴곡이 남은 뾰족한 촉수처럼 보이는 것을 확인했다. 기존 완료 판단을 취소했다.
- 원인은 위·아래 오프셋만 적용하고 원래 팔의 앞·뒤 원통형 단면과 곡면 노멀을 유지한 것이었다. 팔 단면 전체를 얇은 칼날 평면으로 다시 매핑하고 전면·후면 노멀을 칼날 평면 방향으로 보정했다.
- 최종 평면 낫날 수치:
  - 앞·뒤 두께 비 `0.159188`: 원래 팔 두께의 약 15.9%
  - 앞·뒤 중심축 치우침 `0.017347m`
  - 칼날 면 종횡비 `8.031011`
  - 아래 곡선 깊이 `0.099468m`
  - 날끝 상승 `0.265851m`
  - 날끝 두께 비 `0.334533`
  - 진행축 길이 `0.393158m`, 기준 대비 연장 `0.101585m`
  - 비오른팔 변화 `0m`, 변형 정점 `738`
- 평면 재구성 첫 적용은 내부 아래 폭 `0.185844m`로 중단됐다. 이후 실제 추가 깊이는 `0.109199m`, `0.196783m`로 두 번 연속 부족해 반복 수치와 원인을 보고한 뒤 경계 상수를 조정했다.
- 깊이 과보정 상태는 실측 `0.328853m`, 평면 종횡비 `26.55147`이었고 교체 캡처에서 거대한 V자 판처럼 보였다. 해당 캡처와 완료 판단을 폐기하고 아래 폭을 약 31%로 줄여 길고 좁은 곡선 날로 재조정했다. 승인된 교체 캡처 1회를 이미 사용했으므로 추가 캡처는 실행하지 않았다.
- 최종 Apply와 저장 상태 Validate가 통과했다. 지면 관통 `0.00336m`, 본 스케일 오차 `0`, 모델·슬롯 root motion `0`이다.
- BlendShape는 시작 `0`, 전개 완료·급강하·하강 완료·공격 후반·마지막 프레임 모두 `100`이다. 급강하 최고속도 `7.752739m/s`, 직전 `0.166209m/s`, 이후 `0.115888m/s`를 유지했다.
- Unity 재시작은 실행하지 않았다. `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 각 검증의 실행·재실행·실패 조사·로그 확인·결과 문서화·관련 생성물 작성은 실행하지 않았다.

### 두 번째 Game View 영상 기준 낫날 실루엣 재수정

- 사용자 지정 MP4 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-16 17-21-50.mp4`를 확인했다. 영상은 `1920x1000`, 약 `5.236초`이며 원본은 수정하지 않았다.
- `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/user_capture_review_172150/`에 4fps 접촉 시트와 `1.50~4.25초` 구간 프레임을 추출했다. 실제 Game View에서는 BlendShape가 재생되지만 어깨부터 넓어지는 잎·지느러미형 실루엣으로 보여 낫날 판독에 실패한 것을 확인했다.
- 팔 단면 수식을 넓은 잎에서 가는 곡선 날로 교체했다. 아래 절삭면 깊이 `0.36m`, 위쪽 두께 `0.09m`, 끝 상승 상수 `0.56m`, 앞뒤 두께 비 상수 `0.08`을 사용하며, 중간은 아래로 처지고 끝은 위로 크게 휘면서 가늘어지도록 구성했다.
- 축 연장 `0.40m`의 첫 Apply와 실제 BakeMesh 경계 Validate는 모두 동일하게 `GroundPenetration=0.349554`로 실패했다. 두 실패 수치와 원인을 보고한 뒤 팔 하강과 함께 연장분이 아래로 회전하는 문제를 막기 위해 축 연장을 `0.12m`로 줄였다.
- 최종 Apply와 독립 Validate가 통과했다. 주요 수치는 `GroundPenetration=0.076155`, `BladeDown=0.123372`, `ScytheTipRise=0.540578`, `ScytheExtension=0.135845`, `ScytheAxialSpan=0.417353`, `ScytheTipAverageRise=0.490767`, `ScytheTipThicknessRatio=0.529704`, `ScytheFrontThicknessRatio=0.069943`, `ScytheBladePlaneAspect=22.67087`, `NonArmDelta=0`, 변형 정점 `731`이다.
- BlendShape는 시작 `0`, 옆 전개 완료·급강하·하강 완료·공격 후반·마지막 프레임 모두 `100`이다. 순간 하강 최고속도 `7.752739m/s`, 직전 `0.166209m/s`, 이후 `0.115888m/s`, 속도 대비 `46.64455`를 유지했다.
- 기존 `CaptureApprovedGraveCurtainCallAttack`이 별도 정사영 카메라·임시 조명·BakeMesh 프록시를 사용해 사용자 Game View를 대표하지 못한 것이 확인돼 실제 씬 카메라 복사 방식으로 교체했다. 승인된 1회 캡처에서 `Model Cam`을 선택했으나 실제 사용자 녹화 화면보다 대상이 훨씬 작아 이 결과도 품질 근거에서 제외했다.
- 캡처 카메라 선택은 최종적으로 활성 `MainCamera` 태그의 Player 카메라를 사용하도록 수정했다. 승인된 캡처 횟수를 이미 사용했으므로 추가 캡처는 실행하지 않았고, 공격 전용 편집 모드 4회 반복 미리보기만 실행했다. 미리보기에서 `Mesh=Grave_Attack_ScytheBlade_Body`, `BlendShapeBindings=Grave_Body:blendShape.GraveRightArmScytheBlade`, `PreviewSaved=False`, `SceneDirtyPreserved=True`를 확인했다.
- Unity는 재시작하지 않았다. `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 각 검증의 실행·재실행·실패 조사·로그 확인·결과 문서화·관련 생성물 작성은 실행하지 않았다.
- 지정 공격 슬롯 밖 Grave 오브젝트, 실제 카메라 설정, 다른 씬 루트, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX·AI·물리·피해·전투 로직·프로젝트 설정·Git은 변경하지 않았다.

## Grave 커튼콜 공격의 아래날 낫 변형 및 종료 복구

- 대상은 `Approved Grave Enemy Placement/Grave_03_Attack_RightArm_GiantSweep/Grave_Model`의 기존 `Grave_Attack_CurtainCall_Sweep` 공격 하나로 한정했다.
- 기존 커튼콜형 3초 공격, 12개 본의 Quaternion Transform 커브 48개, 옆 전개·순간 급강하·왼쪽 마무리 궤적은 유지했다. 변경은 공격 전용 메시의 `GraveRightArmScytheBlade` BlendShape 형상과 종료 뒤 표시 복구, Player 카메라 캡처 선택에 한정했다.
- 이전 형상은 어깨 가까운 `progress 0.10`부터 팔 단면 전체를 넓히고 날끝을 `0.56m` 올려 실제 Game View에서 넓은 잎·지느러미처럼 보였다. 이를 전완 중간 `progress 0.28~0.40`부터 형성되는 얇은 칼날 평면으로 교체했다.
- 어깨와 근위 팔은 정상 굵기의 낫 등·자루로 유지하고, 전완 아래쪽에 절삭날을 펼쳤다. 위쪽 칼등은 얇게 남기며 끝부분은 짧게 연장·상승한 뒤 가늘게 모이도록 했다.
- BlendShape 커브는 공격 시작 `0`, `0.35초=0`, `0.85초=55`, 오른팔 옆 전개 완료 `1.20초=100`, 공격 마지막 `3.00초=100`이다. 마지막 프레임까지 낫을 유지하고 공격 종료 뒤 표시 상태는 `0`으로 복구한다.
- 첫 적용은 기존의 원본 아래쪽 정점 추가 이동량 판정이 압축된 칼날 평면을 `BladeDown=0`으로 계산해 중단됐다. 중심축 기준 실제 아래 절삭날과 위 칼등을 직접 측정하도록 바꾼 두 번째 적용은 아래날 `0.103014m`가 임시 최소치 `0.18m`보다 작아 중단됐다.
- 두 반복 수치와 원인을 보고한 뒤 아래 절삭 깊이 상수만 `0.31m`에서 `0.50m`로 조정했다. 다른 형상 상수와 공격 본 커브는 유지했다.
- 최종 Apply 내부 확인과 독립 저장 상태 Validate가 동일 수치로 통과했다.
  - 전체 커브 `49`: 기존 Transform `48` + 낫 BlendShape `1`.
  - 중심축 아래 절삭날 `0.155669m`, 위 칼등 `0.017183m`, 아래쪽 우세 비율 `9.059692`.
  - 앞뒤 두께 비 `0.1`, 칼날 평면 종횡비 `22.21617`, 날끝 상승 `0.082543m`, 축 연장 `0.082403m`.
  - 근위 팔 변화 `0m`, 비오른팔 변화 `0m`, 변형 정점 `702`.
  - BlendShape 시작 `0`, 옆 전개·급강하·하강·후반·마지막 프레임 `100`, 공격 후 복구 `0`.
  - 옆 전개 `0.666412m`, 전체 하강 `0.657076m`, 하강 우세 비율 `4.317004`, 급강하 최고속도 `7.752739m/s`.
  - 본 스케일 오차 `0`, 반복 본 자세 오차 `0`, 모델·슬롯 root motion `0`, 지면 관통 `0.00336m`.
- 이번 변경에서 컴파일 오류는 발생하지 않았다.
- 전용 확인 통과 뒤 승인된 최종 캡처를 한 번 실행했다. `Player` 하위 활성 `Player Camera`를 사용한 것은 확인됐지만 대상이 작게 기록돼 낫 실루엣의 시각 품질 근거로 사용하지 않는다. 추가 캡처는 실행하지 않았다.
- 마지막으로 저장하지 않는 공격 전용 편집 모드 4회 반복 미리보기를 실행했다. `Mesh=Grave_Attack_ScytheBlade_Body`, `BlendShapeBindings=Grave_Body:blendShape.GraveRightArmScytheBlade`, 시작 표시값 `0`, `PreviewSaved=False`, `SceneDirtyPreserved=True`를 확인했다.
- 생성·수정 범위:
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Grave/Models/Grave_Attack_ScytheBlade_Body.asset`
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Art/Enemies/Grave/Controllers/Grave_Attack_CurtainCall_Sweep.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 지정 공격 슬롯 메시·Animator 참조
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/`
  - `docs/PROGRESS_2026-07-16.md`
- Unity 재시작, 하네스 관련 테스트·검증, 다른 테스트·빌드, 다른 Grave 슬롯, AI·이동·물리·피해·전투 로직, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX, 지정 슬롯 밖 씬 오브젝트, 실제 카메라 설정, Git은 실행하거나 변경하지 않았다.

## Grave 오른팔 낫 급강하 가속 및 칼날 정면 유지

- 대상은 `Approved Grave Enemy Placement/Grave_03_Attack_RightArm_GiantSweep/Grave_Model`의 기존 3초 커튼콜형 공격으로 한정했다.
- 원인은 옆 전개 뒤 하강 키가 `1.30 → 1.36 → 1.50초`로 분산되고 첫 급강하 키가 목표 자세의 `88%`만 반영해 하강이 충분히 폭발적이지 않았던 점, 하강 목표 팔 방향에 모델 전후축 성분이 남아 칼날 면이 내려가는 도중 정면에서 틀어질 수 있었던 점이었다.
- 하강 키를 `1.30 → 1.325 → 1.42초`로 압축하고 `1.325초`에 하강 목표 자세의 `97%`까지 도달하도록 변경했다. 옆 전개 자세와 이후 마무리 동작은 유지했다.
- 하강·스윕 시 RightArm/RightForeArm 목표 방향의 전후축 성분을 `0`으로 고정해, 낫날 평면이 모델 정면을 향하도록 했다.
- 하강 직후 `1.325초`와 하강 완료 `1.65초`의 BakeMesh 법선 정렬을 계측하는 검증을 추가했다. 최저 정면 정렬값 `0.573383`, 평균 `0.661636`으로 설정한 최저 `0.45`, 평균 `0.55` 기준을 통과했다.
- 급강하 기준을 최고 속도 `12m/s` 이상, 전후 완만 구간 대비 순간 가속비 `6배` 이상으로 강화했다. 최종 저장 상태는 최고 하강 속도 `22.56031m/s`, 직전 평균 `0.190833m/s`, 이후 평균 `0.000468m/s`, 순간 가속비 `118.2203`이다.
- Apply 내부 확인과 독립 `ValidateApprovedGraveCurtainCallAttack`가 동일 수치로 통과했다. 전체 커브 `49`, 본 스케일 오차 `0`, 반복 자세 오차 `0`, 모델·슬롯 root motion `0`, 비오른팔 정점 변화 `0`, 지면 관통 `0.00336m`를 확인했다.
- `GraveRightArmScytheBlade`는 공격 시작 `0`, 옆 전개 완료·급강하·후반·마지막 프레임 `100`, 공격 종료 후 `0`으로 복구된다.
- 승인된 최종 캡처는 한 번 실행됐지만 기존 `Model Cam`의 편집기 저장 구도 때문에 대상이 지나치게 작게 기록되어 시각 판정 근거에서 제외했다. 카메라 설정 변경이나 추가 캡처는 수행하지 않았다.
- 최종 리그 점검에서 `Mesh=Grave_Attack_ScytheBlade_Body`, BlendShape 1개, `BladeWeight=0`, 컨트롤러·클립 `Grave_Attack_CurtainCall_Sweep`, `PreviewSaved=False`, `SceneDirtyPreserved=True`를 확인했다.
- 실행 로그와 결과물:
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ApplyFastFrontFacingDrop.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ValidateFastFrontFacingDrop_Final.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/CaptureFastFrontFacingDrop_Final.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/InspectFastFrontFacingDrop_Final.log`
- Unity 재시작, 하네스 관련 테스트·검증, 다른 테스트·빌드, 다른 Grave 슬롯, AI·이동·물리·피해·전투 로직, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX, 지정 슬롯 밖 씬 오브젝트, 실제 카메라 설정, Git은 실행하거나 변경하지 않았다.

## Grave 오른팔 낫 메시 뒤틀림 및 몸통 파묻힘 제거

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-16 21-40-18.mp4`를 확인했다. 영상은 `1920x1000`, 약 `6.935초`, H.264이며 233프레임이 기록돼 있다.
- `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/`에 12fps 개별 프레임 84개와 10fps Game View 접촉 시트를 생성했다. 하강·인사 구간에서 낫으로 변형된 팔이 팔꿈치에서 넓은 판처럼 접히고 몸통 중심을 통과하는 현상을 확인했다.
- 원인은 칼날 정면 수치를 맞추기 위해 프레임마다 위팔·아래팔에 서로 다른 `-180~180도` 축 비틀기를 자동 적용한 점과, 기존 커튼콜 끝 자세에서 위팔과 아래팔 방향 차이가 약 70도로 커 하나의 낫날 메시가 팔꿈치에서 접힌 점이었다.
- 프레임별 자동 축 비틀기 검색과 적용 코드를 완전히 제거했다. 커튼콜 끝 자세는 위팔 `(-0.58,-0.64,0.50)`, 아래팔 `(-0.65,-0.55,0.52)` 방향을 사용해 두 팔 구간을 거의 같은 방향으로 정렬하고 몸통 정면 쪽에 띄웠다.
- 빠른 `1.28~1.58초` 원호 베기와 `1.58~2.35초` 커튼콜 자세 유지는 보존했다. 최종 베기 속도는 최저 `4.280913m/s`, 평균 `6.19628m/s`, 최고 `8.050647m/s`이며 속도 균일도는 `0.690884`다.
- 공격 전 구간을 24구간으로 샘플링해 최대 팔꿈치 굽힘과 몸통 투영 범위 대비 아래팔·손 메시 간격을 검사하는 전용 검증을 추가했다. 최대 팔꿈치 굽힘은 `6.651514도`이며 아래팔·손 정점이 몸통의 좌우·상하 투영 범위 안으로 들어오는 표본은 없었다.
- 커튼콜 몸통 앞 위치는 `FinalForward=0.401505m`, 왼쪽 위치는 `FinalLateral=-0.223053m`, 자세 유지 편차는 `0.000004m`다. 본 스케일 오차 `0`, 반복 자세 오차 `0`, 모델·슬롯 root motion `0`, 비오른팔 정점 변화 `0`, 지면 관통 `0.00336m`다.
- 낫 BlendShape는 옆 전개부터 마지막 프레임까지 `100`, 공격 종료 후 `0`으로 복구된다. 기존 곡선 낫 형상과 메시 변형 범위는 유지했다.
- Apply 내부 확인과 독립 저장 상태 Validate가 동일 수치로 통과했다. 최종 캡처는 한 번 정상 실행됐지만 기존 `Model Cam` 저장 구도 때문에 대상이 작게 기록되어 시각 판정 근거에서 제외했다. 카메라 설정 변경이나 추가 캡처는 수행하지 않았다.
- 주요 최종 로그:
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ApplyUntwistedTorsoClearSlash.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ValidateUntwistedTorsoClearSlash_Final.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/CaptureUntwistedTorsoClearSlash_Final.log`
- Unity 재시작, 하네스 관련 테스트·검증, 다른 테스트·빌드, 다른 Grave 슬롯, AI·이동·물리·피해·전투 로직, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX, 지정 슬롯 밖 씬 오브젝트, 실제 카메라 설정, Git은 실행하거나 변경하지 않았다.

## Grave 오른팔 낫의 연속 고속 베기와 커튼콜 몸통 앞 마무리

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-16 21-17-52.mp4`를 확인했다. 영상은 `1920x1000`, 약 `4.996초`, H.264이며 169프레임이 기록돼 있다.
- `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/`에 12fps 개별 프레임 60개, 전체 접촉 시트, 10fps Game View 접촉 시트를 생성했다. 영상에서 `옆 전개 → 갑작스러운 하강 → 반동 복귀 → 느린 몸통 앞 이동`으로 나뉘어 하나의 베기가 아니라 끊긴 두 동작처럼 보이는 것을 확인했다.
- 원인은 이전 급가속 키가 옆 자세에서 기존 `1.65초` 하강 자세까지만 압축했고, 이후 `1.65 → 2.00 → 2.35초`의 느린 스윕·당기기 커브가 그대로 남아 있던 점이었다. 오버슈트와 안정 키도 하강 직후 되튐을 만들었다.
- 오른쪽 `RightShoulder`, `RightArm`, `RightForeArm`, `RightHand`의 중간 하강·스윕·당기기 키를 제거하고, 옆 전개 뒤 `1.28~1.58초`의 `0.30초` 동안 기존 하강 자세를 멈추지 않고 경유해 기존 `2.35초` 몸통 앞 커튼콜 자세로 직접 이어지는 원호 베기로 재구성했다.
- `1.58초` 도착 뒤 `2.35초`까지 같은 커튼콜 자세를 유지한 후 기존 복귀 구간으로 연결한다. 몸통 로컬 기준 커튼콜 자세 유지 편차는 `0.000002m`다.
- 첫 연속 경로 적용은 속도·연속성은 통과했지만 낫날 정면값이 최저 `0.276`으로 중단됐다. 두 번째 비틀기 적용은 정면값을 최저 `0.480`으로 개선했으나 손이 몸통 앞에서 뒤로 밀려 중단됐다.
- 위팔 비틀기를 아래팔 부모 공간에서 역보상해 손 위치를 복구했지만 직선 관절 보간의 중앙 자세에서만 정면값이 `0.311`로 반복 실패했다. 시점별 측정 결과 시작 `0.761`, 중앙 `0.311`, 도착 `0.642`, 유지 `0.664`였다.
- 기존 정면 정렬이 정상인 하강 자세를 베기 중앙 경유점으로 사용하고 전체 `0.30초` 안에 통과시키면서 중앙 비틀림을 제거했다. 최종 정면값은 시작 `0.760681`, 중앙 `0.683283`, 도착 `0.641573`, 유지 `0.66416`, 최저 `0.641573`, 평균 `0.687425`다.
- 최종 베기 속도는 최저 `4.245992m/s`, 평균 `6.491895m/s`, 최고 `9.068729m/s`다. 속도 균일도 `0.654045`, 원호 경로 효율 `0.589664`이며 중간 정지·오버슈트·되튐 없이 몸통 앞에 도달한다.
- Apply 내부 확인과 독립 저장 상태 Validate가 동일 수치로 통과했다. 몸통 앞 위치 `FinalForward=0.311012m`, 왼쪽 도달 `FinalLateral=-0.231796m`, 본 스케일 오차 `0`, 반복 자세 오차 `0`, 모델·슬롯 root motion `0`, 비오른팔 정점 변화 `0`, 지면 관통 `0.00336m`다.
- 낫 BlendShape는 옆 전개부터 마지막 프레임까지 `100`, 공격 종료 후 `0`으로 복구된다. 기존 곡선 낫 형상과 메시 범위는 유지했다.
- 최종 캡처 시점을 `0|0.85|1.2|1.28|1.38|1.48|1.58|2|2.35|2.65|3`으로 변경해 베기 구간을 포함했다. 캡처 명령은 한 번 정상 실행됐지만 기존 `Model Cam` 저장 구도가 대상을 작고 아래가 잘린 상태로 기록해 시각 판정 근거에서는 제외했다. 카메라 설정 변경이나 추가 캡처는 수행하지 않았다.
- 주요 최종 로그:
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ApplyContinuousBodyFrontSlash_Final.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ValidateContinuousBodyFrontSlash_Final.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/CaptureContinuousBodyFrontSlash_Final.log`
- Unity 재시작, 하네스 관련 테스트·검증, 다른 테스트·빌드, 다른 Grave 슬롯, AI·이동·물리·피해·전투 로직, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX, 지정 슬롯 밖 씬 오브젝트, 실제 카메라 설정, Git은 실행하거나 변경하지 않았다.

## Grave 오른팔 낫 하강 가속의 화면 가독성 재수정

- 이전 수정은 하강 목표의 `97%`를 `0.025초`에 몰아 60fps에서도 약 `1.5프레임`밖에 되지 않았다. 최고속도 수치는 높았지만 중간 자세가 화면에서 건너뛰어져 가속 과정이 보이지 않는 원인으로 판정했다.
- 옆 전개 후 `1.34초`까지 하강 목표의 `2%`로 정지에 가깝게 유지하고, `1.44초`까지 `8%`만 진행한 뒤 `1.50초`에 `110%`까지 강하게 내리꽂도록 재구성했다. 이후 `1.62초`에 원래 하강 목표 자세로 안정시켜 짧은 타격 오버슈트가 보이게 했다.
- 화면 가독성 검증을 추가해 60fps 기준 전체 가속 구간 최소 `8프레임`, 강한 타격 구간 최소 `3프레임`을 요구한다. 최종 결과는 전체 `9.6프레임`, 타격 `3.6프레임`이다.
- Apply 내부 확인과 독립 저장 상태 Validate가 동일 수치로 통과했다. 최고 하강 속도 `9.975266m/s`, 준비 구간 평균 `0.009681m/s`, 타격 뒤 평균 `0.001013m/s`, 속도 대비 `1030.343`이다.
- 낫날 정면 정렬은 최저 `0.573383`, 평균 `0.614835`로 기준을 통과했다. 낫 BlendShape는 옆 전개부터 마지막 프레임까지 `100`, 공격 종료 후 `0`으로 복구된다.
- 최종 캡처는 한 번 실행했지만 기존 고정 캡처 시점이 `1.20초` 다음을 `1.65초`로 건너뛰어 핵심 `1.34~1.50초` 구간을 포함하지 못했다. 기존 `Model Cam` 구도도 대상이 작으므로 해당 이미지는 가속의 시각 판정 근거에서 제외했으며 추가 캡처는 실행하지 않았다.
- 실행 로그:
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ApplyVisiblyAcceleratedDrop.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/ValidateVisiblyAcceleratedDrop_Final.log`
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/CaptureVisiblyAcceleratedDrop_Final.log`
- Unity 재시작, 하네스 관련 테스트·검증, 다른 테스트·빌드, 다른 Grave 슬롯, AI·이동·물리·피해·전투 로직, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX, 지정 슬롯 밖 씬 오브젝트, 실제 카메라 설정, Git은 실행하거나 변경하지 않았다.

## 사용자 낫날 이미지 기준 Grave 오른팔 곡선 날 재구성

- 작업 시작 전 Unity 리프레시와 공격 리그 점검으로 현재 Grave 공격 코드가 정상 컴파일되는 것을 먼저 확인했다. 새 컴파일 오류는 재현되지 않았다.
- 사용자 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-16 20-39-18.mp4`를 확인했다. 영상은 `1920x1000`, 약 `5.725초`, H.264이며 원본은 수정하지 않았다.
- `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/user_capture_review_203918/`에 4fps 프레임 23개와 접촉 시트를 추출했다. 영상에서 오른팔이 아래날이 있는 낫이 아니라 끝까지 굵은 직선형 곤봉·잎처럼 보이는 것을 확인했다.
- 사용자가 제공한 낫 이미지의 손잡이가 아니라 날 부분을 기준으로 삼았다. 목표 실루엣은 `좁은 연결부 → 아래로 휜 등선 → 중간이 볼록한 아래 절삭면 → 아래로 굽으며 가늘게 수렴하는 끝`이다.
- 기존의 수평 팔 아래 폭만 확장하는 수식을 제거하고, 팔 진행률에 따라 위쪽 등선과 아래쪽 절삭선을 각각 계산하도록 바꿨다. 연결부와 끝에는 별도 테이퍼를 적용하고 중심선 자체를 아래로 굽혔다. 앞뒤 두께는 원래 단면의 `6%`로 압축했다.
- 기존 커튼콜형 3초 공격, 옆 전개, 순간 급강하, 왼쪽 마무리, Transform 커브 48개는 유지했다. `GraveRightArmScytheBlade` BlendShape는 시작 `0`, 옆 전개 완료 `100`, 공격 마지막 프레임 `100`, 공격 종료 뒤 `0` 복구다.
- 첫 적용은 날끝 최대 하강 `0.415395m`, 평균 `0.347191m`, 중심축 아래 폭 `0.30582m`로 과도해 중단했다. 날 폭·중간 볼록함·끝 하강을 줄였다.
- 두 번째 적용은 날끝 최대 하강 `0.23779m`, 평균 `0.173648m`, 아래 폭 `0.142089m`로 개선됐지만 끝 하강 상한 `0.22m`를 초과해 중단했다. 끝 하강 상수만 `0.12m`에서 `0.10m`로 줄였다.
- 세 번째 적용은 날끝 최대 하강 `0.219802m`로 들어왔지만 이전 형상의 절대 아래 폭 최소치 `0.14m`에 대해 실제 `0.128709m`로 중단했다. 길고 좁은 기준 날에 맞게 절대 폭 대신 폭/축 길이 비율 `0.22~0.45`를 판정하도록 변경했다.
- 최종 Apply 내부 확인과 독립 저장 상태 Validate가 동일 수치로 통과했다.
  - 전체 커브 `49`: 기존 Transform `48` + 낫 BlendShape `1`.
  - 낫 축 길이 `0.419996m`, 중심축 아래 폭 `0.128709m`, 폭/축 길이 비 `0.306452`.
  - 날끝 최대 하강 `0.219802m`, 평균 하강 `0.154645m`, 축 연장 `0.136106m`.
  - 끝 두께 비 `0.194034`, 앞뒤 두께 비 `0.06`, 칼날 평면 종횡비 `27.57093`.
  - 근위 팔 변화 `0m`, 비오른팔 변화 `0m`, 변형 정점 `730`.
  - BlendShape 시작 `0`, 옆 전개·급강하·후반·마지막 프레임 `100`, 공격 후 복구 `0`.
  - 기존 옆 전개 `0.666412m`, 전체 하강 `0.657076m`, 급강하 최고속도 `7.752739m/s`.
  - 본 스케일 오차 `0`, 반복 본 자세 오차 `0`, 모델·슬롯 root motion `0`, 지면 관통 `0.00336m`.
- 사용자 영상의 Game View 구도에 맞추기 위해 최종 캡처 카메라 선택은 활성 `Model Cam`을 우선하도록 변경했다. 카메라 설정 자체는 수정하지 않았다.
- 승인된 최종 캡처를 한 번 실행해 `Camera=Model Cam`을 확인했지만, 편집기에 저장된 카메라 Transform이 사용자 영상의 Play Mode 구도와 달라 대상이 작게 기록됐다. 이 캡처는 낫날의 시각 품질 근거로 사용하지 않으며 추가 캡처는 실행하지 않았다.
- 마지막으로 저장하지 않는 공격 전용 편집 모드 4회 반복 미리보기를 실행했다. 공격 메시와 BlendShape 바인딩, 시작 표시값 `0`, `PreviewSaved=False`, `SceneDirtyPreserved=True`를 확인했다.
- 수정·생성 범위:
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Grave/Models/Grave_Attack_ScytheBlade_Body.asset`
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Art/Enemies/Grave/Controllers/Grave_Attack_CurtainCall_Sweep.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 지정 공격 슬롯
  - `docs/validation/grave_scythe_blade_accelerated_attack_2026-07-16/`
  - `docs/PROGRESS_2026-07-16.md`
- Unity 재시작, 하네스 관련 테스트·검증, 다른 테스트·빌드, 다른 Grave 슬롯, AI·이동·물리·피해·전투 로직, 원본 FBX·Blender·리그·본 가중치·UV·머티리얼·텍스처·VFX, 지정 슬롯 밖 씬 오브젝트, 실제 카메라 설정, Git은 실행하거나 변경하지 않았다.
