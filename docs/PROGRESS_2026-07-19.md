# PROGRESS 2026-07-19

## 최신 사용자 불합격·작업 중단 - 원본 큰 칼날 궤적 기반 오스티나토 공격 재구현

> 2026-07-19 최종 Unity 런타임 비교 결과에 대해 사용자가 원본 큰 칼날 모션과 다른 결과라고 판정했다. 이 절의 구현은 Unity에 반영되어 있으나 완료 판정은 철회한다. 현재 모션과 생성 수치는 후속 공격 모션의 승인 기준이나 재사용 기준으로 취급하지 않는다.

- 사용자 실행 영상 `Bellerophon ... 21-46-43.mp4`에서 이전 공격이 원본 `C:/Users/gus68/OneDrive/바탕 화면/1111.mp4`의 큰 칼날과 다른 것을 확인했다. 아래의 이전 완료 판정은 철회하고, 기존 생성 수치와 단계 존재 여부를 완료 근거로 사용하지 않았다.
- 원본 15프레임을 하나의 연속 궤적으로 다시 분석했다. 큰 칼날 A를 `RightArm`, 큰 칼날 B를 `LeftArm`에 대응시키고, 원본 화면에서 보이는 궤적을 실제 게임 정면에서도 읽히도록 오스티나토의 X/Z 평면에 옮겼다. Y축은 전방으로 뻗은 뒤 몸 가까이 당기는 깊이 변화만 담당한다.
- `Ostinato_ScissorAttack_Generate.py`는 승인 정적 모델과 기존 리그를 사용하는 독립 양팔 2본 IK/FK 베이크 방식이다. 별도 후보 모델이나 별도 Blender 프로젝트는 만들지 않았다. 손목 본에는 키를 만들지 않았고 전완 축 보정은 최대 `±12도`만 사용해 칼날 면이 갑자기 뒤집히지 않게 했다.
- 4초·60fps의 15개 대응 시점과 10fps 41개 연속 프레임을 사용했다. 실제 화면에서 초기 자세 → 서로 반대 높이의 큰 전개 → 한쪽 상방·반대쪽 측방 스윙 → 넓은 낮은 V → 안쪽 교차 → 같은 쪽 중첩과 몸쪽 압축 당김 → 낮은 U자 회복 → 기본 자세 복귀가 이어지는 것을 원본과 직접 대조했다.
- 정면 연속 렌더에서 한쪽 팔이 머리 위에 오래 접혀 있고 다른 팔이 아래에 고정되는 첫 재구현은 시각 불합격 처리했다. 원인을 원본 Y/Z 궤적이 정면에서 깊이에 가려지는 것으로 확인하고 X/Z 화면 평면으로 재매핑했다. 후반 IK 모드 전환으로 팔이 위로 튀던 프레임도 최소 팔꿈치 각도의 연속 보간으로 제거했다.
- 최종 Blender 직접 비교 자료는 `docs/validation/ostinato_scissor_attack_user_review_2026-07-19/`의 `Reference_All15.png`와 `Ostinato_Blender_FrontContinuous41.png`에 저장했다. 반복 수정 중 생성된 중간 렌더와 개별 프레임은 로컬 검토 자료로만 남기고 Git 커밋에서는 제외했다.
- 최종 `Ostinato_04_Scissor_Attack_Source.blend`와 `Ostinato_04_Scissor_Attack.fbx`를 출력하고 Unity의 `Approved Ostinato Enemy Placement/Ostinato_04_Scissor_Attack`에만 적용했다. 적용 결과는 `4초`, LoopTime, RootMotion 비활성, 다른 슬롯 불변이며, 실제 표시 메시의 외형 지문은 승인 정적 메시와 같은 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`이다.
- Unity Play Mode에서 한 루프를 정면·3/4 각 41프레임으로 다시 캡처했다. `docs/validation/ostinato_scissor_attack_2026-07-19/Ostinato_ScissorAttack_RuntimeContinuous.png`와 `runtime_continuous_frames/`를 확인했으며, 원본과 같은 시간축의 대조본은 `docs/validation/ostinato_scissor_attack_user_review_2026-07-19/Reference_vs_UnityRuntime_Front_Normalized.mp4`와 `Reference_vs_UnityRuntime_Front_15Stages.png`다. 에이전트는 이 자료에서 전개·교차·중첩·당김·복귀가 대응한다고 판단했으나 사용자가 최종 결과를 불합격으로 판정했으므로 해당 시각 통과 판단을 철회한다.
- 기존 `ReviewOstinatoScissorAttackAnimation`은 이전 Y/Z 구현의 큰 깊이 차를 요구해 `WindupDepthOffset=0.078323`에서 실패했다. 리뷰 명령이 실패했고 사용자 직접 확인도 불합격이므로 이 작업에는 유효한 완료 판정이 없다.
- `Refresh-UnityProject.ps1`, 공격 슬롯 적용, 공격 전용 리뷰, 공격 런타임 캡처만 실행했다. 하네스 검증, EditMode/PlayMode 테스트, Windows 빌드, 다른 슬롯·AI·물리·게임플레이 수정과 Unity 재시작은 실행하지 않았다. 최종 사용자 불합격 판정까지 포함한 작업 기록과 관련 자산만 별도 Git 커밋·푸시 대상으로 정리했다.

## 이전 완료 판정 철회 - 울트라리스크 기반 오스티나토 공격 최종 Unity 시각 검증

> 이 절의 구현 수치와 완료 판정은 사용자 실행 영상 및 원본 큰 칼날 직접 대조 결과와 일치하지 않아 철회했다. 현재 결과의 근거로 사용하지 않는다.

- 폐기된 공격 생성 방식을 전부 제거하고 `Ostinato_ScissorAttack_Generate.py`를 독립 2본 IK 목표·팔꿈치 Pole 기반으로 다시 작성했다. 영상의 큰 칼날 A는 해부학적 `RightArm`, B는 `LeftArm`으로 기준 카메라에서 직접 확인했으며, 각 팔의 상완·전완을 별도로 풀어 FK 회전으로 굽는다.
- `C:/Users/gus68/OneDrive/바탕 화면/1111.mp4`의 F01~F15와 같은 4초 시간축을 사용한다. F03~F04 반대 방향 장전, F05~F08 전방 가위식 교차, F09~F13 겹친 칼날의 몸쪽 압축 당김, F14~F15 낮은 U자 회복과 중립 복귀를 정면·3/4 및 10fps 연속 Blender 렌더로 직접 대조했다.
- 손목 뼈에는 회전 키를 만들지 않았다. F05부터 전완 전체에 서로 반대인 축 회전을 단계적으로 분산하고 F08~F09에서 최대 ±75도로 만들어, 정면에서 두 긴 칼날이 평행 세로로 서지 않고 X자 대각 교차하며 3/4 시점에서 안쪽 U자 겹침을 형성한다.
- F05~F13의 팔꿈치 내부 각도는 양팔 모두 `135~169도`, 기본 굽힘 방향과의 점곱은 전 프레임 양수다. 손과 팔꿈치는 해부학적 좌우 반공간을 유지하고 `LeftHand`·`RightHand` 로컬 회전 커브, Root·Hips 커브와 오브젝트 이동은 모두 `0`이다.
- 첫 Unity 공격 리뷰에서 교차 깊이 `0.843632`에 비해 당김 깊이가 `0.747681`로 몸쪽 이동이 `0.095951`뿐이어서 불합격 처리했다. F11~F13을 더 몸쪽·아래로 당겨 최종 리뷰는 당김 깊이 `0.534378`, 몸쪽 이동 `0.309254`, 교차 손 간격 `0.115843`, 당김 손 간격 `0.103119`로 통과했다.
- 최종 `Ostinato_04_Scissor_Attack_Source.blend`와 `Ostinato_04_Scissor_Attack.fbx`를 생성해 Unity 4번 슬롯에 적용했다. 클립은 `4초`, `60fps`, LoopTime이며 RootMotion은 꺼져 있다. 실제 재생 외형은 정적 승인 메시 지문 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`와 승인 머티리얼 4개를 그대로 사용하고 다른 슬롯은 변경하지 않았다.
- 승인된 Unity 캡처 1회로 만든 중간판 런타임 합성본은 정면 충돌 구간의 칼날이 평행 세로로 보이는 것을 직접 발견해 시각 불합격 처리했다. 이후 Blender에서 ±75도 전완 회전판을 직접 검토하고 최종 FBX·Unity 적용·공격 전용 리뷰를 갱신한 뒤, 최종 Unity Play Mode 재캡처 1회를 실행했다.
- 최종 Blender 비교 프레임은 `docs/validation/ostinato_scissor_attack_reanalysis_2026-07-19/direct_final_preview/`의 `reference_15_stages`, `front_15_stages`, `continuous_10fps`에 있다. Unity 구조·적용·수치 리뷰와 최종 재생 기록은 `docs/validation/ostinato_scissor_attack_2026-07-19/`의 `Ostinato_ScissorAttackInspection.txt`, `Ostinato_ScissorAttackApply.txt`, `Ostinato_ScissorAttackReview.txt`, `Ostinato_ScissorAttackRuntimePlayback.txt`에 기록했다. 최종 정면·3/4 연속 합성본은 같은 폴더의 `Ostinato_ScissorAttack_RuntimeContinuous.png`, 41개 원본 프레임은 `runtime_continuous_frames/`에 있다.
- 최종 Unity 프레임을 원본 F01~F15와 직접 대조했다. 넓은 비대칭 장전, 양팔 전체의 전방 스윙, X자·안쪽 U자 칼날 교차, 낮고 좁은 몸쪽 압축 당김, 중립 복귀 순서가 유지됐으며 손목만 따로 꺾이는 프레임, 비정상적인 중간 팔 들림, 루트 이동은 보이지 않았다. 마지막 프레임은 시작 자세와 일치해 시각 검증을 통과했다. 하네스 검증, EditMode/PlayMode 테스트, Windows 빌드, Unity 재시작과 Git 작업은 실행하지 않았다.

## 최신 불합격·폐기 - 울트라리스크 큰 칼날 기반 오스티나토 4초 공격 재구현

> 2026-07-19 사용자 실제 실행 영상 `Bellerophon ... 20-06-28.mp4`에서 왼팔 역관절이 확인됐다. 아래 구현과 완료 판정은 모두 철회한다. 현 `Ostinato_ScissorAttack_Generate.py`, `Ostinato_04_Scissor_Attack_Source.blend`, `Ostinato_04_Scissor_Attack.fbx`, `Ostinato_04_Scissor_Attack.controller`의 모션 방식과 수치는 새 공격 구현에 재사용하지 않는다. 슬롯 참조를 깨는 물리 삭제는 새 구현 교체 작업에서 일괄 처리한다.

- 사용자 제공 원본 `C:/Users/gus68/OneDrive/바탕 화면/1111.mp4`의 15프레임을 직접 비교했다. 기존 대칭 허그·손목 보정 방식은 폐기하고, 큰 칼날 하나가 위·뒤로 열리고 반대 칼날이 아래·앞으로 열리는 비대칭 준비와 서로 반대인 대각선 공격 궤적을 오스티나토 양팔에 대입했다.
- 새 시간축은 기본 자세(0~0.30초) → 비대칭 준비(0.30~1.05초) → 전방 가위식 스윙(1.05~1.75초) → 수평 칼날 교차·압축 회수(1.75~2.55초) → 복귀(2.55~3.45초) → 기본 자세 유지(3.45~4.00초)다.
- `Ostinato_ScissorAttack_Generate.py`를 전면 교체했다. 별도 모델·리그·Blender 프로젝트는 만들지 않고 승인된 기존 Blender 모델과 리그를 재사용한다. `Arm→ForeArm`을 거의 곧은 하나의 긴 칼날 체인처럼 구동하며 `LeftHand`·`RightHand` 로컬 회전은 전 구간 `0도`다.
- 승인 메시의 전완 가중 칼날 정점으로부터 좌우 칼날 면 법선을 구해, 준비 이후 각 칼날 면을 월드 수평 절단면에 놓는다. 손목 단독 축 회전 대신 어깨부터 전완까지 같은 월드 축 회전을 적용하므로 머리 위 세로 칼날과 비정상적인 손목 뒤집힘을 제거했다.
- 15단계 측면 시트와 31프레임 정면·상단 3/4 연속 시트를 원본 영상과 직접 대조했다. 화면에서 비대칭 준비, 전방 교차, 가로 칼날 면, 팔을 편 채 전방 깊이를 줄이는 회수, 기본 자세 복귀를 확인했다. 결과는 `docs/validation/ostinato_scissor_attack_2026-07-19/ultralisk_reference_preview/`에 저장했다.
- Blender 평가 순서 때문에 최초 1프레임과 후반 기본 자세의 전완 회전이 미세하게 달랐던 문제를 발견했다. 실제 1프레임 로컬 회전을 208~241프레임에 다시 키잉해 Blender의 1·208·240·241프레임 양손 위치와 전완 회전을 완전히 동일하게 고정했다.
- `Ostinato_04_Scissor_Attack_Source.blend`와 `Ostinato_04_Scissor_Attack.fbx`를 갱신하고 `Approved Ostinato Enemy Placement/Ostinato_04_Scissor_Attack`에 적용했다. 클립은 `4초`, `60fps`, 루프이며 루트 모션을 사용하지 않는다.
- 공격 FBX는 애니메이션 공급원으로만 사용하고 실제 슬롯 4 외형은 승인 정적 메시 `Ostinato_ApprovedUnity.fbx`와 승인 머티리얼 4개를 사용한다. 적용 메시와 정적 승인 메시의 외형 지문은 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`로 일치하며 다른 8개 슬롯은 변경되지 않았다.
- Unity 공격 전용 검토는 비대칭 준비 높이차 `1.155081`, 깊이차 `0.760788`, 교차 손 간격 `0.544637`, 교차→회수 전방 깊이 `0.867104→0.323143`, 손목 로컬 회전 `0도`, 충돌 몸통 회전 `12.99998도`, 시작·끝 동일 자세와 루트 고정을 확인했다.
- 승인된 최종 캡처 1회로 실제 Play Mode Animator의 한 루프를 정면·3/4 각 41프레임으로 기록했다. `Ostinato_ScissorAttack_RuntimeContinuous.png`를 직접 확인한 결과 Blender에서 승인한 궤적과 수평 칼날 면이 Unity 슬롯 4에서도 유지됐다.
- 구조·적용·재생 기록은 같은 검증 폴더의 `Ostinato_ScissorAttackInspection.txt`, `Ostinato_ScissorAttackApply.txt`, `Ostinato_ScissorAttackReview.txt`, `Ostinato_ScissorAttackRuntimePlayback.txt`에 저장했다. 게임플레이·피해·히트박스·AI, 다른 애니메이션과 슬롯, 모델·텍스처·머티리얼, Unity 재시작과 Git 작업은 수행하지 않았다.

## 최신 완료 - Unity 오스티나토 일반 보행 개체의 정적 승인 외형 동기화

- 사용자 지시에 따라 `Approved Ostinato Enemy Placement/Ostinato_03_Walking`의 기존 보행 AnimationClip·커브·속도·루프·루트 모션 설정은 수정하지 않고, 보이는 모델링 외형만 정적 승인 모델과 동일하게 동기화했다.
- 기존 보행 FBX는 단일 서브메시와 단일 `Ostinato_Walking_BodyTone.mat`을 사용해 정적 승인본의 갑각·연부 조직·갈고리 칼날·복안 4부위 PBR 외형을 재현하지 못했다.
- 정적 승인 메시를 기존 보행 리그에 직접 연결하는 첫 두 경로는 변경 전에 중단했다. 본 이름·순서는 같았지만 계층 경로가 달랐고, 승인 메시 bind pose와 현재 보행 리그의 최대 행렬 차이는 `1.73324025`였다. 리그 루트 이름을 보행 계층에 맞춘 뒤에도 기존 보행 인스턴스와의 rest 행렬 차이가 `1.524142`여서 직접 메시 교체는 사용하지 않았다.
- 승인 Blender 원본의 메시를 기반으로 애니메이션을 포함하지 않는 `Assets/_Project/Art/Enemies/Ostinato/Models/Ostinato_WalkingAppearanceSynced.fbx`를 생성했다. 리그 루트는 기존 보행 클립 경로와 동일한 `Armature`로 맞추고, 정점 좌표 해시 `2BA73D1431D2260D5880D0AA376FA4D0E8E7B7597DA1387A5C85669433E43E2B`, Blender 정점 `2,649개`, 폴리곤 `5,279개`, 재질 구획 `3093|1274|838|74`, `OstinatoSampleUV`, 24본과 기존 vertex group을 유지했다.
- 3번 슬롯의 모델 하위만 파생 FBX 인스턴스로 교체하고 기존 `Ostinato_03_Walking.controller`와 원본 보행 FBX 내장 클립 참조 및 재생 설정을 그대로 이관했다. 파생 메시에는 정적 모델과 동일한 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 승인 머티리얼 4개를 연결했다.
- Unity에서 정적 승인 메시와 파생 메시의 정점·법선·탄젠트·UV·bone weight·서브메시 인덱스 외형 지문은 모두 `C90223F66A7778E706E8B2492EB4B3FAA0499D836F6D6D8C425B0A9417B41F2C`로 일치했다. 보행 클립의 250개 커브 바인딩은 파생 리그에서 전부 해석됐으며 미해결 바인딩은 `0`이다.
- 기존 `1.033333초`, 60fps 보행 클립을 복제 개체에 `0`, `0.25`, `0.5`, `0.75`, `0.99` 시점으로 샘플링했다. 모든 시점에서 `5,279개` 정점과 유효 Bounds가 유지됐고 최대 정점 이동은 `1.144826`으로 실제 전신 보행 변형이 확인됐다.
- Animation 지문은 적용 전후 `273575C350A8005C0AB839BE0B407E8333F3F2A5BEB911474C2EA833CDE80916`으로 동일하다. 원본과 Unity 보행 FBX의 SHA-256도 적용 전후 `B7F6A7148B7A1D66E4EA6DADBA725427B352B76C273E353E06BA53CAAE86FB5A`로 유지됐다.
- 최종 비교본 `docs/validation/ostinato_walking_appearance_2026-07-19/Ostinato_WalkingAppearanceSync_Comparison.png`은 전면·측면·후면 3행과 승인 Blender 샘플·Unity 정적 기준·동기화된 보행 개체 3열로 구성했다. Unity 정적 기준과 보행 개체의 실루엣, 부위 형상, 재질 분포와 칼날 표면이 세 방향에서 동일한 것을 직접 확인했다.
- 구조·불변 기록은 같은 폴더의 `Ostinato_WalkingAppearanceSync.txt`, 보행 바인딩·메시 변형 기록은 `Ostinato_WalkingPlaybackAppearanceReview.txt`에 저장했다. 다른 오스티나토 8개 슬롯의 지문과 3번 슬롯 Transform은 변경되지 않았다.
- Blender 백그라운드 파생 모델 내보내기, `Refresh-UnityProject.ps1`, 오스티나토 승인 외형 적용 브리지만 실행했다. 범위 밖 자동 테스트·빌드, Unity 재시작과 Git 작업은 실행하지 않았다.

## 최신 완료 - Unity 오스티나토 일반 대기 실제 전신 호흡 재구성

- 사용자가 실제 실행 화면에서 몸통만 모핑된다고 확인해 기존 전신 호흡 완료 판단을 철회했다. 기존 단일 BlendShape는 팔·다리에도 수치상 정점 변화가 있었지만 화면 외곽 변화가 몸통에 묻혀 전신 호흡으로 읽히지 않았다.
- 단일 가중치 확대를 반복하지 않고 메시 변형을 `CoreMorph`, `UpperMorph`, `LowerMorph` 세 채널로 분리했다. 상체가 먼저 흡기에 반응하고 몸통과 하체가 뒤이어 반응하도록 각 채널의 호흡 곡선도 분리했다.
- BlendShape만으로는 실제 화면의 골반·다리 외곽 변화가 여전히 거의 보이지 않는 첫 Play 상태 캡처 결과를 확인했다. 이에 척추·목·머리의 상승, 양쪽 어깨의 바깥·위쪽 열림, 양쪽 허벅지의 바깥쪽 반응을 같은 Idle clip에 연결했다. 이 자세 변화는 각 부위의 BlendShape 호흡과 같은 시간축으로 움직인다.
- 칼날 정점 `996개`는 세 BlendShape 모두 변화량 `0`이다. 칼날은 팔 관절을 따라 위치만 이동하며 날 자체의 길이·두께·곡률·실루엣은 모핑하지 않는다.
- 최종 확인은 `CaptureOstinatoIdleBreathingRuntimePlayback`으로 열린 Unity 에디터의 실제 Play 상태에서 수행했다. 씬 Animator가 두 호흡 주기를 재생하는 동안 12fps, 총 78프레임을 캡처했고 BlendShape 값을 직접 설정하지 않았다.
- 실제 프레임에서 머리·흉곽 상승, 좌우 어깨·팔 열림, 골반·양다리 간격 변화, 호기 복귀가 모두 식별됐다. 첫 번째와 두 번째 주기에서 같은 순서가 반복되고 첫 프레임과 각 루프 경계는 중립 자세로 돌아온다.
- 연속 재생 검토는 `docs/validation/ostinato_idle_breathing_2026-07-19/Ostinato_IdleBreathingRuntimePlayback.html`, 전체 프레임은 같은 폴더의 `runtime_frames/`, 육안 확인 기록은 `Ostinato_IdleBreathingVisualReview.txt`에 저장했다.
- `Refresh-UnityProject.ps1`, `ApplyOstinatoIdleBreathingAnimation`, `CaptureOstinatoIdleBreathingRuntimePlayback`만 실행했다. 하네스 검증, EditMode/PlayMode 테스트, Windows 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 이전 확인 - Unity 오스티나토 일반 대기 Animator 연결 확인

- 기존 `Ostinato_IdleBreathing_Progression.png`은 확인 코드가 Animator를 끄고 `SetBlendShapeWeight`를 직접 적용해 만든 이미지였으므로, 메시 변형 가능성은 보여도 씬의 Animator 실제 재생 증거는 아니었다. 해당 완료 판단의 검증 근거를 정정했다.
- `ReviewOstinatoIdleAnimatorPlayback` 전용 확인 명령을 추가해 `CargoRunMvp/Approved Ostinato Enemy Placement/Ostinato_02_Idle_Breathing`의 실제 Animator를 기본 상태에서 시작하고 `Animator.Update`로 한 루프를 전진시켰다. 이 확인 경로에서는 `SetBlendShapeWeight`를 호출하지 않았다.
- 씬 Animator Controller는 `Ostinato_02_Idle_Breathing_Morph.controller`, 기본 상태는 `Ostinato_02_Idle_Breathing_Morph`, 실제 재생 Clip은 `Ostinato_02_Idle_Breathing_Morph.anim`, 바인딩 대상은 `Ostinato_Model/char1`의 `blendShape.Ostinato_Idle_Breathing_BodyMorph`로 확인됐다.
- normalized time은 `0 → 0.25 → 0.50 → 0.75 → 1.00`으로 전진했고 Animator가 실제로 출력한 가중치는 `0 → 73.928 → 86.226 → 28.679 → 0`이었다. 모든 시점이 Clip 커브 평가값과 일치했고 상태 이탈은 없었다.
- 루프 시작과 끝의 가중치는 모두 `0`, HookBlade BlendShape 최대 이동은 `0`, 다른 오스티나토 슬롯에 연결된 Animator는 `0`, 확인으로 인한 씬 변경은 없었다.
- 실제 Animator 프레임은 `docs/validation/ostinato_idle_breathing_2026-07-19/Ostinato_IdleAnimatorPlayback_Progression.png`, 수치 보고서는 같은 폴더의 `Ostinato_IdleAnimatorPlaybackReview.txt`에 기록했다.
- `Refresh-UnityProject.ps1`과 `ReviewOstinatoIdleAnimatorPlayback`만 실행했다. 하네스 검증, EditMode/PlayMode 테스트, Windows 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - Unity 오스티나토 일반 대기 전신 호흡 모션

- 확정 애니메이션 목록의 2번 `일반 대기 모션`을 `Approved Ostinato Enemy Placement/Ostinato_02_Idle_Breathing`에 적용했다.
- 최초 몸통 중심 모핑과 이후의 본 중심 국소 팽창 강화는 수치만 달라지고 실제 화면에서는 몸통 외 변화가 거의 보이지 않았다. 사용자 확인에 따라 해당 완료 판단을 철회하고 계산 방식을 교체했다.
- 최종 방식은 각 정점의 본 중심 팽창에 발바닥 기준 전신 실루엣 변형장을 결합한다. 흡기 시 머리·몸통은 위로 늘어나고 골반·다리는 바깥쪽으로 벌어져 전신 외곽이 연결되어 변한다. 손의 전역 이동은 0으로 감쇠해 칼날 접합부를 유지한다.
- 칼날 재질의 서브메시 정점을 별도로 마스킹했다. 칼날을 제외한 유기체 정점 `4,283개`가 전부 변형되고 칼날 정점 `996개`의 최대 이동은 `0`이다.
- 변형 정점 분포는 몸통 `380개`, 머리·목 `445개`, 팔·손 `917개`, 다리·발 `2,541개`다. 최종 최대 이동은 몸통 `0.070139`, 머리·목 `0.104738`, 팔·손 `0.062323`, 다리·발 `0.047420`이다.
- 유기체 실루엣 크기는 흡기에서 가로 `0.031481`, 세로 `0.086109`, 깊이 `0.081593`만큼 변한다. 발바닥 영역의 최대 수직 이동과 칼날 정점 `996개`의 최대 이동은 모두 `0`이다.
- `Ostinato_02_Idle_Breathing_Morph.anim`은 `3.20초`, `30fps`, `LoopTime=True`로 구성했다. 흡기 최고점은 `1.10초`이며 루트 모션은 사용하지 않는다.
- 전용 메시·클립·AnimatorController를 `Assets/_Project/Art/Enemies/Ostinato/Animations/`에 생성하고 2번 슬롯에만 연결했다. 승인 재질과 나머지 8개 슬롯의 Transform·Animator는 변경하지 않았다.
- `docs/validation/ostinato_idle_breathing_2026-07-19/Ostinato_IdleBreathing_Progression.png`에 `0%·50%·100%` 흡기의 전신 행과 유기체 확대 행을 배치했다. 머리, 어깨·팔 조직, 골반·허벅지·다리 외곽이 단계적으로 변하고 칼날 외곽과 발바닥 기준점은 유지되는 것을 직접 확인했다.
- 처음 두 적용 호출은 열린 에디터가 새 브리지 코드를 재컴파일하기 전이라 `Unknown bridge command`로 종료됐고 장면·에셋은 변경되지 않았다. 추가 승인 후 `Refresh-UnityProject.ps1`을 실행하고 `ApplyOstinatoIdleBreathingAnimation` 적용을 완료했다.
- 전신 모핑 조정에서도 `Refresh-UnityProject.ps1`과 `ApplyOstinatoIdleBreathingAnimation`만 다시 실행했다.
- 새 실루엣 변형장의 첫 적용은 세로·깊이 변화는 충분했지만 칼날 고정 상태의 가로 변화가 과도한 자체 기준에 못 미쳐 장면 반영 전에 중단됐다. 칼날이 전체 폭을 차지하는 구조를 반영해 유기체 가로 기준을 `0.025`로 조정한 뒤 적용했다.
- 최종 적용 검사는 머리·팔 최대 이동 `0.040` 이상, 다리 최대 이동 `0.030` 이상, 유기체 실루엣 가로·세로 변화, 칼날·발바닥 이동 `0`을 함께 요구한다. 정점 개수나 최대 이동값만으로 완료를 판단하지 않는다.
- 하네스 검증, EditMode/PlayMode 테스트, Windows 빌드, Unity 재시작, Git 커밋·푸시는 실행하지 않았다.

## 최신 완료 - Unity 오스티나토 칼날 줄무늬 제거

- 사용자 지시에 따라 승인 Blender 샘플과 `artSample/enemies/ostinato/` 산출물은 수정하지 않고 Unity 적용 결과만 조정했다.
- `Assets/_Project/Editor/OstinatoApprovedSampleApplicator.cs`에서 `HookBlade` 전용 Unity 오버라이드를 추가했다. 칼날의 BaseColor·Normal·MetallicSmoothness 텍스처 연결을 제거해 반복적인 밝은 띠와 평행 요철이 나타나지 않게 했다.
- 칼날은 청회색 `BaseColor=(0.11, 0.15, 0.16)`, `Metallic=0.86`, `Smoothness=0.60`의 일정한 URP/Lit 금속면을 사용한다. 어두운 금속 바탕, 형상에 따른 날 경계와 조명 반사는 유지한다.
- `CargoRunMvp.unity`의 `Approved Ostinato Enemy Placement` 아래 9개 `SkinnedMeshRenderer`에 공유되는 `Ostinato_Approved_HookBlade.mat`을 재적용했다.
- 적용 후 `SubMeshes=4`, `Materials=4`, `Bones=24`, `TransformsChanged=False`, `RigHierarchyChanged=False`, `PhysicsChanged=False`, `AiChanged=False`, `OtherSceneRootsChanged=False`를 확인했다.
- 캡처 전 전·측·후면 재질 분포 및 스튜디오 가독성 검사를 통과했다. 평균 휘도는 전면 `0.304160`, 측면 `0.306799`, 후면 `0.324789`다.
- 진단 이미지와 최종 비교본 `docs/validation/ostinato_approved_material_2026-07-18/Ostinato_ApprovedMaterial_FinalComparison.png`에서 칼날 줄무늬 제거, 몸통 V자 갑각·눈·관절·후면 텍스처 불변을 확인했다.
- `Refresh-UnityProject.ps1`과 오스티나토 전용 적용·렌더 점검·최종 캡처 브리지만 실행했다. 하네스 검증(`Run-HarnessValidation.ps1`), EditMode/PlayMode 테스트, Windows 빌드, Unity 에디터 재시작과 Git 작업은 실행하지 않았다.

## 최신 조사 중단 - Unity 오스티나토 일반 보행 외형 호환성

- 원본 `enemies model/ostinato walking.fbx`를 `Assets/_Project/Art/Enemies/Ostinato/Models/Ostinato_Walking.fbx`로 바이트 변경 없이 복사했다. 두 파일의 SHA-256은 `B7F6A7148B7A1D66E4EA6DADBA725427B352B76C273E353E06BA53CAAE86FB5A`로 일치한다.
- 보행 FBX의 `char1`은 정점 `3,728개`, UV `3,728개`, 본·바인드포즈 `24개`이며, 정적 승인 모델과 본 이름 및 순서가 일치한다. 포함 애니메이션은 `Armature|Armature|Armature|Armature|walking_man|baselayer`(`1.033333초`, 60fps)와 `mixamo.com`(`1.5초`, 60fps)이다.
- 외형 동기화 전 메시 무수정 호환성을 확인한 결과, 보행 메시에는 서브메시가 `1개`뿐이고 현재 정적 승인 모델은 서브메시 `4개`에 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼을 분리 적용한다.
- `Ostinato_ApprovedReference.mat`은 최종 정적 외형의 4종 PBR 머티리얼이 아니라 정면·측면·후면 승인 이미지를 화면 기준으로 투영하는 과거 비교용 셰이더이므로 대체 수단으로 사용하지 않았다.
- 사용자 지시에 따라 메시, 정점, 인덱스, UV, 서브메시, 스킨 웨이트, 바인드포즈를 수정하지 않았다. 승인된 외형을 같은 부위에 배치할 수 없으므로 `ApplyOstinatoWalkingAnimation`과 런타임 캡처를 실행하지 않았으며, 3번 슬롯은 `Ostinato_03_Static_Review` 상태로 유지했다.
- 조사 결과는 `docs/validation/ostinato_walking_2026-07-19/Ostinato_WalkingSourceInspection.txt`에 기록했다. 하네스 검증, EditMode/PlayMode 테스트, Windows 빌드, Unity 재시작, Blender 작업, Git 작업은 실행하지 않았다.
