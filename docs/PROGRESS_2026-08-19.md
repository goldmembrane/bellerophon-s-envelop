# 2026-08-19 진행 기록

## 이슈판트 신규 Mixamo 이동 모션 연결

- 사용자 지정 `enemies model/išpant-new walking.fbx`를 `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_New_Walking_Source.fbx`로 원본 그대로 복사했다. 양쪽 파일은 7,740,064바이트, SHA-256 `7132D83B27CD5C0C11D6D7F014F3138473312D5BC7645623C2EB86A6788B1C5A`로 일치한다.
- FBX의 두 Take 중 사용자 지정에 따라 `mixamo.com`만 선택했다. 해당 Take는 0–61프레임, 60fps, 1.016667초이며 반복 재생으로 설정했다.
- 공급 원본과 현재 `Ispant_New_Direct_Source.fbx`는 Generic 24본 리그이고 본 이름·부모 계층이 정확히 일치한다. Avatar 생성이나 수동 본 매핑 없이 Mixamo Generic 본 커브를 직접 연결했다.
- `Ispant_New_Walking_InPlace.anim`을 새로 만들었다. 원본의 자세·발 동작·골반 수직 바운스는 유지하고, 약 1.84m 전진하던 골반 로컬 X/Y 위치만 고정해 `Ispant_03_Move` 슬롯 안에서 반복되도록 했다.
- 별도 `Ispant_Approved_LongSword_10K` 메시가 골반에서 분리되지 않도록 같은 Mixamo 골반 움직임에서 계산한 위치·회전 커브를 적용 클립에 베이크했다. 모델 프리팹이나 씬 계층에는 제약 컴포넌트를 남기지 않았다.
- `Ispant_New_Walking.controller`를 만들고 현재 배치된 `Approved Ispant Enemy Placement/Ispant_03_Move/Ispant_New_Direct_Model`의 Animator에 연결했다. Root Motion은 비활성화했다.
- 전 주기 검사 결과 골반 수평 이동 `0m`, 수직 이동 `0.1025192m`, 양발 최대 이동 `0.7628192m`, 장검 상대 위치 최대 오차 `0.000612372m`, 상대 각도 최대 오차 `0°`, 렌더러 2개 유지로 판정됐다.
- Unity AnimationMode에서 4주기 반복 재생 후 정지했으며 씬 Transform 복구와 씬 비변경 검사를 통과했다.
- 기존 직접 모델 FBX SHA-256 `5CE54F6117AF08F141BC18A0E46C823AD07877D815DA2906D59CA2967A4974FF`와 직접 FBX 씬 참조 12개를 유지했다. 다른 11개 이슈판트 슬롯과 배치 밖 씬 루트는 변경하지 않았다.
- 최종 씬 SHA-256은 `125A10C775EAA23E9B459B37B4F02F5D2A35A77B5DF3765AA669E199348ED142`다.
- 상세 기록: `docs/validation/ispant_move_2026-08-19/Ispant_New_Walking_Inspection.md`

## 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 수동 재리깅·수동 본 매핑·Avatar 생성
- 원본 FBX 재저장·변환
- 모델·메시·본·웨이트·UV·머티리얼·텍스처 수정
- 무기 메시 분리·교체
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- AI·내비게이션·전투·물리 이동 구현
- Git 작업

## 이슈판트 신규 Mixamo 발검 모션 연결(초기 적용, 아래 수정 기록으로 대체됨)

- 사용자 지정 `enemies model/išpant-new draw sword.fbx`를 `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_New_DrawSword_Source.fbx`로 원본 그대로 복사했다. 양쪽 파일은 7,776,608바이트, SHA-256 `EFF460E3201EFF5749A13705898B019C68036F25A7FEEFC9B18F7503FCEF1F81`로 일치한다.
- `mixamo.com` Take의 0–90프레임, 60fps, 1.5초 Mixamo 동작을 현재 `Ispant_04_DrawSword` 모델에 연결했다. 공급 FBX와 현재 모델의 Generic 24본 이름·부모 계층이 일치해 Avatar 생성이나 수동 본 매핑은 하지 않았다.
- 원본 시작·끝 자세 차이가 커 직접 반복하면 끊기므로, 공급된 프레임을 순방향 뒤 역방향으로 재생하는 3초 반복 클립 `Ispant_New_DrawSword_Loop.anim`을 만들었다. 시작·끝 위치와 각도 차이는 모두 0이다.
- 현재 10K 장검의 칼자루 중심을 오른손 손바닥에 맞추고, 기존 승인 발검 마운트 방향을 기준으로 오른손을 따르는 Transform 커브를 전 프레임에 베이크했다. 장검은 20,409정점·19,950삼각형의 비스키닝 메시이며 메시 변형, 본 웨이트, 블렌드셰이프, 제약 컴포넌트를 추가하지 않았다.
- `Ispant_New_DrawSword.controller`를 만들고 현재 배치된 `Approved Ispant Enemy Placement/Ispant_04_DrawSword/Ispant_New_Direct_Model`의 Animator에 연결했다. Root Motion은 비활성화했다.
- 181프레임 검사 결과 최대 칼자루–오른손 손바닥 거리는 `0.000015263m`, 손 대비 장검 상대 각도 최대 편차는 `0°`, 루프 이음새 위치·각도 차이는 모두 0으로 판정됐다.
- Unity AnimationMode에서 6주기 반복 재생 후 정지했으며 Transform과 씬 상태 복구를 통과했다. 캡처는 생성하지 않았다.
- 기존 직접 모델 FBX SHA-256 `5CE54F6117AF08F141BC18A0E46C823AD07877D815DA2906D59CA2967A4974FF`를 유지했다. 다른 11개 이슈판트 슬롯과 배치 밖 씬 루트는 변경하지 않았다.
- 최종 씬 SHA-256은 `E07192ED87DD9225E262C72847CE869496B1537BE3AF9B190D7B70156B234FB5`다.
- 상세 기록: `docs/validation/ispant_draw_sword_2026-08-19/Ispant_New_DrawSword_Inspection.md`

## 이 발검 모션 작업에서 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 수동 재리깅·수동 본 매핑·Avatar 생성
- 원본 FBX 재저장·변환
- 모델·메시·본·웨이트·UV·머티리얼·텍스처 수정
- 장검 메시 분리·교체 또는 장검 스키닝
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- AI·내비게이션·전투·물리 이동 구현
- 캡처 생성
- Git 작업

## 이슈판트 발검 모션 정방향 반복 및 장검 실시간 추종 수정(1차 방향 적용, 아래 반전 기록으로 대체됨)

- 사용자 제공 5.7239초 Unity 영상을 확인해 초기 결과의 역방향 반복과 몸 안쪽을 향한 장검 축을 재현했다.
- 초기 3초 순방향·역방향 클립을 원본과 동일한 1.5초·60fps 정방향 전용 클립으로 교체했다. 0–90프레임을 원본 시간 그대로 재생한 뒤 마지막 프레임 다음에 첫 프레임으로 즉시 돌아가며 역순 프레임은 없다.
- 장검의 베이크된 Transform 커브를 제거했다. `IspantRigidSwordFollower`가 Animator 갱신 뒤 `LateUpdate`에서 현재 `RightForeArm → RightHand` 방향으로 칼날 로컬 `-X` 축을 맞추고, 칼자루 중심을 오른손 손바닥 기준점에 매 프레임 다시 배치한다.
- 장검은 오른팔과 오른손의 현재 위치·회전 변화에 실시간 반응하며 기존 20,409정점·19,950삼각형 비스키닝 메시를 유지한다. 메시 변형, 본 웨이트, 블렌드셰이프, 장검 교체는 없다.
- 전 91프레임 검사 결과 최대 칼자루–손바닥 거리는 `0.000170872m`, 최대 칼날–오른팔 바깥 방향 각도는 `0.01978234°`, 최소 방향 내적은 `0.9999999`다.
- 정방향 마지막 자세에서 첫 자세로 즉시 복귀하는 차이는 위치 `0.47708m`, 각도 `177.0693°`다. 이 구간을 보간하거나 역재생하지 않는다.
- Unity AnimationMode에서 9주기 반복 재생 후 정지했으며 Transform과 씬 상태 복원을 통과했다.
- 컨트롤러와 실시간 추종 컴포넌트는 `Ispant_04_DrawSword`에 각각 1개만 연결했다. 다른 11개 이슈판트 슬롯과 배치 밖 씬 루트는 변경하지 않았다.
- 직접 모델 FBX SHA-256은 `5CE54F6117AF08F141BC18A0E46C823AD07877D815DA2906D59CA2967A4974FF`, 최종 씬 SHA-256은 `729EAAB81F4416425F521F188E79FBD6023DD035B3090D6DBB82F6E3CCBAC400`다.
- 상세 기록: `docs/validation/ispant_draw_sword_fix_2026-08-19/Ispant_New_DrawSword_ForwardOnly_Inspection.md`

## 이 발검 수정 작업에서 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 역방향 애니메이션 생성 또는 재생
- 수동 재리깅·수동 본 매핑·Avatar 생성
- 원본 FBX 수정
- 장검 메시 변형·스키닝·분리·교체
- 다른 이슈판트 슬롯 및 배치 밖 씬 루트 수정
- AI·내비게이션·전투·물리 이동 구현
- 새 Unity 영상 또는 이미지 캡처 생성
- Git 작업

## 이슈판트 발검 장검 방향 180도 반전

- 사용자 제공 5.116766초 Unity 영상에서 실제 칼날이 오른팔의 `RightForeArm → RightHand` 방향과 같은 쪽을 향한 문제를 확인했다.
- 사용자 지시에 따라 칼자루–손바닥 결합과 실시간 오른팔 추종은 유지하고, 실제 칼날 로컬 `-X` 축을 정확한 반대인 `RightHand → RightForeArm` 방향으로 180도 반전했다.
- 발검 애니메이션은 원본과 동일한 1.5초·60fps 정방향 전용 루프다. 마지막 프레임 다음에 첫 프레임으로 즉시 돌아가며 역방향 프레임은 없다.
- `IspantRigidSwordFollower`는 Animator 갱신 뒤 `LateUpdate`에서 오른팔·오른손의 현재 Transform으로 장검 위치와 각도를 매 프레임 다시 계산한다.
- 전 91프레임 검사 결과 실제 칼날과 요청 반대 방향의 최대 각도 오차는 `0°`, 최소 내적은 `0.9999999`, 최대 칼자루–손바닥 거리는 `0.000170865m`다.
- Unity AnimationMode에서 9주기 반복 재생 후 정지했으며 Transform과 씬 상태 복원을 통과했다.
- 컨트롤러와 실시간 추종 컴포넌트는 `Ispant_04_DrawSword`에 각각 1개만 연결했다. 직접 모델과 장검 메시, 다른 11개 이슈판트 슬롯, 배치 밖 씬 루트는 변경하지 않았다.
- 직접 모델 FBX SHA-256은 `5CE54F6117AF08F141BC18A0E46C823AD07877D815DA2906D59CA2967A4974FF`, 최종 씬 SHA-256은 `954424D80F2B5B536A50B56B1AD25D632D4D7B7E2086DC702C9C3D44B053E25C`다.
- 상세 기록: `docs/validation/ispant_draw_sword_fix_2026-08-19/Ispant_New_DrawSword_BladeOpposite_Inspection.md`

## 이 장검 방향 반전 작업에서 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 역방향 애니메이션 생성 또는 재생
- 수동 재리깅·수동 본 매핑·Avatar 생성
- 원본 FBX 수정
- 장검 메시 변형·스키닝·분리·교체
- 다른 이슈판트 슬롯 및 배치 밖 씬 루트 수정
- AI·내비게이션·전투·물리 이동 구현
- 새 Unity 영상 또는 이미지 캡처 생성
- Git 작업

## 이슈판트 발검 칼자루 외향 이동 및 칼날 상향 전환

- `Ispant_04_DrawSword`의 칼자루 기준점을 오른손 손바닥에서 칼날 반대쪽으로 요청값 `0.1m` 이동했다. 손과 칼자루 형상의 접촉 한도를 넘으면 자동 축소하도록 했으며, 현재 모델에는 축소 없이 `0.1m`가 적용됐다.
- 칼날은 기존 승인 시작 방향을 유지한 채 발검 전 구간에서 `SmoothStep` 기반으로 자연스럽게 회전해 마지막 프레임에서 모델의 위쪽 축을 향하도록 했다.
- 전 91프레임 검사 결과 칼자루 거리 최대 오차는 `0.000121459m`, 최소 접촉 여유는 `0.1295469m`다.
- 칼날은 위쪽 축과 `58.58714°`에서 시작해 `0°`로 끝났고, 중간 90개 모든 프레임에서 방향이 변했다. 프레임당 최대 각도 변화는 `5.321118°`다.
- 장검은 20,409정점·19,950삼각형의 비스킨 강체 메시를 유지하며, `LateUpdate`에서 오른팔·오른손 움직임을 실시간 추종한다.
- 애니메이션은 원본과 동일한 `1.5초`, `60fps` 정방향 전용 루프이며 마지막 자세 다음에 역재생 없이 첫 자세로 즉시 복귀한다.
- Unity AnimationMode에서 7회 반복 재생 후 중지했고 Transform과 씬 상태 복원을 통과했다. 새 캡처는 생성하지 않았다.
- 컨트롤러와 실시간 추종 컴포넌트는 씬에 각각 1개만 참조된다. 다른 11개 이슈판트 슬롯, 배치 밖 씬 루트, 직접 모델 FBX와 장검 메시를 변경하지 않았다.
- 직접 모델 FBX SHA-256은 `5CE54F6117AF08F141BC18A0E46C823AD07877D815DA2906D59CA2967A4974FF`, 최종 씬 SHA-256은 `58F4CD42F749B9F8577A6BF0082B101FEE87330D4902C9FFE35E9C729FD7DCAC`다.
- 상세 기록: `docs/validation/ispant_draw_sword_fix_2026-08-19/Ispant_New_DrawSword_GripOffset_UpwardFinish_Inspection.md`

## 이 칼자루·칼날 수정 작업에서 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 역방향 애니메이션 생성 또는 재생
- 수동 재리깅·수동 본 매핑·Avatar 생성
- 원본 FBX 수정
- 장검 메시 변형·스키닝·분리·교체
- 다른 이슈판트 슬롯 및 배치 밖 씬 루트 수정
- AI·내비게이션·전투·물리 이동 구현
- 새 Unity 영상 또는 이미지 캡처 생성
- Git 작업

## 이슈판트 발검 칼날 시각적 위쪽 기준 수정

- 사용자 확인으로 기존 종료 기준 `animatedModel.up`이 화면상 아래쪽을 가리키는 문제를 확인해, 정확한 반대축인 `-animatedModel.up`으로 종료 목표를 변경했다.
- 칼날은 시각적 위쪽 축과 `121.4129°`에서 시작해 발검 전체 90개 전환 프레임에서 연속 회전하고 마지막에 `0°`로 정렬된다. 프레임당 최대 각도 변화는 `5.971924°`다.
- 칼자루 외향 이동은 `0.1m`를 유지했다. 전 91프레임에서 거리 최대 오차는 `0.000123993m`, 손·칼자루 최소 접촉 여유는 `0.1295443m`다.
- 수치 검사 통과 뒤 시작·25%·50%·75%·종료 전신, 종료 손잡이 근접, 종료 측면의 총 7개 격리 렌더를 직접 확인했다.
- 직접 확인 결과 칼날은 중간에 대각선 위쪽으로 올라가 종료 전신·측면에서 화면 위쪽을 향한다. 종료 근접 화면에서 칼자루는 오른손 손바닥·손가락 범위 안에 있고 허공에 분리된 틈이 보이지 않았다.
- Unity AnimationMode에서 7회 반복 재생 후 중지했고 Transform과 씬 상태 복원을 통과했다.
- 다른 11개 이슈판트 슬롯, 배치 밖 씬 루트, 직접 모델 FBX와 장검 메시를 변경하지 않았다. 컨트롤러와 실시간 추종 컴포넌트는 씬에 각각 1개다.
- 직접 모델 FBX SHA-256은 `5CE54F6117AF08F141BC18A0E46C823AD07877D815DA2906D59CA2967A4974FF`, 최종 씬 SHA-256은 `58F4CD42F749B9F8577A6BF0082B101FEE87330D4902C9FFE35E9C729FD7DCAC`다.
- 상세 기록: `docs/validation/ispant_draw_sword_fix_2026-08-19/Ispant_New_DrawSword_VisibleUp_Visual_Inspection.md`

## 이 시각적 위쪽 수정 작업에서 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 역방향 애니메이션 생성 또는 재생
- 원본 FBX 수정
- 장검 메시 변형·스키닝·분리·교체
- 다른 이슈판트 슬롯 및 배치 밖 씬 루트 수정
- 전체 화면 또는 다른 프로그램이 포함된 데스크톱 캡처
- Git 작업

## 이슈판트 발검 모션 왼팔 메시 늘어짐 제거

- 사용자 영상 121프레임을 확인하고 12fps 기준 43개 분석 프레임에서 이슈판트 기준 왼팔 메시가 어깨에서 손 방향으로 길게 당겨지는 현상을 확인했다.
- 원본 Mixamo 애니메이션과 현재 직접 모델은 본 경로가 같지만 왼팔 휴식 자세 회전이 최대 `33.95817°` 달랐다. 본 길이와 스케일은 전 프레임에서 안정적이므로 스케일 애니메이션이 아니라 서로 다른 휴식 자세의 절대 회전과 혼합 웨이트 정점이 원인이었다.
- 발검 클립의 왼쪽 `Shoulder`, `Arm`, `ForeArm` 회전 12개 곡선만 현재 직접 모델의 안정된 휴식 회전으로 고정했다. 왼팔은 애니메이션된 몸통 부모 움직임을 계속 따라가며, 오른팔 발검 곡선과 장검 추종은 유지했다.
- 수정 뒤 상완·전완 본 길이 변화가 없고 본 스케일 최대 편차는 `0.000001788`이었다. 왼팔 영향 정점의 본 선분 기준 최대 표면 이격은 `0.9664012m`에서 `0.5840566m`로 감소했다.
- 시작·25%·50%·원본 최악 시점·수정 후 수치상 최악 시점·종료·전신의 총 7개 격리 렌더를 원본 해상도로 직접 확인했다. 모든 시점에서 왼쪽 어깨 장갑부터 손까지 윤곽이 정상적으로 이어지고 영상의 늘어진 메시가 보이지 않았다.
- 기존 발검 검사를 다시 실행해 `1.5초`, `60fps`, 정방향 루프, 오른손 검 추종 최대 오차 `0.000123993m`, 종료 칼날 위쪽 오차 `0°`, 장검 비스킨 메시 유지, 씬 비변경을 확인했다.
- Unity AnimationMode에서 6회 반복 재생한 뒤 중지했고 씬 상태를 정상 복구했다.
- 원본 Mixamo 애니메이션 FBX, 직접 모델 FBX, 메시, 본 웨이트, 다른 11개 이슈판트 슬롯은 변경하지 않았다. 최종 씬 SHA-256은 `052F1274372D74C3E5A01F9810B1E64A5A707A1FF70B8AA35DB8F5039ECDDC12`다.
- 상세 기록: `docs/validation/ispant_draw_sword_left_arm_fix_2026-08-19/Ispant_New_DrawSword_LeftArmStretch_Inspection.md`

## 이 왼팔 늘어짐 수정 작업에서 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 원본 애니메이션 FBX 및 직접 모델 FBX 수정
- 메시 토폴로지, 본 웨이트, 머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- AI, 내비게이션, 전투 로직 수정
- Git 작업

## 이슈판트 발검 왼팔 메시 늘어짐 제거 재수정(원본 자세 보존)

- 사용자 제공 `2026-08-19 15-00-35` Unity 영상은 `4.872366초`, 영상 프레임 165개였으며 12fps 기준 분석 프레임 58개와 원본 해상도 대표 프레임을 확인했다. 이전 왼팔 회전 고정 뒤에도 `Ispant_04_DrawSword`의 왼팔 메시가 어깨에서 팔 방향으로 늘어나 몸통을 가로지르는 넓은 판처럼 변형되는 현상이 남아 있었다.
- 사용자 지시에 따라 왼팔을 휴식 자세로 고정하던 기존 검사 기준을 폐기했다. 수정 뒤 전 91프레임에서 왼팔을 포함한 모든 Transform 커브가 원본 `Ispant_New_DrawSword_Mixamo`와 `0.000001` 허용값 안에서 동일한지 확인하도록 변경했다.
- 원본 발검 자세와 본 위치·회전·스케일, 원본 메시 웨이트와 bindpose는 그대로 유지했다. `IspantRigidSwordFollower`가 해당 슬롯의 `char1` 렌더러에 `SkinQuality.Bone1`을 적용해 여러 원거리 본의 혼합으로 메시가 늘어나는 대신 각 정점을 단일 지배 본으로 스키닝하도록 했다.
- 발검 전용 수치 검사는 `1.5초`, `60fps`, 정방향 루프, Root Motion 비활성화, `BodySkinQuality=Bone1`로 통과했다. 오른손–장검 추종 최대 오차는 `0.000123993m`, 최소 접촉 여유는 `0.1295443m`, 종료 칼날 위쪽 오차는 `0°`였으며 장검은 20,409정점·19,950삼각형의 비스킨 메시를 유지했다.
- 수치 검사 뒤 시작·25%·50%·75%·종료·종료 손잡이 근접·종료 측면의 최종 격리 렌더 7개를 한 번 생성해 직접 확인했다. 모든 확인 시점에서 왼쪽 어깨–상완–전완–손 윤곽이 정상적으로 이어졌고 사용자 영상의 넓게 늘어진 메시가 보이지 않았다.
- 최종 캡처 경로는 `docs/validation/ispant_draw_sword_fix_2026-08-19/captures`다. 캡처 뒤 씬 비변경을 확인했고 최종 브리지 로그의 컴파일 오류·예외·에러 패턴은 0건이었다.
- `CargoRunMvp.unity`, 원본 애니메이션 FBX, 직접 모델 FBX, 메시 토폴로지, 본 웨이트, bindpose, 다른 이슈판트 11개 슬롯과 배치 밖 씬 루트는 이번 재수정에서 변경하지 않았다.

## 이 왼팔 늘어짐 재수정 작업에서 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 왼팔 또는 몸통의 Transform 자세 변경
- 원본 애니메이션 FBX 및 직접 모델 FBX 수정
- 메시 토폴로지, 본 웨이트, bindpose, 머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 추가 Unity 캡처
- Git 작업

## 이슈판트 발검 왼팔 메시 늘어짐 최종 제거(원본 자세 유지)

- 사용자 제공 `2026-08-19 15-23-00` Unity 영상은 `6.511567초`, 영상 프레임 219개였으며 12fps 기준 78개 분석 프레임에서 발검 초반 왼팔 아래에 손 모양 끝단을 가진 긴 메시가 반복해서 나타나는 것을 확인했다.
- 이전 `SkinQuality.Bone1` 처리는 관절 경계의 같은 삼각형 정점을 서로 다른 단일 본에 강제로 붙였다. 최악 구간은 첫 프레임의 `RightShoulder → LeftShoulder` 경계였고 에지가 휴식 상태 대비 `23.37827배`까지 벌어져 새 영상의 긴 판 모양 변형을 만들었다.
- 직접 모델과 발검 원본은 본 순서 24개, `Hips` 루트 본, 바인드포즈와 외형 경계 크기 `1.3118 × 1.91 × 0.5128298m`가 일치하지만 메시 토폴로지가 각각 8,755정점과 8,983정점으로 달랐다. 가장 가까운 표면 정점의 평균 거리는 `0.000060324m`였고, 웨이트 전사만으로는 변형 수치가 줄지 않아 분리된 관절 경계가 필요한 것으로 확인했다.
- 발검 원본 `char1` 메시를 독립 에셋 `Assets/_Project/Art/Enemies/Ispant/Models/Ispant_New_DrawSword_Body.asset`으로 복제하고 `Ispant_04_DrawSword/Ispant_New_Direct_Model/char1`에만 연결했다. 파생 메시는 8,983정점·10,028삼각형이며 발검 원본과 정점·삼각형·웨이트·바인드포즈가 정확히 일치한다.
- 직접 모델의 기존 24본, `Hips` 루트 본과 머티리얼은 유지했다. `IspantRigidSwordFollower`의 매 프레임 `Bone1` 강제 처리를 제거하고 슬롯 4 본체 렌더러는 `SkinQuality.Bone4`를 사용한다.
- 발검 클립은 원본 `Ispant_New_DrawSword_Mixamo`의 모든 Transform 커브와 `0.000001` 허용값 안에서 동일하다. 본 위치·회전·스케일, 1.5초·60fps 정방향 루프와 Root Motion 비활성화 상태를 변경하지 않았다.
- 전 91프레임 검사에서 오른손–장검 추종 최대 오차 `0.000123993m`, 최소 접촉 여유 `0.1295443m`, 종료 칼날 위쪽 오차 `0°`를 유지했고 씬 비변경으로 통과했다.
- 수치 통과 뒤 최종 캡처 명령을 한 번만 실행했다. 기존 사선·측면·손잡이 확인 7장과 사용자 영상에 대응하는 정면 시작·25%·50% 3장을 포함한 총 10장을 직접 확인했으며 모든 시점에서 왼팔 아래 늘어진 판이 사라지고 어깨·상완·전완·손 윤곽이 정상적으로 이어졌다.
- 최종 캡처 경로는 `docs/validation/ispant_draw_sword_fix_2026-08-19/captures`다. 관련 적용·검사·캡처 브리지 로그에서 컴파일 오류, 예외, 실패 패턴은 0건이며 다른 이슈판트 11개 슬롯과 배치 밖 씬 루트는 변경하지 않았다.

## 이 최종 제거 작업에서 실행하지 않은 항목

- 하네스 검증 전체 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 사용자 지시에 따라 `artSample/` 제작
- 왼팔 또는 몸통의 Transform 자세 및 애니메이션 커브 변경
- 원본 애니메이션 FBX 및 직접 모델 FBX 덮어쓰기
- 텍스처·머티리얼 변경
- 정상 부위 숨김 또는 렌더러 비활성화
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 추가 Unity 캡처
- Git 커밋·푸시

## 이슈판트 4번 발검 왼팔 결함 메시 제거 재수정(15:57:35 영상)

- 사용자 제공 `2026-08-19 15-57-35` Unity 영상은 `5.546233초`, 영상 프레임 187개였으며 12fps 기준 67개 분석 프레임에서 왼팔 아래의 넓은 회색 판 형태 메시가 반복해서 크게 벌어지는 것을 확인했다.
- 이전 발검 FBX 전체 메시 복제는 직접 모델과 다른 8,983정점 토폴로지를 사용해 변형을 악화시켰다. 이를 폐기하고 슬롯 4 파생 메시의 기반을 직접 모델 원본 `char1` 8,755정점·9,798삼각형으로 되돌렸다.
- 직접 모델에서 변형을 만드는 독립 컴포넌트 8개를 확인했다. 시드와 정점 수는 `8177/16`, `8322/34`, `8356/17`, `8373/24`, `8397/7`, `8404/5`, `8460/4`, `8729/6`이며 합계 113정점이다. 모두 다른 본과 혼합되지 않은 `LeftShoulder=1` 웨이트였다.
- 대칭 본 재바인딩 후보는 최대 이격을 `0.9664012m`에서 `0.6216875m`로 줄였지만, 60·62프레임 최종 캡처에서 작은 메시 조각이 캐릭터 밖에 떠 있는 문제가 확인돼 완료 판정을 폐기했다.
- 직접 모델과 발검 원본의 해당 113정점은 최대 거리 `0m`로 정확히 겹치고 발검 원본도 모두 `LeftShoulder=1`이었다. 정상 발검 웨이트를 전사할 수 있는 부위가 아니라 원본부터 잘못 묶인 독립 결함 조각임을 확인했다.
- 사용자 지시대로 자세를 변경하지 않고, 슬롯 4 전용 파생 메시에서 이 8개 컴포넌트의 삼각형 119개만 제거했다. 현재 파생 메시는 8,755정점·9,679삼각형이며 제거 대상 밖 정점 위치·노멀·탄젠트·UV·바인드포즈·본 웨이트와 모든 서브메시 삼각형 순서가 직접 모델 원본과 동일하다.
- 최종 수치 검사는 `1.5초`, `60fps`, 정방향 루프, Root Motion 비활성화, `Bone4`, 본 순서 24개와 `Hips` 루트 본을 확인했다. 모든 Transform 커브는 원본 발검 애니메이션과 허용값 `0.000001` 안에서 동일하다.
- 오른손–장검 추종 최대 오차는 `0.000123993m`, 최소 접촉 여유는 `0.1295443m`, 종료 칼날 위쪽 오차는 `0°`다. 장면 비변경, 다른 슬롯 비변경과 파생 메시 씬 참조 1개를 확인했다.
- 최종 캡처 명령은 수치 통과 뒤 한 번 실행했으나 중간 재바인딩 후보의 부유 조각을 발견한 실패 증거가 됐다. 규칙에 따라 추가 캡처는 실행하지 않았고, 오인 방지를 위해 14장은 `.analysis-temp/ispant04_left_arm_2026-08-19/source_video_155735_review/failed_intermediate_capture`로 이동해 보존했다.
- 최종 적용 로그는 `.analysis-temp/ispant04_left_arm_2026-08-19/source_video_155735_review/apply_remove_malformed_components.log`, 최종 검사 로그는 같은 폴더의 `inspect_removed_components_final.log`다.

## 이 재수정 작업에서 실행하지 않은 항목

- 하네스 검증 전체 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 사용자 지시에 따른 `artSample/` 제작
- 왼팔 또는 몸통의 Transform 자세 및 애니메이션 커브 변경
- 원본 애니메이션 FBX 및 직접 모델 FBX 덮어쓰기
- 정상 정점 데이터·본 웨이트·바인드포즈·텍스처·머티리얼 변경
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 실패 중간 상태 확인 뒤 추가 Unity 캡처
- Git 커밋·푸시

## 이슈판트 4번 발검 왼팔–왼다리 스키닝 분리(16:39:48 영상)

- 사용자 제공 `2026-08-19 16-39-48` Unity 영상은 `7.5701초`, 1920×1000, 영상 프레임 254개였으며 12fps 기준 91개 분석 프레임에서 왼팔 아래의 넓고 긴 장갑 메시와 왼다리 형태 부품이 함께 끌려가는 변형을 확인했다.
- 슬롯 4 파생 메시의 왼팔 4본과 왼다리 4본을 직접 대조한 결과 양쪽 본을 동시에 받는 정점은 742개, 양쪽 영향이 각각 10% 이상인 정점은 177개, 팔·다리 영향이 같은 삼각형에 존재하는 경우는 212개였다.
- 최악 구간은 애니메이션 40프레임의 삼각형 8338이었다. 정점 7443과 7441이 `LeftForeArm`과 `LeftUpLeg`, `Hips`, `Spine02`를 함께 받아 에지가 휴식 상태의 `5.692885배`까지 벌어졌다. 사용자 판단대로 왼팔과 왼다리의 스키닝 맞물림이 직접 원인이었다.
- 해당 형상은 하나의 연속 메시가 아니라 이미 작은 독립 연결 섬으로 나뉘어 있었다. 혼합 정점이 있는 85개 섬을 평균 팔 영향과 순수 팔·다리 정점으로 분류해 팔 부품 26개와 몸통·다리 부품 59개로 구분했다.
- 팔 부품은 기존 왼팔 본 영향만 남기고, 몸통·다리 부품은 기존 왼팔 본 영향을 제거한 뒤 각 부품 안의 남은 기존 웨이트만 정규화했다. 총 804정점의 교차 웨이트를 분리했으며 정점 위치, 삼각형, 노멀, 탄젠트, UV, 바인드포즈, 머티리얼과 본 Transform은 변경하지 않았다.
- 후보 수치에서 혼합 정점은 `742→0`, 결합 삼각형은 `212→0`, 결합 에지 최대 비율은 `5.692885→1.537992`로 감소했다. 실제 적용 뒤 다시 검사해 혼합 정점 0개, 강한 혼합 정점 0개, 결합 삼각형 0개, 관련 최악 에지 없음으로 통과했다.
- 이전에 확인한 결함 독립 조각 8개·113정점·119삼각형 제거 상태를 유지했고, 최종 파생 메시는 8,755정점·9,679삼각형과 `Bone4` 스키닝을 사용한다.
- 발검 애니메이션은 원본과 동일한 `1.5초`, `60fps`, 정방향 루프이며 Root Motion은 비활성화 상태다. 모든 Transform 커브와 자세를 변경하지 않았다.
- 오른손–장검 추종 최대 오차는 `0.000123993m`, 최소 접촉 여유는 `0.1295443m`, 종료 칼날 위쪽 오차는 `0°`를 유지했다. 다른 이슈판트 슬롯과 배치 밖 씬 루트는 변경하지 않았다.
- 수치 통과 뒤 최종 캡처 명령을 한 번 실행했다. 기존 전신·정면·측면 14장과 40·60·62프레임을 포함한 왼팔 근접 7장, 총 21장을 원본 해상도로 직접 확인했다. 모든 시점에서 왼팔–왼다리 늘어짐, 다리 장갑 끌림, 부유 조각과 새 메시 구멍이 보이지 않았다.
- 최종 캡처는 `docs/validation/ispant_draw_sword_fix_2026-08-19/captures`, 적용·검사·캡처 로그는 `.analysis-temp/ispant04_left_arm_2026-08-19/source_video_163948_review/`에 저장했다.

## 이 왼팔–왼다리 분리 작업에서 실행하지 않은 항목

- 하네스 검증 전체 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 사용자 지시에 따른 `artSample/` 제작
- 왼팔·왼다리·몸통의 자세 또는 Transform 애니메이션 커브 변경
- 원본 애니메이션 FBX와 직접 모델 FBX 덮어쓰기
- 정점 위치·삼각형·노멀·탄젠트·UV·바인드포즈·텍스처·머티리얼 변경
- 정상 메시 숨김 또는 렌더러 비활성화
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 최종 1회 외 추가 Unity 캡처
- Git 커밋·푸시

## 이슈판트 4번 발검 왼팔 단일 삼각형 돌출 조각 제거(16:58:59 영상)

- 사용자 제공 `2026-08-19 16-58-59` Unity 영상은 `4.846433초`, 1920×1000이었으며 12fps 기준 58개 분석 프레임에서 넓은 왼팔 늘어짐은 사라졌지만 왼팔 아래에 작은 삼각형 조각 하나가 돌출되는 것을 확인했다.
- 직접 메시의 기존 팔 분류 컴포넌트 26개를 정점 수, 삼각형 수, 휴식 위치와 크기, 팔·다리 웨이트로 대조했다. 영상 형상과 일치한 대상은 `seed 249`의 독립 컴포넌트로, 3정점·1삼각형, 중심 `(-0.3672326, 1.134703, 0.06984243)`, 크기 `(0.04546252, 0.0231849, 0.01128349)`, 지배 웨이트 `LeftForeArm=0.9653599`였다.
- 자세나 애니메이션 커브를 변경하지 않고 슬롯 4 전용 파생 메시에서 해당 컴포넌트의 삼각형 1개만 추가 제거했다. 기존 8개·113정점·119삼각형과 합쳐 현재 제거 서명은 9개·116정점·120삼각형이며, 파생 메시는 8,755정점·9,678삼각형이다.
- 제거 전 직접 메시 토폴로지를 웨이트 분리 기준으로 사용해 기존 분리 서명 `팔 26개 / 몸통·다리 59개 / 변경 정점 804개`를 그대로 유지했다. 정점 위치, 노멀, 탄젠트, UV, 바인드포즈, 머티리얼과 본 Transform은 변경하지 않았다.
- 최종 수치 검사에서 팔–다리 혼합 정점 0개, 강한 혼합 정점 0개, 결합 삼각형 0개, 강한 결합 삼각형 0개, 관련 최악 에지 없음으로 통과했다. 애니메이션은 원본과 같은 `1.5초`, `60fps`, Root Motion 비활성 상태이며 검 추종 최대 오차 `0.000123993m`, 최소 접촉 여유 `0.1295443m`, 종료 칼날 위쪽 오차 `0°`를 유지했다.
- 수치 통과 뒤 최종 캡처 명령을 한 번 실행해 `docs/validation/ispant_draw_sword_fix_2026-08-19/captures`의 21장을 갱신했다. 모든 전신·정면·측면·왼팔 근접 시점에서 단일 삼각형 돌출 조각이 사라졌고 정상 왼팔 장갑에 새 구멍이나 추가 손실이 없음을 원본 해상도로 확인했다.
- 적용 로그는 `.analysis-temp/ispant04_left_arm_2026-08-19/source_video_165859_review/apply_remove_single_triangle_fragment_final.log`, 수치 검사 로그는 같은 폴더의 `inspect_single_triangle_fragment_final.log`, 최종 캡처 로그는 `capture_single_triangle_fragment_final.log`다.

## 이 단일 삼각형 조각 제거 작업에서 실행하지 않은 항목

- 하네스 검증 전체 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- 사용자 지시에 따른 `artSample/` 제작
- 왼팔·왼다리·몸통의 자세 또는 Transform 애니메이션 커브 변경
- 원본 애니메이션 FBX와 직접 모델 FBX 덮어쓰기
- 대상 3정점·1삼각형 밖 정상 메시 제거 또는 렌더러 비활성화
- 정점 위치·노멀·탄젠트·UV·바인드포즈·텍스처·머티리얼 변경
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- 최종 1회 외 추가 Unity 캡처
- Git 커밋·푸시
