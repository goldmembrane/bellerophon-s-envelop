# 진행 기록 2026-06-21

## 오늘 작업 범위

- 현재 날짜를 `2026-06-21`로 확인했다.
- 오늘 날짜 진행문서 `docs/PROGRESS_2026-06-21.md`가 없어 최신 진행문서 `docs/PROGRESS_2026-06-19.md`를 기준으로 최근 상태를 확인했다.
- Unity 에디터를 승인된 전용 스크립트 `.\scripts\Open-UnityProject.ps1`로 열었다.
- 이어서 작업을 시작하기 위한 오늘 날짜 진행문서를 미리 생성했다.

## 최근 진행 상태 요약

- ER-09 체력 스크린은 승인된 엔진룸 09 체력 스크린 루트 기준으로 Unity 배치가 진행된 상태다.
- ER-10 오버클럭 레버 스위치는 `artSample/engine_room_overclock_control/` 샘플 승인 후 ER-09 스크린 오른쪽 동력벽 범위 안에 Unity 배치가 반영된 상태다.
- ER-15 손전등 충전 슬롯은 `artSample/engine_room_flashlight_charging_dock/` 샘플 승인 후 ER-09 좌측 보조 스크린보다 더 왼쪽의 동력벽 안쪽에 Unity 배치가 반영된 상태다.
- ER-20 칸타빌레 공명 경고등은 `artSample/engine_room_cantabile_warning_light/` 샘플과 Unity 배치 반영이 진행된 상태다.
- ER-11 깨진 체력 스크린은 `artSample/engine_room_broken_health_screen/` 샘플을 ER-09 기반으로 조정 중이며, 승인 전 Unity 런타임 씬이나 프리팹에는 연결하지 않는 범위로 관리해야 한다.
- ER-09 디스플레이 화면 좌우반전 및 파손 화면 프리뷰 관련 수정이 있었고, 이후 `Ensure`, `Validate`, `Smoke`, `Test`, `Build` 계열 검증은 아직 별도 실행 대상으로 남아 있다.

## Unity 에디터 실행

- 실행 명령:
  - `.\scripts\Open-UnityProject.ps1`
- 결과:
  - `D:\Bellerophon2\Bellerophon` 프로젝트용 Unity 에디터가 열렸다.
  - 출력 기준 Unity 프로세스 ID는 `18372`이다.

## 오늘 생성한 문서

- 생성 파일:
  - `docs/PROGRESS_2026-06-21.md`
- 목적:
  - 다음 작업을 과거 날짜 문서에 고정하지 않고 오늘 날짜 기준으로 이어갈 수 있도록 작업 기준 문서를 마련했다.

## 실행하지 않은 작업

- Unity Refresh
- Ensure 계열 명령
- Validate 계열 명령
- Smoke 계열 명령
- EditMode 테스트
- PlayMode 테스트
- Windows 개발 빌드
- Unity 씬, 프리팹, 런타임 자산 수정
- 기존 진행문서 또는 기획 문서 수정

## 다음 작업 후보

- ER-11 깨진 체력 스크린 샘플을 사용자 피드백 기준으로 계속 조정한다.
- ER-20 칸타빌레 공명 경고등의 실제 공명 상태 로직, 점멸 또는 회전 관련 애니메이션 구현 범위를 별도 승인 후 진행한다.
- ER-10 오버클럭 레버와 ER-15 손전등 충전 슬롯의 실제 게임 로직 구현 범위를 별도 승인 후 진행한다.
- 조종석 관련 수정이 필요하면 기존 엔진룸 오브젝트 범위와 섞지 않고 별도 작업 범위로 승인받아 진행한다.
- 코드 또는 Unity 배치 변경 후에는 승인된 범위 안에서 검증 사다리를 낮은 단계부터 실행한다.

## 통제실 오브젝트 문서화

- 사용자가 동력실 모델링을 필요한 만큼 구현했으므로 다음 범위를 통제실로 전환한다고 밝혔다.
- 원본 기획서 `docs/GAME_DESIGN_SOURCE.txt`, 구현 순서 `docs/MVP_IMPLEMENTATION_ORDER.md`, 기존 형식 참고용 `docs/ENGINE_ROOM_OBJECTS.md`, 오늘 진행문서 `docs/PROGRESS_2026-06-21.md`를 승인된 범위 안에서 확인했다.
- 새 문서 `docs/CONTROL_ROOM_OBJECTS.md`를 생성했다.
- 통제실 룸 셸, 이송창고/조종실/동력실/무기실 방향 출입구, 대형/가로형/세로형 스크린, 구역 상태 표시, CCTV 화면과 전환 조작부, 복도 폐쇄 상태, 침입 감지 시스템, 화물 손상 경고 표시, 손상 상태 시각 요소, 상호작용 앵커, 복도 방향 표시 레이블을 정리했다.
- 원본에 기능은 있으나 구체 외형이 없는 항목은 임의 확정하지 않고 확인 필요로 표시했다.
- Unity 실행, Refresh, Ensure, Validate, Smoke, Test, Build는 실행하지 않았다.
- Unity 씬, 프리팹, 런타임 자산, UI 흐름은 수정하지 않았다.

## 동력실 복구 스냅샷 생성

- 사용자가 통제실 작업 전, 현재 만들어둔 동력실 전체를 나중에 예상치 못한 문제 발생 시 현재 상태 그대로 복구할 수 있게 보존하라고 요청했다.
- 오브젝트를 건드리지 않는 조건으로, Unity 브리지나 Refresh 없이 디스크에 저장된 파일만 복사하는 방식으로 진행했다.
- 생성 위치:
  - `Backups/engine_room_snapshot_2026-06-21_145754/`
- 포함한 주요 범위:
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `Assets/_Project/Editor/Validation/ApprovedEngineRoomHealthScreenBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Art/Ship/EngineRoom/`
  - `docs/ENGINE_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
  - `artSample/engine_room_*`
  - `scripts/*EngineRoom*`
- `git status --short`에서 `Assets/_Project/Art/Ship/EngineRoom/` 아래 동력실 머티리얼 파일들이 수정 상태로 확인되어 사용자 추가 승인 후 해당 폴더 전체를 백업에 포함했다.
- 스냅샷 폴더에는 `README.md`를 생성해 포함 범위, 복구 기준, 실행하지 않은 작업을 기록했다.
- 실행하지 않은 작업:
  - Unity 브리지
  - Unity Refresh
  - Ensure, Validate, Smoke, Test, Build
  - Unity 씬 저장, 오브젝트 재생성, 배치 변경
  - git commit, branch, reset, checkout, tag

## CR-01 통제실 룸 셸 샘플 시작

- 사용자가 통제실 `CR-01`에 대한 샘플을 기존 샘플 제작 방식과 동일하게 진행하라고 승인했다.
- 확인한 기준 파일:
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
  - `scripts/GenerateEngineRoomShellSample.py`
  - `scripts/Run-EngineRoomShellSample.ps1`
  - `artSample/engine_room_shell/README.md`
  - `artSample/engine_room_shell/ASSET_MANIFEST.json`
- 신규 생성 파일:
  - `scripts/GenerateControlRoomShellSample.py`
  - `scripts/Run-ControlRoomShellSample.ps1`
- 샘플 생성 대상:
  - `artSample/control_room_shell/`
- 샘플 목표:
  - 통제실 바닥, 벽, 천장 셸
  - 이송창고, 조종실, 동력실, 무기실 방향 출입구
  - 짧은 복도 스텁과 방향 표시
  - 향후 대형 메인 스크린을 넣을 비기능성 빈 벽면 베이
  - 실제 스크린 UI, CCTV, 복도 폐쇄, 침입 감지, 화물 손상 경고 로직은 제외
- 실행 명령:
  - `.\scripts\Run-ControlRoomShellSample.ps1`
- 실행 결과:
  - Blender 샘플 생성 스크립트가 정상 종료했다.
  - 실행 래퍼의 필수 산출물 존재 검사가 통과했다.
  - 방향 레이블은 `이송창고`, `조종실`, `동력실`, `무기실` 한국어 표기로 조정 후 다시 생성했다.
- 생성 산출물:
  - `artSample/control_room_shell/index.html`
  - `artSample/control_room_shell/README.md`
  - `artSample/control_room_shell/ASSET_MANIFEST.json`
  - `artSample/control_room_shell/APPROVAL_STATUS.json`
  - `artSample/control_room_shell/blender/control_room_shell.blend`
  - `artSample/control_room_shell/exports/control_room_shell.fbx`
  - `artSample/control_room_shell/exports/control_room_shell.glb`
  - `artSample/control_room_shell/renders/01_overview.png`
  - `artSample/control_room_shell/renders/02_floor_plan.png`
  - `artSample/control_room_shell/renders/03_main_wall_shell.png`
  - `artSample/control_room_shell/renders/04_cargo_entry.png`
  - `artSample/control_room_shell/renders/05_side_entries.png`
  - `artSample/control_room_shell/renders/06_ceiling_and_wall_panels.png`
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 Unity 배치
  - 동력실 오브젝트 수정

## CR-01 통제실 룸 셸 샘플 천장 제외 조정

- 사용자가 샘플이 알아보기 힘들다고 하며 천장만 빼달라고 요청했다.
- 수정 파일:
  - `scripts/GenerateControlRoomShellSample.py`
- 수정 내용:
  - CR-01 샘플에서 천장 판 생성 호출을 제거했다.
  - 천장 보강재와 천장 케이블 트레이가 생성되지 않도록 했다.
  - 바닥, 벽, 네 방향 출입구, 복도 스텁, 비기능성 메인 스크린 벽면 베이는 유지했다.
  - README, 매니페스트, HTML 설명을 천장 제외 기준으로 갱신했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomShellSample.ps1`
- 실행 결과:
  - 천장 없는 `artSample/control_room_shell/` 샘플이 정상 재생성됐다.
  - 실행 래퍼의 필수 산출물 존재 검사가 통과했다.
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 Unity 배치
  - 동력실 오브젝트 수정

## CR-01 통제실 출입구 가시성 개선

- 사용자가 샘플에서 조종실, 동력실, 무기실 방향 출입구가 알아보기 힘들다고 지적했다.
- 수정 파일:
  - `scripts/GenerateControlRoomShellSample.py`
- 수정 내용:
  - 조종실, 동력실, 무기실 방향 출입구에 큰 색상 포털 프레임을 추가했다.
  - 각 출입구에 더 큰 방향 표시판을 적용했다.
  - 각 출입구 앞 바닥에 방향 가이드 스트립을 추가했다.
  - 동력실/무기실이 같은 동쪽 벽에 붙어 보여 혼동되지 않도록 두 출입구 사이에 분리 스파인을 추가했다.
  - 천장은 계속 제외했다.
  - 이송창고 방향 출입구의 기본 구조는 유지했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomShellSample.ps1`
- 실행 결과:
  - `artSample/control_room_shell/` 샘플이 정상 재생성됐다.
  - 실행 래퍼의 필수 산출물 존재 검사가 통과했다.
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 Unity 배치
  - 동력실 오브젝트 수정

## CR-01 통제실 사용자 구조 변경 반영

- 사용자가 원본 기획서에 적힌 통제실 기본 구조를 일부 변형한다고 확인했다.
- 반영한 사용자 확인 구조:
  - 통제실 내부에 스크린 쪽과 입구 쪽을 나누는 내부 가벽을 추가했다.
  - 내부 가벽에는 출입 가능한 문을 추가했다.
  - 위에서 내려다본 기준 왼쪽에 조종실 복도와 동력실 복도를 배치했다.
  - 조종실 쪽 문은 약 45도 정도 기울어진 형태로 만들었다.
  - 조종실 복도와 동력실 복도는 서로 조금 떨어진 구조로 만들었다.
  - 위에서 내려다본 기준 6시 방향에 운송창고 복도와 무기실 복도를 바로 옆에 붙여 배치했다.
  - 운송창고 복도와 무기실 복도는 서로 붙어 있는 구조로 만들었다.
  - 천장은 계속 제외했다.
- 수정 파일:
  - `scripts/GenerateControlRoomShellSample.py`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 실행 명령:
  - `.\scripts\Run-ControlRoomShellSample.ps1`
- 실행 결과:
  - `artSample/control_room_shell/` 샘플이 사용자 확인 구조 기준으로 정상 재생성됐다.
  - 실행 래퍼의 필수 산출물 존재 검사가 통과했다.
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 Unity 배치
  - 동력실 오브젝트 수정

## CR-01 조종실/동력실 복도 하향 배치 조정

- 사용자가 동력실 복도와 조종실 복도를 더 아래쪽으로 배치해서 조종실 입구가 내부 가벽 기준 위쪽으로 올라가지 않게 해달라고 요청했다.
- 수정 파일:
  - `scripts/GenerateControlRoomShellSample.py`
- 수정 내용:
  - 조종실 45도 출입구 중심을 내부 가벽 아래쪽 입구 영역으로 내렸다.
  - 동력실 출입구 중심도 더 아래쪽으로 내려 조종실/동력실 복도 간격을 유지했다.
  - 조종실/동력실 분리 기둥과 관련 방향 표시, 바닥 가이드, 렌더 카메라 구도를 새 위치에 맞췄다.
  - 운송창고/무기실 6시 방향 붙은 복도 구조, 내부 가벽, 가벽 문, 천장 제외 상태는 유지했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomShellSample.ps1`
- 실행 결과:
  - `artSample/control_room_shell/` 샘플이 조정된 배치로 정상 재생성됐다.
  - 실행 래퍼의 필수 산출물 존재 검사가 통과했다.
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 Unity 배치
  - 동력실 오브젝트 수정

## CR-01 통제실 아래쪽 입구 영역 확장

- 사용자가 가벽 위치를 옮기는 것이 아니라, 통제실 전체 면적을 6시 방향으로 늘려 가벽 아래쪽 영역을 넓히는 것이라고 정정했다.
- 수정 파일:
  - `scripts/GenerateControlRoomShellSample.py`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 수정 내용:
  - 내부 가벽 위치는 유지했다.
  - 통제실 남쪽 경계를 별도 기준으로 분리하고, 6시 방향으로 바닥과 벽 셸 면적을 확장했다.
  - 확장된 아래쪽 입구 영역 안에 조종실 45도 복도와 동력실 복도가 들어가도록 유지했다.
  - 운송창고/무기실 6시 방향 붙은 복도 구조는 확장된 남쪽 경계 기준으로 유지했다.
  - 렌더 카메라와 문서 설명을 넓어진 아래쪽 영역 기준으로 조정했다.
  - 천장은 계속 제외했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomShellSample.ps1`
- 실행 결과:
  - `artSample/control_room_shell/` 샘플이 정상 재생성됐다.
  - 실행 래퍼의 필수 산출물 존재 검사가 통과했다.
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 Unity 배치
  - 동력실 오브젝트 수정

## CR-01 동력실 복도 추가 하향 조정

- 사용자가 동력실 복도 입구를 더 아래로 내려 조종실 복도와 더 떨어지게 해달라고 요청했다.
- 수정 파일:
  - `scripts/GenerateControlRoomShellSample.py`
- 수정 내용:
  - 동력실 출입구 중심을 더 아래쪽으로 이동했다.
  - 동력실 방향 라벨, 색상 포털, 바닥 가이드, 복도 스텁을 새 위치로 맞췄다.
  - 조종실 45도 복도 위치는 유지했다.
  - 조종실/동력실 사이 분리 기둥과 5번 렌더 카메라 구도를 새 간격에 맞췄다.
  - 내부 가벽 위치, 아래쪽 확장 영역, 운송창고/무기실 6시 방향 붙은 복도 구조, 천장 제외 상태는 유지했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomShellSample.ps1`
- 실행 결과:
  - `artSample/control_room_shell/` 샘플이 정상 재생성됐다.
  - 실행 래퍼의 필수 산출물 존재 검사가 통과했다.
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 Unity 배치
  - 동력실 오브젝트 수정

## CR-01 조종실 복도 각도 및 내부 침범 조정

- 사용자가 조종실 복도가 가벽에 겹치지 않게 배치하고, 각도를 40도로 바꾸며, 통제실 내부로 들어온 부분은 삭제해서 샘플을 다시 그리라고 요청했다.
- 수정 파일:
  - `scripts/GenerateControlRoomShellSample.py`
- 수정 내용:
  - 조종실 방향 표시와 포털 표기를 `조종실 40도`로 변경했다.
  - 조종실 복도 방향을 왼쪽 벽 기준 약 40도 기울어진 형태로 조정했다.
  - 조종실 복도 중심을 가벽 아래쪽 입구 영역으로 내려 가벽과 겹치지 않도록 했다.
  - 조종실 복도 몸체는 통제실 벽 바깥에서 시작하도록 이동해 내부로 파고드는 부분이 보이지 않게 했다.
  - 조종실/동력실 사이 분리 기둥을 새 간격 중간으로 다시 배치했다.
  - 동력실 복도, 운송창고/무기실 6시 방향 붙은 복도, 내부 가벽, 아래쪽 확장 영역, 천장 제외 상태는 유지했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomShellSample.ps1`
- 실행 결과:
  - `artSample/control_room_shell/` 샘플이 정상 재생성됐다.
  - 실행 래퍼의 필수 산출물 존재 검사가 통과했다.
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 Unity 배치
  - 동력실 오브젝트 수정

## CR-01 통제실 Unity 반영

- 사용자가 승인한 `artSample/control_room_shell/` CR-01 샘플을 `CargoRunMvp` 씬에 반영했다.
- 수정/추가 파일:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `scripts/Run-ApprovedControlRoomShell.ps1`
  - `Assets/_Project/Art/Ship/ControlRoom/`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
- 반영 방식:
  - 새 루트 `Approved Control Room 01 Shell`만 생성/갱신한다.
  - 기존 `Approved Engine Room 01 Shell`과 승인된 조종실 루트들은 보호 대상으로 스냅샷을 잡고, 적용 전후 transform 및 활성 상태 변경이 없으면 성공 처리한다.
  - 통제실은 조종실 동쪽 옆에 배치하고, 동력실 및 조종실 bounds와 겹치지 않도록 실제 렌더러 bounds 기준으로 위치를 산정한다.
  - 샘플의 천장 제외 상태, 내부 가벽과 출입문, 조종실 40도 복도, 동력실 분리 복도, 운송창고/무기실 6시 방향 붙은 복도를 Unity 프리미티브 기반으로 재현했다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomShell.ps1`
- 실행 결과:
  - 적용 성공.
  - 로그 기준 루트 위치는 `13.21,0.00,19.27`이다.
  - 로그 기준 렌더러 수는 `104`개다.
  - 로그에서 `CockpitUntouched=True`, `EngineRoomUntouched=True`, `ControlRoomPlacedNextToCockpit=True`, `ControlRoomOverlapsEngineRoom=False`, `ControlRoomOverlapsCockpit=False`를 확인했다.
- 실행하지 않은 작업:
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild
  - 동력실 오브젝트 수정
  - 조종실 오브젝트 수정

## CR-01 통제실 사용자 편집값 스크립트 반영

- 사용자가 Unity 에디터에서 편집한 `Approved Control Room 01 Shell` 현재 상태를 재생성 스크립트에 반영했다.
- 수정/추가 파일:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `scripts/Run-ApprovedControlRoomShellCapture.ps1`
  - `artSample/control_room_shell/editor_current/cr01_current_objects.md`
  - `docs/PROGRESS_2026-06-21.md`
- 반영 방식:
  - 열린 `CargoRunMvp` 씬에서 현재 `Approved Control Room 01 Shell` 루트 위치와 하위 transform, 활성 상태를 캡처했다.
  - 캡처된 값을 `UserEditedControlRoomCenter`와 `UserEditedTransformOverrides`에 반영했다.
  - 재생성 시 snapshot에 없는 통제실 생성 오브젝트는 제거되도록 하여 사용자가 삭제한 통제실 오브젝트 상태도 유지한다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomShellCapture.ps1`
  - `.\scripts\Refresh-UnityProject.ps1`
- 실행 결과:
  - 캡처 성공.
  - 최종 리프레시 성공.
- 실행하지 않은 작업:
  - `.\scripts\Run-ApprovedControlRoomShell.ps1`
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild
  - 동력실 오브젝트 수정
  - 조종실 오브젝트 수정

## CR-07 가로형 보조 스크린 샘플 제작

- 사용자가 CR-01 작업 중 CR-02, CR-03, CR-04, CR-05, CR-06을 함께 진행했다고 확인하고, 대형 메인 스크린 오른쪽 상단에 넣을 `CR-07` 샘플 제작을 요청했다.
- 확인한 기준 파일:
  - `docs/PROGRESS_2026-06-21.md`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `scripts/GenerateControlRoomShellSample.py`
  - `artSample/control_room_shell/README.md`
  - `scripts/GenerateEngineRoomHealthScreenSample.py`
  - `scripts/Run-EngineRoomHealthScreenSample.ps1`
- 신규 생성 파일:
  - `scripts/GenerateControlRoomAuxScreenSample.py`
  - `scripts/Run-ControlRoomAuxScreenSample.ps1`
- 샘플 생성 대상:
  - `artSample/control_room_aux_screen/`
- 샘플 목표:
  - CR-06 대형 메인 스크린 오른쪽 상단에 붙는 가로형 보조 스크린의 위치와 비례를 보여준다.
  - 장갑 프레임, 벽면 장착 패드, 진동 방지 가스켓, 케이블 소켓, 서비스 래치, `C2_ElC2Disp.png`가 꽉 찬 화면 표면을 포함한다.
  - CR-06은 위치 관계 확인용 기준면으로만 둔다.
  - 실제 구역 상태 UI, CCTV 피드, 상호작용 로직, Unity 배치는 제외한다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomAuxScreenSample.ps1`
- 실행 결과:
  - 첫 실행은 렌더 진행 중 124초 제한에서 타임아웃됐으나, 산출물 생성은 완료됐다.
  - 같은 명령을 더 긴 제한 시간으로 재실행했고 종료 코드 0으로 정상 완료했다.
  - 실행 래퍼의 필수 산출물 존재 검사가 통과했다.
  - 대표 렌더 확인 후 CR-07 레이블 크기와 전면/근접/전체 관계 카메라 구도를 조정해 다시 생성했다.
  - 최종 재생성도 종료 코드 0으로 정상 완료했다.
- 생성 산출물:
  - `artSample/control_room_aux_screen/index.html`
  - `artSample/control_room_aux_screen/README.md`
  - `artSample/control_room_aux_screen/ASSET_MANIFEST.json`
  - `artSample/control_room_aux_screen/APPROVAL_STATUS.json`
  - `artSample/control_room_aux_screen/blender/control_room_aux_screen.blend`
  - `artSample/control_room_aux_screen/exports/control_room_aux_screen.fbx`
  - `artSample/control_room_aux_screen/exports/control_room_aux_screen.glb`
  - `artSample/control_room_aux_screen/renders/01_context_overview.png`
  - `artSample/control_room_aux_screen/renders/02_front_alignment.png`
  - `artSample/control_room_aux_screen/renders/03_aux_screen_closeup.png`
  - `artSample/control_room_aux_screen/renders/04_side_mount_depth.png`
  - `artSample/control_room_aux_screen/renders/05_top_right_relation.png`
- 문서 갱신:
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 CR-07 Unity 배치
  - 동력실 오브젝트 수정
  - 조종실 오브젝트 수정

## CR-07 메인 스크린 비가림 배치 조정

- 사용자가 CR-07 위치가 메인 스크린을 가리지 않게 다시 배치해달라고 요청했다.
- 수정 파일:
  - `scripts/GenerateControlRoomAuxScreenSample.py`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 수정 내용:
  - CR-07 중심 위치를 오른쪽으로 이동해 장착 패드와 좌측 브래킷까지 CR-06 대형 메인 스크린 표시 면 바깥에 놓이도록 조정했다.
  - 이동된 CR-07에 맞춰 오른쪽 배경 벽 패널, 케이블 레이스웨이, 서비스 심을 조정했다.
  - 전면 정렬, 전체 관계, 근접 렌더 카메라 구도를 새 배치에 맞게 넓혔다.
  - 생성되는 README, HTML, 매니페스트 설명을 메인 스크린 비가림 배치 기준으로 갱신했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomAuxScreenSample.ps1`
- 실행 결과:
  - 샘플 재생성이 종료 코드 0으로 정상 완료됐다.
  - `artSample/control_room_aux_screen/renders/01_context_overview.png`, `02_front_alignment.png`, `03_aux_screen_closeup.png`, `05_top_right_relation.png`를 확인했다.
  - 대표 렌더 기준 CR-07이 CR-06 대형 메인 스크린 표시 면을 가리지 않고 오른쪽 바깥 벽면에 배치된 것을 확인했다.
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 CR-07 Unity 배치
  - 동력실 오브젝트 수정
  - 조종실 오브젝트 수정

## CR-07 상단 배치 및 C2 디스플레이 에셋 적용

- 사용자가 가로 보조 스크린을 메인 스크린보다 조금 더 위에 자리 잡도록 위치를 수정하고, 디스플레이에는 `C2_ElC2Disp.png` 에셋을 스크린에 꽉 차게 넣으라고 요청했다.
- 확인한 에셋:
  - `Assets/Heavy Station Kit/_common/Textures/GUI/C2_ElC2Disp.png`
- 수정 파일:
  - `scripts/GenerateControlRoomAuxScreenSample.py`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 수정 내용:
  - CR-07 중심 높이를 CR-06 대형 메인 스크린 상단보다 조금 더 위로 올렸다.
  - 기존 임시 구획선 대신 `C2_ElC2Disp.png`를 Blender 이미지 텍스처 머티리얼로 로드했다.
  - CR-07 표시 면에 별도 XZ 평면을 만들고 UV 0~1로 매핑해 이미지가 스크린 전체를 채우도록 했다.
  - 올라간 배치에 맞춰 배경 벽 패널, 케이블 레이스웨이, 렌더 카메라 구도를 조정했다.
  - 생성되는 README, HTML, 매니페스트 설명을 상단 배치 및 C2 디스플레이 에셋 적용 기준으로 갱신했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomAuxScreenSample.ps1`
- 실행 결과:
  - 샘플 재생성이 종료 코드 0으로 정상 완료됐다.
  - `artSample/control_room_aux_screen/renders/01_context_overview.png`, `02_front_alignment.png`, `03_aux_screen_closeup.png`, `05_top_right_relation.png`를 확인했다.
  - 대표 렌더 기준 CR-07이 CR-06 대형 메인 스크린보다 위쪽에 자리 잡고, `C2_ElC2Disp.png`가 표시 면 전체에 들어간 것을 확인했다.
- 실행하지 않은 작업:
  - Unity 씬/프리팹/런타임 자산 수정
  - Unity 브리지, Refresh, Ensure, Validate, Smoke, Test, Build
  - 통제실 CR-07 Unity 배치
  - 동력실 오브젝트 수정
  - 조종실 오브젝트 수정

## CR-07 가로형 보조 스크린 Unity 반영

- 사용자가 `artSample/control_room_aux_screen/` 샘플을 승인하고 Unity에 그대로 구현하라고 요청했다.
- 추가 조건:
  - 동력실 오브젝트를 건드리지 않는다.
  - 조종실 오브젝트를 건드리지 않는다.
  - 기존 통제실 오브젝트도 건드리지 않는다.
- 수정/추가 파일:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomAuxScreenBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `scripts/Run-ApprovedControlRoomAuxScreen.ps1`
  - `Assets/_Project/Art/Ship/ControlRoom/M_Cr07_*.mat`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 반영 방식:
  - 새 루트 `Approved Control Room 07 Aux Screen`만 생성/갱신한다.
  - 기존 `Approved Control Room 01 Shell`은 부모로 사용하지 않고, 위치 계산 기준으로만 렌더러 bounds를 읽는다.
  - 기존 통제실, 조종실, 동력실 루트는 적용 전후 transform, 활성 상태, renderer/material 참조를 보호 확인한다.
  - CR-07은 CR-01 메인 스크린보다 조금 위쪽의 오른쪽 바깥 영역에 배치한다.
  - `Assets/Heavy Station Kit/_common/Textures/GUI/C2_ElC2Disp.png`를 CR-07 표시 면에 꽉 차게 적용한다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomAuxScreen.ps1`
- 실행 결과:
  - 적용 성공.
  - 로그 기준 루트 bounds 중심은 `16.34,3.05,22.22`이다.
  - 로그 기준 렌더러 수는 `20`개다.
  - 로그에서 `ControlRoomUntouched=True`, `CockpitUntouched=True`, `EngineRoomUntouched=True`를 확인했다.
  - 로그에서 `AuxScreenAboveMainScreen=True`, `DisplayTextureApplied=True`, `AuxScreenOverlapsEngineRoom=False`, `AuxScreenOverlapsCockpit=False`를 확인했다.
- 실행하지 않은 작업:
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild
  - 기존 통제실 오브젝트 수정
  - 동력실 오브젝트 수정
  - 조종실 오브젝트 수정
  - CR-08 이후 오브젝트 구현
  - 실제 CCTV, 구역 상태, 상호작용 로직 연결

## 주의사항

- 아트, 모델링, 텍스처링 작업은 승인 전 `artSample/` 산출물로만 관리한다.
- 승인된 `artSample/` 결과물을 Unity에 반영할 때는 단순 존재 확인이 아니라 시각적 재현율을 기준으로 비교해야 한다.
- 기존 ER-01~ER-10, ER-15, ER-20 또는 조종석 관련 오브젝트는 사용자 승인 없이 위치, 크기, 회전, 머티리얼, 런타임 연결을 변경하지 않는다.
- `Library`, `Temp`, `Logs`, `UserSettings`, `Builds`, `TestResults`는 생성물로 취급하며 직접 편집하지 않는다.
- Unity 에디터 실행은 계속 `.\scripts\Open-UnityProject.ps1`를 사용하고, 변경 반영은 재시작보다 `.\scripts\Refresh-UnityProject.ps1`를 우선한다.
## CR-01 현재 편집 상태 복구 스크립트 반영

- 사용자가 Unity 에디터에서 직접 편집한 현재 CR-01 상태를 나중에 그대로 복구할 수 있도록 스크립트에 반영해 달라고 요청했다.
- 승인된 범위:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomShellCapture.ps1`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `artSample/control_room_shell/editor_current/cr01_current_objects.md`
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomShellBootstrap.cs`
  - `docs/PROGRESS_2026-06-21.md`
- 실행 결과:
  - 열린 Unity 에디터 기준 현재 `Approved Control Room 01 Shell` 오브젝트 상태를 캡처했다.
  - 캡처 결과는 `artSample/control_room_shell/editor_current/cr01_current_objects.md`에 저장됐다.
  - 캡처된 `UserEditedControlRoomCenter` 및 `UserEditedTransformOverrides` 값 목록과 `ApprovedControlRoomShellBootstrap.cs`의 복구 블록을 맞췄다.
  - 현재 캡처에는 없는 `Dressing - individually editable/CR-01 side wall utility conduit east +2.60` 오버라이드 1개를 복구 스크립트에서 제거했다.
- 실행하지 않은 작업:
  - `.\scripts\Run-ApprovedControlRoomShell.ps1`
  - CR-01 재생성/복구 적용
  - CR-07 수정, 삭제, 재배치
  - 동력실/조종실 오브젝트 수정
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild
## CR-08 세로형 보조 스크린 묶음 샘플 제작

- 사용자가 CR-08 세로형 보조 스크린 샘플 제작을 요청했다.
- 기준 문서:
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/GAME_DESIGN_SOURCE.txt`
  - `docs/MVP_IMPLEMENTATION_ORDER.md`
  - `docs/PROGRESS_2026-06-21.md`
- 참고한 기존 샘플 방식:
  - `scripts/GenerateControlRoomAuxScreenSample.py`
  - `scripts/Run-ControlRoomAuxScreenSample.ps1`
  - `artSample/control_room_aux_screen/README.md`
  - `artSample/control_room_aux_screen/ASSET_MANIFEST.json`
- 신규 생성 파일:
  - `scripts/GenerateControlRoomVerticalAuxScreensSample.py`
  - `scripts/Run-ControlRoomVerticalAuxScreensSample.ps1`
- 샘플 산출물:
  - `artSample/control_room_vertical_aux_screens/index.html`
  - `artSample/control_room_vertical_aux_screens/README.md`
  - `artSample/control_room_vertical_aux_screens/ASSET_MANIFEST.json`
  - `artSample/control_room_vertical_aux_screens/APPROVAL_STATUS.json`
  - `artSample/control_room_vertical_aux_screens/blender/control_room_vertical_aux_screens.blend`
  - `artSample/control_room_vertical_aux_screens/exports/control_room_vertical_aux_screens.fbx`
  - `artSample/control_room_vertical_aux_screens/exports/control_room_vertical_aux_screens.glb`
  - `artSample/control_room_vertical_aux_screens/renders/01_context_overview.png`
  - `artSample/control_room_vertical_aux_screens/renders/02_front_alignment.png`
  - `artSample/control_room_vertical_aux_screens/renders/03_vertical_bank_closeup.png`
  - `artSample/control_room_vertical_aux_screens/renders/04_side_mount_depth.png`
  - `artSample/control_room_vertical_aux_screens/renders/05_cr07_relation_clearance.png`
- 샘플 구성:
  - CR-06 대형 메인 스크린을 위치 기준 컨텍스트로 배치했다.
  - CR-07 가로형 보조 스크린은 오른쪽 상단 위치 관계 확인용 컨텍스트 프레임으로만 표시했다.
  - CR-08은 CR-06 왼쪽 보조 베이에 붙는 3개 세로 패널 묶음 제안안으로 제작했다.
  - 패널 내부의 `ZONE`, `CCTV`, `LOCK` 표시는 실제 UI가 아니라 기능 방향을 암시하는 더미 표시다.
  - 원본 기획서에 세로형 스크린의 정확한 개수는 없으므로 3개 패널 구성은 승인 전 확인이 필요한 추론이다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomVerticalAuxScreensSample.ps1`
- 실행 결과:
  - 종료 코드 0으로 정상 완료했다.
  - 실행 스크립트의 필수 산출물 존재 확인을 통과했다.
- 문서 갱신:
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 실행하지 않은 작업:
  - Unity 실행, Refresh, 브리지 명령
  - CR-01, CR-07, 동력실, 조종실 오브젝트 수정
  - Unity 씬, 프리팹, 머티리얼 반영
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild
## CR-08 세로형 보조 스크린 비율 재조정

- 사용자가 CR-08 세로형 보조 스크린 샘플의 폭은 줄이고 높이는 조금 더 길게 하는 방향으로 다시 제작해 달라고 요청했다.
- 수정 파일:
  - `scripts/GenerateControlRoomVerticalAuxScreensSample.py`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 재생성 산출물:
  - `artSample/control_room_vertical_aux_screens/`
- 수정 내용:
  - CR-08 패널 폭을 `0.42`에서 `0.34`로 줄였다.
  - CR-08 패널 높이를 `1.36`에서 `1.62`로 늘렸다.
  - CR-06/CR-07 위치 관계는 유지했다.
  - README, HTML, 매니페스트 설명은 폭이 좁고 높이가 긴 세로형 3패널 제안안 기준으로 갱신되도록 했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomVerticalAuxScreensSample.ps1`
- 실행 결과:
  - 종료 코드 0으로 정상 완료했다.
  - 실행 스크립트의 필수 산출물 존재 확인을 통과했다.
- 실행하지 않은 작업:
  - Unity 실행, Refresh, 브리지 명령
  - CR-01, CR-07, 동력실, 조종실 오브젝트 수정
  - Unity 씬, 프리팹, 머티리얼 반영
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild
## CR-08 세로형 보조 스크린 비율 대폭 재조정

- 사용자가 이전 조정은 변화가 보이지 않는다고 지적했고, 폭을 대폭 줄이고 높이를 변화가 보이게 높이라고 요청했다.
- 수정 파일:
  - `scripts/GenerateControlRoomVerticalAuxScreensSample.py`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 재생성 산출물:
  - `artSample/control_room_vertical_aux_screens/`
- 수정 내용:
  - CR-08 패널 폭을 `0.34`에서 `0.20`으로 대폭 줄였다.
  - CR-08 패널 높이를 `1.62`에서 `2.20`으로 확실히 늘렸다.
  - 좁아진 패널 묶음에 맞춰 하단 케이블 글랜드 위치를 고정 좌표가 아니라 CR-08 묶음 중심 기준 오프셋으로 배치했다.
  - `03_vertical_bank_closeup.png` 렌더의 카메라 스케일을 `2.20`에서 `3.05`로 늘려 길어진 전체 높이가 잘리지 않게 했다.
  - README, HTML, 매니페스트 설명은 폭을 대폭 줄이고 높이를 확실히 늘린 세로형 3패널 제안안 기준으로 갱신되도록 했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomVerticalAuxScreensSample.ps1`
- 실행 결과:
  - 종료 코드 0으로 정상 완료했다.
  - 실행 스크립트의 필수 산출물 존재 확인을 통과했다.
- 실행하지 않은 작업:
  - Unity 실행, Refresh, 브리지 명령
  - CR-01, CR-07, 동력실, 조종실 오브젝트 수정
  - Unity 씬, 프리팹, 머티리얼 반영
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild
## CR-08 Unity 반영 기록

- 사용자가 `artSample/control_room_vertical_aux_screens/` 샘플을 승인하고 Unity에 옮기라고 요청했다.
- 사용자는 기존 오브젝트는 위치뿐 아니라 오브젝트 자체를 아예 건드리지 말라고 추가 조건을 확인했다.
- Unity 루트 이름은 `Approved Control Room 08 Vertical Aux Screens`이다.
- 반영 방식:
  - CR-08은 기존 CR-01 또는 CR-07의 자식으로 붙이지 않고 씬 루트의 별도 오브젝트로 생성했다.
  - 기존 CR-01 메인 스크린 기준 오브젝트의 렌더러 bounds를 읽어 위치만 계산하고, 기존 오브젝트의 트랜스폼, 컴포넌트, 머티리얼, 이름, 계층, 활성 상태는 수정하지 않았다.
  - 기존 CR-08 루트가 이미 있으면 삭제하거나 덮어쓰지 않고 중단하도록 했다.
  - 승인된 샘플 비율인 패널 폭 `0.20`, 패널 높이 `2.20`, 3패널 묶음을 Unity 생성 스크립트에 반영했다.
- 수정/추가 파일:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomVerticalAuxScreensBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `scripts/Run-ApprovedControlRoomVerticalAuxScreens.ps1`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomVerticalAuxScreens.ps1`
- 실행 결과:
  - CR-08 적용 성공.
  - 적용 스크립트에서 `ExistingObjectsUntouched=True`, `ControlRoomUntouched=True`, `CockpitUntouched=True`, `EngineRoomUntouched=True` 확인을 통과했다.
  - 적용 스크립트에서 `VerticalAuxScreensLeftOfMainScreen=True`, `VerticalAuxScreensOverlapsEngineRoom=False`, `VerticalAuxScreensOverlapsCockpit=False`, `VerticalAuxScreensOverlapsCr07=False` 확인을 통과했다.
- 실행하지 않은 작업:
  - CR-01, CR-07, 동력실, 조종실 기존 오브젝트 수정
  - 기존 오브젝트 삭제, 재생성, 재배치, 부모 변경, 리네임
  - 실제 구역 상태 UI, CCTV 피드, 복도 폐쇄 로직, 상호작용 로직 연결
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild
## CR-07 사용자 편집 기준 복구

- 사용자가 CR-08 반영 후 CR-07 가로형 보조 스크린 위치가 바뀌어 보인다고 지적했고, 이전에 스크립트에 반영한 사용자 편집 기준으로 즉시 되돌리라고 요청했다.
- 복구 기준:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomAuxScreenBootstrap.cs`
  - 루트 이름 `Approved Control Room 07 Aux Screen`
  - 스크립트 기준 `MainToAuxCenterX = 3.76`, `AuxCenterAboveMainTop = 0.55`
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomAuxScreen.ps1`
- 실행 결과:
  - CR-07 적용 스크립트가 종료 코드 0으로 정상 완료했다.
  - 로그 기준 CR-07 루트 bounds 중심은 `16.34,3.05,22.22`이다.
  - 로그 기준 CR-07 렌더러 수는 `20`개다.
  - 로그에서 `ControlRoomUntouched=True`, `CockpitUntouched=True`, `EngineRoomUntouched=True`를 확인했다.
  - 로그에서 `AuxScreenAboveMainScreen=True`, `DisplayTextureApplied=True`, `AuxScreenOverlapsEngineRoom=False`, `AuxScreenOverlapsCockpit=False`를 확인했다.
- 실행하지 않은 작업:
  - CR-08 삭제 또는 재배치
  - CR-01, 동력실, 조종실 수정
  - 테스트, 빌드, 스모크, 추가 Validate 실행
## 통제실 현재 편집 상태 최종 복구 시점 저장

- 사용자가 현재 Unity에서 편집한 상태 전체를 스크립트에 반영하고, 사용자가 편집한 후 스크립트에 반영한 상태를 최종 복구 시점으로 저장하라는 규칙을 추가하라고 요청했다.
- 앞선 CR-07 단독 복구 접근은 사용자의 현재 통제실 편집 범위를 잘못 좁힌 것이므로, 이번 기록의 기준은 CR-07 단독이 아니라 현재 열린 CargoRunMvp 씬의 `Approved Control Room ...` 루트 전체다.
- 추가 규칙:
  - `AGENTS.md`에 “사용자가 Unity에서 편집한 후 스크립트 반영을 요청한 경우, 편집값을 스크립트에 반영한 뒤 해당 상태를 최종 복구 시점으로 저장해야 한다.”를 추가했다.
- 추가/수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomCurrentStateCapture.cs`
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomCurrentStateSnapshot.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `scripts/Run-ApprovedControlRoomCurrentStateCapture.ps1`
  - `artSample/control_room_current/editor_current/control_room_current_objects.md`
  - `AGENTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 캡처 대상:
  - `Approved Control Room 01 Shell`
  - `Approved Control Room 07 Aux Screen`
  - `Approved Control Room 08 Vertical Aux Screens`
- 저장한 현재 상태:
  - 각 루트와 하위 오브젝트의 인덱스 경로, 이름 경로, 이름, 활성 상태, 로컬 위치, 로컬 회전, 로컬 스케일, 컴포넌트 서명, 렌더러 활성 상태, 머티리얼 참조 정보를 캡처했다.
  - 같은 이름의 형제 오브젝트가 있어도 복구 기준을 잃지 않도록 이름 경로뿐 아니라 sibling index 기반 `IndexPath`를 함께 저장했다.
  - `ApprovedControlRoomCurrentStateSnapshot.cs`에는 캡처값과 `RestoreCapturedControlRoomCurrentState` 복구 메서드가 생성됐다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomCurrentStateCapture.ps1`
  - `.\scripts\Refresh-UnityProject.ps1`
- 실행 결과:
  - 통제실 현재 상태 캡처가 종료 코드 0으로 정상 완료했다.
  - 캡처 로그에서 `RootCount=3`을 확인했다.
  - 캡처 파일 `artSample/control_room_current/editor_current/control_room_current_objects.md`가 생성됐다.
  - 복구용 스냅샷 소스 `Assets/_Project/Editor/Validation/ApprovedControlRoomCurrentStateSnapshot.cs`가 생성됐다.
  - 생성된 복구용 스냅샷 소스를 반영하기 위한 후속 리프레시가 종료 코드 0으로 정상 완료했다.
- 실행하지 않은 작업:
  - CR-01, CR-07, CR-08 재생성/적용 실행
  - CR-08 삭제 또는 재배치
  - 동력실, 조종실 수정
  - 테스트, 빌드, 스모크, Validate, Ensure 실행

## CR-17 복도 방향 표시 레이블 샘플 제작

- 사용자가 CR-09~CR-15는 UI 관련 작업이라 현재 모델링 범위에서 스킵한다고 확인했다.
- 사용자가 CR-16 조작 위치는 스크린 앞 지점이며 독립 모델링 작업이 아니라고 확인했다.
- 이에 따라 남은 모델링 후보인 CR-17 복도 방향 표시 레이블의 승인용 샘플을 제작했다.
- 샘플 위치:
  - `artSample/control_room_direction_labels/`
- 추가 파일:
  - `scripts/GenerateControlRoomDirectionLabelsSample.py`
  - `scripts/Run-ControlRoomDirectionLabelsSample.ps1`
- 샘플 구성:
  - 조종실, 동력실, 이송창고, 무기실 방향을 표시하는 벽면 레이블 패널.
  - 각 방향별 컬러 트림, 한글/영문 보조 표기, 체결 볼트, 바닥 방향 화살표.
  - 조종실/동력실은 통제실 탑다운 기준 좌측 분리 복도 관계로 표현했다.
  - 이송창고/무기실은 통제실 탑다운 기준 6시 방향 인접 복도 관계로 표현했다.
  - 천장은 제외하고 기준 벽, 내부 가벽, 복도 바닥 스텁만 낮은 밀도로 표시했다.
- 생성 산출물:
  - `artSample/control_room_direction_labels/index.html`
  - `artSample/control_room_direction_labels/README.md`
  - `artSample/control_room_direction_labels/ASSET_MANIFEST.json`
  - `artSample/control_room_direction_labels/APPROVAL_STATUS.json`
  - `artSample/control_room_direction_labels/blender/control_room_direction_labels.blend`
  - `artSample/control_room_direction_labels/exports/control_room_direction_labels.fbx`
  - `artSample/control_room_direction_labels/exports/control_room_direction_labels.glb`
  - `artSample/control_room_direction_labels/renders/01_context_overview.png`
  - `artSample/control_room_direction_labels/renders/02_left_corridor_labels.png`
  - `artSample/control_room_direction_labels/renders/03_south_adjacent_labels.png`
  - `artSample/control_room_direction_labels/renders/04_panel_closeup.png`
  - `artSample/control_room_direction_labels/renders/05_topdown_layout.png`
- 실행 명령:
  - `.\scripts\Run-ControlRoomDirectionLabelsSample.ps1`
- 실행 결과:
  - 종료 코드 0으로 정상 완료했다.
  - 실행 스크립트의 필수 산출물 존재 확인을 통과했다.
  - 실행 중 Blender 탐색 후보에 기존 샘플 스크립트와 동일하게 `Blender 5.1`, `Blender 5.0` 경로를 추가했다.
- 실행하지 않은 작업:
  - Unity 실행, Refresh, 브리지 명령
  - Unity 씬, 프리팹, 런타임 자산 반영
  - CR-01, CR-07, CR-08, 동력실, 조종실 오브젝트 수정
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild

## 통제실 현재 편집 상태 최종 복구 시점 저장 기록 이관

- 사용자가 Unity에서 현재 편집한 통제실 전체 상태를 스크립트에 반영해 복구 가능하게 저장하라고 요청했다.
- 현재 날짜는 2026-06-22였지만, 사용자가 22일자 진행문서를 삭제하고 21일자 진행문서에 기록하라고 요청해 이 문서에 이관했다.
- 사용한 캡처 스크립트:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomCurrentStateCapture.cs`
  - `scripts/Run-ApprovedControlRoomCurrentStateCapture.ps1`
- 갱신 산출물:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomCurrentStateSnapshot.cs`
  - `artSample/control_room_current/editor_current/control_room_current_objects.md`
- 캡처 시각:
  - `2026-06-22 00:17:53 +09:00`
- 캡처 대상:
  - `Approved Control Room 01 Shell`
  - `Approved Control Room 07 Aux Screen`
  - `Approved Control Room 08 Vertical Aux Screens`
  - `Approved Control Room 17 Direction Labels`
- 저장 내용:
  - 각 루트와 하위 오브젝트의 인덱스 경로, 이름 경로, 이름, 활성 상태, 로컬 위치, 로컬 회전, 로컬 스케일, 컴포넌트 서명, 렌더러 활성 상태, 머티리얼 참조 정보를 캡처했다.
  - 같은 이름의 형제 오브젝트가 있어도 복구 기준을 잃지 않도록 이름 경로뿐 아니라 sibling index 기반 `IndexPath`를 함께 저장했다.
  - `ApprovedControlRoomCurrentStateSnapshot.cs`에 현재 캡처값과 `RestoreCapturedControlRoomCurrentState` 복구 메서드가 갱신됐다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomCurrentStateCapture.ps1`
  - `.\scripts\Refresh-UnityProject.ps1`
- 실행 결과:
  - 세 명령 모두 종료 코드 0으로 정상 완료했다.
  - 통제실 현재 상태 캡처 산출물과 복구용 스냅샷 소스가 갱신됐다.
  - 후속 리프레시로 갱신된 복구용 스냅샷 소스를 Unity에 반영했다.
- 실행하지 않은 작업:
  - 통제실 오브젝트 값 직접 수정
  - CR-01, CR-07, CR-08, CR-17 재생성/복구/적용 실행
  - 조종실 오브젝트 수정
  - 동력실 오브젝트 수정
  - 기존 오브젝트 삭제, 이름 변경, 부모 변경, 위치/회전/스케일 변경, 머티리얼 변경
  - Unity 에디터 재시작
  - 테스트, 빌드, Validate, Ensure, Smoke 실행

## 통제실 현재 상태 백업 사본 생성 기록 이관

- 사용자가 현재 통제실 전체 오브젝트를 지금 상태로 복구 가능하게 백업해두라고 요청했다.
- 실제 Unity 오브젝트 값은 건드리지 않고, 직전 캡처 산출물의 사본만 별도 백업 폴더에 저장했다.
- 백업 기준:
  - `2026-06-22 00:17:53 +09:00`
- 백업 위치:
  - `artSample/control_room_current/backups/2026-06-22_001753/`
- 백업 파일:
  - `artSample/control_room_current/backups/2026-06-22_001753/ApprovedControlRoomCurrentStateSnapshot.cs`
  - `artSample/control_room_current/backups/2026-06-22_001753/control_room_current_objects.md`
  - `artSample/control_room_current/backups/2026-06-22_001753/README.md`
- 백업 대상 루트:
  - `Approved Control Room 01 Shell`
  - `Approved Control Room 07 Aux Screen`
  - `Approved Control Room 08 Vertical Aux Screens`
  - `Approved Control Room 17 Direction Labels`
- 실행하지 않은 작업:
  - Unity 에디터 실행/재시작/Refresh
  - Unity 브리지 실행
  - 통제실 오브젝트 값 직접 수정
  - CR-01, CR-07, CR-08, CR-17 재생성/복구/적용 실행
  - 조종실 오브젝트 수정
  - 동력실 오브젝트 수정
  - 테스트, 빌드, Validate, Ensure, Smoke 실행

## 22일자 진행문서 정리

- 사용자가 22일자 진행문서는 지우고 21일자 진행문서만 수정하라고 요청했다.
- `docs/PROGRESS_2026-06-22.md`의 기록을 이 문서로 이관했다.
- `docs/PROGRESS_2026-06-22.md`는 삭제했다.
- 실행하지 않은 작업:
  - Unity 실행/Refresh/브리지 실행
  - Unity 씬/오브젝트 수정
  - 조종실/동력실/통제실 오브젝트 수정
  - 테스트, 빌드, Validate, Ensure, Smoke 실행

## CR-17 Unity 반영 기록

- 사용자가 영어 메인 표기 CR-17 샘플을 승인하고 Unity 구현을 요청했다.
- 사용자는 조종실, 동력실, 통제실의 모든 기존 오브젝트를 하나도 건드리지 말라고 조건을 확인했다.
- Unity 루트 이름은 `Approved Control Room 17 Direction Labels`이다.
- 반영 방식:
  - CR-17은 기존 CR-01, CR-07, CR-08의 자식으로 붙이지 않고 씬 루트의 별도 오브젝트로 생성했다.
  - 기존 통제실 루트의 트랜스폼을 위치 정렬 기준으로 읽기만 하고, 기존 오브젝트의 트랜스폼, 컴포넌트, 머티리얼, 이름, 계층, 활성 상태는 수정하지 않았다.
  - 기존 CR-17 루트가 이미 있으면 삭제하거나 덮어쓰지 않고 중단하도록 했다.
  - 적용 전 기존 오브젝트 스냅샷을 잡고 적용 후 이름, 부모, 활성 상태, 트랜스폼, 컴포넌트, 렌더러, 머티리얼 변경 여부를 비교하도록 했다.
  - 레이블은 `COCKPIT`, `ENGINE ROOM`, `CARGO HOLD`, `ARMORY` 영어 메인 표기와 한국어 보조 표기로 구성했다.
  - 생성된 CR-17 콜라이더는 전부 비활성화했다.
- 수정/추가 파일:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomDirectionLabelsBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `scripts/Run-ApprovedControlRoomDirectionLabels.ps1`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `Assets/_Project/Art/Ship/ControlRoom/M_Cr17_*.mat`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomDirectionLabels.ps1`
- 실행 결과:
  - CR-17 적용 스크립트가 종료 코드 0으로 정상 완료했다.
  - 적용 스크립트에서 `ExistingObjectsUntouched=True`, `ControlRoomUntouched=True`, `CockpitUntouched=True`, `EngineRoomUntouched=True` 확인을 통과했다.
  - 적용 스크립트에서 `EnglishMainLabels=True`, `DirectionLabelsOverlapsEngineRoom=False`, `DirectionLabelsOverlapsCockpit=False` 확인을 통과했다.
- 실행하지 않은 작업:
  - CR-01, CR-07, CR-08 재생성/복구/적용 실행
  - 기존 통제실, 동력실, 조종실 오브젝트 수정
  - 기존 오브젝트 삭제, 재생성, 재배치, 부모 변경, 리네임
  - 실제 다국어 UI 시스템, 상호작용 로직, CCTV/구역 상태 UI 연결
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild

## CR-17 글자 표시 결함 수정

- 사용자가 Unity에 반영된 CR-17에서 글자가 보이지 않는다고 지적했다.
- 원인:
  - `TextMesh`에 일반 URP/Lit 머티리얼을 직접 적용해 폰트 아틀라스 텍스처가 빠진 상태였다.
- 수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomDirectionLabelsBootstrap.cs`
  - `scripts/Run-ApprovedControlRoomDirectionLabels.ps1`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `Assets/_Project/Art/Ship/ControlRoom/M_Cr17_*.mat`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 수정 내용:
  - CR-17 루트만 재생성 대상으로 허용하고, 기존 CR-17 외의 모든 기존 오브젝트는 스냅샷 보호 대상으로 유지했다.
  - `TextMesh` 머티리얼을 Unity 기본 폰트 머티리얼 기반으로 생성해 폰트 아틀라스 텍스처가 포함되도록 바꿨다.
  - `TextMesh` 생성 시 기본 런타임 폰트, 굵은 스타일, 텍스트 컬러, 정렬 순서를 명시했다.
  - 실행 스크립트에서 `VisibleTextMeshes=8` 확인을 추가해 영어 메인 4개와 한국어 보조 4개 텍스트 메쉬가 보이는 머티리얼을 갖는지 검사한다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomDirectionLabels.ps1`
- 실행 결과:
  - CR-17 재적용 스크립트가 종료 코드 0으로 정상 완료했다.
  - 적용 스크립트에서 `VisibleTextMeshes=8`, `ExistingObjectsUntouched=True`, `ControlRoomUntouched=True`, `CockpitUntouched=True`, `EngineRoomUntouched=True` 확인을 통과했다.
- 실행하지 않은 작업:
  - CR-01, CR-07, CR-08 재생성/복구/적용 실행
  - 기존 통제실, 동력실, 조종실 오브젝트 수정
  - 테스트, 빌드, 스모크, Validate, Ensure 실행

## CR-17 라벨 텍스트 패널 맞춤 수정

- 사용자가 CR-17 글자가 간판 안에 들어가지 않는다고 지적했다.
- 수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedControlRoomDirectionLabelsBootstrap.cs`
  - `scripts/Run-ApprovedControlRoomDirectionLabels.ps1`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 수정 내용:
  - `COCKPIT`, `ENGINE ROOM`, `CARGO HOLD`, `ARMORY` 영어 메인 표기와 한국어 보조 표기를 라벨 패널 폭 기준으로 검사하도록 했다.
  - 생성된 `TextMesh` 렌더 bounds를 측정하고, 패널 안쪽 허용 폭/높이를 넘으면 적용이 실패하도록 했다.
  - 실행 스크립트에 `TextFitChecked=8`, `TextFitsLabelPanels=True` 검사를 추가했다.
  - CR-17 루트만 재생성했고, 기존 CR-17 외의 통제실, 조종실, 동력실 오브젝트는 스냅샷 보호 대상으로 유지했다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Run-ApprovedControlRoomDirectionLabels.ps1`
- 실행 결과:
  - 첫 실행에서는 보조 글자 높이 기준과 긴 영어 표기 폭 기준이 과하게 좁아 실패했고, 실제 라벨 패널 내부 기준으로 검사값을 조정했다.
  - 최종 실행은 종료 코드 0으로 정상 완료했다.
  - 적용 스크립트에서 `VisibleTextMeshes=8`, `TextFitChecked=8`, `TextFitsLabelPanels=True`, `ExistingObjectsUntouched=True`, `ControlRoomUntouched=True`, `CockpitUntouched=True`, `EngineRoomUntouched=True` 확인을 통과했다.
- 실행하지 않은 작업:
  - CR-01, CR-07, CR-08 재생성/복구/적용 실행
  - 기존 통제실, 동력실, 조종실 오브젝트 수정
  - Unity 에디터 재시작
  - 테스트, 빌드, 스모크, Validate, Ensure 실행

## CR-17 레이블 영어 메인 표기 재생성

- 사용자가 다국어 지원을 고려해 한글만 있으면 안 되며, 영어를 메인으로 하라고 요청했다.
- 수정 파일:
  - `scripts/GenerateControlRoomDirectionLabelsSample.py`
  - `docs/CONTROL_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-21.md`
- 수정 내용:
  - 레이블 메인 표기를 `COCKPIT`, `ENGINE ROOM`, `CARGO HOLD`, `ARMORY`로 변경했다.
  - 한국어 `조종실`, `동력실`, `이송창고`, `무기실`은 작은 보조 표기로 유지했다.
  - 샘플 내 설명성 표기도 영어가 먼저 보이도록 조정했다.
  - HTML, README, 매니페스트 설명을 영어 메인/한국어 보조 표기 기준으로 갱신했다.
- 실행 명령:
  - `.\scripts\Run-ControlRoomDirectionLabelsSample.ps1`
- 실행 결과:
  - 종료 코드 0으로 정상 완료했다.
  - `artSample/control_room_direction_labels/` 산출물을 영어 메인/한국어 보조 표기 기준으로 재생성했다.
  - 실행 스크립트의 필수 산출물 존재 확인을 통과했다.
- 실행하지 않은 작업:
  - Unity 실행, Refresh, 브리지 명령
  - Unity 씬, 프리팹, 런타임 자산 반영
  - CR-01, CR-07, CR-08, 동력실, 조종실 오브젝트 수정
  - HarnessValidation, EditModeTests, PlayModeTests, WindowsDevBuild
