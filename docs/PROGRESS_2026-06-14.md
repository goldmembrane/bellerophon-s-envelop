# 진행 기록 2026-06-14

## 현재 작업 주제

- 화물선 복도 2단계 에셋 적용은 실제 유니티 씬 적용 전에 `artSample/` 검토 시안을 먼저 만든다.
- 사용자가 기존 낡은 버전 시안이 충분히 낡아 보이지 않는다고 판단했다.
- 이번 방향은 낡음 표현이 아니라, 현재 가져온 에셋 중 철판처럼 보이는 부품으로 복도 내부를 덮은 시안을 먼저 확인하는 것이다.

## 오늘 생성한 검토 시안

- 위치: `artSample/asset_dressing_samples/step02_corridors_thresholds_steel_plate_2026-06-14/`
- 주요 이미지:
  - `view_01_player_entry.png`
  - `view_02_threshold_diagonal.png`
  - `view_03_layout_topdown.png`
  - `view_04_module_stack.png`
- 설명 문서:
  - `README.md`
  - `ASSET_MANIFEST.md`
  - `APPROVAL_STATUS.json`
  - `index.html`

## 사용한 주요 에셋 방향

- 직선 복도 뼈대는 `Sci-Fi Styled Modular Pack`의 `Corridor_I.prefab`를 유지한다.
- 내부 철판 덮개는 `Heavy Station Kit`의 `Floor_5_base_Plate.prefab`를 바닥, 좌우 벽, 천장 안쪽에 반복 배치하는 방식으로 구성했다.
- 철판 이음매와 체결부는 검토용 표면 보조 요소로 추가했다.
- 입구 중앙을 막는 벽은 두지 않고, 좌우 기둥과 상단 보강재 중심으로만 유지했다.

## 작업 중 확인한 문제와 처리

- `Floor_5_base_Plate.prefab`의 원본 재질을 그대로 쓰면 일부 표면이 분홍색 오류 재질로 렌더링됐다.
- 그래서 원본 재질 보존 방식은 폐기하고, 프로젝트 검토용 금속 재질을 강제 적용했다.
- 분홍색 오류 재질이 남지 않도록 렌더를 다시 생성해 확인했다.

## 검증

- `.\scripts\Refresh-UnityProject.ps1` 실행 완료.
- `.\scripts\Run-AssetDressingStep02SteelPlateSample.ps1` 실행 완료.
- 생성 로그: `Logs/AssetDressingStep02SteelPlateSample.log`
- 새 시안은 아직 미승인 상태이며 실제 게임 씬에는 적용하지 않았다.

## 다음 단계

- 사용자가 `artSample/asset_dressing_samples/step02_corridors_thresholds_steel_plate_2026-06-14/index.html` 또는 개별 이미지를 검토한다.
- 승인되면 같은 배치 방향을 실제 화물선 복도에 적용한다.
- 미승인되면 철판 패널 밀도, 색감, 이음매, 조명, 벽면 덮개 범위를 먼저 조정한다.

## 추가 시안: 요철 철판 낡은 버전

- 사용자가 에셋 안에 있는 오돌토돌한 철판 소재를 복도에 쓰고, 더 낡게 변형한 여러 구도 시안을 요청했다.
- 새 시안 위치: `artSample/asset_dressing_samples/step02_corridors_thresholds_bumpy_worn_plate_2026-06-14/`
- 사용 질감:
  - `Assets/Heavy Station Kit/_common/Textures/plate_D.png`
  - `Assets/Heavy Station Kit/_common/Textures/plate_N.png`
  - `Assets/Heavy Station Kit/_common/Textures/plate_S.png`
- `plate.prefab`는 단면 평면이라 정면 구도에서 시야를 막는 문제가 있어, 최종 시안은 같은 요철 철판 텍스처를 두께 있는 패널에 적용했다.
- 더 강한 낡음 표현으로 녹, 기름때, 그을음, 긁힘, 벗겨진 모서리 색을 추가했다.
- `.\scripts\Run-AssetDressingStep02BumpyWornPlateSample.ps1` 실행 완료.
- 새 시안은 아직 미승인 상태이며 실제 게임 씬에는 적용하지 않았다.

## 사용자 판단과 현재 결론

- 사용자가 `Assets/Heavy Station Kit/_common/Prefabs/plate.prefab`를 직접 확인했고, 해당 프리팹을 오돌토돌한 요철 철판으로 보기 어렵다고 판단했다.
- 이전에 `plate_N.png` 노멀 텍스처만 보고 요철 철판 후보라고 본 판단은 부정확했다.
- 따라서 `step02_corridors_thresholds_bumpy_worn_plate_2026-06-14` 시안은 승인 기준으로 쓰지 않는다.
- 2단계 복도 적용은 현재 보류한다.
- 사용자는 에셋을 직접 찾아볼 예정이며, 특정 프리팹 경로를 지정하면 이후에는 그 경로를 기준으로만 시안을 만든다.

## 유니티 에디터 상태

- 사용자가 에디터를 기존 상태로 돌려달라고 요청했다.
- 규칙에 따라 직접 `Unity.exe`를 실행하지 않고 아래 순서로 처리했다.
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "OpenCargoRunMvpScene" -LogPath "Logs\OpenCargoRunMvpScene.log" -TimeoutSeconds 120`
- 로그에서 `CargoRunMvp scene opened.`를 확인했다.
- 새 철판 시안 또는 요철 철판 시안은 실제 게임 씬에 적용하지 않았다.

## 다음 작업 기준

- 2단계 복도 꾸미기를 재개하기 전에, 다운로드 및 임포트된 4개 에셋을 먼저 정확히 파악한다.
- 대상 에셋:
  - `Sci-Fi Styled Modular Pack`
  - `Free Sci-Fi Office Pack`
  - `GoldenFrame Terminal - FREE`
  - `Sci-Fi Heavy Station Kit base`
- 다음 작업은 복도 적용이 아니라 에셋 조사 단계로 시작한다.
- 조사 항목:
  - 폴더 구조와 주요 프리팹 위치
  - 복도, 벽, 바닥, 천장, 문, 조명, 장식, 터미널 후보 분류
  - 실제 형태를 확인할 수 있는 프리팹 미리보기 시트 생성
  - 각 후보가 화물선 내부 어디에 적합한지 한글 설명 작성
- 이 조사 결과를 `artSample/`에 승인 검토용으로 저장한 뒤, 사용자가 확인한 다음에만 2단계 복도 시안을 다시 만든다.
- 승인 전에는 실제 `CargoRunMvp` 씬, 프리팹, 런타임 자산에 새 에셋을 적용하지 않는다.

## 추가 시안: 사용자 지정 Floor_5_base_Plate / Wall 2 복도

- 사용자가 복도 바닥, 벽, 천장 후보를 직접 지정해 새 다각도 시안을 요청했다.
- 새 시안 위치: `artSample/asset_dressing_samples/step02_corridor_floor5_wall2_dense_floorbase_2026-06-14/`
- 사용 에셋:
  - 바닥 하부: `Assets/Heavy Station Kit/BASE/Meshes/Floors/Floor_5_base_Plate.fbx`
  - 바닥 상부 반복 레이어: `Assets/Heavy Station Kit/BASE/Prefabs/Floors/Floor Base 1 F.prefab`
  - 벽 전체 패널: `Assets/ScifiOfficeLite/Meshes/Walls/Wall 2.FBX`
  - 벽 절반 패널: `Assets/ScifiOfficeLite/Meshes/Walls/Wall 2 Half.FBX`
  - 천장 후보: `Assets/Heavy Station Kit/BASE/Prefabs/Top-Bottom/TB_2.prefab`
  - 천장 보조 레일: `Assets/ScifiOfficeLite/Prefabs/Wall/Wall Top Piece.prefab`
- 생성 이미지:
  - `view_01_player_entry.png`
  - `view_02_floor_wall_diagonal.png`
  - `view_03_ceiling_and_wall_underlook.png`
  - `view_04_layout_topdown.png`
  - `view_05_floor_stack_detail.png`
- `view_04_layout_topdown.png`는 바닥/벽 배치 확인을 위해 천장 렌더러를 잠시 숨긴 컷어웨이 구도다.
- `view_05_floor_stack_detail.png`는 `Floor_5_base_Plate.fbx` 위에 `Floor Base 1 F.prefab`를 2열로 촘촘히 올린 구조를 보여준다.
- 원본 Asset Store 머티리얼의 URP 호환 문제를 피하기 위해 렌더용 임시 URP 머티리얼을 사용했다. 원본 에셋 파일은 수정하지 않았다.
- `.\scripts\Run-AssetDressingStep02SelectedCorridorSample.ps1` 실행 완료.
- 새 시안은 아직 미승인 상태이며 실제 게임 씬에는 적용하지 않았다.

## 추가 수정: Floor_5_base_Plate / Wall 2 복도 벽 메움 보강판

- 사용자가 1차 사용자 지정 복도 시안을 검토한 뒤 벽에 구멍이 너무 많고, 벽 에셋을 더 써서 구멍이 보이지 않도록 촘촘히 메워야 한다고 피드백했다.
- 기존 1차 시안은 보존하고, 벽 메움 보강판을 새 검토 폴더로 생성했다.
- 새 시안 위치: `artSample/asset_dressing_samples/step02_corridor_floor5_wall2_dense_floorbase_wallfill_2026-06-14/`
- 추가한 벽 보강 에셋:
  - `Assets/ScifiOfficeLite/Prefabs/Wall/Wall 2 Variant.prefab`
  - `Assets/ScifiOfficeLite/Prefabs/Wall/Wall 2 Half Variant.prefab`
  - `Assets/ScifiOfficeLite/Prefabs/Wall/Wall Pillar.prefab`
  - `Assets/ScifiOfficeLite/Prefabs/Wall/Wall Pillar 3.prefab`
- 측면 벽 뒤쪽에는 관통 구멍 사이로 외부 배경이 보이지 않도록 숨겨진 연속 뒤판을 추가했다.
- 복도 끝의 큰 검은 빈 공간도 `Wall 2 Variant.prefab` 폐쇄 패널과 뒤판으로 막아 구멍처럼 읽히지 않게 조정했다.
- 보강 재질은 구멍 방지를 위한 숨은 뒤판만 단색 금속으로 두고, 눈에 보이는 추가 벽 에셋은 기본 벽 재질을 사용하도록 조정했다.
- 생성 이미지:
  - `view_01_player_entry.png`
  - `view_02_floor_wall_diagonal.png`
  - `view_03_ceiling_and_wall_underlook.png`
  - `view_04_layout_topdown.png`
  - `view_05_floor_stack_detail.png`
- `.\scripts\Refresh-UnityProject.ps1` 실행 완료.
- `.\scripts\Run-AssetDressingStep02SelectedCorridorSample.ps1` 실행 완료.
- 이 보강판도 아직 미승인 상태이며 실제 `CargoRunMvp` 씬, 프리팹, 런타임 자산에는 적용하지 않았다.

## 추가 수정: Floor_5_base_Plate / Wall 2 복도 벽 무겹침 보강판

- 사용자가 벽 메움 보강판을 검토한 뒤 벽이 서로 겹쳐 보이며, 벽은 겹치는 곳 없이 깔끔하게 메워져야 하고 필요하면 각 벽 사이에 틈새 에셋을 끼워 넣으라고 피드백했다.
- 이전 `wallfill` 시안은 보존하고, 무겹침 벽 배치를 새 검토 폴더로 생성했다.
- 새 시안 위치: `artSample/asset_dressing_samples/step02_corridor_floor5_wall2_dense_floorbase_cleanwall_2026-06-14/`
- 변경 방식:
  - 같은 모듈 구간에 여러 벽 패널을 겹쳐 넣던 방식을 제거했다.
  - 좌우 벽은 모듈당 `Wall 2.FBX` 또는 상하 분할 `Wall 2 Half.FBX` 한 세트만 사용하도록 정리했다.
  - 벽 모듈 사이의 빈 간격은 `Wall Pillar.prefab`, `Wall Pillar 3.prefab` 기둥형 틈새 에셋으로 메웠다.
  - 벽 구멍 사이로 외부 배경이 보이는 문제는 벽 뒤쪽에 얇은 숨은 뒤판을 두어 처리했다. 이 뒤판은 표면 벽 패널과 같은 면에 겹치지 않는다.
  - 출구 쪽 폐쇄 패널도 서로 겹치지 않도록 간격을 둔 `Wall 2 Variant.prefab` 세그먼트와 기둥형 틈새 에셋으로 재구성했다.
- 생성 이미지:
  - `view_01_player_entry.png`
  - `view_02_floor_wall_diagonal.png`
  - `view_03_ceiling_and_wall_underlook.png`
  - `view_04_layout_topdown.png`
  - `view_05_floor_stack_detail.png`
- `.\scripts\Refresh-UnityProject.ps1` 실행 완료.
- `.\scripts\Run-AssetDressingStep02SelectedCorridorSample.ps1` 실행 완료.
- 이 무겹침 보강판도 아직 미승인 상태이며 실제 `CargoRunMvp` 씬, 프리팹, 런타임 자산에는 적용하지 않았다.

## 추가 수정: Floor_5_base_Plate / Wall 2 복도 벽/격벽 패턴 통일판

- 사용자가 무겹침 보강판을 검토한 뒤 벽이 어느 정도 깔끔해졌지만 패턴이 뒤죽박죽이라 어지러워 보이며, 벽과 격벽의 패턴을 통일해서 다시 그리라고 피드백했다.
- 이전 `cleanwall` 시안은 보존하고, 벽/격벽 패턴 통일판을 새 검토 폴더로 생성했다.
- 새 시안 위치: `artSample/asset_dressing_samples/step02_corridor_floor5_wall2_dense_floorbase_unifiedwall_2026-06-14/`
- 변경 방식:
  - 측면 벽에서 `Wall 2 Half.FBX` 혼합을 제거하고 `Wall 2.FBX` 반복으로 통일했다.
  - 출구 쪽 격벽도 `Wall 2 Variant.prefab` 대신 `Wall 2.FBX`를 회전 배치해 측면 벽과 같은 패턴으로 맞췄다.
  - 벽 모듈 사이와 격벽 사이 이음매는 `Wall Pillar.prefab` 한 종류를 반복해 리듬을 통일했다.
  - 벽 구멍 관통 방지를 위한 얇은 숨은 뒤판은 유지하되, 표면 패턴을 방해하지 않도록 벽 뒤쪽에만 배치했다.
- 생성 이미지:
  - `view_01_player_entry.png`
  - `view_02_floor_wall_diagonal.png`
  - `view_03_ceiling_and_wall_underlook.png`
  - `view_04_layout_topdown.png`
  - `view_05_floor_stack_detail.png`
- `.\scripts\Refresh-UnityProject.ps1` 실행 완료.
- `.\scripts\Run-AssetDressingStep02SelectedCorridorSample.ps1` 실행 완료.
- 이 패턴 통일판도 아직 미승인 상태이며 실제 `CargoRunMvp` 씬, 프리팹, 런타임 자산에는 적용하지 않았다.

## 추가 수정: Floor_5_base_Plate / Wall 2 복도 촘촘한 Floor Base 1 F 바닥판

- 사용자가 벽/격벽 패턴 통일판을 검토한 뒤 그 구성을 유지하면서 바닥의 `Floor Base 1 F.prefab` 사이즈를 더 줄여 더 촘촘히 배치하라고 피드백했다.
- 이전 `unifiedwall` 시안은 보존하고, 바닥 상부 패널을 더 작게 줄인 새 검토 폴더를 생성했다.
- 새 시안 위치: `artSample/asset_dressing_samples/step02_corridor_floor5_wall2_dense_floorbase_unifiedwall_tighterfloor_2026-06-14/`
- 변경 방식:
  - 벽과 격벽은 이전 통일판처럼 `Wall 2.FBX` 패턴을 유지했다.
  - `Floor Base 1 F.prefab` 상부 바닥 레이어를 기존 2열 배치에서 3열 배치로 늘렸다.
  - 각 `Floor Base 1 F.prefab` 목표 크기를 줄이고 행 간격도 좁혀 총 25행으로 더 촘촘하게 반복했다.
  - 실제 원본 프리팹 파일은 수정하지 않고, 검토 렌더용 인스턴스 크기만 조정했다.
- 생성 이미지:
  - `view_01_player_entry.png`
  - `view_02_floor_wall_diagonal.png`
  - `view_03_ceiling_and_wall_underlook.png`
  - `view_04_layout_topdown.png`
  - `view_05_floor_stack_detail.png`
- `.\scripts\Refresh-UnityProject.ps1` 실행 완료.
- `.\scripts\Run-AssetDressingStep02SelectedCorridorSample.ps1` 실행 완료.
- 이 촘촘한 바닥판도 아직 미승인 상태이며 실제 `CargoRunMvp` 씬, 프리팹, 런타임 자산에는 적용하지 않았다.

## 추가 수정: Floor_5_base_Plate / Wall 2 복도 전폭 촘촘한 Floor Base 1 F 바닥판

- 사용자가 이전 촘촘한 바닥판을 검토한 뒤 `Floor Base 1 F.prefab`를 좀 더 작게 해서 더 촘촘히 넣고, 양 옆에 빈 공간이 없게 다시 그리라고 피드백했다.
- 이전 `unifiedwall_tighterfloor` 시안은 보존하고, 바닥 상부 패널을 전폭으로 확장한 새 검토 폴더를 생성했다.
- 새 시안 위치: `artSample/asset_dressing_samples/step02_corridor_floor5_wall2_dense_floorbase_unifiedwall_fullwidthfloor_2026-06-14/`
- 변경 방식:
  - 벽과 격벽은 이전 통일판처럼 `Wall 2.FBX` 반복 패턴을 유지했다.
  - `Floor Base 1 F.prefab` 상부 바닥 레이어를 6열 40행으로 늘려 좌우 벽 근처까지 채우도록 전폭 배치했다.
  - 각 렌더용 `Floor Base 1 F.prefab` 인스턴스 목표 크기를 더 줄여 행/열 간격을 좁혔다.
  - 실제 원본 프리팹 파일은 수정하지 않고, 검토 렌더용 인스턴스 크기와 배치만 조정했다.
- 생성 이미지:
  - `view_01_player_entry.png`
  - `view_02_floor_wall_diagonal.png`
  - `view_03_ceiling_and_wall_underlook.png`
  - `view_04_layout_topdown.png`
  - `view_05_floor_stack_detail.png`
- `.\scripts\Refresh-UnityProject.ps1` 실행 완료.
- `.\scripts\Run-AssetDressingStep02SelectedCorridorSample.ps1` 실행 완료.
- 이 전폭 촘촘한 바닥판도 아직 미승인 상태이며 실제 `CargoRunMvp` 씬, 프리팹, 런타임 자산에는 적용하지 않았다.

## 승인 기록: 전폭 촘촘한 Floor Base 1 F 복도 샘플

- 사용자가 `step02_corridor_floor5_wall2_dense_floorbase_unifiedwall_fullwidthfloor_2026-06-14` 샘플을 검토한 뒤 "좋아 저정도면 될 것 같아."라고 승인했다.
- `APPROVAL_STATUS.json`의 `approvalState`를 `승인`으로 갱신하고, 승인된 조합을 기준으로 `CargoRunMvp` 복도 적용안을 만들 수 있도록 표시했다.
- 승인된 기준:
  - `Floor_5_base_Plate.fbx` 위에 더 작게 줄인 `Floor Base 1 F.prefab` 렌더용 인스턴스를 6열 40행 전폭 배치한다.
  - 벽과 격벽은 `Wall 2.FBX` 반복 패턴으로 통일한다.
  - 좌우 벽 근처의 바닥 빈 공간을 최소화한다.
- 승인 기록만 남겼으며, 아직 실제 `CargoRunMvp` 씬, 프리팹, 런타임 자산에는 적용하지 않았다.

## 실제 적용: 승인 복도 샘플 CargoRunMvp 반영

- 사용자가 승인된 전폭 촘촘한 `Floor Base 1 F` 복도 샘플을 다음 단계까지 진행하라고 요청했다.
- `AssetStoreShipDressingBootstrap`의 Step 2 복도 드레싱 생성을 승인 샘플 기준으로 교체했다.
- 실제 적용 방식:
  - 모든 `Asset Store Ship Dressing/01 Corridors And Thresholds` 복도 루트가 재생성될 때 `Floor_5_base_Plate.fbx` 하부 바닥을 사용한다.
  - `Floor Base 1 F.prefab`는 각 복도 세그먼트 방향과 경사에 맞춰 6열 고밀도 전폭 상부 바닥으로 반복 배치한다.
  - 측면 벽은 `Wall 2.FBX` 반복 패턴으로 통일하고, 이음매는 `Wall Pillar.prefab` 반복으로 맞춘다.
  - 천장은 `TB_2.prefab` 반복과 `Wall Top Piece.prefab` 보조 레일을 사용한다.
  - 승인 샘플의 끝 격벽은 검토용 폐쇄 구도였으므로 실제 게임 복도에서는 방 사이 통행을 막지 않는 열린 출입구 프레임으로 유지했다.
  - 원본 Asset Store 파일은 수정하지 않고, `Assets/_Project/Art/Ship/Materials/ApprovedCorridor_*.mat` URP 호환 머티리얼 10개를 새로 만들어 적용했다.
- `CargoRunMvp.unity`에 실제 복도 드레싱이 저장됐다.
- Step 2 검증 결과:
  - `CorridorRoots=10`
  - `Renderers=7018`
  - `EnabledColliders=0`
  - `ErrorMaterialRenderers=0`
  - `Wall2Panels=540`
  - `DenseFloorOverlays=8304`
  - `WallPillarSeams=596`
  - `CargoCeilingPanels=270`
  - `ThresholdCenterBlockers=0`
  - `EnabledLegacyCorridorRenderers=0`
- 승인 샘플과 실제 Unity 적용 결과 비교 캡처를 생성했다.
- 비교 위치: `artSample/asset_dressing_samples/step02_corridor_floor5_wall2_dense_floorbase_unifiedwall_fullwidthfloor_2026-06-14/unity_applied_comparison/index.html`
- 대표 비교 확인:
  - 플레이어 진입 컷에서 전폭 촘촘한 바닥, 통일 벽 패턴, 열린 통로가 확인된다.
  - 상단 컷에서 복도 전체에 고밀도 바닥 레이어가 반복 적용된 것이 확인된다.
  - 바닥 상세 컷에서 `Floor Base 1 F.prefab`가 작게 줄어 촘촘히 깔린 것이 확인된다.
- 검증:
  - `.\scripts\Refresh-UnityProject.ps1` 통과.
  - `.\scripts\Run-AssetStoreShipDressingStep2.ps1` 통과.
  - `.\scripts\Run-AssetStoreShipDressingStep2Comparison.ps1` 통과.
  - `.\scripts\Run-HarnessValidation.ps1` 통과.
  - `.\scripts\Run-EditModeTests.ps1` 통과, `243/243`.
  - `.\scripts\Run-PlayModeTests.ps1` 통과, `9/9`.
  - `.\scripts\Build-WindowsDev.ps1` 통과.
  - `git diff --check` 통과, CRLF 경고만 남음.
  - `.\scripts\Run-AssetStoreShipDressingStep2.ps1` 통과.
  - `.\scripts\Run-AssetStoreShipDressingStep2Comparison.ps1` 통과.
  - `.\scripts\Run-HarnessValidation.ps1` 통과.
  - `.\scripts\Run-EditModeTests.ps1` 통과, `243/243`.
  - `.\scripts\Run-PlayModeTests.ps1` 통과, `9/9`.
  - `.\scripts\Build-WindowsDev.ps1` 통과.
  - `git diff --check`는 줄끝 공백 오류 없이 CRLF 경고만 남았다.

## 후속 수정: 운송창고 연결 경사 복도 샘플 동기화

- 사용자가 높낮이가 있는 복도, 특히 운송창고와 이어지는 경사 복도에서 `Floor Base 1 F.prefab` 바닥이 촘촘하게 깔리지 않고 벽도 승인 샘플처럼 보이지 않는다고 확인했다.
- 원인은 평지 복도에는 승인 샘플의 6열 전폭 배치가 충분했지만, 운송창고 연결 경사 구간에서는 카메라 각도와 원본 메시의 빈 공간 때문에 바닥이 긴 검은 띠처럼 듬성듬성 보이는 것이었다.
- `AssetStoreShipDressingBootstrap`에서 운송창고 연결 복도만 별도 처리해 `Floor Base 1 F.prefab` 상부 레이어를 8열, 더 짧은 행 간격, 더 넓은 표시 폭으로 배치하도록 수정했다.
- 기존 Stage 3 운송창고 시작 복도 장식 렌더러가 남아 Wall 2 패턴을 방해하지 않도록 Step 2 적용 시 해당 렌더러도 숨기고 검증하게 했다.
- 운송창고 연결 경사 복도에는 낮은 강도의 따뜻한 보조 조명 `Approved Cargo Slope Wall Fill Light`를 추가해 `Wall 2.FBX` 내측 패턴이 승인 샘플처럼 읽히도록 했다.
- 비교 캡처에 `unity_view_06_cargo_hold_engine_slope.png`, `unity_view_07_cargo_hold_armory_slope.png`를 추가해 운송창고-엔진실, 운송창고-무기고 경사 복도를 직접 확인할 수 있게 했다.
- 최신 Step 2 검증 로그 기준:
  - `CorridorRoots=10`
  - `DenseFloorOverlays=10544`
  - `CargoHoldDenseFloorOverlays=4928`
  - `CargoHoldWall2Panels=176`
  - `CargoSlopeWallFillLights=12`
  - `EnabledLegacyCorridorRenderers=0`
  - `EnabledStage3CargoStartRenderers=0`
- 재생성된 비교 위치: `artSample/asset_dressing_samples/step02_corridor_floor5_wall2_dense_floorbase_unifiedwall_fullwidthfloor_2026-06-14/unity_applied_comparison/index.html`

## 후속 수정: 복도 천장 차폐, 비조종실 벽 패턴 통일, 통제실-무기고 바닥/충돌 보정

- 사용자 플레이 확인 후 세 가지 문제를 수정했다.
  - 모든 복도 천장 위가 뚫려 보이는 문제: 기존 `TB_2.prefab` 반복 천장 위에 불투명 천장 캡을 유지하고, 천장 양옆에 `Project Opaque Ceiling Side Skirt` / `Project Opaque Joint Ceiling Side Skirt`를 추가해 위쪽과 가장자리의 빈 공간을 더 닫았다.
  - 운송창고-조종실을 제외한 복도 벽이 승인 샘플과 다르게 보이는 문제: 잘못된 텍스처 스킨 방식은 제거하고, 비조종실 기준 복도에 `Project Approved Horizontal Wall Band` 시각 전용 라이너를 추가해 승인 샘플의 가로 밴드 벽 리듬으로 맞췄다. Collider는 생성 즉시 비활성화한다.
  - 무기고-통제실 복도 바닥/충돌 문제: 전 구간 `Floor Base 1 F.prefab` 고밀도 배치를 8열 전폭 배치로 확장하고, 조인트 바닥도 8x8로 촘촘하게 덮었다. 시각적으로 제거된 돌출부에 걸리는 문제는 legacy doorway/joint clearance collider 비활성화 검증으로 막았다.
- 비교 캡처를 갱신했다.
  - `artSample/asset_dressing_samples/step02_corridor_floor5_wall2_dense_floorbase_unifiedwall_fullwidthfloor_2026-06-14/unity_applied_comparison/index.html`
  - 추가 확인 이미지: `unity_view_08_control_armory_dense_floor_wall.png`
- 최신 Step 2 검증 로그 기준:
  - `CorridorRoots=10`
  - `Renderers=12450`
  - `EnabledColliders=0`
  - `ErrorMaterialRenderers=0`
  - `Wall2Panels=540`
  - `HorizontalWallBandLiners=1310`
  - `DenseFloorOverlays=15152`
  - `CargoHoldDenseFloorOverlays=4928`
  - `ControlArmoryDenseFloorOverlays=4352`
  - `OpaqueCeilingCaps=308`
  - `OpaqueCeilingSideSkirts=278`
  - `CorridorWallFillLights=40`
  - `EnabledLegacyClearanceColliders=0`
- 검증:
  - `.\scripts\Refresh-UnityProject.ps1` 통과.
  - `.\scripts\Run-AssetStoreShipDressingStep2.ps1` 통과.
  - `.\scripts\Run-AssetStoreShipDressingStep2.ps1` 통과.
  - `.\scripts\Run-AssetStoreShipDressingStep2Comparison.ps1` 통과.
  - `.\scripts\Run-HarnessValidation.ps1` 통과.
  - `.\scripts\Run-EditModeTests.ps1` 통과, `243/243`.
  - `.\scripts\Run-PlayModeTests.ps1` 통과, `9/9`.
  - `.\scripts\Build-WindowsDev.ps1` 통과.
  - `git diff --check` 통과, CRLF 경고만 남음.

## 되돌림 기록: 후속 복도 보정 제거

- 사용자 요청에 따라 `천장 차폐 / 비조종실 벽 패턴 통일 / 통제실-무기고 바닥 및 충돌 보정` 이후에 진행한 복도 후속 보정 작업을 제거했다.
- 제거 대상: 추가 벽 레이어 제거, 통제실-무기고 낙하 방지벽, 복도-구역 입구 연결부 메움, 방별 입구 포켓 보강, 곡선형 입구 실링, void closure, 관련 추가 비교 캡처와 검증 조건.
- 유지 대상: 승인된 복도 에셋 배치, 운송창고 연결 경사 복도 동기화, 천장 차폐, Wall 2 수평 밴드 패턴, 통제실-무기고 고밀도 바닥 보정, legacy clearance collider 비활성화 검증.

## 되돌림 기록: 내부 구조 임의 변경 제거

- 사용자 추가 요청에 따라 `Phase4CargoShipGrayboxBootstrap.cs`와 `PostDetailedStage3GameplayPropsBootstrap.cs`의 임의 구조/방 내부 소품 위치 변경을 기준 버전으로 되돌렸다.
- `Phase4 -> PostDetailedStage3 props -> AssetStoreShipDressing Step2` 순서로 씬을 다시 생성해 되돌린 내부 구조 위에 승인 복도 에셋 배치만 다시 적용했다.
- 최종 확인: 두 내부 구조 생성 파일은 더 이상 git diff에 남지 않으며, 씬/코드에서 후속 입구 메움, 낙하 방지벽, 곡선/void 보정 이름도 검색되지 않는다.

## 되돌림 기록: Stage 3 배경/방 내부 잔존 제거

- 사용자 확인 스크린샷에서 오늘 작업 시작 시점과 다른 빨간 줄무늬 화물창 배경과 Stage 3 장비/아트샘플 오브젝트가 보이는 문제가 확인되었다.
- 원인은 내부 구조 되돌림 이후 `Post Detailed Stage 3 Gameplay Props` 생성 루트가 씬에 다시 남아 있었고, Step 2가 해당 루트 전체를 제거하지 않고 일부 cargo-start 렌더러만 숨기고 있었기 때문이다.
- `CargoRunMvp.unity`와 기존 실내/그레이박스/Phase20 머티리얼 파일을 추적본 기준으로 되돌린 뒤, 승인된 복도 Step 2만 다시 적용했다.
- `AssetStoreShipDressingBootstrap`은 Step 2 적용 전에 `PostDetailedStage3GameplayPropsBootstrap.Stage3RootName`과 일치하는 씬 오브젝트를 삭제하도록 보정했다.
- `AssetStoreShipDressingEditorValidation`과 `Run-AssetStoreShipDressingStep2.ps1`은 `Stage3GameplayPropRoots=0`을 검증 조건으로 추가했다.
- 확인 결과:
  - 씬 검색 기준 `Post Detailed Stage 3 Gameplay Props`, `Stage 3 Cargo Hold Art Sample Dressing`, `Stage 3 Cargo Start Corridor Art Sample Dressing`, `Stage 3 Art Sample Room Dressings`는 0건이다.
  - 기존 실내/그레이박스/Phase20 머티리얼은 실제 내용 diff가 없다.
  - Step 2 로그 기준 `Stage3GameplayPropRoots=0`, `EnabledStage3CargoStartRenderers=0`, `EnabledLegacyClearanceColliders=0`.
  - 적용 비교 이미지 `unity_view_01`, `unity_view_06`, `unity_view_08`에서 빨간 줄무늬 화물창 배경과 Stage 3 장비 형태가 보이지 않는 것을 육안 확인했다.
- 검증:
  - `./scripts/Refresh-UnityProject.ps1` 통과.
  - `./scripts/Run-AssetStoreShipDressingStep2.ps1` 통과.
  - `./scripts/Run-AssetStoreShipDressingStep2Comparison.ps1` 통과.
  - `./scripts/Run-HarnessValidation.ps1` 통과.
  - `./scripts/Run-EditModeTests.ps1` 통과, `243/243`.
  - `./scripts/Run-PlayModeTests.ps1` 통과, `9/9`.
  - `./scripts/Build-WindowsDev.ps1` 통과.
  - `git diff --check` 통과, CRLF 경고만 있음.
