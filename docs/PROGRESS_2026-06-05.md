# Progress 2026-06-05

## Next-Day Handoff For 2026-06-06

- Before continuing, read `docs/PROGRESS_2026-06-02.md`, this handoff section, `docs/DETAILED_IMPLEMENTATION_PLAN.md`, and `docs/MVP_PLAYTEST_CHECKLIST.md`.
- Current scope remains detailed single-player implementation. Do not start online/multiplayer gameplay yet; multiplayer should wait until detailed implementation is further completed.
- Detailed implementation steps 1, 2, and 3 are complete.
  - Step 1 added runtime skeleton data types and related EditMode coverage.
  - Step 2 added temporary detailed planet/route/contract/cargo catalog data and reward/acceptance rules.
  - Step 3 split planet maintenance from the contract board, added reputation/association economy rules, and added the low-fame revival contract flow.
- Latest UX correction completed after Step 3:
  - Planet maintenance only opens the contract board; it does not directly start follow-up contracts.
  - Contract board category buttons (`Association`, `Private`, `Special`) only select a category/default row.
  - Listed contract row buttons select one visible contract while keeping the board open.
  - Only the separate `Accept` button starts the selected contract.
  - Damaged ships can browse/select contracts, but `Accept` stays disabled until repair/readiness requirements are met.
- Important changed areas for the UX correction:
  - `Assets/_Project/Runtime/Core/Session/ContractBoardController.cs`
  - `Assets/_Project/Editor/Validation/Phase10PlanetMaintenanceBootstrap.cs`
  - `Assets/_Project/Editor/Validation/Phase10PlanetMaintenanceEditorValidation.cs`
  - Phase10/11/12/14/15/18 open-editor smoke tests.
- Latest verification after the UX correction:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `110/110`
  - `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`: passed
  - `.\scripts\Run-Phase11AsteroidHazardSmoke.ps1`: passed
  - `.\scripts\Run-Phase12ManualTurretSmoke.ps1`: passed
  - `.\scripts\Run-Phase14ParvumIntruderSmoke.ps1`: passed
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed
  - `git diff --check`: passed with CRLF warnings only
- Known validation note:
  - `.\scripts\Run-PlayModeTests.ps1` timed out in the open-editor Unity Test Runner callback after request pickup. This matched the known Test Runner bridge instability described in `docs/HARNESS.md` / `docs/PLAYMODE_TEST_RUNNER_STABILITY.md`; feature-specific PlayMode smoke tests above were used as the validation path.
- Cleanup completed:
  - Generated `Assets/InitTestScene*.unity*`, `ProjectSettings/SceneTemplateSettings.json`, `Temp/__Backupscenes`, and stale `Temp/UnityLockfile` were removed.
  - Unity editor was reopened to the project after validation.
- Next suggested work:
  - Continue with `docs/DETAILED_IMPLEMENTATION_PLAN.md` step 4, `개인 화물과 상점 판매 루프`.
  - Before implementing step 4, explain the step to the user and confirm any ambiguous personal cargo collection/sale rules.

## Detailed Implementation Step 3 Completion

- Completed the separate detailed implementation step 3 without adding online or multiplayer gameplay implementation.
- Split contract selection out of planet maintenance:
  - `PlanetMaintenanceController` now acts as the maintenance hub for repair, contract board, shop, personal cargo, and upgrades.
  - `ContractBoardController` owns the separate contract board screen, category buttons, visible contract list, and next transport selection.
  - Follow-up UX correction: category buttons only select a category/contract row, selectable contract row buttons choose a listed contract, and only the separate `Accept` button starts the next transport.
- Added reputation runtime rules:
  - Fame and association fame are stored on `GameSessionState`.
  - Clean success, failure, crew death, and cargo loss update reputation by contract difficulty.
  - Private contracts are hidden below low fame.
  - A one-time association revival contract appears at low fame and resets fame to zero on clean completion.
  - Low-fame non-members who take an association contract are forced into association membership.
- Linked association maintenance fee constants through detailed contract rules and settlement input construction.
- Hardened reward display so negative fame cannot create negative reputation pay or break the contract board.
- Updated Phase10/11/12/14/15/18 open-editor smoke tests to use the maintenance -> contract board -> contract selection flow.
- Tightened Phase10/11/12/14/15/18 smoke tests so association category clicks and contract row clicks must keep the board open until `Accept` is clicked.
- Verification on 2026-06-05:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `110/110`
  - `.\scripts\Run-PlayModeTests.ps1`: timed out in the open-editor Test Runner callback after request pickup during the follow-up UX correction; feature-specific PlayMode smoke tests below were used as the validation path per `docs/HARNESS.md`.
  - `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`: passed
  - `.\scripts\Run-Phase11AsteroidHazardSmoke.ps1`: passed
  - `.\scripts\Run-Phase12ManualTurretSmoke.ps1`: passed
  - `.\scripts\Run-Phase14ParvumIntruderSmoke.ps1`: passed
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed

## Detailed Implementation Step 2 Completion

- Completed the separate detailed implementation step 2 without adding online or multiplayer gameplay implementation.
- Added temporary pure C# default catalog data for planets, routes, cargo, and contracts through `DetailedContractCatalogRules.CreateDefaultStepTwoCatalog()`.
- Route distance is a simple numeric input and not a planet coordinate system; it is used as a reward/transport-time proxy.
- Contract rewards now have a traceable pure rule: cargo value 60%, route distance 30%, and fame or association fame 10%.
- Association-specific reward inputs are separated in rules: 10% repair support estimate, safe-streak bonus, and association maintenance fee tracking.
- Association contract visibility is verified as at least 5 for members and exactly 2 for non-members.
- Cargo hold score acceptance rules are verified separately from the current maintenance UI.
- Existing tutorial and post-tutorial UI-compatible contract pair remain stable: `association-local-001` and `private-sample-001`.
- Verification on 2026-06-05:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `102/102`
  - `.\scripts\Run-PlayModeTests.ps1`: passed in open Unity editor, `7/7`
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed in open Unity editor

## Detailed Implementation Step 1 Completion

- Completed the separate detailed implementation step 1 without adding online or multiplayer gameplay implementation.
- Added pure C# runtime skeleton data types for later detailed stages: planets, routes, cargo, contracts, ship rooms, equipment, hostile factions, hostile units, hazards, and a catalog container.
- Added EditMode coverage for the new skeleton models and validation rules.
- Stabilized the PlayMode test runner so open-editor `Run-PlayModeTests.ps1` survives PlayMode domain reload and writes result XML.
- Updated the Phase2 PlayMode baseline to avoid `-nographics` render texture failures and to validate the current interaction target path.
- Extended the harness and MVP playtest checklist documentation with the detailed step 1 regression items.
- Verification on 2026-06-05:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `93/93`
  - `.\scripts\Run-PlayModeTests.ps1`: passed in open Unity editor, `7/7`
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed in open Unity editor
  - `.\scripts\Build-WindowsDev.ps1`: passed

이 문서는 2026-06-04 진행 문서 이후 이어서 작업한 MVP 구현 상태와 17~18단계 범위 확정 사항을 정리한다.

## 사용자 확인 사항

- 17단계는 실제 네트워크 패키지, Steam 로비, 온라인 접속을 도입하지 않는다.
- 이번 범위는 네트워크 독립 인터페이스와 로컬 2인 세션 시뮬레이션까지로 제한한다.
- 이후 본격 멀티플레이는 최대 5인까지 고려하되, 서버 권한 모델과 상호작용 소유권을 상세히 확정한 뒤 진행한다.

## 완료된 구현

### Phase 17: 협동 플레이 기반

- `Bellerophon.Core.Coop` 계층을 추가해 네트워크 SDK와 분리된 협동 세션 인터페이스를 만들었다.
- `LocalCoopSessionAuthority`를 추가해 같은 프로세스 안에서 2명 이상의 플레이어가 같은 세션 스냅샷을 공유하도록 했다.
- 플레이어 위치, 시점, 현재 구역, 상호작용 중인 장치 상태를 동기화 대상 스냅샷으로 분리했다.
- 장치 소유권을 추가해 같은 장치를 두 플레이어가 동시에 조작하려 할 때 권한 측에서 한 명만 수락하도록 했다.
- 세션 상태, 선박 상태, 화물 상태, 운행 상태, 위험 결과, 침입자 상태, CCTV 대상, 엔진 오버클럭 상태를 공유 스냅샷에 포함했다.
- 선박 구역 피해 같은 결과는 로컬 클라이언트가 직접 확정하지 않고 권한 세션의 authoritative 적용 경로로만 반영하도록 했다.
- `IPlatformMultiplayerServices`와 `NullPlatformMultiplayerServices`를 추가해 Steam/온라인 전송 기능이 아직 붙지 않은 상태를 플랫폼 인터페이스 뒤에 명시했다.
- `GameSessionState.WithShipState`를 추가해 권한 세션에서 확정한 선박 상태를 세션 상태에 일관되게 반영할 수 있게 했다.
- Phase 17 EditMode 테스트와 `.\scripts\Run-Phase17CoopFoundationSmoke.ps1` 검증 경로를 추가했다.

### Phase 18: 반복 가능한 플레이테스트 루프

- `docs/MVP_PLAYTEST_CHECKLIST.md`를 추가해 수동 플레이테스트 항목을 시작, 튜토리얼, 첫 정산/정비, 후속 운송, 반복 가능성으로 나누어 정리했다.
- `MvpPlaytestLoopTests`를 추가해 협회 시작, 튜토리얼 운송, 정산/수리, 후속 위험 대응, 파르붐 처치, 로컬 협동 스냅샷, 정산 도착 게이트 회귀를 EditMode에서 검증한다.
- `Phase18MvpPlaytestLoopPlayModeSmoke`와 `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`를 추가해 실제 `CargoRunMvp` PlayMode에서 시작부터 두 번째 정비 준비까지 자동으로 확인한다.
- Phase 18 smoke는 Phase16 HUD/맵/분위기와 Phase17 로컬 협동 스냅샷 경계를 함께 회귀 검증한다.
- 검증 중 `TransportSettlementController`의 정산 도착 게이트가 이전 운송의 정산 표시 상태 때문에 두 번째 운송 도착을 막는 결함을 발견했다. 도착 게이트를 완료 운송 횟수 기준으로 보강해 같은 정산 중복 표시만 막고 다음 운송 정산은 정상 처리되게 수정했다.

## 검증 결과

- `.\scripts\Run-Phase17CoopFoundationSmoke.ps1`: 통과
  - `Players=2; Phase=Transporting; RemotePose=Cockpit; Cctv=CargoHold; EngineRoom=75; OnlineTransport=Deferred`
- `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: 통과
  - `Start=Association; Tutorial=Completed; Repair=1200; FollowUp=association-local-001; Hazard=Avoided+Neutralized; Intruder=Neutralized; Completed=2; NextReady=True`
- `.\scripts\Run-EditModeTests.ps1`: 통과, `88/88`
- `.\scripts\Run-HarnessValidation.ps1`: 통과
- `.\scripts\Build-WindowsDev.ps1`: 통과
- `.\scripts\Run-PlayModeTests.ps1`: 열린 에디터 Test Runner 완료 콜백 타임아웃으로 결과 XML 미생성. Phase18 범위의 씬/UI/입력 통합 검증은 `Run-Phase18MvpPlaytestLoopSmoke.ps1`로 통과했다.

## 다음 단계

- `docs/MVP_IMPLEMENTATION_ORDER.md`의 18단계까지 완료했다.
- 다음 구현 단계는 사용자 확인 후 별도 범위를 확정한다.
- 본격 온라인/최대 5인 멀티플레이는 별도 설계로 다루며, 이번 17단계 결과물은 그 설계를 위한 상태 경계와 검증 가능한 로컬 시뮬레이션 기반으로 둔다.
