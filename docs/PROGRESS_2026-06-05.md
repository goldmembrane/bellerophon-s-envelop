# Progress 2026-06-05

## Next-Day Handoff For 2026-06-08

- Detailed implementation steps 4, 5, 6, 7, 8, 9, 10, 11, and 12 are complete.
- Current completed gameplay scope:
  - Step 4: personal cargo collection, capacity, transport damage, and selected shop sale flow.
  - Step 5: maintenance repair billing, towing billing, and ship upgrade purchase/equip skeletons.
  - Step 6: six-room damage rule connections to launch readiness, controls, HUD, hazards, intruders, and storage.
  - Step 7: supply storage, 3 base hand slots with 4-slot upgrade, item catalog, buy routing, selected sell/disposal flow.
  - Step 8: first-pass weapon/protective/treatment/enhancement/utility item rules and active item effects.
  - Step 9: player combat and source-named status effects using only `정지`, `화상`, `출혈`, `탈진`, `피로`, `현기증`, and `혼란`.
  - Step 10: cockpit manual-flight booster rule connection, armory manual/auto weapon operation separation, upgraded manual turret magazine/plasma, and control-room screen/CCTV/internal purification hardening.
  - Step 11: external hazard expansion for asteroid field small/large, Cargo Freedom League region, Space Pirate region, and Alien Life region; concealed black hole remains deferred.
  - Step 12: intruder AI common framework with source objective categories, room/corridor route assessment, control-room damage effects, and source faction relationships.
- Step 9 implementation notes:
  - The previous temporary Step 9 code was reset before reimplementation.
  - `CombatStatusModels` and `PlayerCombatModels` hold pure C# rules for status ticking, damage, protection, and side effects.
  - First-person status, movement, equipment use, Parvum intruder state, and Phase16 HUD display now consume those rules.
  - Equipment connections include source-aligned first-pass effects for charged electric baton `정지`, sustained mini flamethrower `화상`, flashbang `혼란`, bandage `출혈` clear, and enhancer aftereffects `탈진`/`피로`/`현기증`.
- Step 10 implementation notes:
  - Cockpit manual-flight booster is connected to active transport hazards and engine damage limits. For the current asteroid-field validation path, using the booster in manual flight reduces the active hazard by 10 seconds. No new input binding was added because the source design does not specify a booster key.
  - Armory weapon operation now separates auto turret and manual turret states. Manual flight forces the weapon room into auto turret mode, matching the source rule that the armory cannot be used during manual flight.
  - Weapon systems upgrades now affect manual turret magazine capacity through the source values `50`, `60`, `75`, and `100`. Plasma cannon availability starts at the upgraded weapon tier that includes plasma, uses right click in manual turret mode, runs for 3 seconds, deals 50 damage every 0.1 seconds, and recovers its 1 minute cooldown even outside manual mode.
  - Armory damage gates upgraded weapon operation: at `<=75%`, plasma/auto-aim are unavailable; at destroyed armory, weapons cannot operate.
  - Control-room CCTV now uses the five non-control rooms and includes Supply Room. Screen operation is separated into main CCTV, vertical room list, and horizontal ship-layout modes.
  - Control-room internal purification is implemented as the selected-room system from the control-room vertical screen: only the clicked room is temporarily sealed, fire damage totals 500 over 30 seconds, players inside the selected room are damaged too, room durability is not damaged by the purification itself, and the room reopens when the operation ends.
  - This selected-room internal purification is intentionally separate from the special item `복도 정화 장치`, which targets all corridors and remains future work.
- Latest user-facing fix:
  - ESC now closes the shared device HUD for engine-room and supply-room interactions, so their information panels no longer remain after leaving the interaction.
  - `Phase6RoomInteractionsSmoke` now validates ESC closing for engine-room, control-room, and supply-room interaction panels.
  - Control-room screen interaction now exits on ESC, restores first-person input/cursor state, and hides the control-room panel.
  - Control-room vertical room list now creates clickable room buttons and also accepts number keys `1` through `6`; selecting a room through this UI starts the selected-room internal purification.
  - `Phase6RoomInteractionsSmoke` now validates the actual UI click path for internal purification and ESC control-room exit, not only the underlying API.
  - The lower-right Phase16 transport map was missing because running the lower Phase15 smoke/bootstrap alone could save `CargoRunMvp` at a Phase15-only generated state.
  - `Phase15EquipmentLoopPlayModeSmoke` now re-applies Phase16 HUD/map/atmosphere after a passing run.
  - `CargoRunMvp` currently contains `Phase 16 Ship Map` and `ShipInteriorMapHud`.
- Latest source-design update:
  - On 2026-06-07, `docs/GAME_DESIGN_SOURCE.txt` was replaced from `C:\Users\gus68\OneDrive\문서\spce.txt`; the latest replacement used the file saved at 2026-06-07 21:57:03.
  - For step 11, user confirmed `은폐 블랙홀` should be deferred to later hazard expansion instead of implemented now.
  - The updated source provides external hazard fame gates: `화물 자유 연대` requires fame 1800 or higher, `우주 해적` 3000 or higher, `외계 생명체` 900 or higher, and `은폐 블랙홀` 4500 or higher.
  - User confirmed the fame gates are per-session unlocks: once a hazard gate has been crossed in the current session, falling below that fame value does not disable the hazard again; only game-over reset clears the unlocks.
- Step 11 implementation notes:
  - `TransportHazardType` now separates `AsteroidFieldSmall`, `AsteroidFieldLarge`, `CargoFreedomLeagueRegion`, `SpacePirateRegion`, `AlienLifeRegion`, and the documented-but-deferred `ConcealedBlackHole`.
  - Source hazard values are connected: asteroid check every 5 seconds at 30%, small/large asteroid damage 10/20 and turret target health 400/1000; Cargo Freedom League check every 10 seconds at 15%; Space Pirate check every 10 seconds at 5%; Alien Life check every 5 seconds at 10%.
  - `TransportHazardUnlockState` is stored on `GameSessionState`; fame gates unlock per session and do not relock when fame drops. New session/game-over reset starts from the default locked state.
  - Runtime hazard occurrence now ticks during transport instead of forcing an asteroid immediately at follow-up start.
  - Step 11 results cover room damage, boarding events, and pirate bombardment. Cargo Freedom League and Alien Life regions intentionally create boarding events without invented direct room-damage values.
  - `은폐 블랙홀` cannot be started by runtime or validation APIs in step 11 and remains later hazard expansion.
- Step 12 implementation notes:
  - User confirmed the second seed entity name as `푸가`; the source replacement and detailed plan now use that name.
  - `IntruderDefinition` and `IntruderEntityState` now preserve common mobility and command authority data for later hostile unit types.
  - `IntruderRules` now evaluates source objective categories, sealed-room target selection, Phase16 map corridor routes, control-room corridor closure, engine blackout count, CCTV/detection/suppression state, and internal intruder stat multiplier.
  - Faction relationship profiles now represent `적대`, `경쟁`, `동맹`, `유대`, and `지휘`, including marker kinds and friendly-fire health/status behavior from the source design.
  - Existing Parvum logic now moves through the shared route model and no longer applies pressure to an unreachable sealed target room.
- Latest verification after adding the full phase sweep on 2026-06-07:
  - `.\scripts\Run-Phase1To18Smokes.ps1`: passed, Phase 1 through Phase 18 in order.
  - Added focused Phase 1, Phase 3, and Phase 5 smoke scripts so the sweep covers every phase number rather than only the previously scripted phase subset.
- Latest verification after the control-room interaction fix on 2026-06-07:
  - `.\scripts\Run-HarnessValidation.ps1`: passed.
  - `.\scripts\Run-EditModeTests.ps1`: passed, `179/179`.
  - `.\scripts\Run-Phase6RoomInteractionsSmoke.ps1`: passed, including control-room room-button click and ESC close.
  - `.\scripts\Run-Phase16HudMapAtmosphereSmoke.ps1`: passed after Phase6 to restore latest HUD/map scene state.
  - `git diff --check`: passed with CRLF warnings only.
- Latest verification after Step 11 on 2026-06-07:
  - `.\scripts\Run-HarnessValidation.ps1`: passed.
  - `.\scripts\Run-EditModeTests.ps1`: passed.
  - `.\scripts\Run-Phase11AsteroidHazardSmoke.ps1`: passed, including asteroid small damage, manual large-asteroid avoidance, Cargo Freedom League boarding events, Space Pirate boarding plus bombardment, and Alien Life boarding events.
  - `.\scripts\Run-Phase12ManualTurretSmoke.ps1`: passed after updating asteroid target health expectations to source values.
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed after rerunning sequentially from Edit mode.
  - `.\scripts\Run-Phase1To18Smokes.ps1`: passed, Phase 1 through Phase 18 in order.
  - `.\scripts\Build-WindowsDev.ps1`: passed.
  - `git diff --check`: passed with CRLF warnings only.
- Latest verification after Step 12 on 2026-06-07:
  - `.\scripts\Run-HarnessValidation.ps1`: passed.
  - `.\scripts\Run-EditModeTests.ps1`: passed.
  - `.\scripts\Run-Phase13IntruderFrameworkSmoke.ps1`: passed with target, route, suppression, and relation validation.
  - `.\scripts\Run-Phase14ParvumIntruderSmoke.ps1`: passed after the shared route-model connection.
  - `.\scripts\Run-Phase1To18Smokes.ps1`: passed, Phase 1 through Phase 18 in order.
  - `.\scripts\Build-WindowsDev.ps1`: passed.
  - `git diff --check`: passed with CRLF warnings only.
  - The Unity editor was restarted once because it retained a stale script-compilation failure flag after a fixed compiler error.
- Latest verification after Step 10 on 2026-06-07:
  - `.\scripts\Run-HarnessValidation.ps1`: passed.
  - `.\scripts\Run-EditModeTests.ps1`: passed, `177/177`.
  - Feature PlayMode smokes passed:
    - `.\scripts\Run-Phase6RoomInteractionsSmoke.ps1`: passed, including control-room screen switch, selected-room purification, player damage inside the selected room, no room durability damage, and room reopening.
    - `.\scripts\Run-Phase11AsteroidHazardSmoke.ps1`: passed.
    - `.\scripts\Run-Phase12ManualTurretSmoke.ps1`: passed, including upgraded plasma, manual-flight booster, and manual-flight-forces-auto-turret regression.
    - `.\scripts\Run-Phase16HudMapAtmosphereSmoke.ps1`: passed after restarting the editor from a stuck PlayMode Test Runner state.
    - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed.
  - `.\scripts\Build-WindowsDev.ps1`: passed.
  - `git diff --check`: passed with CRLF warnings only.
- Known validation note:
  - `.\scripts\Run-PlayModeTests.ps1` still fails in the open-editor Unity Test Runner callback path with `PlayModeTests did not return a Unity Test Runner completion callback within 120 seconds.`
  - The latest open-editor retry produced no `TestResults\playmode-results.xml` and left the editor in Play mode until the editor was restarted.
  - Feature-specific PlayMode phase smokes and Windows dev build passed afterward.
- Next suggested work:
  - Next target is `docs/DETAILED_IMPLEMENTATION_PLAN.md` step 13, seed entity detailed implementation. Explain the step to the user before implementing it.
  - Do not start online/multiplayer gameplay yet.

## Detailed Implementation Step 8 Completion (2026-06-06)

- Completed detailed implementation step 8, weapons/protective/treatment/enhancement/utility item first pass, without final item animations or VFX.
- Expanded item rules from the design source:
  - Added source-aligned first-pass definitions for shotgun, mini flamethrower, electric baton, and dagger.
  - Shotgun now has confirmed magazine data shell and reload input skeleton; musket reload remains pending confirmation.
  - Presence Detector remains special-unlock locked data only.
- Added active item effects:
  - Flashlight activates a timed utility state and uses durability.
  - Protective Suit and Suppression Shield can be equipped from supply storage and add damage reduction on top of the basic protective suit.
  - Injury Reliever, Bandage Set, and Shield Charge Battery can apply recovery deltas; runtime treatment can update `FirstPersonPlayerStatus`.
  - Strength Enhancer is a timed melee damage buff following the source direction: stick/electric baton gain +40%, dagger gains +10, ranged weapons keep base damage.
- Updated UI and scene wiring:
  - Equipment HUD now shows active protection, strength, and flashlight timers.
  - Dagger alternate mode uses the existing right-click toggle path as throwing mode.
  - Shop buy controls now include representative step 8 buttons for Shotgun, Protective Suit, and Strength Enhancer.
  - Phase 15 scene bootstrap was regenerated through `EnsurePhase15EquipmentLoop` so the new buttons are saved in `CargoRunMvp`.
- Updated validation:
  - Added EditMode coverage for step 8 weapon definitions, supply item effects, flashlight timed state, and treatment recovery through `ShipDeviceInteractionState`.
  - Extended Phase15 smoke to buy and use flashlight, treatment, protective gear, and strength enhancer; sync used supply equipment back to the session; sell a selected personal cargo row and selected flashlight row; then resolve Parvum with strengthened stick and base-damage musket.
- Latest verification after Step 8:
  - `.\scripts\Run-HarnessValidation.ps1`: passed before the later open-editor bridge stuck state.
  - `.\scripts\Run-EditModeTests.ps1`: passed before the later open-editor bridge stuck state; latest result file reports `155/155`.
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command EnsurePhase15EquipmentLoop`: passed.
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed.
    - `Wallet=51; Slots=3/3; StickHit=42; MusketHit=50; Reload=ReloadSkeleton; Intruder=Neutralized; Shop=Step8ItemEffects`
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed.
    - `Start=Association; Tutorial=Completed; Repair=400; FollowUp=association-local-001; Hazard=Avoided+Neutralized; Intruder=Neutralized; Completed=2; NextReady=True`
  - `.\scripts\Run-PlayModeTests.ps1`: timed out with only a pending bridge log marker, matching the known open-editor Test Runner bridge instability.
  - `.\scripts\Build-WindowsDev.ps1`: timed out because `UnityEditorValidationBridge` stopped picking up requests after the Test Runner pending state; no build result was produced in this pass.
- Harness note:
  - `UnityEditorValidationBridge` was patched so recovered PlayMode results can be processed before the `isRunning` guard and stale Test Runner requests fail out after 120 seconds instead of blocking all later bridge requests.
  - The currently open editor did not reload the patched bridge during this run; restart/reload the Unity editor before re-running full bridge-based validation or Windows build.
- Reverification on 2026-06-07:
  - `.\scripts\Build-WindowsDev.ps1`: passed.
    - `Build Finished, Result: Success`
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed.
    - `Wallet=51; Slots=3/3; StickHit=42; MusketHit=50; Reload=ReloadSkeleton; Intruder=Neutralized; Shop=Step8ItemEffects`
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed.
    - `Start=Association; Tutorial=Completed; Repair=400; FollowUp=association-local-001; Hazard=Avoided+Neutralized; Intruder=Neutralized; Completed=2; NextReady=True`
  - `.\scripts\Run-PlayModeTests.ps1`: still fails in the open-editor Test Runner callback path with `PlayModeTests did not return a Unity Test Runner completion callback within 120 seconds.`
- HUD map restoration fix on 2026-06-07:
  - Cause: after Phase16 added the lower-right ship map, running the lower Phase15 smoke/bootstrap alone could leave `CargoRunMvp` saved at a Phase15-only generated scene state.
  - Fix: a passing `Phase15EquipmentLoopPlayModeSmoke` now re-applies Phase16 HUD/map/atmosphere assets before writing the final smoke log.
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed with `Scene=Phase16HudMapRestored`.
  - `.\scripts\Run-Phase16HudMapAtmosphereSmoke.ps1`: passed.
    - `Vitals=100/100; Map=Cockpit after move; Crosshair=Hidden; Fog=On; AudioSignals=3; RightClick=Toggle`
  - `.\scripts\Build-WindowsDev.ps1`: passed after restoring the map.
- Next target:
  - Continue with `docs/DETAILED_IMPLEMENTATION_PLAN.md` step 9, status effects and countermeasure details, after user confirmation.

## Detailed Implementation Step 7 Completion (2026-06-06)

- Completed detailed implementation step 7, `비품창고, 인벤토리, 상점 구매 상세`, with item effects beyond existing stick/musket combat left for step 8.
- Updated the design-source hand-slot rule per user confirmation:
  - Player hand slots now start at 3.
  - The pouch upgrade model expands hand slots to 4.
- Expanded equipment state:
  - `PlayerEquipmentState` now tracks unlocked hand slots, unlocked supply slots, durability percentage, purchase price metadata, and purchased-item presence.
  - Supply storage capacity now follows the equipped `SupplySlots` ship upgrade effect values: 3/5/10/25.
  - Starting association equipment still grants the basic protective suit and one stick, leaving the extra base hand slots empty.
- Expanded equipment catalog and shop rules:
  - Added data-backed entries for weapon, protective, treatment, enhancement, utility, fame-limited, and special products from the design source.
  - Shop buy catalog is separated into common products, fame-limited products, and special products.
  - Purchases route to hand slots or supply storage according to item storage target.
  - Special products remain locked until later special-contract unlock work.
  - Purchased items can be disposed for 1% of purchase price in the shop sell tab.
  - Personal cargo sale remains separate from purchased-item disposal.
- Updated UI skeletons:
  - Supply storage shows tab labels for All, Weapon, Protective, Treatment, Enhancement, and Utility.
  - Shop buttons now cover musket, flashlight, treatment item purchase, sell-list selection, selected purchased-item disposal, and selected personal cargo sale.
- Updated validation:
  - Added EditMode coverage for 3-to-4 hand slot upgrade, supply-slot upgrade capacity, catalog filtering, hand/supply purchase routing, 1% disposal, and durability damage.
  - Extended Phase15 smoke to verify musket, flashlight, treatment purchase, sell-list selection before disposal/sale, selected purchased-item disposal, selected personal cargo sale, and later Parvum combat regression.
  - Extended the MVP playtest checklist and harness docs for detailed step 7.
- Latest verification after Step 7:
  - `.\scripts\Run-HarnessValidation.ps1`: passed.
  - `.\scripts\Run-EditModeTests.ps1`: passed, `155/155`.
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed.
    - `Wallet=104; Slots=3/3; StickHit=30; MusketHit=50; Reload=ReloadSkeleton; Intruder=Neutralized; Shop=Step7SelectedSell`
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed.
    - `Start=Association; Tutorial=Completed; Repair=400; FollowUp=association-local-001; Hazard=Avoided+Neutralized; Intruder=Neutralized; Completed=2; NextReady=True`
  - `.\scripts\Build-WindowsDev.ps1`: passed.
- Next target:
  - Continue with `docs/DETAILED_IMPLEMENTATION_PLAN.md` step 8, `무기, 방호, 치료, 강화, 기타 아이템 1차`, after user confirmation.

## Detailed Implementation Step 6 Completion (2026-06-06)

- Completed detailed implementation step 6, six-room damage effect rule connections, without adding final work animations or visual effects.
- Added centralized six-room damage rules in `ShipStateRules`:
  - Cockpit damage affects auto pilot availability, manual flight input response, and destroyed-cockpit launch readiness.
  - Cargo hold damage affects cargo capacity, personal cargo transport, cargo loss, and launch readiness at critical cargo hold damage.
  - Engine room damage affects transport duration, booster/overclock availability, blackout room count, offline room count, and destroyed-engine towing readiness.
  - Armory damage affects manual turret availability, auto aim/plasma availability, active auto turret count, and manual turret aim response.
  - Control room damage affects corridor seal percentage, CCTV channel count, intruder detection, cargo damage warning, intruder suppression, seed intruder occurrence, and internal intruder room damage.
  - Supply room damage affects usable storage slots, storage security, equipment durability risk, and destroyed-room equipment explosion chance.
- Connected the rules to runtime flows:
  - Transport start now checks `EvaluateStartReadiness` and includes personal cargo transport readiness when personal cargo is stored.
  - Manual flight input speed is reduced by cockpit damage.
  - Personal cargo loading is blocked when the cargo hold cannot transport personal cargo.
  - Contract board and planet maintenance readiness text now include personal cargo blocking.
  - Engine overclock, CCTV cycling, manual turret operation, supply slots, and seed intruder occurrence/damage now read from six-room damage rules.
- Updated room UI/HUD skeletons:
  - Planet maintenance room rows show concrete damage effects instead of generic risk text.
  - Device HUD panels show input response, duration multiplier, booster/overclock status, CCTV channels, detection/suppression status, turret/aim/plasma status, storage slot/security risk, cargo capacity, and personal cargo transport status.
- First-pass numeric connection notes:
  - Cockpit manual input response is 75% at `<=75%`, 50% at `<=25%`, and 0% when destroyed.
  - Armory manual turret aim response is 50% at `<=25%` and 0% when destroyed.
  - These values are placeholder rule-connection values because the current design source specifies the affected systems but not exact input-delay numbers.
- Updated validation:
  - Added EditMode coverage for six-room threshold effects, personal cargo transport blocking, cockpit manual input response, CCTV/storage/turret HUD limits, and control-room destroyed seed intruder damage.
  - Updated Phase6 smoke to verify damaged control room CCTV shutdown, damaged supply storage slot reduction, and destroyed armory manual turret shutdown.
  - Updated Phase10 smoke to expect concrete room damage effect text instead of old generic risk wording.
- Latest verification after Step 6:
  - `.\scripts\Run-HarnessValidation.ps1`: passed.
  - `.\scripts\Run-EditModeTests.ps1`: passed, `142/142`.
  - `.\scripts\Run-Phase6RoomInteractionsSmoke.ps1`: passed.
  - `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`: passed.
  - `.\scripts\Run-Phase11AsteroidHazardSmoke.ps1`: passed.
  - `.\scripts\Run-Phase12ManualTurretSmoke.ps1`: passed.
  - `.\scripts\Run-Phase14ParvumIntruderSmoke.ps1`: passed.
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed.
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed.
  - `.\scripts\Build-WindowsDev.ps1`: passed.
- Cleanup completed:
  - Generated `ProjectSettings/SceneTemplateSettings.json` was removed after validation.

## Detailed Implementation Step 5 Completion (2026-06-06)

- Completed detailed implementation step 5, `정비와 화물선 업그레이드 상세`.
- Updated repair billing to follow `docs/GAME_DESIGN_SOURCE.txt` settlement-summary rules:
  - Normal repair uses the sum of missing durability percentage across all 6 rooms at `$5` per 1%.
  - Normal repair is capped at `599 x 5 = $2995`.
  - If all 6 rooms are at 0%, normal repair cost is separated from total-loss billing and the total-loss claim is `$5000`.
- Updated towing billing:
  - Session state now stores `TowingIncidentCount`.
  - Towing costs use the running incident number: 1st `$2000`, 2nd `$3000`, 3rd `$5000`, then +`$2500` per additional tow.
- Added ship upgrade runtime state:
  - Five upgrade categories are tracked: durability, weapon systems, auto pilot, supply slots, internal control.
  - Purchase tier and equipped tier are stored separately for categories that require loadout selection.
  - Appearance customization placeholder slots are stored as data only for later visual implementation.
  - Upgrade costs follow the updated design source: durability `$1000/$2000/$4000`, weapons `$1500/$2500/$4500`, autopilot `$3000/$4800/$6500`, supply slots `$1000/$2500/$5000`, internal control `$2500/$5000/$10000`.
- Added ship upgrade UI:
  - Planet maintenance `Upgrades` opens a separate full-screen upgrade screen.
  - Durability upgrades apply automatically when purchased.
  - Weapon systems, auto pilot, supply slots, and internal control still use separate `Buy` and `Equip` actions.
  - Back returns to planet maintenance without leaving the upgrade screen over the maintenance UI.
- Updated validation:
  - Added EditMode coverage for settlement-summary repair cost, total-loss separation, towing incident costs, durability auto-apply, and non-durability upgrade purchase/equip separation.
  - Updated Phase10 smoke to verify upgrade screen open, durability tier purchase auto-apply, separate equip for other upgrades, repair, multiple accepted contracts, and `Start Run`.
- Latest verification after Step 5:
  - `.\scripts\Run-HarnessValidation.ps1`: passed.
  - `.\scripts\Run-EditModeTests.ps1`: passed.
  - `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`: passed.
  - `.\scripts\Run-Phase9SettlementGameOverSmoke.ps1`: passed.
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed.
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed.
  - `.\scripts\Run-PlayModeTests.ps1`: open-editor bridge did not complete the request and left only a pending log marker; this matches the known PlayMode Test Runner bridge instability. Feature-specific PlayMode smoke tests above were used as the validation path.
  - `.\scripts\Build-WindowsDev.ps1`: passed after clearing the stale bridge request and rerunning through batch mode.
- Cleanup completed:
  - Generated `Assets/InitTestScene*.unity*`, `ProjectSettings/SceneTemplateSettings.json`, `Temp/__Backupscenes`, and stale bridge request files were removed without separate confirmation because they are validation/generated artifacts.

## Next-Day Handoff For 2026-06-06

- Before continuing, read `docs/PROGRESS_2026-06-02.md`, this handoff section, `docs/DETAILED_IMPLEMENTATION_PLAN.md`, and `docs/MVP_PLAYTEST_CHECKLIST.md`.
- Current scope remains detailed single-player implementation. Do not start online/multiplayer gameplay yet; multiplayer should wait until detailed implementation is further completed.
- Detailed implementation steps 1, 2, 3, and 4 are complete.
  - Step 1 added runtime skeleton data types and related EditMode coverage.
  - Step 2 added temporary detailed planet/route/contract/cargo catalog data and reward/acceptance rules.
  - Step 3 split planet maintenance from the contract board, added reputation/association economy rules, and added the low-fame revival contract flow.
  - Step 4 added personal cargo collection, cargo-hold capacity checks, trait-based shop sale, and transport damage sale-price reduction.
- Latest UX correction completed after Step 3:
  - Planet maintenance only opens the contract board; it does not directly start follow-up contracts.
  - Contract board category buttons (`Association`, `Private`, `Special`) only select a category/default row.
  - Listed contract row buttons select one visible contract while keeping the board open.
  - Only the separate `Accept` button starts the selected contract.
  - Damaged ships can browse/select contracts, but `Accept` stays disabled until repair/readiness requirements are met.
- Important changed areas for the UX correction:
  - `Assets/_Project/Runtime/Core/Session/ContractBoardController.cs`
  - `Assets/_Project/Editor/Validation/Phase10PlanetMaintenanceBootstrap.cs`
  - `Assets/_Project/Editor/Validation/Phase10PlanetMaintenanceEditorValidation.cs`
  - Phase10/11/12/14/15/18 open-editor smoke tests.
- Latest verification after Step 4:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `116/116`
  - `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`: passed
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed
- Known validation note:
  - `.\scripts\Run-PlayModeTests.ps1` timed out in the open-editor Unity Test Runner callback/request pending state on 2026-06-06. This matched the known Test Runner bridge instability described in `docs/HARNESS.md` / `docs/PLAYMODE_TEST_RUNNER_STABILITY.md`; feature-specific PlayMode smoke tests above were used as the validation path.
  - The timed-out bridge left a stale `UnityEditorBridge` request; the Unity editor was restarted and the subsequent Windows development build passed.
- Cleanup completed:
  - Generated `Assets/InitTestScene*.unity*`, `ProjectSettings/SceneTemplateSettings.json`, `Temp/__Backupscenes`, and stale `Temp/UnityLockfile` were removed.
  - Unity editor was reopened to the project after validation.
- Next suggested work:
  - Continue with `docs/DETAILED_IMPLEMENTATION_PLAN.md` step 5, `정비와 화물선 업그레이드 상세`.
  - Step 5 repair-cost implementation must follow the `docs/GAME_DESIGN_SOURCE.txt` settlement summary rule, not the planet-maintenance per-room-rate sentence: sum missing durability across all 6 rooms at `$5` per 1%, cap normal repair at `599 x 5 = $2995`, and treat all rooms at 0% as separate `$5000` total-loss billing.
  - Upgrade costs are now confirmed in the updated `docs/GAME_DESIGN_SOURCE.txt`: durability `$1000/$2000/$4000`, weapons `$1500/$2500/$4500`, autopilot `$3000/$4800/$6500`, supply slots `$1000/$2500/$5000`, internal control `$2500/$5000/$10000`.

## Detailed Implementation Step 4 Completion

- Completed the separate detailed implementation step 4 without adding final collection work animations.
- Added personal cargo runtime state:
  - Personal cargo has grade, size, base sale value, origin trait, and durability.
  - Session state stores a personal cargo hold and the current planet trait.
  - Transport completion updates the current planet trait from the active contract destination.
  - Transport cargo loss and critical cargo-hold loss reduce stored personal cargo durability, lowering future sale value.
- Added personal cargo rules:
  - Full cargo hold capacity is 300 units scaled by existing cargo-hold score.
  - Common/Rare/Premium cargo sizes are 50/100/200 units.
  - Same-trait sale penalty is -50%; cross-trait bonuses follow `docs/GAME_DESIGN_SOURCE.txt`.
  - Collection is free and uses a deterministic grade roll for the current skeleton.
- Added UI skeletons:
  - Planet maintenance `Cargo` opens a personal cargo collection screen.
  - `Collect` adds one free personal cargo if the hold has enough available capacity.
  - Shop `Sell` tab lists stored personal cargo, quotes trait/durability-adjusted sale value, and sells only the selected listed item after the player presses Sell Selected.
  - Contract cargo remains unsellable.
- Updated validation:
  - Added `PersonalCargoRulesTests` for loading capacity, sale modifier, collection/sale, and transport damage sale-price reduction.
  - Updated Phase15 smoke to collect personal cargo and sell it through the shop Sell tab.

## Detailed Implementation Step 3 Completion

- Completed the separate detailed implementation step 3 without adding online or multiplayer gameplay implementation.
- Split contract selection out of planet maintenance:
  - `PlanetMaintenanceController` now acts as the maintenance hub for repair, contract board, shop, personal cargo, and upgrades.
  - `ContractBoardController` owns the separate contract board screen, category buttons, visible contract list, and next transport selection.
  - Follow-up UX correction: category buttons only select a category/contract row, selectable contract row buttons choose a listed contract, and only the separate `Accept` button starts the next transport.
- Added reputation runtime rules:
  - Fame and association fame are stored on `GameSessionState`.
  - Clean success, failure, crew death, and cargo loss update reputation by contract difficulty.
  - Private contracts are hidden below low fame.
  - A one-time association revival contract appears at low fame and resets fame to zero on clean completion.
  - Low-fame non-members who take an association contract are forced into association membership.
- Linked association maintenance fee constants through detailed contract rules and settlement input construction.
- Hardened reward display so negative fame cannot create negative reputation pay or break the contract board.
- Updated Phase10/11/12/14/15/18 open-editor smoke tests to use the maintenance -> contract board -> contract selection flow.
- Tightened Phase10/11/12/14/15/18 smoke tests so association category clicks and contract row clicks must keep the board open until `Accept` is clicked.
- Verification on 2026-06-05:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `110/110`
  - `.\scripts\Run-PlayModeTests.ps1`: timed out in the open-editor Test Runner callback after request pickup during the follow-up UX correction; feature-specific PlayMode smoke tests below were used as the validation path per `docs/HARNESS.md`.
  - `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`: passed
  - `.\scripts\Run-Phase11AsteroidHazardSmoke.ps1`: passed
  - `.\scripts\Run-Phase12ManualTurretSmoke.ps1`: passed
  - `.\scripts\Run-Phase14ParvumIntruderSmoke.ps1`: passed
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed

## Detailed Implementation Step 2 Completion

- Completed the separate detailed implementation step 2 without adding online or multiplayer gameplay implementation.
- Added temporary pure C# default catalog data for planets, routes, cargo, and contracts through `DetailedContractCatalogRules.CreateDefaultStepTwoCatalog()`.
- Route distance is a simple numeric input and not a planet coordinate system; it is used as a reward/transport-time proxy.
- Contract rewards now have a traceable pure rule: cargo value 60%, route distance 30%, and fame or association fame 10%.
- Association-specific reward inputs are separated in rules: 10% repair support estimate, safe-streak bonus, and association maintenance fee tracking.
- Association contract visibility is verified as at least 5 for members and exactly 2 for non-members.
- Cargo hold score acceptance rules are verified separately from the current maintenance UI.
- Existing tutorial and post-tutorial UI-compatible contract pair remain stable: `association-local-001` and `private-sample-001`.
- Verification on 2026-06-05:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `102/102`
  - `.\scripts\Run-PlayModeTests.ps1`: passed in open Unity editor, `7/7`
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed in open Unity editor

## Detailed Implementation Step 1 Completion

- Completed the separate detailed implementation step 1 without adding online or multiplayer gameplay implementation.
- Added pure C# runtime skeleton data types for later detailed stages: planets, routes, cargo, contracts, ship rooms, equipment, hostile factions, hostile units, hazards, and a catalog container.
- Added EditMode coverage for the new skeleton models and validation rules.
- Stabilized the PlayMode test runner so open-editor `Run-PlayModeTests.ps1` survives PlayMode domain reload and writes result XML.
- Updated the Phase2 PlayMode baseline to avoid `-nographics` render texture failures and to validate the current interaction target path.
- Extended the harness and MVP playtest checklist documentation with the detailed step 1 regression items.
- Verification on 2026-06-05:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `93/93`
  - `.\scripts\Run-PlayModeTests.ps1`: passed in open Unity editor, `7/7`
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed in open Unity editor
  - `.\scripts\Build-WindowsDev.ps1`: passed

이 문서는 2026-06-04 진행 문서 이후 이어서 작업한 MVP 구현 상태와 17~18단계 범위 확정 사항을 정리한다.

## 사용자 확인 사항

- 17단계는 실제 네트워크 패키지, Steam 로비, 온라인 접속을 도입하지 않는다.
- 이번 범위는 네트워크 독립 인터페이스와 로컬 2인 세션 시뮬레이션까지로 제한한다.
- 이후 본격 멀티플레이는 최대 5인까지 고려하되, 서버 권한 모델과 상호작용 소유권을 상세히 확정한 뒤 진행한다.

## 완료된 구현

### Phase 17: 협동 플레이 기반

- `Bellerophon.Core.Coop` 계층을 추가해 네트워크 SDK와 분리된 협동 세션 인터페이스를 만들었다.
- `LocalCoopSessionAuthority`를 추가해 같은 프로세스 안에서 2명 이상의 플레이어가 같은 세션 스냅샷을 공유하도록 했다.
- 플레이어 위치, 시점, 현재 구역, 상호작용 중인 장치 상태를 동기화 대상 스냅샷으로 분리했다.
- 장치 소유권을 추가해 같은 장치를 두 플레이어가 동시에 조작하려 할 때 권한 측에서 한 명만 수락하도록 했다.
- 세션 상태, 선박 상태, 화물 상태, 운행 상태, 위험 결과, 침입자 상태, CCTV 대상, 엔진 오버클럭 상태를 공유 스냅샷에 포함했다.
- 선박 구역 피해 같은 결과는 로컬 클라이언트가 직접 확정하지 않고 권한 세션의 authoritative 적용 경로로만 반영하도록 했다.
- `IPlatformMultiplayerServices`와 `NullPlatformMultiplayerServices`를 추가해 Steam/온라인 전송 기능이 아직 붙지 않은 상태를 플랫폼 인터페이스 뒤에 명시했다.
- `GameSessionState.WithShipState`를 추가해 권한 세션에서 확정한 선박 상태를 세션 상태에 일관되게 반영할 수 있게 했다.
- Phase 17 EditMode 테스트와 `.\scripts\Run-Phase17CoopFoundationSmoke.ps1` 검증 경로를 추가했다.

### Phase 18: 반복 가능한 플레이테스트 루프

- `docs/MVP_PLAYTEST_CHECKLIST.md`를 추가해 수동 플레이테스트 항목을 시작, 튜토리얼, 첫 정산/정비, 후속 운송, 반복 가능성으로 나누어 정리했다.
- `MvpPlaytestLoopTests`를 추가해 협회 시작, 튜토리얼 운송, 정산/수리, 후속 위험 대응, 파르붐 처치, 로컬 협동 스냅샷, 정산 도착 게이트 회귀를 EditMode에서 검증한다.
- `Phase18MvpPlaytestLoopPlayModeSmoke`와 `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`를 추가해 실제 `CargoRunMvp` PlayMode에서 시작부터 두 번째 정비 준비까지 자동으로 확인한다.
- Phase 18 smoke는 Phase16 HUD/맵/분위기와 Phase17 로컬 협동 스냅샷 경계를 함께 회귀 검증한다.
- 검증 중 `TransportSettlementController`의 정산 도착 게이트가 이전 운송의 정산 표시 상태 때문에 두 번째 운송 도착을 막는 결함을 발견했다. 도착 게이트를 완료 운송 횟수 기준으로 보강해 같은 정산 중복 표시만 막고 다음 운송 정산은 정상 처리되게 수정했다.

## 검증 결과

- `.\scripts\Run-Phase17CoopFoundationSmoke.ps1`: 통과
  - `Players=2; Phase=Transporting; RemotePose=Cockpit; Cctv=CargoHold; EngineRoom=75; OnlineTransport=Deferred`
- `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: 통과
  - `Start=Association; Tutorial=Completed; Repair=1200; FollowUp=association-local-001; Hazard=Avoided+Neutralized; Intruder=Neutralized; Completed=2; NextReady=True`
- `.\scripts\Run-EditModeTests.ps1`: 통과, `88/88`
- `.\scripts\Run-HarnessValidation.ps1`: 통과
- `.\scripts\Build-WindowsDev.ps1`: 통과
- `.\scripts\Run-PlayModeTests.ps1`: 열린 에디터 Test Runner 완료 콜백 타임아웃으로 결과 XML 미생성. Phase18 범위의 씬/UI/입력 통합 검증은 `Run-Phase18MvpPlaytestLoopSmoke.ps1`로 통과했다.

## 다음 단계

- `docs/MVP_IMPLEMENTATION_ORDER.md`의 18단계까지 완료했다.
- 다음 구현 단계는 사용자 확인 후 별도 범위를 확정한다.
- 본격 온라인/최대 5인 멀티플레이는 별도 설계로 다루며, 이번 17단계 결과물은 그 설계를 위한 상태 경계와 검증 가능한 로컬 시뮬레이션 기반으로 둔다.
