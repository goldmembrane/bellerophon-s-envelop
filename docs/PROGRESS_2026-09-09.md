# 2026-09-09 진행 기록

> 현재 상태: **두 구멍 국소 보수만으로 시각 문제 미해결 — 기존 오른손 손가락 형상 재구성 경계에서 미완료**. 후보의 두 경계를 닫고 가중치 뒤집힘을 줄였으나 직접 확대 확인에서 원래 손끝·손등의 날카로운 형상이 남는다. 후보는 공용 원본과 144개 사용처에 반영하지 않았다. 검토 재생은 종료했다. 실제 키보드·마우스 입력 조작은 직접 검증하지 않았다.

## 23:50 직접 비교 미통과와 기존 손가락 형상 보수 범위

- **직접 확인:** 21번의 네 소비품 손바닥/손등 확대에서도 원래 손가락 끝과 손등에 날카롭고 깨져 보이는 윤곽이 남는다. `hand_weight_review/diagnostic_04`~`diagnostic_05`의 동일 구도 기본 자세에서도 원본에 존재하는 형상이다. 새 패치 내부 곡률만 바꾼 후보도 시각적으로 해결하지 못했다.
- 마지막 파지 후보는 다섯 손가락 접촉 조건을 만족했지만, Battery/Converter/Buffer/Nano 면 교차 2/0/2/6개와 주사기 축소 면 1개로 실패했다. 이 수치 개선을 시각 완료로 인정하지 않았다. 18~21은 모두 미통과이며 최종 캡처를 하지 않았다.
- 지금까지는 두 열린 경계 내부에 면을 추가하고 새 내부 정점·가중치·파지만 보수했다. 기존 손가락 끝과 관절 주변 정점/면까지 재구성하는 작업은 두 구멍 국소 보수 범위를 넘어서는 것으로 판단하여 아직 실행하지 않았다. 다음 범위가 승인되면 기존 장갑 스타일/재질, 손목·본·몸체·왼팔·시간표를 보존하면서 오른손 다섯 손가락 형상과 변형용 면 연결을 보수하고 전체 144개 및 두 보정 메시를 일관되게 갱신해야 한다.
- 실제 원본 FBX, 씬, 두 보정 메시, 원본 가중치 JSON 및 네 실제 파지 프로필을 작업 전 백업과 바이트 비교해 모두 동일함을 확인했다. 후보만 변경됐다. 기존 다른 사용자 변경은 보존한다.
- `StopConsumableRiggedSharedMotionReview`와 `StopPlayerHandRigReview`로 검토를 정리했다. `restored_for_finger_shape_scope.log`: Edit Mode, 일시정지/애니메이션 프리뷰/프레임 디버거 꺼짐, 임시 관찰 카메라 0, 관찰 비활성, 입력 설정 미변경. 실제 입력 조작 정상은 미검증이다. `console_for_finger_shape_scope.log`: 콘솔 오류 0, 경고 1. 자동 테스트·빌드·Git 쓰기는 미실행이다.

## 23:30 승인된 국소 면 보수 후보 — 직접 확인 미통과

- 사용자가 두 열린 경계의 면 추가·재연결, 가중치·네 파지 재맞춤, 공용 144개 연결 및 기존 보정 메시 보존/회귀 범위를 묶어 승인했다. 이 범위 안에서는 추가 승인을 요청하지 않는다.
- **직접 확인:** `diagnostic_15`~`diagnostic_17`의 실제 자연 재생 손바닥·손등 확대에서 새 면의 줄무늬 UV는 개선됐지만 손가락 윤곽의 뾰족한 접힘이 남는다. 시각 미통과를 수치 개선으로 상쇄하지 않았다.
- **후보 보수:** 원래 정점·면·UV·본을 유지하면서 두 경계만 연결했다. 처음 삼각분할은 기존 모서리와 중복돼 폐기하고, 기존 모서리 중복을 금지하는 연결로 교체했다. Blender FBX 재임포트에서 해당 경계가 닫힌 것을 확인했다. 기존 두 경계는 별도 분리된 손 조각이 아니라 전신과 연결된 면의 구멍이다.
- 16/17의 손-소품 교차는 Battery/Converter/Buffer/Nano 순으로 2/6/30/19개 및 2/6/25/19개이며 일부 손가락 접촉도 실패했다. 관찰 C#의 작은 면 법선 정규화가 0을 반환하는 오류를 고쳤다. 이전 면 뒤집힘 수는 누락 가능성이 있어 통과 근거로 사용하지 않는다.
- 실제 관찰 자세를 입력으로 하는 후보 가중치 제작 계산이 엄지 고정 패드 경계에서 접힘을 키워 내보내기를 거부했다. 해당 면의 정점·고정 여부·가중치와 목적함수의 미분을 조사 중이다. 불량 후보를 실제 모델에 적용하지 않는다.
- 후속 원인 확인: Adam 갱신 뒤 단순 비례 정규화가 포화된 엄지 끝마디 가중치를 빼앗아 경계 면을 접었다. 가중치 합 제약을 Adam의 대각 계량에서 투영하도록 교체했다. 제작 계산 및 `diagnostic_18` 자연 재생의 면 뒤집힘은 0이나, 직접 확대에서 날카로운 윤곽이 남고 손-소품 교차 12/3/19/11개와 일부 접촉 실패로 여전히 미통과다. 현재 새 스킨 기준 네 파지를 다시 제작 중이다. 원본/후보 FBX와 실제 관찰 메시 연결 검사에서 후보 오른손 경계·동방향 공유 모서리·비다양체 모서리는 모두 0이다.
- 19번 파지 재제작은 교차 12/6/38/17개, 전환기 검지 접촉 실패로 미통과다. 패치의 새 정점 255개만 최대 3mm 곡면화한 20번도 직접 화면을 충분히 개선하지 못했고 완충제에 뒤집힘 1개가 생겨 채택하지 않았다. `patch_curvature_before/`의 면 뒤집힘 없는 후보 FBX/JSON으로 복구했다. 기존 원본 정점은 이 시도에서도 움직이지 않았다.
- `hand_weight_review/diagnostic_04`와 `diagnostic_05` 기본 자세의 원본/후보 비교에서 원래 손끝의 뾰족한 윤곽이 남음을 확인했다. 원본 24본/작업 전 54본/현재 후보의 비인접 손 표면 자체 관통은 읽기 전용 보조 계산에서 0이다. 현재 제작은 관찰 기준을 바꾸지 않고 소품 표면으로부터 0.8mm 여유, 1mm 목표 접촉을 둔 파지를 다시 맞추는 중이다.
- `player.fbx`, 원본 가중치 JSON, 네 실제 파지 프로필, 씬과 기존 보정 메시는 이번 복구 작업에서 변경하지 않았다. 최종 캡처·자동 테스트·빌드·Git 쓰기 미실행. 최종 검토 환경 복구는 작업 종료 시 다시 확인해야 한다.

## 22:50 오른손 원본 지오메트리 결함 확인

- **직접 확인:** `docs/validation/consumable_item_grips_2026-09-09/diagnostic_13` 및 `diagnostic_14`의 네 소비품 파지 확대에 열린 면이 남는다. `hand_weight_review/diagnostic_02`에서 원래 공용 모델과 가중치 후보의 기본 자세를 절단 범위가 넓은 동일 카메라로 비교했고, 원본 손가락 사이에도 뾰족하고 열린 면을 확인했다.
- **보조 확인:** 손 리그 작업 이전 24본 FBX부터 오른손 경계 두 루프(8변·7변)가 존재한다. 이전 국소 분할 후 공용 54본 및 이번 후보는 같은 두 경계를 50변·49변으로 유지한다. 가중치 보정만으로 없는 면을 만들 수 없으므로 국소 면 추가·재연결이 필요하다.
- 가중치/파지 후보 07~12는 변형·면 교차·접촉 이격으로 미통과했다. 실제 네 관찰 자세를 활용한 가중치 제작 후보 13~14는 최대 모서리 비율 약 1.63~1.87배로 줄었으나, 열린 면과 일부 손가락 접촉/면 교차가 남아 미통과다. GPU와 BakeMesh 위치는 최대 약 0.0214mm 차이로 일치한다. 수치 개선을 시각 통과로 사용하지 않았다.
- 지오메트리 보수는 현재의 형상 보존 가중치 승인 범위 밖이므로 실행하지 않았다. 원래 외형·기존 UV/재질·본은 보존하면서 열린 두 경계의 면 연결/국소 분할, 가중치 및 네 파지 재맞춤, 공용 원본·144개·기존 손목/호흡 보정에 일관되게 반영하는 범위가 필요하다.
- 작업 전 `finger_weights_before/`와 실제 원본 FBX, 씬, 원본 가중치 JSON 및 네 실제 파지 프로필의 동일성을 확인했다. 후보는 본 적용하지 않았고 기존 사용자 변경은 보존한다. 검토 재생과 임시 관찰 카메라를 정리했다. 콘솔 오류 0개·경고 58개이며 경고 전수 원인과 실제 입력 조작은 확인하지 않았다. 최종 캡처·자동 테스트·빌드·Git 쓰기는 미실행이다. 상세 근거/실패 이력은 해당 README를 따른다.

## 공용 오른손 가중치 복구 재개

- 공용 원본·파생 보정 스킨·전체 144개 연결과 네 소비품 파지 회귀 확인을 사용자가 승인했다. 첫 후보의 손가락 경계는 직접 확인에서 찢어짐이 남고 보조 모서리 늘어남도 악화돼 미통과다. 기존 공용 원본·실제 대상은 보존하고 별도 후보에서 반복 검토한다.
- 최근접 공간 분류 대신 실제 메시 연결에 따른 손가락 영역과 손바닥 전이를 제작 중이다. 자세·표시 상태를 관찰 중 조작하지 않으며 기존 판정 기준은 유지한다. 상세 이력은 `docs/validation/consumable_item_grips_2026-09-09/README.md`를 따른다.

## 21:52 세 소비품 전용 소품 적용 및 손가락 깨짐 원인 확인 — 미완료

- **직접 확인:** 전환기에 `shield switch.fbx`, 완충제에 `Rapid protective barrier buffer.fbx`, 나노머신에 `Nanomachine therapeutics.fbx`를 연결해 오른손 파지 및 플러그/바늘 하향 접촉을 관찰했다. Battery_Aux_Plug 구성과 공용 몸통·왼팔·5초 시간표는 보존했다. 새 도구 원본 형상과 내장 재질을 유지한 Unity 사본을 사용했다.
- 첫 파지에서 전환기 엄지 벌어짐·중지 이격과 주사기 후방 테두리 침투가 보여 실패로 판정했다. 실제 도구 삼각형 표면과 끝마디 패드, 원래 관절 굽힘 범위를 이용해 파지를 다시 맞췄다. 두 번째 파지의 정점 중심 보조 결과는 실패 0이었지만 이것이 손가락 메시 품질 통과는 아니었다.
- 사용자가 지적한 손가락의 찢어진 면은 절단 범위를 넓힌 관찰 카메라에서도 그대로였다. 변경하지 않은 Battery에도 재현돼 공용 손 스킨 문제로 좁혔다. 이 현상을 앞선 완료 검토에서 충분히 구분하지 못했으므로 손 메시 전체 완료 주장을 유지하지 않는다.
- **보조 원인:** 약지 쪽 정점에 엄지 중간 관절 영향 22.79%가 섞이고 바로 옆 정점에는 없는 등, 서로 다른 손가락의 가중치 전이가 불연속적이다. 4.301mm 모서리가 기존 Battery 파지에서 약 5.970배 늘어난다. UV 중복 정점 분리·손 정점 카메라 절단은 확인되지 않았다. 삼각형 교차는 전환기/완충제/주사기 29/108/53개로 반복돼 미통과다.
- 공용 원본·스킨 가중치 재작업은 이번 승인에서 제외됐으므로 아직 변경하지 않았다. 원본 오른손 가중치, 파생 보정 스킨과 수송자 전체 사용처를 함께 수정·회귀 확인해야 하며 필요한 범위를 묶어 승인받는다. 세 소품의 현재 적용 상태·진단 근거·백업은 보존하고, 완료 최종 캡처는 하지 않았다.
- 상세 근거: `docs/validation/consumable_item_grips_2026-09-09/README.md`, `diagnostic_04/`, `finger_weight_readout.txt`. 작업 전 및 적용별 백업은 `Backups/ConsumableItemGrips_2026-09-09/`에 있다. 재생·일시정지·미리보기·프레임 디버거 해제, 임시 관찰 카메라 0개와 입력 설정 미변경을 확인했다. 마무리 콘솔 오류 0개·경고 1개이며 실제 입력 조작은 직접 검증하지 않았다. 자동 테스트·빌드·Git 쓰기는 실행하지 않았다.

## 21:08 공용 양손 리그·네 소비품 모션 완료 및 검토 환경 복구

- **직접 확인:** `consumable_battery_grip_2026-09-09/diagnostic_04`의 네 대상 사선 전신 상체 시트와 손목·손바닥·엄지 확대를 확인했다. 왼팔은 원래 신전각을 유지한 채 앞으로 올라간다. 오른손은 배터리를 감싸고 손목을 따로 꺾지 않은 채 플러그를 아래로 향하게 하여 왼손목 뒷부분에 접근·접촉·분리한다. 관찰 구간에서 새 몸통/양팔 관통이나 비정상적인 관절 늘어남·균열은 보이지 않았다. 손바닥 전용 확대에서 주변 몸통이 잘리는 부분은 카메라 근·원거리 평면의 절단이며, 몸통 판정은 잘리지 않은 상체 시트로 했다.
- 기존 사용자 영상의 손목 급굴곡 및 `diagnostic_02/03`의 불충분한 파지와 비교했다. 마지막 결함은 손가락 목표 부모 `RightHandTarget`이 리그 스트림에 동기화되지 않아 손가락 첫 관절이 다른 세계 회전을 받던 문제였다. `RigTransform`으로 부모를 동기화했다. 수정 전 표면 관찰에서 네 손가락 이격 20~58mm·엄지 침투 약 3.5mm, 수정 후 다섯 손가락 접촉 및 0.5mm 초과 침투 없음이 확인됐다. 원래 목표 Transform의 값만 읽어 일치한다고 보고했던 중간 결과는 실제 스킨 자세의 근거로 부족했음을 정정한다.
- 공용 원본과 144개 수송자의 54본 연결, 기존 산탄총 55본 보정과 호흡 BlendShape 보존은 20:35 기록을 따른다. 기존 애니메이션 직접 회귀 비교는 대기·걷기·달리기·재장전·두 투척 준비의 대표 6개이며, 모든 기존 클립을 전수 시각 검토했다는 뜻은 아니다.
- **보조 결과:** 네 대상 × 16단계와 관찰 중 모든 개체-프레임을 읽었다. 마지막 진단 3,841개, 단 한 번 실행한 `final/` 3,645개에서 실패 0. 최종 최소 몸통 여유 78.867mm, 팔 중심선 여유 200.935mm, 오른팔 도달 여유 59.017mm, 최대 오른손 목표 오차 0.233mm, 왼팔 신전각 변화 0.016129°였다. 손목 중립·플러그 하향·접촉 구간 오차를 포함한 기존 기준은 완화하지 않았다. 손 표면을 읽은 뒤 관절값을 확인하고 실제 파지 접촉/침투도 보조 기록했다.
- `CaptureConsumableRiggedSharedMotionFinal`은 관련 직접 확인·보조 관찰·콘솔 확인 후 1회 실행했다. 최종 네 대상의 사선 또는 손목/엄지 시트를 다시 확인했다. 승인된 진단 4회와 최종 1회 한도를 지켰다. 기존 5초 시간표의 3.5~5초 분리 유지 및 5→0초 무보간 전환은 그대로다.
- `StopConsumableRiggedSharedMotionReview` 후 `restored_completed_work.log`: Edit Mode, 일시정지/애니메이션 프리뷰/프레임 디버거 꺼짐, GPUBatched 복구, 복구 대기 없음, 임시 루트·씬 밖 검토 모델·검토 카메라 0. 사용자 카메라/입력 코드는 변경하지 않았다. 실제 키보드·마우스 조작 정상 여부는 별도 미검증이다.
- `console_completed_work.log`에서 최종 콘솔 오류 0개·경고 1개를 확인했다. 변경 공백 검사에서는 Unity가 직렬화한 두 보정 메시의 빈 `m_BoneNameHashes` 필드 뒤 공백 2곳이 보고됐으며, 엔진의 출력 형식을 임의로 재작성하지 않았다. 직접 요청되지 않은 자동 테스트·빌드는 실행하지 않았고 Git 커밋·푸시도 하지 않았다. 상세 증거는 두 검증 폴더의 README와 `final/observation.txt`를 따른다.

## 20:47 네 소비품의 공용 손 리그 연결 및 첫 자연 재생 확인

- **직접 확인:** 배터리 대상의 정면·양 사선·측면·손목 확대에서 왼팔이 펼쳐져 올라가고 오른손의 플러그가 하향 접근·접촉·분리하는 것을 확인했다. 오른손 파지가 충분히 보이는 손바닥 구도와 나머지 세 대상의 직접 확인은 추가 진행 중이며, 수치 통과만으로 완료하지 않는다.
- 기존 척추 3본의 분산 회전, 어깨–상완–전완의 힌트 기반 TwoBone IK, 중립 손목을 유지하는 전완 회전, 검토된 공용 프로필의 오른손 15개 손가락 제약을 연결했다. 런타임은 리그 목표만 갱신한다. 원본 본 길이·위치·스케일·메시·머티리얼과 승인된 5초 시간표를 유지했다.
- **보조 관찰:** `consumable_battery_grip_2026-09-09/diagnostic_02`의 4,505개 개체-프레임에서 기존 수치 기준 실패 0, 최소 몸통 여유 83.896mm, 팔 중심선 여유 200.935mm, 오른팔 도달 여유 59.015mm, 최대 오른손 목표 오차 0.209mm였다. 검증 기준은 변경하지 않았다. 진단은 총 4세트 중 2세트 사용했고 최종 캡처는 아직 없다.

## 20:35 공용 손 리그 전체 반영 및 기존 보정 메시 렌더링 복구

- **직접 확인:** 후보 34의 양손 맨손/파지 자연 재생을 확인했다. 전체 반영 전 `shared_before_202250`과 반영 후 `shared_after_203357`의 대기·걷기·달리기·산탄총 재장전·두 투척 준비를 비교했다. 몸체/손 연결과 기존 모션이 유지되며 산탄총 손목 보정도 다시 표시된다. 기존 근접 화면의 일부 각진 손목 표면까지 새로 개선했다는 주장은 하지 않는다.
- 공용 `player.fbx`를 54본 손 리그로 갱신했고 144개 수송자 연결에 적용했다. 산탄총 보정은 추가 본을 보존해 55본, 호흡 보정은 54본과 기존 BlendShape 1개를 유지한다. 기존 컨트롤러·머티리얼·정적 모델의 Animator 유무를 보존했다. 적용 직전 백업은 `Backups/PlayerHandsRig_2026-09-09/global_apply_202427`이다.
- 최초 반영 후 보정 메시 3개 사용처의 몸체가 보이지 않아 실패로 판정했다. 메시 데이터 재구성, GPU 업로드/연결 갱신만으로는 해결되지 않았다. 직접 오류는 기존 영속 Mesh 객체의 데이터 크기/vertex stride 불일치였다. 두 보정 Mesh를 같은 경로/GUID로 새 객체에 저장하고 연결해 렌더링을 복구했다. 의심했던 LOD 인덱스 0은 정상 메시에도 있어 원인으로 확정하지 않는다. 실패 시트·로그는 보존한다.
- 자연 재생 후 임시 검토를 종료했다. 전체 작업 완료·최종 캡처·실제 게임 입력 정상화를 선언하지 않는다. 다음 단계는 새 공용 파지를 사용하는 네 소비품의 몸통·어깨·팔 리깅 모션이다.

## 20:20 파지 후보 확인 및 전체 반영의 가중치 보존 처리 — 미완료

- **직접 확인:** 33번 자연 재생의 손바닥/손등에서 엄지와 네 손가락의 양면 파지를 확인했다. 보조 관찰 프레임의 면 교차·0.5mm 초과 침투는 없고 열 손가락 각각에 3mm 안 접촉 근접 표본이 있었다. 전체 수송자와 네 소비품 모션의 완료는 아니다.
- 전체 적용 전 정점 대응에서 작은 전완 가중치가 Unity 임포트 중 누락되는 것을 확인했다. 후보 FBX 자체는 올바른 값을 보존한다. 실제 FBX 가중치를 임포트 후 복원하는 처리로 작은 영향 430개와 원본 면/UV/가중치 대응을 확인했다. 새 임포트 처리 등록으로 Unity 모델 캐시 재임포트가 발생했지만 비대상 에셋 데이터 변경 코드는 없다.
- 전체 적용 코드는 정적 모델의 Animator 유무를 유지하고, 기존 손목 보정·호흡 BlendShape·컨트롤러·머티리얼을 보존하도록 준비 중이다. 아직 공용 원본·144개 씬 연결·네 소비품 모션을 변경하지 않았다. 갱신된 후보 34를 자연 재생으로 다시 직접 확인한 후 진행한다. 상세 원인과 실패 로그는 손 리그 근거 문서에 기록했다.

## 19:51 손끝·삼각형 면·중간 경로 보완 — 미완료

- **직접 확인:** 28~30은 엄지가 앞면 쪽으로 닫히는 형태가 개선됐고, 30의 맨손 양손 및 오른손 파지를 손바닥·사선에서 확인했다. 31도 손바닥·손등으로 확인했으나 중간 면 교차가 남아 통과하지 못했다.
- 엄지 연결부 보정이 끝마디까지 손바닥 가중치를 남기던 범위를 줄였다. 기존 비손 가중치 차이 0, 원래 손 가중치 총량 차이 최대 3.73e-8과 기존 본 행렬 보존을 제작 보조 검사로 확인했다. 원본 외형·UV·기존 몸통/팔 본은 유지한다.
- **보조 관찰:** 30에서 실제 배터리와 교차하는 유지 자세 손 삼각형 좌/우 14/32개, 중간 오른검지 침투 약 1.185mm를 확인했다. 31은 유지 교차가 사라졌지만 중간 오른손 19개 면 교차가 남았다. 정점상 기준을 만족해도 면 교차가 있으면 통과시키지 않는다. 32는 제작 단계부터 실제 관절 경로의 19개 중간 진행률과 인접 손가락을 함께 고려하도록 수정 중이다.
- 원본·144개 연결·네 실제 소비품 모션은 아직 변경하지 않았다. 브리지 호출 순서 실수와 재시도는 손 리그 근거 문서에 기록했으며, 완료·환경 완전 복구·실제 입력 정상화를 주장하지 않는다. 최종 캡처·금지 검증·빌드·Git 쓰기는 미실행이다.

## 19:35 파지 후보와 팔 전체 도달 제약 조사 — 미완료

- **직접 확인:** 진단 25에서 오른검지의 과도한 접힘과 엄지 파지가 개선됐지만, 해당 배터리 방향으로는 실제 플러그 하향 모션의 도달 조건을 만족하기 어렵다. 진단 26~27은 엄지가 벌어지거나 모서리에만 걸쳐 실패했다. 손바닥·사선의 실제 자연 재생 결과를 기준으로 판정하며 원본과 씬에 적용하지 않았다.
- **보조 확인:** 25·27의 자연 여닫기 24구간에서 0.5mm 초과 침투 정점 표본은 없었지만, 불충분한 파지의 시각 실패를 상쇄하지 않는다. 회전된 물체의 실제 넓은 면을 기준으로 제작 접촉을 계산하도록 보완했다. 원본 본 길이와 중립 손목을 이용한 제작 도달 계산은 어깨·몸통의 분담 필요성을 보여 주며 실제 몸통 모션 통과를 뜻하지 않는다.
- 현재 28번 후보를 자연 재생으로 검토 중이다. 공용 원본·144개 사용처·네 소비품 실제 모션은 변경하지 않았고, 전체 적용·기존 보정 스킨/모션 보존·마지막 환경 복구 확인이 남아 있다. 기존 승인 안에서 진행하며 최종 캡처·금지 검증·빌드·Git 쓰기는 하지 않았다. 상세 수치는 손 리그 근거 문서에 기록했다.

## 19:10 엄지-검지 가중치 경계의 비정상 늘어남 원인 확인

- **직접 확인:** 진단 19~23에서 중간 겹침, 연결부 늘어남 또는 불충분한 파지가 남아 계속 미통과다. 원본과 수송자 144개 연결은 보존했으며 실제 네 소비품 모션은 아직 수정 완료가 아니다.
- **보조 원인:** 약 1.4~1.7mm로 이어진 인접 정점이 엄지 중간 본과 검지 본에 각각 거의 100% 배정돼 약 75~78mm까지 벌어졌다. 후보 제작의 공간상 최근접 본 분류가 만든 불연속 가중치 문제다. 표면 연결 기반 분류·경계 혼합·손바닥 쪽 엄지 연결부 재배분을 추가했다. 기존 정점 위치·UV·팔과 몸통 본·전완 가중치는 보존한다.
- 가중치 경계 혼합 뒤 최악 늘어남이 줄었지만 숫자만으로 완료하지 않는다. 현재는 배터리의 긴 변을 손가락 길이 방향으로 바꾸고 엄지가 좁은 측면을 돌아 잡는 후보를 제작하고 있다. 검증 기준 완화, 금지 검증·빌드·Git 쓰기·최종 캡처는 하지 않았다. 상세 실패 이력과 수치는 손 리그 근거 문서에 보존했다.

## 18:47 양손 파지·엄지 연결부 가중치 보완 진행

- 사용자가 중간 손 진단을 완료까지 반복하는 범위 확대를 승인했다. 기존 진단 증거를 보존하고 횟수 상한만 제거했으며 완료 기준은 유지했다.
- **직접 확인:** 진단 13~18에서 물체 겹침, 엄지 간격 또는 모서리에만 걸치는 파지가 남아 통과하지 못했다. 진단 18의 맨손 굽힘·펼침은 연속적으로 보이지만 배터리 파지·전체 적용 완료의 근거로 확대하지 않는다.
- 후보의 엄지-검지 연결부 가중치를 손바닥과 혼합하고 배터리의 양면을 마주 잡는 방향으로 제작했다. 원본 정점 위치·UV·몸통과 팔의 기존 본은 유지한다. 뼈 끝점 대신 장갑 표면을 쓰는 제작 계산의 좌표 오류를 수정했고 진단 19 자연 재생을 진행 중이다.
- **보조 관찰:** 진단 16의 오른엄지 최근접 거리 13.54mm, 진단 17의 2.60mm/접촉 근접 표본 1개 등은 직접 확인 실패를 상쇄하지 않는다. 진단 15의 왼손바닥 침투 값 일부는 좌표 분류 오류라 품질 판정에 사용하지 않는다. 상세 수치·오류 정정·증거 경로는 `docs/validation/player_hands_rig_2026-09-09/README.md`에 기록했다.
- 공용 원본과 기존 수송자 144개, 네 소비품 실제 모션 연결은 그대로이며 전체 작업은 미완료다. 실제 게임 입력 조작 정상화는 직접 확인하지 않았다. 금지 검증·빌드·Git 쓰기·최종 캡처는 미실행이다.

## 18:03 손 표시 불일치 원인 확인 및 임시 환경 정리

- **직접 확인:** `docs/validation/player_hands_rig_2026-09-09/diagnostic_12/Articulation_view_3.png` 및 손바닥 방향 `Articulation_view_2.png`에서 이전에 겹쳐 남던 펼친 손이 사라지고 자연 재생의 굽힘·유지·펼침이 한 손으로 표시되는 것을 확인했다. 후보 모델·스킨·모션을 수정한 결과가 아니라 이전 검토 잔여물 정리 결과다.
- 오른손 배터리 파지는 `BatteryGrip_view_6.png`, `BatteryGrip_view_8.png`에서 여전히 물체를 충분히 감싸지 못하므로 통과로 판정하지 않았다. 공용 양손 리그 전체 적용, 기존 파생 스킨·기존 애니메이션 보존 확인, 네 소비품의 배터리 접촉 모션은 미완료다.
- **보조 추적:** 실제 카메라 렌더링 전후와 종료 프레임 데이터를 연결하고, 이전의 `scene.IsValid()` 필터를 제거해 씬 없이 남은 이전 검토 모델 8쌍을 찾았다. 현재 후보 표면이 없던 화면 위치에 이전 복제본의 삼각형이 교차했다. 이전의 “중복 렌더러 없음” 및 “임시 카메라 정리 완료” 보고는 잘못됐음을 정정한다. 이 상태에서 수집한 GPU/CPU 비교를 엔진 결함의 확정 또는 배제 근거로 사용하지 않는다.
- 에이전트 소유가 확인된 메모리상의 모델 16개와 화면에 직접 출력하던 검토 카메라 3개만 제거했다. 원본 파일·사용자 객체는 삭제하지 않았으며 모델은 후보 프리팹에서 재생성 가능하다. 정리 코드를 보완했고 검토 시작 전 잔여 임시 객체 검사도 추가했다.
- 종료 후 Edit Mode·일시정지 아님·GPUBatched·임시 루트 0·씬 밖 잔여 검토 모델 0·씬 밖 잔여 검토 카메라 0을 확인했다. 기존 사용자 카메라는 유지했다. 실제 입력 조작 검증은 수행하지 않았으므로 입력 정상화는 확정하지 않는다. 콘솔 오류 0개·경고 1개다.
- 승인된 진단 12세트를 모두 사용했으며 최종 캡처는 실행하지 않았다. 추가 시각 진단이 필요한 작업을 검증 없이 완료하거나 캡처 상한을 임의로 변경하지 않는다. 금지 검증·빌드·Git 쓰기는 실행하지 않았다. 상세 근거는 같은 폴더의 `README.md`, `camera_render_trace.txt`, `orphan_cleanup.txt`, `restored_after_orphan_fix.log`, `console_after_orphan_fix.log`다.

## 네 소비품 공용 내장 리그 모션 재구현

> 아래 최초 구현의 완료 판정은 14:26:41 사용자 영상에서 확인된 왼팔 접힘과 손목 접촉 요구 불충족으로 유효하지 않다. 기존 검토 도구는 `Rebind`와 강제 시점 평가를 사용했으므로 자연 재생의 완료 근거로 사용하지 않는다. 후속 수정 및 검증 상태는 아래 추가 기록을 따른다.

- 대상은 `Battery_Aux_Plug`, `Converter_Shield_Plug`, `Buffer_Shield_Plug`, `Nanomachine_Inject`다.
- 기존 `ForearmPlugShared` 컨트롤러, `ForearmPlugSharedMotion` 런타임, 에디터 도구, 장면 연결과 `docs/validation/consumable_forearm_plug*` 검증 산출물 430개를 전부 파기했다. 2026-09-08 진행문서의 해당 구현과 완료 주장은 폐기 상태로 표시했다.
- 네 장면 대상은 동일한 `player.fbx` 내장 뼈대를 유지한다. 각 대상 루트에 새 `ConsumableRiggedShared.controller`, `RigBuilder`, `ConsumableRiggedSharedRig`과 `ConsumableRiggedSharedMotion`을 연결했다.
- 왼팔은 `LeftArm`-`LeftForeArm`-`LeftHand`의 `TwoBoneIKConstraint`와 `LeftElbowHint`를 사용한다. 오른팔은 몸통을 가로지르는 무리한 2본 도달을 피하기 위해 `RightShoulder`부터 `RightHand`까지의 `ChainIKConstraint`를 사용한다.
- 런타임은 `LeftHandTarget`, `LeftElbowHint`, `RightHandTarget`만 움직인다. 모델 루트, 몸통, 팔 뼈의 localPosition·localScale, 공유 메시, 스킨, 머티리얼은 수정하지 않는다. 새 컨트롤러에는 Transform 애니메이션 곡선이 없다.

### 5초 공용 동작

- `0~0.5초`: 오른팔은 시작 자세를 유지하고 왼팔을 앞·위로 제시한다.
- `0.5~1초`: 왼팔을 유지하며 오른팔을 얼굴 아래 준비 위치로 옮긴다.
- `1~1.2초`: 오른팔을 약간 들어 올린다.
- `1.2~1.7초`: 오른팔을 몸통 앞쪽 경로로 내려 왼팔 전완부에 접촉한다.
- `1.7~3초`: 접촉 자세를 고정한다.
- `3~3.5초`: 오른팔을 위·앞 방향으로 분리한다.
- `3.5~5초`: 분리 자세를 고정하고 5초에 보간 없이 0초 자세로 전환한다.

### 직접 확인과 보조 수치

- 기준 캡처 1회, 진단 캡처 4회와 전체 조건 통과 뒤 최종 캡처 1회를 사용했다. 최종 비교는 `docs/validation/consumable_rigged_shared_motion_2026-09-09/consumable_rigged_shared_motion_final.png`다.
- 1순위 직접 확인에서 네 대상의 0초, 0.5초, 1초, 1.2초, 1.7초, 3초, 3.5초, 4.95초, 5초→0초를 정면, 양 사선과 측면으로 비교했다. 왼팔 제시, 오른팔 준비·상승·접촉·분리와 두 유지 구간이 구분되며 몸통-팔 및 팔-팔 관통, 어깨·겨드랑이·팔꿈치·손목의 늘어남, 비틀림과 균열이 보이지 않았다.
- 2순위 보조 확인에서 네 대상은 동일한 결과를 냈다. 최소 몸통 여유는 `0.053903m`, 접촉 구간 양팔 중심선 간격은 `0.086427m`, 접촉 구간 오른팔 도달 여유는 `0.138759m`, 전체 목표 오차는 `0.000445m` 이하이고 시작점 최소 도달 여유는 허용 오차 안의 `0.044979m`다.
- 검증 중 모델 루트 Transform, 렌더러·메시·머티리얼 서명과 팔을 포함한 주요 뼈의 localPosition·localScale은 바뀌지 않았다.
- 최종 Unity 콘솔 확인 결과는 오류 `0`이다.

## 사용자 영상 피드백: 왼팔 신전 유지·손목 뒤 전완 접촉 수정

- 14:26:41 사용자 영상과 기존 구현을 대조했다. 이전 왼손 목표점은 어깨와 가까워져 팔꿈치를 접었고 오른손 접촉점은 모델 루트 기준 고정 좌표였다.
- 승인 후 왼팔 TwoBone IK와 팔꿈치 힌트를 제거하고 `LeftArm`의 `MultiRotationConstraint`로 변경했다. 원본의 약 176.36° 팔꿈치 신전과 손목 자세를 유지하며 어깨에서 팔 전체를 앞으로 90° 올린다.
- 왼쪽 손목에서 팔꿈치 쪽으로 0.065m 떨어진 전완에 `LeftWristDorsalSocket`을 붙였다. 오른손은 이 소켓을 향해 하강 접촉하고 위로 분리한다. 모델에는 손가락 뼈가 없어 실제 오른손 가중치 메시의 표면 중심 방향으로 손의 방향 기준을 구했다.
- 오른팔은 기존 어깨-손 Chain IK를 유지하되 세 입력 회전 제약으로 매 프레임 동일한 기본 자세를 제공해 누적 비틀림을 방지한다. 5초 단계별 시간, 네 제작 키, 원본 메시·스킨·머티리얼·본 위치·본 스케일·루트 Transform은 보존했다.
- Unity 반영: `Refresh-UnityProject.ps1`, `ApplyConsumableRiggedSharedMotion`, `EnterConsumableRiggedSharedMotionReview`. 적용 초기에 확인된 지역 변수명 충돌과 손가락 뼈 부재 문제를 수정한 뒤 네 대상 반영을 완료했다.
- 검증 도구는 수동 시점/강제 재평가를 제거하고 자연 자동 재생 종료 프레임을 관찰한다. 첫 진단의 프레임 지연과 이웃 모델 중첩은 통과 근거로 사용하지 않았다. 두 번째 진단의 네 대상 × 네 시점 방향 × 핵심 및 중간 16개 포즈를 직접 확인했다. 팔 형태·접촉·분리·유지·시작 복귀가 구분되고 비의도 관통이나 비정상적인 늘어남·균열을 발견하지 않았다.
- 직접 확인 뒤 `InspectConsumableRiggedSharedMotion`으로 896개 개체-프레임의 보조 검증을 통과했다. 왼팔 각도 변화 최대 0.007675°, 오른팔 목표 오차 최대 0.000478m, 도달 여유 최소 0.049742m다. 기존 오른팔 도달 여유 등 기준은 완화하지 않았다.
- 상세 근거: `docs/validation/consumable_extended_arm_wrist_contact_2026-09-09/README.md`. 허용된 진단 네 번 중 두 번을 사용했다. 직접 확인·보조 검증·콘솔 오류 0개 확인 후 최종 캡처 한 번을 요청했다.
- 최종 캡처 완료: `final/`의 네 대상 × 네 방향 시트. 네 대상의 최종 사선 시트를 다시 직접 확인했고 동작·접촉·분리가 유지된다. 최종 4,381개 개체-프레임에서 실패 0개, 최종 콘솔 오류 0개·경고 1개다. 이번 수정의 구현·직접 확인·승인된 보조 검증을 완료했다.
- 사용자 직접 요청이 없는 금지 검증 명령, 프로젝트 빌드, Git 커밋·푸시는 실행하지 않았다.

## 14:50:38 영상 피드백: 배터리 그립과 중립 손목 수정 — 미완료

- 사용자가 지정한 `item model/auxiliary battery.fbx`를 네 대상 모두 오른손에 적용하는 범위를 승인했다. 원본 FBX는 보존하고 `Assets/_Project/Art/Items/Consumable/AuxiliaryBattery/`에 복사했다. 원본 내장 텍스처를 추출하고 금속도·매끄러움 채널을 URP 형식으로 변환했으며, 처음 자동 연결된 타 장비 동명 텍스처를 배터리 자체 텍스처로 바로잡았다.
- 왼팔의 기존 신전 상승과 5초 시간표를 유지했다. 오른팔 시험안은 어깨→팔꿈치 Chain IK 뒤 전완 회전 제약을 적용하고, 원본 오른손목 로컬 회전을 유지한다. 실제 배터리 두 핀 끝의 중심에서 왼팔 소켓까지 경로를 계산한다. 뼈 위치·스케일이나 원본 메시·스킨은 수정하지 않았다.
- **1순위 직접 확인: 불합격.** `diagnostic_01/Battery_Aux_Plug_wrist_oblique_detail.png` 및 측면 확대에서 급격한 손목 꺾임은 줄었으나, 열린 손을 배터리가 관통하고 손가락이 물체를 감싸지 않는다. 정면·사선에서 준비 전환 중 몸통 쪽으로 지나가는 팔도 확인했다. 이를 잡기 성공이나 비관통 통과로 판정하지 않는다.
- **2순위 보조 수치: 불합격.** 4,445개 개체-프레임에서 252개 실패가 기록됐다. 최소 몸통 여유는 -0.013478m이며 0.015m 기준을 통과하지 못했다. 준비 구간 오른팔 팔꿈치 각도는 보고된 실패에서 172.220200°까지 나와 기존 상한 170°를 넘었다. 오른손목 중립 회전 유지와 낮은 목표 오차가 위 직접 확인 실패를 상쇄하지 않는다. 기존 기준은 완화하지 않았다.
- 현재 모델에는 독립 손가락 본이 없다. 설득력 있는 잡기 자세에는 오른손 손가락 리그·가중치 보완 검토가 필요하므로, 해당 범위는 이번 승인에서 제외한 조건에 따라 임의로 수정하지 않고 사용자 확인을 요청한다. 그립 확정 후 접근 경로의 몸통 간섭 및 팔꿈치 각도 문제도 재수정·재확인이 필요하다.
- 진단 세트는 승인된 최대 네 번 중 한 번 사용했다. 최종 캡처와 완료 판정은 하지 않았다. Play Mode를 종료했고, 씬과 코드는 실패한 시험안 상태임을 명시한다. Unity 콘솔 오류 0개·경고 1개 확인. 금지 검증 및 Git 커밋·푸시는 실행하지 않았다.
- 근거: `docs/validation/consumable_battery_grip_2026-09-09/README.md`.

## 수송자 전체 공용 양손·손가락 리그 조사 — 미완료

- 사용자는 네 소비품 전용이 아닌 수송자 공용 모델 전체의 양손·손가락 리그 및 스킨 가중치 보완을 승인했다. 메시 형상·토폴로지 수정은 제외했고, 필요하면 추가 승인을 받는 조건이다.
- 작업 전 공용 `player.fbx`와 meta, 현재 씬, 브리지 파일을 `Backups/PlayerHandsRig_2026-09-09/`에 보존했다. 기존 사용자 변경은 그대로 유지했다.
- 1순위 직접 확인에서 원본 양손의 펼친 형태와 손등·일부 손가락 끝의 날카로운 면을 확인했다. 같은 정점의 연결도에서도 관절을 가로지르는 긴 삼각형과 부족한 관절부 분할이 확인됐다. 자연스러운 다관절 파지를 위해 국소 메시 분할·연결 보완이 필요하다고 판단했다.
- 2순위 보조 조사: 공용 원본은 24개 스킨 본이며 손가락 본이 없다. 확인한 원위부 정점은 모두 Hand 본 가중치 1.0이므로 날카로운 면을 가중치 혼합 오류로 간주하지 않았다. 열린 씬의 확인된 사용처는 원본 메시 141개와 기존 보정 메시 3개, 총 144개 렌더러다.
- 읽기 전용 `PlayerHandRigTools.cs`와 관련 브리지 명령, 기준 1세트 및 보조 토폴로지 진단 1세트만 추가했다. 공용 모델·본·가중치·씬 연결은 아직 변경하지 않았다. 최종 캡처와 완료 판정은 하지 않았다.
- 메시 보완은 기존 승인에서 제외된 작업이므로 임의로 구현하지 않고 추가 승인 대기 상태로 전환한다. 근거: `docs/validation/player_hands_rig_2026-09-09/README.md`.
- 조사 도구 반영 후 콘솔 오류 0개·경고 58개를 확인했다. 경고 전체 원인 조사는 완료하지 않았으며 무경고 상태로 보고하지 않는다. 금지 검증·빌드와 Git 커밋·푸시는 실행하지 않았다.

## 공용 양손 리그 및 국소 관절 메시 후보 제작 — 직접 확인 미통과

- 손가락 관절부 국소 메시 분할·연결 보완 범위를 추가 승인받고 진행했다. 기존 펼친 외형, UV·텍스처, 몸통과 팔 본의 이름·부모·기준 자세를 보존하는 후보를 `Assets/_Project/Art/Player/HandsRig/player_hands_candidate.fbx`에 생성했다. 공용 `player.fbx`와 기존 수송자 144개 연결은 아직 교체하지 않았다.
- **1순위 직접 확인: 미통과.** 두 번째 진단에서 양손의 굽힘·펼침을 확인했으나 배터리 파지에 겹침이 남았다. 세 번째와 네 번째 진단에서도 충분한 파지를 확인하지 못했고, 손바닥 방향에서 펼친 형태와 굽힌 형태가 함께 보이는 문제가 남았다. 중복 렌더러는 확인되지 않았으나 스킨·실제 렌더·관찰 경로 중 원인은 아직 확정하지 못했다. 원본 모델 결함이나 Unity 오류로 단정하지 않는다.
- **2순위 보조 확인:** 양손에 손가락 본 30개를 추가해 후보 스킨은 총 54개 본이다. 기존 본 행렬 차이는 Blender에서 0, Unity 기존 노드 위치 차이 최대 0.00000029m·회전 차이 0°다. 원본 정점 4,990개가 위치를 유지했고 국소 분할 후 Blender 13,103개·Unity 17,230개 정점이다. 자연 재생에서 새 본에 가중된 정점이 움직임을 확인했으나, 이를 시각 실패의 통과 근거로 사용하지 않았다.
- 후보 제작 스크립트, `PlayerHandRigTools.cs`, 자연 재생 관찰 전용 `PlayerHandRigReviewObserver.cs`, 임시 확인용 클립·컨트롤러·프리팹을 추가했다. 후보 파지 자세는 임시 제작 오브젝트에서 계산하고, 실제 관찰 대상의 시간·본·스킨·표시 상태를 강제로 바꾸지 않았다.
- Unity 반영은 `Refresh-UnityProject.ps1`, `PreparePlayerHandRigReview`, `EnterPlayerHandRigReview`와 관련 읽기 전용 확인 명령을 사용했다. 준비 중 Animator 연결 오류는 수정했다. `StopPlayerHandRigReview`로 재생을 종료했고 마지막 콘솔은 오류 0개·경고 3개다.
- 승인된 기준 1세트·진단 총 4세트를 모두 사용했다. 최종 캡처 및 전체 수송자 적용은 하지 않았다. 추가 시각 진단 승인 전에는 캡처를 늘리지 않는다. 기존 파생 메시 보존 적용과 기존 애니메이션 회귀 확인도 남아 있다.
- 상세 근거와 현재 후보 상태: `docs/validation/player_hands_rig_2026-09-09/README.md`. 직접 요청 없는 금지 검증 명령, 프로젝트 빌드, Git 커밋·푸시는 실행하지 않았다.

## 양손 리그 계속 진행과 검토용 화면 복구 — 직접 확인 미통과

- 사용자는 승인 범위 안에서 추가 승인을 반복 요청하지 말고 계속 진행하며, 작업 뒤 검토용 화면을 원래 상태로 복구하도록 지시했다.
- **1순위 직접 확인:** 추가 진단 05~07의 자연 재생에서 펼친 손가락 형태가 남고 배터리를 충분히 감싸지 못하는 상태를 확인했다. 공용 모델 적용 및 완료 판정을 하지 않았다.
- 관찰 카메라를 수동 렌더 호출에서 정상 프레임 RenderTexture 출력으로 바꿨고, 후보 Generic Avatar 연결 및 동일 데이터의 별도 후보 스킨 구성으로 원인을 분리했다. 세 변경으로도 시각 문제는 해결되지 않았다. 원본 `player.fbx`, 기존 수송자 144개 및 기존 손목·호흡 보정 메시 연결은 보존했다.
- **2순위 보조 확인:** 중복 렌더러·본 순서 불일치를 찾지 못했다. 현재·이전 GPU 버퍼와 CPU 스킨은 약 0.000006584m 이하 차이로 일치하지만 화면의 일부 픽셀과 메시 광선 교차 결과는 불일치했다. 이를 시각 통과나 Unity 오류 확정으로 사용하지 않는다. 가중치를 추측으로 재조정하지 않고 렌더 파이프라인·패키지·프로젝트 그래픽 설정의 추가 읽기 조사 범위를 확인한다.
- 검토 카메라가 게임 화면을 덮지 않도록 전용 RenderTexture로 출력하고, 전용 임시 루트·카메라·텍스처·관찰 콜백을 종료 때 정리하도록 했다. `restored_view_state.log`에서 Edit Mode, 일시정지 아님, 임시 검토 루트 0개와 기존 카메라 상태 보존을 확인했다. 실제 키보드·마우스 게임 조작을 수행한 것은 아니므로 조작 검증 완료라고 주장하지 않는다.
- 진단은 총 7세트 사용했고 현재 허용된 상한 8세트 중 1세트가 남았다. 최종 캡처는 사용하지 않았다. 전체 공용 리그 적용, 파생 메시 보존 반영, 기존 모션 회귀, 네 소비품의 실제 파지·접근·비관통 확인은 남아 있다.
- 종료 정리 코드의 리프레시 후 콘솔 오류 0개·경고 58개를 확인했다. 경고 원인 확인은 미완료다. 관련 `git diff --check`는 종료 코드 0이며 금지 검증·빌드·Git 쓰기는 실행하지 않았다.

## 17:07 렌더 설정·패키지 읽기 조사 — 원인 미확정

- 사용자가 추가 읽기 범위를 승인해 프로젝트 그래픽·품질 설정, 적용 URP 설정과 렌더 패키지 및 공식 자료를 조사했다. 설정·패키지·셰이더는 변경하지 않았다.
- **1순위 직접 확인:** 진단 07 정면 시트의 펼친 손가락 잔존과 기존 파지 실패 판정을 유지했다. 새 진단/최종 캡처는 실행하지 않았고, 공용 원본 및 수송자 144개 연결도 교체하지 않았다.
- **2순위 보조 조사:** 현재 실행은 GPUBatched/정점당 본 4개다. 렌더러·Transform 행렬은 같고 정적 배칭이나 별도 MaterialPropertyBlock은 없다. 선형 스킨과 BakeMesh 최대 차이는 약 0.000008764m, GPU 스킨과 BakeMesh 차이는 약 0.000006584m이며 실제 그리는 GPU 삼각형 인덱스는 CPU 데이터와 일치한다. 인덱스 버퍼의 여유 항목 1개는 그리기 범위에 포함하지 않아 비교했다.
- 카메라 클리핑 필터를 제외해도 일부 손가락색 픽셀과 현재 후보 메시 표면이 불일치한다. 읽기 조사만으로 원인을 확정하지 못했으며 엔진 오류나 가중치 오류로 단정하지 않는다. 상세 근거는 `docs/validation/player_hands_rig_2026-09-09/README.md`의 최신 조사 절과 `motion_skin.txt`다.
- 다음 원인 분리에 필요한 스키닝 실행 방식의 임시 GPU/CPU 비교는 기존의 설정 읽기 승인 범위를 벗어나므로 실행하지 않고 범위 승인을 요청한다. 검증 기준은 완화하지 않았다. 원래 네 소비품 공용 모션·전체 리그 적용 작업은 여전히 미완료다.
- 검토 세션을 종료해 Edit Mode·임시 검토 루트 0개 및 기존 카메라 상태 일치를 확인했다. 입력을 직접 조작한 것은 아니다. 마지막 콘솔은 오류 0개·경고 1개이며 `git diff --check`는 종료 코드 0이다. 금지 검증·빌드·Git 쓰기는 실행하지 않았다.

## 승인된 GPU/CPU 비교와 후보 렌더러 재구성 — 미해결

- 사용자가 진행을 승인해 스키닝 방식을 임시 GPU 비배치 및 CPU로 비교하고 종료 시 원래 GPUBatched로 복구했다. 설정의 다른 필드, 패키지·셰이더는 변경하지 않았다.
- **1순위 직접 확인:** 진단 08(GPU), 09(CPU)의 각 12구간 자연 재생에서 펼친 손가락 형태 잔존과 불충분한 파지가 반복됐다. 같은 본·메시·재질을 새 스킨 렌더러에 연결한 후보의 진단 10에서도 해결되지 않았다. 공용 원본이나 기존 144개 수송자 연결에 적용하지 않았다.
- 진단 11의 정적 기본 자세 비교에서는 임포트 모델과 검토 프리팹 모두 펼친 손이 한 겹으로 보인다. 자연 재생 결과와 차이가 있으나, 정적 결과를 애니메이션 성공으로 간주하지 않는다.
- **2순위 보조 조사:** 직접 GPU 텍스처 readback과 기존 이미지 읽기는 픽셀 불일치 0개였다. 접은 구간에 생성한 새 관찰 텍스처 및 독립적인 화면 좌표 삼각형 계산에서도 대표 픽셀과 현재 스킨 표면의 불일치가 남았다. 원인을 아직 해결하지 못했다. `docs/validation/player_hands_rig_2026-09-09/README.md`에 상세 근거를 기록했다.
- 임시 설정은 SessionState에 보관한 원래 값으로 자동 복구된다. 마지막 상태는 Edit Mode, 일시정지 아님, GPUBatched, 복구 대기 없음, 프레임 디버거/애니메이션 미리보기 꺼짐, 임시 검토 루트 0개다. 기존 카메라 상태를 유지했으며 실제 입력 조작은 확인하지 않았다.
- 진단은 승인 상한 12세트 중 11세트 사용했고 최종 캡처는 미실행이다. 남은 1세트를 근거 없는 반복 조정에 쓰지 않았다. 전체 리그 반영·파생 메시 보존·모션 회귀·네 소비품 실제 파지와 비관통 동작 구현은 미완료다. 원인 미확정 상태를 성공으로 바꾸거나 검증을 완화하지 않았다.
- 상태 조회 코드의 API 이름 공간 오류는 수정했고 마지막 콘솔은 오류 0개·경고 58개다. 경고 전체 원인 확인은 미완료다. 관련 `git diff --check`는 종료 코드 0이며 금지 검증·빌드·Git 커밋·푸시는 실행하지 않았다.
