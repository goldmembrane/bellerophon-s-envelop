# Progress 2026-06-07

이 문서는 2026-06-07 작업 종료 시점의 handoff 기록이다. 다음 작업자는 먼저 `docs/PROGRESS_2026-06-02.md`를 읽고, 그 포인터를 따라 이 문서까지 확인한 뒤 이어서 진행한다.

## 최신 상태 요약

- 상세 구현 4~12단계 완료.
- 다음 구현 대상은 `docs/DETAILED_IMPLEMENTATION_PLAN.md`의 13단계, `씨앗체 상세 구현`.
- 13단계는 구현 전에 먼저 사용자에게 상세 범위를 설명해야 한다.
- 온라인/멀티플레이, Steam 로비, 실제 네트워크 기능은 아직 시작하지 않는다.
- `은폐 블랙홀`은 사용자 확인에 따라 후반 위험 확장으로 보류한다.
- 씨앗체 두 번째 개체 명칭은 사용자 확인으로 `푸가`로 확정했다. `docs/GAME_DESIGN_SOURCE.txt`와 `docs/DETAILED_IMPLEMENTATION_PLAN.md`도 `푸가` 기준으로 정리했다.

## 원본 기획서 관련 확인 사항

- 2026-06-07에 `C:\Users\gus68\OneDrive\문서\spce.txt`의 최신 기획서를 `docs/GAME_DESIGN_SOURCE.txt`로 교체 반영했다.
- 외부 위험 유명세 조건은 최신 원본 기준:
  - `외계 생명체` 출몰 구역: 유명세 900 이상
  - `화물 자유 연대` 출몰 지역: 유명세 1800 이상
  - `우주 해적` 출몰 지역: 유명세 3000 이상
  - `은폐 블랙홀`: 유명세 4500 이상, 구현은 후반으로 보류
- 유명세 조건은 세션 내 unlock 방식이다. 한 번 조건을 돌파하면 이후 유명세가 내려가도 해당 세션에서는 계속 출몰 가능하며, 게임오버 초기화/새 세션에서만 잠긴다.
- 통제실 `정화`는 선택한 해당 구역만 대상이다.
- 특수 장비 `복도 정화 장치`는 복도 전체 대상이며, 통제실 내부 정화와 별도 시스템으로 남겨둔다.
- 통제실 내부 정화는 해당 구역 안의 적대 개체뿐 아니라 플레이어도 피해를 받는다.

## 오늘 완료한 주요 구현

### 상세 9단계 재정렬 및 상태 이상

- 원본 기획서와 대조 후 상태 이상 명칭을 원본 기준으로 통일했다.
- 사용하는 상태 이상 명칭은 `정지`, `화상`, `출혈`, `탈진`, `피로`, `현기증`, `혼란`.
- 원본에 없는 `감전`, `둔화`, `환각`, `공포` 같은 명칭은 사용하지 않는다.
- `CombatStatusModels`, `PlayerCombatModels` 기반으로 플레이어 전투/상태 이상/장비 효과를 정리했다.

### 상세 10단계

- 조종실 수동 운행 부스터와 외부 위험 지속시간 감소를 연결했다.
- 무기실 수동/자동 포탑 상태를 분리했다.
- 수동 포탑 탄창 강화 수치와 플라즈마 포 기능을 연결했다.
- 통제실 CCTV, 세로 방 목록, 가로 선박 레이아웃 모드를 정리했다.
- 통제실 내부 정화는 세로 방 목록에서 선택한 단일 구역에만 적용되도록 구현했다.
- 내부 정화는 총 30초 동안 500 피해를 주며, 해당 구역 안의 플레이어도 피해를 받는다.
- 내부 정화 자체는 방 내구도를 손상시키지 않는다.
- ESC로 통제실, 동력실, 비품실 상호작용 패널이 정상 종료되도록 수정했다.

### 상세 11단계

- 외부 위험을 원본 기준으로 확장했다.
- 구현된 위험:
  - 소행성 지대 小
  - 소행성 지대 大
  - 화물 자유 연대 출몰 지역
  - 우주 해적 출몰 지역
  - 외계 생명체 출몰 구역
- `은폐 블랙홀`은 원본 수치와 조건만 문서화하고 런타임 시작/검증 시작을 막아둔 상태다.
- 외부 위험 발생은 운송 시작 즉시 강제 발생이 아니라 원본의 주기/확률 기반 체크로 동작한다.
- 화물 자유 연대와 외계 생명체는 boarding event만 생성하고, 원본에 없는 직접 방 손상 수치는 만들지 않았다.
- 우주 해적은 boarding event와 포격 방 손상을 함께 처리한다.
- `TransportHazardUnlockState`를 `GameSessionState`에 저장해 세션 내 유명세 unlock을 유지한다.

### 상세 12단계

- 침입자 AI 공통 프레임워크를 완성했다.
- `IntruderDefinition`과 `IntruderEntityState`가 이동 방식과 지휘 가능 여부를 보존한다.
- 침입자 목표 유형은 원본 큰 분류 기준:
  - 운송 화물 공격
  - 화물선 공간 점유
  - 플레이어 공격
  - 화물선 파괴
- Phase16 선박 맵의 10개 복도 연결을 침입자 경로 평가에 사용한다.
- 구역 봉쇄, 통제실 복도 폐쇄, 엔진 암전, CCTV, 침입 감지, 내부 침입자 억제 상태가 `IntruderRules` 평가에 들어간다.
- 세력 관계 모델을 원본 기준으로 구현했다:
  - `적대`
  - `경쟁`
  - `동맹`
  - `유대`
  - `지휘`
- 관계 모델은 원형 표시 종류와 friendly-fire 체력/상태이상 적용 여부를 함께 표현한다.
- 기존 Parvum은 새 공통 경로 모델 위에서 동작하도록 연결했다.
- 봉쇄 때문에 목표 방에 도달할 수 없는 경우 Parvum이 봉쇄된 목표 방을 순간이동해 손상시키지 않고 현재 위치 기준으로 압력을 준다.

## 검증 상태

2026-06-07 작업 종료 전 통과한 검증:

- `.\scripts\Run-HarnessValidation.ps1`: passed
- `.\scripts\Run-EditModeTests.ps1`: passed
- `.\scripts\Run-Phase13IntruderFrameworkSmoke.ps1`: passed
- `.\scripts\Run-Phase14ParvumIntruderSmoke.ps1`: passed
- `.\scripts\Run-Phase1To18Smokes.ps1`: passed, Phase 1 through Phase 18 in order
- `.\scripts\Build-WindowsDev.ps1`: passed
- `git diff --check`: passed with CRLF warnings only

주의:

- 12단계 검증 중 한 번 Unity 에디터가 이미 수정된 컴파일 에러 플래그를 계속 붙잡았다. Unity 에디터를 재시작한 뒤 전체 검증은 통과했다.
- generic `.\scripts\Run-PlayModeTests.ps1`의 open-editor Unity Test Runner callback timeout 문제는 기존 알려진 이슈로 남아 있다. 기능별 PlayMode smoke와 Phase 1~18 sweep은 통과했다.

## 다음 작업

다음은 상세 구현 13단계 `씨앗체 상세 구현`이다. 아직 구현하지 말고, 먼저 사용자에게 구현 범위를 설명한다.

13단계에서 특히 확인할 점:

- 씨앗체 명칭은 `파르붐`, `푸가`, `롱가 아르마`, `테르고`, `우르제레`, `소치에타스`, `몬스트룸`, `미메시스`.
- 이미 `푸가` 명칭은 확정됐으므로 다시 `무스카`로 되돌리지 않는다.
- 13단계는 12단계 공통 프레임워크 위에 씨앗체의 금속/화물선 상호작용, 관계, 대표 행동을 올리는 작업이다.
- 개별 외형/모션은 `docs/ENEMY_REFERENCES.md`와 원본 이미지 레퍼런스를 확인한 뒤 진행한다.
- 원본에 없는 능력치, 상태 이상, 행동 수치는 임의로 만들지 않는다.
