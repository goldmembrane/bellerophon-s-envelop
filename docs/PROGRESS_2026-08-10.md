# 진행 기록 - 2026-08-10

## 이슈판트 8번 손 내림 뒤 발검 전환 정지 제거

상태: `ISPANT_SLOT8_TO_SLOT4_CONTINUOUS_TRANSITION_COMPLETE`

- 제공 영상 `2026-08-10 23-50-15.mp4`를 원본 연속 프레임과 전환부 확대 이미지로
  직접 확인했습니다. 이전 수정은 발검 시작 프레임만 옮겼고, 0.3초 중간 동작에
  사용한 `smoothstep`이 시작과 끝 속도를 모두 0으로 만들어 정지 지점만 다른 접점으로
  이동시킨 것이 원인이었습니다.
- 4번 발검 원본과 발검 이어재생 시작 프레임은 더 변경하지 않았습니다. 8번의 손 내림
  마지막 자세와 4번 발검 시작 자세 사이 0.3초 Transform 보간만 선형 연속 이동으로
  바꿔, 양쪽 접점에서 인위적으로 감속해 멈추던 동작을 제거했습니다.
- 8번 원본 구간은 `3.4초`, 별도 손 내림 유지 시간은 `0초`, 중간 동작은 `0.3초`,
  4번 발검 이어재생은 원본 속도를 유지한 `1.2초`이며 전체 반복 주기는 `4.9초`입니다.
- 저장본 검사는 `PASS`입니다. 외형·머스켓·장검 자산과 배치, Y축 연결, 다른 11개 슬롯,
  다른 씬 루트는 변경되지 않았고 씬 저장 뒤 검사에서 `SceneChanged=False`를 확인했습니다.
- 검사 통과 후 최종 캡처를 한 번 생성했습니다. 전면 확대본에서
  `손 내림 끝 → 중간 시작 → 중간 1/6~5/6 → 중간 끝 → 발검 시작 → 발검 첫째·둘째 프레임`
  순서로 직접 대조했으며, 동일 시각인 상태 경계 한 장을 제외한 모든 간격에서 오른팔이
  왼쪽 허리와 발검 방향으로 계속 이동해 여러 프레임 유지되는 정지 구간이 보이지 않습니다.
- Unity 반영 명령: `RefreshAssets`, `ApplyIspant08ChangingToSwordReplacement`
- 저장본 검사·최종 캡처 명령: `InspectIspant08ChangingToSwordReplacement`,
  `CaptureIspant08ChangingToSwordReview`
- 검사 기록:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 최종 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`,
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ContinuousTransition_Front_Close.png`
- 입력 영상 프레임 분석:
  `docs/validation/ispant_changing_to_sword_2026-08-10/source_video_235015_analysis/`
- 실행하지 않은 항목: 하네스 검증(`Run-HarnessValidation.ps1`) 및 관련 실행·재실행·
  조사·로그·문서·생성물, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`,
  `Build-WindowsDev.ps1`, Ensure·Validate·Smoke 계열 명령, Unity 재시작·복구·재생성,
  Git 변경·커밋·이력 조작 작업.

## 이슈판트 8번 허리 정지 구간 재확인 및 실제 발검 시작점 연결

상태: `SUPERSEDED_INCORRECT_COMPLETION_BY_CONTINUOUS_TRANSITION_FIX`

- 후속 제공 영상 `2026-08-10 23-50-15.mp4`에서 정지 구간이 다른 접점으로 이동한 것이
  확인되어 이 절의 완료 판정은 무효입니다. 위 연속 전환 수정 결과가 현재 상태입니다.

- 제공 영상 `2026-08-10 23-41-28.mp4`를 전체 5fps와 전환부 20fps·25fps 연속
  프레임으로 직접 확인했습니다. 기존 수정 뒤에도 발검 상태가 시작된 다음 오른손과
  장검이 왼쪽 허리에서 약 `0.36초` 더 머물렀고, 이전 완료 판정은 잘못됐습니다.
- 입력 영상의 `3.96`, `4.00`, `4.04초` 프레임에서는 허리 자세가 유지되고
  `4.08초` 프레임에서 장검이 처음 움직이는 것을 확대 이미지로 확인했습니다. 현재
  시퀀스 시작 시점과 25fps 원본을 대조해 4번 원본 `14프레임`(`0.56초`)을 마지막
  허리 자세, `15프레임`을 첫 발검 변화로 확정했습니다.
- 4번 원본 애니메이션 파일은 수정하지 않았습니다. 8번 전용 이어재생본만 원본
  14프레임부터 시작하게 하고, `0.3초` 중간 동작의 끝도 원본 14프레임의 정확한
  리그 자세로 연결했습니다. 따라서 중간 동작 직후 다음 1프레임부터 발검 변화가
  시작됩니다.
- 8번 원본 구간은 `3.4초`, 중간 동작은 `0.3초`, 발검 이어재생은 원본 속도를
  유지한 `1.2초`이며 전체 반복 주기는 `4.9초`입니다.
- 저장본 검사는 `PASS`입니다. 중간 동작 양 끝 자세 오차는 `0.000011444`, Hips Y
  이동량은 `0.000000055`, 4번 원본 14프레임 이후 대비 자세 오차는
  `0.000011444`입니다. 외형·머스켓·장검 자산 오차는 `0`이고 다른 11개 슬롯과
  다른 씬 루트 변경은 모두 `False`입니다.
- 검사 통과 후 최종 캡처를 생성해 원본 해상도로 직접 확인했습니다. 캡처는
  `중간 시작 → 중간 50% → 중간 끝 → 발검 시작 → 첫 변화 프레임 → 두 번째 변화
  프레임 → 발검 50%`를 연속 열로 보여주며, 허리 도달 뒤 별도 정지 없이 팔과 장검이
  계속 움직이고 Y축 급변·장비 중복·외형 변형은 보이지 않습니다.
- Unity 반영 명령: `RefreshAssets`, `ApplyIspant08ChangingToSwordReplacement`
- 저장본 검사·최종 캡처 명령: `InspectIspant08ChangingToSwordReplacement`,
  `CaptureIspant08ChangingToSwordReview`
- 검사 기록:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 최종 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`,
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_BridgeToDraw_Frames14_16_DirectReview.png`
- 입력 영상 프레임 분석:
  `docs/validation/ispant_changing_to_sword_2026-08-10/source_video_234128_analysis/`
- 실행하지 않은 항목: 하네스 검증(`Run-HarnessValidation.ps1`) 및 관련 실행·재실행·
  조사·로그·문서·생성물, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`,
  `Build-WindowsDev.ps1`, Ensure·Validate·Smoke 계열 명령, Unity 재시작·복구·재생성,
  Git 작업.

## 이슈판트 8번 허리 정지 없이 4번 발검으로 즉시 연결

상태: `SUPERSEDED_INCORRECT_COMPLETION_BY_FRAME14_FIX`

- 후속 제공 영상 `2026-08-10 23-41-28.mp4`에서 허리 정지 구간이 그대로 남은 것이
  확인되어 이 절의 완료 판정은 무효입니다. 위 `FRAME14` 수정 결과가 현재 상태입니다.

- 제공 영상 `2026-08-10 23-14-57.mp4`의 전환부를 20fps 연속 프레임으로 직접
  확인했습니다. 8번의 `0.3초` 중간 동작이 오른손을 왼쪽 허리까지 가져간 뒤, 4번
  원본 앞쪽에서도 같은 손-허리 접근 동작이 중복되어 발검 전에 멈춘 것처럼 보였습니다.
- 4번 원본 파일은 수정하지 않았습니다. 영상에서 장검이 허리를 벗어나기 시작하는
  원본 `5프레임`(`25fps`, `0.2초`)을 8번 전용 이어재생 시작점으로 사용했습니다.
  중간 동작의 끝 자세도 임의 생성 없이 이 원본 5프레임의 정확한 리그 자세를 향하도록
  다시 생성했습니다.
- 8번 원본 구간 `3.4초`가 끝나면 별도 유지 없이 `0.3초` 중간 동작이 바로 나오고,
  그 직후 원본 속도를 유지한 발검 이어재생 `1.56초`가 시작됩니다. 전체 반복 주기는
  `5.26초`입니다.
- 저장본 독립 검사는 `PASS`입니다. 중간 동작 양 끝 자세 오차는 `0.000011444`,
  Hips Y 이동량은 `0`, 4번 원본 5프레임 이후 대비 자세 오차는 `0.000000246`입니다.
  손·등 머스켓 표시 중복은 없고 장비 장착 오차와 외형 자산 오차는 `0`, 다른 11개
  이슈판트 슬롯과 다른 씬 루트 변경은 모두 `False`입니다.
- 검사 통과 뒤 최종 전·후면 캡처를 한 번 생성해 원본 해상도로 직접 확인했습니다.
  손 내리기 종료, 중간 동작 시작·중간·끝, 원본 5프레임 발검 시작, 발검 진행·종료가
  순서대로 연결되고 Y축 급변, 장비 중복, 외형 변형은 보이지 않습니다.
- Unity 반영 명령: `RefreshAssets`, `ApplyIspant08ChangingToSwordReplacement`
- 저장본 검사·최종 캡처 명령: `InspectIspant08ChangingToSwordReplacement`,
  `CaptureIspant08ChangingToSwordReview`
- 검사 기록:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 최종 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`
- 입력 영상 프레임 분석:
  `docs/validation/ispant_changing_to_sword_2026-08-10/source_video_231457_analysis/`
- 실행하지 않은 항목: 하네스 검증(`Run-HarnessValidation.ps1`) 및 관련 실행·재실행·
  조사·로그·문서·생성물, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`,
  `Build-WindowsDev.ps1`, Ensure·Validate·Smoke 계열 명령, Unity 재시작·복구·재생성,
  Git 작업.

## 이슈판트 8번 손 내린 자세 0.5초 유지 제거

상태: `ISPANT_SLOT8_LOWERED_HOLD_REMOVED_COMPLETE`

- 제공 영상 `2026-08-10 23-02-39.mp4`의 8번 전환부를 직접 확인했습니다. 오른팔을
  내린 동일 자세가 여러 프레임 동안 유지된 뒤 중간 동작이 시작되는 원인은 슬롯 8
  클립 끝에 별도로 덧붙였던 `LoweredPoseHoldDuration=0.5초` 구간이었습니다.
- 사용자 지시대로 별도 유지시간을 `0초`로 변경하고, 손 내리기 종료 자세를 추가로
  복제해 유지하던 커브 생성 처리를 제거했습니다. 이제 원본 `180~230프레임` 손 내리기
  구간이 기존 지정값 `0.4초`로 끝나는 즉시 `0.3초` 중간 동작이 시작됩니다.
- 슬롯 8 클립 길이는 `3.9초`에서 `3.4초`로 줄었고, 이어지는 중간 동작 `0.3초`와
  4번 발검 `1.76초`를 포함한 전체 반복 주기는 `5.46초`입니다.
- 저장본 독립 검사는 `PASS`입니다. `LoweredPoseHoldSeconds=0`,
  `LoweredPoseHoldRemoved=True`이며, 중간 동작 양 끝 자세 오차는 `0.000011444`,
  중간 동작의 Hips Y 이동량과 프레임별 최대 Y 변화는 모두 `0`입니다. 높이 보정을
  제외한 4번 원본 대비 자세 오차는 `0.000000246`입니다. 손·등 머스켓 장착과 장검
  표시 순서는 유지됐고 다른 이슈판트 슬롯 및 다른 씬 루트 변경은 모두 `False`입니다.
- 최종 11열 전·후면 캡처와 `손 내리기 종료 → 중간 시작 → 중간 50% → 중간 끝 →
  발검 시작` 확대본을 원본 해상도로 직접 확인했습니다. 손 내리기 종료와 중간 시작은
  같은 시각·같은 자세이고 바로 다음 중간 지점에서 팔과 몸통이 움직이므로 별도의
  0.5초 정지 구간이 없습니다. Y축 급변, 팔 자세 절단, 장비 중복도 보이지 않습니다.
- Unity 반영 명령: `RefreshAssets`, `ApplyIspant08ChangingToSwordReplacement`
- 저장본 검사·최종 캡처 명령: `InspectIspant08ChangingToSwordReplacement`,
  `CaptureIspant08ChangingToSwordReview`
- 검사 기록:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 최종 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`,
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ImmediateBridge_DirectReview.png`
- 입력 영상 프레임 분석:
  `docs/validation/ispant_changing_to_sword_2026-08-10/source_video_230239_analysis/`
- 실행하지 않은 항목: 하네스 검증(`Run-HarnessValidation.ps1`) 및 관련 실행·재실행·
  조사·로그·문서·생성물, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`,
  `Build-WindowsDev.ps1`, Ensure·Validate·Smoke 계열 명령, Unity 재시작·복구·재생성,
  Git 작업.

## 이슈판트 8번에서 4번 발검으로 넘어가는 Y축·중간 동작 교정

상태: `ISPANT_SLOT8_TO_SLOT4_DRAW_BRIDGE_0_3S_COMPLETE`

- 사용자 지정값대로 8번의 손 내린 자세 `0.5초` 유지 뒤, 현재 4번 발검 시작자세로
  넘어가는 중간 동작을 정확히 `0.3초`로 추가했습니다. 새 자세를 추측하거나 만들지
  않고 8번 유지 끝자세와 4번 발검 시작자세의 실제 리그 Transform만 60fps로
  완만하게 보간합니다.
- 기존 영상에서는 두 상태를 전환시간 `0초`로 직접 연결해 한 프레임에서 몸 전체 자세와
  기준 높이가 함께 바뀌었습니다. 실제 끝점 대조 결과 Hips 기준 높이 차이는
  `0.5051034m`였습니다. 4번 발검의 전체 골격 높이에 `-0.5051034m`의 일정 오프셋을
  적용해 시작 높이를 8번 유지 끝높이에 맞췄고, 4번의 본 회전·상대 움직임·재생속도·
  `1.76초` 길이는 유지했습니다.
- Animator는 `Ispant_ChangingToSword_Mixamo` →
  `Ispant_08_To_04_DrawSword_Bridge` → `Ispant_04_DrawSword` → 처음 상태의 세 상태
  반복으로 구성했습니다. 8번 `3.9초`, 중간 동작 `0.3초`, 4번 `1.76초`로 전체
  반복 주기는 `5.96초`입니다.
- 독립 저장본 검사는 `PASS`입니다. 중간 동작 양 끝 자세 오차는 `0.000011444`,
  중간 동작의 Hips Y 이동량과 프레임별 최대 Y 변화는 모두 `0`입니다. 높이 오프셋을
  제외한 4번 원본 대비 자세 오차는 `0.000000246`, 등 뒤 머스켓 장착 오차·외형 자산
  오차·손 머스켓 장착 오차는 모두 `0`입니다. 다른 이슈판트 슬롯과 다른 씬 루트의
  변경은 모두 `False`입니다.
- 제공 영상 전체 및 전환부를 프레임으로 직접 확인한 뒤, 최종 12열 전·후면 캡처와
  `유지 끝 → 중간 시작 → 중간 50% → 중간 끝 → 발검 시작` 확대본을 원본 해상도로
  직접 확인했습니다. 유지 끝과 중간 시작, 중간 끝과 발검 시작의 위치가 각각 같고,
  중앙 자세가 두 끝자세 사이로 이어지며 갑작스러운 상하 이동이나 팔 자세 절단이
  보이지 않습니다. 등 뒤 머스켓, 허리 장검, 오른손 장검의 기존 표시 순서도 유지됩니다.
- Unity 반영 명령: `RefreshAssets`, `ApplyIspant08ChangingToSwordReplacement`
- 저장본 검사·최종 캡처 명령: `InspectIspant08ChangingToSwordReplacement`,
  `CaptureIspant08ChangingToSwordReview`
- 검사 기록:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 최종 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`,
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_HoldBridgeDrawStart_DirectReview.png`
- 입력 영상 프레임 분석:
  `docs/validation/ispant_changing_to_sword_2026-08-10/source_video_224035_analysis/`
- 실행하지 않은 항목: 하네스 검증(`Run-HarnessValidation.ps1`) 및 관련 실행·재실행·
  조사·로그·문서·생성물, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`,
  `Build-WindowsDev.ps1`, Ensure·Validate·Smoke 계열 명령, Unity 재시작·복구·재생성,
  Git 작업.

## 이슈판트 8번에서 현재 4번 검 뽑기 동작으로 이어지는 반복 시퀀스

상태: `ISPANT_SLOT8_TO_SLOT4_DRAW_SEQUENCE_COMPLETE`

- 8번의 기존 동작을 그대로 유지했습니다. 140프레임에 오른손 머스켓을 숨기고 등 뒤
  머스켓을 표시한 뒤, 오른팔을 앞으로 가져오고 손을 `0.4초` 동안 내립니다. 손을 모두
  내린 같은 8번 애니메이션 자세를 정확히 `0.5초` 유지합니다.
- 유지 종료 직후 현재 4번 `Ispant_04_DrawSword`에서 실제 재생 중인
  `Ispant_04_DrawSword_0_9m_Upward` 애니메이션 전체를 속도 변경 없이 이어 붙였습니다.
  8번 구간은 `3.9초`, 4번 구간은 원본과 같은 `1.76초`이며 전체 반복 주기는
  `5.66초`입니다.
- Animator는 `Ispant_ChangingToSword_Mixamo`와 `Ispant_04_DrawSword` 두 상태로
  구성했습니다. `8번 → 4번 → 8번`의 두 전환은 모두 종료 시점 `1`, 전환 지속시간
  `0초`, 조건 없음으로 설정해 별도 추정 자세나 블렌딩을 만들지 않았습니다.
- 4번의 오른손 장검 `Ispant_ApprovedLongSword`를 현재 4번 개체에서 메시·머티리얼·
  `mixamorig:RightHand` 로컬 장착값 그대로 복제했습니다. 8번 구간에는 왼쪽 허리 장검만,
  4번 구간에는 오른손 장검만 표시합니다. 등 뒤 머스켓은 4번 구간 전체에 유지됩니다.
- 저장본 독립 검사는 `PASS`입니다. 4번 원본 대비 이어재생 자세 오차는
  `0.000000246`, 4번 구간 등 뒤 머스켓 장착 오차는 `0`, 외형 자산 오차는 `0`입니다.
  기존 손 머스켓 장착 오차와 최종 등 뒤 머스켓 장착 오차도 `0`이며, 다른 슬롯과 다른
  씬 루트 변경은 모두 `False`입니다.
- 최종 10열 전·후면 캡처와 유지 종료 이후 확대본을 원본 해상도로 직접 확인했습니다.
  머스켓 부착, 오른팔 내리기, 0.5초 유지, 4번 검 뽑기 시작·진행·종료, 8번 시작 복귀가
  순서대로 보이며 허리 장검과 오른손 장검이 동시에 표시되지 않습니다.
- Unity 반영 명령: `RefreshAssets`, `ApplyIspant08ChangingToSwordReplacement`
- 저장본 검사·캡처 명령: `InspectIspant08ChangingToSwordReplacement`,
  `CaptureIspant08ChangingToSwordReview`
- 저장본 검사 기록:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 최종 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`,
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_DrawSwordContinuation_DirectReview.png`
- 실행하지 않은 항목: 하네스 검증(`Run-HarnessValidation.ps1`) 및 관련 실행·재실행·
  조사·로그·문서·생성물, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`,
  `Build-WindowsDev.ps1`, Ensure·Validate·Smoke 계열 명령, Unity 재시작·복구·재생성,
  Git 작업.

## 이슈판트 6·7·8번 왼쪽 허리 장검 동기화

상태: `ISPANT_SLOT6_7_8_LEFT_WAIST_LONG_SWORD_COMPLETE`

- 새 장검이나 임의 장착 좌표를 만들지 않았습니다. 정적 이슈판트의 승인 장검
  `Ispant_ApprovedLongSword` 메시·머티리얼을 사용해 이미 정적 외형과 맞춰져 있던
  6번의 `Ispant_ApprovedLongSword_LeftWaist` 장착본을 정확한 기준으로 삼았습니다.
  7번은 6번의 메시·머티리얼과 `mixamorig:Hips` 로컬 장착 행렬을 그대로 보존하고,
  8번은 7번의 같은 장착본을 그대로 보존합니다.
- 6번에서는 오른손 장검을 숨기고 왼쪽 허리 장검을 전체 시퀀스와 0.5초 유지 구간에
  계속 표시하도록 수정했습니다. 7번과 8번도 왼쪽 허리 장검 렌더러를 제거하지 않고
  각 모션 전체에서 표시합니다. 각 모델에는 왼쪽 허리 장검 렌더러가 하나만 존재합니다.
- 세 장검 루트의 부모는 모두 해당 모델의 `mixamorig:Hips`입니다. 애니메이션 전체 샘플의
  Hips 로컬 장착 행렬 드리프트는 6·7·8번 모두 `0`입니다. 모델 기준 장검 회전 변화는
  각각 `45.72752도`, `1.56118858도`, `36.67654도`로 측정되어 몸통 움직임을 따라
  위치와 각도가 변하는 것을 확인했습니다. 모델 기준 위치 변화는 각각 `0.173067m`,
  `0.00503124064m`, `0.08101912m`입니다.
- 기존 머스켓 손 접촉·총구 방향·발사 이펙트와 발사 타이밍, 8번의 140프레임 머스켓 전환,
  손 내리기 `0.4초`, 손 내린 자세 `0.5초` 유지 및 반복 재생은 그대로 유지했습니다.
  다른 슬롯과 다른 씬 루트 변경은 검사 결과 모두 `False`입니다.
- Unity 반영 명령: `RefreshAssets`,
  `ApplyIspantSheathToRifleFinalAimArmLiftRevision`,
  `ApplyIspant07FiringReplacement`, `ApplyIspant08ChangingToSwordReplacement`
- 저장본 검사 명령: `InspectIspantSheathToRifleFinalAimArmLiftRevision`,
  `InspectIspant07FiringReplacement`, `InspectIspant08ChangingToSwordReplacement`
  — 모두 `PASS`
- 직접 확인 캡처:
  `docs/validation/ispant_sheath_to_rifle_final_aim_arm_lift_revision_2026-08-09/Ispant_06_SheathToRifle_FinalAimArmLift_FinalReview.png`,
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_FinalReview.png`,
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`
- 저장본 검사 기록:
  `docs/validation/ispant_sheath_to_rifle_final_aim_arm_lift_revision_2026-08-09/Ispant_06_SheathToRifle_FinalAimArmLift_Inspection.txt`,
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_Inspection.txt`,
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 실행하지 않은 항목: 하네스 검증(`Run-HarnessValidation.ps1`) 및 관련 조사·로그·문서·생성물,
  `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`,
  Ensure·Validate·Smoke 계열 명령, Unity 재시작·복구·재생성, Git 작업.

## 이슈판트 8번 머스켓 정리·검 전환 모델 교체

상태: `ISPANT_SLOT8_CHANGING_TO_SWORD_COMPLETE`

### 최신 교정 상태 — 애니메이션 손 내린 자세 0.5초 유지 후 반복

- 이 절이 아래의 모든 이전 8번 기록보다 우선합니다. 사용자 지시에 따라
  손을 내린 뒤 `Ispant_01_Static` 복제본으로 전환하던 처리를 제거했습니다.
  8번 모션 개체에는 기존 정적 복제본 `Ispant_08_ExactStaticHold`이 남아 있지
  않으며, 정적 모델 렌더러로 전환하지 않습니다.
- 원본 FBX의 손 내리기 구간 `180~230프레임`은 기존 승인값대로 `0.4초` 동안
  재생합니다. 손을 모두 내린 230프레임 자세에 도달한 뒤 같은 8번 애니메이션
  개체의 전체 Transform 자세를 `0.5초` 유지하고 처음부터 반복 재생합니다.
  최종 클립 길이는 `3.9초`, 프레임률은 `60fps`, 반복 재생 속도는 `1`입니다.
- 사용자 지정 머스켓 전환은 유지했습니다. `139프레임`까지 오른손 머스켓을
  표시하고 `140프레임`부터 오른손 머스켓을 숨기면서 6번의 실제 머스켓 공유
  메시·머티리얼·`Spine2` 로컬 장착값을 사용한 등 뒤 머스켓을 표시합니다.
  손을 내리는 구간과 0.5초 유지 구간에도 등 뒤 머스켓이 계속 표시됩니다.
- 저장본 전용 검사는 `PASS`입니다. `StaticModelRendererTransition=False`,
  손 내리기 `0.4초`, 손 내린 자세 유지 `0.5초`, 최종 자세 드리프트 `0`,
  등 뒤 머스켓 드리프트 `0`, 최종 장착 오차 `0`, 반복 재생 `True`, 다른 슬롯과
  다른 씬 루트 변경은 모두 `False`입니다.
- 최종 8열 전·후면 이미지와 마지막 4열 확대본을 원본 해상도로 직접 확인했습니다.
  손 내리기 시작·중간·종료가 이어지고, 손 내리기 종료와 0.5초 유지 종료의
  몸체·팔·손·등 뒤 머스켓 외형이 동일합니다. 정적 모델로의 외형 전환, 자세 급변,
  오른손 머스켓 재표시 또는 두 머스켓 중복은 보이지 않았습니다.
- Unity 반영 명령: `RefreshAssets`, `ApplyIspant08ChangingToSwordReplacement`
- 저장본 검사·캡처 명령: `InspectIspant08ChangingToSwordReplacement`,
  `CaptureIspant08ChangingToSwordReview`
- 최종 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`
- 손 내리기 및 유지 구간 확대본:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_AnimatedHandLoweringHold_Review.png`
- 저장본 검사 기록:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 미실행: 하네스 검증 및 관련 조사·로그·문서·생성물 작업,
  `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`,
  Ensure·Validate·Smoke 계열 명령, Unity 재시작·복원·재생성, Git 작업.

### 이전 교정 상태 — 오른팔 원본 복귀 유지 및 0.4초 정적 자세 전환

- 이 절이 아래의 이전 8번 완료 기록보다 우선합니다. 사용자는 오른팔을 뒤로
  보낸 뒤 앞으로 다시 가져오기 전에 머스켓이 등 뒤에 붙고 오른손 머스켓이
  사라져야 하며, 오른팔 복귀 뒤 정적 모델의 서 있는 자세로 `0.4초` 동안
  전환하라고 지정했습니다.
- 기존 구현은 `161프레임`에 정적 몸체 전체를 즉시 표시해 원본 오른팔 전방
  복귀를 잘랐습니다. 이를 폐기했습니다. 최신 사용자 지정값을 그대로 적용해
  `139프레임`까지 오른손 머스켓을 표시하고 `140프레임`부터 손 머스켓만
  숨기면서 등 뒤 머스켓을 표시합니다. 이 값이 아래의 이전 `158→159프레임`
  기록보다 우선합니다.
- `140~179프레임`에는 8번의 원본 애니메이션 몸체와 원본 본 커브를 그대로
  유지합니다. 등 뒤 머스켓은 6번의 실제 승인 MeshRenderer
  `Ispant_Sheath_RigidMusket`을 동일 Mixamo 리그의 `Spine2` 아래에 로컬 위치·
  회전·배율, 공유 메시·머티리얼 그대로 복제해 표시합니다. 새 장비 외형이나
  임의 좌표는 생성하지 않았습니다.
- `180프레임`(`3초`)까지 원본 오른팔 전방 복귀를 그대로 유지합니다. 이어서
  원본 FBX에서 직접 확인한 `180~230프레임`의 서기 자세 복귀 구간을 같은
  Mixamo 리그에서 `0.4초`로 시간 압축해 재생합니다. `3.4초`에만 정적 모델의
  정확한 전체 복제본으로 전환하고 `0.5초` 유지합니다. 최종 클립 길이는
  `3.9초`, 프레임률은 `60fps`, 반복 재생 속도는 `1`입니다.
- 저장본 독립 검사는 `PASS`입니다. 손 머스켓 장착 오차 `0`, 중간 등 뒤
  머스켓의 6번 로컬 장착·공유 자산 오차 `0`, 최종 정적 계층·등 뒤 머스켓
  오차 `0.000018299`, 정적 유지 드리프트 `0`, 다른 슬롯·다른 씬 루트 변경은
  모두 `False`입니다.
- 첫 직접 비교는 서로 다른 리그의 본 위치·배율을 복사해 몸체가 크게 늘어난
  실패를 확인했고 폐기했습니다. 위치·배율 복사를 제거한 다음 비교에서는
  변형은 사라졌지만 정적 몸체를 `161프레임`부터 표시해 `161·170·180프레임`의
  오른팔 실루엣 차이가 확인되지 않아 다시 실패로 판정했습니다. 현재 교정본은
  그 전체 정적 몸체 조기 전환을 제거한 상태입니다.
- 최종 8열 전·후면 비교와 전환 전후 확대본을 원본 해상도로 직접 확인했습니다.
  `139프레임`(`2.316667초`)에는 머스켓이 오른손에 남아 있고 등 뒤 머스켓은
  없으며, `140프레임`(`2.333333초`)에는 오른손 머스켓이 사라지고 동일 머스켓
  하나가 등 뒤에 붙습니다.
  `170프레임`에는 빈 오른팔이 앞으로 내려오며, `180프레임`과 0.4초 복귀
  중간을 지나 마지막 두 화면이 정적 모델의 서기 자세와 일치합니다. 몸체
  늘어짐, 자세 급변, 머스켓 부유·중복은 보이지 않았습니다.
- 최종 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`
- 139·140프레임 전환 직접 확인 확대본:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Frame139_140_Review.png`
- 직접 확인용 좌·우 확대본:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview_Left.png`,
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview_Right.png`
- 저장본 검사 기록:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 전환 프레임 좌·우 측면 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_RearArm_Diagnostic.png`
- 미실행: 하네스 검증 및 관련 조사·로그·문서·생성물 작업,
  `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`,
  Ensure·Validate·Smoke 계열 명령, Unity 재시작·복원·재생성, Git 작업.

### 머스켓 등 뒤 배치 후 정적 자세 0.5초 유지

- 원본 모션 `0~230프레임`을 10프레임 간격으로 앞·뒤에서 직접 비교했습니다.
  오른팔과 손 머스켓이 뒤쪽에 올라가 있는 마지막 확인 시점은 `160프레임`
  (`2.666667초`)이며, 바로 다음 `161프레임`(`2.683333초`)에 손 머스켓을 포함한
  애니메이션 외형을 숨기고 `Ispant_01_Static`의 현재 전체 계층을 그대로 복제한
  `Ispant_08_ExactStaticHold`만 표시합니다.
- 정적 복제본은 본 자세를 새로 추측하거나 생성하지 않습니다. 정적 모델의 전체
  Transform 계층, 메시, 머티리얼과 등 뒤 장비를 그대로 사용하므로 마지막 자세와
  머스켓 배치는 실제 정적 모델과 같습니다.
- 전환 뒤 정적 자세를 정확히 `0.5초` 유지합니다. 최종 클립 길이는
  `3.183333초`, 측정된 유지 시간은 `0.5초`이며 반복 재생 속도는 `1`입니다.
- 저장본 독립 검사는 `PASS`입니다. 최종 정적 계층 오차와 등 뒤 머스켓 포함 외형
  오차는 `0.000018299`, 유지 구간의 자세 변화와 머스켓 변화는 모두 `0`이며,
  손 머스켓의 전환 후 표시는 `False`, 두 머스켓 동시 표시는 `False`, 다른 슬롯과
  다른 씬 루트 변경은 모두 `False`입니다.
- 최종 비교 이미지는 앞면·뒷면에서 `정적`, `오른팔 후방 동작 종료`, `정적 전환 시작`,
  `0.5초 유지 종료`를 보여 줍니다. 원본 크기로 직접 확인해 마지막 두 열이 정적
  모델과 같은 서 있는 자세이고 머스켓은 등 뒤에 있으며 중복 머스켓이 없음을
  확인했습니다. 수치만으로 선택한 잘못된 `84→85프레임` 전환과 별도 머스켓의
  공중 부유 결과는 폐기했고 최종 증빙에는 포함되지 않습니다.
- 진단 비교 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_RearArm_Diagnostic.png`
- 미실행: 하네스 검증 및 관련 조사·로그·문서·생성물 작업,
  `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`,
  Ensure·Validate·Smoke 계열 명령, Unity 재시작·복원·재생성, Git 작업.

### 적용 내용

- `CargoRunMvp`의 기존 `Ispant_08_StowMusketDrawSword` 모델을 삭제하고,
  사용자 지정 원본 `enemies model/išpant changing to sword.fbx`의 프로젝트
  사본 `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_ChangingToSword.fbx`을
  애니메이션 원본으로 사용하는 8번 모델로 교체했습니다.
- 원본과 프로젝트 사본의 SHA-256은 모두
  `90027F34321AF766487DE422B9342ED8239CBCBD9B6B524CB60B3344A1CE0D7F`입니다.
- FBX의 단일 `mixamo` 테이크를 `Ispant_ChangingToSword_Mixamo`로 임포트하고,
  `Ispant_ChangingToSword_Mixamo_Retargeted` 클립을 반복 재생합니다. 원본 모션은
  `3.966667초`이며 그중 `2.683333초`까지 사용합니다. 정적 유지가 포함된 최종
  클립은 `3.183333초`, 프레임률은 `60fps`, Animator 재생 속도는 `1`입니다.
- 새 외형을 추측하거나 생성하지 않았습니다. 정적 이슈판트 외형과 동기화가
  완료된 7번의 `Ispant_Armed_Body`, `Ispant_Crescent_Ornament`,
  `Ispant_Reference_Eye_Slits` 공유 메시·머티리얼·스킨 구성을 그대로 복제했습니다.
- 8번 시작 자세의 양팔과 그 부모 상체 본은 7번 시작 자세를 기준으로 사용하고,
  사용자 FBX 애니메이션의 실제 변화량만 그 위에 유지했습니다. 손 머스켓도
  7번의 오른손 자식 오브젝트, 로컬 Transform, 메시, 머티리얼을 그대로
  복제했습니다.
- 7번에서 사용한 총구 발사 이펙트는 8번에 복제하지 않았습니다. 별도 등 뒤
  머스켓 렌더러는 사용하지 않으며, 전환 뒤에는 실제 정적 모델 복제본의 등 뒤
  장비만 표시됩니다.

### 저장본 검사 및 직접 시각 확인

- 저장 장면 독립 검사: `PASS`, 검사 중 씬 변경 `False`
- 외형 공유 자산 오차 `0`, 손 머스켓 장착 오차 `0`
- 7번 대비 8번 시작 양쪽 어깨 높이 오차 `0`, 기준 자세 회전 오차 `0°`
- 별도 등 뒤 머스켓 렌더러 `0개`, 루트 모션 `False`
- 다른 11개 이슈판트 슬롯 변경 `False`, 다른 씬 루트 변경 `False`
- 최종 이미지는 `정적`, `오른팔 후방 동작 종료`, `정적 전환 시작`, `0.5초 유지 종료`를
  앞면과 뒷면에서 비교합니다. 원본 크기로 직접 확인했으며 전환 뒤에는 실제 정적
  모델과 같은 자세·외형·등 뒤 머스켓만 표시됩니다.
- 저장본 검사:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_Inspection.txt`
- 최종 직접 확인 이미지:
  `docs/validation/ispant_changing_to_sword_2026-08-10/Ispant_08_ChangingToSword_FinalReview.png`

### 구현 및 실행하지 않은 항목

- `Assets/_Project/Editor/Validation/IspantChangingToSwordAnimationTool.cs`에 8번
  적용, 독립 검사, 최종 1회 캡처 기능을 추가했습니다.
- `UnityEditorValidationBridge`에는
  `ApplyIspant08ChangingToSwordReplacement`,
  `InspectIspant08ChangingToSwordReplacement`,
  `CaptureIspant08ChangingToSwordReview` 명령을 연결했습니다.
- 미실행: 하네스 검증 및 관련 작업, EditMode·PlayMode 테스트, Windows 빌드,
  Ensure·Validate·Smoke 계열 명령, Unity 재시작·복원·재생성, Git 작업.

## 이슈판트 7번 머스켓 조준·발사 모델 교체

상태: `ISPANT_SLOT7_BREAKTHROUGH_SPEED_MUZZLE_FLASH_COMPLETE`

### 7번 돌파태세 공격속도 및 총구 발사 이펙트

- 원본 `docs/GAME_DESIGN_SOURCE.txt`의 이슈판트 돌파태세 머스켓 사양인
  연사 딜레이 `2.5초`를 7번 `Ispant_07_BreakthroughMusketAimFire`에
  적용했습니다. 원본 클립 길이 `0.5초`에 대한 Animator 재생 속도는 `0.2`,
  유효 반복 공격 주기는 정확히 `2.5초`입니다.
- 원본 FBX에는 AnimationEvent가 없습니다. 0~30프레임의 실제 머스켓 총구
  이동을 조사해 총열축 반대 방향 최대 이동이 발생하는 `0→1프레임` 직전인
  원본 `0프레임`을 발사 시점으로 사용했습니다. 최대 후방 이동량은
  `0.00198358227m`입니다.
- 새 VFX 외형을 만들지 않았습니다. `APPROVED`, `approved_for_unity=true`인
  기존 `Rebellion_Forward_Burst_Flash.asset/.mat` 메시·머티리얼을 그대로
  재사용했습니다. 기존 승인 사양대로 실제 표시 시간은 `0.08초`입니다.
- 총구 위치는 승인 머스켓 형상 검사와 같은 얇은 끝 20% 정점군의 방사 중심을
  실제 최전방 투영면으로 옮겨 구했습니다. 총구 앵커 위치 오차와 발사축 각도
  오차는 모두 `0`입니다. 발사 전·발사·발사 후 표시 상태 오차도 모두 `0`입니다.
- 첫 적용은 단일 최장축 끝 정점만으로 캡 중심을 요구해 정점 부족으로 실패했고
  저장하지 않았습니다. 실제 얇은 총구 끝 정점군 방식으로 교체했습니다.
  첫 실패가 남긴 Unity dirty 표지는 기존 7번 단일 모델이 보존된 상태만 확인해
  동일 적용 명령 안에서 처리했으며, 성공 저장 후 임시 허용 코드를 제거하고
  다시 깨끗한 씬 보호 조건으로 복구했습니다.
- 저장본 독립 검사는 `PASS`이며 다른 이슈판트 슬롯 변경은 `False`, 검사와
  캡처 중 씬 변경은 `False`입니다. 발사 전·발사·발사 후 원본 이미지를 직접
  확인해 가운데 발사 순간에만 화염이 머스켓 총열 끝 바로 앞에서 총구 방향으로
  표시되고 앞뒤 화면에서는 사라지는 것을 확인했습니다.
- 최종 직접 확인 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_FinalReview.png`
- 저장본 검사:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_Inspection.txt`
- 미실행: `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`,
  `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업, Unity 재시작,
  Git 작업.

### 7번 정적 모델 몸체 하단 기준 Y축 정렬

- 정적 모델과 7번 모델 루트의 기존 로컬 Y는 모두 `0.40032327`이지만,
  7번 firing 시작 자세의 내부 리그 위치 때문에 표시 몸체 하단이 정적 모델보다
  아래에 있음을 확인했습니다.
- 사용자 확인대로 정적 `Ispant_Armed_Body`의 몸체 하단과 7번 시작 자세의
  동일 몸체 하단 차이만큼 7번 모델 루트 Y만 보정했습니다. X·Z, 회전, 배율,
  애니메이션은 변경하지 않았습니다.
- 첫 `SkinnedMeshRenderer.bounds` 하단 정렬은 수치만 통과하고 공통 카메라
  직접 비교에서 실제 발끝이 더 아래였으므로 실패로 판정하고 폐기했습니다.
  두 번째 베이크 정점 하단 정렬도 같은 직접 비교에서 실패해 폐기했습니다.
- 최종 구현은 정적 모델과 7번 시작 자세의 몸체만 동일한 2048×2048 고정
  직교 카메라로 렌더링하고, 실제 비배경 최하단 픽셀의 세계 Y 차이만큼
  7번 모델 루트 Y를 이동합니다. 적용된 7번 로컬 Y는 `0.9182373`입니다.
- 저장본 검사는 `PASS`이며 렌더 몸체 하단 오차는 `0.0009242594m`, 즉
  2048px 측정에서 1픽셀 이내입니다. 공통 카메라 최종 이미지도 원본으로
  직접 확인했습니다. 발바닥 면의 최하단 행은 정적 `530`, 7번 시작·중간·끝
  `530/531/530`으로 동일하며, 자세에 따라 기울어진 발끝 소수 픽셀만 최대
  5픽셀 차이가 있습니다.
- 최종 직접 확인 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_FinalReview.png`
- 적용·저장 로그:
  `docs/validation/ispant_firing_replacement_2026-08-10/ApplyIspant07_StaticRenderedBodyBottomYAlignment.log`
- 미실행: `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`,
  `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업, Unity 재시작,
  Git 작업.

### 6번 마지막·7번 양팔 및 머스켓 0.1m 상향 이동

- 사용자 지정값대로 6번 마지막과 7번 전체의 양팔·머스켓을 기존 위치에서
  위로 `0.1m` 이동합니다. 머스켓 회전과 총구 각도는 변경하지 않습니다.
- 6번은 `LeftShoulder`와 `RightShoulder`의 위치 곡선에 동일한 세계 상향
  이동을 적용합니다. 머스켓은 오른손 자식이므로 오른팔과 같은 이동량을
  그대로 따르며, 기존 양손 파지와 팔·머스켓 회전 곡선은 유지합니다.
- 7번은 수정된 6번 마지막 양쪽 어깨 위치를 시작 기준으로 사용하고, 제공된
  firing 모션의 원래 위치 변화량만 그 위에 유지하도록 구성합니다.
- 6번 적용·저장 및 저장본 검사는 `PASS`입니다. 양쪽 어깨의 `0.1m` 상향
  목표 오차는 각각 `0.000000119m`, 오른손 하부 위치 오차 `0m`, 왼손과
  머스켓 표면 거리 `0.002120566m`, 총구 정면 및 머스켓 하부 방향 오차는
  모두 `0°`입니다. 다른 슬롯·다른 장면 루트 변경은 `False`입니다.
- 7번 적용·저장 및 저장본 검사는 `PASS`입니다. 6번 마지막 대비 7번 시작의
  양쪽 어깨 위치 오차와 조준 본 회전 오차는 모두 `0`, 머스켓 로컬 행렬·
  모델 회전 오차도 `0`입니다. 전 구간 오른손 최대 상대 위치 오차는
  `0.00000015269147m`, 왼손 최대 상대 위치 오차는 `0.0136314947m`, 머스켓
  로컬 장착 드리프트는 `0`입니다. 반복 재생 속도 `0.333333343`과 유효 주기
  `1.5초`도 유지됩니다.
- 진단 12화면과 검사 통과 후 한 번 생성한 최종 12화면을 원본 해상도로 직접
  확인했습니다. 6번 마지막 및 7번 시작·중간·마지막의 양팔과 머스켓이 함께
  가슴 높이로 올라갔고 네 시점의 높이가 일치합니다. 양손 파지는 유지되며
  총구 회전 변화는 없습니다.
- 최종 직접 확인 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_FinalReview.png`
- 미실행: `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`,
  `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업, Unity 재시작,
  Git 작업.

### 사용자 지시 그대로 오른손을 머스켓 아래로 이동

- 아래 완료 판정보다 이 절이 우선합니다. 머스켓 토폴로지의 특정 금속 연결
  부품을 방아쇠라고 단정한 이전 방식은 사용자 지시 밖의 추론이므로 폐기했고,
  관련 식별 코드도 제거했습니다.
- 사용자 지시대로 머스켓과 왼손의 화면 위치는 유지하고 오른팔·오른손만
  아래로 이동합니다. 오른손 자식인 머스켓에는 손 이동량과 정확히 반대인
  로컬 이동을 적용해 머스켓 세계 위치가 따라 내려가지 않도록 구성합니다.
- `AGENTS.md`의 추측 금지 절대 규칙에 “에이전트는 쓸데없이 추론하지 않고
  사용자가 설명한 명확한 지시대로만 작업한다.”를 추가했고 현재 작업부터
  적용합니다.
- 6번은 마지막 조준까지 오른손을 머스켓 기준 아래로 `0.08m` 이동시키고,
  오른손 자식 머스켓에는 정확한 역방향 로컬 이동을 적용했습니다. 따라서
  오른손만 사용자 지정 하부 위치로 내려가고 머스켓·왼손의 기존 화면 위치와
  총구 방향은 유지됩니다. 이전에 특정 메시 부품을 방아쇠라고 추론하던 코드와
  그 수치 판정은 모두 제거했습니다.
- 6번 저장본 검사는 `PASS`이며 오른손 하부 목표 오차 `0m`, 왼손과 머스켓
  표면 거리 `0.002120566m`, 총구 정면 오차 `0°`, 머스켓 하부 방향 오차
  `0°`, 다른 슬롯·다른 씬 루트 변경은 모두 `False`입니다.
- 7번은 수정된 6번 마지막 자세를 다시 받아 저장했습니다. 저장본 검사는
  `PASS`이며 오른손 상대 위치 최대 오차 `0.000000184317187m`, 왼손 상대
  위치 최대 오차 `0.013631437m`, 6번 마지막 대비 머스켓 로컬 행렬·회전
  오차는 모두 `0`, 다른 이슈판트 슬롯 변경과 검사 중 씬 변경은 `False`입니다.
  내장 `Ispant_Firing_Mixamo` 반복, 재생 속도 `0.333333343`, 유효 반복 주기
  `1.5초`도 유지됩니다.
- 진단 이미지 2회와 최종 이미지 1회를 원본 해상도로 직접 확인했습니다.
  6번 마지막과 7번 시작·중간·마지막의 정면·좌·우 화면 모두 오른손 장갑이
  머스켓 상단이 아니라 사용자가 지정한 하부 위치에 겹치고, 왼손은 앞쪽 파지
  위치를 유지하며 총구와 머스켓은 동작 중 흔들려 분리되지 않습니다.
- 최종 직접 확인 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_FinalReview.png`
- 미실행: `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`,
  `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 조사·로그·생성물 작업,
  Unity 재시작, Git 작업.

### 사용자 제공 2222 이미지 기준 오른손 방아쇠 위치 재수정

- 아래 완료 판정보다 이 절이 우선합니다. 사용자 제공 최신 `2222.png`에서
  왼손은 총열 하부를 받치지만 오른손 장갑은 머스켓 방아쇠 조립부가 아니라
  총몸 위쪽에 놓인 실패를 직접 확인했습니다. 직전 완료 판정은 무효입니다.
- 원인은 6번 생성기가 잡는 순간 오른손과 가까운 임의의 머스켓 표면 후보 중
  왼손 지지가 가장 쉬운 점을 오른손 기준점으로 선택한 것입니다. 이 점을
  장갑 중심과 맞춘 수치 검사는 실제 방아쇠 파지를 보장하지 못했습니다.
- 기존 머스켓 메시 토폴로지에서 긴 총열과 분리되고 나무 개머리판 앞 끝에
  정확히 붙은 가장 큰 좌우 한 쌍의 짧은 금속 연결 부품을 방아쇠 조립부로
  식별했습니다. 새 형상이나 임의 오프셋을 만들지 않고 이 원본 부품의 정점
  중심을 오른손 장갑 중심에 맞추며, 왼손·총구·방아쇠 방향은 유지합니다.
- 6번은 잡는 순간의 원본 연속성을 유지하면서 마지막 조준까지 기존 방아쇠
  조립부 중심을 오른손 장갑 중심에 맞추도록 머스켓 로컬 위치 곡선을 다시
  생성했습니다. 이동된 머스켓을 기준으로 왼손 IK도 재생성했고, 7번은 수정된
  6번 마지막 로컬 행렬과 양팔 자세를 그대로 받아 다시 저장했습니다.
- 최종 보조 검사 결과는 6번 방아쇠 중심–오른손 장갑 오차
  `0.000000119m`, 왼손–총열 하부 거리 `0.002576511m`, 총구 정면 오차
  `0°`, 방아쇠 하부 방향 오차 `0°`입니다. 7번 전 구간 오른손 최대 접촉
  오차는 `0.000000130757158m`, 왼손 최대 접촉 오차는 `0.01562951m`,
  6번 마지막 대비 머스켓 로컬 행렬·모델 회전 오차는 모두 `0`입니다.
- 진단 2회 후 최종 12패널을 한 번 생성해 원본과 정면 확대를 직접
  확인했습니다. 6번 마지막과 7번 시작·중간·끝 모두 오른손 장갑이 개머리판
  앞쪽 방아쇠 조립부를 덮어 겹치고, 총몸 위에 분리되어 있지 않습니다.
  왼손은 총열 하부를 유지하며 개머리판은 오른팔을 관통하지 않습니다.
- 최종 직접 확인 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_FinalReview.png`
- 미실행: `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`,
  `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 조사·로그·생성물 작업.

### 1111 이미지 지적 기준 최종 재수정 완료

- 아래의 이전 완료 기록보다 이 절이 우선합니다. 사용자 제공 `1111.png`에서
  확인된 실패 원인은 머스켓을 오른손 자식으로 붙일 때 잡는 순간의 장갑–총
  표면 간격 약 `0.048m`를 고정한 것이었습니다. 팔과 총을 함께 올려도 그
  간격이 유지되어 방아쇠가 양손 아래에 남았습니다.
- 6번은 집는 순간의 연속성을 유지하면서 머스켓 표면 피벗을 마지막 조준
  자세까지 가시 오른손 장갑 중심으로 이동하는 로컬 위치 곡선 3개를 추가했습니다.
  변경된 총열 위치를 기준으로 왼손 장갑이 총열 하부 전방 지지면에 닿도록
  왼팔 IK도 다시 생성했습니다. 7번은 이 수정된 6번 마지막 로컬 위치·회전·
  배율과 양팔 자세를 그대로 받아 다시 생성했습니다.
- 최종 저장본 보조 검사 결과는 6번 `FinalVisibleRightHandGripError=
  0.000000119m`, `FinalLeftHandToMusketSurfaceDistance=0.002124424m`,
  머스켓 위치 곡선 `3개`, 총구 정면 오차 `0°`, 방아쇠 하부 오차 `0°`입니다.
  7번은 오른손 최대 접촉 오차 `0.000000186m`, 왼손 최대 접촉 오차
  `0.0158848725m`, 6번 최종 머스켓 로컬 행렬 오차 `0`입니다.
- 최종 12패널 원본 및 확대 크롭을 직접 확인했습니다. 6번 마지막과 7번
  시작·중간·끝 모두 머스켓 중심선이 양손 장갑을 통과하고, 오른손 장갑은
  방아쇠·손잡이 구역, 왼손 장갑은 총열 하부 전방 지지 구역과 겹칩니다.
  머스켓이 양손 아래 허공에 떨어져 보이지 않으며 개머리판은 오른팔 장갑을
  관통하지 않습니다.
- 최종 직접 확인 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_FinalReview.png`
- 미실행: `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`,
  `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 조사·로그·생성물 작업.

### 사용자 제공 1111 이미지로 기존 완료 판정 재철회

- 사용자 제공 이미지 `C:/Users/gus68/OneDrive/바탕 화면/1111.png`에서 머스켓
  전체가 양손보다 아래에 있고, 방아쇠·그립이 오른손 장갑과 분리되어 허공에
  떠 보이는 실패를 직접 확인했습니다. 직전 완료 판정은 무효입니다.
- 현재 6번 생성기는 잡는 순간의 오른손 장갑 중심과 머스켓 표면 사이 거리
  약 `0.048m`를 오른손 자식 로컬 장착으로 그대로 고정합니다. 이후 팔과 총이
  함께 올라가도 이 상대 간격이 유지되어 방아쇠가 오른손 아래에 남습니다.
- 잡은 뒤 머스켓의 로컬 그립점을 오른손 장갑 중심까지 매끄럽게 이동시키고,
  변경된 최종 총열 위치를 기준으로 왼팔 지지 IK를 다시 생성한 뒤 7번 시작
  자세에 그대로 전파합니다. 완료 기준은 수치가 아니라 방아쇠·오른손 및
  총열 하부·왼손의 실제 화면 겹침입니다.

### 사용자 직접 확인으로 기존 완료 판정 철회

- 사용자 제공 이미지 `C:/Users/gus68/OneDrive/바탕 화면/2222.png`에서 7번
  머스켓이 양손 위의 허공에 분리되어 있고, 개머리판이 오른쪽 상완·어깨를
  관통하는 것을 직접 확인했습니다. 기존 직접 시각 통과 판정은 무효입니다.
- 사용자가 추가로 지적한 6번 마지막 자세도 같은 방식으로 양손과 머스켓이
  떨어져 있습니다. 7번만 보정하지 않고 6번의 최종 양손 파지부터 수정한 뒤
  그 결과를 7번의 조준 기준 자세로 다시 사용합니다.
- 원인은 6번 생성기가 보이는 장갑 손바닥이 아니라 `RightHand`·`LeftHand`
  손목 본 피벗을 머스켓 표면과 맞춘 데 있습니다. 7번도 이 잘못된 6번 기준을
  복사해 손목 피벗 수치만 통과했습니다. 이후 완료 판정은 정면·좌측·우측 확대
  화면에서 실제 장갑 손과 머스켓의 접촉 및 개머리판 비관통이 모두 보여야 합니다.

### 6번·7번 실제 장갑 손 양손 파지 최종 수정

- 손목 본 피벗을 접촉점으로 사용하던 계산을 폐기했습니다. 정적 외형과 동일한
  `Ispant_Armed_Body`의 손·손가락 하위 본 가중 정점을 실제 Unity 스키닝 식
  `bone.localToWorldMatrix × bindpose × vertex`로 변환해 보이는 장갑 손의 고정
  로컬 앵커를 추출합니다.
- 6번은 오른손 장갑 앵커에 방아쇠 쪽 그립 표면을 맞추고, 왼손 장갑 앵커가
  총열 하부의 가죽 지지면에 닿도록 양팔 본 회전을 다시 생성했습니다. 머스켓
  루트 위치 커브를 추가하지 않았고, 총구 전방·방아쇠 하부 방향을 유지했습니다.
- 7번은 손목 두 점의 평균으로 머스켓을 재배치하던 보정을 제거했습니다. 수정된
  6번 마지막 프레임에서 샘플한 머스켓 루트의 로컬 위치·회전·배율과 양팔 조준
  본 자세를 그대로 시작 자세로 사용하고, 제공된 firing FBX의 발사 변화량만
  적용했습니다.
- 최종 12패널을 직접 확인했습니다. 열은 `6번 마지막`, `7번 시작`, `7번 중간`,
  `7번 끝`, 행은 `정면`, `좌측`, `우측`입니다. 모든 패널에서 앞손이 총열
  하부를 받치고 뒤손이 그립·방아쇠 쪽 총몸에 붙어 있으며, 총이 양손 위 허공에
  분리되지 않습니다. 개머리판은 오른쪽 상완·어깨를 관통하지 않고 아래·앞쪽으로
  빠지며, 총구 전방과 방아쇠 하부 방향도 유지됩니다.
- 최종 직접 시각 확인 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_FinalReview.png`

### 최종 저장본 독립 검사

- 6번 적용·저장: `PASS`, 다른 11개 슬롯 및 다른 씬 루트 변경 `False`
- 6번 저장본 검사: `PASS`, `GrabFrame=81`, `GrabTime=1.327044초`,
  `GrabDistance=0.04840654m`, `HandMusketFollowError=0`
- 7번 적용·저장: `PASS`, 다른 11개 슬롯 변경 `False`
- 7번 저장본 검사: `PASS`, 머스켓 방향 오차 `0°`, 로컬 장착 오차 `0`,
  오른손 가시 그립 최대 오차 `0.000000178969088m`, 왼손 지지 최대 오차
  `0.0167405121m`
- firing 클립 길이 `0.5초`, 재생 속도 `0.333333343`, 유효 반복 주기 `1.5초`
- 최종 캡처 전용 검사를 통과한 뒤 최종 캡처를 한 번 실행했고 씬 변경은
  `False`였습니다.

### 머스켓 양손 접촉 직접 시각 재검토

- 사용자 제공 영상
  `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS
  (6000.3.16f1) _DX12_ 2026-08-10 13-47-53.mp4`를 0.5초 간격 8개 프레임으로
  직접 확인했습니다. 머스켓이 양손 아래·앞쪽 허공을 가로지르며 손과
  접촉하지 않는 실패를 확인했고, 이전 완료 판정을 철회했습니다.
- 이전 최종 캡처는 이슈판트의 등 뒤를 보여 총이 정면으로 축약된 상태에서
  손과 총의 실제 접촉을 확인할 수 없었습니다. 수치 일치만으로 이를 통과한
  판정을 폐기했습니다.
- 6번 마지막 자세에서 오른손·왼손이 머스켓 메시의 어느 로컬 지점을
  잡는지를 직접 추출했습니다. 7번은 피벗이나 모델 원점이 아닌 이 두 실제
  잡기점을 양손 사이에 맞추도록 머스켓 루트 위치를 수정했습니다.
- 최종 Unity 비교 카메라를 얼굴·양손이 보이는 정면으로 전환했습니다. 7번
  시작·중간·끝 확대 이미지에서 머스켓의 갈색 몸체가 두 장갑 사이를
  지나고, 오른손은 방아쇠 손잡이 부위와 겹치며 왼손은 총열 아래 지지 부위와
  겹치는 것을 직접 확인했습니다. 머리 위나 가슴 앞 허공에 분리된 머스켓은
  없습니다.
- 영상 프레임과 최종 확대 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/source_video_134753_analysis/`,
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_HandGripCrop.png`

### 총구 정면·방아쇠 하부 방향 재수정

- 사용자가 지적한 대로 기존 최종 완료 판정을 철회하고, 6번 마지막 자세의
  머스켓 방향을 다시 기준으로 삼았습니다.
- 이전 검사는 양손 위치와 머스켓 로컬 장착만 비교해, 총구가 정면을 향하는지와
  방아쇠 부분이 아래를 향하는지를 놓쳤습니다. 이 판정을 폐기했습니다.
- 6번 마지막 프레임의 `Armature`, `Hips`, `Spine`, `Spine1`, `Spine2`,
  양쪽 어깨·팔·손 회전을 7번 조준 기준으로 삼고, 사용자 FBX의 발사 회전
  변화량만 더했습니다.
- Unity `AnimationMode`에서 6번 마지막 머스켓 방향은 7번을 샘플링하기 전에
  즉시 복사하도록 검사 순서를 수정했습니다. 다음 샘플링 시 이전 복제본이
  원래 자세로 복원되는 동작을 오인한 기존 `165.696884°` 오차 판정을 제거했습니다.
- 최종 결과는 6번 마지막 대비 머스켓 모델 기준 회전 오차 `0°`, 조준 자세
  본 회전 오차 `0°`입니다. 최종 5패널 비교에서 7번 시작·중간·끝의
  총구가 6번처럼 정면을 향해 축약되어 보이고, 방아쇠 부분은 아래를 향하며
  머스켓이 양손에 유지되는 것을 직접 확인했습니다.

### 사용자 영상 지적 후 재수정

- 사용자 제공 Unity 캡처 영상
  `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS
  (6000.3.16f1) _DX12_ 2026-08-10 11-24-41.mp4`를 0.5초 간격 대표
  프레임으로 확인해 7번의 잘못된 크기, 남은 등 뒤 머스켓, 늘어난 초승달,
  얼굴에서 떨어진 눈을 재현했습니다.
- 최초 구현은 정적 메시 자산만 공유하고 서로 다른 firing 리그에 스킨 본을
  이름으로 다시 연결해 실제 외형 동기화에 실패했습니다. 이 방식은 폐기했습니다.
- 수정본은 6번에 이미 적용된 정적 외형의 Mixamo용 변환본과 동일 리그를 7번에
  그대로 복제하고, 사용자 firing FBX의 `mixamo.com` Transform 커브만 해당 리그
  경로로 재타기팅합니다. 재타기팅 클립은
  `Ispant_Firing_Mixamo_Retargeted`이며 7번 AnimatorController 내부 서브에셋입니다.
- 7번 루트 배율은 동일 리그의 정적 외형 보정 기준인 6번과 같은
  `(1.08157158, 1.08157158, 1.08157158)`로 변경했습니다.
- 몸체는 정적 외형의 9개 서브메시 중 등 뒤 머스켓의 Wood·Steel 서브메시만
  제외해 이미 6번에서 사용 중인 7서브메시·3063정점 메시를 직접 공유합니다.
  별도 외형이나 메시를 생성하지 않았습니다.
- 초승달과 눈은 6번의 정상 스킨 메시·33본 바인딩을 그대로 사용합니다.
  firing 리그에 정적 24본 바인딩을 강제로 연결하거나 눈을 모델 루트에 두는
  기존 오류를 제거했습니다.
- 손 머스켓은 6번 마지막 양손 조준 상태의 오른손 자식 오브젝트, 로컬
  Transform, 메시, 머티리얼을 그대로 유지했습니다. 7번 시작 자세의 `Armature`와
  `Hips` 및 양쪽 어깨부터 손까지의 조준 본은 6번 마지막 자세를 정확히
  기준으로 삼고,
  사용자 FBX의 발사 회전 변화량만 더했습니다. 원본 클립의 본 크기 커브는
  대상 리그의 정적 외형 크기를 바꾸지 않도록 적용하지 않았습니다.

### 재수정 독립 검사

- 저장 장면 독립 검사: `PASS`, `SceneChanged=False`
- 모델 배율 기준 오차: `0`
- 표시 렌더러: 몸체·초승달·눈·손 머스켓 총 `4개`
- 등 뒤 머스켓 렌더러: `0개`
- 등 뒤 머스켓이 제외된 몸체 서브메시: `7개`
- 7번 시작 몸체 Bounds: `(0.9919622, 1.20507193, 0.9798211)`
- 7번 시작 초승달 Bounds: `(0.149320036, 0.141671181, 0.116418615)`
- 7번 시작 눈 Bounds: `(0.08434422, 0.027808547, 0.07187809)`
- 6번 마지막 자세 대비 머스켓 렌더러 로컬 행렬 오차: `0`
- 6번 마지막 자세 대비 머스켓 모델 기준 회전 오차: `0°`
- 머스켓 실제 잡기점 대비 오른손 전 구간 최대 접촉 오차: `0.000000302827374m`
- 머스켓 실제 잡기점 대비 왼손 전 구간 최대 접촉 오차: `0.0198316742m`
- 손 머스켓 애니메이션 전 구간 로컬 드리프트: `0`
- 6번 마지막 자세 대비 7번 시작 조준 자세 본 최대 회전 오차: `0°`
- 6번 마지막 자세 대비 7번 시작 왼손 지지점 오차: `0.000009956652m`
- 6번 마지막 자세 대비 7번 시작 오른손 잡기 오차: `0.00000599849m`
- 다른 11개 이슈판트 슬롯 변경: `False`
- 중간 시각 비교에서 손 머스켓이 떠 있거나 총구가 위로 향하는 오류를 발견해
  해당 방식을 폐기했습니다. 최종본은 6번 마지막 양손 조준을 기준 자세로
  고정한 후 발사 변화량만 적용했습니다. 최종 5패널 직접 비교에서 7번 시작·중간·끝
  모두 머스켓이 양손 조준에 유지되고, 등 뒤 머스켓·떠 있는 파편·초승달
  변형·얼굴에서 이탈한 눈이 없음을 확인했습니다. 패널은 각 개체 Bounds로 개별
  프레이밍되므로 화면상 픽셀 크기가 아닌 6번 보정 리그의 실제 배율을 크기
  기준으로 대조했습니다.
- 현재 검사 기록:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_Inspection.txt`
- 영상 대표 프레임:
  `docs/validation/ispant_firing_replacement_2026-08-10/source_video_analysis/`

### 적용 내용

- `CargoRunMvp`의 기존 7번 개체를 사용자 제공 원본
  `enemies model/išpant firing.fbx`의 프로젝트 사본
  `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_Firing.fbx`으로
  교체했습니다.
- 원본과 프로젝트 사본의 SHA-256은 모두
  `1EA7256EEEC66221BEF4A33994801145E76C6271EB705976EFA399DD79C26A6E`입니다.
- FBX에 내장된 단일 Mixamo 테이크를 `Ispant_Firing_Mixamo`로 설정해 반복
  재생하며, Animator 상태 속도는 원본의 정확히 `1/3`인 `0.333333343`입니다.
  원본 클립은 `0.5초`, 유효 반복 주기는 `1.5초`입니다.
- 7번 외형은 현재 Unity 정적 이슈판트를 6번 Mixamo 리그에 정확히 맞춘 기존
  `Ispant_Armed_Body`, `Ispant_Crescent_Ornament`,
  `Ispant_Reference_Eye_Slits` 메시와 머티리얼을 직접 공유합니다.
- 머스켓은 6번 마지막 양손 조준에서 사용한 오른손 로컬 장착과 동일하게
  `mixamorig:RightHand` 아래에 강체로 장착했습니다. 7번 애니메이션 전 구간의
  머스켓 로컬 장착 드리프트는 `0`입니다.
- 적용 대상 슬롯은
  `Approved Ispant Enemy Placement/Ispant_07_BreakthroughMusketAimFire`이며,
  다른 11개 이슈판트 슬롯은 변경하지 않았습니다.

### 구현 및 재현 도구

- `Assets/_Project/Editor/Validation/IspantFiringAnimationTool.cs`에 7번 적용,
  독립 검사, 최종 검토 캡처 기능을 추가했습니다.
- `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`에
  `ApplyIspant07FiringReplacement`, `InspectIspant07FiringReplacement`,
  `CaptureIspant07FiringReplacementReview` 명령을 연결했습니다.
- 애니메이션 샘플링은 실제 6번·7번 씬 개체가 아니라 임시 비저장 복제본에서만
  수행해 다른 슬롯의 리그 변환을 바꾸지 않도록 했습니다.

### 독립 검사와 직접 시각 확인

- 적용: `PASS`, `SceneSaved=True`
- 저장 장면 독립 검사: `PASS`, `SceneChanged=False`
- 내장 Mixamo 클립 수: `1개`
- 반복 재생: `True`
- 재생 속도: `0.333333343` (`1/3`)
- 정적 외형 공유 렌더러: `3개`
- 정적 메시·머티리얼 직접 공유: `True`
- 6번 마지막 자세 대비 머스켓 렌더러 로컬 행렬 오차: `0`
- 6번 마지막 자세 대비 머스켓 모델 기준 회전 오차: `0°`
- 오른손·왼손의 실제 메시 잡기점 접촉: 최종 정면 확대 이미지에서 직접 통과
- 머스켓 로컬 장착 최대 드리프트: `0`
- 6번 마지막 자세 대비 7번 시작 조준 자세 본 최대 회전 오차: `0°`
- 루트 모션: `False`
- 다른 11개 이슈판트 슬롯 변경: `False`
- 검사 통과 후 정적 외형, 6번 마지막 조준, 7번 시작·중간·끝을 비교하는 최종
  Unity 캡처로 외형·초승달·눈·머스켓 양손 조준을 직접 확인했습니다.
- 최종 검사 기록:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_Inspection.txt`
- 최종 통과 비교 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_FinalReview.png`
- 시작·중간·끝 양손 접촉 확대 이미지:
  `docs/validation/ispant_firing_replacement_2026-08-10/Ispant_07_Firing_HandGripCrop.png`

### 실행하지 않은 항목

- 원본 `enemies model/išpant firing.fbx` 수정
- 최종 양손 접촉 확인 이후 추가 Unity 캡처
- 이슈판트 7번 외 다른 슬롯 및 다른 장면 루트 수정
- 아트 샘플, 모델링·텍스처·머티리얼·VFX 생성 또는 수정
- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 작업
- EditMode·PlayMode 테스트와 Windows 빌드
- Unity 종료·재시작
- Git 스테이징·커밋·푸시
