# 2026-08-18 진행 기록

## 이슈판트 Unity 측 무단 가공 제거

### 원인

- 사용자 지정 원본: `D:/Bellerophon2/Bellerophon/enemies model/išpant-new.fbx`
- 원본 및 Unity 직접 복사본 SHA-256: `7B682C49CDB2F4A563B736857E544DD7FCCD7213A5C375DEB92F56CE3A2E51B7`
- Unity 직접 복사본: `Assets/_Project/Art/Enemies/Ispant/Models/Ispant_New_Direct_Source.fbx`
- FBX 내부 패킹 텍스처: `Material_1_basecolor`, 2048×2048, 6,767,337바이트
- FBX 내부 패킹 텍스처 SHA-256: `7DE6705FB7BD60E2D347023EFE51E96598845D611631F40D01C64EACC5249570`

Unity `ModelImporter`가 FBX 내부 텍스처 식별자 `texture_0`을 이름만 기준으로 재귀 검색하여 다른 적대 개체인 아타의 `Assets/_Project/Art/Enemies/Ata/Models/output.fbm/texture_0.png`에 연결했다. 이 교차 적대 개체 텍스처 연결 때문에 Unity의 이슈판트가 원본의 흰 장갑·갈색 장비 대신 적색·회색 조각 형태로 표시됐다. 씬의 12개 렌더러에는 별도 머티리얼 오버라이드가 없었으므로 문제는 FBX 임포터 내부 머티리얼 바인딩이었다.

### 정리 내용

- FBX에 패킹된 PNG를 `Assets/_Project/Art/Enemies/Ispant/Models/Textures/Ispant_New_Direct_Source_BaseColor.png`로 그대로 추출했다.
- 추출본 SHA-256이 FBX 내부 패킹 데이터와 정확히 일치함을 확인했다.
- 임포터 머티리얼 검색 범위를 `Local`, 이름 기준을 `BasedOnMaterialName`으로 제한했다.
- 실제 잘못 연결된 `texture_0` 식별자를 정확한 추출본으로 명시 재매핑했다.
- 애니메이션, Camera, Light 임포트를 비활성화했다. 원본 메시와 24본 리그는 변경하지 않았다.
- `Approved Ispant Enemy Placement`의 정적 개체 포함 12개 시각 루트를 원본 FBX 직접 인스턴스로 다시 배치했다.
- 슬롯 Transform과 비시각 컴포넌트 및 이슈판트 배치 밖 씬 루트는 보존했다.

2026-08-18 커밋에서 이전 세션이 생성한 것으로 확인된 다음 파생물을 제거했다.

- `Ispant_New_Source.fbx`
- `Ispant_New_CustomRig.fbx`
- `Ispant_New_MixamoRig.fbx`
- `Ispant_New_DeathRig.fbx`
- `Generated/Ispant_New_CustomRig_Mesh.asset`
- `Generated/Ispant_New_DeathRig_Mesh.asset`
- `Materials/Ispant_New_Model.mat`
- `Textures/Ispant_New_BaseColor.jpg`
- `Textures/Ispant_New_Normal.jpg`
- `Textures/Ispant_New_Metallic.png`
- `Textures/Ispant_New_Roughness.png`
- 위 에셋의 `.meta` 및 비어 있는 `Generated`, `Materials` 폴더
- 이전 재리깅·메시 매핑·머티리얼 가공·Play Mode 재생용 편집기 코드와 브리지 명령
- `docs/validation/ispant_model_replacement_2026-08-17/`의 이전 가공 검사 문서와 잘못된 직접 교체 결과 기록

### 저장 후 독립 검사

- 직접 FBX 인스턴스: 12개
- 잘못된 아타 텍스처 참조: 0개
- 머티리얼 오버라이드: 0개
- 이전 파생 에셋: 0개
- 임포트된 애니메이션 클립: 0개
- 활성 Animator 및 Animator Controller 연결: 0개
- 다른 씬 루트 변경: 없음
- 검사 중 씬 변경: 없음
- 결과: `PASS`

원인 기록은 `docs/validation/ispant_new_direct_replacement_2026-08-18/Ispant_Unity_Appearance_Diagnostic.txt`, 최종 상태는 `Ispant_Unity_Side_Cleanup_Inspection.txt`에 기록했다.

## 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업
- `artSample` 읽기·수정·사용
- Play Mode 및 캡처
- 원본 FBX 수정·재저장·변환
- 원본 모델·리그·웨이트·머티리얼·텍스처 임의 생성 또는 가공
- 기존 애니메이션/컨트롤러 재연결 및 새 애니메이션 생성
- 이슈판트 배치 밖 씬 루트 수정
- Git 변경·복원·커밋

## 이슈판트 장검 분리 검토 샘플

- 원본: `D:/Bellerophon2/Bellerophon/enemies model/išpant-new.fbx`
- 출력: `artSample/enemies/ispant/long_sword_separation/`
- 원본 `char1`은 4,980정점·10,028면의 단일 연결 요소라 Loose Parts 자동 분리는 불가능했다.
- 최초 샘플의 UV 섬 17개·215면 선택은 장검이 아니라 머스켓 일부였다는 사용자 지적을 확인해 해당 결과와 수치 기록을 폐기했다.
- 두 번째 UV 섬 19개·99면 결과는 검신만 포함하고 검자루가 빠진 불완전한 결과였다는 사용자 지적을 확인해 폐기했다.
- 세 번째 29개 UV 섬·242면은 하나로 이어지는 장검 표면으로 분리했다. 이후 칼자루로 추정해 추가한 17개 UV 섬·270면은 사용자 결손 이미지와 본 웨이트·연결 성분 진단 결과 실제 신체 면으로 확인됐다.
- 잘못 추가된 16개 섬·242면은 왼쪽 허리·골반 표면이고 `U530` 28면은 왼쪽 전완 표면이다. 이 270면을 장검 선택에서 제외하고 원본 그대로 본체에 복구했다.
- 현재 분리 결과는 본체 4,882정점·9,786면, 장검 143정점·242면이며 결손 상태보다 본체 정점 122개·면 270개가 복구됐다.
- 본체와 장검이 공유하던 45개 경계 정점은 분리 후 각각 복제됐으며, 46개 열린 경계 에지를 추측한 덮개 면으로 메우지 않았다.
- 좌표·UV·웨이트·면 방향·면별 머티리얼 배정의 결합 서명이 원본과 일치하고 검토용 FBX 재임포트 면 수도 일치했다.
- 정적 비교 렌더에서 원본, 잘못 제거된 신체 270면을 복구한 본체, 연속 장검 표면 242면을 한 장으로 확인했으며 손·전완·허리·골반 장갑과 머스켓은 본체에 남아 있다.
- `Ispant_LongSword_Separation_Comparison.html`에 비교 이미지, 교정된 분리 기준과 수치, 산출물 상대 링크와 Unity 적용 상태를 정리했다.
- 후속 `artSample/` 제작 시 같은 폴더에 요약 또는 비교 HTML을 함께 만드는 규칙을 `docs/agent-rules/04-art-and-modeling.md`에 추가했다.
- 이후 사용자 승인으로 Unity 직접 원본 FBX에 적용했으며, 해당 적용 결과는 아래 `이슈판트 Unity 배치 장검 제거` 절에 기록했다.
- 이 작업에서도 하네스 검증, EditMode/PlayMode 테스트, Windows 빌드, Git 작업은 실행하지 않았다.

## 이슈판트 분할 GLB 장검 10K PBR 샘플

- 분할 원본 `C:/Users/gus68/Downloads/išpant-segment.glb`에서 장검 전체인 `mesh_10`을 확인했다. 검신·가드·손잡이·폼멜이 하나의 연결 요소를 이룬다.
- 원본 장검은 47,052정점·94,104면이며 Decimate 비율 `0.212`로 9,975정점·19,950면까지 감량했다. 감량 후에도 연결 요소는 1개다.
- 분할 GLB에는 UV와 이미지 텍스처가 없고 `Color` 속성은 단일 세그먼트 표시색이므로 실제 재질로 사용하지 않았다.
- 사용자가 UV 원본으로 지정한 `D:/Bellerophon2/Bellerophon/enemies model/išpant-new.fbx`의 장검 전체 29개 UV 섬·242면을 정규화된 표면 대응 방식으로 전사했다.
- 제공된 Base Color·Metallic·Normal·Roughness PNG를 가공 없이 복사해 PBR 머티리얼에 연결했다. Base Color는 현재 Unity 이슈판트 직접 원본 텍스처와 SHA-256이 동일하다.
- GLB 루트의 축 변환을 삭제하기 전에 메시 정점에 베이크해 원본 경계 크기 `(0.075550, 0.023307, 0.007621)`를 보존했다.
- 출력은 `artSample/enemies/ispant/long_sword_10k/`의 Blender 파일, FBX, 텍스처 4종, 비교 PNG, README, 비교 HTML, 재현 스크립트다.
- FBX 재가져오기 결과는 9,975정점·19,950면·UV 레이어 1개이며 정점 분할 없이 원본 장검 전체 실루엣을 유지한다.
- Unity 에셋·씬·프리팹에는 적용하지 않았다. 애니메이션과 리그도 연결하지 않았다.
- `Run-HarnessValidation.ps1` 하네스 검증, EditMode·PlayMode 테스트, Windows 빌드, Git 작업은 실행하지 않았다.

## 이슈판트 분할 GLB 머스켓 8K PBR 샘플

- 분할 원본 `C:/Users/gus68/Downloads/išpant-segment.glb`의 15개 메시를 비교해 머스켓 전체가 `mesh_0`임을 확인했다. 개머리판·방아쇠/격발부·총열 끝이 하나의 연결 성분을 이루며 검과 신체·갑옷 조각은 포함하지 않는다.
- 원본 머스켓은 95,624정점·191,272면이다. Decimate 비율을 단계별로 비교한 뒤, 8,000 이하에서 가장 가까운 `0.0837`을 사용해 7,992정점·16,008면으로 감량했다. 감량 전후 연결 성분은 1개다.
- 사용자가 UV 원본으로 지정한 `D:/Bellerophon2/Bellerophon/enemies model/išpant-new.fbx`에서 머스켓 전체에 해당하는 UV 섬 47개·541면을 확인해 정규화된 표면 대응 방식으로 전사했다.
- 8개 주성분 축 부호 조합을 수치 비교해 양방향 표면 오차가 가장 낮은 `(1, 1, -1)`을 사용했다. 최종 UV 범위는 `(0.011100, 0.000870)`–`(0.999921, 0.995506)`이다.
- 제공된 Base Color·Metallic·Normal·Roughness PNG를 가공 없이 복사해 PBR 머티리얼에 연결했으며 각 SHA-256이 제공 원본과 일치한다.
- 출력은 `artSample/enemies/ispant/musket_8k/`의 Blender 파일, FBX, 텍스처 4종, 비교 PNG, README, 비교 HTML, 재현 스크립트다.
- 최종 비교 렌더에서 개머리판부터 총열 끝까지의 머스켓 전체와 감량 전후 실루엣, 제공 텍스처의 목재·금속 표현을 확인했다.
- FBX 재가져오기 결과는 7,992정점·16,008면·UV 레이어 1개·연결 성분 1개로 제작본과 일치한다.
- 제작 스크립트는 `ISPANT_MUSKET_8K_PASS`를 출력했다. Blender 종료 시 `0.000298 MB` 미해제 메모리 경고로 종료 코드 1이 반환됐지만 저장·재가져오기·렌더는 그 전에 완료됐다.
- Unity 에셋·씬·프리팹에는 적용하지 않았고 리그와 애니메이션도 연결하지 않았다.
- `Run-HarnessValidation.ps1` 하네스 검증, EditMode·PlayMode 테스트, Windows 빌드, Git 작업은 실행하지 않았다.

### Unity 배치 텍스처 대조 후 재작업

- 사용자 지시에 따라 `CargoRunMvp.unity`의 이슈판트 12개 배치를 직접 FBX GUID 기준으로 다시 확인했다. 모두 `Ispant_New_Direct_Source.fbx` 인스턴스이며 오버라이드는 Transform과 이름뿐이다. 렌더러 머티리얼 오버라이드, 추가·제거 컴포넌트는 0개다.
- Unity 임포터는 FBX 내부 `texture_0`을 `Ispant_New_Direct_Source_BaseColor.png`에 명시 연결한다. Unity 직접 FBX와 사용자 원본 `išpant-new.fbx`의 SHA-256이 동일하고, Unity Base Color와 샘플 Base Color의 SHA-256도 `7DE6705F...49570`으로 동일하다.
- 기존 샘플은 제공된 PBR 맵 4종을 모두 연결해 실제 Unity 배치 머티리얼과 달랐다. 이를 폐기하고 Unity 직접 FBX와 동일하게 Base Color 이미지 노드 1개, Metallic `1.0`, Roughness `1.0`, 추가 맵 연결 0개로 다시 구성했다.
- 비교용 Blender 복사본에서 Armature modifier를 제거하지 않아 월드 좌표가 이중 변형된 초기 진단 렌더는 판정에서 제외했다. 원본이나 Unity 파일에는 변형을 적용하지 않았다.
- 원본 머스켓 47개 UV 섬·541면과 감량 머스켓을 단순 PCA로만 정합했던 이전 방식에 15회 ICP 보정을 추가했다. 평균 표면 오차는 `0.043044 → 0.035161`, 95백분위 오차는 `0.131041 → 0.111005`로 감소했다.
- 최종 UV 범위는 `(0.011375, 0.001359)`–`(0.999921, 0.995176)`이며 UV 심을 포함한 정점별 좌표 합계는 8,062개다.
- FBX 재가져오기 결과는 7,992정점·16,008면·UV 1개·연결 성분 1개·머티리얼 1개·이미지 노드 1개다. 재가져온 Metallic과 Roughness도 각각 `1.0`이다.
- `Ispant_Musket_8K_Review.png`에 Unity 배치 전신의 실제 머스켓 텍스처와 최종 8K 결과를 나란히 배치했고, `Ispant_Musket_8K_UV_BeforeAfter.png`에 이전/보정/기각 결과를 정리했다.
- Unity 에셋·씬·프리팹은 읽기만 했고 수정하거나 저장하지 않았다. 하네스 검증, EditMode·PlayMode 테스트, Windows 빌드, Git 작업도 실행하지 않았다.

### 머스켓 UV 진단 구도 개선

- 기존 4열 진단은 서로 다른 실루엣과 작은 표시 크기 때문에 UV 전·후를 직접 판단하기 어려워 폐기했다.
- `Ispant_Musket_8K_UV_BeforeAfter.png`를 동일한 최종 7,992정점·16,008면 메시 두 개의 상하 구도로 다시 렌더했다. 위는 이전 PCA UV, 아래는 현재 ICP 보정 UV다.
- 두 행의 형상·방향·배율·카메라·조명을 고정하고 개머리판·격발부·총열/총구 기준점을 표시해 UV 차이만 바로 비교하도록 구성했다.
- `render_uv_diagnostic.py`로 진단 이미지를 재현할 수 있으며, 최종 FBX·Blend·UV·머티리얼은 변경하지 않았다.
- Unity 에셋·씬·프리팹은 수정하거나 저장하지 않았다. `Run-HarnessValidation.ps1` 하네스 검증, EditMode·PlayMode 테스트, Windows 빌드, Git 작업도 실행하지 않았다.

## 이슈판트 Unity 배치 장검 제거

- 사용자가 승인한 `artSample/enemies/ispant/long_sword_separation/`의 장검 전체 분리 결과를 Unity 직접 원본 `Assets/_Project/Art/Enemies/Ispant/Models/Ispant_New_Direct_Source.fbx`에 적용했다.
- 기존 재현 스크립트의 Unity 적용 모드는 현재 결손 FBX 해시를 확인하고 원본에서 신체 270면이 보존된 본체를 다시 출력하도록 교정했다. 장검 선택은 하나로 이어지는 29개 섬·242면, 경계 46개로 되돌렸으며 잘못 분류된 17개 신체 섬은 별도 상수로 기록해 장검 선택에서 제외한다.
- 최초 원본 SHA-256은 `7B682C49CDB2F4A563B736857E544DD7FCCD7213A5C375DEB92F56CE3A2E51B7`, 신체까지 제거된 결손 SHA-256은 `FB9E5A6E9A1E3F66CB9D2DA03C879090E7AFC9E3C84FC92B043C662B04FD1AEB`, 신체 복구 후 최종 SHA-256은 `95CFE22D341038B1A492457C8DD7E74BDAAF11A1102BBD89ECC037659F63D8AB`다.
- 최종 `char1` 본체는 4,882정점·9,786면·UV 1개·머티리얼 1개이며, Armature 24본과 기존 버텍스 그룹 22개를 유지한다. 결손 상태에서 122정점·270면이 복구됐고 장검 분리 개체는 143정점·242면이다.
- 새 FBX를 독립 재임포트해 `Armature + char1` 계층, Armature modifier, `Material_1`, 2048×2048 패킹 BaseColor가 유지되고 별도 장검 메시가 없는 것을 확인했다. 새 형상이나 덮개 면은 생성하지 않았다.
- `scripts/Refresh-UnityProject.ps1`로 열린 Unity 6000.3.16f1 프로젝트에 변경을 반영했고, Editor 로그에서 GUID `b82e186869158b94f9a618b8613f7bbc`의 FBXImporter 재임포트 완료를 확인했다.
- `CargoRunMvp.unity`의 해당 `m_SourcePrefab` 배치는 12개다. FBX `.meta` SHA-256 `0D3D124DC3ECABD12835F08D699808274FEC265CAF76849E6B774C553AC42738`과 씬 SHA-256 `F7064E9D68103AEFE63D94402136959AF31F6983EAF9675D6A2DBD1055EB2F2A`는 작업 전후 동일하므로 GUID·배치·Transform·씬 저장 상태는 바뀌지 않았다.
- Unity 화면의 최종 단일 캡처를 시도했으나 Windows 포커스 제한으로 Unity가 아닌 다른 창이 캡처되어 판정 자료로 사용하지 않고 즉시 삭제했다. 반복 캡처는 하지 않았으며, Unity 시각 확인 완료로 기록하지 않는다.
- 머스켓·정상 신체·갑옷·리그·머티리얼·텍스처는 변경하지 않고 잘못 제거된 원본 신체 면만 복구했다. 애니메이션은 연결하거나 복구하지 않았다. `Run-HarnessValidation.ps1` 하네스 검증, EditMode·PlayMode 테스트, Windows 빌드, Git 작업은 실행하지 않았다.

### 왼쪽 다리 대칭 결손 복구

- 장검 제거 후 모션 자세에서 드러난 왼쪽 허벅지 안쪽 결손을 사용자 이미지로 확인했다. 원본 FBX와 장검 제거 FBX의 차이는 장검 29개 UV 섬·242면뿐이며, 원본에도 장검 뒤를 덮는 왼쪽 다리 표면은 존재하지 않았다.
- 오른쪽 다리 원본 1,999면을 왼쪽 다리 기존 2,188면과 비교했다. 좌우 `UpLeg`, `Leg`, `Foot` 본의 head/tail 중점으로 계산한 대칭축은 로컬 `X=-0.691260815`다.
- 장검 경계 범위 안에서 오른쪽 다리를 반전한 면의 중심과 기존 왼쪽 다리 표면 사이 거리를 계산했다. 거리 `2.5`를 넘는 후보는 하나의 연결 성분 12면뿐이며, 반전 후 경계는 `(22.8897, 70.3883, -2.5604)`–`(29.4941, 93.4111, 11.5758)`로 사용자 이미지의 왼쪽 허벅지 상단 결손과 일치했다.
- 복구 원본은 오른쪽 다리의 면 인덱스 `1304, 1382, 1401, 1423, 1428, 1431, 1494, 1537, 1587, 1681, 1688, 1787`이다. 이 12면·13정점을 대칭축에서 그대로 반전했고, UV와 머티리얼 면 배정은 보존했다. `Right*` 본 웨이트 이름만 대응하는 `Left*` 그룹으로 치환했다.
- 최종 본체는 4,895정점·9,798면이다. 장검 제거량은 143정점·242면으로 유지되고, 원본에서 복구한 허리·골반·왼쪽 전완 270면도 그대로 보존된다.
- 검토용 FBX 재가져오기와 Unity용 임시 FBX 재가져오기에서 복구 패치 12면의 좌표·UV·웨이트·면 방향 서명을 다시 비교해 일치함을 확인했다. Armature 24본, 버텍스 그룹 22개, UV 1개, 머티리얼 1개도 유지된다.
- Unity 직접 FBX 최종 SHA-256은 `E5A1067392ACE8009782E886202C4F2670B57741DC8A33BDA7D06410FFC71A5F`다. `scripts/Refresh-UnityProject.ps1`을 통해 열린 Unity 에디터에 반영했다.
- `.meta` SHA-256 `0D3D124DC3ECABD12835F08D699808274FEC265CAF76849E6B774C553AC42738`, `CargoRunMvp.unity` SHA-256 `F7064E9D68103AEFE63D94402136959AF31F6983EAF9675D6A2DBD1055EB2F2A`, 동일 GUID 배치 12개는 작업 전후 동일하다. 씬과 Transform은 수정하거나 저장하지 않았다.
- `Run-HarnessValidation.ps1` 하네스 검증, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`, Git 작업은 실행하지 않았다.

## 승인 장검 10K Unity 왼쪽 허리 배치

- 사용자 승인 대상은 `artSample/enemies/ispant/long_sword_10k/Ispant_LongSword_10K_Textured.fbx`이며 SHA-256 `2A6ECA3E5CD74C2E40B03BA5B2D4FD51E2B1E73B7BA2431B7E9D4BE5C6579252`, 9,975정점·19,950면·UV 1개·머티리얼 1개다.
- 원본 Ispant에서 제거했던 기존 장검 29개 UV 섬·242면을 배치 참조로 사용했다. 승인 장검과 참조 장검의 전체 표면을 네 회전 후보와 30회 최근접 표면 정합으로 비교했다.
- 최근접점 ICP의 배율 수축 편향을 배제하기 위해 배율은 두 장검의 주축 길이 비율 `12.345491599084`로 고정하고 회전·이동만 보정했다. 최종 배치 경계 중심은 기존 장검 경계 중심과 같고, 양방향 평균 표면 오차는 기존 장검 길이의 `0.023435707`이다.
- 승인 장검은 `Ispant_Approved_LongSword_10K` 별도 메시로 Armature 루트 아래에 배치했다. 승인된 형상·UV·BaseColor·Metallic·Normal·Roughness를 유지했으며 버텍스 그룹, Armature modifier, 애니메이션은 연결하지 않았다.
- 배치 통합 FBX는 몸체 4,895정점·9,798면, 승인 장검 9,975정점·19,950면, Armature 24본이다. FBX 재가져오기 최대 좌표 오차는 몸체 `0.000000224`, 장검 `0.000000261`이며 토폴로지·면 방향·UV·몸체 웨이트·머티리얼 구조가 일치했다.
- `Ispant_LongSword_10K_UnityPlacement.blend`, `.fbx`, 배치 비교 PNG와 `deploy_approved_long_sword.py`를 승인 장검 샘플 폴더에 추가하고 기존 요약 HTML·README에 Unity 적용 상태를 반영했다.
- 배치 비교 PNG는 렌더용 원본 객체 숨김 누락으로 원본이 왼쪽·중앙에 한 번 중복됐다. 중앙 기존 원본과 오른쪽 승인 장검 결과로 위치를 비교했으며 최종 캡처 1회 제한에 따라 반복 렌더하지 않았다. 재현 스크립트에는 원본 숨김 처리를 교정했다.
- 검증된 배치 FBX를 `Assets/_Project/Art/Enemies/Ispant/Models/Ispant_New_Direct_Source.fbx`에 적용했고 최종 SHA-256은 `465305938E6A5760E2352636436C4A50B311032B9587471B2DBE20DF9CA03922`다.
- `scripts/Refresh-UnityProject.ps1`로 열린 Unity 에디터에 반영했다. `.meta` SHA-256 `0D3D124DC3ECABD12835F08D699808274FEC265CAF76849E6B774C553AC42738`, `CargoRunMvp.unity` SHA-256 `F7064E9D68103AEFE63D94402136959AF31F6983EAF9675D6A2DBD1055EB2F2A`, 기존 GUID 참조 12개는 작업 전후 동일하다.
- `Run-HarnessValidation.ps1` 하네스 검증, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`, Git 작업은 실행하지 않았다.

### 승인 장검 재교체 및 Unity PBR 텍스처 연결

- 사용자가 현재 이슈판트의 장검이 텍스처 없이 추가됐다고 확인해 원인을 다시 조사했다.
- 승인 원본 `Ispant_LongSword_10K_Textured.fbx`와 당시 Unity 직접 FBX 모두 장검 9,975정점·19,950면·UV 1개와 패킹된 Base Color·Metallic·Normal·Roughness를 보유했지만, Unity `ModelImporter.externalObjects`에는 본체의 `texture_0`만 있고 장검 PBR 머티리얼 재매핑이 없었다.
- 현재 Unity FBX에서 기존 `Ispant_Approved_LongSword_10K` 객체를 제거하고 승인 FBX에서 장검을 새로 임포트했다. 위치는 기존 장검 29개 UV 섬·242면의 왼쪽 허리 배치 정합 결과를 그대로 사용했으며 새 형상·UV·텍스처를 추측하거나 생성하지 않았다.
- 최종 통합 FBX는 몸체 4,895정점·9,798면, 승인 장검 9,975정점·19,950면, Armature 24본이다. FBX 재가져오기 최대 좌표 오차는 몸체 `0.000000238`, 장검 `0.000000179`다.
- 최종 `Ispant_LongSword_10K_UnityPlacement.fbx`와 `Assets/_Project/Art/Enemies/Ispant/Models/Ispant_New_Direct_Source.fbx`의 SHA-256은 모두 `5CE54F6117AF08F141BC18A0E46C823AD07877D815DA2906D59CA2967A4974FF`다.
- 승인 텍스처 4종을 `Assets/_Project/Art/Enemies/Ispant/Models/Textures/Ispant_LongSword_10K/`에 원본 바이트 그대로 복사했다. Base Color·Metallic·Normal·Roughness SHA-256은 각각 `7DE6705F...49570`, `674812FC...920D`, `11F5A825...C07`, `45B468DC...9432`로 승인 샘플과 일치한다.
- `Assets/_Project/Art/Enemies/Ispant/Models/Materials/Ispant_LongSword_10K_PBR.mat`을 추가해 기존 `Bellerophon/Ispant/ApprovedAppearance` 셰이더의 `_BaseMap`, `_MetallicMap`, `_NormalMap`, `_RoughnessMap`에 승인 PNG를 각각 연결했다. Normal Strength는 승인 Blender 샘플 기본값 `1`이다.
- FBX 내부 머티리얼 이름 `Ispant_LongSword_10K_PBR`을 위 Unity 머티리얼에 외부 재매핑했다. Base Color는 sRGB, Metallic·Roughness는 선형, Normal은 노멀맵으로 임포트한다.
- 독립 Blender 재가져오기에서 최종 장검의 토폴로지·UV·패킹 텍스처 4종 해시가 승인 FBX와 모두 일치했다.
- `scripts/Refresh-UnityProject.ps1`로 열린 Unity 프로젝트에 반영했다. FBX GUID `b82e186869158b94f9a618b8613f7bbc`, `CargoRunMvp.unity`의 `m_SourcePrefab` 참조 12개, 씬 SHA-256 `F7064E9D68103AEFE63D94402136959AF31F6983EAF9675D6A2DBD1055EB2F2A`는 유지됐다.
- 이슈판트 애니메이션과 씬은 수정하거나 저장하지 않았다. `Run-HarnessValidation.ps1` 하네스 검증, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`, Git 작업은 실행하지 않았다.

### 승인 장검 Unity 외관 직접 대조 교정

- 사용자 제보 캡처를 직접 확인한 결과, Unity 장검은 승인 `Ispant_LongSword_10K_Review.png` 및 배치 리뷰의 밝은 은색 PBR 외관과 달리 거의 검게 표시됐다. 기존 완료 기록의 구조·해시 확인만으로 시각 동기화를 판정한 것은 잘못이므로 해당 완료 판정을 폐기했다.
- 승인 메시·UV·Base Color·Metallic·Normal·Roughness 자체는 Unity FBX와 전용 텍스처 경로에 유지돼 있었다. 직접 원인은 장검이 본체용 `Bellerophon/Ispant/ApprovedAppearance` 셰이더를 공유하면서, 현재 Unity 씬에 승인 Blender 검토본의 중립 스튜디오 반사 환경이 없어 고금속 표면이 어둡게 표시된 것이었다.
- 장검에만 적용되는 `Bellerophon/Ispant/LongSwordApprovedPBR` 셰이더를 추가했다. 기존 네 맵을 그대로 샘플링하고 Normal Strength `1`을 유지하며, 승인 Blender의 중립 스튜디오 반응을 금속 영역에만 보완한다. 텍스처 픽셀·채널·UV·메시·배치는 변경하지 않았다.
- Unity 프로젝트 갱신 뒤 다른 창과 섞이지 않도록 HWND `68058`의 Unity 창만 `PrintWindow`로 캡처했다. 1차와 2차 결과를 직접 보고 승인본보다 어둡다고 기각한 뒤 중립 반사 강도만 조정했다.
- 최종 `Current_Unity_Final.png`와 승인 `Ispant_LongSword_10K_UnityPlacement_Review.png`, 사용자 제보 이미지를 `Approved_Before_Current_Visual_Comparison.png`에 나란히 배치해 직접 확인했다. 검은 단색 검신이 은회색 금속 면과 어두운 장식 문양이 구분되는 상태로 복구됐다.
- 특정 Unity 창 캡처의 Console 카운터에는 오류 `0`이 표시됐다. 화면에 표시된 기존 그림자 아틀라스 경고는 이 작업에서 수정하지 않았다.
- 검증 자료: `docs/validation/ispant_long_sword_visual_sync_2026-08-18/`.
- `Run-HarnessValidation.ps1` 하네스 검증, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`, Git 작업은 실행하지 않았다.

## 이슈판트 신규 수직 대기 모션

- 사용자 지시에 따라 기존 `Ispant_02_Idle.anim`과 `Ispant_Idle.fbx`는 참고만 하고 연결·복사·변형하지 않았다. 기존 도구 `IspantIdleAnimationTool.cs`도 참고 상태로 보존했다.
- 현재 직접 FBX 배치 구조에 맞춘 별도 `IspantVerticalIdleAnimationTool.cs`를 추가했다. 새 변수 `DurationSeconds`, `PeakOffsetMeters`, `TotalVerticalTravelMeters`는 각각 새 모션의 2초 주기, 기준점에서 위·아래 7.5mm, 총 1.5cm 이동 범위를 정의한다.
- 새 `Ispant_02_Idle_VerticalLoop_New.anim`은 `Ispant_New_Direct_Model/m_LocalPosition.y` 하나에만 새로 작성한 다섯 키를 가진다. 기준 → 7.5mm 위 → 기준 → 7.5mm 아래 → 기준 순서이며 2초마다 반복한다.
- 새 `Ispant_02_Idle_VerticalLoop_New.controller`는 새 클립 하나만 참조한다. 기존 대기 클립 GUID는 포함하지 않는다.
- `CargoRunMvp.unity`의 `Approved Ispant Enemy Placement/Ispant_02_Idle`에만 Animator를 추가해 새 Controller를 연결했다. `ApplyRootMotion=False`, `AlwaysAnimate`이며 슬롯 Transform과 다른 11개 이슈판트 슬롯 및 다른 씬 루트는 유지됐다.
- Unity AnimationMode 라이브 미리보기로 새 클립을 직접 반복 재생했다. 중앙 대상만 확대하고 고정 Y 기준선을 표시한 `docs/validation/ispant_idle_motion_2026-08-18/Ispant_Idle_LiveLoop_VisualReview.png`를 직접 확인해 자세·좌우 위치를 유지한 미세한 상하 이동을 판정했다.
- 종료 시점의 직접 미리보기 완료 횟수는 55주기였고, AnimationMode 종료 후 편집 자세를 복구했다.
- `Run-HarnessValidation.ps1` 하네스 검증, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`, Git 작업은 실행하지 않았다.

### 발 고정·무릎 굽힘 호흡 모션 교정

- 사용자 확인에 따라 모델 루트 전체를 움직이던 기존 신규 커브를 폐기하고, 현재 직접 FBX의 `Hips` 본에만 새 5키 수직 호흡 커브를 다시 작성했다. 기존 `Ispant_02_Idle.anim`, `Ispant_Idle.fbx`의 키·본 커브는 복사하거나 연결하지 않았다.
- FBX 상위 Transform의 축과 스케일을 역변환해 월드 기준 `±0.0075m`, 총 `0.015m` 수직 이동이 되도록 `Hips` 로컬 XYZ 커브를 생성했다. 주기는 기존 사용자 승인 범위인 2초다.
- `Approved Ispant Enemy Placement/Ispant_02_Idle` 아래에 `Ispant_New_GroundedBreathing_Rig`을 추가했다. 좌우 발의 초기 위치·회전을 슬롯 기준 목표점으로 고정하고, 좌우 `UpLeg/Leg/Foot`에 Two Bone IK와 무릎 힌트를 각각 연결했다.
- 전용 검사에서 몸통 수직 이동 `0.0149999857m`, 양발 최대 위치 오차 `0.0000040954m`, 왼쪽 무릎 회전 변화 `0.898739159°`, 오른쪽 `2.11479831°`를 확인했다.
- Unity AnimationMode와 RigBuilder 수동 그래프를 함께 사용해 12주기 반복 재생했다. 기즈모를 제거한 전신 5위상과 하체 확대 이미지를 직접 확인해 발끝·발바닥이 같은 바닥 격자 위치에 붙어 있고, 골반·상체의 미세한 상하 이동에 맞춰 양 무릎 굽힘이 달라지는 것을 판정했다.
- 직접 검토 자료는 `docs/validation/ispant_idle_motion_2026-08-18/Ispant_Idle_LiveLoop_VisualReview.png`와 `Ispant_GroundedBreathing_KneeFoot_VisualReview.png`다.
- 적용·검사·라이브 반복은 각각 `ApplyIspantIdleAnimation`, `InspectIspantIdleAnimation`, `StartIspantIdleReviewPlayback`, `StopIspantIdleReviewPlayback` 브리지 명령으로 수행했다.
- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 작업, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`, 발 위치 이동, 루트 모션, 다른 이슈판트 슬롯·씬 루트 수정, Git 작업은 실행하지 않았다.

## 이슈판트 내장 이동 모션 소스 조사

- 사용자 지시에 따라 현재 직접 모델 `Assets/_Project/Art/Enemies/Ispant/Models/Ispant_New_Direct_Source.fbx`의 내장 애니메이션과 무기 메시 분리 상태를 Unity ModelImporter와 프리팹 기준으로 읽기 전용 조사했다.
- 현재 임포터는 `ImportAnimation=False`, 기본 Take `0`, 임포트된 AnimationClip `0`이다. 따라서 이 모델 자체에는 반복 연결할 내장 이동 애니메이션이 없다.
- 렌더러는 `Armature/Ispant_Approved_LongSword_10K`의 별도 MeshRenderer와 `char1` SkinnedMeshRenderer 두 개다. 장검은 독립 강체 메시지만, 머스켓은 별도 렌더러 없이 `char1` 스킨 메시 내부에 포함돼 있다.
- 그러므로 현재 모델만으로는 "내장 이동 애니메이션 반복"과 "머스켓 무변형 강체 추종"을 동시에 구현할 수 없어 임의 애니메이션 선택이나 메시 분리 없이 중단했다.
- `enemies model/išpant walking.fbx`에는 과거 이동 Take가 있으나 현재 모델의 내장 애니메이션이 아니므로 이번 승인 범위에서 사용하지 않았다.
- 진단 도구는 `IspantEmbeddedMoveAnimationTool.cs`, 브리지 명령은 `InspectIspantEmbeddedMoveSource`, 결과 로그는 `docs/validation/ispant_move_2026-08-18/Ispant_EmbeddedMove_SourceInspection.log`다.
- 조사 과정에서 씬, FBX 임포터, 모델, 애니메이션 에셋은 변경하지 않았다.
- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 작업, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`, 외부 이동 클립 적용, 머스켓 메시 분리·가공은 실행하지 않았다.
