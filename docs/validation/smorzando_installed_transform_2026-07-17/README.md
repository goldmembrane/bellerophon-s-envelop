# Smorzando 설치형 → 인간형 변환 모션 기록

## 추가 보정 판정 - 양초 찰흙 압축 통과

- 사용자 설명인 “고무찰흙을 양옆에서 누르는 느낌”에 맞춰 설치형 양초의 폭·깊이 감소와 세로 증가를 같은 압축률에서 계산한다.
- 양초 본체는 기준 반경 `0.56`, 바닥 왁스는 기준 반경 `0.44`까지 안쪽으로 눌린다. 반경 감소가 큰 정점일수록 바닥 기준 세로 배율이 커지며 최하단 정점은 바닥에 고정된다.
- `0.65→0.8→1.0→1.1→1.2초` 캡처에서 넓은 바닥 왁스와 양초 본체가 점차 좁아지는 동시에 기둥이 위로 길어지는 것을 정면·사선에서 확인했다.
- 첫 적용에서는 `1.34초`까지 넓은 기둥이 남아 `1.35초`에 갑자기 사라졌다. `1.17~1.35초` 추가 수렴 구간을 적용해 최종 반경을 `0.08`까지 줄이고 높이를 계속 늘였다.
- 최종 `1.3→1.34→1.35초` 캡처에서는 기둥이 인간형 덩어리 중심의 가는 줄기로 흡수된 뒤 사라져 인계 팝이 보이지 않는다.
- 후반 인간형 조형과 최종 자세는 보정 전과 동일하다. 정면·사선 `3.0/3.5/3.95초` 해시도 각각 기존 값과 일치한다.
- 적용 대상은 `Smorzando_Installed_03` 한 개체이며 다른 Transform과 원본 FBX는 변경하지 않았다.

### 찰흙 압축 확인 자료

- 원형 유지 구간: `automated_visual_capture/Smorzando_Transform_Front_065.png`
- 압축 시작: `automated_visual_capture/Smorzando_Transform_Front_080.png`
- 압축·신장 중간: `automated_visual_capture/Smorzando_Transform_Front_100.png`, `Smorzando_Transform_Front_110.png`
- 높아진 기둥: `automated_visual_capture/Smorzando_Transform_Front_120.png`
- 중심 흡수: `automated_visual_capture/Smorzando_Transform_Front_130.png`, `Smorzando_Transform_Front_134.png`
- 인간형 덩어리 인계: `automated_visual_capture/Smorzando_Transform_Front_135.png`

## 최종 판정 - 통과

- 검토 대상: `Approved Smorzando Enemy Placement/Smorzando_Installed_03`
- 전체 주기: 변환 `3초` + 최종 유지 `1초` = 반복 `4초`
- 최종 구현은 별도 작은 좀비나 사라지는 외피를 사용하지 않는다. Unity 최종 Bake와 같은 `14,193`개 정점이 바닥 왁스 집결부터 최종 인물 위치까지 직접 변형된다.
- `0.5초` 불꽃 소멸, `0.65초` 잔광 종료, `1.0초` 굽은 왁스 기둥 상승, `1.2초` 사람 높이 접근, `1.35초` 한 덩어리 집결, `1.55~2.85초` 연속 조형, `3.0초` 최종 인물 완성 순서가 정면·사선 캡처에서 이어진다.
- 집결 단계는 높이별 나선 꼬임을 사용하고 authored normal도 같은 각도로 회전시킨다. 따라서 작은 완성 인물이 균등 확대되는 인상과 꼬임 중 회색 법선 단절을 함께 제거했다.
- 큰 역권선 성분 `3,201`개 정점은 외형에 필요한 표면이므로 삭제하지 않고 권선만 반전했다. Unity 적용 시 최종 인물 Bake의 authored normal을 정점 수 일치 조건 아래 복사해 몸통·팔다리의 검은 찢김과 구멍처럼 보이던 조명 단절을 제거했다.
- 프록시와 최종 인물은 동일한 `Smorzando_Person_Reference` 재질을 사용한다. `2.85`, `2.95`, `2.99`, `3.0초` 프레임은 형상과 조명이 연속이며 렌더러 교대 팝이 육안으로 보이지 않는다.
- 최종 비교 이미지 `automated_visual_capture/Smorzando_Transform_FinalVsReference.png`에서 왼쪽 최종 결과와 오른쪽 `Smorzando_Person_01`의 형상, 크기, Y축 방향, 발의 바닥 정렬이 일치한다.
- 최종 유지 해시:
  - 정면 `3.0/3.5/3.95초`: `874C2814A11521BFC058F573307B2C47765BF8BAA3AE8FBF31C714DE3924477F`
  - 사선 `3.0/3.5/3.95초`: `509EBBD187FFF526297880ECE8A55A76368B688FB0E30C521473CEEFD88093A0`
- 적용 보고: `Duration=3s`, `FinalHold=1s`, `Cycle=4s`, `ReferencePoseMatched=True`, `OtherTransformsChanged=False`, `SelectionCleared=True`.
- 캡처 보고: `SceneViewFocused=False`, `SceneSaved=False`, `SelectionCleared=True`. 추가 광원 그림자 아틀라스 해상도 축소 경고 외 실패는 없다.
- 원본 `Assets/_Project/Art/Enemies/Smorzando/Models/Smorzando_Person.fbx`는 수정하지 않았으며 SHA-256은 `CDE712400B52965CF0BE0E3D32FB5B59D9349C8C24F777CE7EEFA97233C75BF9`다.
- 사용자 지시에 따라 별도 아트 샘플은 제작하지 않았다.

### 최종 시각 자료

- 집결 시작: `automated_visual_capture/Smorzando_Transform_Front_100.png`
- 사람 높이 접근: `automated_visual_capture/Smorzando_Transform_Front_120.png`
- 집결 완료: `automated_visual_capture/Smorzando_Transform_Front_135.png`
- 조형 중간: `automated_visual_capture/Smorzando_Transform_Front_180.png`, `Smorzando_Transform_Front_240.png`
- 전환 직전: `automated_visual_capture/Smorzando_Transform_Front_285.png`, `Smorzando_Transform_Front_295.png`, `Smorzando_Transform_Front_299.png`
- 최종 및 유지: `automated_visual_capture/Smorzando_Transform_Front_300.png`, `Smorzando_Transform_Front_350.png`, `Smorzando_Transform_Front_395.png`
- 기준 비교: `automated_visual_capture/Smorzando_Transform_FinalVsReference.png`

## 이전 미통과 기록 - 무터널 단일 폐곡면과 안정적인 중간 형상

> 후속 세션용 작업 메모다. 새 작업 승인 전에는 아래 항목을 실행하지 않는다.

1. 오일러 특성값 `-10`, 추정 관통 터널 `6`개인 현재 표면을 오일러 특성값 `2`인 genus `0` 단일 폐곡면으로 재구성한다.
2. 분리된 Bake 부품으로 직접 제한 투영해 터널과 자기 교차가 다시 생기는 방식을 피하고, 최종 좀비 실루엣 방향으로 수렴하는 무터널 표면을 만든다.
3. 경계·비다양체 엣지 `0`, 외향 signed volume 양수, Bounds 일치를 내부 확인한다.
4. 하나의 보이는 표면이 직접 빚어지는 구조를 유지하고 별도 좀비형 확대나 외피 축소 방식으로 되돌리지 않는다.
5. 새 승인을 받은 뒤 `Smorzando_Installed_03` 한 개체에만 적용하고 최종 자동 캡처 한 번으로 다시 판정한다.

현재 중단 상태:

- 파생 표면: `5,838`정점·`11,696`삼각형, 경계 엣지 `0`, 비다양체 엣지 `0`, 오일러 특성값 `-10`, 추정 터널 `6`개.
- 권선: Blender 외향 signed volume `+0.0834070423`, Unity 입력 순서 보존.
- Unity Bounds: 높이·폭·깊이 비율 `1`, 중심 오프셋 `0`.
- 최신 캡처: `1.35~2.85초`에 큰 구조적 공백과 늘어난 삼각형이 남고 `3.00초`에 완성 좀비형으로 불연속 교체됨.
- 판정: 시각 검증 미통과, 완료 아님.

## 2026-07-18 권선·세분화·제한 투영 보정 - 최신 시각 검증 미통과

- 사용자가 이번 작업에 한해 `artSample/` 선행 제작을 생략하도록 직접 승인했다.
- Unity↔Blender 축 변환은 권선을 보존하므로 생성기의 입력·출력 삼각형 반전을 제거했다.
- Blender에서 외향 법선을 재계산하고 signed volume 양수를 강제했다.
- 파생 표면을 한 단계 세분화하고 각 정점의 투영 이동을 주변 평균 엣지 길이 이내로 제한했다.
- 결과는 `5,838`정점·`11,696`삼각형, 경계 엣지 `0`, 비다양체 엣지 `0`, 오일러 특성값 `-10`, signed volume `+0.0834070423`이다.
- 평균 투영 거리는 `0.0187726396`, 최대 투영 거리는 `0.147019416`이다.
- Unity 적용 보고에서 높이·폭·깊이 비율 `1`, 중심 오프셋 `0`, `OtherTransformsChanged=False`, `SelectionCleared=True`를 확인했다.
- 원인 분리를 위해 런타임 반경 보존과 외향 법선 팽창값은 변경하지 않았다.
- 최종 자동 캡처에서 이전의 전체 권선 반전으로 인한 큰 회색 뒷면은 줄었지만, 팔·몸통·골반·다리 사이의 구조적 공백과 늘어난 삼각형은 `1.35~2.85초`에 계속 남았다.
- 폐곡면이 manifold여도 오일러 특성값 `-10`에 해당하는 약 `6`개의 관통 터널이 남아 있어, 변형 과정에서 이 터널이 크게 벌어지는 것이 현재 남은 구조적 원인이다.
- `2.85초` 파생 표면과 `3.00초` 완성 모델 사이의 불연속 전환도 제거되지 않았다.
- 정면 `3.00·3.50·3.95초`와 사선 `3.00·3.50·3.95초`는 각 뷰별 해시가 동일해 최종 자세 1초 유지는 정상이다.
- 캡처 기록은 `SceneViewFocused=False`, `SceneSaved=False`, `SelectionCleared=True`다.
- 원본 `Smorzando_Person.fbx` SHA-256은 작업 전후 `CDE712400B52965CF0BE0E3D32FB5B59D9349C8C24F777CE7EEFA97233C75BF9`로 동일하다.
- Blender는 출력 파일을 저장하고 종료 코드 `0`을 반환했지만, 종료 시 Auto-Rig Pro 확장 정리 핸들러 오류를 별도로 출력했다.
- 승인된 최종 캡처 한 번을 사용했으므로 추가 수정·재캡처는 하지 않았다. 최신 결과는 완료 또는 사용자 승인 상태가 아니다.

## 2026-07-18 폐곡면 정리 2차 - 최신 시각 검증 미통과

- 기존 `11,232`정점 연결 표면은 경계 엣지는 없었지만 오일러 특성값이 `-252`로, 시각적 관통 터널이 약 `127`개 남아 있었다.
- Blender Voxel Remesh를 `0.015 → 0.030`으로 높이고 `0.040` 정리 패스를 추가해 파생 표면을 `1,452`정점·`2,924`삼각형으로 다시 만들었다.
- 새 표면은 경계 엣지 `0`, 비다양체 엣지 `0`, 오일러 특성값 `-10`으로 관통 터널이 약 `6`개까지 줄었다.
- 정리 뒤 축별 Bounds를 Bake 포즈에 재정렬했고 Unity 적용 기록에서 높이·폭·깊이 비율 `1`, 중심 오프셋 `0`을 확인했다.
- 런타임 반경 보존을 `0.72 → 0.90`으로 높이고 표면 흔들림과 지역 지연을 줄여 강한 압축을 완화했다.
- 자동 정면·사선 캡처에서 이전의 전신 미세 구멍은 줄었지만 `1.35~2.85초`에 큰 회색 뒷면과 팔·골반·다리의 찢어진 형상이 남았다.
- `2.85초`의 거친 파생 표면에서 `3.00초`의 완성 좀비형으로 여전히 불연속적으로 교체되어 사용자 목표를 충족하지 못한다.
- 권선 수치 비교에서 최종 Bake 포즈는 외향 판정 `62.43%`, 방향 부호 합 양수였지만 파생 표면은 외향 판정 `37.45%`, 방향 부호 합 음수였다.
- Blender 출력 좌표 변환에서 삼각형 순서가 반전되어 파생 표면 대부분이 Unity에서 뒷면으로 들어온 것이 회색 면과 잘못된 법선 방향 팽창의 직접 원인으로 판단된다.
- 승인된 자동 캡처 1회를 사용했으므로 추가 수정·재캡처는 수행하지 않았다. 현재 상태는 시각 검증 미통과다.
- 최종 정면 및 사선 `3.00·3.50·3.95초`는 각 뷰별 SHA-256이 동일해 최종 자세 1초 유지 자체는 정상이다.
- 캡처 기록은 `SceneViewFocused=False`, `SceneSaved=False`, `SelectionCleared=True`다.

## 2026-07-18 Unity Bake 포즈 연결 표면 - 최신 보정 시각 검증 미통과

- Unity 최종 좀비형의 실제 Bake Mesh를 전용 명령으로 내보내 Blender 기본 Armature 포즈와 FBX 축 차이를 제거했다.
- Bake 원본은 `14,193`정점·`60,684`개 삼각형 인덱스이며, Blender와 Unity 사이에는 축을 명시적으로 변환하는 `meshdata`를 사용한다.
- 본체와 반대 방향으로 겹친 `3,201`정점 대형 폐곡면만 Remesh 입력에서 제외해 이전의 큰 타원형 고체화를 제거했다.
- 최종 연결 표면은 `11,232`정점의 단일 Mesh이며 최종 좀비형과 높이·폭·깊이가 각각 약 `99.7%`, `99.1%`, `99.3%` 일치한다.
- 자동 캡처에서 같은 표면이 `2.4~2.85초`에 좀비 실루엣으로 이어졌지만 강한 압축과 정점별 조형 지연 때문에 표면이 찢어진 듯 보였다.
- 해당 캡처는 미통과로 판정했다.
- 이후 압축을 완화하고, 조형 지연을 높이 방향의 연속 값으로 제한하고, 변환 전용 복사 머티리얼만 양면 표시하도록 수정했다.
- 최신 보정은 세 번째 설치형에 적용됐고 `CoherentVerticalSculptDelay`, `TransformSurfaceDoubleSided`, `StrongRadialCollapse=False` 상태로 구성했다.
- 승인된 자동 캡처 1회로 최신 보정본을 정면·사선에서 다시 확인했다.
- `1.35초`, `1.80초`, `2.40초`에는 사람형 크기와 대략적인 실루엣은 유지하지만, 전신에 큰 구멍과 찢어진 조각이 드러나 하나의 촛농 덩어리로 보이지 않는다.
- `2.85초`에도 표면 공백과 뒤집힌 삼각형이 남아 있고, `3.00초`의 정확한 최종 좀비형으로 불연속적으로 교체된다.
- 양면 머티리얼은 뒷면을 보이게 할 뿐 정점 이동으로 생긴 표면의 구조적 공백을 메우지 못했다. 현재 원인은 연결 Mesh의 정점별 압축·조형 과정에서 삼각형이 과도하게 늘어나고 뒤집히는 데 있다.
- 따라서 `덩어리 자체가 꿈틀거리며 서서히 좀비형으로 빚어지는 표현`은 아직 충족하지 못했고 완료로 처리하지 않는다.
- 최종 좀비형 정면 `3.00·3.50·3.95초`와 사선 `3.00·3.50·3.95초`는 뷰별 SHA-256이 각각 동일해 1초 유지 자체는 확인됐다.
- 캡처 기록은 `SceneViewFocused=False`, `SceneSaved=False`, `SelectionCleared=True`이며 Scene View 포커스·수동 재생·씬 저장은 사용하지 않았다.
- 아래의 이전 구현·캡처 설명 중 성공으로 표현된 항목은 당시 단계의 기록이며, 최신 완료 판정에는 사용하지 않는다.

## 2026-07-18 연결형 파생 메시 1차 적용 - 미통과

- 원본 FBX와 분리된 `Models/Derived/Smorzando_TransformSurface.fbx/.blend`를 만들고 Voxel Remesh로 본체를 하나의 연결 표면으로 구성했다.
- Blender 생성 결과는 본체 `8,932`정점, 단일 연결 성분이며 미세 잔여 섬 세 개만 제거했다.
- Unity 임포트 결과는 `12,419`정점이고, 원본 Bake Mesh Bounds와 높이·폭·깊이·중심을 모두 일치시켰다.
- 런타임은 변환 중 이 연결 표면 하나와 설치형 촛농 재질만 사용하고, 별도 촛농 외피나 별도 확대 좀비 렌더러를 사용하지 않는다.
- 자동 캡처에서 표면 연결은 유지됐지만 Blender 기본 Armature 평가 포즈와 Unity 최종 Bake 포즈가 달라 큰 타원형 몸체가 `2.85초`까지 남았다.
- `3.0초` 원본 좀비형 인계가 순간 교체로 보여 사용자 목표를 충족하지 못했으므로 완료로 판정하지 않는다.
- 다음 보정에는 Unity의 현재 SkinnedMeshRenderer Bake Mesh를 직접 내보낸 뒤 그 좌표·포즈를 기준으로 연결형 표면을 다시 생성해야 한다.
- 승인된 추가 캡처는 한 번만 실행했고, 종료 시 Scene View 포커스·씬 저장 없이 Unity 선택을 해제했다.

## 2026-07-18 단일 표면 1차 보정 결과 - 미통과

- 사용자 제공 실행 영상에서 연결형 촛농 외피가 수축하는 동안 별도 좀비형 메시가 펼쳐지는 교차 전환을 직접 확인했다.
- 런타임에서 별도 외피 렌더러를 제거하고 좀비형 Bake Mesh 정점 하나만 바닥 집결부터 최종 형상까지 변형하도록 수정했다.
- 새 자동 캡처의 `1.35~2.85초` 구간에는 별도 외피의 수축이나 두 렌더러 사이 확대 전환이 없다.
- 다만 원본 좀비형 메시의 분리된 부품 구조 때문에 초기 변형 표면이 연결된 촛농 덩어리 대신 눌린 좀비 부품처럼 보인다.
- 이 결과는 사용자가 요구한 `한 덩어리 자체가 꿈틀거리며 좀비형으로 빚어지는 표현`에 미달하므로 완료로 판정하지 않는다.
- 다음 구현은 연결된 변형 전용 표면을 사용해 촛농 덩어리와 좀비 실루엣을 같은 토폴로지로 유지해야 한다.
- 최종 인간형 `3.0~3.95초` 프레임은 동일하고, 1초 유지·4초 반복·불꽃 타이밍은 유지됐다.
- 캡처 종료 시 Scene View 포커스와 씬 저장 없이 Unity 선택을 해제했다.

## 2026-07-18 꿈틀거리는 좀비형 조형 보정

- 사용자가 매끈한 덩어리에서 완성 모델이 드러나는 방식은 원하는 변형 표현이 아니라고 확인했다.
- 바닥 촛농이 양초 중심으로 모이는 기존 흐름과 `3초 변환 + 1초 유지` 주기는 유지했다.
- `1.35초`에 완성되는 촛농 덩어리의 폭을 넓혀 최종 좀비형과 비슷한 높이·부피를 갖게 했다.
- 연결형 외피의 높이 구간마다 서로 다른 중심축 이동 위상을 적용해 덩어리 전체가 한 방향으로 흔들리지 않고 상·중·하부가 따로 비틀거리게 했다.
- 외피 표면에는 시간·높이·둘레 각도에 따라 서로 다른 팽창·수축 위상을 적용해 돌출 위치가 계속 이동한다.
- 좀비형 Bake Mesh는 최종 전체 높이를 처음부터 유지하고, 위치마다 다른 조형 지연을 사용해 팔·다리·몸통·머리가 한꺼번에 나타나지 않게 했다.
- 아직 빚어지지 않은 좀비형 표면에는 외피와 연결되는 잔여 꿈틀거림을 적용하고, 이 움직임은 `3.0초`까지 점차 감소해 정확한 최종 모델로 수렴한다.
- 자동 캡처 시점은 `0`, `0.35`, `0.65`, `1.0`, `1.35`, `1.55`, `1.8`, `2.1`, `2.4`, `2.65`, `2.85`, `3.0`, `3.5`, `3.95`, `4.05`, `4.35초`다.
- `1.35 → 1.55 → 1.8초` 정면·사선 이미지에서 덩어리 실루엣과 표면 돌출 위치가 서로 다르게 비틀리는 것을 직접 확인했다.
- `2.1초`에는 머리와 발부터 조형되며 중앙은 아직 촛농 덩어리로 남고, `2.4초`에는 팔·다리·몸통이 형성되면서 잔여 촛농이 중앙에 연결되어 있다.
- `2.65~2.85초`에는 남은 촛농과 비틀림이 줄어들면서 좀비형 표면으로 흡수된다.
- `3.0·3.5·3.95초` 정면·사선 이미지는 각 뷰별 SHA-256이 동일해 최종 좀비형이 `1초` 동안 유지된다.
- `4.05초`에는 불꽃과 주변광이 켜진 설치형으로 정상 복귀한다.
- 자동 캡처는 Scene View 포커스와 수동 재생을 사용하지 않았고 씬을 저장하지 않았으며 종료 시 Unity 선택을 해제했다.
- 원본 FBX, 첫 번째·두 번째 설치형, 기존 좀비형, Player 시작점은 변경하지 않았다.
- Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## 사용자 확정 내용

- 변환 검토 개체는 `Smorzando_Installed_03`이다.
- 변환 시간은 총 `3초`다.
- 촛불은 먼저 꺼지고 짧은 잔불을 남긴다.
- 바닥에 퍼진 촛농 몸체가 양초 중심으로 모여 인간형 전체 크기의 덩어리를 먼저 만든 뒤, 그 덩어리 자체가 점차 인간형 모델로 변형된다.
- 촛농이나 양초가 먼저 작아져 사라진 뒤 별도의 작은 인간형이 확대되는 방식은 사용하지 않는다.
- 최종 인간형은 세 번째 설치형 오른쪽의 `Smorzando_Person_01`과 같은 모델, 크기, 방향을 사용한다.
- 최종 위치는 세 번째 설치형의 중심이며 발은 바닥에 붙인다.
- 변환 검토 개체에서만 `3초 변환 → 최종 인간형 1초 유지 → 설치형부터 재시작`을 반복한다.

## 구현 구조

- 런타임 구성요소: `Assets/_Project/Runtime/Enemies/Smorzando/SmorzandoInstalledTransformMotion.cs`
- Unity 적용·캡처 도구: `Assets/_Project/Editor/Validation/SmorzandoInstalledTransformApplyAndReview.cs`
- 적용 대상: `Approved Smorzando Enemy Placement/Smorzando_Installed_03/Smorzando_Installed_Model`
- 숨김 최종 모델: `Smorzando_Installed_03/Smorzando_Transform_PersonTarget`
- 기준 모델: `Smorzando_Person_01/Smorzando_Person_Model`
- 설치형 원본 Mesh와 인간형 FBX를 덮어쓰지 않는다.
- 재생·자동 캡처 중에만 설치형 Mesh 복사본, 하나로 연결된 촛농 외피 Mesh, 인간형 Bake Mesh 복사본을 변형한다.
- 연결형 촛농 외피는 `24`개 높이 고리와 `32`개 둘레 구간으로 만든 단일 표면이며 인간형 전체 높이를 먼저 채운다.
- 인간형 Bake Mesh는 처음부터 외피와 같은 전체 높이를 유지한 채 외피 안에서 표면만 펼쳐지고, 남은 외피 촛농은 몸통 중심으로 흡수된다.
- `3.0초`에 기준 모델과 같은 SkinnedMeshRenderer로 교체해 최종 형상을 정확히 맞춘다.
- 검토 반복 주기는 `4초`이며 `3.0~4.0초`에는 최종 인간형을 그대로 유지한다.
- 도메인 리로드 뒤 초기 상태를 아직 저장하지 않은 구성요소는 복원 처리를 건너뛰어 불꽃 기준값을 기본값으로 덮어쓰지 않는다.

## 3초 타이밍

- `0.0~0.5초`: 기존 불꽃과 주변광이 축소·감쇠한다.
- `0.35초`: 작은 붉은 잔불이 남는다.
- `0.65초`: 불꽃 렌더러와 Point Light가 완전히 꺼진다.
- `0.5~1.35초`: 바닥 촛농이 양초 중심으로 모이면서 연결형 외피가 인간형 전체 높이까지 올라온다.
- `1.35초`: 가로 틈과 내부 구멍이 없는 하나의 인간형 크기 촛농 덩어리가 완성된다.
- `1.35~3.0초`: 전체 높이는 유지한 채 팔·다리·몸통·머리 표면이 덩어리 안에서 점차 형성되고, 남은 촛농 외피는 몸통 중심으로 흡수된다.
- `2.8초`: 인체 형상이 거의 완성되고 연결형 외피의 잔여 촛농만 표면에 남는다.
- `3.0초`: 설치형 Mesh와 변형 프록시가 숨겨지고 최종 인간형만 남는다.
- `3.0~4.0초`: 최종 인간형 자세를 `1초` 유지한다.
- `4.0초`: 불꽃과 주변광이 켜진 최초 설치형으로 돌아가 다음 변환 주기를 시작한다.

## Unity 적용 결과

- 적용 명령: `ApplySmorzandoInstalledTransform`
- 변환 모션 구성요소 수: `1`
- 숨김 인간형 대상 수: `1`
- 최종 인간형 로컬 위치: `(0, 0.313106, 0)`
- 최종 인간형 로컬 회전: Y축 `180도`
- 최종 인간형 로컬 크기: `(1, 1, 1)`
- 위 세 값은 오른쪽 기준 인간형과 동일하다.
- 반복 재생: `True`
- 최종 인간형 유지 시간: `1초`
- 전체 검토 주기: `4초`
- 반복 시작 불꽃 기준: 로컬 크기 `(1, 1, 1)`, Point Light 활성, 강도 `0.45`
- 두 번째 설치형의 대기 모션과 설치형 세 개의 불꽃 모션 구성은 변경하지 않았다.
- 기존 스모르찬도와 Player Transform은 변경하지 않았다.

## 자동 시각 확인

- 캡처 명령: `CaptureSmorzandoInstalledTransformFrames`
- 캡처 시점: `0`, `0.35`, `0.65`, `1.0`, `1.35`, `1.8`, `2.4`, `2.8`, `3.0`, `3.5`, `3.95`, `4.05`, `4.35초`
- 정면과 사선 렌더에서 불꽃 소화, 잔불, 촛농 집결, 인간형 크기 덩어리 완성, 덩어리의 인간형 표면 변환 순서를 직접 확인했다.
- `0.35초`에는 작아진 잔불이 보이고 `0.65초`부터 불꽃과 주변광이 사라진다.
- `1.0초`에는 바닥 촛농과 양초 중심에서 연결된 외피가 위로 형성된다.
- `1.35초`에는 인간형 전체 높이의 단일 촛농 덩어리가 완성되며 이전 구현의 가로 틈과 내부 빈 공간이 없다.
- `1.8초`에는 덩어리 높이를 유지하면서 하단부터 인체 표면이 나오기 시작한다.
- `2.4초`에는 팔·다리·몸통·머리가 덩어리에서 드러나고 남은 촛농이 몸통 중심에 연결되어 있다.
- `2.8초`에는 별도 확대 없이 거의 완성된 인간형 표면으로 이어진다.
- `3.0초`에는 남은 설치형 왁스와 불꽃이 사라지고 인간형 발이 바닥에 놓인다.
- `3.0`, `3.5`, `3.95초` 정면·사선 이미지는 각각 SHA-256까지 동일해 최종 인간형이 `1초` 동안 움직이지 않고 유지된다.
- `4.05초`에는 불꽃과 주변광이 켜진 설치형으로 복귀하고, `4.35초`에는 두 번째 주기의 불꽃 감쇠가 진행된다.
- `Smorzando_Transform_FinalVsReference.png`에서 최종 결과와 오른쪽 기준 인간형의 외형, 크기, 방향, 바닥 정렬이 일치한다.
- 분리된 인간형 표면을 덩어리로 사용해 가로 틈이 생긴 결과와, 연결 외피 상단 정점의 부동소수점 오차로 `NaN`이 발생한 결과는 모두 판정에서 제외했다.
- 외피 반경 입력을 `0 이상`으로 제한한 뒤 최종 캡처에서는 비정상 Mesh bounds와 `NaN` 오류가 재발하지 않았다.
- 자동 캡처는 Scene View 포커스와 수동 재생을 사용하지 않았고 씬을 저장하지 않았다.
- 캡처 종료 시 임시 Mesh·프록시를 제거하고 Unity 선택을 해제했다.

## 시각 자료

- 시작: `automated_visual_capture/Smorzando_Transform_Front_000.png`
- 잔불: `automated_visual_capture/Smorzando_Transform_Front_035.png`
- 완전 소화: `automated_visual_capture/Smorzando_Transform_Front_065.png`
- 촛농 집결 중간: `automated_visual_capture/Smorzando_Transform_Front_100.png`
- 인간형 크기 연결 덩어리: `automated_visual_capture/Smorzando_Transform_Front_135.png`
- 덩어리 표면 변환 시작: `automated_visual_capture/Smorzando_Transform_Front_180.png`
- 인간형 표면 전개: `automated_visual_capture/Smorzando_Transform_Front_240.png`
- 잔여 촛농 흡수: `automated_visual_capture/Smorzando_Transform_Front_280.png`
- 최종 인간형: `automated_visual_capture/Smorzando_Transform_Front_300.png`
- 최종 인간형 유지 중간: `automated_visual_capture/Smorzando_Transform_Front_350.png`
- 최종 인간형 유지 끝: `automated_visual_capture/Smorzando_Transform_Front_395.png`
- 다음 주기 설치형 복귀: `automated_visual_capture/Smorzando_Transform_Front_405.png`
- 다음 주기 불꽃 감쇠: `automated_visual_capture/Smorzando_Transform_Front_435.png`
- 최종 기준 비교: `automated_visual_capture/Smorzando_Transform_FinalVsReference.png`

## 실행하지 않은 항목

- 원본 FBX를 수정하지 않았다.
- 새 모델링, 텍스처, 머티리얼, 별도 VFX 에셋을 만들지 않았다.
- 별도 아트 샘플을 만들지 않았다.
- 반복 재생은 세 번째 변환 검토 개체에만 적용했고 다른 설치형과 인간형에는 적용하지 않았다.
- 전투, 체력, 강제 변환 게임 로직을 구현하지 않았다.
- 첫 번째 설치형, 두 번째 대기 모션, 기존 인간형, Player 시작점을 변경하지 않았다.
- Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업을 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.
