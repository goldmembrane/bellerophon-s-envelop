# 이슈판트 5번 GIF 실제 장검 궤적 직접 판독

> **폐기됨(SUPERSEDED):** 사용자가 모델링 수정 전 이슈판트 5번의 기존 장검 움직임을 현재 기준으로 지정했습니다. 현재 적용 결과는 `docs/validation/ispant_slash_legacy_sword_motion_2026-08-20/` 기록이 대체합니다.

## 판정

- 결과: **PASS**
- 기준: `C:/Users/gus68/OneDrive/바탕 화면/animated.gif`, 220×260, 10fps, 15프레임
- 실제 추적 좌표: `Ispant_05_SlashGifActualTrace_Pixels.csv`
- 확대 직접 비교: `Ispant_05_SlashGifActualTrace_EnlargedComparison.png`

## 이전 판정 폐기 사유

- 이전 구현은 GIF의 실제 좌표를 읽지 않고 사람이 적은 15개 정규화 방향을 사용했다.
- 검사도 같은 방향 배열을 기대값으로 다시 사용해 `0°`를 산출했으므로 순환 검증이었다.
- 특히 GIF에서 검이 깊이 방향을 향해 짧게 보이는 7·9·10·11프레임을 화면 평면의 전장 길이로 펼쳐 큰 호와 몸 관통처럼 보이게 했다.
- 이전 `ispant_slash_gif_upward_trajectory_2026-08-20`의 PASS 판정은 폐기 상태로 변경했다.

## 실제 좌표 대입 방식

- GIF 각 프레임에 좌표 격자를 겹치고 오른손 파지점과 보이는 칼끝을 직접 표시해 15쌍의 픽셀 좌표를 기록했다.
- 좌표는 `(gripX, gripY, tipX, tipY)` 원본값으로 런타임 코드에 들어가며, `칼끝-파지점` 화면 벡터와 보이는 길이를 사용한다.
- 가장 긴 가시 표본 `152px`를 화면 평면 기준 검 길이로 두고, 더 짧게 보이는 표본은 남은 길이를 이슈판트 정면 깊이 성분으로 변환했다. 따라서 정면을 향하는 프레임에서 검이 실제 GIF처럼 짧아진다.
- GIF 6~11프레임의 손은 몸 화면 오른쪽에 있지만 현재 Slash의 오른손은 화면 왼쪽에 남는다. 오른팔을 바꾸지 않기 위해 두 손이 몸의 반대쪽에 있는 구간에만 추적 X를 대칭시켜, GIF의 몸 기준 바깥쪽·안쪽 궤적을 같은 순서로 재현했다.

## 15프레임 직접 판독

- 01~02: 칼자루는 오른손에 있고 칼날은 손 위로 수직에 가깝게 선다.
- 03~04: 칼날이 손 위와 머리 위를 지나 가로 준비 방향으로 전환된다.
- 05: 칼날이 정면 깊이로 향해 화면상 거의 사라진다.
- 06~07: 현재 오른손의 몸 바깥쪽으로 짧게 뻗는다. 이전처럼 몸 안쪽과 머스켓 아래로 숨지 않는다.
- 08~10: GIF처럼 방향을 바꿔 몸 안쪽을 낮게 가로지르며, 가시 길이도 짧게 유지된다.
- 11: 다시 정면 깊이 방향으로 정렬돼 칼날이 거의 보이지 않는다.
- 12~13: 오른손에서 몸 앞을 가로질러 반대쪽 아래로 길게 뻗는 내려베기 후속 방향이 나타난다.
- 14~15: 칼날이 오른손 위로 올라와 수직 준비 방향으로 복귀한다.
- 전체 구간에서 칼자루는 오른손에 남고 오른팔 애니메이션은 공급 Slash와 동일하다.

## 보조 확인

- GIF 실제 좌표 기반 15프레임 방향 적용 최대 오차: `0°`
- 오른팔 곡선 최대 오차: `0`
- 칼자루 최대 오차: `0.000172934m`
- 정면 베기면 최대 오차: `0.01978234°`
- 상체 좌우·높이 최대 오차: `0.00000974m`, `0.000000656m`
- 최종 상태 반복재생: Slash `13주기`, Running 하체 `29주기`, 씬 복원 통과
- 본체 메시: `8,755`정점, `9,798`삼각형, 유한 정점 유지

## 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 오른팔·오른손목 애니메이션 곡선 수정
- 가드 구현
- 공급 FBX와 메시·텍스처·머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- Git 작업
