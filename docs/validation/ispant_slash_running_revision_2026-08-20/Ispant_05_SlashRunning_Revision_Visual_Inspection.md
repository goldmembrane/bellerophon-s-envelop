# 이슈판트 5번 Slash + Running 후속 수정 시각 검증

## 대상과 판정

- 사용자 기준 영상: `C:/Users/gus68/Videos/Captures/Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-08-20 17-45-19.mp4`
- 대상: `Approved Ispant Enemy Placement/Ispant_05_RunningOneHandedSwordAttack/Ispant_New_Direct_Model`
- 판정: **PASS**
- 완료 조건: 상체 왼쪽 치우침 제거, 오른팔 궤적을 따르는 장검의 상향 시작과 정면 베기, 머스켓 연속성 복구와 왼팔-머스켓 부적합 변형 제거를 모두 충족한다.

## 원인과 수정

- 수정 전 상체 어깨 중심의 모델 좌우축 편차 범위는 `-0.07191468m .. -0.000122075m`였다. Slash 자세는 보존하고 `LateUpdate`에서 `Spine02` 이하 상체를 직접 모델 휴식 자세의 어깨 중심 좌우 오프셋에 맞춰 평행 이동하도록 했다.
- 장검은 오른손 위치만 따르며 손 기준 고정 회전을 유지해 베기 중에도 칼날이 지나치게 위쪽을 향했다. 시작 시 칼날을 화면상 위 방향으로 두고, Slash 정규화 시간 `0.2`까지 오른팔 아래팔-손 방향으로 혼합한 뒤 그 방향을 실시간 추종하도록 변경했다.
- 이전 보정 본체 메시가 원본 머스켓 일부를 누락해 총신과 총구가 분리돼 보였다. 새 토폴로지나 웨이트를 만들지 않고 직접 원본 `char1`의 온전한 `9,798`삼각형 메시로 복원했으며, `LeftShoulder`, `LeftArm`, `LeftForeArm`은 직접 모델 휴식 회전으로 안정화했다.

## 수치·재생 확인

- Slash 상체 원본 대비 위치/각도 최대 오차: `0m / 0°`
- Running Hips·하체 원본 대비 위치/각도 최대 오차: `0m / 0°`
- 상체 어깨 중심 최대 좌우 오차: `0.00000974m`
- 칼자루-오른손 최대 위치 오차: `0.000140716m`
- 시작 칼날-화면상 위 방향 각도: `0°`
- 혼합 후 칼날-오른팔 아래팔 방향 최대 각도 오차: `0.01978234°`
- 칼날의 상향 시작 대비 최대 회전 범위: `170.6153°`
- 얼굴·왼팔 휴식 회전 최대 오차: `0°`
- 본체 메시: `8,755`정점, `9,798`삼각형, BakeMesh 유한값 통과
- Root Motion 비활성, 제자리 반복, Loop 활성 상태다.
- Unity Edit Mode 실제 재생에서 Slash `9회`, Running 하체 `21회` 반복 후 정지 및 씬 상태 복원을 확인했다.

## 직접 시각 대조

- 최종 비교 이미지는 실제 Slash 원본, 실제 Running 원본, 완성 합성을 각각 `0%`, `25%`, `50%`, `75%`, `100%` 시점으로 같은 카메라에서 배치했다.
- 완성 합성의 5개 프레임을 확대해 직접 확인했다. 상체는 하체 중심에서 지속적으로 왼쪽으로 밀리지 않고 Slash 고유의 동작 기울기만 남는다.
- 장검은 `0%`에서 오른손에 쥔 채 칼날이 위를 향한다. `25%` 이후 오른팔과 함께 내려오고 `50%~75%`에서 정면을 가로지르는 베기 각도로 변하며, 칼자루가 손에서 분리되지 않는다.
- 머스켓은 개머리판, 몸통, 총신, 총구가 하나로 이어져 있으며 프레임 사이에서 분리되지 않는다. 왼팔 메시도 머스켓 쪽으로 늘어나거나 붙어 변형되지 않는다.
- 얼굴은 정면 안정 회전을 유지하고, 다른 이슈판트 슬롯과 배치 밖 씬 루트는 바뀌지 않았다.

## 산출물

- 수치 검사: `Ispant_05_SlashRunning_Revision_Inspection.txt`
- 최종 1회 비교 캡처: `Ispant_05_SlashRunning_Revision_Comparison.png`
- 최종 씬 SHA-256: `CB9665DBF2D19C15DCE68068ED819A1295C092A20599CE04EDFC65F724792354`
- 비교 캡처 SHA-256: `C46F99D97CE8A1060FD36DD92E8E7C4371FF8BA4499D81F4627E613FAE154438`

## 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 공급 FBX와 메시 토폴로지·본 웨이트·바인드포즈 수정
- 장검·머스켓 모델 형상·UV·머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- AI·내비게이션·전투·물리 이동·Root Motion 수정
- Git 작업
