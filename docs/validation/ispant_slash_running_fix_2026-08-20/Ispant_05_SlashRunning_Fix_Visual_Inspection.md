# 이슈판트 5번 얼굴·장검·왼팔 수정 직접 검토

## 대상과 원인

- 사용자 영상: `C:/Users/gus68/Videos/Captures/Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-08-20 17-07-36.mp4`
- Unity 대상: `Approved Ispant Enemy Placement/Ispant_05_RunningOneHandedSwordAttack/Ispant_New_Direct_Model`
- 영상 8.0231초를 0.5초 간격 16프레임과 4fps 접촉 시트로 직접 확인했다.
- 수정 전 Unity 진단에서 시작 머리는 직접 모델의 정면 휴식 자세와 `63.3831°`, 왼팔 상완은 `9.360298°` 어긋났다.
- 5번은 보정 전 직접 메시를 사용했고 `IspantRigidSwordFollower`가 없어서 장검이 오른손을 따라가지 않았다.

## 적용 내용

- Slash 클립의 `Head` 회전을 직접 모델의 정면 휴식 회전으로 고정했다.
- 기존 승인 자산 `Ispant_New_DrawSword_Body.asset`을 5번 `char1` 렌더러에 재사용하고 `LeftShoulder`, `LeftArm`, `LeftForeArm` 회전을 안정 자세로 고정했다. 새 토폴로지·웨이트·바인드포즈는 만들거나 수정하지 않았다.
- 장검 손잡이 중심을 오른손 가중 정점의 손바닥 중심에 맞췄다.
- 첫 프레임 칼날을 시각적 위쪽으로 맞춘 뒤, `LateUpdate`에서 오른손의 위치와 회전을 그대로 추종하는 5번 전용 모드를 적용했다.
- Slash 전체 몸과 Running `Hips`·하체 합성, 제자리 반복, Root Motion 비활성 상태는 유지했다.

## 수치 검증

- 칼자루-오른손 최대 오차: `0.000138183m`
- 시작 칼날 상향 오차: `0°`
- 장검의 오른손 기준 상대 회전 오차: `0°`
- 장검 월드 이동 범위: `1.404365m`
- 오른손 월드 이동 범위: `0.7884976m`
- 얼굴·왼팔 안정 회전 최대 오차: `0°`
- 수정하지 않은 Slash 상체 위치·각도 오차: `0m`, `0°`
- Running 하체 위치·각도 오차: `0m`, `0°`
- BakeMesh 정점: 전부 유한
- 최종 본체: 8,755정점·9,678삼각형

## 직접 시각 대조

- 최종 비교: `Ispant_05_SlashRunning_Fix_Comparison.png`
- 위 행은 실제 `išpant-new slash.fbx`, 가운데 행은 실제 `išpant-new running.fbx`, 아래 행은 수정된 최종 합성이다. 각 행은 시작·25%·50%·75%·종료 시점이다.
- 공급 FBX와 최종 행의 전신 자세를 직접 대조한 결과, 보정 대상 외 Slash 상체의 준비·베기·회수와 Running 골반·다리의 교대·착지 흐름이 유지됐다.
- 최종 행을 512×512 원본 패널로 확대해 확인했다. 머리·투구 중심축은 전 시점에서 몸통 정면축과 중앙 정렬되어 왼쪽 사선 편향이 없다.
- 시작과 종료 패널에서 칼자루가 오른손에 있고 칼날이 위로 선다. 중간 패널에서 장검은 오른팔의 상승·횡베기·회수 방향에 맞춰 위치와 각도가 함께 변한다.
- 왼팔은 어깨·상완·팔꿈치·전완·손 윤곽이 전 시점에서 연속적이며 머스켓으로 늘어나거나 붙는 메시가 없다.
- 최종 시각 판정: `PASS`

## 반복과 범위 보존

- Unity Edit Mode에서 Slash `10회`, Running 하체 `21회` 반복을 확인한 뒤 씬 상태를 복원했다.
- 다른 이슈판트 11개 슬롯 및 배치 밖 씬 루트 변경: 없음
- 최종 씬 SHA-256: `147E6BA36A171C89F8DAB56F42609A19DC8992752DF4A78EF11E29FD7A72FDE2`
- 최종 비교 PNG SHA-256: `BE03A00118B37F60A0B7475A27A60327BE0C9272208C7D1B32C68643B897F94E`
- 캡처 중 기존 씬 조명에 의한 그림자 아틀라스 해상도 축소 경고만 있었으며 컴파일 오류·예외·검사 실패는 없었다.

## 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트, Windows 빌드
- 공급 FBX 수정
- 새 메시 토폴로지·본 웨이트·바인드포즈 제작 또는 수정
- 장검·머스켓 모델 형상·UV·머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 씬 루트 수정
- AI·내비게이션·전투·물리 이동·Root Motion 수정
- Git 작업
