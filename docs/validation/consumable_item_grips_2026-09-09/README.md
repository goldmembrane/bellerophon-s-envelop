# 세 소비품 전용 소품·오른손 파지 — 공용 손 스킨 결함으로 미완료

## 2026-09-10 00:43 현재 상태 — 사용자 지시로 손가락 작업 중단

- **직접 확인:** 마지막 완료된 자연 관찰은 `diagnostic_25`다. 손가락 갈라짐·날카로운 접힘은 줄었지만 소품 접촉 이격/관통이 남아 미통과다. 뒤집힘·축소·과도한 늘어남 실패는 없으나 면 교차 28/0/33/28개와 일부 손가락 접촉 실패가 있다.
- 중단 지시 전에 시작한 후보 재파지 계산은 네 프로필/프리팹을 저장하고 종료했다. **이 마지막 재파지 결과는 직접 확인하지 않았다.** 25번 결과를 새 프로필의 통과 근거로 사용하지 않는다. 후속 진단과 최종 캡처는 실행하지 않았다.
- 실제 공용 원본·144개 연결·두 보정 메시·네 실제 파지는 손가락 복구 후보로 교체하지 않았다. `ApplySharedRightFingerRepair`는 구현만 했으며 실행하지 않았다.
- 검토 종료 후 Edit Mode, GPUBatched, 복구 대기 없음, 임시 검토 루트/잔여 모델/잔여 검토 카메라 0을 확인했다. 콘솔 오류 0·경고 58. 실제 키보드·마우스 조작은 미검증이다.
- 전체 작업 요약·정정·중단 시 저장 상태는 [2026-09-10 진행 기록](../../PROGRESS_2026-09-10.md)을 따른다. 아래 기록은 각 시점의 이력이다.

## 2026-09-10 00:28 현재 상태 — 오른손 형상 재구성 승인 후 진행 중

- 전날 23:50의 형상 재구성 승인 대기는 해소됐다. [현재 날짜 진행 기록](../../PROGRESS_2026-09-10.md)을 따른다.
- 직접 기본 자세 06~07 및 자연 파지 22~23에서 둥근 손끝과 UV 연결은 개선됐으나 관절 접힘·접촉은 미통과다. 24번 후보를 관찰 중이며 실제 공용 모델·144개 연결·기존 두 보정·실제 네 파지는 아직 변경하지 않았다.
- 이후 아래 각 절은 해당 시각의 이력이며 현재 완료 상태를 뜻하지 않는다. 최종 캡처는 아직 하지 않았다.

## 23:50 현재 상태 — 기존 손가락 형상 재구성 전 미완료

- **직접 확인:** `diagnostic_21/*_contact_16.png`, `*_contact_19.png`의 네 파지에서 날카롭게 깨져 보이는 손끝/손등 윤곽이 남는다. `hand_weight_review/diagnostic_04`~`diagnostic_05`의 기본 자세 원본/후보에도 원래 형상으로 존재한다. 두 구멍 내부를 메우고 새 내부 정점만 곡면화하는 방법으로 시각 목표를 달성하지 못했다.
- 마지막 후보 21은 다섯 손가락의 접촉 기준을 만족하지만 면 교차 Battery/Converter/Buffer/Nano 2/0/2/6개, 주사기 축소 면 1개로 미통과다. 원본 정점 자체와 관절 주변 면을 재구성하는 다음 범위는 아직 실행하지 않았다. 장갑 스타일/원래 재질/손목·기존 본을 유지한 오른손 다섯 손가락의 국소 형상 보수와 전체 144개/두 기존 보정 메시 반영을 함께 다뤄야 한다.
- 실제 원본 FBX, 씬, 두 보정 메시, 원본 가중치 JSON 및 네 실제 파지 프로필은 `finger_weights_before/` 백업과 바이트 비교 동일하다. 후보만 변경했고 실제 수송자에는 반영하지 않았다.
- 검토 재생 종료/관찰 카메라 정리 후 콘솔 오류 0, 경고 1. 실제 입력 조작은 미검증이며 입력 설정은 변경하지 않았다. 최종 캡처·자동 테스트·빌드·Git 쓰기 미실행. 아래 각 시각의 진행 중 문구는 해당 시점의 이력이며 현재 상태는 이 절을 따른다.

## 23:30 국소 지오메트리 보수 승인 후 진행 중

- 두 경계의 면 추가/재연결과 가중치·네 파지 보정, 공용 144개 사용처와 기존 보정 메시 보존/회귀 범위를 사용자가 승인했다. 아래 22:50의 승인 대기는 해소됐다.
- **직접 확인:** 15~17번 실제 자연 재생 손바닥/손등 확대는 아직 뾰족한 접힘이 남아 미통과다. 15의 새 패치 UV 줄무늬는 인접 기존 장갑 UV 차트 연결로 개선했다. 기존 면의 UV·재질은 유지한다.
- `repair_right_hand_topology.py`는 원래 면과 모서리 중복을 피하는 경계 삼각분할 및 새 패치 안의 분할만 수행한다. 후보 FBX 재임포트 경계는 닫혔다. 현재 Blender 13,358 정점/26,800 면이며, 기존 13,103 정점/26,195 면을 유지하고 국소 패치가 추가됐다.
- 16/17 관찰 교차는 Battery/Converter/Buffer/Nano 2/6/30/19개 및 2/6/25/19개다. 일부 중지/새끼 접촉도 실패한다. 작은 삼각형의 `Vector3.normalized`가 0을 반환해 뒤집힘 일부를 누락한 C# 계산을 명시적인 크기 나눗셈으로 정정했다. 이전 해당 수치는 통과 근거가 아니다.
- 표면을 함께 고려한 가중치 제작은 엄지 고정 패드 인접 네 면의 변형이 커져 내보내기를 거부했다. `fold_weight_failed_authoring.json`에 실패 면/정점/가중치를 남기며, 실패는 실제 모델에 반영하지 않는다. `fold_weight_authoring.json`은 아직 이전 성공 계산 기록이므로 현 후보 통과로 해석하지 않는다.
- 후속 정정: 단순 비례 가중치 정규화 대신 Adam의 대각 계량 제약 투영을 구현한 뒤 면 뒤집힘 없는 후보를 얻었다. `fold_weight_authoring.json`은 이 성공 계산(선택 반복 179)으로 갱신됐다. 자연 재생 `diagnostic_18`에서도 뒤집힘 0이지만, 직접 화면의 날카로운 손가락 윤곽과 면 교차 12/3/19/11개, 일부 손가락 접촉 실패로 미통과다. 18은 후보 통과 또는 작업 완료의 근거가 아니다. 다음 파지 제작은 이 스킨과 정정된 면 법선 계산을 사용한다.
- 19번 재파지는 교차 12/6/38/17개 및 전환기 검지 접촉 실패. 새 패치 정점만 최대 3mm 곡면화한 20번은 교차 12/6/40/17개와 완충제 뒤집힘 1개가 남아 미채택했다. `patch_curvature_before/` 후보 FBX/가중치 JSON으로 되돌리고 후보를 재임포트했다. 현재는 여유 간격을 둔 제작 후보를 계산 중이며 관찰 판정 기준은 변경하지 않았다.
- 기본 자세 `hand_weight_review/diagnostic_04`~`diagnostic_05`의 원본/후보 손등 확대에 원래 손끝의 뾰족한 윤곽이 남는다. `rest_hand_self_intersections.json`은 원본 24본/54본/후보의 비인접 손 표면 관통 0을 기록한다. 수치상 폐곡면과 관통 없음이 시각 품질 통과를 뜻하지 않는다.
- 실제 원본·144개 연결·기존 보정·실제 네 파지 프로필은 미변경. 후보 자연 재생의 관찰 코드만 읽기 전용으로 사용한다. 최종 캡처·자동 테스트·빌드·Git 쓰기는 미실행이다.

## 22:50 원본 오른손의 열린 면 두 곳 확인 — 지오메트리 보수 승인 경계

- **직접 확인:** `diagnostic_13/Battery_Aux_Plug_contact_16.png`, `*_contact_19.png`와 `diagnostic_14`의 전환기·완충제·주사기 손등 확대에서 손가락 사이의 뾰족하고 열린 면이 남는다. 후보는 시각 미통과이며 실제 공용 모델에는 적용하지 않았다.
- **기본 자세 직접 비교:** `hand_weight_review/diagnostic_02/SharedSourceRest_view_6.png`, `SharedSourceRest_view_8.png` 및 동일 구도의 `ImportedRest_view_6.png`를 확인했다. 관찰 카메라 절단 범위를 넓힌 원본 기본 자세에서도 손가락 사이의 열린 표면과 뾰족한 면이 보이며, 가중치 후보의 기본 형상은 원본과 동일하다. 단순 파지 회전만의 결함으로 설명되지 않는다. 재질·표시·본·시간을 바꿔 숨기지 않았다.
- **보조 원인 확정:** `hand_source_topology_comparison.json`에서 손 리그 작업 이전 24본 FBX에 오른손의 열린 경계 루프 두 개(8변·7변)가 있다. 이전 국소 분할을 거친 54본 공용 원본과 이번 가중치 후보에는 동일 두 루프가 50변·49변으로 나뉘어 남는다. 15→99는 구멍 개수 증가가 아니라 기존 경계의 분할이다. `inspect_hand_surface_topology.py`는 읽기만 수행했다. 없는 면은 가중치 변경만으로 복구할 수 없다.
- 가중치 전이 결함도 별도로 존재했다. 연결 기반 후보 07~09는 최대 모서리 비율 19.24/13.78/18.66배로 실패했다. 기존 가중치장에 보수적으로 보정한 10~11도 최대 9.14/8.47배였다. 원래 관절/소품을 보존한 후보 파지 재맞춤 12는 최대 약 3.78배로 줄었지만 삼각형 면 뒤집힘과 중지·새끼 접촉 이격이 남았다. 실패를 통과로 바꾸지 않았다.
- `repair_observed_hand_folds.py`는 실제로 관찰한 네 자세의 행렬을 입력으로 사용한 **제작 계산**이다. 손끝·기존 형상·UV·본·비오른손 가중치를 유지하고 주변 전이를 보정했다. 첫 실행의 마지막 반복값은 불량이어서 본 적용하지 않았다. 두 번째 실행은 뒤집힘 없는 중간 후보를 별도 내보냈으며, 독립적인 자연 재생으로 다시 확인했다.
- 13~14 자연 재생 보조 관찰에서 최대 모서리 비율은 Battery/Converter/Buffer/Nano 순으로 약 1.63/1.74/1.84/1.87배, 손/소품 면 교차는 0/0/3/17개다. 중지·새끼 접촉 실패도 남는다. 독립 면 방향 계산의 뒤집힘 수가 줄어도 위 직접 확인 실패 및 접촉 실패를 상쇄하지 않는다.
- `diagnostic_14/observation.txt`의 GPU 정점과 BakeMesh 최대 차이는 약 0.0214mm이며, 실제 렌더 환경은 FourBones/GPUBatched다. 화면 결함을 CPU/GPU 다른 가중치 수나 버퍼 위치 불일치로 설명하지 못했다. 앞선 비촬영 관찰은 보이지 않는 후보의 GPU 버퍼가 없어 중단됐으며, null 처리 후 실제 촬영 프레임에서 다시 읽었다. 실패 로그는 보존한다.
- **승인 경계:** 이번 묶음은 원래 형상·UV·본을 보존하는 오른손 가중치 복구였다. 두 열린 경계의 면 추가·재연결 및 그에 따른 파생 메시 대응 변경은 별도 지오메트리 수정이므로 실행하지 않았다. 이 국소 보수와 가중치·파지 재맞춤, 전체 144개 및 기존 보정 메시 회귀를 함께 승인받아야 근본 수정을 계속할 수 있다. 새 디자인·재질·전신 형상 변경은 필요하지 않다.
- `player.fbx`는 작업 전 백업과 바이너리 비교 동일이며, CargoRunMvp 씬은 원문 문자열 비교 동일하다. Git LFS가 적용된 씬을 백업과 `git diff --no-index`로 비교하면 한쪽만 포인터로 필터링돼 거대한 차이로 표시되므로, 그 출력은 실제 씬 변경 근거로 쓰지 않았다. 실제 공용 가중치 JSON과 네 실제 파지 프로필도 작업 전 상태와 동일하다. 후보만 남아 있고 실제 144개 연결은 이번 턴에 변경하지 않았다.
- 검토 재생 종료 후 `restored_state.txt`에서 재생/일시정지/미리보기/프레임 디버거 꺼짐, 임시 관찰 카메라 0, 입력 설정 미변경을 확인했다. `StopPlayerHandRigReview`도 실행해 소유 검토 환경을 정리했다. 실제 키보드·마우스 조작은 직접 검증하지 않았다. `console_for_topology_scope.log`: 오류 0, 경고 58(전체 원인 미조사). 최종 캡처·자동 테스트·빌드·Git 쓰기는 실행하지 않았다.

아래 내용은 해당 시점의 후보 제작 및 실패 이력이다. 현재 상태는 위 절을 따른다.

## 공용 오른손 가중치 복구 승인 후 후보 검토 진행

- 사용자가 공용 오른손 가중치·파생 보정·수송자 전체 144개 사용처 반영 및 반복 직접 비교를 묶어 승인했다. 작업 전 상태는 `Backups/ConsumableItemGrips_2026-09-09/finger_weights_before/`에 보존했다.
- 후보는 기존 네 모션·소품·파지를 복제한 별도 `WeightReview_*.prefab`에서 자연 재생한다. 실제 씬 대상과 공용 원본은 아직 변경하지 않았다. 관찰은 후보가 재생된 후 읽기만 수행한다.
- `diagnostic_05`에서 직접 손가락 깨짐이 재현됐으며, Unity가 이전 임포트 가중치를 사용한 사실을 확인했다. 새 후보 품질의 근거로 사용하지 않는다. 후보 명시 재임포트와 실제 오른손 변경량 기록을 추가했다.
- `diagnostic_06`의 실제 첫 가중치 후보도 손가락 면이 찢어져 미통과다. 기존 상태의 최대 모서리 비율 약 6.49배와 비교해 최대 16.90배로 악화됐다. 삼각형 교차는 Battery/Converter/Buffer/Nano 각각 35/29/126/84개였다. 공간상 최근접 두 손가락을 이용한 분류와 관절 팔레트 변경이 경계 변형을 충분히 제어하지 못했다.
- 다음 후보는 실제 메시 연결에서 구분되는 손가락 단면을 고정점으로 사용한다. 손 표면을 따라 소유 영역을 계산하고, 서로 다른 손가락 경계에서는 손바닥 영향으로 전이한다. 먼 손가락 관절 영향과 정점별 상위 네 개 가중치 절단을 제거한다. 형상·UV·본과 비대상 가중치는 보존한다. 직접 확인 전 통과·전체 반영을 선언하지 않는다.

## 21:52 사용자 지적 손가락 깨짐 조사 — 원인 확인, 공용 스킨 수정 전 중단

- **직접 확인:** `diagnostic_03/`의 상체·손목·손바닥·엄지 확대에서 세 소품의 실제 파지와 하향 접촉을 확인했다. 다만 사용자가 지적한 손가락 사이의 찢어진 면은 남아 있었다. 이를 수치상 파지 통과와 구분하지 못한 앞선 판단은 충분하지 않았으며 전체 완료 판정은 보류한다.
- `diagnostic_04/*_contact_16.png`, `*_contact_17.png`는 가까운 관찰 카메라에서 근·원거리 절단 범위를 넓혀 같은 자연 재생 자세를 본 것이다. **손가락 깨짐은 그대로 남았으며, 이번에 변경하지 않은 Battery_Aux_Plug에도 재현된다.** 주변 몸통의 절단은 달라지지만 오른손 결함은 사라지지 않는다. 대상 본·시간·레이어·재질·표시 상태를 변경하지 않았다.
- **보조 원인:** `finger_weight_readout.txt`의 모서리 `(1827,1832)`는 원래 4.301mm인데 Battery 파지에서 5.970배, 전환기 파지에서 5.503배 늘어난다. 약지 쪽 정점 1832에 `RightThumbIntermediate=0.2279`가 들어가지만 바로 옆 1827에는 없고 대신 새끼손가락 영향이 들어간다. 다른 검지·중지 면에도 엄지 중간 관절이 약 10.8~18.0% 혼입돼 있다. 서로 다른 손가락의 영향이 인접 정점 사이에서 갑자기 바뀌는 공용 가중치 전이 결함이다.
- 동일 정지 위치의 중복 정점 쌍이 벌어지는 현상은 0개, 파지 카메라 깊이 범위 밖 손 정점도 0개였다. 실제 본/리그 목표는 일치하고 원본·후보 메시의 가중치도 일치하므로, 카메라나 이번 소품 연결의 차이로 설명되지 않는다.
- `diagnostic_03`의 정점 중심 보조 검사는 실패 0이었지만 삼각형 교차 판정까지 포함한 `diagnostic_04`와 후속 자연 재생 관찰에서는 전환기 29개·완충제 108개·주사기 53개의 손/소품 교차 면이 반복됐다. 면 교차와 시각 결함을 정점 검사 통과로 상쇄하지 않는다. 수정 전 Battery의 손/소품 교차는 0개이나 손가락 면 늘어남은 별도로 존재한다.
- 추가된 판정은 보조 검증을 보완한 것이며 기존 손목·도달·하향·접촉·몸통/팔 여유·정점 침투 기준은 완화하지 않았다. 최종 캡처는 실행하지 않았다.
- 세 소품·파지 설정은 적용된 작업 상태로 보존한다. 공용 `player.fbx`, 공용 가중치·원본 형상 및 파생 보정 메시에는 이번 작업에서 변경을 가하지 않았다. 근본 수정에는 이번 승인에서 제외된 공용 손 스킨과 수송자 전체 사용처·파생 메시의 수정 및 회귀 확인이 필요하므로 그 경계에서 중단한다.
- 검토 재생 해제 후 `restored_state.txt`에서 재생·일시정지·애니메이션 미리보기·프레임 디버거 꺼짐, 임시 관찰 카메라 0개, 관찰 비활성 및 입력 설정 미변경을 확인했다. `console_for_weight_scope.log`의 콘솔 오류는 0개, 경고는 1개다. 실제 키보드·마우스 입력 직접 검증은 하지 않았다.

## 승인 범위와 보존 기준

- `Converter_Shield_Plug`: `item model/shield switch.fbx`
- `Buffer_Shield_Plug`: `item model/Rapid protective barrier buffer.fbx`
- `Nanomachine_Inject`: `item model/Nanomachine therapeutics.fbx`
- 원본 파일은 보존하고 Unity용 사본으로 연결한다. 원본의 내장 색상·노멀·금속성·거칠기를 추출하며 URP용 금속성/매끄러움 채널 변환만 수행한다. 새 시각 디자인이나 원본 형상 변경은 없다.
- `Battery_Aux_Plug`, 기존 수송자 메시·스킨 가중치·본 길이·컨트롤러, 몸통·왼팔 궤적과 5초 시간표를 보존한다. 세 개체의 오른손 소품·손가락 리그 목표·물리적 끝점 오프셋 및 그 접점을 위한 오른팔 방향만 조정한다.
- 검증 우선순위는 직접 자연 재생·확대 화면 확인, 그 다음 수치 보조 확인이다. 관찰 중 대상의 본·시간·포즈·가시성을 조작하지 않는다. 이번 작업은 반복 진단 비교가 승인됐으며, 최종 캡처는 통과 후 한 번만 수행한다.

## 원본 직접 확인과 수정 전 기준

- `ShieldSwitch_source_*.png`, `RapidProtectiveBarrierBuffer_source_*.png`, `NanomachineTherapeutics_source_*.png`에서 두 핀·단일 바늘, 몸체 형태 및 원본 재질을 확인했다.
- 두 상자형 도구는 메시 로컬 +X가 핀 방향이고, 주사기는 -Y가 바늘 방향이다. FBX 루트의 -90도 X 회전과 100배 단위를 포함해 구분한다.
- `diagnostic_01/`은 교체 전 자연 재생이다. 전환기 손목 확대와 나노머신 대상 상체 시트를 직접 확인했다. 기존 승인 모션·배터리 파지가 유지되며, 보조 관찰 3,913개 개체-프레임에서 실패 0이었다.
- 최초 재생 진입 브리지의 완료 전에 캡처 요청을 보내 재생 조건 오류가 발생했다. 미완료 요청은 시간 초과로 종료했고, 이후 요청을 직렬로 보내 정상 재생에 진입했다. 실패 로그를 보존하며 캡처 성공으로 취급하지 않는다.

## 첫 적용: 미통과

- `apply_01.log`, `item_applied_213228.txt`에서 세 소품만 교체했다. 적용 전후 Battery_Aux_Plug 전체 직렬화 구성의 동일성과 몸통·왼팔 목표 궤적의 동일성을 확인했다. 공용 모션 런타임 코드는 변경하지 않았다.
- **직접 확인:** `diagnostic_02`에서 전환기 엄지가 벌어져 자연스러운 파지가 부족했고, 주사기 후방 테두리와 손가락이 겹쳤다. 하향 도구 접근·접촉은 보이지만 파지 완료로 판정하지 않았다.
- **보조 확인:** 전환기 중지 이격 5.379mm, 완충제 중지 이격 3.184mm, 주사기 엄지 최대 침투 3.719mm/76개 표본, 검지 최대 침투 2.508mm/20개 표본으로 실패 4개다. 기존 3mm 접촉 근접·0.5mm 초과 침투 금지 기준을 변경하지 않았다.
- 원인은 제작 계산의 단순 외곽 상자/원통이 실제 경사진 면·주사기 후방 테두리를 충분히 반영하지 못하고, 접촉 표본을 손가락 끝으로 제한하지 않아 벌어진 엄지도 해로 선택한 것이다.
- 다음 제작 계산은 실제 삼각형 표면의 거리와 끝마디 패드를 사용하며, 원래 손 리그의 굽힘 범위를 적용한다. BVH는 제작 계산 속도용이고 자연 재생의 독립 표면 판정기는 유지한다.

## 파일·명령

- 작업 전 사본: `Backups/ConsumableItemGrips_2026-09-09/before/`; 적용별 씬 백업은 같은 폴더의 `apply_HHmmss/`.
- 수정 도구: `Assets/_Project/Editor/ConsumableRiggedSharedMotionTools.cs`; 기존 브리지 진입점을 사용하되 `ApplyConsumableRiggedSharedMotion`은 이번 작업의 세 개체 교체 경로로 연결한다. 공용 리그를 재생성하지 않는다.
- `PlayerHandRigTools.HandItemSurface.ClosestTrianglePoint`의 접근 범위만 공유용으로 확장했다. 기존 알고리즘과 검증 허용값은 변경하지 않았다.
- Unity 반영·관찰은 `Refresh-UnityProject.ps1`, `Invoke-UnityEditorBridge.ps1`의 기존 소스 확인·적용·자연 재생·진단·종료 명령을 사용한다.
- 최종 완료 캡처는 아직 실행하지 않았다. 자동 테스트·빌드와 Git 쓰기는 실행하지 않았다.
