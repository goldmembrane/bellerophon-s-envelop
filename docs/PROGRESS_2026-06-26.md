# 2026-06-26 Progress

## 복도 단품 샘플 폐쇄 셔터 높이 재조정

- 사용자 지시:
  - 복도 샘플에서 복도 벽과 폐쇄 셔터의 높이가 서로 맞지 않으므로, 높이를 맞춰 샘플을 다시 제작하라고 지시했다.
- 반영 내용:
  - `scripts/GenerateShipCorridorSegmentSample.py`에서 폐쇄 셔터 높이를 `WALL_HEIGHT + 0.34`에서 `WALL_HEIGHT`로 변경했다.
  - 수평 복도와 경사 복도 모두에서 셔터 패널 높이가 복도 벽 높이 기준과 맞도록 조정했다.
  - 셔터 좌우 가이드 레일도 셔터 패널과 같은 높이로 맞췄다.
  - 샘플 설명 문구를 "상부 하우징에서 아래로 내려와 복도 벽 높이에 맞춰 닫히는 폐쇄 셔터"로 갱신했다.
  - `artSample/ship_corridor_segment/` 샘플을 다시 생성했다.
- 수정/생성 파일:
  - `scripts/GenerateShipCorridorSegmentSample.py`
  - `artSample/ship_corridor_segment/`
  - `docs/PROGRESS_2026-06-26.md`
- 실행한 명령:
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'`
  - `.\scripts\Run-ShipCorridorSegmentSample.ps1`
  - `Get-ChildItem` 및 `rg` 기반 결과 확인
- 확인 결과:
  - 샘플 생성 스크립트가 종료 코드 `0`으로 완료됐다.
  - `artSample/ship_corridor_segment/renders/`에 8개 렌더가 다시 생성됐다.
  - `blender/ship_corridor_segment.blend`, `exports/ship_corridor_segment.fbx`, `exports/ship_corridor_segment.glb`가 다시 생성됐다.
  - `APPROVAL_STATUS.json`과 `ASSET_MANIFEST.json`의 상태는 `미승인`, `unityApplicationAllowed=false`로 유지됐다.
- 수행하지 않은 작업:
  - Unity 씬, 프리팹, 런타임 자산 반영
  - 실제 구역 연결 배치
  - 충돌체, 네비게이션, 폐쇄 로직 구현
  - Restore/Ensure/Validate/Smoke/Test/Build

## 승인된 복도 단품 샘플 Unity 배치

- 사용자 지시:
  - `artSample/ship_corridor_segment/` 샘플을 승인한다고 했다.
  - 승인된 샘플처럼 수평 복도 5개와 경사 복도 5개를 Unity 씬에 배치하라고 지시했다.
  - 배치 위치는 운송창고 오른쪽이며, 운송창고와 겹치지 않고 복도끼리도 겹치지 않아야 한다고 지시했다.
  - 다른 기존 오브젝트를 건드리지 않아야 하며, 기존 테스트/검증 사다리도 실행하면 안 된다고 지시했다.
- 반영 내용:
  - `ApprovedArmoryShellBootstrap.cs`에 새 루트 `Approved Ship Corridor Segments`를 추가하는 전용 명령 `AddApprovedShipCorridorSegmentsOnly`를 추가했다.
  - `UnityEditorValidationBridge.cs`에 `AddApprovedShipCorridorSegmentsOnly` 브리지 명령을 추가했다.
  - 승인된 샘플의 길이, 폭, 벽 높이, 경사 높이, 셔터 상부 블록 높이/폭, 벽 안쪽 폭 기준, 경사 복도 셔터 상부 블록 각도를 Unity 프리미티브/메시 구조로 반영했다.
  - 수평 복도 5개와 경사 복도 5개를 새 루트 하위 오브젝트로만 생성했다.
  - 운송창고 렌더 bounds의 오른쪽(+X)에서 새 복도 루트 전체가 기존 렌더 bounds와 겹치지 않는 위치를 찾도록 했다.
  - 기존 루트 오브젝트들은 보호 스냅샷으로 캡처하고, 새 루트 추가 후 기존 오브젝트 transform/active 상태가 바뀌지 않았는지 확인했다.
- 수정/생성 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/PROGRESS_2026-06-26.md`
- 실행한 명령:
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedShipCorridorSegmentsOnly -LogPath .\Logs\AddApprovedShipCorridorSegmentsOnly.log -TimeoutSeconds 360`
  - `Get-Content` 및 `rg` 기반 로그/씬 확인
- 확인 결과:
  - 브리지 명령이 종료 코드 `0`으로 완료됐다.
  - 브리지 로그에 `Root=Approved Ship Corridor Segments`, `HorizontalSegments=5`, `SlopedSegments=5`가 기록됐다.
  - 브리지 로그에 `RightOfCargoHold=True`, `CorridorSegmentsNonOverlapping=True`, `ExistingObjectsUntouched=True`가 기록됐다.
  - 배치 중심은 `57.87,0.00,-15.84`, bounds는 `center=57.87,2.17,-15.84,size=80.27,4.35,10.05`로 기록됐다.
  - `CargoRunMvp.unity`에서 `Approved Ship Corridor Segments` 루트와 `SC-H01`~`SC-H05`, `SC-S01`~`SC-S05` 계열 오브젝트가 저장된 것을 확인했다.
- 수행하지 않은 작업:
  - 기존 오브젝트 수정/삭제/재생성
  - 운송창고 현재 상태 캡처/복구 기준 갱신
  - 복도를 실제 방/구역과 연결
  - 플레이어 이동, 충돌, 네비게이션 구현
  - 폐쇄/개방/정화 런타임 로직 구현
  - Restore/Ensure/Validate/Smoke/Test/Build
  - `Run-HarnessValidation`, `Run-EditModeTests`, `Run-PlayModeTests`, `Build-WindowsDev`

## 복도 단품 샘플 셔터 상부 블록 벽 관통 방지 및 경사 각도 반영

- 사용자 지시:
  - 셔터 위의 블록이 벽을 뚫어서도 안 되며, 경사 복도에서는 셔터 블록도 복도 각도에 맞추라고 지시했다.
- 반영 내용:
  - `SHUTTER_SIDE_CLEARANCE`를 추가하고, 셔터 상부 블록 폭을 `CORRIDOR_WIDTH - SHUTTER_SIDE_CLEARANCE`로 조정해 벽 안쪽 폭 안에 들어가도록 했다.
  - 셔터 패널, 셔터 전면 장갑판, 경고 라이트 스트립도 같은 벽 안쪽 폭 기준에 맞춰 조정했다.
  - 셔터 상부 블록의 윗면이 경사 복도 천장 평면을 넘지 않도록 `SHUTTER_HOUSING_HEIGHT / cos(pitch)` 기준으로 높이 계산을 조정했다.
  - 경사 복도 셔터 상부 블록에 `(0.0, -pitch, 0.0)` 회전을 적용해 복도 경사각과 맞췄다.
  - 샘플 설명 문구에 "벽 안쪽 폭과 천장 높이 안에 들어간 셔터 상부 블록" 및 "경사 복도 경사각에 맞춰 기울어진 셔터 상부 블록" 기준을 기록했다.
  - `artSample/ship_corridor_segment/` 샘플을 다시 생성했다.
- 수정/생성 파일:
  - `scripts/GenerateShipCorridorSegmentSample.py`
  - `artSample/ship_corridor_segment/`
  - `docs/PROGRESS_2026-06-26.md`
- 실행한 명령:
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'`
  - `.\scripts\Run-ShipCorridorSegmentSample.ps1`
  - `Get-ChildItem` 및 `rg` 기반 결과 확인
- 확인 결과:
  - 샘플 생성 스크립트가 종료 코드 `0`으로 완료됐다.
  - `artSample/ship_corridor_segment/renders/`에 8개 렌더가 다시 생성됐다.
  - `blender/ship_corridor_segment.blend`, `exports/ship_corridor_segment.fbx`, `exports/ship_corridor_segment.glb`가 다시 생성됐다.
  - 생성 스크립트와 샘플 문서에 벽 안쪽 폭 기준 및 경사각 반영 문구가 기록된 것을 확인했다.
  - `APPROVAL_STATUS.json`과 `ASSET_MANIFEST.json`의 상태는 `미승인`, `unityApplicationAllowed=false`로 유지됐다.
- 수행하지 않은 작업:
  - Unity 씬, 프리팹, 런타임 자산 반영
  - 실제 구역 연결 배치
  - 충돌체, 네비게이션, 폐쇄 로직 구현
  - Restore/Ensure/Validate/Smoke/Test/Build

## 복도 단품 샘플 상단 레일 제거 및 셔터 상부 블록 높이 조정

- 사용자 지시:
  - 위쪽 가이드 레일을 없애고, 셔터 위의 블록도 천장 높이보다 높지 않게 반영해 샘플을 다시 제작하라고 지시했다.
- 반영 내용:
  - 복도 벽 위쪽 `upper hand rail` 오브젝트를 생성하지 않도록 제거했다.
  - 셔터 양옆 `vertical shutter guide rail` 오브젝트를 생성하지 않도록 제거했다.
  - `SHUTTER_HOUSING_HEIGHT`를 별도 값으로 두고, 셔터 상부 블록의 윗면이 `floor_z + WALL_HEIGHT`를 넘지 않도록 배치했다.
  - 셔터 패널은 상부 블록 아래에서 바닥까지 내려오는 높이로 조정했다.
  - 샘플 설명 문구에서 상단 핸드레일과 가이드 레일 항목을 제거하고, "천장 높이 안에 들어간 셔터 상부 블록" 기준을 기록했다.
  - `artSample/ship_corridor_segment/` 샘플을 다시 생성했다.
- 수정/생성 파일:
  - `scripts/GenerateShipCorridorSegmentSample.py`
  - `artSample/ship_corridor_segment/`
  - `docs/PROGRESS_2026-06-26.md`
- 실행한 명령:
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'`
  - `.\scripts\Run-ShipCorridorSegmentSample.ps1`
  - `Get-ChildItem` 및 `rg` 기반 결과 확인
- 확인 결과:
  - 샘플 생성 스크립트가 종료 코드 `0`으로 완료됐다.
  - `artSample/ship_corridor_segment/renders/`에 8개 렌더가 다시 생성됐다.
  - `blender/ship_corridor_segment.blend`, `exports/ship_corridor_segment.fbx`, `exports/ship_corridor_segment.glb`가 다시 생성됐다.
  - `upper hand rail`, `vertical shutter guide rail`, `가이드 레일`, `상단 핸드레일` 문구가 생성 스크립트와 샘플 문서에서 남아 있지 않은 것을 확인했다.
  - `APPROVAL_STATUS.json`과 `ASSET_MANIFEST.json`의 상태는 `미승인`, `unityApplicationAllowed=false`로 유지됐다.
- 수행하지 않은 작업:
  - Unity 씬, 프리팹, 런타임 자산 반영
  - 실제 구역 연결 배치
  - 충돌체, 네비게이션, 폐쇄 로직 구현
  - Restore/Ensure/Validate/Smoke/Test/Build
