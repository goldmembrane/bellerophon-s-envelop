# 2026-09-20 진행 기록

## 사망 계열 래그돌 반복 검토

- `Death`는 원본 사망 클립을 1회 재생한 뒤 Animator와 물리가 동시에 뼈를 구동하지 않도록 전신 17개 뼈의 래그돌로 전환하고, 현재 누운 위치를 물리 바닥으로 사용해 0.5초 유지한 다음 첫 자세로 즉시 복귀하도록 구성했습니다. 실제 PlayMode에서 래그돌 중 골반 하강, 머리 상승, 루트 이동과 바닥 관통이 모두 `0m`이며 위치·회전 복귀 오차도 `0`인 것을 확인했습니다.
- `Death_FullBodyLaunch`는 시작 `0.3초` 뒤 Transform 이동이 아니라 Rigidbody 폭발 힘으로 무작위 수평·상향 방향에 날아가게 했습니다. 비행 중 17개 뼈와 제한된 관절로 인간형 골격을 유지하면서 전신이 자연스럽게 기울고 회전하며, 공통 물리 바닥에 쓰러진 뒤 0.5초 유지하고 첫 자세로 복귀합니다. 최종 직접 확인에서는 최대 골반 회전 `102.198도`, 공중 몸통 기울기 `45.43817도`, 관절 앵커 최대 분리 `0.025006m`로 정자세 고정과 연체동물 같은 붕괴가 모두 없었습니다.
- `Death_Tergo`에는 `player model/transfer laying.fbx`의 내장 Mixamo 애니메이션을 원본 곡선 변경 없이 연결했습니다. 전체 클립 재생 후 같은 전신 래그돌을 0.5초 적용하고, 누운 자리를 바닥으로 고정해 더 떨어지거나 머리가 들리지 않게 한 뒤 첫 자세로 즉시 복귀해 반복합니다.
- 직접 확인 자료는 `docs/validation/PlayerDamageReactionAnimations/DeathRagdollFinal.png`, `DeathRagdollGroundAndFullBodyLaunchFinal.png`, `DeathFullBodyLaunchTumbleBalance/Final.png`, `DeathFullBodyLaunchSkeletonStability/Final.png`, `DeathTergo/Review.png`입니다. 검증 대상을 조작하지 않은 자연 PlayMode 재생과 Rigidbody 물리를 기준으로 판정했습니다.
- 사용자 지시에 따라 `Hit_Reaction`, `Knockdown`, `Knockdown_GetUp`, `Death`, `Death_FullBodyLaunch`, `Death_Tergo` 진행 행은 통과 상태로 기록했습니다.

## Stun_Twist 원본 모션·승인 전기 아크 적용

- `Stun_Twist`에 `player model/transfer stunned.fbx`의 내장 Mixamo 애니메이션을 원본 동작 그대로 연결해 반복 재생했습니다.
- 승인된 청백색 전기 아크 샘플을 변경 없이 실제 개체에 적용했습니다. 아크는 가슴·복부·등 주변을 불규칙하게 순환하며 점멸하고, 게임 개체에 연결된 상태에서도 색상·분포·흐름이 승인 샘플과 일치합니다.
- 전면·후면 자연 PlayMode에서 원본 애니메이션 반복과 전기 아크의 가슴·복부·등 범위를 직접 확인했습니다. 최종 자료는 `docs/validation/StunTwistAnimationAndElectricArc/Final.png`와 `Applied/Final.png`입니다.
- 사용자 지시에 따라 `Stun_Twist` 진행 행을 통과 상태로 기록했습니다.

## Exhausted 상체 자세와 방향별 하체 2D Blend Tree

- `Exhausted_Walk_Forward`에는 `player model/transfer exhausted walking.fbx`의 내장 Mixamo 애니메이션을 연결했습니다. 상체를 총 `15도` 더 숙이고 양팔을 팔꿈치 과굴곡 없이 아래로 곧게 늘어뜨렸으며, 모든 손가락은 아래로 펴고 오른팔은 오른쪽 다리와 겹치지 않게 바깥쪽으로 `8도` 조정했습니다.
- `Exhausted_Idle`, `Exhausted_Walk_Backward`, `Exhausted_Walk_Sidestep`, `Exhausted_Walk_Diagonal`은 각각 독립된 `FreeformCartesian2D` Blend Tree를 사용합니다. 루트·골반·양다리는 대응하는 Player 원본 동작이 주도하고, `Spine02` 상체 가지에만 `Exhausted_Walk_Forward`의 탈진 상체와 보행 흔들림을 유지해 네 개체가 전진 방향으로 끌려가지 않게 했습니다.
- 자연 PlayMode에서 각 대상과 대응 Player 하체, 탈진 상체를 같은 위상으로 나란히 직접 확인했습니다. 방향별 하체와 이동은 원본과 일치하고, 허리 경계 튐·발 미끄러짐·몸통 또는 사지 관통·메시 뒤틀림은 보이지 않았으며 Unity 콘솔 오류는 `0`이었습니다. 최종 자료는 `docs/validation/ExhaustedWalkForward/RightArmLegClearance/Final.png`와 `docs/validation/ExhaustedLocomotionBlendTrees/DirectionalLayerCorrection/Final.png`입니다.
- 사용자 지시에 따라 `Exhausted_Idle`, `Exhausted_Walk_Forward`, `Exhausted_Walk_Backward`, `Exhausted_Walk_Sidestep`, `Exhausted_Walk_Diagonal` 진행 행을 통과 상태로 기록했습니다.

## Fatigue_HeadShake·Confused_Walk_Forward 상태 이동

- `Fatigue_HeadShake`는 `player model/transfer tired.fbx`의 내장 Mixamo 상체를 계속 유지하면서 Player 대기·전진·후진·옆걸음·대각선 하체를 각각 1초씩 순회합니다. 이후 대기 하체에서 고개를 아래로 `10도` 숙이고 좌우 `20도`로 네 번 흔들며, 각 회차는 `0.4초`, `0.3초`, `0.15초`, `0.15초`로 가속한 뒤 전체 6초 주기를 반복합니다.
- `Confused_Walk_Forward`에는 `player model/transfer dizzy.fbx`의 내장 Mixamo 상체를 유지하고 Player 대기·전진·후진 하체를 각각 1초씩 순회하는 `FreeformCartesian2D` Blend Tree를 적용했습니다. 움직임에 따른 혼란 상체 흔들림은 끊지 않았고, 시각 Z 정렬은 `Fatigue_HeadShake`와 매 프레임 일치하도록 대상 전용 보정으로 분리했습니다.
- 실제 실행에서 방향별 하체 자세, 연속적인 상체 움직임, 머리 흔들기 가속, 반복 경계와 메시·관절 연결을 직접 확인했습니다. 최종 자료는 `docs/validation/FatigueHeadShakeLocomotion/Final.png`, `docs/validation/FatigueHeadShake/Final.png`, `docs/validation/ConfusedWalkForwardLocomotion/Final.png`, `docs/validation/ConfusedWalkForwardZAlignment/Final.png`입니다.
- 사용자 지시에 따라 `Fatigue_HeadShake`, `Confused_Walk_Forward` 진행 행을 통과 상태로 기록했습니다.

## Knockback_Reaction 물리 반동·착지 회복

- `Knockback_Reaction`은 `player model/transfer knockback.fbx`의 내장 Mixamo 상체와 `Player_Idle` 하체를 분리해 사용합니다. 사전 대기 없이 첫 물리 스텝부터 개체 후방 3m의 폭발 원점에서 `AddExplosionForce`를 적용하며 Transform 경로 이동은 사용하지 않습니다.
- 비행 중 양팔은 실제 반동 이동 방향으로 곧게 뻗고 양손바닥도 같은 방향을 향합니다. 양다리는 제한된 `ConfigurableJoint` 물리 반응으로 골격을 유지하되 고정 자세가 되지 않게 했습니다. 약 `2.5m` 이동 후 착지·회복 구간 전체에서 양팔을 실제 `Player_Idle` 양팔 자세로 자연스럽게 보간하고, 1.5초 유지 뒤 시작점으로 즉시 복귀해 다음 반동을 반복합니다.
- 자연 PlayMode 직접 확인에서 총 이동 `2.537081m`, 측면 이동 `0.500893m`, 이동 방향 일치도 `0.985289`, 다리 자세 변화 `32.26263도`, 팔 굽힘 `0.019782도`, 손바닥 방향 오차 `0도`, 최종 Player_Idle 팔 위치·회전 오차 `0`을 확인했습니다. 최종 자료는 `docs/validation/KnockbackReactionImmediateLaunch/Final.png`, `KnockbackReactionFlightPose/Final.png`, `KnockbackReactionLandingArmRecovery/Final.png`입니다.
- 사용자 지시에 따라 `Knockback_Reaction` 진행 행을 통과 상태로 기록했습니다.

### 이번 통합 기록에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 재실행·리프레시·씬 저장·애니메이션 재생성·추가 캡처
- 사용자가 지시하지 않은 진행 HTML 통과 체크 변경

## PostureBreak 누운 수송자 반복 모션·테르고 하향 공격 배치

- 씬의 수송자 개체를 직접 조사해 `PlayerAnimationLayout/Death_Tergo`가 누워 있는 원본임을 확인했습니다. 이 개체에 연결된 `Assets/_Project/Animations/PlayerDamageReactions/Death_Tergo_Retargeted.anim`의 곡선, 키, 이벤트, 30fps·3.466667초 타이밍과 클립 설정을 변경하지 않은 복사본을 `Assets/_Project/Animations/PlayerStatusEffects/PostureBreak/PostureBreak_Lying.anim`으로 만들고 `PostureBreak`에 연결했습니다. Unity가 복사 자산의 표시 이름만 파일명에 맞게 바꿨으며 애니메이션 내용은 원본과 일치합니다.
- 원본 클립의 `loopTime=False`도 그대로 유지했습니다. 전체 동작 반복은 클립을 수정하지 않고 `PostureBreak_Lying.controller`의 종료 시점 1.0, 전환 시간 0초인 자기 전환으로 구현했습니다.
- `Approved Tergo Enemy Placement` 아래 모든 테르고 상태를 조사한 뒤, 실제 프레임에서 양쪽 드릴 팔을 번갈아 아래로 내리치는 `Tergo_07_Downed_Pounce`를 원본으로 확정했습니다. 현재 씬 개체를 `Tergo_07_Downed_Pounce_PostureBreakCopy`로 그대로 복제해 `PostureBreak`의 수평 정면 2m, 같은 루트 바닥 높이에 배치했습니다.
- 테르고 복제본은 원본의 계층, 컴포넌트, 머티리얼, Animator Controller, 애니메이션, 회전, 스케일을 변경하지 않았습니다. 원본 `Death_Tergo`, 원본 `Tergo_07_Downed_Pounce`, 원본 애니메이션 자산도 변경하지 않았습니다.
- `PostureBreak` 진행 행의 통과 체크와 실행 시작 지점은 변경하지 않았습니다.

### 직접 확인

- 1순위 원본 직접 확인 자료: `Logs/PostureBreakSources.png`
- 1순위 최종 직접 확인 자료: `docs/validation/PostureBreakLyingTergoPlacement/Final.png`
- 원본 확인 시 `Death_Tergo`의 시작·중간·끝이 모두 누운 수송자 동작임을 확인했습니다. `Tergo_07_Downed_Pounce`는 기립 상태에서 대상 위로 내려온 뒤 좌우 드릴 팔을 번갈아 아래로 내리치는 동작임을 실제 다섯 위상에서 확인했습니다.
- 최종 시트 위 행에서 `PostureBreak`의 복사 모션 시작·중간·끝이 원본 누운 자세를 유지하는 것을 확인했습니다. 아래 행에서는 테르고 복제본이 수송자 정면에 분리 배치된 상태로 원본 공격 위상을 그대로 재생하는 것을 확인했습니다.

### 보조 검증

- 수송자 복사 클립의 곡선·키·오브젝트 참조·이벤트·타이밍·클립 설정 서명 일치 `True`
- 수송자 반복 관측 `2회`, 중간 자세 최대 회전 변화 `73.55134도`, 최대 위치 변화 `0.008505m`
- 테르고 반복 관측 `2회`, 중간 자세 최대 회전 변화 `87.73598도`, 최대 위치 변화 `2.349082m`
- 테르고 원본 대비 복제본 계층·컴포넌트·머티리얼·Animator Controller·회전·스케일 일치 `True`
- 배치 정면 거리 `2m`, 측면 오차 `0m`, 루트 바닥 높이 차 `0m`
- Unity 콘솔 오류 `0`
- 첫 원본 확인 캡처에서 두 프리뷰가 같은 전용 레이어와 원점에 동시에 활성화되어 겹쳤습니다. 씬 원본은 변경하지 않은 채 한 번에 한 프리뷰만 렌더링하도록 수정했습니다.
- 다음 원본 확인에서 오프스크린 스키닝 경계 갱신이 늦어 테르고 일부가 잘렸습니다. 각 프레임의 실제 스킨 메시를 베이크해 전체 몸과 양팔을 직접 확인했습니다.
- 첫 적용은 Unity가 복사본 표시 이름을 파일명으로 변경한 차이까지 애니메이션 내용 서명에 포함해 중단됐습니다. 표시 이름을 자산 식별 정보로 분리하고 곡선·키·이벤트·타이밍·설정만 엄격 비교하도록 수정한 뒤 적용했습니다.

### Unity 반영·검증 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakSources`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyPostureBreakAnimationAndTergoPlacement`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakAnimationAndTergoPlacement`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakAnimationAndTergoPlacementRuntime`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CapturePostureBreakAnimationAndTergoPlacementFinal`

### 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- 원본 수송자·테르고 개체, 원본 애니메이션 곡선, 원본 테르고 회전·스케일·Animator Controller 수정
- `PostureBreak` 진행 HTML 통과 체크
- 실행 시작 지점 변경
- Unity 에디터 재시작
- Git 커밋·푸시

## PostureBreak 테르고 첫 가슴 찌르기 이후 1초 주기 움찔

- `PostureBreak_Lying.anim`의 양쪽 `UpLeg`·`Leg`·`Foot`·`ToeBase` 전체 체인을 각 뼈의 애니메이션 첫 자세로 고정했습니다. 따라서 평상 구간에는 다리가 움직이지 않으며, 루트·상체·Tergo 공격 애니메이션과 자기 반복 구조는 그대로 유지됩니다.
- Tergo 공격 클립의 준비&#183;접근 구간과 가슴 찌르기 구간을 실제 PlayMode에서 같은 측면&#183;몸 정면 구도로 직접 비교했습니다. 공격 시작 후 정규화 위상 `35%`, 현재 `7.216667초` 클립의 `2.525833초`부터 드릴 하강 타격이 가슴 지점에 들어오므로 이 시간을 움찔 활성 경계로 고정했습니다.
- 독립 가산 레이어 `PostureBreak_UpperBodyTwitch`의 길이를 Tergo 공격 주기와 같은 `7.216667초`로 맞췄습니다. `0.00~2.525833초`에는 기준 자세만 유지하고, `2.525833초`부터 상체 약 `20도`와 양 무릎 바깥쪽 `7도` 움찔을 1초마다 반복합니다. 각 움찔은 시작 후 `0.10초`에 정점, `0.20초`에 기준 자세로 복귀하며 상체와 양 무릎의 타이밍은 같습니다.
- 적용 자산은 `Assets/_Project/Animations/PlayerStatusEffects/PostureBreak/PostureBreak_Lying.anim`, `PostureBreak_UpperBodyTwitch.anim`, `PostureBreak_UpperBodyTwitch.mask`, `PostureBreak_Lying.controller`입니다. 씬을 저장하지 않았고 원본 `Death_Tergo_Retargeted.anim`, 대상 루트와 Tergo 배치, `CargoRunMvp.unity`는 변경하지 않았습니다. 진행 HTML에는 구현 기록만 갱신했으며 통과 체크는 하지 않았습니다.

### 직접 확인

- 후보 비교: `Logs/PostureBreakUpperBodyTwitch/Sources.png`
- 실제 PlayMode 두 주기 직접 확인: `Logs/PostureBreakUpperBodyTwitch/PlayMode.png`
- 최종 확인: `docs/validation/PostureBreakUpperBodyTwitch/Final.png`
- 실제 PlayMode 씬의 수송자와 Tergo를 복제하거나 포즈를 조작하지 않고 두 공격 주기의 `주기 시작`, `공격 0.10초 전`, `첫 가슴 찌르기 시작`, `첫 정점`, `첫 복귀`, `두 번째 시작`, `두 번째 정점`, `두 번째 복귀`, `세 번째 정점`을 측면과 누운 몸 정면에서 확인했습니다. 두 주기 모두 공격 전에는 추가 움찔이 없고, 첫 가슴 찌르기와 함께 움직이기 시작해 이후 정확히 1초 주기로 반복했습니다. 양다리 첫 자세 유지, 상체&#183;무릎 동시 복귀, 다리 교차&#183;발목 꺾임&#183;메시 관통 없음과 Tergo 배치 보존도 확인했습니다.
- 최종 파일은 직접 확인한 실제 PlayMode 이미지와 SHA-256 `59E8B96649DDE1E3AEBD54358C5E380D1D80E3D80BCA275DB46711BFCFC29CAE`로 동일합니다.

### 보조 검증

- 공격 전 무움찔 구간 `0.00~2.525833초`, 첫 가슴 찌르기 시작 `2.525833초`, 펄스 주기 `1초`, 공격 주기당 펄스 `5회`, 정점 오프셋 `0.10초`, 복귀 오프셋 `0.20초`
- 상체 회전 변화 `19.99998도`, 왼쪽 무릎 바깥 회전 `6.999901도`, 오른쪽 무릎 바깥 회전 `6.999901도`
- 실제 PlayMode 관측에서 상체 상승과 양 무릎 간격 증가는 같은 초기 움찔 구간에 발생했습니다. 캡처 프레임의 실제 위상 차이로 관측량은 주기별로 달랐지만, 직렬화된 가산 클립의 양 무릎 회전은 각각 정확히 약 `7도`이며 상체 키와 동일 시각에 배치됐습니다.
- 실제 두 공격 주기에서 PostureBreak 가산 레이어와 Tergo Animator의 최대 시간 오차 `0초`
- 기본 클립 양다리 전체 체인의 최대 자세 드리프트 `0도`, 가산 레이어 공격 주기 반복 관측 `2회`, 기존 PostureBreak 전체 동작 반복 `2회`, Tergo 원본 공격 반복 `2회`
- 원본 `Death_Tergo_Retargeted.anim` SHA-256 `57902F35E31B2A8CC14C637ADAEB25321BB0231BE79AB74C7B14900840995E36`
- 대상 기본 `PostureBreak_Lying.anim` SHA-256 `75FF8C6E0857B3C0AE08A0FA09FEFC2681007548B5AD8DF000A661693709298C`
- Tergo 공격 `Tergo_Downed_Pounce_FromFbx.anim` SHA-256 `488BB40C7E50AB4E63589929B4248EE1F5A52BCB9F5742831A7EC5D8B710E154`
- 가산 `PostureBreak_UpperBodyTwitch.anim` SHA-256 `D4976C8FA756A62F40773FC86C3AEC57F190CEEC29ED85CA26BBC18B3CD8D4DC`
- 씬 `CargoRunMvp.unity` SHA-256 `D98034EB253F814B9C9EE6FE1FFB7814772BCF50BC294FD7C7E1294001D850C1`, Unity 콘솔 오류 `0`

### Unity 반영·검증 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTwitchAttackSyncSources`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyPostureBreakTwitchAttackSync`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTwitchAttackSync`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTwitchAttackSyncPlayMode`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTwitchAttackSyncRuntime`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CapturePostureBreakTwitchAttackSyncFinal`

### 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- 원본 수송자·테르고 개체, 원본 `Death_Tergo_Retargeted.anim` 및 씬 수정
- `PostureBreak` 진행 HTML 통과 체크
- 실행 시작 지점 변경
- Unity 에디터 재시작
- Git 커밋·푸시

## PostureBreak 양다리 전체 40% 안쪽 모으기

- `PostureBreak`의 기존 누운 동작과 반복 타이밍을 유지한 채 양쪽 허벅지부터 발까지 전체 간격을 약 40% 안쪽으로 모았습니다. 원본 `Death_Tergo_Retargeted.anim`은 변경하지 않고 대상 전용 `PostureBreak_Lying.anim`에서 좌우 `UpLeg`·`Leg`의 회전 곡선만 조정했습니다.
- 첫 후보는 발 회전까지 별도로 보정해 부츠와 발끝이 안쪽으로 말리며 겹쳐 보였으므로 직접 확인에서 기각했습니다. 최종안은 발 로컬 회전을 원본 그대로 유지하고 허벅지·종아리의 골격 길이를 보존하면서 무릎과 발목 위치만 안쪽으로 유도했습니다.
- PlayMode 전환 후 직접 캡처 요청이 `pending`에 머문 원인은 `PostureBreakLegClosePlayMode`가 브리지의 활성 요청 보존 목록에서 누락된 것이었습니다. `UnityEditorValidationBridge`에 해당 명령을 등록하고 리프레시한 뒤 실제 실행모드 캡처를 정상 완료했습니다.
- 상체·루트, Tergo 복제본의 위치·회전·스케일·계층·머티리얼·Animator Controller, 원본 수송자·테르고 애니메이션은 변경하지 않았습니다. 진행 HTML에는 구현 기록만 추가했으며 통과 체크는 하지 않았습니다.

### 직접 확인

- 후보 비교: `Logs/PostureBreakLegClose/Sources.png`
- 실제 PlayMode 8개 위상 정면·측면 확인: `Logs/PostureBreakLegClose/PlayMode.png`
- 최종 확인: `docs/validation/PostureBreakLegClose/Final.png`
- 정면 전 구간에서 허벅지부터 발목까지 간격이 일관되게 좁아졌고, 양다리 메시 교차·뒤집힘·발목 꺾임이 보이지 않았습니다. 측면에서는 기존 누운 동작의 무릎 굽힘, 다리 길이와 전체 타이밍이 유지됐습니다.
- 최종 파일은 직접 확인한 실제 PlayMode 이미지와 SHA-256 `32E6EB3D259C3FDC164154C28092B4CFF4AA45432154F6CF38A9E20AC7DF2AC9`로 동일합니다.

### 보조 검증

- 평균 무릎 간격 `0.606328m -> 0.363785m` (`40.0019%` 감소)
- 평균 발목 간격 `0.399665m -> 0.239801m` (`39.9994%` 감소)
- 평균 발끝 간격 `0.473198m -> 0.300350m` (`36.5276%` 감소, 원본 발 방향 보존)
- 실제 PlayMode 평균 간격: 무릎 `0.363805m`, 발목 `0.238787m`, 발끝 `0.300945m`
- 변경 곡선은 좌우 `UpLeg`·`Leg` 회전 4개 뼈만 해당하며 상체·루트 곡선 일치 `True`
- PostureBreak 전체 동작 반복 `2회`, Tergo 원본 공격 반복 `2회`, Unity 콘솔 오류 `0`
- Tergo 위치·회전 오차 `0`, 보호 씬 저장 `False`

### Unity 반영·검증 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakLegCloseSources`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyPostureBreakLegClose`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakLegClose`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakLegClosePlayMode`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakLegCloseRuntime`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CapturePostureBreakLegCloseFinal`

### 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- 원본 수송자·테르고 개체 및 원본 애니메이션 수정
- `PostureBreak` 진행 HTML 통과 체크
- 실행 시작 지점 변경
- Unity 에디터 재시작
- Git 커밋·푸시

## PostureBreak 테르고 복부 탑승·상부 가슴 공격 배치 조정

- 20:25 사용자 실행 영상의 전체 공격 구간을 4fps로 직접 다시 확인한 결과, 이전 루트 위치에서는 드릴 끝이 빨간 가슴 영역이 아니라 얼굴·헬멧으로 내려오고 있었습니다. 이전 완료 판정을 폐기하고, 손목 뼈 거리만으로 공격점을 판정하던 보조 수치 검사를 직접 시각 판정 뒤의 구조 확인으로 낮췄습니다.
- 20:44 사용자 실행 영상을 4fps, 총 54프레임으로 직접 확인한 결과, 가슴 공격 위치는 맞지만 테르고 골반·허벅지 아래에 지속적인 공중 간격이 남아 있었습니다. 원본 공격 애니메이션은 변경하지 않고 `Tergo_07_Downed_Pounce_PostureBreakCopy`의 수송자 발 방향 이동 `0.28m`와 회전은 유지한 채 루트 높이만 `0.32m`에서 `0.20m`로 낮췄습니다. 최종 루트 위치는 `110.104431, 0.2, -334.805328`, 회전은 `0, 4.209275, 0`입니다.
- 실제 Play Mode 씬 개체를 이동·복제·레이어 변경하지 않은 상태에서 사용자 기준 이미지와 같은 머리 왼쪽·발 오른쪽 측면 구도로 0%·12.5%·25%·35%·41.6667%·50%·75%·87.5%를 다시 확인했습니다. 착석 위상에서 골반·허벅지 아래 공중 간격이 사라지고 양 허벅지·무릎이 수송자 허리·복부 양옆에 맞닿아 걸터앉는 실루엣이 되었습니다. 양쪽 드릴 끝은 얼굴·바이저를 벗어나 빨간 박스의 상부 가슴으로 내려오며, 눈에 보이는 메시 관통은 없습니다.
- 원본 `Tergo_07_Downed_Pounce`, `PostureBreak`, 양쪽 애니메이션 곡선·클립·Animator Controller, 복제본의 스케일·계층·컴포넌트·머티리얼은 변경하지 않았습니다. 진행 HTML에는 구현 기록만 갱신했으며 통과 체크는 하지 않았습니다.

### 직접 확인

- 초기 가슴 기준 후보 비교: `Logs/PostureBreakTergoChestMountSources.png`
- 실제 영상 프레임 비교: `Logs/PostureBreakTergoUserVideoContactSheet.png`, `Logs/PostureBreakTergoUserVideoCroppedSheet.png`
- 허리 위치 후보 비교: `Logs/PostureBreakTergoWaistMountSources.png` (`Hips` 0%, 25%, 50%)
- 선택 후보 전체 타임라인 확대: `Logs/PostureBreakTergoWaistMountTimeline.png`
- 적용 상태 진단: `Logs/PostureBreakTergoChestMountInspection.png`
- 실제 Play Mode 전체 구간 직접 확인: `Logs/PostureBreakTergoChestMountPlayMode.png`
- 좌우 드릴 타격 확대: `Logs/PostureBreakTergoChestMountStrike35.png`, `Logs/PostureBreakTergoChestMountStrike42.png`
- 최종 확인: `docs/validation/PostureBreakTergoChestMount/Final.png`
- 최종 파일은 실제 Play Mode 씬 개체를 제자리에서 렌더링한 진단 파일과 SHA-256 `01A7B1D594F8988DD2EC0B46872E161A46A486CBC9FF2D8999A723CA30FE7232`로 동일합니다. 기준 이미지와 같은 측면 구도에서 노란 탑승 영역의 허벅지·무릎 접촉, 빨간 가슴 타격 영역, 얼굴 비접촉과 비관통을 직접 비교했습니다.

### 보조 검증

- 적용 위치 `110.104431, 0.2, -334.805328`, 회전 Euler `0, 4.209275, 0`
- 기존 위치 대비 수송자 발 방향 이동 `0.28m`
- 이전 공중 배치 대비 루트 수직 하강 `0.12m`
- 선택 기준 대비 위치 오차 `0m`, 회전 오차 `0도`
- 25%·50%·75% 평균 골반 수평 중심 오차 `0.00001m`, 수송자 머리 방향 일치도 `1`
- 손목 뼈 기준 상부 몸통 최근접 거리 왼쪽 `0.173117m`, 오른쪽 `0.213147m`; 드릴 끝의 실제 타격점은 Play Mode 측면 프레임으로 별도 판정
- 수송자 반복 관측 `2회`, 테르고 반복 관측 `2회`
- 원본 테르고 미변경, 복제본 계층·컴포넌트·머티리얼·Animator Controller·스케일 일치 `True`
- Unity 콘솔 오류 `0`
- 현재 씬 길이 `17784714 bytes`, SHA-256 `D98034EB253F814B9C9EE6FE1FFB7814772BCF50BC294FD7C7E1294001D850C1`

### Unity 반영·검증 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTergoChestMountSources`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTergoWaistMountSources`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTergoWaistMountTimeline`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyPostureBreakTergoChestMount`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTergoChestMount`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTergoChestMountRuntime`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectPostureBreakTergoChestMountPlayMode`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CapturePostureBreakTergoChestMountFinal`

### 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- 원본 수송자·테르고 개체 및 애니메이션 수정
- `PostureBreak` 진행 HTML 통과 체크
- 실행 시작 지점 변경
- Unity 에디터 재시작
- Git 커밋·푸시
