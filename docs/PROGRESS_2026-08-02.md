# 진행 기록 — 2026-08-02

## 파후르·쿠르사 누적 작업 정리 및 쿠르사 아트 샘플 승인

- 상태: `PAHUR_WORK_RECORDED_KURSA_ART_SAMPLE_USER_APPROVED_GIT_HANDOFF_PENDING`
- 사용자 승인일: `2026-08-02`
- 쿠르사 승인 리비전: `USER_CROP_VISOR_GROOVE_2X_EYE_SIZE_NO_SHEAR_PROJECTION`
- 쿠르사 샘플 상태: `APPROVED_FOR_UNITY_NOT_APPLIED`

### 파후르 완료 작업 요약

- 파후르 애니메이션 목록을 대기, 이동, 미니 화염방사기 발사, 돌파태세 화염방사,
  수호태세 전용 발사, 정지, 수호태세 진입, 수호태세 해제, 피격, 사망의 총 `10종`으로 확정했다.
- 승인된 적갈색 파후르 외형을 Unity의 기존 파후르 배치에 동기화했다.
- 접지 대기, 이동, 미니·돌파·수호 화염방사 모션을 구성하고 각 지정 모델의
  내장 Mixamo 애니메이션과 승인 외형을 연결했다.
- 정지 모션은 `2초` 동안 고개를 `45°` 숙이고 안광을 `#030303`으로 바꾼 뒤
  `1초` 유지하고 복귀 구간 없이 반복하도록 구성했다.
- 수호태세 진입은 `0.8초` 전환과 `1초` 유지, 해제는 `1.2초` 전환과
  `1초` 유지 뒤 복귀 구간 없이 반복하도록 구성했다.
- 파후르 배치 슬롯을 `11개`로 확장하고 피격·사망 전용 FBX를 교체했다.
- 피격 모션은 전체 구간에서 오른팔 총이 바닥과 평행하고 정면을 향하도록 보정했다.
- 사망 모션은 쓰러진 자세를 `1초` 유지하고 오른팔 옆면이 바닥 방향을 향하도록
  보정했으며, 사용자 정정에 따라 모델과 Armature의 Y축 하강은 제거했다.
- 세부 구현·수치·전용 검증 기록은 `docs/PROGRESS_2026-07-31.md`와
  `docs/PROGRESS_2026-08-01.md` 및 각 `docs/validation/pahur_*` 경로에 보존했다.

### 쿠르사 완료 작업 요약

- 원본 기획서와 사용자 확정을 바탕으로 쿠르사 애니메이션 목록을 총 `11종`으로 문서화했다.
- 지정된 `KUŠkursa.fbx`를 사용해 쿠르사 모델을 `12개` 배치하고 파후르 아래쪽에서
  파후르와 같은 X축 간격으로 정렬했으며, 시작 시점에서 쿠르사 정면을 보도록 구성했다.
- 기준 이미지에서 색 배치, 기계 장갑 질감, 패널 이음선, 금속 마모, 후드·목도리 문양,
  무광 방패의 방향성 긁힘과 오염을 분석해 `artSample/enemies/kursa/appearance_reference_sync/`
  아래 머티리얼 전용 외형 샘플을 제작했다.
- 메시 형상·UV·웨이트·리그·내장 애니메이션은 변경하지 않았고 정점 `2,109`,
  에지 `5,936`, 면 `3,913`, 루프 `11,739`, 본 `24`를 유지했다.
- 사용자 제공 얼굴 부분을 3/4 렌더에 직접 대조해 눈 중심을 폴리곤 `3801`, `3627`의
  바이저 아래 가로 홈으로 확정하고, 좌우 눈 크기를 2배로 확대했다.
- 공통 시각 투영 평면과 동일 깊이 마스크를 적용해 한쪽 눈의 전단·수직 잘림을 제거했으며,
  최종 청색 형태 비율을 왼쪽 약 `1.39:1`, 오른쪽 약 `1.37:1`로 맞췄다.
- 사용자가 최종 샘플을 승인했다. `APPROVAL_STATUS.json`의
  `approved_for_unity`는 `true`, `unity_runtime_applied`는 `false`다.
- 실제 Unity 머티리얼·텍스처 적용은 별도 묶음 승인을 받아 진행한다.

### 이번 인계에서 실행하지 않은 항목

- 하네스 검증 및 관련 작업
- Unity 실행·리프레시·추가 반영
- 승인된 쿠르사 외형의 추가 변경

## 쿠르사 사용자 지정 안면부 눈 위치 재교정

- 상태: `ART_SAMPLE_USER_CROP_VISOR_GROOVE_2X_EYE_SIZE_NO_SHEAR_PROJECTION_USER_APPROVED_NO_UNITY_CHANGES`
- 대상: `artSample/enemies/kursa/appearance_reference_sync`
- 사용자 선택: 별도 입체 눈 오브젝트를 생성하지 않고 기존 기준 이미지의 눈 텍스처를 사용

### 이전 판정 무효화

- 2026-08-01 기록의 단일 평면 `X +5.25` 이동 결과는 최종 눈 위치 판정으로 사용하지 않는다.
- 원본 머리는 프레임 1에서 사선으로 꺾여 있어 로컬 X/Y 한 평면에 양눈 이미지를 함께 투영하면
  한쪽 눈이 실제 눈 홈이 아닌 측면 스트랩과 관자 표면에 걸린다.
- 이전 기록의 `두 육각형 안와 프레임` 판정도 원본 메시 다각도 조사 결과와 일치하지 않아 폐기했다.
- 코 양옆 삼각면을 눈 홈으로 판단했던 폴리곤 `211/512/2302`, `1909/1910/1911/2174` 판정도
  사용자가 지정한 이미지 부분과 일치하지 않으므로 최종 위치에서 폐기했다.

### 다각도 조사

- 원본 FBX의 머리 연결 표면 7을 프레임 1에서 평가하고 정면, 좌우 30도, 좌우 60도 및
  15도 간격 로컬 회전 진단 렌더를 생성했다.
- 전체 후드 형상에 대한 평균 폴리곤 법선, `headfront` 본 방향, 전체 머리 양측 깊이 대칭축은
  비대칭 후드와 스트랩 때문에 실제 안면 정면을 선택하지 못해 최종 기준에서 제외했다.
- 포즈 정면에서 원본 바인드 포즈 로컬 X 구간을 색상화해 눈 홈과 측면 스트랩을 구분했다.
- 잘못 선택됐던 폴리곤 `213`, `1557`은 눈 홈이 아니라 측면 스트랩임을 확인해 제외했다.
- 그 뒤 사용했던 넓은 돌출 안면 판 `2105/2107` 및 `396/397/670/...` 묶음도 사용자가 지적한
  두 오목면의 바닥이 아니므로 최종 판정에서 폐기했다.
- 사용자가 제공한 `1111.png`는 650×387 캔버스이며 실제 얼굴 부분은 좌상단 48×27 픽셀이다.
- 이 48×27 부분을 현재 생성 렌더 전체와 픽셀 대조한 결과
  `renders/02_three_quarter_kursa_reference_match.png`의 `x=534..606`, `y=154..194` 영역과
  1.5배 대응으로 일치했다. 이 영역은 관자나 코 양옆 삼각면이 아니라 바이저 바로 아래의 어두운 가로 홈이다.
- 제공 이미지에서 밝기 75 미만의 어두운 영역을 좌우로 분리한 무게중심은
  왼쪽 `(10.356,14.105)`, 오른쪽 `(34.362,13.221)`이며, 생성 렌더 좌표로 각각
  `(550,176)`, `(586,174)`에 해당한다.
- 위 두 화면 좌표를 프레임 1 원본 메시로 광선 역투영해 다음 표면 위치를 사용했다.
  - 화면 왼쪽: 폴리곤 `3801`, 로컬 위치 `(3.343094, 151.815475, 24.579956)`,
    로컬 법선 `(-0.182243, -0.571750, 0.799931)`
  - 화면 오른쪽: 폴리곤 `3627`, 로컬 위치 `(5.916458, 152.454803, 19.357758)`,
    로컬 법선 `(0.257649, -0.965079, -0.047329)`

### 수정 내용

- 기준 이미지의 기존 좌안과 우안을 각각 투명 텍스처로 분리했다.
  - `textures/kursa_eye_left_reference_overlay.png`
  - `textures/kursa_eye_right_reference_overlay.png`
- 각 눈을 해당 패치의 수직 접선과 수평 접선으로 구성한 별도 접평면에 투영했다.
- 사용자가 확정한 눈 중심·대상 폴리곤·표면 마스크 법선은 유지하고, 후속 지시에 따라 가로·세로 투영 크기를 정확히 2배로 확대했다.
- 왼쪽 크기: `(4.174058, 4.494025)` → `(8.348116, 8.988050)`
- 오른쪽 크기: `(5.038335, 4.448842)` → `(10.076670, 8.897684)`
- 좌우 눈 텍스처를 각각 폴리곤 `3801`, `3627`의 역투영 적중점과 표면 법선 기준 접평면으로 이동했다.
- 메시 형상, UV, 웨이트, 리그, 내장 애니메이션, 몸체·방패 머티리얼은 변경하지 않았다.

### 진단 및 검토 산출물

- 원본 로컬 눈높이 표면 지도:
  `artSample/enemies/kursa/appearance_reference_sync/EYE_SURFACE_MAP.json`
- 프레임 1 포즈 X 구간 지도:
  `artSample/enemies/kursa/appearance_reference_sync/EYE_POSE_MAP.json`
- 사용자 제공 부분과 생성 렌더의 일치 영역 및 표면 광선 적중 기록:
  `artSample/enemies/kursa/appearance_reference_sync/USER_EYE_TARGET_MAP.json`
- 로컬 15도 간격 회전 진단 렌더:
  `artSample/enemies/kursa/appearance_reference_sync/diagnostics/eye_surface_map`
- 프레임 1 정면·좌우 30도·좌우 60도 진단 렌더:
  `artSample/enemies/kursa/appearance_reference_sync/diagnostics/eye_pose_map`
- 최종 눈 표면 5시점 검토판:
  `artSample/enemies/kursa/appearance_reference_sync/renders/09_eye_surface_multiview.png`
- 사용자 지정 눈 위치 이미지 대조 검토판:
  `artSample/enemies/kursa/appearance_reference_sync/renders/14_eye_cavity_geometry_verification.png`
- 최종 Blend·FBX·GLB, 전체 페이지, 요약 페이지, README, 재질 배정표, 승인 상태를 갱신했다.

### 보존 및 검사 결과

- 사용자 제공 `111.png`에서 한쪽 눈이 찌그러져 보이는 원인을 재분석했다.
- 위치 문제는 아니었다. 오른쪽 오목면 폴리곤 법선을 UV 투영 평면에 그대로 사용하면서 대상 시점 화면 축이 약 `35.2°`로 전단되고, 깊이 마스크 `0.58`이 위아래를 추가로 잘랐다.
- 양쪽 눈 중심과 대상 폴리곤은 유지하고, 공통 시각 평면 법선 `(0.552875, -0.117583, 0.824926)`을 UV 투영에만 적용했다.
- 오른쪽 깊이 마스크를 `0.58`에서 `2.05`로 맞춰 수직 잘림을 제거했다.
- 대상 3/4 시점에서 화면 축은 좌우 모두 약 `90°`이며, 최종 청색 형태 비율은 왼쪽 약 `1.39:1`, 오른쪽 약 `1.37:1`이다.
- 사용자 제보 이미지와 최종 결과 비교판: `renders/15_eye_shape_distortion_correction.png`
- `MATERIAL_ASSIGNMENT.json`에서 왼쪽·오른쪽 눈 크기가 각각 `(8.348116, 8.988050)`, `(10.076670, 8.897684)`로 기록되어 직전 값의 정확히 2배임을 확인했다.
- 좌우 눈의 중심, 대상 폴리곤, 표면 마스크 법선은 확대 전 값과 동일하다. 왜곡 교정 단계에서 오른쪽 깊이만 `0.58`에서 `2.05`로 변경했다.
- 정면·좌우 30도·좌우 60도 검토판에서 확대된 눈이 바이저 아래 가로 홈에 유지된다.
- 사용자 지정 3/4 시점에서 두 눈이 바이저 아래 어두운 가로 홈 안에 들어간다.
- 기존 관자·얼굴 옆면에는 청색 눈이 남지 않는다.
- 다른 각도에서는 홈의 자체 가림에 따라 가려지는 눈만 사라지며 다른 표면에 재출현하지 않는다.
- 좌우 60도에서 가려져야 하는 눈은 자연스럽게 가려지며 측면 스트랩·후드에 재출현하지 않는다.
- `GEOMETRY_PRESERVATION.json`: `PASS`
- Blend 보존 수치: 정점 `2,109`, 에지 `5,936`, 면 `3,913`, 루프 `11,739`, 본 `24`, UV 레이어 `1`
- FBX 재가져오기: 메시 `1`, 정점 `2,109`, 에지 `5,936`, 면 `3,913`, 루프 `11,739`,
  재질 슬롯 `9`, 본 `24`, 액션 `1`
- GLB 재가져오기: 메시 `2`, 정점 `2,250`, 면 `3,993`, 재질 슬롯 `9`, 본 `24`, 액션 `1`, 결과 `REVIEW`
- GLB 수치 차이는 머티리얼 프리미티브 및 4개 초과 본 영향 제한에 따른 기존 포맷 변환 결과이며,
  형상 보존의 기준은 Blend와 FBX이다.
- `HTML_VALIDATION.json`: `PASS`, 페이지 `2`, 이미지·내부 링크 누락 `0`
- Blender 종료 시 Auto-Rig Pro 정리 핸들 경고가 출력됐으나 저장·내보내기·재가져오기 이후의
  사용자 플러그인 종료 훅 경고이며 결과에는 영향이 없었다.
- 승인 상태: `APPROVED_FOR_UNITY_NOT_APPLIED`
- Unity 런타임 적용: `False`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`
- Unity 실행·조작·에셋 생성·머티리얼 연결
- 별도 입체 눈 오브젝트 생성
- 쿠르사 메시·UV·웨이트·리그·애니메이션 수정
- 몸체·방패 및 눈 이외 재질 변경
- 현재 배치된 쿠르사 12개 인스턴스 변경
- 파후르 및 다른 적대 개체 변경
- 게임플레이 로직 변경
- Git 작업

## 쿠르사 방패 정면 기본 자세 적용

- 상태: `UNITY_APPLIED_INSPECTION_PASSED_FINAL_CAPTURE_REVIEWED`
- 사용자 지정 범위: 쿠르사의 기본 자세에서 왼팔 각도·위치를 조정해 방패 넓은 면이
  모델 정면을 똑바로 향하도록 수정
- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 대상 개체: `Approved Kursa Enemy Placement` 아래 기존 쿠르사 `12개`
- 승인 리비전: `USER_APPROVED_FRONT_FACING_SHIELD_BASE_POSE`

### 기준 측정과 자세 계산

- 승인 샘플 좌표계에서 모델 정면은 로컬 `+Z`다.
- 기존 프레임 1 표시 자세의 방패 외향 법선은
  `(0.271548, 0.568026, 0.776922)`이며 정면과 `39.020382°`였다.
- 실제 FBX rest 기본 자세의 방패 외향 법선은
  `(-0.993891, -0.099769, 0.047191)`이며 정면과 `87.295145°`였다.
- 방패 AABB 중심을 유지한 강체 회전과 결정론적 2본 IK를 사용해
  `LeftArm`, `LeftForeArm`, `LeftHand` rest 자세를 조정했다.
- 왼손 머리 위치는 샘플 단위
  `(21.407160, 100.416313, -10.873715)`에서
  `(32.238815, 102.186073, 5.022749)`로 이동했다.
- 상완 길이 `24.288805`, 전완 길이 `15.364311`, 목표 거리 `37.694108`로
  도달 가능 범위 안이며 연결 오차는 `0.00000854`다.
- 방패 중심 보정 이동은
  `(0.442944, 0.726921, 0.316605)`이며, 최종 방패 중심 이동 오차는
  Unity 단위 `1.2E-07`이다.
- 최종 방패 외향 법선의 정면 각도는 Blender와 Unity에서 모두 `0.000000°`다.

### 파생 FBX 및 보존 결과

- 런타임 파생 FBX:
  `Assets/_Project/Art/Enemies/Kursa/ApprovedAppearance/Models/Kursa_Appearance_RuntimeProjection.fbx`
- 새 SHA-256:
  `72E2ED50A5D42356546DBE960D7EE371B895A2D66824BAE437CFE89DB69876EF`
- Blender 변환 기록:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_RuntimeProjection_Export.json`
- 방패 기본 자세 분석 기록:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ShieldBasePose_Analysis.json`
- Unity가 보존하는 상위 4개 스킨 영향 중 왼팔 3본이 포함된 정점만 대상으로 제한했다.
- Blender 정점 `2,109`개 중 변경된 왼팔 영향 정점은 `470`개이며,
  대상 밖 변경 정점은 `0`개다.
- 토폴로지·재질 배정·기존 `UV0`·스킨 가중치·본 수 `24`·액션 수 `1`을 보존했다.
- 내장 액션 프레임 `1–63`의 전체 본 로컬 애니메이션 행렬 최대 오차는 `0.0`이다.
- 승인된 눈 투영용 `UV1/UV2/UV3`도 유지했다.
- 승인 Blend, 승인 FBX, `Assets/_Project/Art/Enemies/Kursa/Models/Kursa.fbx` 원본은
  수정하지 않았다.

### Unity 적용 및 전용 검사

- 런타임 임포트 정점 `3,374`, 삼각형 `3,913`, 본 `24`, 재질 슬롯 `9`다.
- 임포트 정점 증가는 새 팔 자세의 노멀 경계 분할이며, UV0·가중치 기반 삼각형 계약은
  승인 FBX와 동일하다.
- Unity rest 외형 비교에서 왼팔 영향 밖 위치는 FBX 재기준화 허용오차 `0.25mm`
  이내이고, 유의미한 위치 변경은 왼팔 영향으로 제한됐다.
- 변경 bind pose는 `LeftArm`, `LeftForeArm`, `LeftHand` 정확히 `3개`다.
- 씬의 12개 모델에는 런타임 bind pose 역행렬로 복원한 세 본의 rest Transform을 적용했다.
- 슬롯 Transform과 나머지 본 Transform, 활성 상태, 애니메이션 비활성 상태,
  다른 씬 루트는 유지했다.
- `ApplyApprovedKursaAppearance`: `PASS`
- `InspectApprovedKursaAppearance`: `PASS`
- 독립 검사 중 씬 변경: `False`
- 눈 투영 유효 정점: 좌안 `51`, 우안 `64`
- 검사 기록:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_Inspection.txt`

### 최종 시각 확인

- 이전 비교 이미지는 다음 경로에 보존했다.
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison_BEFORE_FrontShieldBasePose.png`
- 전용 검사 통과 뒤 최종 비교 캡처를 정확히 `1회` 실행했다.
- 최종 비교 이미지:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison.png`
- Unity 쪽 방패는 모델 정면축에 평행한 세로 평면으로 서 있으며 3/4 시점에서도
  넓은 면이 확인된다.
- 방패 중심 높이와 손–방패 연결이 유지되고 몸통 관통은 보이지 않는다.
- 승인된 청색 눈과 기존 외형도 유지된다.
- 최종 시각 판정: `PASS`

### 실행하지 않은 항목

- 방패 지오메트리·크기·재질 변경
- 오른팔·몸통·다리·머리 자세 변경
- 승인 아트 샘플 원본 수정
- 애니메이션 목록 추가·삭제 또는 게임플레이 로직 변경
- 다른 씬 루트 및 다른 적대 개체 변경
- 추가 캡처
- 프로젝트 전역 테스트·빌드
- Git 작업

## 승인 쿠르사 외형 Unity 적용

- 상태: `UNITY_APPLIED_EYE_RUNTIME_PROJECTION_INSPECTION_PASSED_PROFILE_CAPTURE_REVIEWED`
- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 대상 루트: `Approved Kursa Enemy Placement`
- 대상 개체: 기존 쿠르사 `12개`
- 승인 리비전: `USER_CROP_VISOR_GROOVE_2X_EYE_SIZE_NO_SHEAR_PROJECTION`

### 적용 내용

- 승인 아트 샘플의 FBX와 텍스처를
  `Assets/_Project/Art/Enemies/Kursa/ApprovedAppearance/` 아래에 복사했다.
- 샘플 텍스처 `44개`와 Unity 복사본의 SHA-256이 모두 일치한다.
- 승인 FBX SHA-256은
  `6BEC48F1822B4815ACD18E40006BD8D0567B5E4E67611A692B12F038A3942F9E`다.
- 승인 샘플의 재질 순서 `9개`를 그대로 Unity 재질로 만들고 기존 12개
  쿠르사 Renderer에 같은 순서로 연결했다.
- 색상·알베도·러프니스·메탈릭·노멀 텍스처를 승인 샘플에서 그대로 사용했다.
- 흉부 문양, 후드·목도리 문양, 좌우 눈은 승인 샘플의 투영 범위와
  투명도·발광값을 그대로 사용했다.
- 왼쪽 눈 중심 `(3.343094, 151.815475, 24.579956)`, 크기
  `(8.348116, 8.988050)`, 폴리곤 `3801`을 유지했다.
- 오른쪽 눈 중심 `(5.916458, 152.454803, 19.357758)`, 크기
  `(10.076670, 8.897684)`, 폴리곤 `3627`을 유지했다.
- 양쪽 투영 법선 `(0.552875, -0.117583, 0.824926)`과 깊이 `2.05`를 유지했다.
- 승인 샘플의 스튜디오 조명 반응을 기존 승인 외형과 같은 Unity 방식으로
  재현하되 색상·텍스처·외형 투영값은 변경하지 않았다.

### 보존 및 전용 검사 결과

- `ApplyApprovedKursaAppearance`: `PASS`
- `InspectApprovedKursaAppearance`: `PASS`
- Blender 승인 원본 정점: `2,109`
- Unity 원본·승인 FBX 임포트 정점: `3,372 / 3,372`
- Unity 정점 수 차이는 렌더 경계에 따른 임포트 분할이며 원본과 승인본이 동일하다.
- 삼각형: `3,913 / 3,913`
- 본: `24 / 24`
- 좌표·토폴로지·UV·본 인덱스·웨이트·본 순서가 원본과 승인 FBX에서 일치한다.
- 노멀·탄젠트는 승인 샘플을 렌더한 승인 FBX 값을 사용한다.
- 기존 12개 슬롯의 이름·순서·Transform·활성 상태·애니메이션 비활성 상태를 유지했다.
- 다른 씬 루트는 변경하지 않았다.
- 전용 검사 중 씬 변경: `False`
- 검사 기록:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_Inspection.txt`

### 최종 비교 상태

- 전용 검사 통과 뒤 승인 Blender 3/4 렌더와 Unity 결과를 나란히 둔 비교 캡처를 한 번 실행했다.
- 첫 비교 이미지의 Unity 쪽이 PreviewRenderUtility 조명 전달 문제로 거의 검게 렌더되어
  시각 동기화 증빙으로는 `INVALID`다.
- 첫 무효 비교 이미지는 다음 경로에 보존했다.
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison_INVALID_DarkPreview.png`
- 이후 승인 샘플 조명 반응을 셰이더에 반영하고 적용 및 전용 검사를 다시 통과했다.
- 사용자 추가 승인 뒤 Blender 샘플 좌표와 Unity 오브젝트 좌표의 정확한 단위 차이인
  `100:1`을 적용했다. 승인된 눈 중심·크기·깊이·투영 법선 값은 변경하지 않았다.
- 재적용과 전용 검사는 `PASS`였지만, 추가 비교 캡처에서 Unity 쪽 눈이 여전히 보이지 않아
  시각 동기화 증빙으로는 `INVALID` 판정했다.
- 두 번째 무효 비교 이미지는 다음 경로에 보존했다.
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison_INVALID_EyesMissing.png`
- Unity FBX의 실제 얼굴 서브메시는 `7번`이다. 승인 투영 마스크를 정적 검토 포즈의
  스키닝된 얼굴 좌표에 수치 적용한 결과 좌안·우안 적중 정점 수는 `0 / 0`이었다.
- 변형 전 공유 메시 얼굴 좌표에서는 같은 값으로 우안 적중 정점이 `28개` 확인됐다.
  따라서 남은 원인은 승인 수치나 축이 아니라, 오브젝트 공간 투영이 Unity 스키닝 뒤 좌표를
  사용하면서 승인 샘플의 기준 표면에서 이탈하는 방식 자체다.
- 이 진단 단계에서는 완료로 판정하지 않았고, 승인 PNG 투영값을 기존 `UV0`와 분리된
  추가 UV 채널에 결정론적으로 기록하는 후속 수정 승인을 요청했다.

### 승인 눈 스키닝 안정화 적용

- 사용자 승인 뒤 승인 Blend의 프레임 `1` 평가 좌표로 좌안·우안 투영 좌표와 정규화 깊이를
  계산해 파생 FBX의 추가 UV 채널에 기록했다.
- 승인 Blend와 승인 FBX 원본은 수정하지 않았다.
- 런타임 파생 FBX:
  `Assets/_Project/Art/Enemies/Kursa/ApprovedAppearance/Models/Kursa_Appearance_RuntimeProjection.fbx`
- 파생 FBX SHA-256:
  `775A20D986268B2F6FE461595BB9415AF81993FCF6F8D7B93641AF8C4C4EF56A`
- Blender 변환 기록:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_RuntimeProjection_Export.json`
- Blender 내보내기 전후 메시 위치·폴리곤 재질 배정·기존 `UV0` 해시는
  `76ED53101986D9785599DDD72CB4E3AB9149CFB5F108EA54E306A67D8423CFDC`로 동일하다.
- Blender 승인 투영 포함 루프는 좌안 `185`, 우안 `311`개다.
- Unity 임포트 뒤 기존 `UV0`는 유지하고 `UV1`은 좌안, `UV2`는 우안,
  `UV3`은 양쪽 정규화 깊이로 사용한다.
- Unity 얼굴 서브메시의 유효 투영 정점은 좌안 `51`, 우안 `64`개다.
- 파생 FBX와 승인 FBX는 Unity 기준 정점 `3,372`, 삼각형 `3,913`, 본 `24`,
  메시 bounds가 동일하며 리그·웨이트·애니메이션도 유지된다.
- `ApplyApprovedKursaAppearance`: `PASS`
- `InspectApprovedKursaAppearance`: `PASS`
- 최종 비교 이미지:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison.png`
- 최종 비교의 Unity 모델은 거의 측면을 향하므로 가까운 쪽 청색 눈은 바이저 홈에서 보이고
  반대쪽 눈은 머리 형상에 가려진다. 두 눈의 런타임 투영 채널 존재와 얼굴 영역 적중은
  전용 검사 수치 `51 / 64`로 확인했다.
- 승인 범위의 캡처 1회를 사용했으므로 추가 캡처는 실행하지 않았다.

### 실행하지 않은 항목

- 승인 샘플의 색상·텍스처·재질·투영값 재해석 또는 신규 디자인
- 쿠르사 배치 수·위치·리그·웨이트·UV·애니메이션 변경
- `Approved Kursa Enemy Placement` 밖의 씬 오브젝트 변경
- 승인 아트 샘플 원본 파일 변경
- 두 번째 무효 캡처 이후 추가 캡처
- 프로젝트 전역 작업
- Git 작업

## 최신 상태 우선 적용 메모

- 이 문서의 `쿠르사 방패 정면 기본 자세 적용` 절이 현재 쿠르사 런타임 모델과
  `CargoRunMvp` 씬의 최신 상태다.
- 해당 절의 런타임 FBX SHA-256
  `72E2ED50A5D42356546DBE960D7EE371B895A2D66824BAE437CFE89DB69876EF`,
  Unity 임포트 정점 `3,374`, 방패 정면 각도 `0.000000°`가 앞선 눈 안정화 절의
  이전 런타임 FBX 해시·동일 정점 수 설명을 대체한다.
- 승인 눈 외형과 `UV1/UV2/UV3` 투영 채널은 최신 파생 FBX에도 그대로 포함된다.

## 쿠르사 방패 앞뒤 위치 오류 수정

- 상태: `UNITY_APPLIED_INSPECTION_PASSED_FINAL_CAPTURE_REVIEWED`
- 직전 작업은 정면 각도만 맞췄지만 순수 rest의 방패 중심 Z `-8.556700`을
  보존해 방패가 쿠르사 뒤로 이동한 잘못된 결과였다.
- 직전 비교 이미지는 다음 경로에 `INVALID`로 보존했다.
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison_INVALID_ShieldBehind.png`
- 승인 샘플 프레임 1의 앞쪽 방패 중심
  `(51.722313, 112.916504, 28.720819)`을 새 기본 자세 기준으로 다시 사용했다.
- 수정 후 Blender 방패 중심은
  `(51.722301, 112.916489, 28.720825)`이며 중심 이동 오차는 Unity 단위
  `1.99133041E-07`이다.
- Blender 방패 정면 각도는 `0.000000°`다.
- Unity 삼각분할 뒤 대표 전면 각도는 `1.391423°`, 법선의 Z 정렬도는
  `99.9705%`다.
- Unity에서 방패 중심은 비방패 몸체 중심보다 모델 정면 쪽으로
  `+0.302919745` 앞에 있다.
- 왼손 목표는 샘플 단위
  `(47.638023, 118.532173, 15.260474)`, 팔 연결 오차는 `0.00002054`다.
- 새 런타임 FBX SHA-256은
  `047D452F45374B92C129329D5B0EC3754FF4533C05FEB89E058C1B1186C6F349`다.
- Blender 변경 정점은 왼팔 상위 4개 스킨 영향 정점 `470개`, 대상 밖 변경은
  `0개`다.
- 토폴로지·재질 배정·UV0·스킨 가중치·본 `24개`·액션 `1개`와
  프레임 `1–63` 로컬 애니메이션 채널을 유지했다.
- Unity 임포트 결과는 정점 `3,376`, 삼각형 `3,913`, 본 `24`, 재질 슬롯 `9`다.
- 변경 bind pose는 `LeftArm`, `LeftForeArm`, `LeftHand` 정확히 `3개`다.
- 눈 투영 유효 정점은 좌안 `51`, 우안 `64`로 유지됐다.
- `ApplyApprovedKursaAppearance`: `PASS`
- `InspectApprovedKursaAppearance`: `PASS`
- 독립 검사 중 씬 변경: `False`
- 전용 검사 통과 후 최종 캡처를 `1회` 실행했다.
- 최종 비교 이미지:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison.png`
- 최종 이미지에서 방패 넓은 면은 쿠르사 몸 앞쪽에 있으며, 손은 방패 뒤쪽 그립에
  연결되고 몸통 관통은 보이지 않는다.
- 승인된 눈·외형도 유지된다.
- 최종 시각 판정: `PASS`
- 이 절의 해시·정점 수·앞쪽 오프셋이 앞선 방패 기본 자세 절과 최신 상태 메모를
  대체한다.

## 쿠르사 방패 세로·정면 기본 자세 수정

- 상태: `UNITY_APPLIED_INSPECTION_PASSED_FINAL_CAPTURE_REVIEWED`
- 직전 앞쪽 배치는 방패 면을 정면으로만 맞추고 면 안쪽 회전을 제한하지 않아
  방패의 긴 축이 사선으로 남아 있었다.
- 직전 사선 비교 이미지는 다음 경로에 `INVALID`로 보존했다.
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison_INVALID_ShieldDiagonal.png`
- 승인 샘플 프레임 1의 방패와 왼손 위치를 기준으로, 방패 넓은 면은 모델 정면
  `+Z`, 긴 축은 모델 위쪽 `+Y`에 맞췄다.
- Blender 기준 원래 방패 긴 축의 세로 오차는 `52.097545°`, 면 정면 오차는
  `39.020352°`였으며, 면 정렬 뒤 필요한 롤 보정은 `-47.772409°`였다.
- 수정 후 Blender 기준 방패 면 정면 오차와 긴 축 세로 오차는 모두
  `0.000000°`다.
- 세로·정면 회전 뒤 손 목표가 기존 팔 최대 길이보다 `1.990661` 샘플 단위
  멀어져, 방패와 손 전체를 팔뿌리 방향으로 최소 거리 `1.990761`만 이동했다.
- 수정 후 방패 중심은
  `(49.845474, 113.333305, 28.204185)`로 모델 앞쪽에 유지됐고, 팔 연결 오차는
  `0.000003815`다.
- 새 런타임 FBX SHA-256은
  `E364F32C34306552C468A245093BFC0FEB4BC1FCB8730D2C2CD6FCE3EF7AD9B5`다.
- Blender 변경 정점은 왼팔 영향 정점 `470개`, 대상 밖 변경은 `0개`이며,
  변경 rest bone은 `LeftArm`, `LeftForeArm`, `LeftHand` 정확히 `3개`다.
- 토폴로지·재질 배정·UV0·스킨 가중치·본 `24개`·액션 `1개`와
  프레임 `1–63` 로컬 애니메이션 채널을 유지했다.
- Unity 임포트 결과는 정점 `3,376`, 삼각형 `3,913`, 본 `24`, 재질 슬롯 `9`다.
- Unity 기준 방패 대표 전면 각도는 `1.391283°`, 전체 방패 긴 축의 세로 각도는
  `1.000342°`다.
- Unity에서 방패 중심은 비방패 몸체 중심보다 모델 정면 쪽으로
  `+0.300336421` 앞에 있다.
- 눈 투영 유효 정점은 좌안 `51`, 우안 `64`로 유지됐다.
- `ApplyApprovedKursaAppearance`: `PASS`
- `InspectApprovedKursaAppearance`: `PASS`
- 독립 검사 중 씬 변경: `False`
- 전용 검사 통과 후 최종 캡처를 정확히 `1회` 실행했다.
- 최종 비교 이미지:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison.png`
- 최종 이미지에서 방패는 쿠르사 앞에 있고 긴 변이 세로로 반듯하며 넓은 면이
  정면을 향한다. 왼손은 방패 뒤 그립 위치에 연결되고 몸통 관통은 보이지 않는다.
- 승인된 파란 눈과 기존 외형도 유지된다.
- 최종 시각 판정: `PASS`
- 이 절의 해시·각도·앞쪽 오프셋과 최종 비교 이미지가 앞선 방패 자세 절의
  최신 상태를 대체한다.

## 쿠르사 방패 몸통 중심선 근접 기본 자세 수정

- 상태: `UNITY_APPLIED_INSPECTION_PASSED_FINAL_CAPTURE_REVIEWED`
- 사용자 확정값에 따라 방패 중심과 몸통 중심선 사이의 기존 좌우 간격을 정확히
  `50%` 줄였다. 방패 중심을 몸통 중심선과 완전히 일치시키지는 않았다.
- 몸통 중심선은 방패 높이를 지나는 `Spine01`의 프레임 1 중심선으로 계산했다.
- Blender 기준 몸통 중심선 X는 `-1.718690`, 직전 방패 중심 X는
  `49.845486`, 직전 좌우 간격은 `51.564176`이다.
- 목표 좌우 간격은 `25.782088`, 목표 방패 중심 X는 `24.063398`이며,
  X축으로만 `-25.782087` 이동했다.
- 최종 Blender 방패 중심은
  `(24.063396, 113.333282, 28.204151)`이고 실제 좌우 간격은
  `25.782087`, 직전 대비 비율은 정확히 `0.5`다.
- 방패 높이 Y와 전방 깊이 Z는 직전 세로·정면 자세 값에서 변경하지 않았다.
- 왼팔 목표 거리는 `17.590477`로 기존 팔 도달 범위 안이며 연결 오차는
  `0.000011050`이다.
- Blender 기준 방패 면 정면 오차와 긴 축 세로 오차는 모두 `0.000000°`다.
- 새 런타임 FBX SHA-256은
  `5FD558BFBF0FC0E309C6B09B231E2AC83B516EF7837D96969960BBC6F547F057`다.
- Blender 변경 정점은 왼팔 영향 정점 `470개`, 대상 밖 변경은 `0개`이며,
  변경 rest bone은 `LeftArm`, `LeftForeArm`, `LeftHand` 정확히 `3개`다.
- 토폴로지·재질 배정·UV0·스킨 가중치·본 `24개`·액션 `1개`와
  프레임 `1–63` 로컬 애니메이션 채널을 유지했다.
- Unity 임포트 결과는 정점 `3,376`, 삼각형 `3,913`, 본 `24`, 재질 슬롯 `9`다.
- Unity 기준 몸통 중심선과 방패 중심의 좌우 간격은 `0.257820874`,
  직전 대비 비율은 정확히 `0.5`다.
- Unity 기준 방패 대표 전면 각도는 `1.391423°`, 긴 축 세로 각도는
  `1.000342°`, 비방패 몸체 대비 전방 오프셋은 `+0.3003363`이다.
- 눈 투영 유효 정점은 좌안 `51`, 우안 `64`로 유지됐다.
- `ApplyApprovedKursaAppearance`: `PASS`
- `InspectApprovedKursaAppearance`: `PASS`
- 독립 검사 중 씬 변경: `False`
- 직전 세로·정면 결과 이미지는 다음 경로에 이동 전 비교본으로 보존했다.
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison_BEFORE_ShieldTorsoHalfGap.png`
- 전용 검사 통과 후 최종 캡처를 정확히 `1회` 실행했다.
- 최종 비교 이미지:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison.png`
- 최종 이미지에서 방패는 몸통 중심선 쪽으로 가까워졌지만 중심선과 완전히
  일치하지 않으며, 몸통 앞에서 세로·정면 자세를 유지한다. 왼손은 방패 뒤 그립에
  연결되고 의도하지 않은 몸통 관통은 보이지 않는다.
- 승인된 파란 눈과 기존 외형도 유지된다.
- 최종 시각 판정: `PASS`
- 이 절의 해시·좌우 간격·앞쪽 오프셋과 최종 비교 이미지가 앞선 방패 자세 절의
  최신 상태를 대체한다.

## 쿠르사 오른팔 차렷 기본 자세 수정

- 상태: `UNITY_APPLIED_INSPECTION_PASSED_FINAL_CAPTURE_REVIEWED`
- 사용자 승인에 따라 기존 왼팔과 방패 자세는 유지하고, 벌어져 있던 오른팔을
  몸통 옆으로 내려 차렷자세에 가까운 기본 자세로 수정했다.
- 대상 rest bone은 기존 왼팔의 `LeftArm`, `LeftForeArm`, `LeftHand`와 새 오른팔의
  `RightArm`, `RightForeArm`, `RightHand`를 합친 정확히 `6개`다.
- 원래 오른팔 뼈 위치는 어깨 `(-24.287945, 125.050240, 5.819906)`, 팔꿈치
  `(-45.390827, 118.824570, 14.364237)`, 손목
  `(-67.557579, 104.002251, 29.007885)`였다.
- 오른팔은 최대 도달 길이의 `95%`를 사용해 아래로 내리고, 팔꿈치는 몸통 뒤쪽으로
  `8.359862`만 굽혀 옆으로 벌어지지 않게 했다.
- 목표 팔꿈치는 `(-24.287945, 102.977432, -2.539957)`, 목표 손목은
  `(-24.287945, 73.726532, 5.819906)`다.
- 상완의 아래쪽 정렬 오차는 `74.706280°`에서 `20.743699°`, 전완은
  `60.841809°`에서 `15.949858°`로 줄었다.
- 오른팔 영향 메시 중심과 몸통 중심선의 간격은 Blender 기준
  `37.760796`에서 `15.709287`로 줄었다.
- 오른팔 메시 최저 높이는 `85.865463`에서 `59.749344`로 내려가 손이 허벅지 옆에
  위치하며, 2본 연결 오차는 `0.000017101`이다.
- 새 런타임 FBX SHA-256은
  `952BD2961450BF41C88199F1E760248AA82F8CB6A048B3DE36EE578410A05FA4`다.
- Blender 변경 정점은 기존 왼팔 `470개`, 오른팔 `462개`, 합계 `932개`이며
  양팔 영향 밖 변경은 `0개`다.
- 토폴로지·재질 배정·UV0·스킨 가중치·본 `24개`·액션 `1개`와
  프레임 `1–63` 로컬 애니메이션 채널을 유지했다.
- Unity 임포트 결과는 노멀 경계 분할 정점 `3,377`, 삼각형 `3,913`, 본 `24`,
  재질 슬롯 `9`다.
- Unity 기준 오른팔 중심의 몸통 중심선 간격은 `0.168648049`, 손 쪽 최저 높이는
  `0.5974935`다.
- 기존 방패의 몸통 중심선 간격 비율 `0.5`, 실제 간격 `0.257820874`, 전방 오프셋
  `+0.3003363`, 전면 각도 `1.391423°`, 세로 각도 `1.000342°`를 유지했다.
- 눈 투영 유효 정점은 좌안 `51`, 우안 `64`로 유지됐다.
- `ApplyApprovedKursaAppearance`: `PASS`
- `InspectApprovedKursaAppearance`: `PASS`
- 독립 검사 중 씬 변경: `False`
- 직전 오른팔 벌림 상태 이미지는 다음 경로에 이동 전 비교본으로 보존했다.
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison_BEFORE_RightArmAttention.png`
- 전용 검사 통과 후 최종 캡처를 정확히 `1회` 실행했다.
- 최종 비교 이미지:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison.png`
- 최종 이미지에서 오른쪽 상완은 몸통 옆으로 내려오고 팔꿈치는 약간 뒤로 굽으며,
  손은 허벅지 위쪽 옆에 있다. 오른팔과 몸통·허벅지의 비정상 관통은 보이지 않는다.
- 기존 왼팔·방패 위치와 승인된 파란 눈·외형도 유지된다.
- 최종 시각 판정: `PASS`
- 이 절의 해시·양팔 rest bone·오른팔 수치와 최종 비교 이미지가 현재 쿠르사 기본
  자세의 최신 상태다.

## 쿠르사 오른팔 허벅지 관통 제거

- 상태: `UNITY_APPLIED_INSPECTION_PASSED_FINAL_CAPTURE_REVIEWED`
- 사용자가 직전 결과에서 오른팔이 오른쪽 허벅지를 통과한다고 확인했으므로,
  `쿠르사 오른팔 차렷 기본 자세 수정` 절의 `허벅지 비정상 관통은 보이지 않는다`는
  시각 판정은 무효화했다.
- 직전 관통 상태 이미지는 다음 경로에 `INVALID`로 보존했다.
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison_INVALID_RightArmThighIntersection.png`

### 원인 및 수정

- 직전 전용 검사는 오른팔 영향 메시의 중심선 간격과 최저 높이만 확인했으며,
  오른팔 표면과 오른쪽 허벅지 표면의 실제 삼각형 교차를 검사하지 않아 관통을
  놓쳤다.
- 직전 런타임 FBX를 상위 스킨 가중치가 과반인 표면 삼각형으로 직접 분리해 측정한
  결과 오른팔–오른쪽 허벅지 교차는 `85쌍`, 표면 최근접 거리는
  `0.0528915` 샘플 단위였다.
- 기존 오른팔 최대 길이의 `95%` 연장 비율과 팔꿈치 뒤쪽 굽힘 `8.359871`은
  유지하고, 손목 목표를 캐릭터 오른쪽으로 `16.0` 샘플 단위 이동했다.
- 팔 길이를 유지하면서 바깥쪽으로 이동할 수 있도록 손목 높이는
  `73.726532`에서 `76.284241`로 `2.557709` 샘플 단위만 올라갔다.
- 최종 팔꿈치 목표는 `(-31.169071, 104.077423, -2.539965)`, 손목 목표는
  `(-40.287945, 76.284241, 5.819906)`다.
- 상완 아래쪽 각도는 `27.305913°`, 전완 아래쪽 각도는 `23.994260°`이며
  몸통 옆을 향하는 차렷 형태를 유지한다.
- 생성기와 Unity 전용 검사에 오른팔·오른쪽 허벅지 영향 표면 삼각형의 실제 교차와
  최소 간격 검사를 추가했다.

### 파생 FBX 및 보존 결과

- 새 런타임 FBX SHA-256은
  `D9F30E87DE6C8D2438D8A8C56D7CD1E394E8F3E6CD15248A1F69CFB8F62472E9`다.
- Blender 표면 검사에서 오른팔 삼각형 `538개`, 오른쪽 허벅지 삼각형 `145개`,
  교차 `0쌍`, 최소 간격 `1.251807` 샘플 단위다.
- Blender 변경 정점은 기존 왼팔 `470개`, 오른팔 `462개`, 합계 `932개`이며
  양팔 영향 밖 변경은 `0개`다.
- 변경 rest bone은 `LeftArm`, `LeftForeArm`, `LeftHand`, `RightArm`,
  `RightForeArm`, `RightHand` 정확히 `6개`다.
- 토폴로지·재질 배정·UV0·스킨 가중치·본 `24개`·액션 `1개`와
  프레임 `1–63` 로컬 애니메이션 채널을 유지했다.
- 기존 방패의 몸통 중심선 간격 비율 `0.5`, 실제 간격 `0.257820874`, 전방
  오프셋 `+0.3003363`, 전면 각도 `1.391423°`, 세로 각도 `1.000342°`를 유지했다.

### Unity 적용 및 최종 확인

- Unity 임포트 결과는 정점 `3,377`, 삼각형 `3,913`, 본 `24`, 재질 슬롯
  `9개`다.
- Unity 실제 삼각형 검사에서 오른팔–오른쪽 허벅지 교차는 `0쌍`, 최소 표면
  간격은 `0.0125181321`이며 목표 `0.01`을 통과했다.
- Unity 기준 오른팔 바깥 이동은 `0.16`, 몸통 중심선 간격은 `0.251269966`,
  오른팔 메시 최저 높이는 `0.600720167`이다.
- 눈 투영 유효 정점은 좌안 `51`, 우안 `64`로 유지됐다.
- `ApplyApprovedKursaAppearance`: `PASS`
- `InspectApprovedKursaAppearance`: `PASS`
- 독립 검사 중 씬 변경: `False`
- 기존 12개 슬롯, 승인 외형·머티리얼·UV·웨이트·리그·내장 애니메이션,
  왼팔·방패 자세와 다른 씬 루트가 유지됐다.
- 전용 검사 통과 후 최종 캡처를 정확히 `1회` 실행했다.
- 최종 비교 이미지:
  `docs/validation/kursa_approved_appearance_2026-08-02/Kursa_ApprovedAppearance_UnityComparison.png`
- 최종 이미지에서 오른팔은 몸통 옆 차렷 방향을 유지하면서 오른쪽 허벅지 바깥에
  위치하고, 팔과 허벅지 사이에 틈이 보이며 관통은 없다.
- 최종 시각 판정: `PASS`
- 이 절의 해시·오른팔 위치·표면 간격과 최종 비교 이미지가 현재 쿠르사 기본 자세의
  최신 상태다.

### 실행하지 않은 항목

- 하네스 검증 및 관련 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 왼팔·방패 지오메트리·위치·각도·재질 변경
- 오른팔의 불필요한 방향·높이 변경
- 몸통·다리·머리 자세 또는 승인 아트 샘플 원본 변경
- 애니메이션 추가·삭제, 게임플레이 로직, 다른 씬 루트 및 다른 적대 개체 변경
- 추가 캡처
- Git 작업

## 쿠르사 이동 모션 외형 전용 동기화

- 상태: `UNITY_APPLIED_INSPECTION_PASSED_FINAL_CAPTURE_REVIEWED`
- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 대상 개체: `Approved Kursa Enemy Placement/Kursa_03_Move/Kursa_Model`
- 현재 이동 개체를 삭제하고 지정 원본
  `D:/Bellerophon2/Bellerophon/enemies model/KUŠkursa walking.fbx`의 정확한 Unity
  복사본으로 교체했다.
- Unity 자산 경로:
  `Assets/_Project/Art/Enemies/Kursa/Animations/Models/KUŠkursa walking.fbx`
- 원본과 Unity 복사본 SHA-256은 모두
  `A1E365B11C6A0C316E7208891E223746C125B49ECB61E9A15629D852C959603C`로 일치한다.
- 이동 개체의 외형 전용 메시 데이터 자산은
  `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_03_Move_AppearanceOnly.asset`이다.
- 이 자산의 정점 위치·노멀·탄젠트·UV0·본 가중치·bind pose와 삼각형 형상은 지정
  walking FBX 데이터만 사용한다. 승인 외형 자료에서는 9개 표면 구획과 승인 눈
  UV1·UV2·UV3만 삼각형 코너별로 정확히 대응해 가져온다.
- Renderer의 9개 `sharedMaterials`는 현재 배치된 승인 정적 쿠르사의 순서와 참조를
  그대로 사용한다.
- 정적 모델의 정점 위치, 본 가중치, bind pose, 본 Transform은 이동 개체에 사용하지
  않는다. 따라서 정적 모델에서 조정했던 양팔·방패 자세가 이동 모션으로 딸려오지 않는다.
- 사용자 지시에 따라 보행 전체에서 오른팔과 오른쪽 허벅지가 겹치지 않는 최소 보정을
  측정했다. 원본 상태는 120Hz 기준 233개 시점 중 96개 시점에서 교차했고 최대 교차
  삼각형 쌍은 261개였다.
- 좌우 어깨선으로 계산한 캐릭터 바깥 방향으로 `RightArm` 상박 회전을 탐색한 결과,
  전 구간 표면 교차가 처음 해소되는 각도 `18.66도`를 확정해 모든 보행 키에 적용했다.
  `RightForeArm`·`RightHand`의 원본 로컬 움직임은 유지되며 왼팔·방패 보정은 없다.
- 내장 `Armature|mixamo.com|Layer0`을 `Kursa_03_Move_Mixamo`로 선택해 반복 재생한다.
- 원본 본 위치·회전은 그대로 복사하고 `Hips` 수평 위치 두 성분만 첫 프레임 값으로
  고정해 제자리 재생한다. 예외는 위 `RightArm` 바깥 방향 `18.66도` 보정 하나뿐이다.
- 다른 11개 쿠르사의 발바닥 지면은 모두 정확히 `Y=0`으로 일치했다. 기존 이동 모션의
  보행 주기 최저점 `-0.04956641`을 `Y=0`에 맞추도록 이동 모델 높이를 조정했다.
- 파생 클립:
  `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_03_Move_InPlace.anim`
- 반복 Controller:
  `Assets/_Project/Art/Enemies/Kursa/Animations/Kursa_03_Move.controller`

### 전용 점검 결과

- `ApplyKursaMoveAnimation`: `PASS`
- `InspectKursaMoveAnimation`: `PASS`
- 클립 길이 `1.93333328초`, 프레임 레이트 `60`, 움직이는 본 `15개`다.
- 원본 Mixamo 대비 본 위치·회전·스케일 최대 오차는 모두 `0`이다.
- 수평 루트 이동 범위 `0`, 반복 경계 위치·회전 오차 `0`이다.
- 보행 중 지면 범위는 `0.03995252`다.
- 보행 주기 최저 발바닥 Y는 `0.0000000198740135`, 다른 쿠르사의 공통 지면 Y는
  `0`이며 허용 오차 안에서 일치한다.
- 120Hz 전 구간 오른팔·오른쪽 허벅지 교차 시점 `0`, 최대 교차 삼각형 쌍 `0`이다.
- 오른팔 보정 각도 `18.66도`, 보정 각도 최대 오차는
  `0.00009918213도`다.
- walking 정점·노멀·탄젠트·UV0·본 가중치·bind pose·삼각형 형상 보존: `True`
- 승인 외형 표면 구획과 정적 `sharedMaterials` 일치: `True`
- 승인 눈 UV 채널 일치: `True`
- 정적 팔 지오메트리·bind pose 사용: `False`
- 본별 별도 보정: `RightArm` 바깥 방향 `18.66도` 하나
- 다른 11개 쿠르사 슬롯과 다른 씬 루트는 변경되지 않았다.
- 독립 점검 중 씬 변경: `False`
- 검사 기록:
  `docs/validation/kursa_move_animation_2026-08-02/Kursa_03_Move_Inspection.txt`
- 전용 점검 PASS 후 외형 동기화 상태 캡처를 정확히 `1회` 생성했다.
- 최종 검토 이미지:
  `docs/validation/kursa_move_animation_2026-08-02/Kursa_03_Move_Review.png`
- 최종 이미지에서 승인된 표면 외형과 파란 눈이 다섯 시점 모두 표시되고, 오른팔이
  오른쪽 허벅지 바깥으로 떨어져 관통하지 않으며 왼팔·방패와 시점별 보행 변화가
  유지됨을 확인했다. 첫 시점과 마지막 시점은 동일하다.
- 최종 시각 판정: `PASS`

### 실행하지 않은 항목

- 하네스 검증 및 관련 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 왼팔·방패 자세 또는 움직임 수정
- 오른팔의 허벅지 교차 해소에 필요한 `18.66도` 외 추가 위치·각도·움직임 수정
- 정적 모델의 팔·방패 지오메트리, bind pose, 본 Transform 적용
- 충돌 회피·감쇠·추가 동작 생성
- 승인 정적 모델과 지정 원본 FBX 수정
- 다른 슬롯·게임플레이·물리 로직 수정
- 추가 캡처
- Git 작업

## 쿠르사 대기 모션 구현

- 상태: `UNITY_APPLIED_INSPECTION_PASSED_FINAL_CAPTURE_REVIEWED`
- 적용 대상은 `Approved Kursa Enemy Placement/Kursa_02_Idle` 하나다.
- 사용자 확정 곡선은 `0초: 현재 높이`, `0.5초: -1.5cm`, `1초: -3cm`,
  `1.5초: -1.5cm`, `2초: 현재 높이`이며 2초 루프로 설정했다.
- 발과 발목은 고정하고 `Hips`, `LeftUpLeg`, `LeftLeg`, `LeftFoot`,
  `RightUpLeg`, `RightLeg`, `RightFoot`만 실제로 움직이도록 구성했다.
- Blender 생성기는 현재 승인된 런타임 FBX
  `D9F30E87DE6C8D2438D8A8C56D7CD1E394E8F3E6CD15248A1F69CFB8F62472E9`만
  입력으로 사용한다.
- 현재 씬의 쿠르사 배치 스케일 `0.5495786`을 반영해 원본 이동량을
  `0.0545872783`으로 생성했고, Unity 투영 이동량은 정확히 `0.03`이다.
- 대기 FBX SHA-256은
  `F87EB8AB62687571DC686350DD2BB7CF9734DE6EA54E5DB9BA86A233DD469AAE`다.
- FBX 좌표계 변환용 루트 스케일 및 비대상 본 상수 채널은 전용 `.anim`에
  복사하지 않았고, 현재 모델 계층에서 계산한 실제 본 경로에 7개 대상 본의
  위치·회전 곡선만 연결했다.
- Blender 검사 결과 Unity 투영 이동량 `0.0299999817`, 최대 발 위치 오차
  `0.0000002084`, Unity 투영 지면 변화 `0.0005167143`, 루프 오차 `0`이다.
- `ApplyKursaIdleAnimation`: `PASS`
- `InspectKursaIdleAnimation`: `PASS`
- Unity 검사 결과 실제 이동량 `0.0299999714`, 최대 곡선 오차
  `0.0000000279`, 최대 발 오차 `0.0000000596`, 지면 변화 `0.0000000894`,
  루프 오차 `0`이다.
- Unity 실제 표면 검사에서 오른팔·오른쪽 허벅지 교차는 `0쌍`, 최소 간격은
  `0.00293031242`다.
- 루트 모션과 본 스케일링은 없으며, 비대상 본의 승인된 로컬 자세와 눈·방패·외형·
  머티리얼·UV·웨이트·리그, 다른 11개 쿠르사 슬롯 및 다른 씬 루트가 유지됐다.
- 기존 승인 외형 검사기는 `Kursa_02_Idle`의 위 전용 Controller 한 개만 승인된
  애니메이션 상태로 인정하도록 갱신했다.
- 독립 검사 PASS 뒤 최종 5시점 캡처를 정확히 `1회` 실행했다.
- 최종 비교 이미지:
  `docs/validation/kursa_idle_animation_2026-08-02/Kursa_02_GroundedIdle_Review.png`
- 최종 시각 판정: `PASS`

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 승인 외형·모델·텍스처·머티리얼·눈·방패 변경
- 다른 애니메이션 및 다른 쿠르사 슬롯 변경
- 본 스케일링, 발 이동, 루트 모션
- 게임플레이·AI·물리 로직, 다른 씬 루트 및 다른 적대 개체 변경
- 추가 캡처
- Git 작업
