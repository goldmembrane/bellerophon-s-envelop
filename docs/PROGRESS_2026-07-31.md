# 2026-07-31 진행 기록

## 파후르 접지 대기 애니메이션

- 상태: `UNITY_APPLIED_AND_VALIDATED`
- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 대상 개체:
  `Approved Pahur Enemy Placement/Pahur_02_Idle/Pahur_Model`
- 클립:
  `Assets/_Project/Art/Enemies/Pahur/Animations/Pahur_02_GroundedIdle.anim`
- 컨트롤러:
  `Assets/_Project/Art/Enemies/Pahur/Controllers/Pahur_02_GroundedIdle.controller`

### 구현 내용

- 대기 모션을 `2초`, `60fps`, 루프 클립으로 구성했다.
- 몸통 모핑이나 BlendShape를 사용하지 않았다.
- `Hips`를 기준으로 상체 전체가 총 `3cm` 상하 이동하도록 했다.
  애니메이션 중앙 자세는 원래 자세보다 `1.5cm` 낮다.
- 좌우 `UpLeg`, `Leg`, `Foot` 회전으로 무릎을 굽히고, 실제
  베이크 메시에서 각 발뼈 주변 `30cm` 안의 최저 `5mm` 밑창
  정점을 추적해 발 위치 커브를 보정했다.
- `Animator`는 `Pahur_02_Idle/Pahur_Model`에만 활성화하고
  `PahurGroundedIdle` 단일 상태를 연결했다.
- 루트 모션은 비활성화했으며 애니메이션 커브 `33`개는
  `Hips/LeftFoot/RightFoot` 위치와 양쪽 다리 회전에만 한정했다.

### 적용 및 검증 결과

- `ApplyPahurGroundedIdleAnimation`: `PASS`
- `ValidatePahurGroundedIdleAnimation`: `PASS`
- `CapturePahurGroundedIdleAnimationReview`: `PASS`
- 클립 길이 `2초`, 골반·상체 총 이동 `0.03m`, 루프 경계 오차
  `0`, 모델 루트 이동 오차 `0`이다.
- 발뼈 최대 위치 오차는 `0.000025m`, 실제 밑창 최대 위치 오차는
  `0.000006m`, 모델 바닥 최저점 변화는 `0.000006m`이다.
- 이동 거리 축소로 다리 회전 합산 최대값은 왼쪽 `30.95432°`,
  오른쪽 `58.46921°`로 감소했다.
- BlendShape 바인딩은 `0`, 승인 외형과 다른 파후르 슬롯 `9개`,
  비대상 씬 루트는 변경되지 않았다.
- `CargoRunMvp`를 대상 상태로 저장했다.

### 검증 결과물

- `docs/validation/pahur_idle_animation_2026-07-31/Pahur_02_GroundedIdle_Validation.txt`
- `docs/validation/pahur_idle_animation_2026-07-31/Pahur_02_GroundedIdle_Review.png`
- 검토 이미지는 고정 카메라에서 `0`, `0.5`, `1`, `1.5`, `2초`
  자세를 한 장에 배치했으며 비대상 씬 렌더러는 제외했다.

### 실행하지 않은 항목

- 하네스 검증
- EditMode·PlayMode 테스트
- Windows 개발 빌드
- Unity 편집기 재시작
- 다른 파후르 슬롯, 외형, 텍스처, 머티리얼, 게임플레이 수정
- Git 작업

## 파후르 눈 세로 크기 조정 아트 샘플

- 상태: `SUPERSEDED_BY_REVISION_22`
- 리비전: `2026-07-31_reference_eye_vertical_scale_rework_21`
- 작업 위치:
  `artSample/enemies/pahur/appearance_reference_sync/`
- Unity 적용: 리비전 `22`에 포함되어 완료

### 목표

기준 이미지의 파후르 눈이 기존 아트 샘플보다 세로로 더 크다는
사용자 피드백을 반영하되, 직전 작업에서 확정한 화난 눈 각도와
표면 배치를 유지한다.

### 반영 내용

- 좌우 눈의 접선 평면 투영 너비 `5.0`을 유지했다.
- 좌우 눈의 접선 평면 투영 높이만 `3.0`에서 `4.5`로 확대했다.
- 화면 왼쪽 눈의 회전 `-16°`, 오른쪽 눈의 회전 `-14°`를 유지했다.
- 좌우 눈의 표면 기준점과 접선축을 변경하지 않았다.
- 눈 원본 오버레이·발광 텍스처를 재생성하거나 수정하지 않았다.
- Blender 검토본과 FBX·GLB, 정면·3/4·머리 확대·비교 렌더를
  새 수치로 재생성했다.

### 확인 결과

- 정면과 3/4 렌더에서 눈의 세로 크기가 증가했다.
- 기존의 중앙을 향해 내려가는 화난 눈 각도가 유지됐다.
- 두 눈은 중앙 금속·관자놀이·두건·얼굴 하단을 침범하지 않는다.
- 텍스처 `96`개를 수정 전 매니페스트와 SHA-256으로 비교했으며
  불일치는 `0`개다.
- `MATERIAL_ASSIGNMENT.json`과 `GEOMETRY_PRESERVATION.json`의
  SHA-256은 수정 전과 동일하다.
- 구조 검사 결과는 `PASS`다. 승인된 독립 연결 표면 `97` 삭제를
  제외한 Blend와 FBX는 정점 `2,434`, 에지 `6,548`, 면 `4,330`,
  루프 `12,990`, 본 `24`와 나머지 좌표·본 가중치·UV를 유지한다.
- HTML 정적 DOM·자산 검사는 `PASS`이며 깨진 이미지와 링크는 없다.
  자동 브라우저 렌더는 브라우저 연결 메타데이터가 없어 실행되지
  않았고, 생성 렌더를 직접 확인했다.

### 검토 자료

- `artSample/enemies/pahur/appearance_reference_sync/renders/07_head_front_detail.png`
- `artSample/enemies/pahur/appearance_reference_sync/renders/07_head_three_quarter_detail.png`
- `artSample/enemies/pahur/appearance_reference_sync/renders/08_head_detail_comparison.png`
- `artSample/enemies/pahur/appearance_reference_sync/index.html`
- `artSample/enemies/pahur/appearance_reference_sync/summary.html`

### 보존 범위

- 프로젝트 원본 `Assets/_Project/Art/Enemies/Pahur/Models/Pahur.fbx`는
  수정하지 않았다.
- 눈 이외의 얼굴·두건·화염 문양·몸통·팔·다리·가스통·무기 외형과
  재질 자산은 변경하지 않았다.
- Unity 반영·새로고침·재시작과 Git 작업은 수행하지 않았다.

## 승인 파후르 외형 Unity 적용

- 상태: `UNITY_RUNTIME_APPLIED_AND_VALIDATED`
- 적용 리비전: `2026-07-31_red_brown_palette_rework_22`
- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 대상 루트: `Approved Pahur Enemy Placement`
- 적용 인스턴스: `10`

### 적용 내용

- 승인 샘플 FBX를
  `Assets/_Project/Art/Enemies/Pahur/Models/Pahur.fbx`에 그대로
  복사했다.
- 승인 샘플과 Unity FBX의 SHA-256은
  `5A2354A0B89A451DB98EF5AA5409C61EE12CF5638FA6EC2C88110B4C146B537C`
  로 일치하며 기존 `.meta` GUID
  `f4abedb45727f0d4cb04ba0dbb219298`을 유지했다.
- 승인 샘플 셰이더가 참조하는 텍스처 `77`개와 승인 재질 정의를
  Unity 전용 외형 폴더에 배치했다.
- `Bellerophon/Pahur/ApprovedAppearance` URP 셰이더로 적갈색
  외장·두건, 중립 건메탈·탱크·무기·호스, 주황 불꽃 표식과 암적색
  눈 발광을 재현했다.
- 눈은 승인 폭 `5.0`, 높이 `4.5`, 좌우 회전 `-16°/-14°`를
  유지했다.
- Unity 임포트 메시와 승인 샘플 좌표를 대조해 변환이
  `X×-100, Y×100, Z×100`임을 확인했다. 승인 프레임 1 눈 좌표는
  동일 정점쌍의 강체 역변환으로 바인드 포즈 좌표로 옮겼다.
- 바인드 포즈 투영 좌표를 임포트 시 전용 UV 채널에 기록해
  애니메이션 중에도 눈과 두건 표식이 표면에 붙도록 했다.

### 적용 및 검증 결과

- `ApplyApprovedPahurAppearance`: `PASS`
- `ValidateApprovedPahurAppearance`: `PASS`
- `CaptureApprovedPahurAppearance`: `PASS`
- 대상 슬롯 `10`, 렌더러당 사용 재질 `15`, 삼각면 `4,330`,
  본 `24`, 승인 FBX 해시 일치를 확인했다.
- 적용 전후 슬롯 변형·활성 상태·게임플레이 컴포넌트와 비대상 씬
  루트가 동일하며 `CargoRunMvp`를 저장했다.
- 정면·3/4 프리뷰에서 승인된 적갈색 팔레트, 불꽃 표식, 커진 눈과
  화난 각도를 직접 확인했다. 프리뷰의 무조명 판독 모드는 저장되지
  않는 복제본에만 사용했고 실제 씬 재질은 URP PBR을 유지한다.

### 결과물

- `artSample/enemies/pahur/appearance_reference_sync/unity_validation/Pahur_ApprovedAppearance_UnityReport.txt`
- `artSample/enemies/pahur/appearance_reference_sync/unity_validation/Pahur_UnityApprovedAppearance_Front.png`
- `artSample/enemies/pahur/appearance_reference_sync/unity_validation/Pahur_UnityApprovedAppearance_ThreeQuarter.png`

### 비대상 보존

- Player, HUD, EventSystem, 기존 카메라·조명과 다른 적 루트는
  수정하지 않았다.
- Unity 편집기를 재시작하지 않았고 Git 작업은 수행하지 않았다.

## 파후르 적갈색 팔레트 아트 샘플

- 상태: `APPROVED_AND_APPLIED_TO_UNITY`
- 리비전: `2026-07-31_red_brown_palette_rework_22`
- 작업 위치:
  `artSample/enemies/pahur/appearance_reference_sync/`
- Unity 적용: 완료 (`승인 파후르 외형 Unity 적용` 기록 참조)

### 목표

현재 파후르 아트 샘플의 청회색 장갑·남색 두건·푸른 눈·흉부
청색 표시를 원본 이미지 색상 참고 없이 적갈색 중심의 팔레트로
변경한다.

### 반영 내용

- 외장·어깨·팔·다리·흉갑·골반 연결판을 산화철·마호가니 계열의
  중간~어두운 적갈색 금속으로 변경했다.
- 두건을 장갑보다 어두운 적갈색 천으로 변경했다.
- 눈은 어두운 적갈색 소켓과 따뜻한 암적색 발광으로 변경했다.
- 흉부 색상·발광 텍스처도 적갈색과 암적색 계열로 변경했다.
- 레거시 머티리얼 ID와 텍스처 파일명은 기존 참조 호환을 위해
  유지하고 Blender 표시명만 현재 색상 의미에 맞췄다.
- `build_pahur_textures.py --red-brown-palette-only`와
  `build_pahur_review_assets.py --red-brown-palette-only` 경로를
  추가했다. 두 경로 모두 원본 이미지를 열지 않는다.

### 색상 확인

- 주요 적갈색 알베도의 평균 색상 Hue는 약 `5.3°~8.6°`다.
- 어두운 두건 알베도의 평균 색상 Hue는 `353.7°`다.
- 눈 오버레이·발광 평균 Hue는 `6.2°~7.6°`다.
- 정면·3/4·머리·상반신·몸통 렌더에서 푸른 계열 대신 적갈색
  중심으로 보이며 금속과 천의 기존 명암·거칠기 구분은 유지된다.

### 보존 및 검증 결과

- 수정 전 매니페스트의 텍스처 `96`개와 비교했다.
- 승인된 색상 대상 텍스처 `13`개만 변경됐고 비대상 `83`개는
  SHA-256이 동일하다.
- 러프니스·메탈릭·노멀·높이 맵은 변경하지 않았다.
- `MATERIAL_ASSIGNMENT.json`과 `GEOMETRY_PRESERVATION.json`의
  SHA-256은 이전 리비전과 동일하다.
- `EYE_SURFACE_ANALYSIS.json`도 이전 매니페스트와 SHA-256이
  동일해 눈 중심·크기·각도 설정이 보존됐다.
- 구조 검사는 `PASS`다. Blend와 FBX는 정점 `2,434`, 에지 `6,548`,
  면 `4,330`, 루프 `12,990`, 본 `24`와 나머지 좌표·본 가중치·UV를
  유지한다.
- HTML 정적 DOM·자산 검사는 `PASS`이며 깨진 이미지와 링크는 없다.
  자동 브라우저 렌더는 연결 메타데이터 부재로 실행되지 않았고,
  생성 렌더를 직접 확인했다.

### 비대상 보존

- 건메탈 얼굴·무기·가스통, 검은 내부 기계부·호스와 주황 화염
  문양은 유지했다.
- 샘플 제작 단계에서는 프로젝트 원본 FBX를 수정하지 않았다.
- 원본 파후르 이미지를 열거나 분석하거나 색상을 추출하지 않았다.
- 샘플 제작 단계에서는 Unity 반영·새로고침·재시작과 Git 작업을
  수행하지 않았다. 이후 사용자 승인에 따라 위 Unity 적용 기록의
  범위만 반영했다.

## 파후르 이동 모델 교체와 승인 외형 동기화

- 상태: `APPLIED_AND_VALIDATED`
- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 대상 개체:
  `Approved Pahur Enemy Placement/Pahur_03_Move/Pahur_Model`
- 이동 모델:
  `Assets/_Project/Art/Enemies/Pahur/Models/PahurRunning.fbx`
- 원본 SHA-256:
  `03E78188715D61056ED2DE77D9F8289019A1F80A4604C073F94DE64A9A3F93B8`
- 원본 클립: 이동 FBX 내장 `mixamo.com`
- 재생 클립: `Pahur_03_Running_InPlace`, 반복 재생

### 반영 내용

- 기존 이동 모션 개체의 모델을 지정된 달리기 FBX로 교체했다.
- 같은 경로에 있던 이전 파일과 현재 원본의 해시가 다름을 확인하고,
  현재 원본을 Unity 임포트본에 다시 복사했다.
- 달리기 FBX의 정점 `4,184`개, 형상, 본, 스킨 가중치, 바인드 포즈,
  달리기 포즈와 내장 애니메이션은 유지했다.
- 정적 FBX와 이동 FBX의 bind-pose 메시 높이 비율을 계산하고
  이동 모델에 균일 스케일 `(0.497024357,0.497024357,0.497024357)`을
  적용해 다른 파후르와 실제 크기를 맞췄다.
- `mixamo.com`의 달리기 회전·상하 움직임·팔다리 곡선은 유지하고,
  FBX 축 변환을 고려해 모델 기준 X/Z에 해당하는 Hips 위치 곡선만
  첫 프레임 값으로 고정했다.
- 승인된 정적 파후르가 사용하는 머티리얼 `15`개를 같은 순서와
  참조로 연결했다.
- 새 모델의 기존 UV0는 보존했다. 정적 모델과 UV 삼각형이 비트
  단위로 동일한 `4,330`개 삼각형은 해당 승인 머티리얼 슬롯에
  직접 연결했다.
- 새 모델에만 있는 미대응 삼각형 `18`개와 정확한 UV3 대응이 없는
  정점 `54`개는 사용자 허용 후 정적 표면을 기준으로 배치를
  작성했다.
- 신규 색상·머티리얼·텍스처는 만들지 않고 승인된 기존 자산만
  사용했다.
- 다른 파후르 슬롯과 씬 루트는 변경하지 않았다.

### 결과와 검토 자료

- 적용: `PASS`
- 저장 상태 재검증: `PASS`
- 정적 파후르 기준 높이와 균일 스케일 일치: `PASS`
- 한 주기 Hips 모델 기준 X/Z 고정: `PASS`
- `Apply Root Motion`: `False`
- 적용 후 씬 저장: `True`
- 재검증 중 씬 변경: `False`
- 검증 기록:
  `docs/validation/pahur_running_model_animation_2026-07-31/Pahur_03_Running_Validation.txt`
- 검토 이미지:
  `docs/validation/pahur_running_model_animation_2026-07-31/Pahur_03_Running_Review.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Git 작업

## 파후르 돌파 화염방사기 모델 교체와 모션 적용

- 상태: `APPLIED_AND_DEDICATED_VALIDATION_PASSED`
- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 대상 개체:
  `Approved Pahur Enemy Placement/Pahur_05_BreakthroughFlamethrower/Pahur_Model`
- 교체 모델:
  `Assets/_Project/Art/Enemies/Pahur/Models/PahurBreakthrough.fbx`
- 지정 원본: `enemies model/pāḫḫur breakthrough.fbx`
- 원본 SHA-256:
  `B203DCC2134B2FFD54EDBC4D5E5A193675DC4B49C6170B95A7D89CF5A5C59F4A`
- 원본 클립: 교체 FBX 내장 `mixamo.com`
- 재생 클립:
  `Assets/_Project/Art/Enemies/Pahur/Animations/Pahur_05_BreakthroughFlamethrower_InPlace.anim`

### 반영 내용

- 기존 05번 돌파 화염방사기 모션 개체의 모델만 지정 FBX로
  교체했다.
- 임포트본과 지정 원본의 SHA-256 일치를 확인했다.
- 교체 FBX의 내장 애니메이션에서 이름 또는 take 이름에
  `mixamo`가 들어가는 항목이 `mixamo.com` 하나임을 확인하고
  이것만 반복 재생하도록 구성했다.
- 돌파 메시를 승인된 이동 외형 메시와 직접 대조해 정점, 노멀,
  탄젠트, UV0, 본 가중치, 삼각형과 본 배열이 정확히 같은 계약임을
  확인했다.
- 돌파 FBX의 정점·노멀·탄젠트·UV0·본 가중치·바인드 포즈와 원본
  애니메이션 자세는 유지하고, 승인 외형의 UV3와 `15`개 서브메시
  배치를 같은 정점 인덱스로 이전했다.
- 승인된 정적 파후르의 머티리얼 `15`개를 같은 순서와 참조로
  연결했다. 신규 색상·머티리얼·텍스처는 만들지 않았다.
- 정적 모델과 실제 높이를 맞춘 균일 스케일
  `(0.497024357,0.497024357,0.497024357)`과 같은 Y 위치
  `0.284377128`을 적용했다.
- 애니메이션의 자세·상하 움직임·회전은 유지하고 Hips의 모델 기준
  X/Z 이동 곡선만 상수화해 실제로 앞으로 진행하지 않게 했다.
- 오른팔 주무기의 연결 성분 `47`개 정점과 `RightHand` 본을
  유일하게 식별하고, 전 프레임에서 총열이 바닥과 평행하도록
  오른손 회전 곡선을 작성했다.
- 04번 미니 화염방사기 개체에 현재 배치된 총구 오브젝트와 두
  파티클 자식을 그대로 복제해 돌파 총열의 실제 끝점에 부착했다.
  파티클 직렬화 값, 자식 로컬 변환, 기존 화염 머티리얼 참조를
  원본 미니 화염과 대조했다.
- 미니 화염에서 승인된 전체 좌우 폭 `2배`, 끝부분 좌우 폭 `5배`,
  원거리 상하 확산 설정을 그대로 유지했다.
- 다른 파후르 슬롯과 씬 루트는 변경하지 않았다.

### 결과와 검토 자료

- 원본 계약 검사: `PASS`
- 적용 및 씬 저장: `PASS`
- 전용 저장 상태 재검증: `PASS`
- 승인 외형 채널의 정확 이전: `PASS`
- 원본 메시·스킨·바인드 포즈 보존: `PASS`
- 내장 Mixamo 클립 반복 재생: `PASS`
- 한 주기 Hips 모델 기준 X/Z 고정: `PASS`
- 주무기 최대 수직 기울기: `0.2974057°`
- 기존 미니 화염 이펙트 복사: `PASS`
- 총구 파티클 시스템: `2`
- 재검증 중 씬 변경: `False`
- 검증 기록:
  `docs/validation/pahur_breakthrough_flamethrower_2026-07-31/Pahur_05_BreakthroughFlamethrower_Validation.txt`
- 최종 검토 캡처는 전용 검증 통과 후 규칙에 따라 한 번만
  실행했으며 장면 변경 없이 PNG를 생성했다.
- 다만 생성된 PNG는 모델이 렌더되지 않은 빈 배경이므로 시각
  증빙으로는 `INVALID`이다. 같은 캡처를 재실행하지 않았다.
- 검토 이미지:
  `docs/validation/pahur_breakthrough_flamethrower_2026-07-31/Pahur_05_BreakthroughFlamethrower_Review.png`

### 총과 고개 정면 보정

- 기존 보정은 총열의 수직 성분만 제거해 바닥과 평행하게 만들고
  좌우 방향은 원본 애니메이션대로 남겨 두었으며, `Head` 회전은
  수정하지 않는 상태였다.
- 돌파 리그에는 `Neck` 이름의 본이 없음을 확인했다. 임의로 다른
  척추 본을 목으로 간주하지 않고 명시적으로 존재하는 `Head` 본만
  승인된 정적 파후르의 같은 `Head` 로컬 회전과 일치시켰다.
- `RightHand`에 연결된 실제 총열 `47`개 정점을 매 프레임 분석해
  총열 축이 파후르 모델 로컬 정면을 향하도록 오른손 회전 곡선을
  다시 작성했다.
- 키 사이 쿼터니언 보간에서 발생한 `0.359364659°` 편차를 허용값
  완화로 처리하지 않고, 보정 키 밀도를 원본 프레임률의 두 배로
  높여 정면 편차를 줄였다.
- 적용 후 전체 반복 구간의 총열 최대 정면 편차는
  `0.251788318°`, 최대 수직 편차는 `0.230982°`이다.
- `Head` 로컬 회전과 승인된 정적 파후르 `Head` 회전의 최대
  편차는 `0°`이다.
- 총구 위치와 화염 분사 방향은 보정된 총열 끝점과 정면 방향을
  다시 기준으로 계산했다.
- 승인 외형, 원본 메시·스킨·바인드 포즈, `15`개 머티리얼,
  균일 스케일, Y 위치, 제자리 반복과 기존 미니 화염 이펙트 복사는
  유지했다.
- 다른 파후르 슬롯과 씬 루트는 변경하지 않았다.
- 전용 검증: `PASS`
- 검증 중 씬 변경: `False`
- 기존 빈 최종 캡처는 재실행하지 않았다.

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Git 작업
- 신규 화염 VFX·머티리얼·텍스처 생성
- 미니 화염방사기 슬롯 수정
- 최종 캡처 재실행

## 파후르 미니 화염방사기 모델 교체와 모션 적용

- 상태: `APPLIED_AND_VALIDATED`
- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 대상 개체:
  `Approved Pahur Enemy Placement/Pahur_04_MiniFlamethrower/Pahur_Model`
- 교체 모델:
  `Assets/_Project/Art/Enemies/Pahur/Models/PahurFlameAttack.fbx`
- 지정 원본:
  `enemies model/pāḫḫur flame attack.fbx`
- 원본 SHA-256:
  `35AC10B350B7B643B4BB6C8A0E2CE3541A65B7DA8C7A121DD7DF7C74FDC16E5E`
- 원본 클립: 교체 FBX 내장 `mixamo.com`
- 재생 클립:
  `Assets/_Project/Art/Enemies/Pahur/Animations/Pahur_04_MiniFlamethrower_Horizontal.anim`

### 반영 내용

- 기존 미니 화염방사기 모션 개체의 모델을 지정된 FBX로 교체했다.
- 임포트본은 지정 원본과 바이트 단위로 같은 SHA-256을 유지한다.
- 교체 모델과 승인된 이동 외형 메시를 직접 대조한 결과 정점,
  노멀, 탄젠트, UV0, 본 가중치, 삼각형과 본 배열이 모두 정확히
  같고 바인드 포즈만 달랐다.
- 교체 모델의 바인드 포즈와 공격 자세는 보존하고, 동일한 정점
  인덱스를 기준으로 승인 외형의 UV3와 `15`개 서브메시 배치를
  그대로 이전했다.
- 승인된 정적 파후르의 머티리얼 `15`개를 같은 순서와 참조로
  연결했다. 신규 색상·머티리얼·텍스처는 만들지 않았다.
- 정적 모델의 실제 높이와 Y 위치를 기준으로 균일 스케일
  `(0.497024357,0.497024357,0.497024357)`과 Y
  `0.284377128`을 적용했다.
- FBX 내장 `mixamo.com`만 사용하고 반복 재생하도록 구성했다.
  애니메이션 자세와 상하 움직임은 유지하고 모델 기준 X/Z 이동만
  고정해 모션 개체가 앞으로 진행하지 않게 했다.
- 오른팔 사슬에 연결된 실제 주무기 본체를 연결 성분으로 분리해
  총열 축을 계산하고, 전 프레임에서 바닥과 수평이 되도록 오른손
  회전 곡선을 작성했다.
- 총구 끝에 `Pahur_MiniFlamethrower_Muzzle`을 배치하고 기존
  `Smorzando_Flame_Outer`와 `Smorzando_Flame_Core` 머티리얼을
  재사용한 반복 파티클 두 겹을 추가했다. 화염은 총열 축을 따라
  앞으로 연속 분사된다.
- 사용자 확인에 따라 총구 위치를 총열 끝부분 평균점에서 실제
  총열 최외곽 평면으로 수정했다. 각 파티클 레이어의 최초 가시
  뒤쪽 가장자리를 이 평면에 맞추고, 뒤로 늘어나던 Stretch 렌더를
  전방 이동형 Billboard로 교체해 총열이 화염에 덮이지 않도록 했다.
- 사용자 확인에 따라 총구에서 가까운 화염은 좁게 유지하고,
  멀어진 끝부분만 위아래로 퍼지도록 로컬 Y축 양방향 속도 분산을
  추가했다.
- 사용자 녹화본
  `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-31 17-58-29.mp4`
  에서 주 화염 덩어리의 원거리 외곽이 여전히 직선으로 보이는
  것을 확인했다.
- 실시간 재생에서도 주 화염 자체가 퍼지도록 외곽 레이어는 화염
  길이 대비 `±0.75`, 중심 레이어는 `±0.50`의 Y축 속도 분산을
  사용하고, 수명 `72%` 지점의 Y 크기를 `1.75배`로 확장했다.
  X/Z 추가 속도는 `0`으로 유지한다.
- 사용자 지정값에 따라 Billboard의 좌우 축인 `size.x`만 조정해
  화염 전체 좌우 폭을 변경 전 대비 최소 `2배`로 확대하고, 수명
  `72%`의 가시 끝부분은 변경 전 대비 정확히 `5배`로 확대했다.
  총구 시작 위치, 전방 길이, 로컬 Y축 상하 확산과 파티클 재질은
  변경하지 않았다.
- 사용자 첨부 이미지 `C:/Users/gus68/OneDrive/바탕 화면/1111.png`에서
  기존 보정이 밑창 최저점 하나만 지면에 맞춰 왼발 뒤꿈치가 들린
  까치발 상태임을 확인했다.
- 공격 메시의 바인드 포즈에서 `LeftLeg → LeftFoot → LeftToeBase`
  해부학적 축을 계산하고, 발뒤꿈치 끝과 앞꿈치 끝의 실제 최저
  정점군을 각각 추출했다. 전 발사 루프에서 두 접촉면을 수평으로
  맞추는 `LeftFoot` 회전과 `LeftUpLeg → LeftLeg` 접지 CCD를
  교대로 수렴시켜 왼발바닥 전체가 바닥과 평행하게 닿도록 했다.
- 왼발의 기존 X/Z 위치 흐름은 유지했으며 오른발·상체·무기·불꽃
  모션은 변경하지 않았다.
- 다른 파후르 슬롯과 씬 루트는 변경하지 않았다.

### 결과와 검토 자료

- 적용: `PASS`
- 저장 상태 재검증: `PASS`
- 승인 외형 채널의 정확 이전: `PASS`
- 내장 Mixamo 클립 반복 재생: `PASS`
- 한 주기 Hips 모델 기준 X/Z 고정: `PASS`
- 주무기 최대 수직 기울기: `0.07841153°`
- 총구 파티클 시스템: `2`
- 화염 최초 가시 영역과 실제 총구 끝면 정렬:
  `FlameVisibleStartAlignedToMuzzle=True`
- 화염 끝부분 상하 확산:
  `FlameTailVerticalSpread=True`
- 원거리/근거리 화염 상하 반폭 비율: `3.55631018`
- 화염 전체 최소 좌우 폭 배율: `2`
- 화염 끝부분 좌우 폭 배율: `5`
- 화염 끝부분/근거리 실제 좌우 폭 비율: `1.18149936`
- 왼발 접지: `LeftFootGrounded=True`
- 왼발 밑창 최대 지면 오차: `0.00658081472m`
- 왼발 밑창 최대 높이 차: `0.00519262534m`
- 왼발 밑창 최대 기울기: `0.07912937°`
- 적용 후 씬 저장: `True`
- 재검증 중 씬 변경: `False`
- 검증 기록:
  `docs/validation/pahur_mini_flamethrower_2026-07-31/Pahur_04_MiniFlamethrower_Validation.txt`
- 검토 이미지:
  `docs/validation/pahur_mini_flamethrower_2026-07-31/Pahur_04_MiniFlamethrower_Review.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Git 작업

## 파후르 수호 화염방사기 모델 교체와 모션 적용

- 상태: `APPLIED_AND_DEDICATED_VALIDATION_PASSED`
- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 대상 개체:
  `Approved Pahur Enemy Placement/Pahur_06_GuardianFlamethrower/Pahur_Model`
- 교체 모델:
  `Assets/_Project/Art/Enemies/Pahur/Models/PahurGuardianPose.fbx`
- 지정 원본: `enemies model/pāḫḫur guardian pose.fbx`
- 원본 SHA-256:
  `143C3FB64A7A42CC64A9EA18AA43B4282A21FD96DA859E7E439AA007C7E8B49D`
- 원본 클립: 교체 FBX 내장 `mixamo.com`
- 재생 클립:
  `Assets/_Project/Art/Enemies/Pahur/Animations/Pahur_06_GuardianFlamethrower_InPlace.anim`

### 반영 내용

- 기존 06번 수호 화염방사기 모션 개체의 모델만 지정 FBX로
  교체했다.
- 임포트본과 지정 원본의 SHA-256 일치를 확인했다.
- 이름 또는 take 이름에 `mixamo`가 포함된 내장 애니메이션이
  `mixamo.com` 하나임을 확인하고 이것만 반복 재생하도록 구성했다.
- Guardian 메시와 승인된 이동 외형 메시의 정점, 노멀, 탄젠트,
  UV0, 본 가중치, 삼각형과 본 배열이 정확히 같은 계약임을
  확인했다.
- Guardian 원본 메시·스킨·바인드 포즈·애니메이션 자세는 유지하고
  승인 외형의 UV3와 `15`개 서브메시 배치를 같은 정점 인덱스로
  이전했다.
- 승인된 정적 파후르의 머티리얼 `15`개를 같은 순서와 참조로
  연결했다. 신규 모델·메시·머티리얼·텍스처는 만들지 않았다.
- 정적 모델과 실제 높이를 맞춘 균일 스케일
  `(0.497024357,0.497024357,0.497024357)`과 같은 Y 위치
  `0.284377128`을 적용했다.
- 애니메이션의 자세·상하 움직임·회전은 유지하고 모델 기준 X/Z
  이동만 고정해 제자리에서 반복되게 했다.
- `RightHand`에 연결된 실제 총열 `47`개 정점을 전 프레임에서
  분석해 총열이 바닥과 평행하고 모델 정면을 향하도록 오른손
  회전 곡선을 작성했다.
- 04번 미니 화염방사기의 현재 총구 계층과 두 파티클 레이어를
  그대로 복제하고 Guardian 총열의 실제 끝점에 배치했다.
- 미니 화염의 머티리얼, 전체 좌우 폭 `2배`, 끝부분 좌우 폭
  `5배`, 기존 상하 분산을 유지했다.
- Guardian 복사본의 시작 전방 속도를 미니 화염 원본 대비 정확히
  `2배`로 설정해 수호 모드 화염 길이를 늘렸다. 이 값은
  `GuardianFlameLengthMultiplier`로 전용 도구에 기록했다.
- 미니 화염의 끝 폭 확대 기준 `72%`와 Guardian 상승 시작 기준을
  분리했다. Guardian 화염은 직선 기준 전체 이동거리의 `75%`까지
  기존 수평 진행을 유지하고, 이후 로컬 Y 중심 속도를 높이는
  동시에 로컬 Z 추가 속도로 전방 중심 속도를 점진적으로
  상쇄한다. 끝 지점의 전방 중심 속도는 `0`, 진행 방향은 수평
  기준 위쪽 `90°`다. 시작 비율과 끝 각도는 각각
  `GuardianTailRiseStartLengthRatio`, `GuardianTailRiseAngleDegrees`로
  전용 도구에 기록했다.
- 상승 구간의 Y 중심 속도 곡선을 적분했을 때 상승 시작점부터
  끝까지의 Y축 중심 변위가 수호 화염의 직선 기준 전체 길이의
  `1.5배`가 되도록 끝 상승 속도를 전방 속도의 `12배`로 설정했다.
  이는 직전 상승 높이 `0.5배`를 정확히 `3배` 확대한 값이다.
  목표 비율은 `GuardianTailRiseHeightRatio`로 기록하고, 전용 검증이
  각 파티클 레이어의 실제 곡선 적분값을 확인하도록 했다.
- 수평 구간의 기존 두 파티클 레이어와 방출률은 유지했다. 동일한
  재질·크기·속도·궤적을 가진 상승 구간 전용 레이어 `2`개를
  복제하고 각각 기존 방출률의 `4배`를 추가해 상승 구간의 합산
  방출 밀도를 정확히 `5배`로 만들었다.
- 보강 레이어는 수명 `75%`까지 알파 `0`이며, Unity Gradient 키
  정밀도를 고려해 `75%→76%` 구간에서 짧게 나타난 뒤 기존 레이어의
  색상·알파에 합류한다. 따라서 수평 구간의 가시 밀도는 바뀌지
  않는다. 보강 레이어의 `maxParticles`도 기존의 `4배`로 늘려 방출
  밀도가 상한에 걸리지 않게 했다.
- 첫 적용 시 알파 전환 폭 `0.0001`이 Unity Gradient 키 시간
  정밀도보다 좁아 경계 알파 `0.02287053`, 색상 차이 `0.0178675652`가
  검출됐다. 이 적용은 씬 교체 전에 중단되어 기존 상태가 보존됐고,
  전환 폭을 `0.01`로 바꾼 뒤 적용과 전용 검증을 통과했다.
- 사용자 실행 후 보고된 컴파일 오류를 조사하기 위해 Unity
  `Editor.log`의 최신 구간과 강제 자산 새로고침 직후 로그를
  확인했다. 현재 Guardian 코드 이후의 `error CS`,
  `Compilation failed`, `Scripts have compiler errors`는 `0건`이며,
  검색된 컴파일 오류는 이전 작업 시점의 누적 기록이었다.
- 상승 시작점 `75%`, 상승 높이 `1.5배`, 끝 각도 `90°` 및 길이
  `2배` 수정 후 Unity 자산 새로고침, Guardian 적용과 전용 검증을
  다시 실행해 현재 소스가 정상 컴파일되는 것을 확인했다.
- 실제 실행 시 `Particle Velocity curves must all be in the same mode`
  메시지가 반복되는 원인을 확인했다. 미니 화염 원본의 X/Y/Z
  속도는 모두 `TwoConstants`였지만 Guardian 변환에서 Y만
  `TwoCurves`로 바꾸고 X/Z를 `TwoConstants`로 남겨 Unity가
  Velocity over Lifetime 모듈 전체를 거부하고 있었다.
- 이 모드 불일치 때문에 당시 Y축 상승 곡선도 실제 파티클
  시뮬레이션에 적용되지 않았다.
- Guardian 화염의 X/Y/Z 속도 모드는 모두 `TwoCurves`로 통일했다.
  X는 전 수명 구간에서 정확히 `0`이고, Z는 이동거리 `75%`까지 `0`을
  유지한 뒤 끝 지점에서 시작 전방 속도를 정확히 상쇄한다. Y는
  기존 상하 분산을 유지하면서 이동거리 `75%` 이후 중심이 상승한다.
- 수정 후 적용·전용 검증 전후의 해당 오류 누적 횟수는
  `16846 → 16846`으로 새 오류 발생은 `0건`이었다.
- 다른 파후르 슬롯과 씬 루트는 변경하지 않았다.

### 결과와 검토 자료

- 원본 계약 검사: `PASS`
- 적용 및 씬 저장: `PASS`
- 전용 저장 상태 재검증: `PASS`
- 승인 외형 채널의 정확 이전: `PASS`
- 원본 메시·스킨·바인드 포즈 보존: `PASS`
- 내장 Mixamo 클립 반복 재생: `PASS`
- 한 주기 Hips 모델 기준 X/Z 고정: `PASS`
- 주무기 최대 수직 기울기: `0.0402537435°`
- 주무기 최대 정면 편차: `0.06852804°`
- 기존 미니 화염 이펙트 복사: `PASS`
- 총구 파티클 시스템: 기본 `2` + 상승 밀도 보강 `2` = `4`
- 수호 화염 길이 배율: `2`
- 화염 상승 시작 위치 비율: `0.75`
- 직전 대비 화염 상승 높이 배율: `3`
- 화염 상승 높이 목표 비율: `1.5`
- 두 파티클 레이어의 실제 상승 높이 비율:
  `1.49999976 / 1.50000024`
- 상승 구간 목표 밀도 배율: `5`
- 두 레이어의 실제 합산 밀도 배율: `5 / 5`
- 보강 레이어 비가시 종료 비율: `0.75`
- 보강 레이어 완전 가시 시작 비율: `0.76`
- 화염 끝부분 목표 상승각: `90°`
- 두 파티클 레이어의 실제 끝부분 상승각:
  `90° / 90°`
- Velocity over Lifetime X/Y/Z 모드: `TwoCurves / TwoCurves / TwoCurves`
- X 추가 속도: `0`
- 상승 시작 지점의 Z 추가 속도: `0`
- 끝 지점 전방 중심 속도: `0`
- 수정 후 새 Velocity 모드 오류: `0건`
- 최신 Guardian 관련 활성 컴파일 오류: `0건`
- 재검증 중 씬 변경: `False`
- 검증 기록:
  `docs/validation/pahur_guardian_flamethrower_2026-07-31/Pahur_06_GuardianFlamethrower_Validation.txt`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Git 작업
- 신규 화염 디자인·머티리얼·텍스처 생성
- 정적·미니·돌파 및 다른 파후르 슬롯 수정
- 다른 Unity 씬 열기 또는 저장
- 최종 캡처 실행
