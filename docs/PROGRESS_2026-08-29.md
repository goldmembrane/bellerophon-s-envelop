# 진행 기록 2026-08-29

## Hands_Draw_Back 공용 메시 통일과 오른팔 전방 인출

- `docs/PROGRESS_2026-08-28.md`의 `Hands_Draw_Back 공용 메시 통일 후 오른팔 전방 인출 수정 계획 — 미적용`을 기준으로 두 단계 작업을 완료했다. 계획에 없는 목표값·구도·판정 기준은 추가하지 않았다.
- 1단계에서 `Hands_Draw_Back/char1`의 상태 전용 `m_Mesh` 및 `m_BlendShapeWeights` 프리팹 오버라이드를 제거했다. 최종 렌더러는 `Hands_Empty_Idle`과 같은 `Assets/_Project/Art/Player/player.fbx`의 공용 `char1` 메시를 상속하며, 머티리얼 배열·root bone·bone 경로와 순서·로컬 bounds·렌더러 플래그가 일치한다. 기존 `Hands_Draw_Back_ChestCorrected.asset`과 과거 검증 자료는 삭제하거나 수정하지 않았고 현재 씬은 해당 메시를 참조하지 않는다.
- 1단계 직접 확인에서는 기존 상태 전용 메시, 공용 메시 적용 결과와 `Hands_Empty_Idle`을 같은 12위상의 전신 정면·측면·오른쪽 가슴 확대 구도로 나란히 비교했다. 공용 메시 결과에 부품 누락, 오른쪽 가슴의 새 돌출, 겨드랑이 찢김, 표면 꺼짐과 머티리얼 차이가 보이지 않아 애니메이션 단계로 진행했다.
- 2단계에서 원본 `Hands_Draw_Back_Mixamo.fbx`의 2.3초·30fps·69프레임 `mixamo.com` Take를 다시 표본화해 `Hands_Draw_Back_ForwardAdjusted.anim`을 생성했다. 현재 원본의 등 뒤 인출 시작은 43프레임, 바깥 경로 중간은 54프레임, 최대 도달은 66프레임으로 재측정됐고, `RightArm`, `RightForeArm`, `RightHand` 회전 곡선만 교체했다. 몸통·목·머리·왼팔·하체·루트·이벤트와 오른팔 외 곡선은 원본 그대로 유지했으며 BlendShape 곡선은 없다.
- 2단계 직접 확인에서는 실제 Unity Play Mode에서 공용 메시를 사용하는 원본과 수정본을 같은 12위상의 전신 정면·측면, 오른팔 확대 정면·측면, 오른쪽 가슴 확대 정면으로 나란히 비교했다. 원본의 머리 위 지향과 달리 수정본은 팔꿈치가 몸 오른쪽 실루엣 밖으로 먼저 나오고 손이 명치 앞쪽으로 진행했다. 몸통·얼굴 교차, 팔꿈치 역꺾임, 손목 직각 꺾임, 프레임 사이 튐, 가슴·겨드랑이 변형과 반복 경계 불연속은 보이지 않았다.
- 직접 확인 뒤 보조 검증에서 최대 도달의 정면 방향 오차는 `0°`, 손–명치 높이 차이는 약 `0.000002623m`, 팔꿈치 굽힘은 `29.998163°`, 손바닥 왼쪽 목표 오차는 `0°`였다. `69프레임×2루프`에서 런타임 적용 위치·회전 오차는 최대 약 `0.000000147m / 0°`, 오른팔 외 자세 차이와 루트 변위는 `0`, `Loop=True`, `ApplyRootMotion=False`였다.
- 원본 `player model/transfer grab from behind.fbx`와 Unity FBX의 SHA-256은 모두 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`, 공용 플레이어 FBX는 `E025F1CB33BE02D527312A25CCA2820F3F0155FF07BE1ABC7FFD54CC6074DA75`, 상태 전용 메시 에셋은 `588075A76E5DD9658BA9C2ED52156F14C76BA9431A82EE9D491B5C9034024579`, Stow Controller는 `C0AFF2A9F8FE7684E9A1E776D2D84CAA212C688C392C4287DF998B64F6ADE0D5`로 적용 전후 동일하다. 최종 파생 클립은 `A71BE82D62431C7A15DB14AE6E7859438E9EBC61BB642BB952F300D89B926B30`, Draw Controller는 `27B6D1BDAC11ABEE609A21CB2A9A18ECD212653A6F7C80B75C055021456C69D4`다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsDrawBackCommonMesh`, `CapturePlayerHandsDrawBackCommonMeshReview`, `ApplyPlayerHandsDrawBackCommonMeshForward`, `CapturePlayerHandsDrawBackCommonMeshForwardReview`, `CapturePlayerHandsDrawBackCommonMeshForwardFinal`을 사용했다. 첫 적용 전에 발생한 에디터 API 컴파일 오류는 씬 변경 전에 기존 저장소 패턴으로 교정했고 이후 리프레시와 두 적용 명령이 정상 완료됐다.
- 적용·직접 확인·보조 지표와 한 번만 확정한 최종 이미지는 `docs/validation/player_hands_draw_back_common_mesh_forward_2026-08-29/`에 저장했다. 승인된 작업 범위의 남은 항목은 없다.
- 아트 샘플, 요구사항 HTML, 테스트·빌드와 Git 작업은 실행하지 않았다.

## Hands_Draw_Back 오른팔 얼굴 실루엣 겹침 수정

- 사용자가 현재 `Hands_Draw_Back` 오른팔과 얼굴의 겹침을 지적한 뒤, 기존 최종 비교 이미지의 수정본 12위상을 다시 직접 확인했다. 후반 인출 구간에서 오른쪽 전완과 손이 정면 기준 얼굴 하단·목 앞 실루엣으로 들어오는 상태를 확인했다.
- 원인은 공용 메시 전방 인출 생성이 기존 `outerElbowPath`만 사용하고, 저장소에 이미 구현된 `fullTorsoClearance`와 `frontSilhouetteClearance` 경로를 연결하지 않은 것이었다. 새 목표값을 만들지 않고 기존 기본 바깥 방향 `35°`와 최소 전방 실루엣 여유 `0.005m` 기준을 현재 생성·검토 경로에 연결했다.
- 원본 `Hands_Draw_Back_Mixamo.fbx`의 2.3초·30fps·69프레임 Take에서 `RightArm`, `RightForeArm`, `RightHand` 회전 곡선만 다시 생성했다. 공용 `player.fbx` 메시, BlendShape, 오른팔 외 곡선·이벤트, 루트, 다른 Animator와 `Hands_Stow_Back`은 변경하지 않았다. 새 검증 산출물은 `docs/validation/player_hands_draw_back_face_clearance_2026-08-29/`로 분리했다.
- 직접 확인은 Unity Play Mode에서 원본과 수정본을 같은 12위상의 전신 정면·측면, 오른팔 확대 정면·측면, 얼굴·가슴 확대 정면으로 나란히 비교했다. 수정본의 상완·전완·손 전체가 모든 위상에서 얼굴·헬멧 실루엣 밖으로 분리됐고, 후반 인출에서도 목 앞을 가로지르지 않았다. 손의 가슴 앞 도달, 팔꿈치·손목 연결, 가슴·겨드랑이 외형과 루프 경계도 정상으로 직접 판정했다. 첫 수정이 통과해 추가 각도 조정과 추가 검토 캡처는 하지 않았다.
- 직접 확인 뒤 보조 검증에서 최소 얼굴 실루엣 여유는 약 `0.028796m`, 최대 도달 방향은 약 `34.999737°`, 손–명치 높이 차이는 약 `0.000000238m`, 팔꿈치 굽힘은 약 `29.999018°`였다. `69프레임×2루프`의 루트 변위는 `0`, 오른팔 외 자세 차이는 `0`, 런타임 적용 오차는 최대 약 `0.000000147m / 0°`, `Loop=True`, `ApplyRootMotion=False`였다.
- 적용 뒤 원본과 Unity FBX의 SHA-256은 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`, 공용 플레이어 FBX는 `E025F1CB33BE02D527312A25CCA2820F3F0155FF07BE1ABC7FFD54CC6074DA75`, 보존 메시 에셋은 `588075A76E5DD9658BA9C2ED52156F14C76BA9431A82EE9D491B5C9034024579`, Stow Controller는 `C0AFF2A9F8FE7684E9A1E776D2D84CAA212C688C392C4287DF998B64F6ADE0D5`로 기존과 동일하다. 최종 파생 클립은 `CAB1425F79AA9A6E3A1CFA71F045CEB84E04103677D6C0083BBBD911DF188585`, Draw Controller는 `C0811F84D283EBDF0106945F2BB32DE4FF4D07ABE4D2DAB458EA3118429E1226`다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsDrawBackCommonMeshForward`, `CapturePlayerHandsDrawBackCommonMeshForwardReview`, `CapturePlayerHandsDrawBackCommonMeshForwardFinal`을 사용했다. 최종 이미지는 직접 확인을 통과한 검토 이미지를 한 번만 복사해 확정했다.
- 별도 아트 샘플, 테스트·빌드와 Git 작업은 실행하지 않았다.

## Hands_Throw_Ready와 Hands_Throw_Release Mixamo 연결

- 사용자 지정 원본 `player model/transfer throwing.fbx`를 `Assets/_Project/Art/Player/Animations/Hands_Throw_Mixamo.fbx`로 그대로 복사했다. 두 파일의 SHA-256은 모두 `AF4F841C549ABFD62D5FC0E349CC744BD6A0837E578E9078F515F8A55DDB7BF5`로 일치하며, Unity에서는 내장 `mixamo.com` Take를 Generic 리그·곡선 재표본화 해제·애니메이션 압축 해제 상태로 가져왔다. 원본 바이너리와 원본 동작값은 수정하지 않았다.
- 전체 원본을 30fps의 모든 프레임으로 먼저 직접 확인했다. 2.2초·66프레임 구간의 오른손 최고점은 유일한 `23프레임`, 시간 `0.7666667초`였고, 이 프레임을 Ready 절단점으로 사용했다.
- `Hands_Throw_Ready`에는 원본 시작부터 23프레임까지의 곡선을 그대로 복사하고 최고점 자세를 정확히 `0.5초` 유지하는 `Hands_Throw_Ready_MixamoPeakHold.anim`을 연결했다. 전체 길이는 `1.2666667초`이며 끝에서 처음으로 반복된다. 원본에 없는 중간 키·보간·동작은 생성하지 않았다.
- `Hands_Throw_Release`에는 별도 파생 동작을 만들지 않고 FBX의 2.2초 전체 `mixamo.com` Take를 Controller에서 직접 참조해 끝까지 반복 재생하도록 연결했다. 두 Animator 모두 `Loop=True`, `ApplyRootMotion=False`이며 다른 Animator와 루트 상태는 변경하지 않았다.
- 1순위 직접 확인에서는 Unity Play Mode에서 원본 기대 동작과 실제 Ready·Release를 각각 같은 12위상의 전신 정면·측면과 오른팔 확대 정면으로 나란히 비교했다. Ready는 원본과 단계별로 일치하며 오른팔 최고점 도달 뒤 같은 자세가 유지됐고, Release는 원본 전체 던지기와 복귀 동작이 전 구간 일치했다. 모델 누락, 자세 파손, 루트 이동과 지시하지 않은 동작 변화는 보이지 않았다.
- 직접 확인 뒤 보조 검증에서 Ready는 `38프레임×2루프`, Release는 `66프레임×2루프`를 확인했다. Ready 런타임 원본 차이는 최대 약 `0.000000080m / 0°`, 원본 접두 구간과 0.5초 정지 구간의 직접 곡선 차이는 모두 `0`이었다. Release 런타임 원본 차이는 최대 약 `0.000000104m / 0°`였고, 두 개체의 루트 변위는 모두 `0`이었다.
- 첫 Ready 런타임 비교에서 파생 클립에 남아 있던 원본 `stopTime=2.2초` 때문에 재생 시간이 늘어나는 문제를 직접 확인했다. 동작값을 바꾸지 않고 파생 클립의 종료 시간을 실제 길이 `1.2666667초`로 맞춘 뒤 승인된 추가 비교 1회에서 원본과의 일치를 확인했다. 앞선 생성 과정에서 Generic Euler 메타데이터가 누락된 문제도 원본 클립 직렬화 메타데이터를 보존하는 방식으로 교정했으며, 두 교정 모두 새로운 모션 값은 만들지 않았다.
- 최종 SHA-256은 Ready 파생 클립 `665F8012F19461B9EE693C5BDD3EBB2D3A1F8B85403DC5C73B90B8D7C9A13FB0`, Ready Controller `8A66BD1A5E851C58ABB4E0BA54A8634457ED2AD2E71938CE9EF7CEB002054825`, Release Controller `91C9C0B731A3C96709D4AA71438F2DEE613F8A663968D0BF34DEA3956BC3650D`다.
- Unity 반영과 확인에는 `./scripts/Refresh-UnityProject.ps1`, `CapturePlayerHandsThrowSourceDiagnostic`, `ApplyPlayerHandsThrowMixamo`, `CapturePlayerHandsThrowMixamoReview`, `CapturePlayerHandsThrowMixamoFinal`을 사용했다. 전체 프레임 진단, 적용 지표, 직접 비교와 한 번만 확정한 최종 이미지는 `docs/validation/player_hands_throw_mixamo_2026-08-29/`에 저장했다.
- 요구사항 HTML, 테스트·빌드와 Git 작업은 실행하지 않았다.

## Hands_Throw_Cancel Ready 역방향 반복

- `Hands_Throw_Cancel`은 `Hands_Throw_Ready_MixamoHeadHeightBreathing.anim`의 호흡 시작 전 머리 높이 자세인 19프레임을 기준으로 한다. 시작 자세를 호흡·무릎·몸 상하 동작 없이 정확히 `0.5초` 유지한 뒤, Ready 19→0프레임 역동작 전체에서 `Player_Idle.anim` 0프레임 자세 반영률을 프레임별 `0%→100%`로 균등하게 높인다. 역동작 마지막 프레임은 정확히 Player Idle 자세이고, 이 자세를 다시 `0.5초` 유지한 뒤 처음부터 반복한다.
- `Assets/_Project/Art/Player/Animations/Hands_Throw_Cancel_MixamoReverse.anim`과 전용 `Hands_Throw_Cancel.controller`를 씬의 `Hands_Throw_Cancel` Animator에만 연결했다. 전체 길이는 `1.6333333초`, 역동작·점진 보정 구간은 `0.6333333초`, 시작과 마지막 유지 구간은 각각 `0.5초`다. `Loop=True`, `ApplyRootMotion=False`이며 BlendShape 곡선과 이벤트는 없다. Cancel은 공용 `Assets/_Project/Art/Player/player.fbx` 메시를 그대로 사용한다.
- 1순위 직접 확인에서는 Unity Play Mode에서 예상 혼합 자세와 실제 Cancel을 같은 12개 시점의 전신 정면·측면으로 비교했다. 시작 자세가 유지된 뒤 오른팔·몸통·하체가 프레임마다 연속적으로 Player Idle 자세에 맞춰졌고, 마지막 여러 시점은 같은 Idle 자세를 유지했다. 프레임 사이 튐, 관절 역꺾임, 메시 변형, 장비·신체의 새 겹침은 보이지 않았다.
- 직접 확인 뒤 보조 검증에서 시작 정지 구간은 `0m / 0°`, 프레임별 혼합 기대 자세와 실제 클립 차이는 최대 약 `0.000000217m / 0°`, 역동작 마지막 프레임과 뒤 0.5초는 Player Idle 0프레임과 최대 약 `0.000000217m / 0°`였다. 실제 Animator는 `49프레임×2루프`를 재생했고 생성 클립과의 최대 차이는 약 `0.000000095m / 0°`, 혼합 기대 자세와의 최대 차이는 약 `0.000000023m / 0°`, 최종 Idle과 유지 구간 차이는 `0m / 0°`, 루트 변위는 `0m`였다.
- 첫 적용에서는 개별 Quaternion 커브의 탄젠트를 수학적으로 뒤집은 뒤 Unity 연속성 재계산이 적용되면서 정지 회전 차이 `7.430302°`, 역구간 위치·회전 차이 약 `0.122521m / 169.110245°`가 발생했다. 직접 검토 캡처 전에 실패를 확인했으며, 임의 자세를 생성하지 않고 Ready의 실제 30fps 프레임을 19→0 순서로 그대로 표본화해 복사하는 방식으로 교정했다. 교정 후 모든 직접 자세 차이가 `0`으로 통과했다.
- 적용 전후 Player Idle 클립 SHA-256은 `F835EA47600940846039E4BD323D3D6FEC6B05C676E9DEBB9293344B839AA853`, Ready 클립은 `B064818B60576385598964A8C1AF492C6B7A6F9D614356BB963B0391243B85B6`, Ready Controller는 `0B8485EF5EC16C168C39893052805F9C395A96710D182745D017FCA9A88D810D`, Release Controller는 `91C9C0B731A3C96709D4AA71438F2DEE613F8A663968D0BF34DEA3956BC3650D`로 동일하다. 최종 Cancel 클립은 `3E4740469B11DDBBB78C25D4642B018E0ACED161B64095761D23558E3ABE655A`, Cancel Controller는 `6A72A3D5F7AA127E63E0C9A63B93E34873959DB15373C1DE23131145665BAF7F`다.
- Unity 반영과 확인에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsThrowCancel`, `CapturePlayerHandsThrowCancelReview`, `CapturePlayerHandsThrowCancelFinal`을 사용했다. 적용 지표, 직접 비교와 한 번만 확정한 최종 이미지는 `docs/validation/player_hands_throw_cancel_2026-08-29/`에 저장했다.
- 진행 HTML 통과 체크, 별도 아트 샘플, Harness·EditMode·PlayMode 테스트 스크립트, Windows Build와 Git 작업은 실행하지 않았다.

## Hands_Throw_Ready 머리 높이 반복 지점 조정

- 사용자 답변에 따라 기존 오른손 추적 기준을 유지하고, 원본 상승 구간에서 오른손이 Head 본 높이에 처음 도달하는 프레임을 새 Ready 종료 기준으로 삼았다. 전체 원본 프레임을 먼저 직접 확인한 뒤 원본 본 위치로 정확한 프레임을 판별했으며, 18프레임에서는 오른손이 Head 본보다 약 `0.073049m` 아래, 19프레임에서는 약 `0.005899m` 위였다. 따라서 첫 도달 지점은 `19프레임`, 시간 `0.6333333초`이고 기존 최고점 23프레임보다 앞이다.
- 원본 `mixamo.com` Take의 0~19프레임 곡선을 그대로 복사하고 19프레임 자세를 정확히 `0.5초` 유지하는 `Assets/_Project/Art/Player/Animations/Hands_Throw_Ready_MixamoHeadHeightHold.anim`을 생성했다. 전체 길이는 `1.1333333초`이며 끝에서 처음으로 반복된다. 원본에 없는 중간 키·보간·동작은 생성하지 않았다.
- `Hands_Throw_Ready.controller`의 `HandsThrowReady` 상태만 새 HeadHeightHold 클립으로 연결했다. 기존 `Hands_Throw_Ready_MixamoPeakHold.anim`, 원본 외부·Unity FBX, `Hands_Throw_Release.controller`, 씬과 다른 Animator는 수정하지 않았다.
- 1순위 직접 확인에서는 Unity Play Mode에서 원본 0~19프레임 기대 동작과 실제 Ready를 같은 12위상의 전신 정면·측면과 오른팔 확대 정면으로 나란히 비교했다. 두 동작은 단계별로 일치했고 오른손이 머리 옆 높이에 도달한 뒤 같은 자세를 유지했으며, 기존 최고점까지 올라가지 않았다. 팔·손목 꺾임, 모델 누락, 루트 이동과 반복 중 자세 파손은 보이지 않았다. 하단의 Release 기대·실제도 기존 전체 Take와 일치했다.
- 직접 확인 뒤 보조 검증에서 Ready는 `34프레임×2루프`를 재생했고 런타임 원본 자세 차이는 최대 약 `0.000000076m / 0°`였다. 원본 0~19프레임 접두 구간과 0.5초 정지 구간의 직접 곡선 차이는 모두 `0`, 루트 변위는 `0`, `Loop=True`, `ApplyRootMotion=False`였다. Release는 기존 `66프레임×2루프`, 최대 약 `0.000000104m / 0°`, 루트 변위 `0` 상태를 유지했다.
- 원본 외부·Unity FBX의 SHA-256은 모두 `AF4F841C549ABFD62D5FC0E349CC744BD6A0837E578E9078F515F8A55DDB7BF5`로 일치한다. 기존 PeakHold 클립은 `665F8012F19461B9EE693C5BDD3EBB2D3A1F8B85403DC5C73B90B8D7C9A13FB0`, Release Controller는 작업 전후 `91C9C0B731A3C96709D4AA71438F2DEE613F8A663968D0BF34DEA3956BC3650D`로 동일하다. 새 HeadHeightHold 클립은 `D66B0EFF4A6C70AC28DEF79FEA62AE6CB6DE958DC174C879798012B33CD49AE7`, 변경된 Ready Controller는 `F42F954E85EF070A0D41847D0BB5666029D4051F7EBCDEC81B93DECACB43DB6C`다.
- Unity 반영과 확인에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsThrowMixamo`, `CapturePlayerHandsThrowMixamoReview`, `CapturePlayerHandsThrowMixamoFinal`을 사용했다. 적용 지표, 직접 비교와 한 번만 확정한 최종 이미지는 `docs/validation/player_hands_throw_ready_head_height_2026-08-29/`에 저장했다.
- 요구사항 HTML, 테스트·빌드와 Git 작업은 실행하지 않았다.

## Hands_Throw_Ready 3초 호흡·무릎 굽힘 유지 구간

- 사용자 지정에 따라 머리 높이 도달 프레임인 19프레임까지는 원본 `mixamo.com` Take를 그대로 유지하고, 이후 유지 구간을 `0.5초`에서 `3초`로 변경했다. 전체 클립 길이는 `3.6333334초`이며 3초 유지가 끝나면 처음부터 반복한다.
- 유지 구간에는 1초에 한 번씩 총 3회의 호흡을 넣었다. `Hands_Throw_Ready_Breathing.asset`의 `Breathing` BlendShape를 Ready의 `char1`에만 연결했고 최대 가중치는 사용자 지정 `30%`다. 변형은 가슴 앞·좌우의 191개 정점에만 적용되며 측정된 최대 가슴 팽창은 약 `0.010008m`다.
- 같은 1초 주기에 무릎을 굽혔다 펴면서 몸이 사용자 지정 `0.03m`만큼 상하 이동하도록 했다. 측정된 최대 하강은 약 `0.0299997m`, 최소 무릎 굽힘 증가는 약 `6.480629°`, 양발의 최대 위치 편차는 약 `0.0003276m`다. 원본 0~19프레임, Release 전체 Take, 기존 PeakHold·HeadHeightHold 클립과 공용 `player.fbx`는 변경하지 않았다.
- 1순위 직접 확인에서는 Unity Play Mode의 같은 12위상 전신 정면·측면 및 상체 확대 비교로 Ready가 머리 높이에 도달한 뒤 가슴이 30% 범위에서 호흡하고, 무릎 굽힘과 함께 몸이 약 0.03m 내려갔다가 올라오는 동작을 확인했다. 발은 바닥에 유지됐고 관절 역꺾임, 메시 찢김·돌출, 루트 이동과 반복 경계의 새 자세 파손은 보이지 않았다. Release는 기존 전체 던지기 동작을 그대로 유지했다.
- 직접 확인 뒤 보조 검증에서 Ready는 `109프레임×2루프`, Release는 `66프레임×2루프`를 재생했다. Ready 원본 접두 구간 차이는 `0m / 0°`, Ready 런타임 원본 자세 차이는 최대 약 `0.000000246m / 0°`, Release는 최대 약 `0.000000104m / 0°`, 두 개체의 루트 변위는 `0m`였다. 호흡 최대 가중치 `30%`, 호흡 피크 3회, 몸 하강 약 `0.0299997m`도 저장된 지표와 일치했다.
- 원본 외부·Unity FBX의 SHA-256은 모두 `AF4F841C549ABFD62D5FC0E349CC744BD6A0837E578E9078F515F8A55DDB7BF5`, 공용 플레이어 FBX는 `E025F1CB33BE02D527312A25CCA2820F3F0155FF07BE1ABC7FFD54CC6074DA75`, 기존 HeadHeightHold 클립은 `D66B0EFF4A6C70AC28DEF79FEA62AE6CB6DE958DC174C879798012B33CD49AE7`, PeakHold 클립은 `665F8012F19461B9EE693C5BDD3EBB2D3A1F8B85403DC5C73B90B8D7C9A13FB0`, Release Controller는 `91C9C0B731A3C96709D4AA71438F2DEE613F8A663968D0BF34DEA3956BC3650D`로 적용 전후 동일하다. 최종 Ready 클립은 `B064818B60576385598964A8C1AF492C6B7A6F9D614356BB963B0391243B85B6`, Ready Controller는 `0B8485EF5EC16C168C39893052805F9C395A96710D182745D017FCA9A88D810D`다.
- Unity 반영과 확인에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsThrowMixamo`, `CapturePlayerHandsThrowMixamoReview`, `CapturePlayerHandsThrowMixamoFinal`을 사용했다. 적용 지표, 직접 비교와 한 번만 확정한 최종 이미지는 `docs/validation/player_hands_throw_ready_breathing_2026-08-29/`에 저장했다.

## 플레이어 손 애니메이션 요구사항 HTML 통과 상태

- `docs/PLAYER_ANIMATION_REQUIREMENTS.html`에서 현재 완료된 작업 결과에 대응하는 `Hands_Draw_Back`, `Hands_Stow_Back`, `Hands_Throw_Ready`, `Hands_Throw_Release`, `Hands_Throw_Cancel` 행에 `animation-complete`와 `data-completion="완료"`가 적용돼 있다.
- 이 기록 작업에서는 HTML 상태를 추가 변경하지 않았고, 작업 트리에 이미 반영된 통과 상태만 확인해 기록했다.
