# 2026-07-28 진행 상황

## 오른팔 총신 완전 결합 재교정 - 오른팔 고유 공통 축 링 회전

- 사용자 제공 `23-41-28.mp4`에서 이전 개별 피벗 방식이 오른팔 총신의
  연결 성분 일부만 따로 움직이고, 각 성분 중심을 공전점으로 사용해
  원반 밖까지 벌어지는 문제를 확인했습니다.
- 정상인 실제 왼팔은 개별 총신 부품마다 공전 피벗을 두지 않고, 선택된
  긴 총신 어셈블리 전체를 하나의 회전 본과 공통 축에 결합한다는 구조를
  확인했습니다. 왼팔 메시·본·클립은 변경하지 않았습니다.
- 오른팔의 형상을 왼팔에서 복사하지 않고, 오른팔 자체의 손 가중치와
  긴 총신 성분에서 총신 방향을 구했습니다. 오른팔 총구 링 중심을
  공통 축선의 중심으로 사용했습니다.
- 이전 오른팔 8개 개별 피벗·역회전 구조를 제거하고, 오른팔의 8개 긴
  총신 성분을 하나의 결합 메시로 만들어 공통 회전 본에 연결했습니다.
  총신 구성 전체가 함께 회전하므로 일부만 선택되어 남거나 부품별
  반경이 벌어지지 않습니다.
- 중간에 원통 영역을 넓혀 23개 성분을 선택한 시도는 원반 뒤 날개형
  하우징까지 회전시켜 즉시 폐기했습니다. 해당 캡처는
  `Logs/RevolutionMachineGunInvalidDiagnostics/Revolution_04_MachineGunAttack_VisualReview_overselected_right_housing_234128.png`로
  격리했습니다.
- 최종 정면·총구·사선 `0.00~0.20초` 연속 프레임과 `16-02-35.mp4`의
  실제 왼팔을 직접 대조했습니다. 오른팔의 긴 총신 묶음과 총구 캡은
  함께 원반 내부 반경으로 회전하며, 개별 조각이 밖으로 튀어나오지
  않습니다.
- 현재 직접 비교 이미지는
  `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_VisualReview.png`입니다.
- 일반 자동 검증·테스트·빌드·Unity 재시작·Git 작업은 수행하지
  않았습니다.

## 폐기된 오른팔 개별 공전 교정 - 부분 선택 및 반경 이탈

- 사용자 제공 `23-30-34.mp4`를 `16-02-35.mp4`의 개체 실제 왼팔과
  프레임별로 다시 대조했습니다. 이전 교정은 오른팔 회전축만 바로잡고
  8개 총신 성분을 하나의 강체 메시로 함께 돌려, 일부 총신이 회전각을
  따라 대각선으로 기울어지는 문제가 남아 있었습니다.
- 정상인 개체 실제 왼팔의 메시, 회전 본, 축, 자세와 애니메이션 경로는
  변경하지 않았습니다.
- 오른팔만 8개 총신 성분을 각각 별도 자식 피벗과 메시로 분리했습니다.
  부모 원반 회전으로 각 총신의 위치는 원반 둘레를 공전하고, 자식
  피벗에는 같은 각도의 역회전을 적용해 총신 길이 방향을 유지합니다.
- 당시 새 정면·총구·사선 프레임에서는 기울기만 사라졌다고 판단했지만,
  사용자 제공 `23-41-28.mp4`에서 선택된 연결 성분 일부만 개별
  공전하고 회전 반경이 원반 밖으로 벌어지는 문제가 확인됐습니다.
- 현재 직접 비교 이미지는
  `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_VisualReview.png`입니다.
  수정 전 이미지는
  `Logs/RevolutionMachineGunInvalidDiagnostics/Revolution_04_MachineGunAttack_VisualReview_before_right_barrel_counterrotation_20260728.png`로
  옮겨 보존했습니다.
- 일반 자동 검증·테스트·빌드·Unity 재시작·Git 작업은 수행하지
  않았습니다.

## 폐기된 오른팔 축 교정 - 강체 총신 묶음의 기울기 잔존

- 사용자 확인에 따라 이미 정상인 개체의 실제 왼팔 총신 선택, 결합 위치,
  회전축, 자세와 애니메이션은 변경하지 않았습니다.
- 오른팔이 왼팔 반사축으로 회전하면서 오른팔 원반의 실제 로컬 면과
  어긋나 기울어 보이던 구성을 제거했습니다.
- 오른팔 총신만 오른팔 원반의 실제 고정 면 법선을 회전축으로 사용하도록
  바꿨습니다. 총신은 원반에 꽂힌 위치를 유지하고, 길이 방향을 기울이지
  않은 채 원반 중심 주위를 링 회전합니다.
- 당시에는 수정 전·후 정면, 총구와 사선 연속 프레임을 대조해 축이
  교정됐다고 판단했지만, 사용자 제공 `23-30-34.mp4`에서 여러 총신이
  강체 묶음 회전각을 따라 기울어지는 문제가 남았음을 확인했습니다.
- 현재 직접 비교 이미지는
  `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_VisualReview.png`이며,
  수정 전 오른팔 반사축 이미지는
  `Logs/RevolutionMachineGunInvalidDiagnostics/Revolution_04_MachineGunAttack_VisualReview_RightMirroredLeftAxis.png`로
  옮겨 보존했습니다.

## 재교정 완료 - 16:02:35 영상의 개체 왼팔 기준 양팔 사격 모션 복제

- 사용자가 정정한 방향 기준에 따라 화면상 좌우가 아니라
  `Revolution_04` 개체 자체의 `LeftArm/LeftForeArm/LeftHand`를
  사격 모션 원본으로 사용했습니다.
- 이전 구현은 오른팔에서 검출한 총신 링 축을 왼팔로 반사하고 팔마다
  다른 조준 보정을 사용했으므로 사용자 지정 기준과 반대였습니다.
- 총신 링 면 법선과 원반 결합 위치를 실제 왼팔에서 먼저 구하고, 모델
  중심면으로 반사한 축을 오른팔 총신에 적용했습니다.
- 실제 왼팔의 상완 방향, 전완 방향과 총신 링 좌표계를 월드 공간에서
  모델 중심면으로 반사해 오른팔에 복제했습니다. 좌우 골격의 로컬축
  차이 때문에 같은 수치만 넣었을 때 오른팔이 위로 들리던 중간 결과는
  폐기했습니다.
- 새 정면·총구·사선 연속 프레임에서 양팔이 실제 왼팔 자세의 거울
  대칭을 유지하고, 총신은 원반에 결합된 채 회전축이 기울지 않는 링
  회전을 하는지 직접 확인했습니다.
- 새 직접 비교 이미지는
  `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_VisualReview.png`입니다.
  수정 전 리뷰 이미지는
  `Logs/RevolutionMachineGunInvalidDiagnostics/`로 옮겨 보존했습니다.
- DX12 Unity 게임 뷰를 외부 `gdigrab`으로 다시 녹화한 시도는 검은
  화면만 기록되어 비교 자료와 완료 근거에서 제외했습니다.
- 일반 자동 검증·테스트·빌드·Unity 재시작·Git 작업은 수행하지
  않았습니다.

## 폐기된 완료 판정 - 사용자 제공 16:02:35 기준 Revolution_04 공격 모션 복구

- `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_04`의 외형과
  공격 애니메이션을 사용자 제공 `16-02-35.mp4` 기준으로 복구했습니다.
- 로컬 작업 세션에서 영상 생성 직전까지 성공한 소스 패치 30개를 원래
  순서대로 복원해 당시의 좌우 총열 자식 메시, 고정 총구 링 축, 대칭축,
  발사 플래시와 5초 반복 공격을 되살렸습니다.
- 재적용 전에 기존 Animator를 비활성화하고 승인 원본 본 자세를 복구해
  조준 자세가 누적되던 문제를 제거했습니다. 영상과 직접 대조해 모델
  왼팔의 높이와 전방 투영도 기준 실루엣에 맞췄습니다.
- 완료 판단에는 자동 수치 판정이나 모션 판정 스크립트를 사용하지
  않았습니다. 기준 영상, 보존된 당시 캡처, 새 정면·총구·사선 캡처와
  실제 런타임 플레이어 카메라 프레임을 직접 나란히 확인했습니다.
- 붉은 시야 콘 뒤의 `Revolution_04`에서 화면 왼쪽 총열의 하향 전방
  조준과 화면 오른쪽 총열의 발사 높이, 양쪽 섬광 반복 및 총열 링 회전
  실루엣이 기준 영상과 같이 보이는 상태로 씬을 저장했습니다.
- 최종 직접 비교 기록과 자료는
  `docs/validation/revolution_machinegun_attack_2026-07-28/`의
  `Revolution_04_MachineGunAttack_DirectVisualReview.txt`,
  `Revolution_04_MachineGunAttack_VisualReview.png`,
  `Revolution_04_MachineGunAttack_DirectVisualReview.mp4`,
  `Revolution_04_MachineGunAttack_Front.mp4`,
  `Revolution_04_MachineGunAttack_RuntimeSceneReview.mp4`입니다.
- 실패한 색상 분해와 자세 조정 진단은
  `Logs/RevolutionMachineGunInvalidDiagnostics/`로 격리했습니다.
- `Revolution_04` 외 Revolution 슬롯과 플레이어·카메라·다른 씬 루트는
  변경하지 않았습니다. 일반 자동 검증·테스트·빌드·Unity 재시작·Git
  작업은 수행하지 않았습니다.

## 폐기 이력 - 사용자 지정 복구 전 레볼루션 04 최종 재교정

- `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_04`의
  최종 기관총 공격은 `4.8초` 루프, `0.2초` 간격 `24회` 사격,
  발사마다 `120°` 회전으로 구성했습니다. 좌우 각 팔의 실제 긴 총구는
  3개이며 루프당 8회전합니다. Root Motion은 사용하지 않습니다.
- 이전의 총구별 분리 메시·역회전 상쇄 구조를 폐기했습니다. 승인 원본의
  정점·삼각형·UV·서브메시를 그대로 둔 단일 파생 스킨 메시에서 좌우
  3총구 어셈블리 정점만 전용 회전 본에 강체 가중했습니다. 최종 선별은
  왼팔 10개 연결 성분·86정점, 오른팔 10개 연결 성분·80정점입니다.
- 회전축은 각 손 본의 고정 로컬 전방축을 사용합니다. 좌우 총구 3개는
  원반 기부에 꽂힌 상태를 유지하며 링 회전하고, 원반·손 하우징·팔 전체와
  짧고 넓은 주변 부품은 회전하지 않습니다.
- 양팔 상완은 전방 `0.68`, 몸 안쪽 `0.32` 비중으로 모았습니다. 각
  총구축도 전방 `0.88`, 몸 안쪽 `0.12` 비중으로 중앙을 향해 수렴시켜
  정면 적을 조준하는 자세를 분명하게 만들었습니다.
- 리벨리온의 기존 플래시 메시와 머티리얼을 재사용합니다. 축 투영으로
  총구 끝을 추정하던 방식을 폐기하고, 각 3총구 섹터에서 손 관절로부터
  가장 먼 실제 끝면 중심을 구해 위쪽 총구에 플래시를 고정했습니다.
- Unity 캡처는 자유 카메라 방향이 아닌 `Revolution_04` 슬롯 전방을
  기준으로 고정했으며, 실제 스킨 변형을 프레임마다 베이크해 기록했습니다.
  좌우 클로즈업에서 0.6초 한 바퀴의 모든 중간 자세를 직접 보며 세 총구,
  원반 기부, 비회전 하우징, 플래시 위치를 확인했습니다. 자동 수치 판정은
  완료 근거로 사용하지 않았습니다.
- 최종 직접 확인 기록과 자료는
  `docs/validation/revolution_machinegun_attack_2026-07-28/`의
  `Revolution_04_MachineGunAttack_DirectVisualReview.txt`,
  `Revolution_04_MachineGunAttack_VisualReview.png`,
  `Revolution_04_MachineGunAttack_Front.mp4`,
  `Revolution_04_MachineGunAttack_LeftCloseup.mp4`,
  `Revolution_04_MachineGunAttack_RightCloseup.mp4`입니다.
- 손 전체 회전, 전완→손 축, 개별 메시 대각선축, 전체 선택 진단,
  개별 총열 추출·역회전, 자유 카메라 캡처 등 실패 상태와 진단 이미지는
  `Logs/`로 격리했습니다.
- `InspectRevolutionMachineGunAttackMotion`, `Run-HarnessValidation.ps1`,
  `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`,
  `Build-WindowsDev.ps1`, Unity 재시작과 Git 작업은 실행하지 않았습니다.

## 폐기 이력 - 레볼루션 04 이전 기관총 공격 교정

아래 절은 최종 재교정 이전의 시행착오 기록입니다. 아래에서 `현재`,
`최종`, `완료`, `PASS`라고 표현된 구현과 수치는 모두 위 최종 재교정으로
대체됐으며 현재 Unity 상태를 설명하지 않습니다.

- `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_04`에
  `Revolution_04_MachineGun_Attack.controller`를 연결했습니다. 기본 상태
  `RevolutionMachineGunAttack`은 5초 동안 25회 사격하고 반복 재생하며
  Root Motion은 사용하지 않습니다.
- 현재 승인 모델은 양팔이 이미 기관총 형태이므로 기관총 전환 모션이나 새
  총구·무기 모델을 만들지 않았습니다. 최초 구현은 각 전완과 손 사이의
  피벗으로 `Hand` 스킨 전체를 회전시켜 팔과 원반형 하우징까지 비틀렸고,
  팔이 전방 적을 조준하지 않아 무효 처리했습니다.
- 교정 구현은 양쪽 팔·전완·손을 동일한 수평 전방 목표에 고정했습니다.
  원본 스킨 메시의 연결 성분에서 총열 관만 분리하고, 원본 바인드포즈와
  본 가중치로 전방 조준 자세의 좌표를 계산해 전용 회전 본
  `Revolution_Left_Barrel_Ring_Bone`,
  `Revolution_Right_Barrel_Ring_Bone`의 자식 메시로 만들었습니다. 왼쪽은
  12개 연결 성분·113정점, 오른쪽은 8개 연결 성분·72정점입니다.
- 사용자 제공 `15-41-55.mp4`에서 총구 묶음만 분리된 점은 맞지만
  손목–전완 연결선을 회전축으로 쓴 탓에 총열이 원반에 고정된 채 도는
  대신 축이 기울고 뒤틀려 보이는 문제를 확인했습니다. 해당 축 구현과
  기존 직접 검토 자료는 무효 처리했습니다.
- 두 번째 교정은 각 총구 끝의 원형 배치가 만드는 평면의 중심 법선을
  양팔에서 각각 추정해 회전 본의 고정축으로 사용했습니다. 사용자 제공
  `15-53-36.mp4`를 다시 확인한 결과 모델 오른팔은 정상이나 모델 왼팔의
  독립 추정축은 총열 길이 방향과 어긋나, 왼쪽 총구가 기울어진 채
  회전했습니다. 해당 왼팔 축과 직접 검토 자료는 무효 처리했습니다.
- 세 번째 교정은 정상인 모델 오른팔의 고정 법선축을 모델 좌우 대칭면으로
  반사해 왼팔에 사용했지만, 사용자 제공 `16-02-35.mp4`에서 왼팔 총열
  옆면이 계속 보이고 전방축과 어긋난 상태가 유지됐습니다. 오른팔의 축
  데이터를 복사·반사한 이 구현과 검토 자료도 무효 처리했습니다.
- 사용자 제공 `16-19-43.mp4`에서 양팔 총구 묶음이 여전히 회전 각도에
  따라 함께 기울어지는 것을 확인했습니다. 총열 전체를 한 개 회전 본에
  결합한 구조에서는 위치 이동과 총열 자체 회전을 분리할 수 없었던 것이
  원인입니다. 왼팔의 개별 총열 장축을 쓴 구현과 해당 검토 자료도 무효
  처리했습니다.
- `16-19-43` 이후 교정은 양쪽 모두 `FixedMuzzleRingPlaneNormal`을 원주 공전축으로
  사용합니다. 선택된 총열 연결 성분은 각 팔에서 5개 원주 위치 그룹으로
  나눴습니다. 공전 본이 발사마다 `72°` 회전할 때 각 그룹 자식은 같은
  각도를 반대로 회전합니다. 이 상쇄 구조로 총열 그룹의 위치만 링을 따라
  이동하고 각 총열의 길이 방향과 기울기는 고정됩니다. 팔·손목·원반형
  하우징은 회전하지 않습니다.
- 사용자 제공 `16-44-07.mp4`에서 이 방식이 총열이 아닌 넓은 쐐기형
  끝 부품까지 공전시키고, 반대로 일부 긴 총열은 원본 위치에 남기는 것을
  확인했습니다. 손 본 가중치와 끝 위치를 기준으로 선택한 8개·12개 연결
  성분을 강제로 5개 그룹으로 묶은 것이 원인이었습니다. 해당 선택 방식과
  직접 검토 자료는 무효 처리했습니다.
- 현재 교정은 본 가중치와 짧은 끝 부품을 선택 기준에서 제외합니다. 각
  손의 전방으로 길게 뻗은 연결 성분을 길이순으로 판별해 양팔에서 실제
  긴 총열 5개씩만 선택합니다. 각 총열은 하나의 독립 공전 그룹이며,
  짧고 넓은 끝 부품은 원반과 함께 고정됩니다. 양팔 자세는 전방 비중을
  `0.72→0.82`, 바깥 벌어짐 비중을 `0.28→0.18`로 조정해 이전보다
  몸 안쪽과 전방으로 모았습니다.
- 사용자 제공 `16-10-10.mp4`에서는 정상 코드를 유지한 오른팔까지
  기울어졌습니다. 재적용 함수가 이전 Animator 클립의 팔·전완·손 회전을
  그대로 둔 채 새 조준 회전을 다시 더해, 적용할 때마다 양팔 자세가
  누적된 것이 원인이었습니다. 현재는 재생성 전에 기존 Animator를
  비활성화하고 컨트롤러를 분리한 뒤, 승인 원본 모델과 이름·순서가 같은
  24개 본의 로컬 위치·회전·스케일을 원본 자세로 복원합니다. 그 상태에서
  총열 분리와 양팔 조준을 한 번만 계산합니다.
- 리벨리온 연속 사격과 같은 `0.2초` 사격 간격과 `0.08초` 발사 플래시
  타이밍을 사용합니다. 각 발마다 양쪽 총열이 화면상 오른쪽으로 `72°`
  회전하며 5초 동안 정확히 5회전한 뒤 루프 시작 자세로 복귀합니다.
- 양쪽 발사 플래시는 기존 리벨리온의
  `Rebellion_Forward_Burst_Flash.asset`과
  `Rebellion_Forward_Burst_Flash.mat`을 직접 참조합니다. 새 VFX 메시,
  머티리얼, 텍스처는 생성하지 않았습니다.
- 이전 `InspectRevolutionMachineGunAttackMotion`의 수치 `PASS`와 자동
  판정 보고서는 실제 화면에서 총열이 움직이지 않거나 피벗 밖으로 공전한
  상태를 걸러내지 못했으므로 무효 처리했습니다. 이번 확정에는 해당 명령을
  실행하지 않았고, 제공된 리벨리온 영상과 Unity에서 30fps로 샘플링한
  일반 재질 시퀀스를 직접 대조했습니다.
- 최종 결과를 `16-44-07.mp4`의 0.0~1.4초 확대 프레임 및
  `16-02-35.mp4`의 화면 왼쪽 총구와 직접 대조했습니다. 최종 Unity
  정면·사선 캡처에서 넓은 쐐기형 끝 부품의 공전은 사라졌고, 양팔의 긴
  총열만 원반 중심 주위를 이동합니다. 원본 위치에 남아 있는 긴 총열은
  보이지 않으며 총열의 길이 방향은 고정됩니다. 양팔은 이전보다 몸
  안쪽과 전방으로 모였고, 섬광은 고정 사격 위치에서 반복됩니다.
- 파생 기본 메시에서는 선택된 총열 삼각형만 제외하고, 동일 삼각형을
  `Revolution_04_Left_MachineGun_BarrelGroup_01~05.asset`과
  `Revolution_04_Right_MachineGun_BarrelGroup_01~05.asset`에 나눠
  담았습니다. 결합 기준으로 원본 정점·삼각형·UV, 서브메시와 승인
  머티리얼을 유지했습니다.
  `Revolution_01~03`, `Revolution_05~08`, 플레이어·카메라 및 다른 씬
  루트는 변경하지 않았습니다.
- 직접 시각 확인 기록은
  `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_DirectVisualReview.txt`,
  최종 비교 이미지는 같은 폴더의
  `Revolution_04_MachineGunAttack_VisualReview.png`, 30fps 최종 영상은
  `Revolution_04_MachineGunAttack_DirectVisualReview.mp4`입니다.
  손목–전완 축 버전, 왼팔 축 독립 추정 버전, 오른팔 축 복사·반사 버전,
  재적용 시 양팔 조준 자세가 누적된 버전, 양쪽 총구 전체 묶음이 함께
  기울던 `16-19-43` 기준 실패 버전, 넓은 끝 부품이 공전하고 긴 총열이
  남았던 `16-44-07` 기준 실패 버전, 동적 메시 경계 때문에 카메라 배율이
  튄 캡처, 잘못된 구현의 기존 캡처 및 자동 판정 보고서는 `Logs/`로
  이동해 최종 근거에서 제외했습니다.
- 전용 도구
  `Assets/_Project/Editor/Validation/RevolutionMachineGunAttackMotionTool.cs`
  와 `UnityEditorValidationBridge.cs`의 세 전용 명령 연결을 추가했습니다.
  `InspectRevolutionMachineGunAttackMotion`, `Run-HarnessValidation.ps1`,
  `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`,
  `Build-WindowsDev.ps1` 및 관련 조사·로그 작업, Unity 재시작, 다른
  애니메이션, 게임플레이 공격 판정과 Git 작업은 실행하지 않았습니다.

## 완료 - 2026-07-27 적대 개체 작업 Git 체크포인트

### 최종 Unity 상태

- `CargoRunMvp/Approved Resistance Enemy Placement`의 기능형 비교 슬롯 중
  `Resistance_05`는 사용자 제공 피격 FBX와 반복 Mixamo 피격 모션,
  `Resistance_06`은 사용자 제공 사망 FBX와 완전 전도 후 노란색·주황색
  자폭 및 반복 초기화, `Resistance_07`은 사용자 제공 무기 탈취 FBX와
  제자리 반복 Mixamo 모션으로 구성됐습니다.
- 레지스탕스 세 슬롯은 기존 승인 외형 메시와 `Resistance_01`의 승인
  머티리얼을 직접 참조합니다. 피격·사망·무기 탈취 원본 FBX의 메시,
  본, 내장 Mixamo 키는 임의로 변형하지 않았습니다.
- `CargoRunMvp/Approved Revolution Enemy Placement`에는
  `Revolution_01~08`이 높이 `2m`, X 간격 `2.444763m`, 레지스탕스 행과
  Z 간격 `7.376999m`로 배치돼 있습니다. 플레이어 시작점은 여덟 개체의
  정면을 바라보도록 저장됐습니다.
- 레볼루션 여덟 개체는 사용자 승인 아트 샘플의 동일 FBX, 직접 크롭
  텍스처 8개, 활성 머티리얼 8개와 전용 셰이더를 사용합니다. 메시·UV·본을
  새로 생성하거나 변형하지 않았고, 오른쪽 팔 기준 좌우 색 배열과
  몸통·어깨 이음부 재질 배치를 승인 샘플 그대로 유지했습니다.
- `Revolution_02`에만 2초 반복 대기 상태
  `RevolutionIdleBreathing`을 연결했습니다. 골반과 상체의 상하 이동은
  `0.03m`, 최대 발 위치 오차는 `0.000211m`, 바닥 최저점 변화는
  `0.000059m`, 루프 경계와 모델 루트 위치 오차는 `0`입니다.

### 기획서 대조

- `docs/GAME_DESIGN_SOURCE.txt`의 레지스탕스는 인간형 중형 2족 기계이며
  비품실에서 무기를 탈취하는 개체입니다. 이번 피격·사망·무기 탈취
  비교 슬롯은 해당 기능형 모션을 분리해 확인할 수 있도록 구성했습니다.
- 같은 기획서의 레볼루션은 높이 약 `200cm`, 원형 몸통과 양쪽 기계팔,
  두꺼운 로봇 다리를 가진 대형 2족 기계입니다. 현재 배치 높이와 승인
  외형은 이 기준을 유지합니다.
- 사용자 확정에 따른 레볼루션 8개 비교 슬롯 목록은 다음과 같습니다.
  1. `Revolution_01` — 정적 모델링
  2. `Revolution_02` — 일반 대기
  3. `Revolution_03` — 이동
  4. `Revolution_04` — 기관총 공격
  5. `Revolution_05` — 근접 공격
  6. `Revolution_06` — 피격
  7. `Revolution_07` — 방향 전환
  8. `Revolution_08` — 사망
- 방향 전환은 다른 개체보다 느린 레볼루션의 특성을 분리해 보여주는 전용
  모션으로 확정했습니다. 원본 기획서의 `360도 방향 전환 3초`를 기준으로
  이후 제작하며, 현재 모델은 팔이 이미 기관총 형태이므로 기관총 전환 모션은
  이 목록에 포함하지 않습니다.
- 이번 체크포인트에서 실제 적용된 것은 `Revolution_02`의 일반 대기뿐입니다.
  이동·기관총 공격·근접 공격·피격·방향 전환·사망 모션은 이번 커밋 범위에서
  구현하지 않았습니다.
- 레지스탕스 무기별 무장 모션도 이번 커밋 범위에서 구현하지 않았습니다.

### 적용 자산과 도구

- 레지스탕스 피격·사망·무기 탈취 FBX, Animator Controller, 사망 폭발
  프리팹·머티리얼, `ResistanceDeathExplosionLoop` 런타임과 각 전용
  적용·검사 도구를 포함합니다.
- 레볼루션 기본·공격형 FBX 교체 이력, 승인 외형 아트 샘플 원본과 내보내기,
  Unity 승인 외형 FBX·텍스처·머티리얼·셰이더, 대기 클립·컨트롤러 및
  배치·교체·외형·대기 전용 적용·검사 도구를 포함합니다.
- 열린 Unity 에디터의 전용 명령 연결을 위해
  `UnityEditorValidationBridge.cs`에 레지스탕스와 레볼루션 전용 명령을
  추가했습니다.
- 최종 씬 변경은 `Assets/_Project/Scenes/CargoRunMvp.unity`에 저장돼
  있습니다. 상세 작업 과정과 개별 SHA-256은
  `docs/PROGRESS_2026-07-27.md`에 기록돼 있습니다.

### 저장된 검사 근거

- 레지스탕스 피격:
  `docs/validation/resistance_hit_model_2026-07-27/`
- 레지스탕스 사망·자폭:
  `docs/validation/resistance_death_model_2026-07-27/`
- 레지스탕스 무기 탈취:
  `docs/validation/resistance_picking_model_2026-07-27/`
- 레볼루션 배치:
  `docs/validation/revolution_placement_2026-07-27/`
- 레볼루션 공격형 FBX 교체 이력:
  `docs/validation/revolution_attack_model_2026-07-27/`
- 레볼루션 승인 외형:
  `docs/validation/revolution_approved_appearance_2026-07-27/`
- 레볼루션 대기 모션:
  `docs/validation/revolution_idle_motion_2026-07-27/`
- 각 작업의 전용 적용·검사 결과는 PASS로 기록됐으며 최종 캡처가 허용된
  작업은 검사 통과 뒤 한 번만 캡처했습니다. 이번 Git 체크포인트 작업에서는
  Unity 씬이나 자산을 다시 적용하거나 별도 검사를 재실행하지 않았습니다.

### Git 포함·제외 범위

- `main` 브랜치의 기존 `origin/main` 체크포인트
  `d2404a8` 이후 현재 작업에 해당하는 레지스탕스·레볼루션 자산, 도구,
  씬, 아트 샘플, 검사 근거와 7월 27일·28일 진행 문서를 커밋 대상으로
  확정했습니다.
- Blender 자동 백업인
  `artSample/enemies/resistance/exports/resistance_current_model_material_sample.blend1`,
  `artSample/enemies/revolution/exports/revolution_replaced_model_reference_sample.blend1`
  과 Python 바이트코드 캐시
  `artSample/enemies/resistance/tools/__pycache__/`는 커밋하지 않습니다.
- Unity 재시작, Unity 씬·모델·애니메이션 추가 수정, 일반 검증·테스트·빌드,
  강제 푸시와 Git 이력 재작성은 수행하지 않습니다.
