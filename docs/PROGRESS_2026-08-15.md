# Bellerophon Progress — 2026-08-15

## 파르붐 공격 모션 입 전면 및 몸통 팽창 수정

`PARVUM_ATTACK_VISIBLE_FRONT_MOUTH_AND_BROAD_BODY_EXPANSION_APPLIED`

- 사용자 지정 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-08-15 00-02-33.mp4`를 프레임 단위로 확인했습니다.
- 기존 구현은 윗입 `Bone_002`, 아랫입 `Bone_017·018`의 일부 직접 가중치만 사용하고, 아랫입 앞부분이 연결된 `Bone_016` 이하를 제외해 화면에 보이는 입 앞부분이 고정되었습니다.
- 본 계층과 메시 연결 성분을 다시 식별해 윗입 전면 표면은 `Bone_006→002`, 아랫입 전면 표면은 `Bone_018→011` 전체 체인으로 확장했습니다.
- 초기 교정에서는 치아 전용 가지 `Bone_009·010·020·022·024·026`을 감쇠·제외했으나, 아래 후속 교정에서 사용자 지정 입 전면 범위에 맞춰 상악과 동일 변위로 움직이도록 변경했습니다.
- 최종 영향 범위는 윗입 `1,321개`, 아랫입 `954개` 정점이며, 바깥 윗입 `1,157개`와 바깥 아랫입 `809개` 정점이 공격 닫힘에 포함됩니다.
- 충격 시 바깥 윗입은 `0.123761m` 아래로, 바깥 아랫입은 `0.187446m` 위로 움직입니다. 입 개구 폭은 `0.574855m`에서 `0.01142m`로 닫히며 교차하지 않고 닫힘률은 `98.01344%`입니다.
- `Parvum_Model`의 위치·회전 애니메이션 곡선을 완전히 제거했습니다. 모델의 X·Y·Z 위치 이동량과 회전 이동량은 모두 `0`입니다.
- 강한 물기 표현은 `Attack_Full_Body_Impact_Expansion`으로 전환했습니다. 입 묶음을 제외한 바닥 고정 몸통 전체 `1,135개` 정점이 최대 전방 `0.401744m`, 좌우 `0.138081m` 팽창했다가 복귀합니다.
- 공격 주기 `3초`와 기존 입 안쪽 `58개` 정점의 움직임은 유지했습니다.
- 최종 비교 캡처는 관련 Inspect 통과 후 지정 영상에 가까운 측면 3/4 시점으로 한 번만 생성했습니다. 상단은 몸통 전체, 하단은 사용자 지정 입 전면 확대 비교입니다.

## 적용 및 수정 파일

- `Assets/_Project/Editor/Validation/ParvumAttackMotionTool.cs`
- `Assets/_Project/Art/Enemies/Parvum/Models/parvum_attack_bite_mesh.asset`
- `Assets/_Project/Art/Enemies/Parvum/Animations/Parvum_Attack_NewModel.anim`
- `Assets/_Project/Art/Enemies/Parvum/Animations/Controllers/Parvum_Attack_NewModel_Controller.controller`
- `Assets/_Project/Scenes/CargoRunMvp.unity`
- `docs/validation/parvum_attack_motion_2026-08-15/`
- `docs/PROGRESS_2026-08-15.md`

## Unity 반영 및 전용 확인 명령

- `Refresh-UnityProject.ps1`
- `InspectParvumAttackOuterLipRegion`
- `ApplyParvumAttackMotion`
- `InspectParvumAttackMotion`
- `CaptureParvumAttackMotionComparison`

## 검증 결과

- `ApplyParvumAttackMotion`: PASS
- `InspectParvumAttackMotion`: PASS
- 공격 주기: `3초`
- 넓게 벌린 비율: `90.59254%`
- 입 닫힘률: `98.01344%`
- 몸통 전체 메시 팽창: 전방 `0.401744m`, 좌우 `0.138081m`
- `Parvum_Model` Transform 곡선: 없음
- 공격 슬롯 루트 Transform 곡선: 없음
- 바닥 높이 변화: `0m`
- Rigidbody·Collider·Motion Path 드라이버: 유지
- 다른 Parvum 슬롯 변경: 없음
- 다른 씬 루트 변경: 없음

## 최종 확인 산출물

- `docs/validation/parvum_attack_motion_2026-08-15/Parvum_Attack_Motion_Report.txt`
- `docs/validation/parvum_attack_motion_2026-08-15/Parvum_Attack_Outer_Lip_Identification.txt`
- `docs/validation/parvum_attack_motion_2026-08-15/Parvum_Attack_Motion_Final_Comparison.png`
- `docs/validation/parvum_attack_motion_2026-08-15/input_video_review/Parvum_Input_Video_000233_ContactSheet.png`
- `docs/validation/parvum_attack_motion_2026-08-15/input_video_review/Parvum_Input_Video_000233_MouthTimeline.png`

## 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업: 실행하지 않음
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업: 실행하지 않음
- Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령: 실행하지 않음
- 원본 영상·원본 GLB·텍스처·머티리얼 수정: 실행하지 않음
- 전투 판정·AI·체력·플레이어 피격 로직 수정: 실행하지 않음
- 다른 Parvum 배치·상태 슬롯 및 작업 범위 밖 씬 루트 수정: 실행하지 않음
- Git 작업: 실행하지 않음
- 하위 에이전트 사용: 실행하지 않음

## 파르붐 피격 몸통·고개 동시 타이밍 교정

`PARVUM_HIT_BODY_HEAD_SYNCHRONIZED`

- 사용자 확인에서 몸통 찌그러짐이 먼저 발생하고 고개가 뒤늦게 회전하는 순차 동작으로 판정돼 기존 완료 상태를 교정했습니다.
- 원인은 몸통 곡선이 `0초`부터 시작해 `0.24초`에 최고점에 도달하는 반면, 고개 곡선은 `0.30초`까지 정지하고 `0.68초`에 최고점에 도달하도록 분리돼 있던 것입니다.
- 고개 곡선을 몸통 곡선과 동일하게 변경했습니다. 두 동작은 `0초`에 함께 시작하고, `0.24초`에 함께 최고점에 도달하며, `0.38초`까지 함께 유지된 뒤 `0.78초`에 함께 복구됩니다.
- `0·0.12·0.24·0.38·0.62·0.78·3초`에서 몸통과 고개 곡선 값을 대조하는 동기화 검사를 추가했습니다.
- 전체 모션 길이 `3초`, 고개 반동 `1회`, 정면 기준 왼쪽 몸통 찌그러짐, 개체 기준 왼쪽 고개 회전 조건은 유지했습니다.
- 최종 비교 이미지에서 `0.12초`부터 두 동작이 함께 진행되고 `0.24초`에 동시에 최고점에 도달하며, `0.78초`에 함께 복구되는 것을 직접 확인했습니다.
- 메시 찢김·바닥 들림·루트 Transform 곡선은 없고, 기존 피격 애셋 미사용, 물리 설정·다른 Parvum 슬롯·다른 씬 루트 무변경 상태를 유지했습니다.

### 최종 반영 및 확인

- `ApplyParvumHitMotion`: PASS
- `InspectParvumHitMotion`: PASS
- `CaptureParvumHitMotionComparison`: PASS
- 보고서 `BodyHeadTiming`: `SimultaneousStartPeakHoldAndRecovery`

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업: 실행하지 않음
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업: 실행하지 않음
- Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령: 실행하지 않음
- 기존 `Parvum_Hit.anim`, `Parvum_Hit_Controller.controller` 사용 또는 수정: 실행하지 않음
- 전투 판정·AI·체력·플레이어 피격 로직 및 다른 슬롯 수정: 실행하지 않음
- Git 작업: 실행하지 않음
- 하위 에이전트 사용: 실행하지 않음

## 파르붐 공격 모션 전면 입·치아 및 몸통 전체 팽창 후속 교정

`PARVUM_ATTACK_IMAGE2_FRONT_MOUTH_AND_FULL_BODY_EXPANSION_APPLIED`

- 사용자 첨부 `[Image #1]`은 몸통과 가까운 입뿌리, `[Image #2]`는 실제 공격 대상과 맞닿는 전면 윗입·아랫입·치아 부위로 구분했습니다.
- `[Image #2]`의 윗니 가지는 `Bone_009·010·020·022·024·026`이며, 로컬 Y `0.81~1.19m`, Z `0.99~1.37m`에 집중된 것을 전용 식별 보고서로 확인했습니다.
- `[Image #2]`의 아랫입 앞단은 `Bone_011~016`이며, 가중 중심이 로컬 Y `0.63~0.65m`, Z `1.17~1.24m`인 것을 확인했습니다.
- 윗입 앞단과 여섯 윗니 가지는 동일 상악 변위로, 아랫입 앞단과 아랫니 부위는 동일 하악 변위로 묶었습니다. 전용 Inspect에서 각 묶음 내부의 개방·물기 변위 편차가 허용 오차 이하인지 확인했습니다.
- 기존 전방 정점 임계값을 제거하고, 입 묶음을 제외한 몸통 전체를 뒤쪽과 바닥에 고정해 전후 `15%`, 좌우 `11%`, 상하 `8%` 팽창하도록 변경했습니다.
- 최종 `ApplyParvumAttackMotion`과 별도 `InspectParvumAttackMotion`이 모두 통과했고, 최종 비교 캡처는 한 번만 갱신했습니다.

## 사용자 이미지 기준 재수정 — 실제 최전방 입 직접 맞물림

`PARVUM_ATTACK_IMAGE2_DIRECT_APERTURE_AND_STRONG_FULL_BODY_EXPANSION_APPLIED`

- 직전 구현의 완료 판정을 철회하고 다시 수정했습니다. 직전 보고서의 `Image2FrontUpperBiteDown`·`Image2FrontLowerBiteUp`은 실제 최전방 입 측정값이 아니라 바깥입 평균값을 중복 표기하고 있었습니다.
- `[Image #2]`에 해당하는 로컬 Z `1.12m` 이상 최전방 윗입·윗니와 아랫입·아랫니 정점군을 별도로 지정했습니다. 이 군은 전체 입 평균 비율이 아니라 자체 상·하 간격으로 개방·닫힘 변위를 계산합니다.
- 실제 최전방 입 간격은 휴식 `0.390430m`, 최대 개방 `0.718390m`, 충돌 `0.009762m`이며, 최대 개방 상태 대비 닫힘률은 `98.64115%`입니다.
- 충돌 시 실제 최전방 윗입·윗니 묶음은 `0.182720m` 아래로, 아랫입·아랫니 묶음은 `0.197948m` 위로 이동합니다. 두 묶음은 각각 동일 변위를 사용해 치아나 입 앞단이 분리되지 않습니다.
- 몸통 전체 충격 팽창 계수는 전후 `25%`, 좌우 `19%`, 상하 `14%`로 강화했습니다. 입 묶음을 제외한 접지 몸통 `1,135개` 정점에서 실제 최대 전방 `0.669573m`, 좌우 `0.238503m` 팽창이 측정됐습니다.
- 전체 입은 최대 개방 `88.79951%`, 충돌 시 닫힘 `98.38386%`이며, 최종 개구 폭 `0.009203m`로 교차하지 않습니다.
- `Parvum_Model`의 위치·회전 이동량과 바닥 높이 변화는 모두 `0m`입니다. 몸통의 강한 전진감은 Transform 이동이 아니라 메시 팽창으로만 생성됩니다.
- 재수정 후 `ApplyParvumAttackMotion`과 별도 `InspectParvumAttackMotion`이 통과했으며, Inspect 통과 뒤 최종 비교 캡처를 새 상태로 한 번 생성해 직접 확인했습니다.
- 하네스 검증 및 관련 작업, EditMode·PlayMode 테스트, 빌드, Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령, Git 작업, 하위 에이전트 사용은 실행하지 않았습니다.

## 사용자 재지적 반영 — 주둥이 외피 실루엣 직접 변형

`PARVUM_ATTACK_EXTERIOR_SNOUT_TAPER_BITE_ATTEMPT_REJECTED`

- 수치상 입 간격만 통과하고 실제 주둥이 외피 변화가 약했던 직전 완료 판정을 철회했습니다.
- 기존 중앙 치아 주변 범위에서 벗어나 `Bone_002~006`의 넓은 윗주둥이 외피와 `Bone_011~018`의 아랫주둥이·바깥 턱 외피를 공격 변형군에 포함했습니다.
- 넓은 외피를 치아와 함께 평행 이동시키던 중간 구현도 폐기했습니다. 치아는 강체로 유지하고 외피는 주둥이 뿌리 `51%`에서 실제 앞단 `118%`까지 이동량이 증가하는 전방 테이퍼 메시 변형으로 분리했습니다.
- 이 구조로 눈과 주둥이 뿌리는 덜 움직이고, 충돌 시 윗주둥이 앞단이 아래로 꺾이며 아랫주둥이 앞단과 바깥 턱이 위로 치고 올라오는 상대 실루엣 변화가 생깁니다.
- 최종 외피 영향 범위는 바깥 윗주둥이 `1,324개`, 바깥 아랫주둥이·턱 `847개` 정점입니다. 충돌 시 평균 이동량은 윗외피 하강 `0.141270m`, 아랫외피 상승 `0.180843m`입니다.
- 전체 공격 입은 최대 개방 `89.36651%`, 충돌 닫힘 `98.44774%`이고 최종 개구 폭은 양수로 유지되어 교차하지 않습니다.
- 캡처 중 SkinnedMeshRenderer가 이전 컬링 경계를 사용해 모델 일부만 렌더링하던 문제를 수정했습니다. 캡처 동안에만 독립 카메라·화면 밖 갱신·확장 컬링 경계를 적용하고 원래 렌더러 상태를 복구합니다.
- 수정 후 `ApplyParvumAttackMotion`과 별도 `InspectParvumAttackMotion`의 내부 수치 판정은 통과했고 다섯 시점 캡처도 정상 렌더링됐지만, 사용자 확인에서 주둥이 외피의 실제 변화가 요구 수준에 미달한 것으로 판정됐습니다. 이 시도와 캡처는 승인 결과나 완료 근거로 사용하지 않습니다.
- 하네스 검증 및 관련 작업, EditMode·PlayMode 테스트, 빌드, Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령, Git 작업, 하위 에이전트 사용은 실행하지 않았습니다.

## 현재 최종 상태 — 파르붐 공격 주둥이 모션 실패

`PARVUM_ATTACK_SNOUT_MOTION_FAILED_USER_REVIEW`

- 사용자 최종 판정에 따라 방금까지 진행한 파르붐 공격 주둥이 외피 수정은 `실패`입니다.
- 내부 `ApplyParvumAttackMotion`·`InspectParvumAttackMotion`의 수치 통과는 시각적 성공이나 작업 완료를 의미하지 않습니다. 관련 보고서의 `Result=PASS`와 비교 캡처는 도구 실행 기록일 뿐 사용자 승인 산출물이 아닙니다.
- 실제 문제는 사용자가 요구한 주둥이 외피 변화가 화면에서 충분히 나타나지 않았다는 점입니다. 치아·입 안쪽 수치 또는 정점 평균 이동량으로 이 문제를 대체 검증할 수 없습니다.
- 현재 생성된 공격 메시·클립·컨트롤러·씬 연결과 `ParvumAttackMotionTool.cs`는 실패한 최신 시도 상태를 보존한 작업 기록이며, 파르붐 공격 모션의 완료 상태로 취급하지 않습니다.
- 파르붐 모델 교체, 대기 모션, 이동 모션의 기존 작업 기록과 별개로 공격 모션만 미완료·실패 상태입니다.
- 이번 문서 기록 및 Git 커밋·푸시 작업에서는 공격 모션을 추가 수정하거나 Unity에 다시 반영하지 않습니다.

## 후속 재구현 완료 — 전체 입 베어물기와 일체형 몸통 팽창

`PARVUM_ATTACK_WHOLE_MOUTH_BITE_AND_COHERENT_BODY_SCALE_APPLIED`

- 위 실패 상태를 사용자 후속 지시에 따라 재구현했습니다. 이번 항목이 이 문서의 파르붐 공격 모션 최신 상태이며, 위 실패 기록은 폐기된 시도의 이력으로만 유지합니다.
- 메시의 연결 성분 전체를 턱으로 승격하던 방식을 폐기했습니다. 소스 메시의 분리된 몸통 조각이 입 후보와 같은 연결 성분으로 묶여 충돌 프레임에서 찢어지던 것이 원인이었습니다.
- 윗입과 아랫입을 각각 넓은 기하 범위로 지정하고 뒤·옆 접합부에만 감쇠를 남겼습니다. 윗입 `1,227개`, 아랫입 `822개` 정점이 공통 평행 이동과 턱 피벗 회전을 함께 사용하므로 입의 특정 일부만 움직이지 않습니다.
- 실제 최전방 입은 휴식 `0.392696m`, 최대 개방 `0.822530m`, 충돌 `0.011579m`이며 닫힘률은 `98.59230%`입니다. 최전방 윗입은 `0.140421m` 내려오고 아랫입은 `0.240697m` 올라갑니다.
- 전체 입은 휴식 `0.325628m`에서 `0.600945m`까지 벌어져 개방률 `84.54953%`를 만들고, 충돌 시 `0.003691m`까지 비관통 폐합되어 닫힘률 `99.38586%`를 만듭니다.
- 정점 기반 몸통 팽창 BlendShape는 제거했습니다. 스킨 가중치별로 팽창 델타 방향이 달라져 메시가 갈라지던 원인이었기 때문입니다.
- 몸통 팽창은 접지 원점의 `Parvum_Model` 전체 스케일로 교체했습니다. 충돌 시 X `1.16배`, Y `1.08배`, Z `1.20배`로 일체형 팽창하며 전체 `3,215개` 정점에 적용됩니다. 측정된 전방 확장은 `0.604784m`, 좌우 확장은 `0.203538m`입니다.
- 물리 루트는 이동·회전·스케일하지 않고, 시각 모델만 로컬 +Z로 `0.58m` 돌진했다가 복귀합니다. 측정된 전방 진행은 `1.572960m`, 직전 대비 충돌 돌진은 `1.464111m`입니다.
- 별도 `InspectParvumAttackMotion`에서 바닥 높이 변화 `0m`, 모델 위치 Y·X 이동 `0m`, 모델 회전 이동 `0도`, Rigidbody·Collider·Motion Path 보존, 다른 Parvum 슬롯과 다른 씬 루트 미변경을 확인했습니다.
- 최종 다섯 프레임 비교 캡처는 각 프레임의 현재 입 위치와 스케일을 추적하도록 확대 카메라를 보정해 다시 생성했습니다. 휴식 → 최대 개방 → 전방 압박 → 팽창 폐합 → 원위치 복귀가 상단 전체 실루엣과 하단 입 확대에서 확인되며, 충돌 프레임의 메시 찢김과 바닥 침범은 없습니다.

### 최종 반영 및 확인

- `ApplyParvumAttackMotion`: PASS
- `InspectParvumAttackMotion`: PASS
- `CaptureParvumAttackMotionComparison`: PASS
- 최종 보고서: `docs/validation/parvum_attack_motion_2026-08-15/Parvum_Attack_Motion_Report.txt`
- 최종 비교 이미지: `docs/validation/parvum_attack_motion_2026-08-15/Parvum_Attack_Motion_Final_Comparison.png`

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업: 실행하지 않음
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업: 실행하지 않음
- Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령: 실행하지 않음
- 원본 영상·원본 GLB·텍스처·머티리얼 수정: 실행하지 않음
- 전투 판정·AI·체력·플레이어 피격 로직 수정: 실행하지 않음
- 다른 Parvum 배치·상태 슬롯 및 작업 범위 밖 씬 루트 수정: 실행하지 않음
- Git 작업: 실행하지 않음
- 하위 에이전트 사용: 실행하지 않음

## Unity 콘솔 비활성 Animator 샘플링 오류 수정

`RESISTANCE_DEATH_INACTIVE_ANIMATOR_CONSOLE_ERRORS_FIXED`

- Unity 콘솔의 `Game object with animator is inactive`와 `Can't call Animator.Update on inactive object` 오류를 확인했습니다.
- 두 오류는 `ResistanceDeathExplosionLoop.OnDisable()`이 `ResetSequence()`를 호출한 뒤, 이미 비활성 계층에 속한 Animator에 `Animator.Play`와 `Animator.Update(0f)`를 실행하면서 발생했습니다.
- `SampleAnimator()`가 Animator와 Controller 존재 여부뿐 아니라 `animator.gameObject.activeInHierarchy`도 확인하도록 수정했습니다. 비활성화 중에는 Animator 샘플링만 건너뛰며 렌더러·폭발 파티클·라이트 초기화는 기존대로 유지됩니다.
- 다시 활성화될 때는 기존 `OnEnable()`의 `ResetSequence()`가 활성 Animator를 정상적으로 처음 상태에 샘플링하므로 재사용 동작은 변경하지 않았습니다.
- `Refresh-UnityProject.ps1` 실행 후 `Bellerophon.Runtime.dll`을 포함한 Unity 스크립트 컴파일과 도메인 재로드가 성공했습니다. 해당 전체 로그 구간에서 위 두 오류와 새 C# 컴파일 오류가 발생하지 않았습니다.

### 수정 파일

- `Assets/_Project/Runtime/Enemies/Resistance/ResistanceDeathExplosionLoop.cs`
- `docs/PROGRESS_2026-08-15.md`

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업: 실행하지 않음
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업: 실행하지 않음
- Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령: 실행하지 않음
- 오류와 무관한 런타임·에디터·아트·씬 수정: 실행하지 않음
- Git 작업: 실행하지 않음
- 하위 에이전트 사용: 실행하지 않음

## 파르붐 신규 왼쪽 찌그러짐 피격 모션 구현

`PARVUM_NEW_LEFT_CRUSH_HIT_MOTION_APPLIED`

- 기존 파르붐 피격 애니메이션을 사용하지 않고 `Parvum_04_Hit/Parvum_Model` 전용 메시·클립·컨트롤러를 새로 생성했습니다.
- 전체 길이는 `3초`이며, 정면 기준 왼쪽 몸통이 먼저 안쪽으로 찌그러진 뒤 그 반동으로 머리 전체가 파르붐 개체 기준 왼쪽으로 `1회` 흔들리고 원래 자세로 복구됩니다.
- 몸통 찌그러짐은 로컬 `+X`를 정면 화면 왼쪽으로 대응해 최대 `0.38m` 안쪽 변형을 적용했습니다. 총 `1,426개` 정점이 영향을 받고 반대편 최대 이동은 `0m`입니다.
- 머리 반동은 로컬 `-X`를 파르붐 개체 기준 왼쪽으로 대응했으며, 총 `2,162개` 정점이 함께 움직입니다. 최대 왼쪽 이동은 `0.191894m`, 흔들림 피크는 `1회`입니다.
- 주요 시점은 휴식 `0초`, 찌그러짐 최고점 `0.24초`, 유지 `0.38초`, 머리 왼쪽 반동 최고점 `0.68초`, 몸통 복구 `0.78초`, 머리 복구 `1.18초`, 루프 끝 `3초`입니다.
- 루트와 모델 Transform 이동 곡선 없이 두 신규 BlendShape만 사용했습니다. 측정된 바닥 높이 변화는 `0m`입니다.
- 원본 `parvum.glb`의 SHA-256은 `E27840896F1DFA15BEE6F45F2BA943D28375A485E141907283CF79446B5640AB`로 유지했습니다.
- Rigidbody·Collider·`ParvumPhysicsMotionDriver` 및 Motion Path 설정이 보존됐고, 다른 Parvum 슬롯과 다른 씬 루트에는 변경이 없습니다.
- 최종 비교 이미지에서 정면 화면 왼쪽의 넓은 몸통 압축, 복원 뒤 개체 기준 왼쪽 머리 반동, 메시 무찢김, 접지 유지, 원자세 복귀를 직접 확인했습니다.

### 생성 및 수정 파일

- `Assets/_Project/Editor/Validation/ParvumHitMotionTool.cs`
- `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
- `Assets/_Project/Art/Enemies/Parvum/Models/parvum_hit_left_crush_mesh.asset`
- `Assets/_Project/Art/Enemies/Parvum/Animations/Parvum_Hit_LeftCrush_NewModel.anim`
- `Assets/_Project/Art/Enemies/Parvum/Animations/Controllers/Parvum_Hit_LeftCrush_NewModel_Controller.controller`
- `Assets/_Project/Scenes/CargoRunMvp.unity`
- `docs/validation/parvum_hit_motion_2026-08-15/Parvum_Hit_Motion_Report.txt`
- `docs/validation/parvum_hit_motion_2026-08-15/Parvum_Hit_Motion_Final_Comparison.png`
- `docs/PROGRESS_2026-08-15.md`

### 최종 반영 및 확인

- `ApplyParvumHitMotion`: PASS
- `InspectParvumHitMotion`: PASS
- `CaptureParvumHitMotionComparison`: PASS
- 기존 `Parvum_Hit.anim` 및 `Parvum_Hit_Controller.controller` 할당·사용: 없음

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업: 실행하지 않음
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업: 실행하지 않음
- Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령: 실행하지 않음
- 기존 `Parvum_Hit.anim`, `Parvum_Hit_Controller.controller` 사용 또는 수정: 실행하지 않음
- 원본 GLB·텍스처·머티리얼 수정: 실행하지 않음
- 전투 판정·AI·체력·플레이어 피격 로직 수정: 실행하지 않음
- 다른 Parvum 배치·상태 슬롯 및 작업 범위 밖 씬 루트 수정: 실행하지 않음
- Git 작업: 실행하지 않음
- 하위 에이전트 사용: 실행하지 않음

## 파르붐 피격 충격·감쇠 반동 교정

`PARVUM_HIT_IMPACT_AND_DAMPED_REACTION_APPLIED`

- 사용자 확인에서 기존 동작이 몸통과 고개를 단순 이동시키는 것처럼 단조롭게 보인다는 판정을 반영했습니다.
- 원인은 몸통과 고개가 같은 최고값을 일정 시간 유지한 뒤 같은 비율로 풀렸고, 고개 변형도 수평 회전 한 축만 사용한 것이었습니다.
- 몸통은 `0.18초` 주 충격 최고점 뒤 `0.30초`에 빠르게 반발하고, `0.40초`에 주 충격보다 작은 `34%` 재압축 반동을 만든 뒤 `0.52초`의 `9%` 잔류 진동을 거쳐 `0.68초`에 복구합니다.
- 고개는 몸통과 함께 `0.08초`부터 움직여 `0.18초`에 개체 기준 왼쪽으로 한 번 강하게 꺾입니다. 이후 두 번째 방향 전환 없이 `0.30초`의 `58%`, `0.42초`의 `24%`를 거쳐 계속 감쇠합니다.
- 고개 변형을 단순 수평 이동처럼 보이지 않도록 피치 `4도`, 요 `-18도`, 롤 `6도`를 결합하고 맞은 쪽 머리 표면에 최대 `0.055m`의 추가 국소 압축을 적용했습니다.
- 최대 고개 왼쪽 이동은 `0.329281m`이며 머리 전체 `2,162개` 정점이 영향을 받습니다. 고개 흔들림 피크는 `1회`입니다.
- 몸통 왼쪽 변형은 최대 `0.38m`, 영향 정점 `1,426개`, 반대편 최대 이동 `0m`를 유지합니다.
- 최종 비교 이미지에서 휴식 → 동시 충격 시작 → 기울기·국소 눌림을 포함한 충격 최고점 → 작은 몸통 재압축과 고개 감쇠 → 복구를 직접 확인했습니다.
- 전체 모션 길이 `3초`, 바닥 높이 변화 `0m`, 루트·모델 Transform 곡선 없음, 물리 설정·다른 Parvum 슬롯·다른 씬 루트 무변경 상태를 유지했습니다.

### 최종 반영 및 확인

- `ApplyParvumHitMotion`: PASS
- `InspectParvumHitMotion`: PASS
- `CaptureParvumHitMotionComparison`: PASS
- 보고서 `BodyReaction`: `MainCrushThenOneDampedAftershock`
- 보고서 `HeadReaction`: `SingleImpactSnapWithPitchYawRollAndLocalCompression`

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업: 실행하지 않음
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업: 실행하지 않음
- Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령: 실행하지 않음
- 기존 `Parvum_Hit.anim`, `Parvum_Hit_Controller.controller` 사용 또는 수정: 실행하지 않음
- 전투 판정·AI·체력·플레이어 피격 로직 및 다른 슬롯 수정: 실행하지 않음
- Git 작업: 실행하지 않음
- 하위 에이전트 사용: 실행하지 않음

## 파르붐 피격 고개 회전 반경 대폭 확대

`PARVUM_HIT_HEAD_ROTATION_RADIUS_VISIBLY_EXPANDED`

- 사용자 지시에 따라 미세 조정이 아니라 화면에서 즉시 구분되도록 고개 피벗 회전을 크게 확대했습니다.
- 고개 회전은 피치 `4도 → 7도`, 요 `-18도 → -32도`, 롤 `6도 → 12도`로 변경했습니다.
- 측정된 개체 기준 왼쪽 최대 고개 이동은 `0.329281m → 0.543704m`로 약 `65%` 증가했습니다.
- 최대 왼쪽 이동이 `0.45m` 미만이면 반영을 실패시키는 최소 확대 기준을 추가해 미세한 결과로 되돌아가지 않도록 했습니다.
- 정면과 3/4 비교 이미지에서 입·머리 중심과 상단 실루엣이 큰 호를 그리며 옆으로 꺾이는 것을 직접 확인했습니다.
- 머리 전체 `2,162개` 정점의 피벗 회전 구조와 국소 압축을 유지했으며, 단순 Transform 평행 이동은 추가하지 않았습니다.
- 몸통 감쇠 반동, 전체 길이 `3초`, 고개 흔들림 `1회`, 바닥 높이 변화 `0m`, 물리 설정·다른 슬롯·다른 씬 루트 무변경 조건을 유지했습니다.

### 최종 반영 및 확인

- `ApplyParvumHitMotion`: PASS
- `InspectParvumHitMotion`: PASS
- `CaptureParvumHitMotionComparison`: PASS
- 보고서 `HeadImpactEulerDegrees`: `(7,-32,12)`
- 보고서 `HeadRotationRadius`: `DeliberatelyExpandedForClearSilhouette`

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업: 실행하지 않음
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업: 실행하지 않음
- Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령: 실행하지 않음
- 기존 `Parvum_Hit.anim`, `Parvum_Hit_Controller.controller` 사용 또는 수정: 실행하지 않음
- 전투 판정·AI·체력·플레이어 피격 로직 및 다른 슬롯 수정: 실행하지 않음
- Git 작업: 실행하지 않음
- 하위 에이전트 사용: 실행하지 않음

## 파르붐 강한 측면 피격 고개·아랫턱·몸통 재구현

`PARVUM_STRONG_SIDE_HIT_HEAD_JAW_BODY_REACTION_APPLIED`

- 사용자 확인에서 직전 고개 회전도 충분히 크지 않고 아랫턱 반동이 없으며 몸통 충격감도 약한 것으로 판정돼 다시 재구현했습니다.
- 고개 피벗 회전은 피치 `7도 → 12도`, 요 `-32도 → -48도`, 롤 `12도 → 20도`로 크게 확대했습니다.
- 개체 기준 왼쪽 최대 고개 이동은 `0.543704m → 0.753774m`로 증가했고, 최소 허용 반경도 `0.70m`로 강화했습니다.
- 공격 모션 도구에서 검증된 `Bone_011~Bone_018` 가중치와 로컬 X `-0.62~0.62`, Y `0.28~0.98`, Z `0.44~1.44` 범위를 사용해 아랫턱 전체 전용 BlendShape를 새로 추가했습니다.
- 아랫턱 전용 영향 정점은 `879개`, 최대 반동 이동은 `0.381413m`입니다. `0.26초` 지연 최고점 `100%`, `0.38초` 반대 반발 `-42%`, `0.50초` 감쇠 반동 `24%`, `0.72초` 복구 구조입니다.
- 몸통은 `0.18초` 주 압축 `100%` 뒤 `0.30초`에 휴식 외곽을 넘어 `-28%`로 크게 튕기고, `0.40초`에 `50%`로 재압축한 뒤 `0.52초`의 `-15%` 잔류 반발을 거쳐 복구합니다.
- 최종 여섯 시점 비교 이미지에서 휴식 → 고개·몸통 강한 충격 → 아랫턱 지연 최고점 → 몸통 바깥 반발 → 몸통 재압축 → 복구를 정면과 3/4 시점으로 직접 확인했습니다.
- 아랫턱은 머리와 분리되지 않으며 메시 찢김·바닥 들림이 없습니다. 바닥 높이 변화 `0m`, 루트·모델 Transform 곡선 없음, 다른 Parvum 슬롯·씬 루트·물리 설정 무변경 상태를 유지했습니다.
- 기존 파르붐 피격 클립·컨트롤러는 사용하거나 수정하지 않았습니다.

### 최종 반영 및 확인

- `ApplyParvumHitMotion`: PASS
- `InspectParvumHitMotion`: PASS
- `CaptureParvumHitMotionComparison`: PASS
- 보고서 `BodyReaction`: `StrongCrushOutwardReboundRecompressionAndDampedSettle`
- 보고서 `HeadReaction`: `LargeSingleSideImpactSnapWithPitchYawRollAndLocalCompression`
- 보고서 `LowerJawReaction`: `DelayedPeakOppositeReboundAndDampedAftershock`

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업: 실행하지 않음
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업: 실행하지 않음
- Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령: 실행하지 않음
- 기존 `Parvum_Hit.anim`, `Parvum_Hit_Controller.controller` 사용 또는 수정: 실행하지 않음
- 전투 판정·AI·체력·플레이어 피격 로직 및 다른 슬롯 수정: 실행하지 않음
- Git 작업: 실행하지 않음
- 하위 에이전트 사용: 실행하지 않음

## 파르붐 신규 전신 용해 사망 모션 구현

`PARVUM_NEW_WHOLE_BODY_MELT_DEATH_MOTION_NEEDS_REVISION`

> 현재 상태: 추가 수정 필요 / 미완료. 현 사망 모션은 구현·적용·전용 검사까지 진행됐지만 사용자 최종 승인 전이며, 다음 작업에서 시각 모션을 계속 교정해야 합니다.

- 기존 파르붐 사망 애니메이션을 사용하지 않고 `Approved Parvum Enemy Placement/Parvum_05_Death/Parvum_Model` 전용 메시·클립·컨트롤러와 최종 웅덩이 시각 요소를 새로 구현했습니다.
- 전체 길이는 `3초`입니다. `0~2초`에는 전신 `3,215개` 정점이 아래로 녹아내리고, `2~3초`에는 최종 웅덩이가 정확히 `1초` 동안 동일한 형상으로 유지된 뒤 `3초` 경계에서 원래 몸통으로 하드 리셋되어 반복됩니다.
- 입·윗입·아랫입·내부 입·치아에 해당하는 `2,068개` 정점을 전면 기하 범위와 치아 본 분기로 분류해 용해 중 중심 바닥으로 가라앉혔습니다. 최종 웅덩이에는 입이나 치아 형태가 남지 않습니다.
- 최종 시각 요소는 `Parvum_Death_Puddle_Visual`이며, Unity 내장 구체를 낮은 타원체로 눌러 원본 몸통 너비와 맞췄습니다. 측정 폭은 원본 `2.544220m` 대비 `2.544224m`, 비율 `1.000002`입니다.
- 최종 웅덩이 깊이는 `1.831841m`, 최종 높이는 원본 몸통 높이의 `0.052941`, 바닥 오차는 `0.002897m`입니다.
- 최종 웅덩이 메시와 머티리얼은 `parvum_death_melt_puddle_mesh.asset` 내부 서브에셋으로 저장했습니다. 원본 몸통 머티리얼·텍스처는 수정하지 않았고, 사망 슬롯 전용 복제 머티리얼에서만 녹색 색조와 낮은 금속성·반사값을 사용했습니다.
- 클립에는 전신 용해 BlendShape 2개와 `2초` 몸체/웅덩이 표시 전환 곡선 2개만 있습니다. 루트·모델 Transform 곡선은 없으며 Rigidbody·Collider·`ParvumPhysicsMotionDriver` 설정은 보존했습니다.
- 초기 최종 형상이 각진 청회색 판으로 보인 캡처들은 완료 증거에서 반려했습니다. 입 정점 오분류, 극좌표 정점 재배열, 원본 glTF 재질의 반사와 텍스처 전역 투영을 순서대로 교정한 뒤 녹색 타원형 웅덩이 결과만 최종 캡처로 채택했습니다.
- 최종 비교 이미지에서 휴식 `0초` → 전신 처짐 `0.60초` → 깊은 용해 `1.20초` → 웅덩이 전환 `1.60초` → 녹색 웅덩이 `2초` → 동일 형상 유지 `2.5초`와 `3초`를 정면과 3/4 시점으로 직접 확인했습니다.
- 원본 `parvum.glb`의 SHA-256은 `E27840896F1DFA15BEE6F45F2BA943D28375A485E141907283CF79446B5640AB`로 유지했습니다.
- 다른 Parvum 상태 슬롯과 다른 씬 루트는 변경하지 않았고, 기존 `Parvum_Death.anim`과 `Parvum_Death_Controller.controller`는 할당·사용하지 않았습니다.

### 생성 및 수정 파일

- `Assets/_Project/Editor/Validation/ParvumDeathMotionTool.cs`
- `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
- `Assets/_Project/Art/Enemies/Parvum/Models/parvum_death_melt_puddle_mesh.asset`
- `Assets/_Project/Art/Enemies/Parvum/Animations/Parvum_Death_MeltPuddle_NewModel.anim`
- `Assets/_Project/Art/Enemies/Parvum/Animations/Controllers/Parvum_Death_MeltPuddle_NewModel_Controller.controller`
- `Assets/_Project/Scenes/CargoRunMvp.unity`
- `docs/validation/parvum_death_motion_2026-08-15/Parvum_Death_Motion_Report.txt`
- `docs/validation/parvum_death_motion_2026-08-15/Parvum_Death_Motion_Final_Comparison.png`
- `docs/PROGRESS_2026-08-15.md`

### 현재 반영 및 확인

- `ApplyParvumDeathMotion`: PASS
- `InspectParvumDeathMotion`: PASS
- `CaptureParvumDeathMotionComparison`: PASS
- 현재 캡처 상태: 녹색 몸통 너비 타원형 웅덩이, 입·치아 흔적 없음, `2~3초` 동일 형상 유지
- 완료 판정: 보류. `Apply`·`Inspect`·캡처 PASS는 현재 구현 상태의 기술 확인 결과이며, 파르붐 사망 모션은 추가 시각 교정이 필요합니다.
- 기존 `Parvum_Death.anim` 및 `Parvum_Death_Controller.controller` 할당·사용: 없음

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업: 실행하지 않음
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1` 및 관련 작업: 실행하지 않음
- Restore·Regenerate·Ensure·Validate·Smoke·Test·Build 계열 명령: 실행하지 않음
- 기존 `Parvum_Death.anim`, `Parvum_Death_Controller.controller` 사용 또는 수정: 실행하지 않음
- 원본 GLB·원본 텍스처·원본 머티리얼 수정: 실행하지 않음
- 전투 판정·AI·체력·플레이어 피격·실제 사망 로직 수정: 실행하지 않음
- 다른 Parvum 배치·상태 슬롯 및 작업 범위 밖 씬 루트 수정: 실행하지 않음
- Git 작업: 실행하지 않음
- 하위 에이전트 사용: 실행하지 않음
