# 진행 기록 - 2026-08-06

## 이슈판트 승인 외형·정적 모델·대기·이동 작업 GitHub 반영

상태: `ISPANT_WORK_STATE_RECORDED`

### 반영할 작업 범위

- 승인된 `artSample/enemies/ispant_armed/` 외형을 Unity 이슈판트 12개 슬롯에 적용했습니다. 흰색 기계 장갑, 얼굴 검은 선 패턴, 청록 눈, 초승달, 가죽·목재·금속 PBR 표현을 승인 샘플과 직접 동기화했습니다.
- 허리띠·파우치·버클은 제거하고 가슴 대각선 스트랩은 유지했습니다. 얼굴 패턴은 눈 중앙에서 급격히 상승한 뒤 완만해지고 다시 상승해 외곽까지 이어지는 승인 형태를 적용했습니다.
- 몸통 흰색 장갑 평균 명도는 사용자 지정에 따라 기준 대비 25% 상승하도록 보정했고 Helmet과 초승달의 기존 밝기는 분리 보존했습니다.
- 현재 Unity 정적 이슈판트를 `enemies model/Ispant_Static.fbx`로 내보냈고, 눈 메시가 초승달 위로 이탈하던 내보내기 배율 문제를 교정했습니다.
- 이슈판트 애니메이션 슬롯 12개의 영문 이름을 사용자 확정 순서대로 저장했습니다.
- `Ispant_02_Idle`에는 2초 주기, 최고점–최저점 총 1.5cm, 양발 고정, 몸통·머리·팔·머스켓 동반 이동 대기 모션을 적용했습니다.
- `Ispant_03_Move`는 사용자 제공 `enemies model/išpant walking.fbx`의 Mixamo 동작을 사용합니다. 외형은 정적 이슈판트 머티리얼을 직접 참조하며 임의 생성하지 않았습니다.
- 이동 개체의 몸체 높이를 정적 개체와 같은 `1.02430773m`로 맞췄고 바닥 높이 차이는 `0m`입니다.
- 원본 Hips의 Z축 전진 `103.98352098cm`만 제거한 `Ispant_03_Move_InPlace.anim`을 반복 재생합니다. 적용 후 Hips 전진 범위는 `0.00000000186264515m`이고 다리 동작은 유지됩니다.
- 머스켓은 정적 FBX의 154삼각형 형상을 `mixamorig:Spine2`에, 장검은 정적 FBX의 78삼각형 형상을 `mixamorig:Hips`에 강체 연결했습니다. 부모 기준 변형 오차는 모두 `0`이며 몸통을 따라 이동합니다.
- `ApplyIspantMoveRevision`과 최종 6패널 비교 캡처가 통과했고 `Ispant_03_Move` 외 다른 슬롯과 다른 씬 루트는 변경되지 않았습니다.

### 주요 결과물

- 승인 샘플: `artSample/enemies/ispant_armed/`
- Unity 승인 외형: `Assets/_Project/Art/Enemies/Ispant/ApprovedAppearance/`
- Unity 애니메이션: `Assets/_Project/Art/Enemies/Ispant/Animations/`
- 정적 FBX: `enemies model/Ispant_Static.fbx`
- 이동 원본: `enemies model/išpant walking.fbx`
- 기준 이미지: `image/išpant(이슈판트)-armed.png`
- 적용 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 이동 개정 검사: `docs/validation/ispant_move_revision_2026-08-05/Ispant_03_Move_Revision_Inspection.txt`
- 이동 개정 최종 이미지: `docs/validation/ispant_move_revision_2026-08-05/Ispant_03_Move_Revision_FinalReview.png`
- 세부 작업 기록: `docs/PROGRESS_2026-08-05.md`

### 커밋에서 제외할 항목

- 별도 사용자 변경 `enemies model/išpant.fbx`
- 이슈판트 작업과 무관한 쿠르사 기준 이미지 변경
- Pahur·Resistance·Revolution의 `.blend1` 백업 및 Python `__pycache__`
- Revolution 과거 검증 이미지
- 이슈판트 검토 중 발생한 실패 프레이밍 이미지와 사용자 영상 추출 프레임
- 작업 외 파일과 생성물

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`
- Unity 씬·모델·애니메이션 추가 수정
- 강제 푸시, 리베이스, 이력 변경 및 브랜치 삭제

## 이슈판트 3번 이동 모션 왼팔·장검 겹침 교정

상태: `ISPANT_MOVE_LEFT_ARM_CLEARANCE_PASS`

- 사용자 제공 영상 `C:/Users/gus68/Videos/Captures/Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-08-06 08-32-12.mp4`에서 캐릭터 기준 왼팔이 걷기 스윙 중 허리 장검과 겹치는 현상을 확인했습니다.
- 원본 Mixamo 걷기와 기존 제자리 보행 변환은 유지하고, 파생 `Ispant_03_Move_InPlace.anim`의 `mixamorig:LeftArm` 상완 회전에 부모 좌표계 기준 바깥쪽 15° 보정을 합성했습니다.
- 2.4초·25fps 전 프레임 검사에서 원본 대비 왼팔 각도는 `14.9998741°~15.0000315°`, 왼손 바깥쪽 이동은 최소 `0.0103058666m`, 최대 `0.06275581m`입니다.
- 제자리 Hips 전진 범위 `0.00000000186264515m`, 정적·이동 몸체 높이 `1.02430773m`, 바닥 높이 차이 `0m`를 유지했습니다.
- 머스켓·장검의 부모 기준 변형 오차는 모두 `0`이며 `mixamorig:Spine2`와 `mixamorig:Hips` 추종을 유지했습니다. 승인 외형 머티리얼 직접 참조와 원본 FBX 해시도 변경하지 않았습니다.
- `ApplyIspantMoveLeftArmClearance`: PASS. `Ispant_03_Move`만 저장했고 다른 이슈판트 슬롯과 다른 씬 루트는 변경되지 않았습니다.
- `InspectIspantMoveLeftArmClearance`: PASS. 전 프레임 수치 검사 후 `SceneChanged=False`를 확인했습니다.
- `CaptureIspantMoveLeftArmClearanceReview`: PASS. 정적 기준과 이동 5개 위상의 최종 비교 이미지 `docs/validation/ispant_move_left_arm_2026-08-06/Ispant_03_Move_LeftArm_FinalReview.png`을 한 번 생성했습니다.
- 구조·수치 기록: `docs/validation/ispant_move_left_arm_2026-08-06/Ispant_03_Move_LeftArm_Inspection.txt`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 원본 `enemies model/išpant walking.fbx`, 외형·텍스처·머티리얼 수정
- `Ispant_03_Move` 외 다른 슬롯, AI, 공격 판정, 실제 물리 이동 및 다른 씬 루트 수정
- Git 작업

## 이슈판트 4번 발검 모션 모델 교체·오른손 장검 연결

상태: `ISPANT_DRAW_SWORD_REPLACEMENT_PASS`

- `Approved Ispant Enemy Placement/Ispant_04_DrawSword`의 기존 모션 개체를 사용자 제공 `enemies model/išpant draw sword.fbx` 기반 파생 모델로 교체했습니다. 다른 이슈판트 슬롯과 다른 씬 루트는 변경하지 않았습니다.
- 원본 발검 FBX SHA-256은 `EDDE5E8B668C987E0C92F37E2D7809AC196F5A4EAC0BE3F5B2408CBAC8862E72`이며, 프로젝트 원본 사본 `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_DrawSword_Source.fbx`도 같은 해시입니다. 원본 FBX는 수정하지 않았습니다.
- 정적 기준 `enemies model/Ispant_Static.fbx`와 발검 원본 몸체의 정점별 최대 월드 좌표 오차는 `0.000000127315577m`입니다. 교체본의 모든 머티리얼 슬롯은 `Ispant_01_Static`이 사용 중인 승인 머티리얼 객체를 직접 참조하며 임의 외형을 생성하지 않았습니다.
- 발검 원본의 유일한 Mixamo 액션 `Armature|mixamo.com|Layer0` 1~46프레임을 `Ispant_DrawSword_Mixamo`로 임포트했습니다. 길이는 `1.76초`, 프레임률은 `25fps`, 반복 재생은 `True`, Root Motion은 `False`입니다.
- 발검 원본 몸체의 연결 구성요소를 좌표·가중치·구성요소 간 최소 간격으로 식별했습니다. 머스켓 `41,75,76`은 `mixamorig:Spine2`, 칼집 `77,80`은 `mixamorig:Hips`, 뽑힌 장검의 칼날·손잡이 `78,79`는 `mixamorig:RightHand`에 강체 연결했습니다. 장검 손잡이는 원본 1프레임에서 오른손에 가장 가까운 `79` 구성요소입니다.
- 파생 FBX는 원본의 정점·면만 사용하며 SHA-256은 `B9DEB78C6BECA61C81EE5ECD86C4763E56186B8925EED29720B4B62ED482CE42`입니다. 몸체 `3,364`, 머스켓 `154`, 칼집 `38`, 뽑힌 장검 `40`삼각형으로 합계 `3,596`삼각형을 보존했습니다.
- 46프레임 전체 검사에서 머스켓·칼집·장검의 부모 기준 부착 오차는 모두 `0`입니다. 최대 몸체 추종 변화는 각각 `0.284035921`, `0.153551236`, `0.8910529`이며 오른손 최대 이동은 `0.691209555m`입니다.
- 오른손과 뽑힌 장검 표면의 최근접 거리는 전 구간 `0.0277633276m~0.0277794562m`입니다. 정적 모델 대비 높이 비율은 `1.00000012`, 지면 높이 차이는 `0m`입니다.
- `ApplyIspantDrawSwordAnimation`, `InspectIspantDrawSwordAnimation`, `CaptureIspantDrawSwordAnimationReview`가 모두 PASS했습니다. 최종 캡처 후 씬 dirty 상태는 바뀌지 않았습니다.

### 주요 결과물

- 원본 사본: `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_DrawSword_Source.fbx`
- 파생 FBX: `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_DrawSword.fbx`
- 반복 재생 컨트롤러: `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_04_DrawSword.controller`
- 적용 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- Blender 제작 기록: `docs/validation/ispant_draw_sword_2026-08-06/Ispant_DrawSword_BuildReport.json`
- Unity 전 프레임 검사: `docs/validation/ispant_draw_sword_2026-08-06/Ispant_04_DrawSword_Inspection.txt`
- 최종 6패널 이미지: `docs/validation/ispant_draw_sword_2026-08-06/Ispant_04_DrawSword_FinalReview.png`
- 최종 이전 진단 이미지는 파일명에 `Rejected`를 명시해 최종 결과물과 분리했습니다.

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- 정적 이슈판트의 장검을 오른손 장검으로 재사용하거나 원본 FBX를 수정하는 작업
- 임의 장검·손잡이·외형 생성 또는 원본 칼날 크기·형상 수정
- `Ispant_04_DrawSword` 외 다른 슬롯, AI, 공격 판정, 물리 기반 모션 및 다른 씬 루트 수정
- Git 작업

## 이슈판트 발검 장검 외형 아트 샘플

상태: `ISPANT_DRAW_SWORD_ART_SAMPLE_APPROVED`

- 현재 Unity 발검 파생 FBX `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_DrawSword.fbx`의 몸체·리그·Mixamo 1~46프레임과 승인된 정적 이슈판트 외형을 기준으로 검토용 3D 샘플을 제작했습니다.
- 기준 이미지 `image/išpant(이슈판트)-armed.png`에서 확인되는 길고 좁은 은회색 칼날, 마모된 가장자리와 짙은 갈색 손잡이 계열을 반영했습니다. 기준에서 보이지 않는 후면은 앞면 윤곽의 대칭 두께만 사용했고 별도 장식을 추가하지 않았습니다.
- 기존 발검 장검·칼집은 아트 샘플 안에서만 숨겼습니다. 새 `Ispant_Reference_LongSword`는 손잡이·가드·폼멜·칼날이 한 개로 결합된 2,080정점·4,092삼각형 메시입니다.
- 전체 길이는 `1.0715m`, 칼날은 `0.82m × 0.068m × 0.012m`, 손잡이는 `0.17m × 0.052m`, 가드 폭은 `0.19m`입니다.
- 새 장검은 `mixamorig:RightHand` 본의 직접 자식입니다. 기존 발검 손잡이 중심과의 정렬 오차는 `0m`, 오른손 본 원점–손잡이 중심 거리는 `0.0557747075m`입니다.
- 몸체 38개 머티리얼 슬롯은 승인된 정적 외형 BLEND의 기존 머티리얼 객체와 동기화했고 장검에만 마모 강철·짙은 갈색 가죽·어두운 각인 PBR 재질을 만들었습니다.
- 최종 검토본은 기준 이미지, 정면, 측면, 손잡이 확대를 한 화면에 배치했습니다. 과노출과 가림이 있던 이전 렌더는 `diagnostics/FAILED_*`로 분리했습니다.
- 기존 `artSample/enemies/ispant_armed/appearance_reference_sync/`의 HTML 형식과 동일한 산업용 패널 디자인·기준/결과 2열 비교·승인 경계·구조 보존 구성을 장검 샘플의 `index.html`과 `summary.html`에 적용했습니다.
- 두 HTML의 로컬 참조는 전체 페이지 14개, 요약 페이지 5개이며 누락은 `0`입니다. 데스크톱 1,440px 2열과 좁은 화면 720px 1열 렌더에서 한글·상태 코드·이미지 잘림과 겹침이 없음을 확인했습니다.
- BLEND·FBX·GLB를 각각 독립 재가져오기해 장검 메시 1개, 재질 3개, 4,092삼각형, `mixamorig:RightHand` 부모 연결과 동일 치수를 확인했습니다. 결과는 `PASS`입니다.
- Unity 발검 FBX SHA-256 `B9DEB78C6BECA61C81EE5ECD86C4763E56186B8925EED29720B4B62ED482CE42`는 제작 전후 동일합니다. 샘플 제작 단계에서는 Unity 씬·프리팹·런타임 자산을 변경하지 않았고, 사용자 승인 뒤 아래 12개 슬롯 적용 작업으로 반영했습니다.

### 주요 결과물

- 샘플 폴더: `artSample/enemies/ispant_draw_sword/`
- 최종 비교 이미지: `artSample/enemies/ispant_draw_sword/Ispant_DrawSword_FinalReview.png`
- 전체 비교 HTML: `artSample/enemies/ispant_draw_sword/appearance_reference_sync/index.html`
- 요약 HTML: `artSample/enemies/ispant_draw_sword/appearance_reference_sync/summary.html`
- HTML 데스크톱·720px 확인 이미지: `artSample/enemies/ispant_draw_sword/appearance_reference_sync/Ispant_DrawSword_*Desktop.png`, `artSample/enemies/ispant_draw_sword/appearance_reference_sync/Ispant_DrawSword_*Narrow.png`
- Blender 원본: `artSample/enemies/ispant_draw_sword/Ispant_DrawSword_ArtSample.blend`
- FBX·GLB 검토본: `artSample/enemies/ispant_draw_sword/Ispant_DrawSword_ArtSample.fbx`, `artSample/enemies/ispant_draw_sword/Ispant_DrawSword_ArtSample.glb`
- 제작 기록: `artSample/enemies/ispant_draw_sword/Ispant_DrawSword_ArtSample_Report.json`
- 재가져오기 검사: `artSample/enemies/ispant_draw_sword/Ispant_DrawSword_ExportValidation.json`
- 설명·매니페스트: `artSample/enemies/ispant_draw_sword/README.md`, `artSample/enemies/ispant_draw_sword/ASSET_MANIFEST.md`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`
- Unity 씬·프리팹·런타임 자산 반영과 원본 FBX 수정
- 다른 이슈판트 슬롯, AI, 공격 판정, 물리 기반 모션 수정
- Git 작업

## 승인 장검의 이슈판트 12개 슬롯 Unity 적용

상태: `ISPANT_APPROVED_LONGSWORD_ALL_SLOTS_PASS`

- 사용자 승인본 `artSample/enemies/ispant_draw_sword/Ispant_DrawSword_ArtSample.blend`의 장검을 Unity 적용용 FBX와 1,024px PBR 텍스처로 추출했습니다. 승인 원본 SHA-256은 `F112EFF207D2EAB5FC89AF5735103877B51F973FBAD9B5EBD8D3DBEB44770FB9`이며 원본은 수정하지 않았습니다.
- 승인 장검의 형상은 2,080정점·4,092삼각형, 치수 `0.198372349m × 0.075999998m × 1.071500063m`입니다. 정적·이동·발검용 FBX는 동일 승인 형상과 재질을 유지하면서 각 기존 장착 좌표계만 보존한 파생본입니다.
- 이슈판트 12개 슬롯의 기존 장검을 승인 장검으로 교체했습니다. 정적 슬롯 `1,2,5,6,7,8,9,10,11,12`는 기존 `Hips`, 이동 슬롯 `3`은 기존 `mixamorig:Hips` 장착 위치를 유지했습니다.
- `Ispant_04_DrawSword`만 승인 장검을 `mixamorig:RightHand`의 직접 자식으로 연결했습니다. 손잡이는 오른손 안에 위치하며 1~46프레임 반복 동작을 따라갑니다.
- 발검 전 프레임 최대 부착 오차는 `0m`, 오른손 표면 최대 거리는 `0.01422674m`, 장검 최대 월드 이동량은 `0.7790516m`입니다.
- 정적 몸체에서는 기존 장검 구성요소만 제거해 3,518삼각형을 유지했고 이동·발검 몸체는 3,364삼각형을 유지했습니다. 기존 장검 렌더러와 발검 칼집 렌더러 잔존 수는 각각 `0`입니다.
- `ApplyIspantApprovedLongSwordAllSlots`: `PASS`, 12개 슬롯 적용 및 씬 저장 완료.
- `InspectIspantApprovedLongSwordAllSlots`: `PASS`, 검사로 인한 씬 변경 `False`.
- `CaptureIspantApprovedLongSwordReview`: 구조 통과 뒤 생성했으나 노출 보정 전 어두운 결과로 판정해 최종 결과에서 제외했습니다.

### 주요 결과물

- 적용 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 승인 장검 Unity 자산: `Assets/_Project/Art/Enemies/Ispant/ApprovedLongSword/`
- 추출 기록: `Assets/_Project/Art/Enemies/Ispant/ApprovedLongSword/Ispant_ApprovedLongSword_Export.json`
- 구조 검사: `docs/validation/ispant_approved_longsword_2026-08-06/Ispant_ApprovedLongSword_Inspection.txt`
- 제외 이미지: `docs/validation/ispant_approved_longsword_2026-08-06/discarded/Ispant_ApprovedLongSword_PreExposure_Dark_Rejected.png`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`
- 이슈판트 슬롯의 기존 배치 위치, 발검 외 슬롯의 부모 장착 방식, AI, 공격 판정 및 물리 기반 모션 변경
- 원본 발검 FBX와 승인 Blender 샘플 수정
- Git 작업

## 이슈판트 장검 가시성·발검 손잡이·회전 추종 개정

상태: `ISPANT_LONGSWORD_VISIBILITY_GRIP_ROTATION_REVISION_PASS`

- Unity 적용 전 BaseColor PNG가 승인 Blender의 선형 채널값을 그대로 저장하면서 Unity의 sRGB 디코딩을 다시 거쳐 어두워진 원인을 확인했습니다. 승인 선형색을 정확한 sRGB 전달함수로 인코딩해 PNG 채널 최대값을 `52 → 125`로 교정했습니다.
- 금속성 `0.92`의 마모 강철이 현재 CargoRun 조명에서 검게 잠기는 문제를 막기 위해 승인 색상·UV·마모 관계를 유지한 `3×` Unity 재질 노출을 적용했습니다.
- 기존 적용본은 장검 메시의 어느 한 정점과 오른손 본 원점 사이 거리만 검사해 손잡이 중심이 실제 손바닥에 들어갔는지 판정하지 못했습니다. 발검 1프레임의 오른손 가중치 메시 정점 중심을 계산해 승인 장검의 손잡이 중심을 그 위치에 정렬했습니다.
- 개정 후 1~46프레임 손잡이 중심–오른손 가중치 메시 중심 거리는 최소 `0.00000006m`, 최대 `0.004224267m`입니다.
- 장검과 오른손의 최대 월드 회전 변화는 각각 `179.3398°`, 상대 회전 오차는 `0°`입니다. 장검은 `mixamorig:RightHand` 직접 자식으로 위치와 회전을 함께 추종합니다.
- 나머지 11개 장착 위치, 12개 슬롯 배치 Transform, 몸체 메시, 반복 애니메이션과 다른 씬 루트는 변경하지 않았습니다.
- `ApplyIspantApprovedLongSwordAllSlots`: `PASS`, 씬 저장 완료.
- `InspectIspantApprovedLongSwordAllSlots`: `PASS`, 검사로 인한 씬 변경 `False`.
- 노출 `3×` 적용 전에 생성한 어두운 이미지는 `docs/validation/ispant_approved_longsword_2026-08-06/discarded/Ispant_ApprovedLongSword_PreExposure_Dark_Rejected.png`로 분리했습니다. 승인된 1회 캡처 범위를 넘는 추가 캡처는 실행하지 않았습니다.

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`
- 승인 장검 메시·비례·UV·마모 무늬 재설계
- Unity 종료·재시작
- AI, 공격 판정, 물리 기반 모션 및 다른 씬 루트 수정
- Git 작업

## 이슈판트 장검 0.9m 칼날 길이 개정 아트 샘플

상태: `ISPANT_DRAW_SWORD_0_9M_SOURCE_APPLIED_DIRECTLY_2026-08-06`

- 사용자 지정값에 따라 기존 승인 장검의 전체 길이 `1.0715m`를 `0.9m`로 줄였습니다. 손잡이·가드·폼멜은 고정하고 칼날만 `0.82m`에서 `0.6485m`로 축소했습니다.
- 최초 재구축 방식은 Smart UV 재투영으로 GLB 분할 정점이 `3,597 → 3,596`으로 변해 승인 외형의 UV 구조가 달라졌으므로 폐기했습니다. 승인 원본을 직접 불러와 칼날 연결 성분 3개의 길이축 좌표만 줄이는 분리 개정 방식으로 다시 제작했습니다.
- 원본 승인 BLEND SHA-256 `F112EFF207D2EAB5FC89AF5735103877B51F973FBAD9B5EBD8D3DBEB44770FB9`는 그대로 보존했습니다.
- 손잡이 영역 1,798정점·1,732폴리곤의 형상·UV·머티리얼 서명은 수정 전후 `06AD25964A6E973EC10E336AA5BDE8F04A8562180DF66EA4094F4E51FD7FEB87`로 같습니다.
- 칼날 영역 282정점·268폴리곤의 토폴로지·UV·머티리얼 서명은 수정 전후 `54BF28D5975A324BADCC3E44EFF6600C242D27771B8B757F0BFE46FA4211CA4F`로 같습니다.
- BLEND·FBX·GLB를 독립 재가져오기해 전체 길이 `0.9m`, 4,092삼각형, 재질 3개와 `mixamorig:RightHand` 부모 연결을 확인했습니다. 구조 검사 결과는 `PASS`입니다.
- 검토 HTML은 기존 이슈판트 샘플의 산업형 패널·반응형 비교 형식을 유지했고, 최종 전후 비교 이미지 1장을 구조 통과 뒤 생성했습니다.
- 사용자가 별도 아트 샘플 승인 단계 없이 Unity에 바로 적용하도록 지시해, 이 개정본을 아래 직접 적용 작업의 정확한 형상 원본으로 사용했습니다.

### 주요 결과물

- 개정 폴더: `artSample/enemies/ispant_draw_sword/length_0_9m_revision/`
- 최종 비교: `artSample/enemies/ispant_draw_sword/length_0_9m_revision/Ispant_DrawSword_0_9m_FinalReview.png`
- 전체 검토 HTML: `artSample/enemies/ispant_draw_sword/length_0_9m_revision/appearance_reference_sync/index.html`
- 요약 HTML: `artSample/enemies/ispant_draw_sword/length_0_9m_revision/appearance_reference_sync/summary.html`
- BLEND·FBX·GLB: `Ispant_DrawSword_0_9m_ArtSample.*`
- 제작·검사 기록: `Ispant_DrawSword_0_9m_ArtSample_Report.json`, `Ispant_DrawSword_0_9m_ExportValidation.json`

### 직접 적용으로 전환된 범위

- 이슈판트 12개 슬롯의 장검을 0.9m 외형으로 교체했습니다.
- 장착 위치는 유지하고 발검 개체만 오른손 추종을 유지했습니다.
- 후속 개정에서 별도 `0.8초` 회전 구간을 제거하고, 원본 발검 동작이 진행되는 동안 장검이 함께 회전해 마지막 자세에서 칼끝이 위를 향하도록 수정했습니다.

### 실행하지 않은 항목

- Unity 종료·재시작
- `Run-HarnessValidation.ps1` 하네스 검증
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`
- Git 작업

## 이슈판트 0.9m 장검 12개 슬롯 직접 적용·발검 상향 회전

상태: `ISPANT_0_9M_LONGSWORD_AND_DRAW_UPWARD_APPLIED_PASS`

- `Assets/_Project/Art/Enemies/Ispant/ApprovedLongSword/export_approved_longsword.py`의 입력을 0.9m 개정 BLEND SHA-256 `52E995ED3B121C5363E53FFA8BB832D7BF9FF4795560711AA7D12105C9CABA3D`로 전환했습니다.
- Unity용 장검은 `2,080` 원본 정점, `4,092`삼각형, 치수 `0.198372m × 0.076m × 0.9m`입니다. 손잡이 중심 `-0.103m`, 손잡이·가드·폼멜, UV와 3개 머티리얼 구성을 유지했습니다.
- 이슈판트 12개 슬롯에 0.9m 장검을 적용했습니다. 정적 슬롯 `1,2,5,6,7,8,9,10,11,12`는 기존 `Hips`, 이동 슬롯 `3`은 기존 `mixamorig:Hips` 장착 위치를 유지했습니다.
- 발검 슬롯 `4`만 `mixamorig:RightHand`의 직접 자식입니다. 별도 후속 회전 구간 없이 원래 Mixamo 1~46프레임의 발검 동작 전체에서 장검을 부드럽게 회전하고, 마지막 자세에서 칼날 축이 월드 위쪽을 향하는 `Ispant_04_DrawSword_0_9m_Upward.anim`을 반복 컨트롤러에 연결했습니다.
- 회전은 장검 원점이 아니라 손잡이 중심을 피벗으로 사용합니다. 장검 루트를 손잡이 중심으로 재배치하고 렌더러 로컬 오프셋을 역보정해, 별도 위치 곡선 없이 오른손 직접 자식의 고정 로컬 위치를 유지합니다.
- 파생 클립 길이는 원본과 같은 `1.76초`, 프레임률 `25fps`, Unity 임포트 포즈 샘플은 끝점을 포함해 `45개`, 반복 재생 `True`, Root Motion `False`입니다.
- 적용 중 보호 검사에서 FBX 센티미터 배율 누락, Unity 내부 `.anim` 이름 정규화, 장검 원점 회전으로 인한 손잡이 이탈을 각각 확인하고 씬 저장 전에 복구했습니다.
- 원래 발검 구간 Transform 오차 `0.04931524`가 두 번 반복된 원인은 자동 쿼터니언 연속성 보정이 상향 회전 접선을 이전 구간까지 전파한 것이었습니다. 자동 재계산을 제거하고 실제 `25fps` 포즈 격자에서 직접 계산한 부호 연속 키를 유지해 해결했습니다.
- 손잡이 피벗의 월드 행렬 왕복 검사에서 약 `0.9~1.3mm`의 부동소수점 편차가 반복됐습니다. 부착은 `RightHand` 직접 자식의 로컬 위치 불변으로 검사하고, 실제 손 안 배치는 손 표면 거리와 가중 손바닥 중심 거리로 분리 검사하도록 좌표계를 바로잡았습니다.
- 최종 적용 명령 `ApplyIspantApprovedLongSwordAllSlots`: `PASS`, 씬 저장 완료.
- 독립 구조 검사 `InspectIspantApprovedLongSwordAllSlots`: `PASS`, `SceneChanged=False`.
- 최종 수치: 장착 오차 `0`, 손–검 표면 최대 거리 `0.02066763m`, 손잡이 중심–손바닥 중심 최대 거리 `0.004221719m`, 발검 중 장검 월드 회전 `179.7451°`, 상향 회전 `119.6447°`, 프레임당 최대 상향 회전 `4.075761°`, 최종 칼끝 상향 오차 `0°`.

### 주요 결과물

- 적용 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 0.9m 장검 Unity 자산: `Assets/_Project/Art/Enemies/Ispant/ApprovedLongSword/`
- 발검 파생 클립: `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_04_DrawSword_0_9m_Upward.anim`
- 반복 컨트롤러: `Assets/_Project/Art/Enemies/Ispant/Animations/Ispant_04_DrawSword.controller`
- 구조 검사 기록: `docs/validation/ispant_approved_longsword_2026-08-06/Ispant_ApprovedLongSword_Inspection.txt`

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`
- Unity 종료·재시작
- 이슈판트 배치 Transform, 발검 외 슬롯의 장착 방식, 몸체, AI, 공격 판정 및 다른 씬 루트 수정
- 최종 캡처 생성
- Git 작업

## 이슈판트 왼팔·발검·승인 장검 작업 GitHub 반영

상태: `ISPANT_LEFT_ARM_DRAW_SWORD_LONGSWORD_GITHUB_COMMIT_READY`

- 직전 GitHub 기준 커밋 `8fa868f` 이후 진행한 이슈판트 작업을 하나의 후속 커밋으로 정리했습니다.
- 이동 슬롯 `Ispant_03_Move`의 왼팔 15° 바깥쪽 보정, 발검 슬롯 `Ispant_04_DrawSword`의 사용자 제공 Mixamo 모델 교체와 승인 정적 외형 동기화, 승인 장검 아트 샘플 및 0.9m 칼날 개정 결과를 포함합니다.
- Unity 이슈판트 12개 슬롯에는 동일한 승인 0.9m 장검을 적용했습니다. 기존 11개 장착 위치는 유지하고 발검 슬롯만 오른손 직접 자식으로 연결했습니다.
- 발검 클립은 별도 후속 0.8초 구간 없이 원본 1~46프레임·1.76초 동안 장검을 부드럽게 회전하며, 마지막 칼끝 상향 오차 `0°`, 오른손 부착 오차 `0m` 상태입니다.
- Unity 대상 적용과 독립 검사는 모두 `PASS`였고 검사에 의한 씬 변경은 `False`였습니다.
- 커밋 대상은 관련 Unity 에셋·Editor 도구·씬, 승인 아트 샘플의 최종 원본과 내보내기 파일, 재현 스크립트, 최종 검사 문서·이미지로 한정합니다.
- 별도 사용자 변경 `enemies model/išpant.fbx`, 쿠르사 기준 이미지, Unity 복구 씬, `.blend1` 자동 백업, `__pycache__`, 실패·폐기 진단 이미지, 임시 로그, 타 적대 개체 파일은 커밋에서 제외합니다.
- 대상 브랜치는 `main`, 업스트림은 `origin/main`, 커밋 메시지는 `feat: finalize Ispant longsword and draw motion`입니다.

### 실행하지 않은 항목

- `Run-HarnessValidation.ps1` 하네스 검증
- `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`
- Unity 실행·종료·재시작 및 추가 씬 변경
- 리베이스, 강제 푸시, 브랜치 삭제 및 이력 변경
