# PROGRESS 2026-07-21

## 오늘 작업 전체 Git 반영 및 씬 백업 범위

- 오늘 진행한 오스티나토 공격 모션 후속 조정과 5번 피격, 6번 포효·폭주 전환, 7번 착석 휴식, 8번 기립, 9번 사망 FBX 교체 작업을 현재 씬·전용 에디터 도구·애니메이션 자산·당일 기록과 함께 Git 반영 대상으로 정리했다.
- 오늘 진행한 돌로레 7개 슬롯 배치, 승인 외형 동기화, 3번 원본 FBX 이동 반복, 2번 몸통 호흡 대기, 3·4번 공격 촉수 승인 샘플 적용과 최종 액자 중앙 부착 작업을 현재 씬·전용 에디터 도구·아트 자산·당일 기록과 함께 Git 반영 대상으로 정리했다.
- 2026-07-18~20에 생성된 별도 오스티나토 중간 모션·과거 검증 산출물, Grave 관련 변경, 복구 장면 및 임시 `blend1`·`__pycache__` 파일은 이번 커밋에서 제외한다.
- Git 푸시가 완료된 뒤 현재 `CargoRunMvp.unity` 전체를 `Assets/_Project/Scenes/Backups/CargoRunMvp_2026-07-21.unity`로 복제한다. 이 날짜 백업은 기존 파일을 덮어쓰지 않으며 저장소의 Backups 제외 규칙에 따라 이번 커밋에는 포함하지 않는다.

## 최신 완료 - 돌로레 3·4번 공격 촉수 실제 절단면 부착

- 사용자 승인 샘플 `artSample/enemies/dolore/attack_attachment/`의 공격 촉수를 `Approved Dolore Enemy Placement/Dolore_04_Tentacle_Stab_Attack`과 `Dolore_05_Execution_Pull_In`에 적용했다. 제공 원본과 Unity 반입본의 SHA-256은 모두 `56C400903A1B977024DFE7999F6D8AD0A19F6E0634E40816CCC18BCC04EF20A0`이다.
- 화면에 표시하는 촉수는 승인 참조 GLB의 메시·내장 재질·13본을 직접 사용하며 별도 애니메이션은 적용하지 않았다. 모델 형상, 본, 재질은 수정하지 않았다.
- 사용자 제공 `2026-07-21 23-07-46.mp4`와 `11111.png`에서 촉수가 액자에 붙지 않고 전방 허공에 떨어진 상태를 확인했다. 개체별 정면 캡처는 깊이 차이를 눌러 보여 이 이격을 놓쳤으며, 기존 검사는 보정된 월드 AABB에서 만든 잘못된 목표 좌표를 자기참조식으로 검사하고 있었다.
- 기존 계산은 대상 모델의 전면 축 부호까지 반대로 해석했다. 스킨 변형 후 `Dolore_Faded_Portrait`의 실제 장면 전면은 로컬 `min Z=0.008501058`인데, 보이는 촉수 메시의 액자 쪽 표면은 `0.0105063`에 있어 월드 기준 약 `0.225`만큼 떨어져 있었다. 정면에서는 두 개체가 겹쳐 보였지만 좌·우 측면에서는 검은 빈 공간이 명확했다.
- 촉수 메시의 분리 삼각형 정점을 위치 기준으로 용접해 `2,200`개 정점으로 환산했고, 한 번만 사용되는 외곽선에서 정점 5개의 단일 절단 고리를 복원했다. UV 영역 평균점 대신 이 실제 절단 고리 중심을 부착 기준으로 사용한다.
- 깊이는 보이는 촉수 메시의 로컬 `max Z`와 액자 전면 `min Z`를 직접 일치시켜 유지한다. 가로·세로는 좌표상의 초상 중앙을 그대로 쓰지 않고 실제 정면 화면을 기준으로 보정했다. 첫 시각 보정은 축 방향을 반대로 적용해 촉수 시작부가 액자 왼쪽 바깥으로 밀렸고 실패 처리했으며, 최종 화면 보정값은 초상 폭 기준 `-0.42`, 높이 기준 `-0.26`이다.
- 저장 상태 검사에서 원본 GLB 해시, 13본, 승인 외형, 무애니메이션, 다른 돌로레 슬롯·기본 모델·다른 장면 루트 불변을 확인했다. 수치 보고서는 `Assets/_Project/Art/Generated/Enemies/Dolore/AttackAttachment/Review/Dolore_AttackAttachment_Inspection.txt`, 좌표 근거는 같은 폴더의 `Dolore_AttackAttachment_CoordinateDiagnostic.txt`에 저장했다.
- 수정 중 `Dolore_AttackAttachment_ContactDiagnostic.png`에서 승인 샘플 정면·측면과 Unity 정면·위쪽 3/4·좌우 측면을 직접 비교했다. 최종 정면에서 잘린 촉수 시작면이 액자 중앙에 있고 오른쪽으로 뻗으며, 양쪽 측면에서는 액자와 촉수 사이의 검은 빈 공간이 사라진 상태를 확인했다. 수치 통과가 아니라 이 화면을 최종 위치 판정 기준으로 사용했다.
- 저장 상태 점검 통과 뒤 승인 샘플·3번·4번의 정면·측면·3/4·근접 최종 비교를 `Dolore_AttackAttachment_Comparison.png`에 한 번 기록했다. 최종 검사에서 촉수 정면 방향 정렬 최솟값 `0.99999994`, 배치 계산 재현 오차 최대 `1.52662378E-05`였으며 화면 판정과 함께 확인했다. 실행한 승인 범위 명령은 `Refresh-UnityProject.ps1`, `ApplyApprovedDoloreAttackAttachment`, `InspectDoloreAttackAttachment`, `InspectDoloreAttackCoordinates`, 중간 `CaptureDoloreAttackContact`, 최종 `CaptureDoloreAttackAttachment` 1회다. Harness/EditMode/PlayMode/Build 계열, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 돌로레 2번 몸통 호흡 대기 모션

- `Approved Dolore Enemy Placement/Dolore_02_Idle`의 승인 FBX 직접 인스턴스, 모델·슬롯 Transform, 27본과 승인 재질 3종은 유지하고 슬롯 루트에 대기 전용 Animator를 연결했다.
- `Dolore_02_Idle_BreathingMesh.asset`은 승인 메시의 기본 정점·UV·삼각형·스키닝을 그대로 유지하면서 `Dolore_Breathing` BlendShape 하나를 추가한다. 몸통 재질의 독립 정점 `3,282개`만 변형되며 초상 `252개`와 액자 `1,639개`의 변형 정점은 `0개`다.
- 사용자가 승인한 최대 팽창 비율은 가로·깊이 `1.03`, 세로 `1.015`다. 모핑은 몸통 중심을 기준으로 전체 생체 조직에 적용하고, 액자와 초상은 고정한다.
- `Dolore_02_Idle_Breathing.anim`은 `2.4초`, `60fps`, 반복 재생이다. BlendShape 가중치는 `0초=0`, `0.6초=50`, `1.2초=100`, `1.8초=50`, `2.4초=0`이며 시작·종료 값이 일치한다. 커브 바인딩은 BlendShape 하나뿐이고 루트·뼈 Transform 커브는 없다.
- 첫 두 적용 시도는 임포트 FBX 루트의 누락 Animator 참조에 Unity의 특수 null 처리가 적용돼 `MissingComponentException`으로 저장 전에 중단됐다. Animator를 모델 내부가 아닌 `Dolore_02_Idle` 슬롯 루트에 배치하고 클립 경로를 슬롯 기준으로 생성해 해결했으며 실패 상태는 씬에 저장되지 않았다.
- 저장 상태 재검사에서 실제 샘플 가중치 `0, 50, 100, 50, 0`, 몸통 팽창 비율 `(1.03, 1.015, 1.03)`, 액자·초상 변형 정점 `0`, 승인 재질과 다른 돌로레 슬롯 불변을 확인했다.
- 점검 통과 후 최종 캡처 명령을 한 번만 실행했다. 밝은 검토 배경의 5개 시점에서 생체 몸통이 완만하게 팽창·수축하고 외곽 액자와 초상이 고정된 상태를 확인했으며 결과는 `docs/validation/dolore_idle_breathing_2026-07-21/Dolore_02_Idle_Breathing.png`에 저장했다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `InspectDolore02IdleBreathingTarget`, `ApplyDolore02IdleBreathing`, `InspectDolore02IdleBreathing`, `CaptureDolore02IdleBreathing`이다. 적용용 임시 에디터 명령은 제거했다. Unity 재시작, Harness/EditMode/PlayMode/Build 검증과 Git 작업은 실행하지 않았다.

## 최신 완료 - 돌로레 3번 원본 FBX 직접 배치 및 내장 이동 반복

- `Approved Dolore Enemy Placement/Dolore_03_Move_Quadruped`의 이전 개체를 삭제하고, 사용자 지정 `enemies model/dolore.fbx`와 SHA-256 `0A8DF2A16B881B24A5FC856E2E3534A05D506049CE4C49975BB8433E71A2204E`가 같은 `Assets/_Project/Art/Enemies/Dolore/Models/Dolore.fbx` 직접 인스턴스로 교체했다.
- 원본의 단일 내장 클립 `Armature|Armature|Armature|Armature|Unreal Take|baselayer`를 수정하지 않고 `60fps`, `1초`, `Animation.playAutomatically=True`, `WrapMode.Loop`으로 반복 재생한다. 적용 전후 커브 지문은 `70C360802ADB6D23CA1D77D86CCC980D456689D0EC21BB8815B95F8B942912B6`으로 같고, 280개 커브 바인딩은 변경하지 않았다.
- 원본 FBX 렌더 메시에는 UV가 없고 단일 서브메시만 있어 승인 외형의 3개 재질을 직접 표현할 수 없었다. `Dolore_SourceRig_ApprovedAppearance.asset`은 원본 메시의 정점 위치·본 가중치·바인드 포즈를 유지하고, 승인 정적 모델의 정확히 대응하는 중복 정점·UV·3개 서브메시 삼각형 영역만 이식한다. `Dolore_Wet_Deep_Teal_Tissue`, `Dolore_Faded_Portrait`, `Dolore_Oxidized_Brass_Frame` 재질 순서는 정적 모델과 같다.
- 새 개체의 모델 배치 스케일은 정적 모델과 같은 `(112.3365, 112.3365, 112.3365)`이며 원본 리그 루트는 재생 전 구간 `(1, 1, 1)`이다. 0·25·50·75·100% 샘플에서 실제 자세 변화와 최대 렌더러 크기 `(1.083051, 1.566879, 1.998736)`를 확인했다. 별도 스케일 보정 컴포넌트는 사용하지 않으며 다른 돌로레 슬롯은 변경하지 않았다.
- 수치 점검 뒤 승인된 추가 최종 캡처 한 번으로 정적 개체와 내장 클립 5개 시점을 기록했다. 승인 재질의 어두운 몸통은 검은 캡처 배경에 묻혀 액자 윤곽 위주로 보이지만, 원본 스키닝과 승인 UV·서브메시·재질 대응은 저장 상태 재검사에서 모두 일치했다. 결과는 `docs/validation/dolore_move_direct_fbx_2026-07-21/Dolore_03_DirectFbxMove.png`에 저장했다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `ApplyDolore03DirectFbxReplacement`, `InspectDolore03DirectFbxReplacement`, `CaptureDolore03DirectFbxReplacement`다. 이전 `DoloreEmbeddedMoveRigScaleLock`과 적용용 임시 에디터 명령은 제거했다. Unity 재시작, Harness/EditMode/PlayMode/Build 검증과 Git 작업은 실행하지 않았다.

## 폐기된 이전 구현 - 돌로레 3번 내장 이동 모션 런타임 스케일 보정

- `Approved Dolore Enemy Placement/Dolore_03_Move_Quadruped`는 원본 `Dolore.fbx`의 단일 내장 테이크를 `60fps`, `1초`, 자동 반복으로 계속 사용한다. 원본 애니메이션 커브와 승인 모델·메시·머티리얼은 수정하지 않았다.
- 확대 원인은 승인 모델 `Armature`의 기본 로컬 스케일 `(0.01, 0.01, 0.01)`을 내장 클립의 상수 스케일 커브 `(1, 1, 1)`이 재생 중 덮어쓰는 단위 불일치였다. 수정 전 런타임 샘플의 렌더러 크기는 `(139.5349, 180, 245.3248)`이었다.
- `DoloreEmbeddedMoveRigScaleLock`을 3번 개체에만 추가했다. 이 컴포넌트는 `LateUpdate`에서 `Armature` 스케일만 승인 기본값 `0.01`로 복구하며, 위치·회전과 하위 뼈대 이동 자세는 그대로 둔다.
- 수정 후 0·25·50·75·100% 샘플 모두 리그 로컬 스케일 `(0.01, 0.01, 0.01)`, 위치 `(0, 0, 0)`, 렌더러 크기 `(1.395349, 1.8, 2.453248)`를 유지했다. 하위 뼈대 자세 변화, 내장 클립 반복, 모델·슬롯 Transform, 메시, 재질과 다른 돌로레 슬롯 불변도 확인했다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `InspectDolore03EmbeddedMoveRuntimeScale`, `ApplyDolore03EmbeddedMoveScaleGuard`다. 적용용 에디터 명령은 제거했으며 Unity 재시작, Harness/EditMode/PlayMode/Build 검증과 Git 작업은 실행하지 않았다.

## 최신 완료 - 승인 돌로레 외형 샘플 Unity 동기화

- 사용자가 `artSample/enemies/dolore/`의 현재 샘플을 승인했다. 승인 상태는 `approved_by_user`이며 적용 범위는 모델을 변경하지 않는 Unity 머티리얼·텍스처 동기화다.
- 샘플은 제공 원본 `enemies model/dolore.fbx`의 단일 `char1` 스킨 메시를 그대로 사용한다. 원본 형상은 `2,223정점·4,139폴리곤·27본·1메시`이며 추가 모델 메시와 공격 미리보기는 없다.
- 기존 화면 투영 방식과 단일 투영 머티리얼을 제거했다. 승인 샘플의 FBX를 `Assets/_Project/Art/Enemies/Dolore/ApprovedSample/Models/Dolore_CurrentModel_ReferenceSync.fbx`로 바이트 그대로 가져오고, 승인 UV와 `Dolore_Wet_Deep_Teal_Tissue`, `Dolore_Oxidized_Brass_Frame`, `Dolore_Faded_Portrait` 3개 재질을 사용한다.
- `CargoRunMvp.unity`의 `Approved Dolore Enemy Placement` 아래 7개 슬롯을 승인 FBX의 직접 인스턴스로 교체했다. 슬롯·모델 트랜스폼, 27본 이름, Collider 수, Animator·Animation 비활성 상태, 다른 씬 루트는 보존했다.
- 승인 FBX SHA-256은 `F5FC1F20A88AE5FFB882AF26C45F3278D173157EF1302AFA95A89B062B3F3491`이다. 원본과 승인 FBX의 정점 위치 지문 `6481749B...C1`, 토폴로지 지문 `D6877665...4C7`이 같으며 원본 제어점 `2,223`, 폴리곤 `4,139`, 본 `27`을 유지한다. Unity 렌더 정점 `5,173`은 UV·재질 경계 분리에 따른 임포트 결과다.
- 저장 후 읽기 전용 점검에서 7개 직접 인스턴스, UV0, 3개 재질 슬롯, 몸통 `2,697면`, 액자 `1,175면`, 초상 `267면` 대응과 씬 비변경을 확인했다. 승인 렌더와 Unity 정면·측면·후면 비교 캡처도 생성했으며 엔진 조명과 금속 반사 차이는 남아 있다.
- `Run-HarnessValidation.ps1`, EditMode/PlayMode 테스트, 빌드, Unity 재시작과 Git 작업은 실행하지 않았다.

## 최신 완료 - 제공 FBX 기반 돌로레 7개 슬롯 배치

- 사용자 지정 `enemies model/dolore.fbx`를 바이트 그대로 `Assets/_Project/Art/Enemies/Dolore/Models/Dolore.fbx`에 복사했다. 양쪽 SHA-256은 `0A8DF2A16B881B24A5FC856E2E3534A05D506049CE4C49975BB8433E71A2204E`로 일치한다.
- 아트 샘플이나 대체 모델을 만들지 않고 제공 FBX의 직접 인스턴스 7개를 `Approved Dolore Enemy Placement` 아래에 배치했다. 슬롯은 정적 검토, 대기, 사족 이동, 촉수 찌르기 공격, 처형 끌어오기, 피격, 사망 순서이며 이번 작업에서는 애니메이션을 적용하지 않았다.
- 돌로레 루트는 오스티나토 `(57.86535, 0, -111.744)`보다 Z축 아래에 Longa Arma와 Tergo의 간격 `7.376999`만큼 떨어진 `(57.86535, 0, -119.121)`에 놓았다. 슬롯 X축 간격은 오스티나토의 첫 두 슬롯과 같은 `2.444763`이다.
- 각 슬롯은 제공 FBX의 `char1` 렌더러만 표시하고 높이 `1.8m`로 맞춰 지면 정렬했다. 원본 FBX가 참조하는 `output.fbm/None` 이미지가 제공되지 않아 임의 텍스처·머티리얼을 만들지 않았으며 표면은 흰색으로 표시된다.
- Player 시작 위치를 `(65.25806, 0, -128.8872)`, 전방을 `+Z`로 옮겼다. 저장 후 독립 점검에서 7개가 모두 정면 화면 안에 들어오고 다른 씬 루트가 바뀌지 않았음을 확인했다.
- 점검 통과 뒤 최종 캡처 명령을 한 번만 실행했다. 결과는 `docs/validation/dolore_cargo_run_placement_2026-07-21/`에 저장했다. 보호 검증 명령, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 사망 FBX 기반 오스티나토 사망 개체 교체

- 사용자가 지정한 `enemies model/ostinato death.fbx`를 바이트 그대로 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_09_Death.fbx`에 복사했다. 양쪽 SHA-256은 `28476FC7621DF2139CB21C377A73BD75E516468AFC8CD03504E8E5D2F4F90AB3`로 일치한다.
- FBX의 두 기본 테이크 중 사용자가 지정한 `mixamo.com`을 정확히 하나 선택했다. Unity 기준 전체 `0~181프레임`, `60fps`, 재생 길이 `3.016667초`이며 키·포즈·타이밍·커브 수정 없이 `LoopTime=True`만 설정했다.
- `Approved Ostinato Enemy Placement/Ostinato_09_Static_Review`의 기존 정적 자식 개체를 삭제하고 제공된 FBX의 직접 인스턴스로 교체했으며 슬롯 이름을 `Ostinato_09_Death`로 변경했다. GlobalObjectId가 `...-919132149155446097-699944039`에서 `...-919132149155446097-2140544925`로 바뀌어 실제 교체가 확인됐다.
- 새 인스턴스의 24본 리그에 `Ostinato_01_Static_Review`가 사용하는 승인 메시와 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했다. 별도 비승인 렌더러는 표시하지 않는다.
- Controller 상태 속도는 `1`, `Animator.applyRootMotion=False`이다. 적용 전후 애니메이션 커브 지문은 `581E255E5905DAA33069E76C9468EA005DFF9ADF516CBF181C5EEC3420F13FAA`로 같고 60개 커브 바인딩이 모두 해석됐다.
- 적용 및 저장 후 독립 점검에서 다른 오스티나토 8개 슬롯이 바뀌지 않았음을 확인했다. 입력 점검을 통과한 뒤 최종 캡처 명령은 한 번만 실행했다.
- Unity Editor Play Mode 실제 Animator를 정면·사선 15개 시점으로 확인했다. 웅크린 자세에서 일어서고 뒤로 쓰러져 누운 자세까지 이어지는 사망 동작과 반복 경계를 확인했으며, 승인 외형 유지와 메시·관절 상태에 이상이 없었다. 관찰된 개체 루트 이동·회전은 모두 `0`이다.
- 결과는 `docs/validation/ostinato_death_fbx_replacement_2026-07-21/`에 저장했다. Unity 재시작과 Git 작업은 실행하지 않았다.

## 최신 완료 - 제공 FBX 기반 오스티나토 기립 개체 교체

- 사용자 지정 `enemies model/ostinato stand up.fbx`를 바이트 그대로 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_08_Stand_Up.fbx`에 복사했다. 양쪽 SHA-256은 `F024ABB7CEA423226BEDC60A65533808E4D6C88058FB18A10D6E61A623DDECA2`로 일치한다.
- 제공 FBX의 두 기본 테이크 중 사용자가 지정한 `mixamo.com`을 선택했다. 선택 구간은 기본 테이크 전체 `0~97프레임`, `60fps`, 재생 길이 `1.616667초`이며 키·포즈·타이밍·커브 수정 없이 `LoopTime=True`만 설정했다.
- `Approved Ostinato Enemy Placement/Ostinato_08_Static_Review`의 기존 정적 자식 개체를 삭제하고 제공 FBX 직접 인스턴스로 교체한 뒤 슬롯 이름을 `Ostinato_08_Stand_Up`으로 변경했다. GlobalObjectId는 `...-1141233713`에서 `...-1687915296`으로 변경됐다.
- 새 인스턴스의 24본 리그에 `Ostinato_01_Static_Review`가 사용하는 승인 메시와 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했다. 제공 FBX의 별도 비승인 렌더러는 표시되지 않게 했다.
- Controller 상태 속도는 `1`, `Animator.applyRootMotion=False`다. 적용 전후 애니메이션 커브 지문은 `23884BDA984D1F8894829CA53788E2935C79226BEB44950BC468D49A1C27B252`로 같고 60개 커브 바인딩이 새 FBX 인스턴스에서 모두 해석됐다.
- 적용 및 독립 점검에서 다른 오스티나토 8개 슬롯이 바뀌지 않았음을 확인했다. 점검 통과 뒤 최종 캡처 명령을 한 번만 실행했다.
- Unity Editor Play Mode 실제 Animator의 정면·사선 15개 시점에서 깊은 착석 자세에서 상체와 골반을 들어 기립 자세에 도달하는 전 구간을 확인했다. 반복 경계에서는 시작 착석 자세로 돌아간다. 승인 외형이 유지됐고 메시 찢어짐, 관절 분리, 개체 소실은 보이지 않았다. 관찰된 개체 루트 이동·회전은 모두 `0`이다.
- 결과는 `docs/validation/ostinato_stand_up_fbx_replacement_2026-07-21/`에 저장했다. Unity 재시작과 Git 커밋·푸시는 실행하지 않았다.

## 최신 완료 - 제공 FBX 기반 오스티나토 착석 휴식 개체 교체

- 사용자 지정 `enemies model/ostinato sitting.fbx`를 바이트 그대로 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_07_Seated_Rest.fbx`에 복사했다. 양쪽 SHA-256은 `5A8750A865B24DFB1C32134994CC0FEDB2B90BDE2CB383DB51FC52C048EDAECA`로 일치한다.
- 제공 FBX의 두 기본 테이크 중 사용자가 처음부터 지정한 `mixamo.com`을 선택했다. 선택 구간은 기본 테이크 전체 `0~96프레임`, `60fps`, 재생 길이 `1.6초`이며 키·포즈·타이밍·커브 수정 없이 `LoopTime=True`만 설정했다.
- `Approved Ostinato Enemy Placement/Ostinato_07_Static_Review`의 기존 정적 자식 개체를 삭제하고 제공 FBX 직접 인스턴스로 교체한 뒤 슬롯 이름을 `Ostinato_07_Seated_Rest`로 변경했다. GlobalObjectId는 `...-21739810`에서 `...-1405355437`로 변경됐다.
- 새 인스턴스의 24본 리그에 `Ostinato_01_Static_Review`가 사용하는 승인 메시와 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했다. 제공 FBX의 별도 비승인 렌더러는 표시되지 않게 했다.
- Controller 상태 속도는 `1`, `Animator.applyRootMotion=False`다. 적용 전후 애니메이션 커브 지문은 `99518A569E731406F6ACCA90D5D2733988EF26A8D9A51DE61F1906CC7EB55B91`로 같고 60개 커브 바인딩이 새 FBX 인스턴스에서 모두 해석됐다.
- 첫 코드 복제 과정에서 도구 출력 생략 표식이 파일에 들어가 대상 조사 전에 컴파일이 중단됐고, 완전한 1,068줄 템플릿으로 즉시 교체했다. Unity에 남은 컴파일 실패 상태는 사용자 확인 후 해소됐으며, 이 단계까지 7번 장면 개체는 변경되지 않았다.
- 이후 적용 및 독립 점검에서 다른 오스티나토 8개 슬롯이 바뀌지 않았음을 확인했다. 점검 통과 뒤 최종 캡처 명령을 한 번만 실행했다.
- Unity Editor Play Mode 실제 Animator의 정면·사선 15개 시점에서 기립 상태에서 몸을 낮춰 깊게 착석하고 양팔·칼날을 아래로 이완한 뒤 다시 일어나는 전 구간을 확인했다. 승인 외형이 유지됐고 메시 찢어짐, 관절 분리, 개체 소실은 보이지 않았다. 관찰된 개체 루트 이동·회전은 모두 `0`이다.
- 결과는 `docs/validation/ostinato_seated_rest_fbx_replacement_2026-07-21/`에 저장했다. Unity 재시작과 Git 커밋·푸시는 실행하지 않았다.

## 최신 완료 - 제공 FBX 기반 오스티나토 포효 및 폭주 전환 개체 교체

- 사용자 지정 `enemies model/ostinato roar.fbx`를 바이트 그대로 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_06_Roar_Enrage_Transition.fbx`에 복사했다. 양쪽 SHA-256은 `A850D9E035FC48A3FC5D9CD7484CDAEB3EC5351016D02624AE28F9404D6D87C1`로 일치한다.
- 제공 FBX의 두 기본 테이크 중 사용자 확인에 따라 `mixamo.com`을 선택했다. 선택 구간은 기본 테이크 전체 `0~184프레임`, `60fps`, 재생 길이 `3.066667초`이며 키·포즈·타이밍·커브 수정 없이 `LoopTime=True`만 설정했다.
- `Approved Ostinato Enemy Placement/Ostinato_06_Static_Review`의 기존 정적 자식 개체를 삭제하고 제공 FBX 직접 인스턴스로 교체한 뒤 슬롯 이름을 `Ostinato_06_Roar_Enrage_Transition`으로 변경했다. GlobalObjectId는 `...-1677376858`에서 `...-1147846032`로 변경됐다.
- 새 인스턴스의 24본 리그에 `Ostinato_01_Static_Review`가 사용하는 승인 메시와 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했다. 제공 FBX의 별도 비승인 렌더러는 표시되지 않게 했다.
- Controller 상태 속도는 `1`, `Animator.applyRootMotion=False`다. 적용 전후 애니메이션 커브 지문은 `0B7106FACD97F7BED3E2D3947E39AC317E55896F34EE7E5D7912D185A2C16844`로 같고 60개 커브 바인딩이 새 FBX 인스턴스에서 모두 해석됐다.
- 적용 및 독립 점검에서 다른 오스티나토 8개 슬롯이 바뀌지 않았음을 확인했다. 점검 통과 뒤 최종 캡처 명령을 한 번만 실행했다.
- Unity Editor Play Mode 실제 Animator의 정면·사선 15개 시점에서 낮은 자세, 상체와 양팔을 크게 벌리는 포효, 전방으로 몸을 낮추는 폭주 전환, 기립 자세 복귀가 연속적으로 확인됐다. 승인 외형이 유지됐고 메시 찢어짐, 관절 분리, 개체 소실은 보이지 않았다. 관찰된 개체 루트 이동·회전은 모두 `0`이다.
- 결과는 `docs/validation/ostinato_roar_fbx_replacement_2026-07-21/`에 저장했다. Unity 재시작과 Git 커밋·푸시는 실행하지 않았다.

## 최신 완료 - 제공 FBX 기반 오스티나토 피격 개체 교체

- 사용자가 지정한 `enemies model/ostinato hitted.fbx`를 바이트 그대로 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_05_Hit_Recoil.fbx`에 복사했다. 양쪽 SHA-256은 `AB55910AE10AA48A100C1DD433917BB68E08786B9AE34290EA9062DA9C74C6C3`으로 일치한다.
- 제공 FBX의 두 기본 테이크 중 사용자 확인에 따라 `mixamo.com`을 선택했다. 선택 구간은 기본 테이크 전체 `0~86프레임`, `60fps`, 재생 길이 `1.433333초`이며 별도 자르기·포즈·키·커브 수정 없이 `LoopTime=True`만 설정했다.
- `Approved Ostinato Enemy Placement/Ostinato_05_Hit_Recoil`의 기존 자체 제작 피격 자식 개체를 삭제하고 제공 FBX 직접 인스턴스로 교체했다. GlobalObjectId가 `...-139071030`에서 `...-1731651667`로 변경됐고 프리팹 원본 경로는 제공 FBX의 Unity 복사본이다.
- 새 인스턴스의 24본 리그에 `Ostinato_01_Static_Review`가 사용하는 승인 메시와 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했다. 제공 FBX에 포함된 별도 비승인 렌더러는 표시되지 않게 했다.
- Controller 상태 속도는 `1`, `Animator.applyRootMotion=False`다. 임포트 전후 애니메이션 커브 지문은 모두 `F4495E826BDF017E73D01F33E2CD351DDB185198748629D0E8A29472191E782D`로 같고, 60개 커브 바인딩이 새 FBX 인스턴스에서 모두 해석됐다.
- 적용 및 독립 점검에서 다른 오스티나토 8개 슬롯이 바뀌지 않았음을 확인했다. 최종 캡처 명령은 점검 통과 뒤 한 번만 실행했다.
- Unity Editor Play Mode 실제 Animator의 정면·사선 15개 시점에서 제공 모션의 전신 움찔과 연속 자세 변화를 확인했다. 승인 외형이 전 구간 유지됐고 메시 찢어짐, 관절 분리, 개체 소실은 보이지 않았다. 관찰된 개체 루트 이동·회전은 모두 `0`이다.
- 결과는 `docs/validation/ostinato_hit_fbx_replacement_2026-07-21/`에 저장했다. 이전 자체 제작 Blend·생성기와 과거 점검 기록은 이력으로 보존했지만 현재 슬롯과 Controller에서는 사용하지 않는다. Unity 재시작과 Git 커밋·푸시는 실행하지 않았다.

## 폐기된 이전 구현 - 자체 제작 오스티나토 반복 피격 모션

- 사용자 확정에 따라 `Approved Ostinato Enemy Placement/Ostinato_05_Static_Review`를 `Ostinato_05_Hit_Recoil`로 전환했다. 피격 직후 고개를 뒤로 크게 흔들고 몸통도 함께 뒤로 움찔한 뒤 빠르게 기본 자세로 복귀하며, 검토용 개체에서는 반복 재생한다.
- 승인된 `artSample/enemies/ostinato/blender/Ostinato_CurrentModel_TexturedSample.blend`의 메시와 24본 리그를 재사용해 `Ostinato_05_Hit_Recoil_Source.blend`와 `Ostinato_05_Hit_Recoil.fbx`를 생성했다. 새 모델링·텍스처·머티리얼은 만들지 않았으며 표시 개체는 기존 승인 메시와 머티리얼 4종을 그대로 사용한다.
- 클립은 `0.70초`, `60fps`, `LoopTime=True`다. 최대 반동은 `0.183333초`, 기본 자세 복귀는 `0.50초`이며 반복 경계까지 기본 자세를 유지한다.
- Unity 전용 점검에서 몸통 회전 `26.99983도`, 머리 회전 `58.99811도`, 머리 후방 이동 `0.110458m`, 척추 후방 이동 `0.036476m`를 확인했다. 머리 반응은 몸통보다 `31.99828도` 크다.
- 복귀·종료 위치와 회전 오차, 모델 루트 이동과 회전은 모두 `0`이다. `Animator.applyRootMotion=False`이며 225개 커브 바인딩이 25개 Transform에 모두 해석됐다.
- 첫 Blender 실행은 Blender 5.1 Action API 변경, 두 번째는 승인 원본의 극미세 루트 회전값을 완전한 0으로 비교한 검사 때문에 결과물 저장 전에 중단됐다. 이후 첫 Unity 적용은 새 FBX의 리그 루트와 승인 모델의 `Armature` 경로 불일치로 전용 점검에서 중단됐고, 실제 승인 리그 경로에 맞춰 FBX를 재생성한 뒤 적용·독립 점검이 통과했다.
- 수치 점검 통과 뒤 최종 캡처 명령을 한 번만 실행했다. Unity Editor Play Mode 실제 Animator의 정면·사선 15프레임에서 고개와 상체의 명확한 후방 반동, 빠른 기본자세 복귀, 고정된 발을 확인했다. 메시 찢어짐, 관절 역전, 칼날·손목 분리와 개체 소실은 보이지 않았다.
- 승인 메시·재질, 5번 슬롯 Transform·sibling index, 다른 오스티나토 8개 슬롯은 유지했다. `CargoRunMvp.unity`는 5번 슬롯 이름과 Animator Controller 연결만 저장했다.
- 실행한 전용 명령은 Blender 백그라운드 생성, `Refresh-UnityProject.ps1`, `InspectOstinatoHitRecoilTarget`, `ApplyOstinatoHitRecoilAnimation`, `InspectOstinatoHitRecoilAnimation`, `CaptureOstinatoHitRecoilAnimation`이다. 결과는 `docs/validation/ostinato_hit_recoil_2026-07-21/`에 저장했다. 승인 범위 밖 검증과 Unity 재시작, Git 커밋·푸시는 실행하지 않았다.

## 최신 완료 - 오스티나토 폐합 베기 v5 관절별 곡선 궤적 적용

- 사용자 영상 `Bellerophon ... 2026-07-21 00-10-12.mp4`와 00:15에 수립한 계획을 기준으로, v4의 `공통 진행률 + 최단 Euler 직선 회전` 방식을 폐기했다. v4는 폐합 `0.333333초`에 바깥 베기 `0.116667초`의 평균 속도 비율만 늘려 적용해 네 본이 동시에 접히고 관절별 선행 차이와 넓은 칼날 호를 잃은 상태였다.
- FBX importer 표식을 `Bellerophon.OstinatoForwardSlashMotion.v5`로 올렸다. `LeftArm`, `LeftForeArm`, `RightArm`, `RightForeArm`의 회전 12개 바인딩마다 바깥 베기 `101~115프레임`의 공간 궤적과 누적 각속도 진행을 별도로 추출했다.
- 바깥 베기의 공간 호는 역방향으로 사용해 칼날이 바깥쪽에서 아래·전방 안쪽으로 큰 호를 그리며 닫히게 했고, 시간 진행은 정방향 바깥 베기의 빠른 진입과 상완·전완 선행 차이를 유지했다.
- `53~67프레임`에 전체 폐합의 `90%`를 진행한다. Animator 전체 `2배속`에서 주 베기는 바깥 베기와 같은 `0.116667초`다. `67~93프레임`은 속도가 감소하는 후속 회전으로 나머지 `10%`를 진행하며 마지막 구간 최소 평균 관절 회전 `0.092691도`를 유지한다.
- 주 베기 평균 양손 속도는 `4.711325`, 최대 폐합 속도는 `54프레임`의 `18.20618`, `54프레임` 양손 평균 속도는 `12.19131`이다. `67프레임` 손 간격은 `0.349002`이며 `93프레임`에 `0.073065`로 닫힌다.
- 최단 직선 경로 대비 곡선 편차는 최대 `21.46088도`, 관절별 진행률 최대 차이는 `0.147193`이다. v5 프레임당 최대 관절 회전 `8.252419도`는 기준 바깥 베기 `20.97926도` 이내이며 승인 관절 범위 초과는 `0도`다.
- 최종 커브 지문은 `044FF743B760B0BD7D7E6D019146FF17CE57511C64FAE7202479AA605254B7F6`, 비대상 커브 지문은 `FA28500CB635103B310340574C0D20FEB8B9A662A01F7D14CDC9F56C1726C289`다. `53·93프레임` 자세, 손목·손·칼날·위치·스케일 커브, 모델·메시·머티리얼·본 계층은 유지했다.
- 외부 원본과 Unity FBX SHA-256은 모두 `0430EEE08D7D2AFBC645ECD3DFDA7B37C1DBF7BB4A6A34AF761E9AFAD1708260`이다. 전체 Controller 속도는 `2배속`, 추가 가속 Behaviour는 `0개`다.
- 적용·전용 점검·속도 분석이 모두 `PASS`한 뒤 최종 캡처 명령을 한 번만 실행했다. Unity Play Mode 실제 Animator 41개 연속 시점의 정면·3/4에서 넓은 폐합 베기 호와 종점까지 이어지는 후속 동작을 확인했고 손목·칼날 분리, 메시 늘어짐, 개체 소실은 보이지 않았다. 관찰된 루트 이동·회전은 모두 `0`이다.
- 결과는 `docs/validation/ostinato_attack_forward_slash_motion_2026-07-21/`에 저장했다. 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackForwardSlashMotion`, `InspectOstinatoAttackForwardSlashMotion`, `AnalyzeOstinatoAttackForwardSlashVelocity`, `CaptureOstinatoAttackForwardSlashMotion`이다. `CargoRunMvp.unity` 저장과 Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 폐합을 바깥 베기 속도 곡선과 동기화

- 사용자 제공 `Bellerophon ... 2026-07-20 23-47-52.mp4`와 현행 공격을 대조했다. 현행 v3 폐합은 `53~73프레임`을 거의 균일하게 이동한 뒤 `73~93프레임`에 멈추지만, 원본의 양팔 바깥 베기 `101~115프레임`은 시작부터 빠르게 진입하고 105~106프레임에서 정점을 지난 뒤 자연스럽게 감속한다는 차이를 확인했다.
- FBX importer 표식을 `Bellerophon.OstinatoForwardSlashMotion.v4`로 올렸다. `LeftArm`, `LeftForeArm`, `RightArm`, `RightForeArm`의 Euler 회전 12개 바인딩에 바깥 베기의 정규화된 관절 각속도 진행률을 적용했다.
- 폐합은 이제 `53~93프레임` 전체에서 계속 움직인다. 별도 정지 구간은 없으며 `101~115프레임`의 `초기 진입 → 가속 정점 → 자연 감속` 비율을 40개 프레임에 재매핑했다.
- 전체 Animator 속도는 `2배속` 고정이고 추가 가속 Behaviour는 없다. 53·93프레임 자세와 축별 최단 회전 호, 왼손 `0.655438`·오른손 `0.650829`의 시작→종점 변위는 유지했다.
- 폐합 양손 평균 속도는 시작 `3.437368`, 최대 `3.437368`, 평균 `2.155606`, 최소 `0.812071`이다. 초기/평균 비율은 `1.594618`이며 v3처럼 균일 이동 후 갑자기 멈추지 않고 기준 바깥 베기와 같은 가속·감속 진행률로 종점까지 이어진다.
- 기준 바깥 베기는 양손 평균 속도 시작 `16.10822`, 최대 `28.6969`, 평균 `16.53715`, 최소 `0.850712`다. 절대 이동거리는 확대하지 않고 이 구간의 정규화된 관절 각속도 비율만 폐합에 사용했다.
- 최종 커브 지문은 `B463F3A2CB5A022CBC2A0034D19BCF0A5CAEDB5C2BE3C0587B56F7D88BAB78FC`다. 기준 진행률 최대 오차는 `0.001276`, 좌우 공통 진행률 최대 오차는 `0.000014`, 최단 회전 호 오차는 `0도`, 프레임당 최대 관절 회전은 `1.733202도`다.
- 비대상 커브 지문은 `FA28500CB635103B310340574C0D20FEB8B9A662A01F7D14CDC9F56C1726C289`로 유지됐다. 손목·손·칼날·위치·스케일 커브와 모델·메시·머티리얼·본 계층은 수정하지 않았다.
- 외부 원본과 Unity FBX SHA-256은 모두 `0430EEE08D7D2AFBC645ECD3DFDA7B37C1DBF7BB4A6A34AF761E9AFAD1708260`이다.
- 적용·속도 분석·전용 점검이 모두 `PASS`한 뒤 Unity Play Mode 실제 Animator를 41개 연속 시점, 정면·3/4로 최종 1회 캡처했다. 승인 외형이 유지됐고 관찰된 루트 이동·회전은 모두 `0`이다.
- 결과는 `docs/validation/ostinato_attack_forward_slash_motion_2026-07-20/`와 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/Ostinato_AttackAppearanceComparison.png`에 저장했다.
- 실행한 명령은 영상 정보 확인·프레임 추출, `Refresh-UnityProject.ps1`, `AnalyzeOstinatoAttackForwardSlashVelocity`, `ApplyOstinatoAttackForwardSlashMotion`, `InspectOstinatoAttackForwardSlashMotion`, `CaptureOstinatoAttackForwardSlashMotion`이다. 최종 캡처 명령은 한 번만 실행했다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 이전 v4 GitHub 반영 범위 기록

- 이번 Git 반영은 최종 오스티나토 공격 모션 v4에 필요한 원본·Unity FBX, Controller, FBX 임포트 후처리, 공격 애니메이션 공통 도구, 속도 Behaviour 타입, 최종 전용 분석·점검·캡처 기록과 `2026-07-20~21` 진행 문서로 한정한다.
- 작업 트리에 함께 존재하는 Grave 변경, 오스티나토 재질·대기·보행 변경, 다른 적대 개체 작업, 혼합 변경이 있는 `CargoRunMvp.unity`와 `UnityEditorValidationBridge.cs`, 현재 공격 개체가 사용하지 않는 과거 파생 애셋은 이번 커밋에서 제외한다.
- 제외된 파일은 삭제하거나 되돌리지 않고 사용자 작업 트리에 그대로 보존한다.
