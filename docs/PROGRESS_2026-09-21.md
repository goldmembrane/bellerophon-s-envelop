# 2026-09-21 진행 기록

## 2026-09-24 공간 연결 작업 통합 기록 및 Git 반영

현재 확정 상태는 아래 요약과 직후 창고 적용 완료 항목입니다. 본문에 남아 있는 ‘미완료’, ‘승인 대기’, ‘Pegasus 미적용’은 각 단계 당시의 이력이며 현재 상태를 뜻하지 않습니다. 이전 실패·정정 기록은 삭제하지 않습니다.

- 동력실·조종실·통제실 사이 접점 보완 샘플을 사용자 승인 후 Pegasus에 적용했습니다. 입구 통행 공간을 유지하면서 외부 노출과 측면 끼임 공간을 보완했습니다.
- 통제실–무기실 및 무기실–비품실 연결 샘플을 제작·승인·적용했습니다. 복도 본체는 각각 16m/12m로, 4m/s 보행 기준 4초/3초입니다. 연결부는 본체 길이에서 제외합니다.
- 상단 세 복도는 기존 길이의 25%로 단축했습니다. 통제실 위치·각도를 유지하고 조종실·동력실은 각도를 보존한 채 이동했습니다. 사용자 승인 샘플 결과를 Pegasus에 적용했습니다.
- 다섯 방을 유지하면서 창고와 각 방을 새 곡선 경사 복도로 연결했습니다. 본체 경사 중심선은 각각 약 18m(4m/s 기준 4.5초), 창고 천장은 비품실 보행 바닥보다 0.3m 아래입니다. 높이 차·접점 마감은 새 연결부에서 처리했습니다.
- 창고 복도 양 끝에는 기존 개폐 장치의 닫힌 외형만 총 10곳 배치했습니다. 개폐 기능·애니메이션을 구현하거나 닫힌 셔터의 통과를 검증한 것은 아닙니다.
- 최종 승인 샘플의 창고·다섯 복도·연결부·셔터를 Pegasus에 적용하고, 미사용 SC-S01–S05와 대응 천장을 삭제 없이 숨겼습니다. 실제 실행모드 직접 비교 근거와 제한사항은 다음 항목 및 연결된 검토 기록에 있습니다.
- 이번 Git 반영은 관련 씬·메시·메타·에디터 도구·샘플 자료·진행 기록으로 한정합니다. 조종실 머티리얼 8개와 StunTwistElectricArc의 별도 변경은 포함하지 않습니다. 로컬 백업, 임시 조작 파일, 로그도 제외합니다.
- 이번 문서·Git 작업에서는 Unity 재수정·재실행, 자동 테스트·빌드를 하지 않습니다. 기존 직접 확인 결과를 기록하며 원격 푸시 성공 여부는 실제 Git 결과로 별도 보고합니다.

## 2026-09-24 승인 창고 연결 샘플 Pegasus 적용 완료

- 사용자 승인 후 창고와 다섯 복도·연결부·양 끝 셔터 외형 10곳을 Pegasus에 그대로 적용했습니다. 개폐 기능은 추가하지 않았습니다.
- 실제 실행모드에서 10개 접점 정면·외측 상하, 다섯 복도 내부, 창고 내부 다섯 입구를 직접 확인했습니다. 승인 샘플과 동일 시점 비교에서 형상 일치를 확인했습니다. 기록: docs/validation/PegasusCargoConnections/TransferReview/DirectReview.md 및 Comparison.html.
- SC-S01–SC-S05와 대응 천장 5개는 삭제하지 않고 비활성화했습니다. 기존 다섯 방과 수평 복도는 보존했습니다. 창고 기존 하위 구성은 승인 구성으로 교체했으며 적용 전 씬 백업은 TransferReview/PegasusBeforeApply.unity.txt에 있습니다.
- 승인 샘플 원본은 변경하지 않았습니다. 직접 확인 이후 보조 대조에서 비대상 구성 변경이 없음을 확인했습니다.
- 기존 누수 메시지와 그림자 해상도 경고는 남아 있으나 사용자 지시대로 이번 완료를 막지 않았습니다. 새 컴파일 오류 및 다른 새 콘솔 오류는 확인되지 않았습니다. 자동 테스트·빌드·Git은 실행하지 않았습니다.
- 최종 전체 캡처: TransferReview/Entrance_1500.png. 실행모드 종료, Pegasus 저장 상태로 열어 두었습니다.

## 현재 공간 오브젝트 상태와 천장 아트 샘플

- `CargoRunMvp`에서 사용자가 직접 수정한 무기실, 동력실, 조종실, 비품실, 창고, 통제실과 전체 복도의 현재 계층·활성 상태·로컬 위치·회전·크기를 다시 읽어 `ApprovedShipSpacesCurrentState.cs`, `ApprovedControlRoomCurrentStateSnapshot.cs`, `ApprovedShipCorridorSegmentsCurrentState.cs`에 반영했습니다. 공간 내부 오브젝트와 사용자가 조정한 값은 정리하거나 재해석하지 않고 그대로 보존했습니다.
- 현재 공간을 기준으로 Unity 적용형 천장 아트 샘플을 `Assets/_Project/ArtSamples/ShipSpaceCeilings/`와 `artSample/ShipSpaceCeilings/`에 제작했습니다. 방 6개와 복도 10개를 대상으로 본체·입구·복도 천장을 구성하고 기존 프로젝트 머티리얼을 재사용했습니다.
- 입구 천장은 각 입구 벽 상단 높이에 맞췄고, 경사 복도 천장은 원본 복도와 같은 `5.344374도` 기울기를 유지했습니다. 무기실·비품실·통제실 입구 천장 위의 열린 면은 수직 마감 벽으로 막았습니다. 동력실은 지름 `8.800001m` 원형 천장과 세 입구 전체를 덮는 천장을 적용했습니다.
- 사용자 승인 후 샘플에서 지시된 천장·마감 조각 53개만 실제 `CargoRunMvp` 대응 공간 아래에 적용했습니다. 기존 공간 오브젝트 790개의 상태와 지시하지 않은 샘플 요소는 변경하지 않았습니다.
- 직접 확인에서는 방 6개, 복도 10개, 입구 보강 13개를 상부·실내 하부·측면에서 비교했습니다. 입구 높이 오차와 경사 복도 회전 오차는 `0`, Unity 콘솔 오류는 `0`이었습니다.

## Pegasus 씬 생성과 공간 배치

- 새 씬 `Assets/_Project/Scenes/Pegasus.unity`를 만들고 `CargoRunMvp`의 최신 공간 6개와 `Approved Ship Corridor Segments`만 복제했습니다. 적대 개체와 수송자 개체는 복제하지 않았습니다.
- 동력실·조종실·통제실·무기실·비품실은 같은 천장 높이에 두고, 창고 천장은 다른 공간보다 `0.5m` 낮게 배치했습니다. 조종실과 창고는 배치도를 다시 대조해 각 입구가 올바른 방향을 향하도록 수직축 기준 `180도` 회전했습니다.
- 최초 선별 복사에서 누락된 동력실 내부 1개, 조종실 내부 7개, 통제실 내부 3개와 조종실 손상 콘솔 참조에 필요한 비가시성 `Phase 6 Room Interactions` 1개를 원본 상대 Transform과 참조를 유지한 채 복원했습니다. 원본과 Pegasus를 나란히 직접 비교해 내부 형상·배치 일치와 적대 개체·수송자 부재를 확인했습니다.
- Pegasus 전용 생성·복원·배치·직접 캡처·검증 도구를 `PegasusLayoutSetupTools.cs`에 구성하고 관련 명령을 `UnityEditorValidationBridge.cs`에 연결했습니다.

## 수평 복도 길이와 세 공간실 연결 배치

- 현재 플레이어 설정의 보행 속도 `4m/s`와 평균 이동 시간 5초를 기준으로 기울기 없는 `SC-H01~05`의 가시 길이를 각각 `20m`로 맞췄습니다. 바닥·양측 벽·외부 천장은 길이축만 연장했고, 기존 끝단을 양끝으로 이동한 뒤 원본 리브 간격을 따라 복도별 연장 리브 12개를 추가했습니다. 단면과 경사 복도 `SC-S01~05`는 변경하지 않았습니다.
- 동력실·조종실·통제실과 `SC-H01`, `SC-H02`, `SC-H05`의 첫 정렬 결과는 각 복도 양끝 간격이 `4.30872m`, `0.56889m`, `19.62456m`로 달라 사용자의 직접 확인에서 기각했습니다. 이 결과를 완료 상태로 남기지 않고 세 공간실 루트의 수평 위치와 대상 복도의 위치·회전만 다시 계산했습니다.
- 최종 공간실 루트는 동력실 `(-26.31523, 3.17, 15.98706)`, 조종실 `(-1.155731, 2.649, 63.11723)`, 통제실 `(32.64646, 2.66, 25.71863)`입니다. 동력실 왼쪽·조종실 위쪽·통제실 오른쪽이라는 배치도 관계를 유지합니다.
- 실제 Pegasus 씬의 전체 조감도와 세 연결부 확대를 먼저 직접 확인해 복도 양끝 간격의 균일성, 공간실·복도·중앙 창고·비대상 공간과의 비관통을 판정했습니다. 보조 수치는 여섯 간격 `1.000000~1.000007m`, 전체 편차 `0.000007m`, 중심선 각도 오차 `0도`, 최대 횡방향 오차 `0.000004m`, Unity 콘솔 오류 `0`입니다.
- 공간실 회전·높이·내부 상대 Transform, 복도 길이·단면·끝단·내부 리브, 비대상 공간실과 `SC-H03`, `SC-H04`, `SC-S01~05`는 변경하지 않았습니다. 공간실과 복도 사이를 메우는 별도 연결 조각도 아직 제작하지 않았습니다.

## Unity 에디터 기본 활성 씬

- `scripts/Open-UnityProject.ps1`와 공통 규칙을 현재 작업 흐름에 맞춰 갱신했습니다. 에디터 실행 시 프로젝트와 `Assets/_Project/Scenes/Pegasus.unity`의 현재 배치 표식을 확인하고, 열린 에디터 브리지의 `OpenPegasusScene` 명령으로 Pegasus를 활성 씬으로 엽니다.
- 기존 `CargoRunMvp` 검증은 필요할 때만 `-ValidateCargoRunScene` 옵션으로 수행하도록 분리했습니다. 임의의 `Unity.exe`, `.unity` 직접 실행이나 무조건 재시작은 사용하지 않습니다.

## 세 공간실 중간 연결부 아트 샘플

- `Assets/_Project/ArtSamples/PegasusTriRoomConnectors/PegasusTriRoomConnectors.unity`에 동력실·조종실·통제실과 `SC-H01`, `SC-H02`, `SC-H05` 사이 여섯 접점의 별도 중간 연결부 아트 샘플을 구성했습니다. 생산 씬 `Assets/_Project/Scenes/Pegasus.unity`의 기존 공간실·입구·복도 모델은 연결부 제작 과정에서 수정하지 않았습니다.
- 각 접점은 바닥·양쪽 벽·천장을 하나의 중공 경계 로프트로 연결해 중앙 통행 공간을 유지합니다. 동력실의 `SC-H01` 접점에는 기존 입구와 복도를 건드리지 않고 실제 벽 깊이까지 채우는 벽 폐쇄 메시와 바닥 접촉 채움 2개를 추가했습니다. 통제실의 `SC-H02` 접점에는 기존 사선 입구판 뒤의 쐐기형 빈 공간 전체를 채우는 폐쇄형 벽 접근 메시 2개를 추가했습니다.
- 후속 사용자 직접 확인에서 동력실 `SC-H01` 접점의 벽 겹침 경계와 통제실 `SC-H02` 접점의 사선 벽 옆 끼임 공간이 남아 있음이 확인되어 이전 완료 판정을 무효화했습니다. 검증 카메라를 접점 기준 0.52m·1.05m 거리의 공간실측 12시점과 복도측 12시점으로 확장하고, 각 개별 프레임을 저장해 사용자 캡처와 같은 근접 시점에서 다시 판정했습니다.
- 진단 색상 캡처로 동력실 문제 면이 기존 공간실 벽과 `HollowBoundaryLoft`의 접경이고, 통제실의 큰 사선판은 기존 모델이며 그 뒤가 채워져야 할 빈 포켓임을 식별했습니다. 기존 모델은 그대로 유지하고 두 접점 양쪽에 방 입구 방향과 연결부 Hermite 경계에 접하는 20분할 끼임 방지 가드를 별도 메시로 추가했습니다. 가드는 양 끝에서 삽입량이 0이 되고 두께 끝단을 만들지 않아 새 수직 걸림턱 없이 기존 폐쇄 부피 앞에서 연속 충돌면을 제공합니다.
- 실제 Unity 실행모드 근접 시점 검증 뒤 통과로 기록했으나, 사용자 후속 캡처에서 동력실 `SC-H01` 입구의 이동 방해 돌출면과 통제실 `SC-H02` 왼쪽 상단의 천장–입구 접점 틈이 그대로 남아 있음이 확인됐습니다. 이전 직접 검증과 완료 판정은 무효이며, 사용자 캡처와 같은 시점을 먼저 재현하고 문제 오브젝트를 식별한 뒤 다시 수정하는 상태입니다.
- 후속 수정에서는 동력실 `SC-H01`에 시도했던 바닥 풋프린트, 장거리 곡면 가드, 전체 벽 깊이를 덮는 슬리브가 각각 바닥 턱·얇은 판·큰 옆면을 만드는 것을 실행모드 전체 스윕과 부품 진단 색상으로 직접 확인해 폐기했습니다. 동력실 연결부는 끝점 사이 볼록 범위를 벗어나지 않는 주 벽, 평탄 바닥 접근 채움, 통행 경계 바깥의 좁은 둥근 접점 마감, 접점 전후 0.12m에만 놓이는 네 개의 경계 리본으로 구성했습니다. 통제실 `SC-H02`는 독립 세로 캡을 제거하고 좌우 벽 접근 부피·천장 접근 채움·바닥/벽 풋프린트·내외측 경계막과 방측 0.12m/연결부측 0.22m의 짧은 곡면 가드로 포켓을 닫았습니다. 재생성된 실제 Unity 실행모드의 각 75개 문제 시점과 24개 접점 확대에서는 두 입구 모두 중앙 통로가 열려 있고, 동력실의 추가 바닥 턱·교차 판·전체 깊이 옆면과 통제실 좌상단 포켓·독립 기둥 틈이 보이지 않았습니다. 이전 교체 최종 캡처도 그 뒤 형상이 바뀌어 완료 근거로 사용할 수 없으며, 현재 형상의 새 최종 캡처 전까지 완료를 선언하지 않습니다.

## 문서와 검증 기록

### 2026-09-23 간섭 복도 임시 이동

- 사용자 지시와 진행 승인에 따라 Pegasus의 SC-H03/SC-S01/SC-S02 및 각 대응 천장만 기존 배치 바깥 빈 구역으로 이동하고 저장했습니다. 형상·회전·크기·활성 상태는 유지했으며 승인 샘플 대상과 연결부는 변경하지 않았습니다.
- 실제 실행모드에서 기존과 같은 구도로 H05 통로의 H03 벽, 동력실 H05 입구 바닥의 S01 천장, 조종실 내부의 S02 천장 간섭이 사라진 것을 직접 확인했습니다. 상부 화면에서 임시 이동한 세 복도가 서로 떨어져 있고 기존 배치와 분리돼 있음을 확인했습니다.
- 이동 전후 좌표: `docs/validation/PegasusTriRoomConnectorSample/TransferReview/ParkedCorridors.txt`. 직접 확인: 같은 폴더의 `Pegasus_View_04.png`, `08.png`, `12.png`, `20.png`; 확인 후 최종 배치 캡처: `Pegasus_View_21.png`.
- 실행모드를 종료했으며 자동 테스트/빌드와 Git 작업은 하지 않았습니다. 원본 Collider 개선이나 통행 물리 검증 완료를 뜻하지 않습니다.

### 2026-09-23 승인 샘플 Pegasus 적용 및 직접 비교 불일치

- 사용자가 현재 연결부 샘플을 승인하고, Pegasus의 동력실·조종실·통제실 및 SC-H01/H02/H05와 연결부 여섯 곳을 승인 샘플과 일치시키도록 지시했습니다. 기존 Collider 개선은 이번 범위가 아닙니다.
- 초기 브리지 시간 초과는 이후 컴파일/재로드 및 요청 처리로 해소됐습니다. 에디터 고장으로 확정한 이전 설명은 정정합니다. 재시작하지 않았습니다.
- 저장된 승인 샘플의 `Intermediate Connectors` 전체 여섯 접점을 재생성 없이 Pegasus 루트에 복사하고 저장했습니다. 기존 세 방과 H01/H02/H05 및 대응 천장은 저장 상태가 같아 수정하지 않았습니다. 샘플과 다른 공간실·복도·Collider는 변경하지 않았습니다.
- 실제 Unity 실행모드에서 샘플과 Pegasus의 같은 위치·각도로 접점, 방 내부, 복도 내부 및 상부 배치를 직접 비교했습니다. 화면 자료는 `docs/validation/PegasusTriRoomConnectorSample/TransferReview/`의 `Sample_View_00~12.png`, `Pegasus_View_00~12.png`입니다. 다른 창이 겹친 데스크톱 캡처는 제외하고 실제 Game framebuffer를 사용했습니다. 조명/머티리얼/대상 가시성은 바꾸지 않았습니다.
- 동력실 H01 및 통제실 H02 수정 접점은 같은 형상으로 보였지만, **전체 결과는 불일치하여 완료 판정하지 않았습니다.** Pegasus H05 내부에는 SC-H03 벽이 가로막고, 조종실에는 SC-S02 천장이 들어오며, 동력실 H05 입구 바닥에는 SC-S01 천장이 겹칩니다. 직접 확인 후 메시 교차 조회로 해당 개체 경로를 특정했습니다(`Overlap_12.txt`, `Overlap_8.txt`, `Overlap_4.txt`).
- 이 세 비대상 복도는 기존 승인에서 보존 대상으로 지정됐으므로 이동/삭제/비활성화하지 않았습니다. 해당 간섭 처리 방향에 대한 사용자 지시가 필요합니다. 생산 씬 적용은 저장됐지만 전체 시각 일치 확인은 미완료이며, 최종 캡처나 완료 알림은 하지 않았습니다. 실행모드는 종료했습니다.

### 2026-09-23 두 접점 수정 및 이동 검증 중단

- 위 연결부의 기존 프레임 스윕 기반 통과 서술은 사용자 후속 캡처로 무효화했습니다. 현재 상태는 **미완료**입니다.
- 실제 Unity Scene 화면에서 통제실 캡처의 세로 끝단을 기존 `CR-01 cockpit 40 degree outside only corridor side wall -0.96`, 왼쪽 면을 추가 `Connector_ControlRoom_H02/HollowBoundaryLoft`로 화면 선택하여 특정했습니다. 동력실 사선 돌출판도 추가 `Connector_EngineRoom_H01/HollowBoundaryLoft`로 확인했습니다.
- 샘플의 두 연결부 하위 조각만 실제 입구 벽 끝단과 복도 끝단을 연결하는 `FittedFloor`, `FittedCeiling`, `FittedWallMinimum`, `FittedWallMaximum`으로 교체했습니다. 동력실에는 같은 바닥 높이의 `FittedEntranceDeck`을 추가했습니다. 원본 방/입구/복도, 다른 접점, 생산 Pegasus 씬은 변경하지 않았습니다. 전체 재생성 없이 `RepairConnectorTwoContacts` 명령으로 반영했습니다.
- 수정 뒤 실제 Scene 화면에서 동력실 사선판 제거와 통제실 왼쪽 포켓을 채우는 연속 벽을 확인했으나 통과를 선언하지 않았습니다.
- 실제 실행모드에서 동일 플레이어 크기의 임시 CharacterController를 조작해 통제실 연결 벽을 따라 이동한 뒤, 기존 입구 바닥에서 낙하했습니다. 기존 입구/복도 바닥 BoxCollider가 꺼져 있었습니다. 동력실 원형 바닥에는 Collider가 없고 기존 입구 벽/복도 Collider도 꺼져 있었습니다.
- 원본 Collider 변경은 승인 범위 밖이라 변경하지 않고 실행모드를 종료했습니다. 인접 원본 충돌 범위에 대한 지시 이후 양방향 이동 검증을 이어가야 합니다. 동력실 실행모드 이동 및 두 접점 전체 시야 검증은 미완료입니다. 최종 캡처, 자동 수치 판정, 완료 선언은 하지 않았습니다.
- 최신 기록은 `docs/validation/PegasusTriRoomConnectorSample/DirectReview.txt`와 `LiveReview/`입니다. 기존 `Final.png`는 현재 형상의 완료 근거가 아닙니다.

- `docs/PLAYER_ANIMATION_REQUIREMENTS.html`에 함선 공간 천장 샘플·적용과 Pegasus 생성·내부 복원·수평 복도 길이·최종 1m 균일 배치를 기록했습니다. 기존 애니메이션 통과 상태는 변경하지 않았습니다.
- 천장 샘플 직접 확인 자료는 `artSample/ShipSpaceCeilings/Final.png`와 `Review.html`에 저장했습니다.
- Pegasus 관련 직접 확인과 수치 보고서는 `docs/validation/PegasusLayout/`, `docs/validation/PegasusInteriorRestore/`, `docs/validation/PegasusHorizontalCorridorLength/`, `docs/validation/PegasusTriRoomUniformGap/`에 생성했습니다. 이 경로는 저장소 정책에 따라 Git 생성물 커밋 대상에서는 제외됩니다.

### 이번 통합 기록에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- 공간실과 복도 사이의 별도 연결 조각 제작
- 경사 복도 위치·길이·기울기 재조정
- 기존 애니메이션 진행 행의 통과 상태 변경

### 2026-09-23 통제실–무기실–비품실 별도 연결 샘플

- 사용자 배치도에 따라 기존 상단 세 방·H01/H02/H05와 승인 연결부를 보존하고, 별도 씬 `Assets/_Project/ArtSamples/PegasusArmorySupplyConnections/PegasusArmorySupplyConnections.unity`에 통제실 아래 오른쪽 입구–H03–무기실, 무기실–H04–비품실 오른쪽 아래 입구를 구성했습니다. 무기실·비품실은 샘플 복사본 루트만 이동·회전했습니다.
- 수송자 보행 4m/s 설정으로 복도 본체 H03 16m(4초), H04 12m(3초)를 적용했습니다. 양끝 연결부 각 1m와 기존 입구는 본체 길이에서 제외했습니다. 이는 길이 기준이며 실제 보행 성공 기록이 아닙니다.
- 새 `Armory Supply Intermediate Connectors` 아래 네 접점의 독립 바닥·벽·천장과 경계 마감 메시를 생성했습니다. 직접 보인 무기실 천장 옆 틈, 복도 벽 아래 노출, 통제실 천장 끝단을 별도 메시로 보완했습니다. 원본 방·입구 메시, 원본 머티리얼, Collider는 변경하지 않았습니다.
- 실제 실행모드에서 관찰 카메라를 네 접점의 방 방향·양쪽 바닥 모서리·양쪽 천장 모서리·외부 측면·복도 내부로 조작하고 Game 화면을 직접 확인했습니다. 확인한 연결 경계에서 큰 외부 노출이나 통로를 가로지르는 새 판은 보이지 않았습니다. 조명·대상 가시성을 조작하지 않았습니다. 초기 셔터에 가린 화면은 판정 근거에서 제외했습니다.
- 기존 닫힌 셔터, 바닥 가로 리브, 기존 Collider 상태를 유지했습니다. 플레이어 양방향 통과·끼임 검증은 완료하지 않았으며 아트 시각 확인과 구분합니다. 콘솔 오류·경고는 직접 확인 후 보조 점검에서 0입니다.
- `artSample/PegasusArmorySupplyConnections/Review.html`, `README.md`, `Reference.png`, `Final.png`에 사용자 검토 자료를 작성했습니다. 별도 샘플 사용자 승인 대기이며 생산 Pegasus에는 미적용입니다.
- 원본 Pegasus 오브젝트 직렬화 비교에서 저장본과 일치한 미저장 표시를 확인했습니다. 사용자 변경이라고 단정한 이전 설명을 정정했습니다. 이번 샘플 작업으로 Pegasus를 저장하지 않았고 파일 크기 5,081,851바이트 및 기존 수정 시각 2026-09-23 14:32:46을 유지했습니다.
- 사용 명령: `CreatePegasusArmorySupplySample`, `ReviewPegasusArmorySupplySample`, `Refresh-UnityProject.ps1`. 자동 테스트·빌드·Git 커밋/푸시는 실행하지 않았습니다. 실행모드를 종료하고 별도 샘플을 열린 상태로 남겼습니다.

### 2026-09-23 같은 아트 샘플의 상단 세 복도 75% 단축

- 실제 Unity 실행모드에서 여섯 접점의 안쪽 양방향, 바닥·천장 모서리, 외부 측면·상부와 세 복도 내부를 직접 확인했습니다. 확인한 경계에서 외부 배경 노출이나 새 연결벽의 통로 관통은 보이지 않았습니다. 벽에 가려진 카메라 위치는 근거에서 제외하고 다른 위치에서 다시 확인했습니다.
- 조종실 입구 경계를 안쪽 문틀 끝으로 맞추고, 방/복도 사이에 독립 바닥·벽·천장과 경계 마감을 만들었습니다. 메시 데이터가 바뀌어도 실제 화면에 이전 형상이 남는 문제를 명시적 정점·면·노멀 갱신과 GPU 업로드로 수정한 뒤 같은 구도의 화면에서 다시 확인했습니다.
- 그 뒤 길이 보조 대조: H01 9.694756m(2.423689초), H02 14.54215m(3.635539초), H05 26.89366m(6.723414초). 기존 본체의 25%이며 4m/s 환산입니다. 연결부·방 내부 길이를 제외했습니다.
- 통제실은 위치·각도 보존, 조종실·동력실은 루트 평행 이동만 하고 각도·스케일·높이와 원본 모델을 보존했습니다. H03/H04, 무기실·비품실, 대응 천장과 하단 연결부의 직렬화 내용도 작업 전과 동일합니다.
- 별도 `PegasusArmorySupplyConnections.unity`에만 저장했습니다. 기존 상단 연결부 조각과 길이 축소로 불필요한 확장 리브를 샘플에서 교체·제거했습니다. 작업 전 씬 백업 `BeforeTriangleResize.unity.txt`에서 복구 가능합니다. Pegasus 파일 수정 시각·크기를 유지했습니다.
- 실제 플레이어 보행·끼임 검증은 하지 않았습니다. 기존 닫힌 셔터·가로 리브·원본 Collider는 유지했으며, 아트 형상 확인과 물리 통과 검증을 구분합니다.
- 컴파일 직후 다른 파일의 경고 58건을 내용까지 확인했습니다. 최종 실행모드 재진입·종료 후 콘솔 오류·경고는 0입니다. 자동 테스트·빌드·Git 변경은 하지 않았습니다.
- `TriangleReview.md`, `Review.html`, `README.md`, `TriangleFinal.png`에 근거를 기록했습니다. 실행모드를 종료하고 해당 샘플 씬의 전체 배치를 볼 수 있도록 에디터를 남겼습니다.

### 2026-09-23 승인된 연결 샘플 Pegasus 적용

- 실행모드에서 Pegasus의 상단 여섯 접점과 하단 네 접점, 바닥·천장·외측을 직접 관찰하고 샘플 동일 구도와 비교했습니다. 승인 형상과 다른 새 관통판·큰 외부 노출은 확인되지 않았습니다. 가려진 화면은 판정에서 제외했습니다.
- 다섯 방, H01~H05 및 대응 천장, 두 연결부 루트를 적용했습니다. 첫 전송의 부모 배율 중복 문제는 직접 화면에서 발견한 뒤 대상 루트 월드 크기를 보정하고 다시 확인했습니다. 공통 부모와 비대상은 보존했습니다.
- 직접 확인 후 보조 대조에서 대상 위치/크기와 비대상 직렬화 보존, 샘플 파일 해시 유지를 확인했습니다. 샘플 자산을 재생성하거나 원본 메시를 수정하지 않았습니다.
- 최종 콘솔에 Persistent 16 allocations / TransformAccessArray 8 allocations 누수 메시지 2건이 발생했습니다. 원인은 특정하지 않았으며 전체 검증 완료 판정은 보류합니다. 무관한 런타임 코드는 수정하지 않았습니다.
- 전송 전 씬은 `docs/validation/PegasusArmorySupplyConnections/TransferReview/PegasusBeforeApply.unity.txt`, 직접 확인 기록은 같은 폴더 `DirectReview.md`입니다. 이전 대상 연결부·불필요한 확장 리브를 교체/제거했습니다.
- 기존 셔터·문턱·리브·Collider 상태도 유지했습니다. 실제 플레이어 보행 검증은 하지 않았습니다. 자동 테스트·빌드·Git 작업은 하지 않았습니다. Pegasus 저장 후 실행모드를 종료하고 해당 씬을 열어 두었습니다.

### 2026-09-24 창고 연결 샘플 셔터 외형 배치

- 사용자 ‘외형만 배치’ 답변에 따라 기존 SC-S01 셔터 외형을 신규 창고 복도 다섯 개의 양 끝 총 10곳에 저장했습니다. 개폐 기능·애니메이션은 추가하지 않았습니다.
- 실제 실행모드에서 원본과 비교하고 10곳의 정면·뒷면·바닥 접합·외측 상부를 직접 확인했습니다. 가려진 무기실 뒷면은 시점을 옮겨 다시 확인했습니다. 상세 근거는 `docs/validation/PegasusCargoConnections/ShutterDirectReview.md`입니다.
- 닫힌 고정 외형이며 통과 동작 확인을 주장하지 않습니다. 기존 방·바닥·원래 복도 형상은 보존했고 Pegasus 해시는 유지됐습니다. 신규 복도 조명 15개에는 Unity 저장 시 URP 광원 부가 데이터가 직렬화됐습니다.
- 콘솔에 Persistent 16 allocations / TransformAccessArray 8 allocations 메시지가 남아 전체 완료 판정과 최종 캡처는 보류했습니다. 이전에도 있던 메시지이나 원인은 특정하지 않았습니다. 승인 범위 밖 런타임 코드는 수정하지 않았습니다.
- 실행모드를 종료했고 저장된 별도 샘플 Dirty=False를 확인했습니다. 자동 테스트·빌드·Git은 하지 않았습니다.

### 2026-09-23 창고 연결 샘플 사전 조사 — 미완료

- 실제 실행모드에서 다섯 방의 창고용 입구와 창고 입구 방향을 직접 관찰했습니다. 창고의 일부 개체 이름과 실제 방향 표기가 달라 실제 위치·표기를 기준으로 대응시켰습니다.
- 다섯 방을 고정하고 창고 이동·회전만으로 동일 연결 거리와 본체 경사 길이 18m를 만족하는 위치를 아직 확보하지 못했습니다. 보조 중심점 거리 조사만으로 불가능 또는 통과 판정을 하지 않았습니다.
- 창고 기존 입구 위치·방향 조정 허용 여부를 확인하기 전 임의로 형상을 바꾸거나 연결부를 길게 늘리지 않았습니다. 샘플 씬·신규 복도·연결부는 아직 생성하지 않았습니다.
- 조사 후 실행모드를 종료했습니다. Pegasus 파일은 조사 전후 해시가 같으며 저장·변경하지 않았습니다. 세부 기록: `docs/validation/PegasusCargoConnections/PlacementInvestigation.md`.

### 2026-09-23 창고 연결 샘플 제작 및 이동 확인 — 초기 이력

후속 정정: 사용자 지시에 따라 원래 방 바닥 수정 없이 새 연결부 소속 입구 받침 바닥으로 보완했습니다. 기존 방 변경을 위한 추가 승인이 필요하다는 판단은 철회합니다. 조종실 역방향 이동의 바닥 빠짐을 보완 후 다시 확인했고, 각 접점의 직접 시각·측면 이동 확인을 계속하고 있습니다. 아래 실패·중단 내용은 보완 전 기록이며 전체 작업은 아직 미완료입니다.

최종 후속 상태: 별도 샘플 제작 및 직접 검토를 마쳤으며 사용자 샘플 승인을 기다립니다. 신규 연결부의 받침·측면·상부 마감으로 보완했고 기존 다섯 방 바닥·충돌체는 변경하지 않았습니다. 실제 실행모드에서 다섯 경로의 양방향·양측 CharacterController 이동, 각 순방향 한 회의 전체 화면, 역방향 접점 접근과 양 끝의 내부 저시점·외측 상하를 직접 확인했습니다. 확인한 연결 경계의 외부 노출이나 통로 관통·끼임은 보이지 않았습니다. 실제 게임 플레이어 전체 동작 검증과는 구분합니다. 직접 확인 후 본체 각각 약 18m(4m/s 환산 4.5초)를 대조했습니다. 콘솔 오류는 확인되지 않았고 그림자 해상도 축소 경고는 남았습니다. 최종 캡처 및 자료는 `artSample/PegasusCargoConnections/Final.png`, `Review.html`, `README.md`입니다. 실행모드는 종료했으며 Pegasus 미적용, 자동 테스트·빌드·Git 미실행입니다.

- 사용자 답변으로 창고 입구 조정 범위를 확인한 뒤 `Assets/_Project/ArtSamples/PegasusCargoConnections/PegasusCargoConnections.unity`를 별도로 제작했습니다. 기존 다섯 방과 기존 연결의 형상·변환·충돌체는 보존했습니다.
- 실제 실행모드에서 새 경로를 이동하고 화면을 확인했습니다. 창고 바닥 아래로 떨어지던 문제는 샘플 창고의 기존 바닥 충돌을 활성화한 뒤 재확인했습니다. 동력실 기존 입구의 바닥 누락 구간은 새 연결부의 바닥으로 보완했습니다.
- 조종실 역방향 이동은 기존 방 바닥 진입 후 아래로 내려가므로 실패입니다. 원본 Pegasus를 읽기 전용으로 열어 확인한 결과 해당 바닥에 충돌체가 없습니다. 샘플 복사 누락이 아닙니다. 기존 다섯 방 수정 금지 범위 때문에 조종실 바닥 충돌 보완은 실행하지 않았습니다.
- 전체 시각·양방향·벽 가까이 이동 검토와 최종 콘솔 검토는 미완료입니다. 일부 경로 이동 성공이나 생성 성공으로 완료 판정하지 않았습니다. 기존 방 입구 바닥 충돌 보완 범위 결정이 필요합니다.
- Pegasus 원본 SHA256은 `5160199FF6EDA260648F1CDEC91CD4ADB58D49852440B6B9EAC29217E6D4BB6D`로 유지됐습니다. 실행모드를 종료했고 별도 샘플만 열려 있습니다. 자동 테스트·빌드·Git·Pegasus 반영·최종 캡처는 하지 않았습니다.
