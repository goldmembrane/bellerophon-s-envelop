# PROGRESS 2026-07-17

## Smorzando 변환 검토 개체 4초 반복 재생

- 사용자 지시에 따라 `Smorzando_Installed_03` 변환 검토 개체만 반복 재생하도록 변경했다.
- 재생 구성은 `3초 설치형 → 인간형 변환`, `최종 인간형 1초 유지`, `설치형부터 다음 주기 시작`이며 전체 주기는 `4초`다.
- 기존 변환 단계와 최종 인간형 자세는 수정하지 않았고 다른 설치형·인간형에는 반복을 적용하지 않았다.
- 첫 반복 캡처에서 다음 주기 불꽃이 사라진 상태를 확인해 해당 결과를 판정에서 제외했다.
- 원인은 Unity 도메인 리로드 뒤 초기 상태를 아직 저장하지 않은 변환 구성요소가 기본값으로 불꽃 크기와 조명을 복원한 것이었다.
- `RestoreInitialState`는 초기 상태가 저장된 경우에만 실행되도록 수정하고, 적용 도구에서 세 번째 개체의 불꽃 기준을 로컬 크기 `(1, 1, 1)`, Point Light 활성, 강도 `0.45`, 불꽃 움직임 활성으로 복구했다.
- 사용자 승인 후 보정 캡처를 추가로 한 번만 실행했다.
- 정면·사선의 `3.0`, `3.5`, `3.95초` 이미지는 뷰별 SHA-256이 동일하며 최종 인간형 자세가 `1초` 동안 그대로 유지된다.
- `4.05초` 이미지에서 불꽃과 주변광이 켜진 설치형 복귀를, `4.35초` 이미지에서 두 번째 주기의 불꽃 감쇠를 직접 확인했다.
- 여러 이미지를 동시에 여는 과정에서 `3.5초`가 일시적으로 검게 표시되었으나 저장 파일은 `3.0·3.95초`와 SHA-256이 같았고, 단독으로 다시 열어 동일한 인간형 화면임을 확인했다.
- 자동 캡처는 Scene View 포커스와 수동 재생을 사용하지 않았고 씬을 저장하지 않았으며 종료 시 Unity 선택을 해제했다.
- 상세 기록과 시각 자료는 `docs/validation/smorzando_installed_transform_2026-07-17/`에 저장했다.
- Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## Smorzando 설치형 → 인간형 3초 변환 모션

- 사용자 확인에 따라 `Smorzando_Installed_03`을 설치형 → 인간형 변환 검토 개체로 사용했다.
- 기본 변환 길이는 `3초`이며 이후 사용자 지시로 검토 개체에서만 최종 자세 `1초` 유지 후 반복하도록 확장했다.
- `0~0.5초`에 기존 모델 불꽃과 주변광이 약해지고 `0.35초`에 작은 붉은 잔불을 남긴 뒤 `0.65초`에 완전히 꺼진다.
- `0.5~1.8초`에 바닥 촛농 정점이 중앙으로 끌려와 위쪽으로 접히며 양초 덩어리로 모인다.
- `1.8~3.0초`에 인간형 Bake Mesh가 낮은 양초 기둥 형태에서 아래쪽부터 원래 인체 실루엣으로 펼쳐진다.
- 변환 중 설치형과 인간형은 런타임 임시 Mesh 복사본만 사용하며 원본 FBX는 수정하지 않았다.
- 최종 프레임에서는 설치형 왁스와 불꽃이 사라지고 오른쪽 `Smorzando_Person_01`과 같은 인간형 SkinnedMeshRenderer가 남는다.
- 최종 인간형 Transform은 기준과 동일한 로컬 위치 `(0, 0.313106, 0)`, Y축 `180도`, 크기 `(1, 1, 1)`이다.
- 자동 정면·사선 캡처에서 `잔불 → 완전 소화 → 촛농 집결 → 양초 덩어리 → 인간형 전개 → 최종 인간형` 순서를 직접 확인했다.
- 최종 비교 이미지에서 변환 결과와 기준 인간형의 외형, 크기, 방향, 바닥 정렬이 일치한다.
- 두 번째 설치형 대기 모션, 설치형 세 개의 불꽃 모션 구성, 기존 스모르찬도와 Player Transform은 변경되지 않았다.
- 상세 기록과 시각 자료는 `docs/validation/smorzando_installed_transform_2026-07-17/`에 저장했다.
- 캡처는 Scene View 포커스와 수동 재생을 사용하지 않았고 종료 시 Unity 선택을 해제했다.
- Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## Smorzando 설치형 대기 모션 개체 범위 수정

- 사용자 확인에 따라 설치형 세 개 중 `Smorzando_Installed_02`만 몸체 대기 모션 개체로 확정했다.
- 기존 구현이 몸체 모핑과 불꽃 움직임을 한 컴포넌트에서 처리해 세 설치형 모두에 호흡·물결·상하 움직임이 적용되던 문제를 수정했다.
- `SmorzandoInstalledIdleMotion`은 두 번째 모델에만 남겨 몸체 호흡, 촛농 물결, 전체 상하 움직임과 불꽃 앵커의 몸체 추종만 담당한다.
- 새 `SmorzandoInstalledFlameMotion`은 불꽃 외곽 Shell의 공기 흐름 반응과 Point Light 깜박임을 담당하며 세 설치형 모두에 적용했다.
- 첫 번째·세 번째는 몸체만 정지하고 모델 불꽃, 검은 심지, 밀착 외곽 Shell, 깜박임, 주변광을 유지한다.
- 적용 결과 몸체 대기 모션 구성요소는 `1개`, 불꽃 모션 구성요소는 `3개`이며 기존 스모르찬도 및 Player Transform은 변경되지 않았다.
- 자동 캡처의 동일 구도 `0.00초`·`0.80초` 이미지를 직접 비교해 첫 번째·세 번째 몸체는 같은 자세를 유지하고 두 번째 몸체만 높이와 촛농 표면이 변하는 것을 확인했다.
- 두 시점 모두 세 개체의 불꽃이 심지에 붙어 켜져 있는 것을 확인했다.
- 자동 시각 자료는 `docs/validation/smorzando_installed_idle_2026-07-17/automated_visual_capture/`에 저장했다.
- 캡처는 Play Mode와 Scene View 포커스를 사용하지 않았고 종료 시 Unity 선택을 해제했다.
- Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## Grave 공격 모션 21:40:18 영상 기준 별도 복구 및 실제 화면 확인

- 사용자 지정 영상 `C:/Users/gus68/Videos/Captures/Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-16 21-40-18.mp4`를 복구 기준으로 사용했다.
- 영상은 `1920x1000`, `6.935233초`, 233프레임이며, 2026-07-16에 추출된 `user_capture_214018_*` 프레임과 접촉 시트를 다시 대조했다.
- 첫 복구에서는 위팔 `(-0.58,-0.64,0.50)`, 아래팔 `(-0.65,-0.55,0.52)`를 영상 속 값으로 잘못 판단했다. 해당 값은 영상 촬영 뒤 `UntwistedTorsoClearSlash`에서 적용한 후속 수정값이므로 첫 작업 클립 상태는 폐기했다.
- 영상은 `ApplyContinuousBodyFrontSlash_Final` 적용 직후이자 `UntwistedTorsoClearSlash` 적용 전 상태임을 다시 확인했다.
- 영상 당시의 굽힌 커튼콜 방향인 위팔 `(-0.45,-0.86,0.24)`, 아래팔 `(-0.82,0.18,0.54)`를 작업 클립에 복원했다.
- `1.20초` 옆 전개, `1.28~1.58초` 연속 베기, `1.58~2.35초` 커튼콜 자세 유지에 더해 `2.65초` 중간 복귀와 `3.00초` 시작 자세 복귀를 복원했다.
- `2.65초`와 `3.00초`를 커튼콜 자세로 고정했던 2026-07-16 후속 변경은 본 클립에 유지하고, 이번 별도 작업 클립에서만 제거했다.
- 기존 본 클립의 `clip.ClearCurves()` 재실행을 피하기 위해 별도 작업 클립과 컨트롤러를 생성했다.
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep_Working.anim`
  - `Assets/_Project/Art/Enemies/Grave/Controllers/Grave_Attack_CurtainCall_Sweep_Working.controller`
- 전용 `ApplyRestoredGraveAttackFromUserVideo214018` 명령은 기존 낫 메시를 재생성하지 않고, `Approved Grave Enemy Placement/Grave_03_Attack_RightArm_GiantSweep/Grave_Model`의 Animator만 작업 컨트롤러로 교체한다.
- `Refresh-UnityProject.ps1` 뒤 전용 Apply 명령을 다시 실행해 수정된 작업 클립을 적용했다.
- 작업 컨트롤러 씬 참조는 지정 공격 슬롯 모델 Animator의 1건뿐이며, 작업 컨트롤러의 작업 클립 참조도 1건이다.
- 본 공격 클립 SHA-256은 작업 전후 모두 `2D2F03B7ACA5728E4931BA8E3B9047B9CC000AB254B2FF57EEC011636EF45925`로 동일하다.
- Unity Hierarchy에서 지정 공격 슬롯을 직접 선택하고, Play Mode의 Scene View에 가까이 포커스한 실제 화면을 8초 녹화했다.
- `current_reference_motion_focused_final.mp4`와 0.1초 간격 `current_final_*.jpg` 접촉 시트를 기준 영상의 `reference_214018_*.jpg`와 직접 비교했다.
- 실제 화면에서 기준 영상과 같은 `옆 전개 → 빠른 몸통 안쪽 베기 → 굽힌 커튼콜 유지 → 기본 자세 복귀`가 3초 주기로 반복되는 것을 확인했다. 이 판정에는 스크립트 수치를 사용하지 않았다.
- Play Mode는 실제 화면 녹화 뒤 종료했다.
- 전용 기록, 적용 로그, 기준·현재 화면 자료는 `docs/validation/grave_attack_restore_2026-07-17/`에 저장했다.
- 현재 상태는 사용자 시각 검토 대기다. 작업 클립을 본 클립으로 교체하지 않았고 승인 완료로 처리하지 않는다.
- 실행하지 않은 항목: Unity 재시작, 지정 공격 슬롯 밖 변경, 모델·메시·머티리얼·텍스처·전투 로직 변경, 승인 범위 밖 검증·테스트·빌드, Git 작업.

## Grave 공격 커튼콜 끝 오른팔 소폭 하강

- 사용자가 말한 공격 모션의 끝은 전체 클립 끝이 아니라 `커튼콜 자세의 끝 부분`임을 확인한 뒤 작업했다.
- 별도 작업 클립에서만 `2.15초`까지 기존 커튼콜 자세를 유지하고, `2.15~2.35초`에 `RightArm` 방향을 모델 기준 아래쪽으로 `5도` 부드럽게 내렸다.
- 아래팔·손·낫은 자식 체인으로 함께 이동하므로 기존 팔꿈치 굽힘과 손목 형태는 유지했다. 왼팔, 몸통, 낫 전개, 베기 속도는 수정하지 않았다.
- 기존 `2.65초` 중간 복귀와 `3.00초` 시작 자세 복귀는 낮아진 `2.35초` 자세를 기준으로 다시 이어지게 했다.
- `Refresh-UnityProject.ps1` 뒤 `ApplyRestoredGraveAttackFromUserVideo214018`을 실행해 지정 공격 슬롯에 다시 적용했다.
- Unity Hierarchy에서 `Grave_03_Attack_RightArm_GiantSweep`를 직접 선택하고 Play Mode Scene View에 포커스한 실제 화면을 8초 녹화했다.
- 실제 녹화 `current_curtain_ending_lower5deg_focused.mp4`와 0.05초 간격 확대 시트 `current_curtain_ending_lower5deg_detail_cycle1.jpg`, `current_curtain_ending_lower5deg_detail_cycle2.jpg`를 직접 대조했다.
- 두 반복 주기 모두 기존 커튼콜 유지 뒤 오른팔 체인만 소폭 아래로 이동하고, 꺾임이나 역방향 들림 없이 복귀로 이어지는 것을 실제 화면에서 확인했다.
- 확인 뒤 Play Mode를 종료했으며 지정 공격 슬롯의 선택·포커스 상태는 유지했다.
- 작업 클립 최종 SHA-256은 `B056F97932E4CC017E8C713C4E5FF63AD915A77EC20BA67DD43A63E93BCC15E3`이다.
- 본 공격 클립 SHA-256은 `2D2F03B7ACA5728E4931BA8E3B9047B9CC000AB254B2FF57EEC011636EF45925`로 변하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 본 클립 교체나 승인 완료로 처리하지 않는다.

## Unity 포커스 없는 자동 시각 검증 도입

- 이후 애니메이션 작업을 Unity Scene View 직접 선택·포커스나 Play Mode 수동 재생으로 판정하지 않도록 `CaptureGraveAttackCurtainEndingFrames` 자동 렌더 캡처를 추가했다.
- 작업 클립을 `1.58`, `2.15`, `2.25`, `2.35`, `2.65`, `3.00초`에 샘플링하고, 임시 Bake Mesh·정면 직교 카메라·조명으로 동일 구도의 `720x720` PNG 여섯 장을 생성한다.
- 임시 오브젝트는 `HideAndDontSave`이며 포즈, Animator 활성 상태, BlendShape 값을 원래대로 복원한다. 씬 저장과 Scene View 포커스는 수행하지 않는다.
- 첫 실행은 Unity 6.3에 없는 `EditorSceneManager.MarkSceneClean` 호출로 컴파일이 중단됐다. 해당 호출을 제거하고 캡처 전후 dirty 상태 변화 시 실패하도록 수정했다.
- 사용자가 Unity 새로고침을 수행한 뒤 자동 캡처 명령이 정상 완료됐다.
- 자동 렌더 폴더: `docs/validation/grave_attack_restore_2026-07-17/automated_visual_capture/`
- 접촉 시트 `Grave_Attack_CurtainEnding_ContactSheet.png`와 개별 원본을 직접 비교했다.
- `2.15 → 2.25 → 2.35초`에 오른팔 체인이 급격한 꺾임 없이 아래쪽·몸 안쪽으로 소폭 이동하고, `2.65 → 3.00초`에 기존 복귀로 이어지는 것을 시각적으로 확인했다.
- 이 판정에는 Unity 화면 녹화나 모션 수치 임계값을 사용하지 않았다.
- 자동 캡처 후 Unity 개체 선택은 남기지 않았으며 본 클립과 작업 클립은 수정되지 않았다.

## Grave 뒤로 쓰러지는 사망 모션

- 사용자 지정에 따라 `Grave_05_Death`에 발을 고정한 채 상체부터 뒤로 곧게 쓰러지는 `1.3초` 작업용 사망 모션을 추가했다.
- `0.0~1.0초`에 뒤로 낙하하고 `1.0~1.3초`에는 동일한 최종 자세를 유지해 반동을 제거했다.
- `Hips` 전신 회전·위치 곡선으로 발의 수평 위치를 기준 삼고, 각 시점의 Bake Mesh 최저점을 바닥 높이에 맞췄다.
- 마지막에는 팔이 몸통 양옆을 따라 펴지고 다리는 곧고 평행하게 정렬된다.
- 낫 BlendShape가 존재할 경우에도 전 구간 `0`으로 유지하며 클립은 비루프다.
- 작업 클립 `Grave_Death_BackFall_Working.anim`과 작업 컨트롤러 `Grave_Death_BackFall_Working.controller`를 생성해 `Grave_05_Death/Grave_Model` Animator 한 곳에만 연결했다.
- 첫 자동 렌더에서 기본 리그의 굽은 팔다리가 마지막에 겹쳐 보이는 문제를 확인해 해당 결과를 채택하지 않았다.
- 팔다리 정 자세 곡선을 추가하고 다시 적용한 뒤, 임시 바닥 면이 포함된 측면·사선 자동 렌더를 재생성했다.
- 최종 측면 시트에서 발 기준 뒤쪽 낙하와 `1.0초` 등 바닥 접촉을 확인했다.
- 최종 사선 시트에서 정면 무늬가 위로 향하고 팔은 몸 옆, 다리는 곧고 평행하며 낫 형상이 없음을 확인했다.
- `1.0초`와 `1.3초` 프레임은 동일해 반동이 없다.
- 자동 시각 자료와 기록은 `docs/validation/grave_death_backfall_2026-07-17/`에 저장했다.
- 작업 종료 시 Unity 선택과 포커스를 남기지 않았다.
- 기존 공격 작업 클립과 본 공격 클립 해시는 변하지 않았다.
- Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## Smorzando 모델 불꽃·이펙트 결합 재제작

- 사용자 확인에 따라 원본 모델의 입체 불꽃을 밝은 중심으로 사용하고 하단 심지는 검은색으로 남겼다.
- 원본 Mesh가 불꽃·심지·양초 몸체까지 연결돼 있어 높이별 삼각형 군집을 조사했다.
- 첫 분할은 주황색 영역이 너무 아래까지 내려가 심지가 가려져 폐기하고, 불꽃 시작 높이를 로컬 Z `0.00215`로 올렸다.
- 원본 FBX를 덮어쓰지 않고 모델 불꽃 삼각형 `193개`·정점 `347개`, 심지 삼각형 `129개`·정점 `315개`의 파생 Mesh를 생성했다.
- 모델 불꽃에는 노란색·주황색 발광 재질, 심지에는 검은 비발광 재질을 적용했다.
- 기존 교차 평면 4개를 제거하고 모델 불꽃과 같은 형상을 `1.08배`로 감싼 반투명 외곽 Shell로 교체했다.
- 모델 중심과 심지는 호흡하는 몸체를 따라가고 외곽 Shell만 작게 흔들리도록 모션 제어를 분리했다.
- 범위 `2m`, 기본 세기 `0.45`, 밝기 흔들림 `12%`, 그림자 없음의 주변 Point Light는 유지했다.
- 설치형 3개의 위상과 기존 호흡·물결·상하 움직임 수치는 변경하지 않았다.
- 자동 확대 시트에서 입체 불꽃 중심, 검은 심지, 밀착된 외곽 Shell을 직접 확인했다.
- 세 개체 비교와 실제 Player Main Camera 화면에서도 세 불꽃이 심지 위치에 붙어 있고 주변광이 보인다.
- 파생 Mesh·재질·VFX는 사용자 예외 승인에 따라 별도 `artSample/` 없이 Unity에 직접 적용했다.
- 상세 기록과 시각 자료는 `docs/validation/smorzando_hybrid_flame_2026-07-17/`에 저장했다.
- 원본 FBX, 기존 호흡 수치, 설치형 배치, Player 시작점, 좀비형, 다른 적대 개체, Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 변경하거나 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## Smorzando 설치형 대기 모션

- 사용자 확인에 따라 바닥에 퍼진 촛농과 중앙 양초 전체를 하나의 몸체로 취급했다.
- 원본 단일 Mesh의 로컬 Z 높이와 중심 반경을 이용하는 `SmorzandoInstalledIdleMotion`을 추가했다.
- 원본 FBX는 덮어쓰지 않고 재생 시 임시 Mesh 복사본만 변형한다.
- `3.2초` 주기 동안 중앙 몸체는 최대 `0.8%`, 바닥 촛농은 최대 `0.4%` 수평 호흡하며 중심에서 외곽으로 최대 `6mm` 방사형 물결이 흐른다.
- 촛농 전체는 최대 `8mm` 위아래로 부드럽게 들썩이며 반동은 없다.
- 설치형 3개의 위상은 `0초`, `1.0667초`, `2.1333초`로 나눴다.
- 최상단 심지 위치에 주황색 외곽·노란색 중심의 교차 평면 불꽃 VFX를 추가했다.
- 불꽃은 몸체를 따라 움직이며 범위 `2m`, 기본 세기 `0.45`, 그림자 없음의 따뜻한 Point Light로 주변을 은은하게 밝힌다.
- 불꽃 VFX는 사용자 예외 승인에 따라 별도 `artSample/` 없이 Unity에 직접 적용했다.
- `CaptureSmorzandoInstalledIdleFrames`로 Play Mode와 Scene View 포커스 없이 `3.2초`, `10fps`, `32프레임` 반복 영상을 생성했다.
- 주요 프레임 시트에서 바닥 물결과 전체 상하 움직임이 작게 이어지고, 세 개체 비교 화면에서 서로 다른 위상과 불꽃 기울기가 나타나는 것을 직접 확인했다.
- 실제 Player Main Camera 화면에서도 설치형 3개의 불꽃과 따뜻한 주변광이 모두 보인다.
- 기존 스모르찬도와 Player Transform은 변경되지 않았고 캡처 종료 시 임시 Mesh를 제거하고 Unity 선택을 해제했다.
- 상세 기록과 시각 자료는 `docs/validation/smorzando_installed_idle_2026-07-17/`에 저장했다.
- 원본 FBX, 좀비형, 변환·AI·전투 로직, 다른 적대 개체, Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 변경하거나 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## Smorzando 레퍼런스 색상 적용

- 사용자 지정 레퍼런스 `image/smorzando(스모르찬도).png`와 `image/smorzando-person.png`에서 갈색 왁스 팔레트를 추출해 최종 FBX에 바로 적용했으며 별도 `artSample`은 만들지 않았다.
- 설치형 전용 URP Lit 재질과 좀비형 전용 재질·왁스 알베도 텍스처를 생성했다.
- 설치형 3개 렌더러와 좀비형 5개 렌더러에 각각 같은 전용 재질을 적용했다.
- 설치형은 UV0가 없고 두 모델 모두 단일 서브메시·재질 슬롯이므로, 설치형은 단일 적갈색과 광택으로 표현하고 좀비형만 UV 알베도를 적용했다.
- 장면의 적색 조명에서 분홍색으로 뜨는 현상을 줄이도록 조명 반응 기본색과 갈색 보존 발광값을 조정했다.
- 실제 Player Main Camera 캡처에서 설치형 3개는 적갈색, 좀비형 5개는 짙은 갈색으로 표시되고 기존 회색 재질이 남지 않은 것을 확인했다.
- 원본 모델 구조상 불꽃·심지·눈·입을 별도 색으로 분리하려면 UV 또는 서브메시·재질 슬롯 변경이 추가로 필요하다.
- 적용 전후 Transform 스냅샷을 비교해 스모르찬도 배치와 Player 시작점이 변경되지 않았음을 확인했다.
- 자동 시각 캡처는 Play Mode와 Scene View 포커스를 사용하지 않았고 종료 시 Unity 선택을 해제했다.
- 상세 기록과 시각 자료는 `docs/validation/smorzando_reference_colors_2026-07-17/`에 저장했다.
- 원본 이미지와 FBX 형상·UV·리그·애니메이션, 애니메이션·AI·전투 로직, 다른 적대 개체, Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 변경하거나 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## Grave 사망 모션 검토 개체 전용 반복 재생

- 사용자 지정에 따라 사망 클립 자체는 비루프로 유지하고 `Grave_05_Death/Grave_Model` 검토 개체의 작업 컨트롤러에만 반복을 적용했다.
- `DeathBackFall → DeathBackFall` 자기 전이는 정규화 종료 시점 `1.0`, 전이 시간 `0초`, 오프셋 `0`이므로 `1.3초` 모션이 끝나면 즉시 첫 자세로 돌아간다.
- `ApplyGraveDeathReviewLoop`는 기존 사망 클립을 재생성하거나 곡선을 수정하지 않고 컨트롤러 전이만 저장한다.
- 실제 반복 여부는 `CaptureGraveDeathReviewLoopFrames`로 검토 개체 Animator와 작업 컨트롤러를 `120fps` 간격으로 진행해 자동 렌더했다.
- 첫 주기 `0.00 → 0.80 → 1.29초`와 두 번째 주기 `1.31 → 2.10 → 2.59초`를 같은 측면 구도로 비교했다.
- 접촉 시트에서 두 주기 모두 `서기 → 뒤로 낙하 → 등을 바닥에 붙인 자세`가 동일하게 나타났고, `1.29초` 종료 자세 직후 `1.31초`에 선 자세로 재시작했다.
- 컨트롤러 정규화 시간도 `0.992 → 0.008`로 전환된 뒤 두 번째 주기의 `0.615`, `0.992`까지 다시 진행됐다.
- 사망 작업 클립 SHA-256은 `ADB7BE792AC3263E3171861B8BCB84D6DA0FB149F081E199CC58EDAC8F922BF0`로 변하지 않았다.
- 반복 전이가 추가된 사망 작업 컨트롤러 SHA-256은 `DA3D0E39E46D03E6ACEB4490CF46E47501C4680D7A39C368A7EE97915AF596A8`이다.
- 자동 시각 자료는 `docs/validation/grave_death_backfall_2026-07-17/automated_visual_capture/review_loop/`에 저장했다.
- 작업 종료 시 Unity 선택과 Scene View 포커스를 남기지 않았다.
- Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## Smorzando 설치형 3개·좀비형 5개 씬 배치

- 사용자 지정 FBX `enemies model/smorzando.fbx`와 `enemies model/smorzando person.fbx`를 스모르찬도 전용 Unity 모델 경로에 원본 그대로 복사했다.
- 원본과 Unity 복사본의 SHA-256은 설치형 `4FE8F0D8F303A6D32D2F9737C36069B37E1FFF5F432F89D0957C69D0E41351DC`, 좀비형 `CDE712400B52965CF0BE0E3D32FB5B59D9349C8C24F777CE7EEFA97233C75BF9`로 각각 일치한다.
- `CargoRunMvp`에 `Approved Smorzando Enemy Placement` 루트를 생성했다.
- 첫 설치형은 Grave의 X/Y를 유지하고 Z축 음의 방향으로 Longa Arma–Tergo 간격 `7.376999m`만큼 이동한 `(57.86535, 0, -104.367)`에 배치했다.
- 설치형 3개를 X축 양의 방향으로 배치하고 세 번째 설치형 오른쪽부터 좀비형 5개를 이어서 총 8개를 한 줄로 배치했다.
- 실제 렌더러 bounds를 기준으로 모든 인접 개체 사이에 `0.5m` 간격을 유지했다.
- 설치형은 Unity 임포트 시 `0.018474 × 0.019022 × 0.005362m`로 들어오는 단위·축 차이가 있었다.
- 첫 무보정 캡처에서는 설치형이 점처럼 보여 결과를 채택하지 않았다.
- 설치형 인스턴스에 `100배` 단위 보정 후 X축 `+90도`를 적용한 중간 결과는 중앙 초가 아래로 향해 채택하지 않았다.
- 최종적으로 설치형 인스턴스에만 `100배`, X축 `-90도` 보정을 적용해 촛농 면이 바닥에 놓이고 중앙 초가 위로 솟게 했다.
- 최종 bounds는 설치형 `1.847359 × 0.536204 × 1.902153m`, 좀비형 `1.944767 × 2.495209 × 2.57404m`이며 누락 머티리얼은 없다.
- `CaptureSmorzandoScenePlacementFrames`로 Unity 선택·Scene View 포커스 없이 정면·사선·상단 자동 렌더를 생성했다.
- 최종 접촉 시트에서 설치형 3개와 좀비형 5개, 바닥 정렬, 방향, X축 비중첩을 직접 확인했다.
- 자동 시각 자료와 상세 기록은 `docs/validation/smorzando_scene_placement_2026-07-17/`에 저장했다.
- 작업 종료 시 Unity 선택과 Scene View 포커스를 남기지 않았다.
- 스모르찬도 애니메이션·변환 기능·AI·전투 로직, Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.

## Smorzando 정면 Player 시작점

- 게임 실행 시 스모르찬도 설치형 3개와 좀비형 5개 전체를 정면으로 볼 수 있도록 Player 시작 위치를 이동했다.
- 스모르찬도 전체 bounds와 실행 카메라 FOV를 기준으로 필요한 거리를 계산했다.
- Player 최종 위치는 `(66.77488, 0, -117.163)`, 실행 카메라 위치는 `(66.77488, 1.62, -117.163)`이다.
- 카메라의 기존 높이 `1.62m`와 Player 프리팹 내부 로컬 Transform은 유지했다.
- 시선 대상은 스모르찬도 전체 bounds 중심 `(66.32657, 1.247604, -104.2744)`이며 거리는 `12.89647m`다.
- 실행 카메라의 수직 FOV `60도`, 수평 FOV `91.493도`에서 8개 전체가 들어오도록 배치했다.
- 첫 시도는 씬에 이름이 `Main Camera`인 개체가 없어 적용 전에 중단됐고 Player와 씬은 변경되지 않았다.
- 실행 카메라가 Player 프리팹 내부에 `MainCamera` 태그로 존재함을 확인한 뒤 태그·계층 기준으로 연결했다.
- `CaptureSmorzandoPlayerStartView`로 저장된 실행 Main Camera 화면을 직접 렌더했다.
- 실제 화면에서 스모르찬도 8개가 가장 가까운 중앙 행에 모두 보이는 것을 확인했다.
- 기존 적대 개체 검토 행은 변경하지 않아 스모르찬도 뒤쪽 배경에 그대로 보인다.
- 시작 화면과 기록은 `docs/validation/smorzando_scene_placement_2026-07-17/player_start_view/`에 저장했다.
- 스모르찬도와 기존 적대 개체 Transform은 변경하지 않았고 Unity 선택·Scene View 포커스를 남기지 않았다.
- Unity 재시작, 하네스 검증, 범위 밖 검증·테스트·빌드, Git 작업은 실행하지 않았다.
- 현재 상태는 사용자 시각 검토 대기이며 승인 완료로 처리하지 않는다.
