# PROGRESS 2026-07-20

## 최신 완료 - 오스티나토 폐합 구간 이동거리 재배치 및 전체 2배속 통일

- 전체 공격 Animator 속도를 `2배속`으로 통일하고, 기존 `53~93프레임` 추가 가속 Behaviour를 Controller에서 제거했다. 전 구간에 별도 속도 배율은 없다.
- FBX importer 표식을 `Bellerophon.OstinatoForwardSlashMotion.v3`로 올렸다. `LeftArm`, `LeftForeArm`, `RightArm`, `RightForeArm`의 회전 12개 바인딩만 대상으로 원래 `53~93프레임`의 시작·끝 자세와 총 회전 호를 유지했다.
- 폐합 구간 안의 프레임별 이동거리만 재배치해 `53~73프레임` 동안 축별 최단 회전 호를 선형으로 소진하고, `73~93프레임`에는 93프레임 타격 종점 자세를 유지한다. 이로써 처음부터 안으로 베는 속도가 나고 타격 직전에 느려지는 기 모으기형 진행을 제거했다.
- 실시간 기준 활성 베기 시간은 `0.166667초`다. 54프레임 양손 평균 속도는 이전 v2 균일 2배속 환산 `2.5521`에서 `4.432291`로 `1.736723배` 증가했고, 활성 구간 평균은 `3.805793`, 초기/평균 비율은 `1.164617`이다.
- 시작·끝 손 변위는 왼손 `0.655438`, 오른손 `0.650829`이며 이전과 같은 53·93프레임 자세를 유지한다. 12개 대상 커브의 공통 진행률 최대 오차는 `0.000015`, 목표 타이밍 오차는 `0.000002`, 최단 회전 호 오차는 `0도`다.
- 최종 커브 지문은 `8AE54082F7FACDC70189233CEBEABFC0047CD6D25974430060DE00BF58FDFC81`이다. 비대상 커브 지문은 `FA28500CB635103B310340574C0D20FEB8B9A662A01F7D14CDC9F56C1726C289`로 유지했다.
- 손목·손·칼날·위치·스케일 커브와 모델·메시·머티리얼·본 계층은 수정하지 않았다. 외부 원본과 Unity FBX SHA-256은 모두 `0430EEE08D7D2AFBC645ECD3DFDA7B37C1DBF7BB4A6A34AF761E9AFAD1708260`이다.
- 수치 점검 통과 후 Unity Play Mode 실제 Animator를 41개 연속 시점, 정면·3/4로 최종 1회 캡처했다. 승인 외형이 유지됐고 손목·칼날 분리나 비정상 스케일 변형은 보이지 않았으며 관찰된 루트 이동과 회전은 모두 `0`이다.
- 적용·속도 CSV·점검 기록은 `docs/validation/ostinato_attack_forward_slash_motion_2026-07-20/`, 최종 이미지는 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/Ostinato_AttackAppearanceComparison.png`에 저장했다.
- 실행한 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackForwardSlashMotion`, `AnalyzeOstinatoAttackForwardSlashVelocity`, `InspectOstinatoAttackForwardSlashMotion`, `CaptureOstinatoAttackForwardSlashMotion`이다. 최종 캡처 명령은 한 번만 실행했다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 전방 폐합 감속 제거

- 사용자 제공 `22-50-52.mp4`와 현행 클립을 대조했다. 좌우 타격 순서가 아니라 손이 모이는 실제 폐합 속도가 타격 직전 `13.13754 → 0.077641`로 떨어지는 것이 기 모으기처럼 보이는 원인이었다.
- FBX importer 표식을 `Bellerophon.OstinatoForwardSlashMotion.v2`로 올리고, `53~93프레임`의 `LeftArm`, `LeftForeArm`, `RightArm`, `RightForeArm` 회전 12개 바인딩에 하나의 공통 진행률을 적용했다. 좌우 선행 차이는 만들지 않았다.
- 원본 53·93프레임 자세와 축별 최단 회전 경로는 유지하면서 Unity 실제 손 간격과 실효 배속을 기준으로 진행률을 재배치했다. Controller 기본 `2배속`, 대상 구간 실효 `6→10배속`은 변경하지 않았다.
- 최종 폐합 속도는 `12.87475~13.81359`, 3프레임 이동평균 최대 하락은 `1.8079%`다. 54프레임은 `13.10483`, 93프레임은 `13.22503`으로 타격점까지 속도가 유지된다.
- 최종 커브 지문은 `7718569E22C44275CCF240957B01BBC291C121F1944BDB2E314D4CC55EAFD4BD`다. 좌우 공통 진행률 최대 오차는 `0.000015`, 최단 회전 경로 오차는 `0도`, 프레임당 최대 관절 회전은 `4.009729도`다.
- 비대상 커브 지문은 `FA28500CB635103B310340574C0D20FEB8B9A662A01F7D14CDC9F56C1726C289`로 유지됐다. `52→53` 경계는 `1.51281도`, `93→94` 경계는 `3.717559도`다.
- 손목·손·칼날·위치·스케일 커브와 외부 원본 FBX는 수정하지 않았다. 외부 원본과 Unity FBX SHA-256은 모두 `0430EEE08D7D2AFBC645ECD3DFDA7B37C1DBF7BB4A6A34AF761E9AFAD1708260`이다.
- 수치 점검 통과 후 Unity Play Mode 실제 Animator를 41개 연속 시점, 정면·3/4로 최종 1회 캡처했다. 손목·칼날 분리나 메시 변형은 보이지 않았고 관찰된 루트 이동과 회전은 모두 `0`이다.
- 적용·속도 CSV·점검 기록은 `docs/validation/ostinato_attack_forward_slash_motion_2026-07-20/`, 최종 이미지는 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/Ostinato_AttackAppearanceComparison.png`에 저장했다.
- 실행한 명령은 영상 프레임 추출, `Refresh-UnityProject.ps1`, `AnalyzeOstinatoAttackForwardSlashVelocity`, `ApplyOstinatoAttackForwardSlashMotion`, `InspectOstinatoAttackForwardSlashMotion`, `CaptureOstinatoAttackForwardSlashMotion`이다.
- 하네스 검증 및 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 전방 베기 53~93프레임 연속 가속

- 사용자 제공 `21-59-42.mp4`와 기존 전 프레임 분석을 대조했다. 원본 `53~84프레임`은 프레임당 양손 이동량이 작고 `85~93프레임`에서 이동량이 커지므로, 전 구간에 같은 실효 `6배속`을 적용해도 앞부분이 기를 모으는 동작처럼 읽히는 상태였다.
- Controller 기본 속도 `2`는 유지하고, `53~93프레임`의 추가 배율을 `3→5`로 단조 증가시켜 실효 속도가 `6→10배속`으로 계속 상승하도록 변경했다.
- 주요 측정값은 53프레임 `6`, 70프레임 `7.144708`, 84프레임 `8`, 85프레임 `8.068587`, 90프레임 `9.481482`, 93프레임 `10`이다. `94프레임`부터는 즉시 전체 기본 `2배속`으로 복귀한다.
- 공격 개체 GlobalObjectId, 전체 `0~196프레임`, `60fps`, 반복 설정, 원본 커브 바인딩 `60개`, 커브 지문 `1D8CC5051AEF6698CFC6B484BF1280B2FA8B1EDF08967426BF268B89C1E8B2E4`를 유지했다.
- 원본 FBX·AnimationClip 커브·키 시간·포즈·모델·메시·머티리얼·본·몸체·손목·칼날은 수정하지 않았고 다른 오스티나토 슬롯도 변경하지 않았다.
- 수치 점검 통과 후 Unity Play Mode 실제 Animator를 41개 연속 시점, 정면·3/4로 최종 1회 캡처했다. 승인 외형이 유지됐고 관찰된 모델 루트 이동과 회전은 모두 `0`이다.
- 적용·점검 기록은 `docs/validation/ostinato_attack_downstrike_acceleration_2026-07-20/`, 최종 이미지는 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/Ostinato_AttackAppearanceComparison.png`에 갱신했다.
- 실행한 명령은 영상 프레임 추출, `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackDownstrikeAcceleration`, `InspectOstinatoAttackDownstrikeAcceleration`, `CaptureOstinatoAttackDownstrikeAcceleration`이다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 다른 씬·개체·Controller 수정, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 전체 2배속·양팔 모으기 실효 6배속

- Controller 기본 속도를 `3`에서 `2`로 변경하고, 원본 `53~93프레임`의 추가 속도 배율을 `2.5`에서 `3`으로 변경해 양팔 전방 모으기 전체를 실효 `6배속`으로 맞췄다.
- 원본 `0~52프레임`과 `101~196프레임`은 실효 `2배속`이며, `94~100프레임`은 `6→2배속`으로 복귀한다.
- 수치 점검에서 53·70·84·85·90·93프레임의 실효 속도는 모두 `6`, 94프레임은 `5.778426`, 100프레임은 `2`로 확인됐다.
- 공격 개체 GlobalObjectId, 전체 `0~196프레임`, `60fps`, 반복 설정, 원본 커브 바인딩 `60개`, 커브 지문 `1D8CC5051AEF6698CFC6B484BF1280B2FA8B1EDF08967426BF268B89C1E8B2E4`를 유지했다.
- 원본 FBX·AnimationClip 커브·키 시간·포즈·모델·메시·머티리얼·본·몸체·손목·칼날은 수정하지 않았고 다른 오스티나토 슬롯도 변경하지 않았다.
- 수치 점검 통과 후 Unity Play Mode 실제 Animator를 41개 연속 시점, 정면·3/4로 최종 1회 캡처했다. 승인 외형이 유지됐고 관찰된 모델 루트 이동과 회전은 모두 `0`이다.
- 적용·점검 기록은 `docs/validation/ostinato_attack_downstrike_acceleration_2026-07-20/`, 최종 이미지는 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/Ostinato_AttackAppearanceComparison.png`에 갱신했다.
- 실행한 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackDownstrikeAcceleration`, `InspectOstinatoAttackDownstrikeAcceleration`, `CaptureOstinatoAttackDownstrikeAcceleration`이다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 다른 씬·개체·Controller 수정, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 양팔 모으기 실효 7.5배속 고정

- Controller 기본 속도 `3`은 유지하고, 원본 `53~93프레임`의 추가 속도 배율을 `2.5`로 고정해 양팔 전방 모으기 전체를 실효 `7.5배속`으로 변경했다.
- 원본 `0~52프레임`과 `101~196프레임`은 실효 `3배속`을 유지한다. `94~100프레임`은 `7.5→3배속`으로 복귀한다.
- 0.25프레임 간격 수치 점검에서 `53~93프레임` 전 구간의 추가 배율은 정확히 `2.5`였다. 53·70·84·85·90·93프레임의 실효 속도는 모두 `7.5`, 94프레임은 `7.250729`, 100프레임은 `3`이다.
- 공격 개체 GlobalObjectId, 전체 `0~196프레임`, `60fps`, 반복 설정, 원본 커브 바인딩 `60개`, 커브 지문 `1D8CC5051AEF6698CFC6B484BF1280B2FA8B1EDF08967426BF268B89C1E8B2E4`를 유지했다.
- 원본 FBX·AnimationClip 커브·키 시간·포즈·모델·메시·머티리얼·본·몸체·손목·칼날은 수정하지 않았고 다른 오스티나토 슬롯도 변경하지 않았다.
- 수치 점검 통과 후 Unity Play Mode 실제 Animator를 41개 연속 시점, 정면·3/4로 최종 1회 캡처했다. 승인 외형이 유지됐고 관찰된 모델 루트 이동과 회전은 모두 `0`이다.
- 적용·점검 기록은 `docs/validation/ostinato_attack_downstrike_acceleration_2026-07-20/`, 최종 이미지는 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/Ostinato_AttackAppearanceComparison.png`에 갱신했다.
- 실행한 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackDownstrikeAcceleration`, `InspectOstinatoAttackDownstrikeAcceleration`, `CaptureOstinatoAttackDownstrikeAcceleration`이다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 다른 씬·개체·Controller 수정, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 양팔 전방 폐합 내려찍기 속도 가속

- 현재 전체 Controller 속도 `3`을 유지하면서 `Ostinato_04_Attack_Source` 상태에 `OstinatoAttackAccelerationBehaviour`를 연결했다. 이 프로필은 원본 재생 위치에 따라 해당 Animator의 `speed` 추가 배율만 조절한다.
- 원본 `0~53프레임`은 실효 `3배속`, `53~84프레임`은 `3→3.45배속`, `85~93프레임`은 `3.45→5.25배속`으로 가속한다. `94~100프레임`에서 `5.25→3배속`으로 복귀하고 `101~196프레임`은 다시 기존 `3배속`을 유지한다.
- 측정값은 70프레임 `3.257559`, 84프레임 `3.45`, 85프레임 `3.472222`, 90프레임 `4.25`, 93프레임 `5.25`, 100프레임 `3`이다. 0.25프레임 간격 점검에서 가속 구간은 단조 증가하고 복귀 구간은 단조 감소했다.
- 공격 개체 GlobalObjectId, 전체 `0~196프레임`, `60fps`, 반복 설정, 원본 커브 바인딩 `60개`, 커브 지문 `1D8CC5051AEF6698CFC6B484BF1280B2FA8B1EDF08967426BF268B89C1E8B2E4`를 유지했다.
- 원본 FBX·AnimationClip 커브·키 시간·포즈·모델·메시·머티리얼·본·몸체·손목·칼날은 수정하지 않았고 다른 오스티나토 슬롯도 변경하지 않았다.
- 수치 점검 통과 후 Unity Play Mode 실제 Animator를 41개 연속 시점, 정면·3/4로 최종 1회 캡처했다. 관찰된 모델 루트 이동과 회전은 모두 `0`이다.
- 적용·점검 기록은 `docs/validation/ostinato_attack_downstrike_acceleration_2026-07-20/`, 최종 이미지는 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/Ostinato_AttackAppearanceComparison.png`에 저장했다.
- 실행한 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackDownstrikeAcceleration`, `InspectOstinatoAttackDownstrikeAcceleration`, `CaptureOstinatoAttackDownstrikeAcceleration`이다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 다른 씬·개체·Controller 수정, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 공격 전체 모션 3배속 재생

- 현재 `Approved Ostinato Enemy Placement/Ostinato_04_Scissor_Attack`이 사용하는 `Ostinato_04_Attack_Source` Animator 상태의 재생 속도를 `1`에서 `3`으로 변경했다.
- 원본 전체 `0~196프레임`, `60fps`, `3.266667초`, `Loop Time=True`는 유지되며 실효 반복 주기는 약 `1.088889초`다.
- 향후 동일 공격 FBX를 재적용해도 속도 `3`이 유지되도록 `OstinatoScissorAttackAnimation`의 단일 상수 `AttackPlaybackSpeed`와 Controller 생성·점검 계약을 `3f`로 맞췄다.
- 대상 전용 점검 결과 `PlaybackSpeed=3`, 원본 커브 바인딩 `60개`, 커브 지문 `1D8CC5051AEF6698CFC6B484BF1280B2FA8B1EDF08967426BF268B89C1E8B2E4`, FBX 해시 일치, 승인 메시·머티리얼 유지가 확인됐다.
- 공격 개체를 삭제·재생성·교체하지 않았고 FBX·애니메이션 커브·모델·본·몸체·손목·칼날과 다른 오스티나토 슬롯은 수정하지 않았다.
- 실행한 명령은 `Refresh-UnityProject.ps1`, `InspectOstinatoAttackFbxUnmodified`다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 다른 씬·개체·Controller 수정, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 공격 모션 개체 원본 FBX 재교체 및 전체 반복 복원

- `Approved Ostinato Enemy Placement/Ostinato_04_Scissor_Attack` 아래의 기존 `0~93프레임` 반복 공격 개체를 삭제하고, 사용자 제공 `enemies model/ostinato attack.fbx`와 바이트가 같은 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_04_Scissor_Attack.fbx` 프리팹을 새 공격 모션 개체로 직접 배치했다.
- 원본과 Unity FBX의 SHA-256은 모두 `0430EEE08D7D2AFBC645ECD3DFDA7B37C1DBF7BB4A6A34AF761E9AFAD1708260`으로 일치한다.
- 표시 메시를 정적 승인 모델 `Ostinato_ApprovedUnity.fbx`의 메시로 동기화하고 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했다. 표시 메시 외형 지문은 승인 정적 메시와 같은 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`이다.
- Controller를 원본 `mixamo.com` 전체 테이크 `0~196프레임`, `60fps`, `3.266667초`, 속도 `1`, `Loop Time=True`로 다시 연결했다. 이전 `Ostinato_04_Scissor_Attack_ForwardCloseLoop.anim`은 사용하지 않으며 승인 범위에 따라 파일은 삭제하지 않았다.
- 원본 커브 바인딩은 `60개`, 커브 지문은 `1D8CC5051AEF6698CFC6B484BF1280B2FA8B1EDF08967426BF268B89C1E8B2E4`로 적용 전후 동일하다. 원본 FBX·애니메이션·본·몸체·손목·칼날에는 별도 수정을 가하지 않았다.
- 슬롯 Transform과 sibling index `3`, 다른 오스티나토 슬롯은 유지했다. Animator Root Motion은 비활성 상태이며 관찰된 모델 루트 이동과 회전은 모두 `0`이다.
- 수치 점검 통과 후 Unity Play Mode의 실제 Animator 전체 테이크를 41개 연속 시점, 정면·3/4 시점으로 최종 1회 캡처했다. 적용·점검·캡처 결과는 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/`에 갱신했다.
- 실행한 명령은 `ApplyOstinatoAttackFbxUnmodified`, `InspectOstinatoAttackFbxUnmodified`, `CaptureOstinatoAttackFbxUnmodifiedComparison`이다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 다른 슬롯 수정, 기존 파생 애셋 삭제, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 공격 모션 개체 새 FBX 교체 및 전방 합장 구간 반복

- `Approved Ostinato Enemy Placement/Ostinato_04_Scissor_Attack` 아래의 기존 공격 모션 자식을 삭제하고, 사용자 제공 `enemies model/ostinato attack.fbx`와 바이트가 같은 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_04_Scissor_Attack.fbx`를 새 공격 모션 개체로 직접 배치했다. 적용 전후 GlobalObjectId가 달라 실제 개체 교체를 확인했다.
- 표시 메시를 승인된 정적 오스티나토 `Ostinato_ApprovedUnity.fbx`의 메시로 동기화하고 승인된 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했다.
- 원본 FBX와 Unity 임포트 FBX의 SHA-256은 모두 `0430EEE08D7D2AFBC645ECD3DFDA7B37C1DBF7BB4A6A34AF761E9AFAD1708260`이며, 원본 FBX·원본 애니메이션 커브·모델·본은 수정하지 않았다.
- 별도 `Ostinato_04_Scissor_Attack_ForwardCloseLoop.anim`에 원본 `0~93프레임`만 복제해 `60fps`, `1.55초`, `Loop Time=True`, Controller 속도 `1`로 연결했다. `93프레임`은 양팔이 전방에서 완전히 모인 지점이며 다음 재생 프레임은 `0프레임`이다.
- 원본과 반복 클립의 Float Curve Binding은 60개로 동일하고 Object Curve Binding은 0개다. 0.25프레임 간격의 조밀 비교에서 원본 대비 최대 값 오차는 `0`이었다.
- 슬롯 Transform과 sibling index `3`, 다른 오스티나토 슬롯 8개는 유지했으며 별도 칼날·손목 보정 제어 개체는 없다.
- 수치 검사 통과 후 `0, 15, 30, 53, 70, 84, 85, 90, 93, 0`프레임을 정면·3/4 시점으로 최종 1회 캡처했다. 적용·점검·캡처 기록은 `docs/validation/ostinato_attack_forward_close_loop_2026-07-20/`에 저장했다.
- 실행한 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackFbxForwardCloseLoop`, `InspectOstinatoAttackFbxForwardCloseLoop`, `CaptureOstinatoAttackFbxForwardCloseLoop`다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 원본 FBX·모델링·본·손목·칼날 수정, 다른 슬롯 수정, 기존 파생 애셋 삭제, Git 작업은 실행하지 않았다.

## 최신 완료 - 양팔 공격 유지 및 자연스러운 기본자세 복귀 재수정

- 사용자 영상에서 기존 새 100→101프레임 접합이 원본 100프레임 자세와 원본 151프레임 자세를 직접 연결해 전신을 순간 회전시키는 문제를 확인했다. 탄젠트 오버슈트가 아니라 서로 다른 전신 자세의 직접 접합이 주원인이었다.
- 원본 `0~100프레임`을 잠그고 새 파생 클립에서도 그대로 유지했다. 양팔 폐합·타격 `85~93`, 타격 후 자세 `94~100`을 포함한 공격 구간은 0.25프레임 간격 비교에서 최대 오차 `0`이다.
- 새 `101~118프레임`은 원본 100프레임 자세에서 원본 0프레임 기본자세로 `18프레임/0.3초` 동안 감속 복귀한다. 회전 채널은 최단 각도 방향을 사용하며 시작·종점 속도는 0이다.
- 새 `119~131프레임`은 기본자세를 유지한다. 최종 클립은 `131프레임`, `2.183333초`, `60fps`, 반복 재생이며 Controller 속도는 `1`이다.
- 복귀 첫 프레임 최대 회전 채널 변화는 `0.613648도`, 전체 복귀 최대 회전 채널 변화는 `5.711548도`, 최대 위치 채널 변화는 `0.007759`다. 곡선 오버슈트가 없고 118프레임 기본자세 도달, 131→0 반복 경계 연속을 확인했다.
- 수치 검사 통과 뒤 `0, 53, 84, 85, 93, 94, 100, 101, 104, 109, 114, 118, 119, 131`을 정면·3/4로 최종 1회 캡처했다. 공격 흐름은 그대로이며 100→101 순간 회전 없이 점진적으로 기본자세에 복귀하고, 메시 변형이나 칼날/손목 분리는 보이지 않았다.
- 원본 공격 FBX SHA-256 `0430EEE08D7D2AFBC645ECD3DFDA7B37C1DBF7BB4A6A34AF761E9AFAD1708260`, 정적 승인 외형, 모델링·메시·본 구조, 다른 오스티나토 슬롯은 유지했다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackMotionPhaseCut`, `InspectOstinatoAttackMotionPhaseCut`, `CaptureOstinatoAttackMotionPhaseCut`이다. 이번 최종 캡처는 한 번만 실행했으며 결과는 `docs/validation/ostinato_attack_motion_phase_cut_2026-07-20/`에 저장했다.
- 하네스 검증 및 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 원본 FBX·모델링 수정, 원본 `0~100` 공격 수정, 손목·칼날 별도 보정, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 공격 후 수평 재전개·단계적 회수 구간 삭제

- 원본 `mixamo.com` 공격 클립은 유지하고 `Ostinato_04_Scissor_Attack_PhaseCut.anim` 파생 클립을 생성했다. 새 `0~100프레임`은 원본 `0~100`, 새 `101~146프레임`은 원본 `151~196`에 대응한다.
- 원본의 `급격한 수평 재전개(101~115)`와 `단계적 회수(116~150)`를 재생 대상에서 제외했다. 새 `100프레임=원본 100` 다음에 새 `101프레임=원본 151`이 오며, 별도 중간 자세나 추가 전환 구간은 만들지 않았다.
- 최종 클립은 `146프레임`, `2.433333초`, `60fps`, 반복 재생이며 공격 Controller의 `Ostinato_04_Attack_Source` 상태에 속도 `1`로 연결했다.
- 전체 Float curve binding `60개`에서 새 `0~100`과 원본 `0~100`, 새 `101~146`과 원본 `151~196`의 프레임별 값이 일치했다. 원본 `101~150`은 파생 클립에 포함되지 않는다.
- 원본 FBX SHA-256은 `0430EEE08D7D2AFBC645ECD3DFDA7B37C1DBF7BB4A6A34AF761E9AFAD1708260`으로 유지됐다. 모델링·메시·본·손목·칼날·신체 포즈는 별도로 수정하지 않았으며 정적 승인 외형과 다른 오스티나토 슬롯도 유지했다.
- 수치 검사 통과 뒤 파생 프레임 `0, 53, 84, 93, 94, 100, 101, 110, 130, 146`을 정면·3/4로 최종 1회 캡처했다. 접합 전후 메시 소실·변형이나 칼날/손목 분리는 보이지 않았고, 삭제 구간 없이 기본자세 복귀로 이어졌다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackMotionPhaseCut`, `InspectOstinatoAttackMotionPhaseCut`, `CaptureOstinatoAttackMotionPhaseCut`이며 최종 캡처는 한 번만 실행했다. 결과는 `docs/validation/ostinato_attack_motion_phase_cut_2026-07-20/`에 저장했다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 원본 FBX·모델링 수정, 손목·칼날·신체 포즈 보정, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 공격 모션 좌우 관절·전신 상세 분석

- 기존 전 프레임 분석을 확장해 `Hips`, `Spine`, `Head`, 양쪽 `Shoulder`, `Arm`, `ForeArm`, `Hand`, `UpLeg`, `Leg`, `Foot`의 위치·회전 변화를 `0~196프레임` 전체에서 다시 측정했다.
- 손목 본의 로컬 회전 변화는 좌우 모두 전 구간 `0도`다. 칼날 방향 변화는 손목 비틀기가 아니라 어깨·상완·전완 회전으로 만들어진다.
- 주 타격 `85~93프레임`의 좌우 팔 누적 회전은 `43.5도 / 42.4도`로 거의 대칭이며, 양손 최소 간격 `0.073066`인 `93프레임`이 폐합 충돌점이다.
- 후속 수평 재전개 `101~115프레임`은 좌우 손 이동 `1.810 / 2.049`, 좌우 팔 회전 `242.4도 / 222.3도`다. 주 타격보다 손 이동량은 약 `7.7배`, 팔 회전량은 약 `5.4배` 커 화면에서 더 강하게 읽힐 수 있다.
- 왼손 최대 이동은 `105프레임`, 오른손 최대 이동은 `107프레임`이라 수평 재전개에서 왼팔이 약 2프레임 먼저 가속한다. 최대 손 간격은 `108프레임`의 `1.965359`다.
- 척추 회전은 전반적으로 작고 루트 Transform은 고정이다. 상승·하강과 체중 이동은 골반·머리·양쪽 다리 자세 변화가 담당한다.
- `OstinatoAttackMotionSegmentAnalysis.cs`에 좌우 손 이동 벡터, 관절별 회전량, 시작 자세 대비 회전 차이, 골반·머리·발 위치, 구간별 누적값을 추가했다.
- 상세 결과는 `docs/validation/ostinato_attack_motion_analysis_2026-07-20/Ostinato_AttackMotionDetailedAnalysis.md`와 갱신된 CSV·수치 보고서에 저장했다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `AnalyzeOstinatoAttackMotionSegments`다. FBX·AnimationClip·Controller·씬 수정, 새 캡처, 하네스 검증 및 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 갱신된 오스티나토 공격 모션 전 프레임 구간 분석

- 현재 `mixamo.com` 공격 클립 전체 `0~196프레임`, `3.266667초`, `60fps`를 Unity `AnimationMode`에서 매 프레임 샘플링했다. FBX·AnimationClip·Controller·씬 개체는 수정하거나 저장하지 않았다.
- `Hips`, `Spine`, 양쪽 `Arm`, `ForeArm`, `Hand`의 위치·회전 변화량을 측정하고 기존 정면·3/4 연속 비교 이미지와 대조했다.
- 동작을 `초기 압축(0~7)`, `예비동작 상승·전개(8~30)`, `폐합 진입(31~52)`, `저속 가위 폐합(53~84)`, `주 타격(85~93)`, `타격 후 전환(94~100)`, `급격한 수평 재전개(101~115)`, `회수 시작(116~150)`, `기본자세 복귀(151~183)`, `루프 정지(184~196)`로 구분했다.
- 핵심 타격점은 손 간격이 전체 최소 `0.073066`이 되는 `93프레임`이다. 가장 큰 동작량은 `106프레임`, 최대 손 간격 `1.965359`는 `108프레임`이며, `101~108프레임`은 타격 뒤의 급격한 수평 재전개로 분리했다.
- `184~196프레임`은 두 번째 저속 구간이고 `196프레임` 자세가 `0프레임`과 일치해 반복 재생에 연결된다.
- 분석 코드는 `Assets/_Project/Editor/Validation/OstinatoAttackMotionSegmentAnalysis.cs`, 결과는 `docs/validation/ostinato_attack_motion_analysis_2026-07-20/`에 저장했다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `AnalyzeOstinatoAttackMotionSegments`다. 새 캡처, 하네스 검증 및 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 갱신된 오스티나토 공격 FBX 직접 교체 및 반복 재생

- 사용자가 갱신한 `enemies model/ostinato attack.fbx`를 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_04_Scissor_Attack.fbx`로 바이트 그대로 교체했다. 원본과 Unity 임포트 FBX SHA-256은 모두 `0430EEE08D7D2AFBC645ECD3DFDA7B37C1DBF7BB4A6A34AF761E9AFAD1708260`이다.
- `Approved Ostinato Enemy Placement/Ostinato_04_Scissor_Attack` 아래의 기존 공격 개체를 삭제하고, 갱신된 공격 FBX 프리팹을 새 공격 모션 개체로 직접 배치했다. 슬롯 Transform과 sibling index `3`, 다른 오스티나토 슬롯은 유지했다.
- 새 `mixamo.com` 전체 테이크는 Unity `0~196프레임`, `3.266667초`, `60fps`, 커브 바인딩 `60개`다. 커브 지문은 적용 전후 모두 `1D8CC5051AEF6698CFC6B484BF1280B2FA8B1EDF08967426BF268B89C1E8B2E4`이며 움직임·포즈·타이밍·Root Motion을 수정하지 않았다.
- 전체 테이크의 `Loop Time`만 활성화하고 Controller 속도 `1`로 반복 재생한다. 기본 테이크의 루트 관련 설정은 덮어쓰지 않았다.
- 표시 메시를 정적 승인 모델 `Ostinato_ApprovedUnity.fbx`의 메시로 교체하고 동일 이름 본에 재매핑했다. 승인 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했으며 표시 메시와 정적 승인 메시의 외형 지문은 모두 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`이다.
- 기존 FBX의 `2.65초`와 과거 커브 지문만 허용하던 `OstinatoScissorAttackAnimation`의 고정 계약을 제거했다. 현재 제공 FBX의 유효한 전체 테이크·60fps·반복 설정과 적용 전후 커브 불변을 검사하도록 변경했다.
- Unity에서 새 전체 타임라인을 41개 연속 프레임, 정면·3/4로 최종 1회 캡처했다. 정적 승인 외형이 유지되고 개체 소실이나 메시 분리가 보이지 않았으며 관찰된 루트 이동·회전은 모두 `0`이다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackFbxUnmodified`, `InspectOstinatoAttackFbxUnmodified`, `CaptureOstinatoAttackFbxUnmodifiedComparison`이다. 결과는 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/`에 저장했다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 공격 모션 개체 제공 FBX 원상 복원

- `Approved Ostinato Enemy Placement/Ostinato_04_Scissor_Attack` 아래의 칼날 강체 보정 개체를 삭제하고, 사용자 제공 `enemies model/ostinato attack.fbx`와 바이트가 같은 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_04_Scissor_Attack.fbx` 프리팹을 새 공격 모션 개체로 직접 배치했다.
- 원본과 Unity 임포트 FBX의 SHA-256은 모두 `793B723A8861F5555D795BA4333705FB1EB34DAD8E1CA1856B8D2B0770106C70`이다. 새 개체는 기존 `RigidBladeRig` 표시 메시, `LeftBladeRigidRoot`, `RightBladeRigidRoot`, 내려찍기 칼날 보정 클립을 사용하지 않는다. 관련 보정 에셋 파일은 삭제하지 않았다.
- 표시 메시를 정적 승인 모델 `Ostinato_ApprovedUnity.fbx`의 메시로 교체하고, 공격 FBX 리그의 동일 이름 본에 재매핑했다. 승인 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했다.
- 표시 메시와 정적 승인 메시의 외형 지문은 모두 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`이다. 공격 Controller는 FBX의 `mixamo.com` 테이크를 속도 `1`, 반복 재생으로 사용한다.
- 공격 커브 지문은 적용 전후 모두 `72998B999D597917382DEDCDF39DEBD20986AF7C0B6AD319668DC81B30CA2E37`이며 움직임·포즈·타이밍·Root Motion을 수정하지 않았다. 슬롯 Transform과 sibling index `3`, 다른 오스티나토 슬롯도 유지했다.
- Unity 연속 41프레임을 정면·3/4로 최종 1회 캡처했다. 정적 승인 외형이 전 구간 유지되고 칼날과 손목의 분리, 칼날 변형·늘어짐, 개체 소실이 보이지 않았으며 관찰된 루트 이동·회전은 모두 `0`이다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackFbxUnmodified`, `InspectOstinatoAttackFbxUnmodified`, `CaptureOstinatoAttackFbxUnmodifiedComparison`이다. 결과는 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/`에 저장했다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 칼날 완전 강체화 및 몸통 앞쪽 대향 회전

- 사용자 캡처 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-20 15-33-56.mp4`에서 프레임 사이 칼날이 흰 파편처럼 찢어지고 길게 늘어나 몸통을 관통하는 현상을 확인했다.
- 원인은 스킨된 칼날의 프레임별 BlendShape 역보정값이 중간 시간에 혼합되던 구조였다. 기존 칼날 BlendShape 방식을 폐기했다.
- 몸체 SkinnedMesh에서는 양쪽 주 칼날 표시 삼각형만 제외했다. 승인 모델의 원본 버텍스를 그대로 복제한 `Ostinato_04_Scissor_Attack_LeftRigidBlade.asset`, `Ostinato_04_Scissor_Attack_RightRigidBlade.asset` 정적 메시를 생성했다.
- 양쪽 칼날은 손 본 아래의 `LeftBladeRigidRoot`, `RightBladeRigidRoot`에 붙은 `MeshFilter + MeshRenderer` 강체 개체다. 칼날에는 SkinnedMeshRenderer, 본 가중치, BlendShape, 버텍스 애니메이션, 위치·스케일 커브가 없다.
- 보정 클립에는 두 칼날 루트의 쿼터니언 회전 커브 8개만 추가했다. 왼쪽 칼날은 오른쪽, 오른쪽 칼날은 왼쪽 성분을 유지하면서 양쪽 모두 앞쪽으로 벌어져 몸통을 관통하지 않는다.
- `62~77프레임` 회전, `78~93프레임` 수평 유지, `94~99프레임` 복귀 구간을 유지했다. 기존 신체·손·전완 커브와 공격 타이밍·속도는 변경하지 않았다.
- 반 프레임 간격 독립 점검 결과 몸체 편차 `0`, 칼날 형상 편차 `0.000002`, 변 길이 오차 `0.000001`, 위치·스케일 편차 `0`, 수평 오차 `0.076968도`, 몸통 전방 최소 여유 `0.059232`다.
- 정면·측면·3/4에서 정확 프레임 `50, 61, 62, 67, 72, 77, 78, 83, 93, 94, 99`를 최종 1회 캡처했다. 사용자 영상의 칼날 찢어짐·늘어남·비강체 굽힘이 없고 칼날 외형이 전 구간 동일하게 유지되는 것을 직접 확인했다.
- 원본 공격 FBX와 Unity 임포트 FBX SHA-256은 모두 `793B723A8861F5555D795BA4333705FB1EB34DAD8E1CA1856B8D2B0770106C70`이며 수정하지 않았다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackDownstrikeBladeRotation`, `InspectOstinatoAttackDownstrikeBladeRotation`, `CaptureOstinatoAttackDownstrikeBladeRotation`이다.
- 결과는 `docs/validation/ostinato_attack_downstrike_blade_rotation_2026-07-20/`에 저장했다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 내려찍기 칼날 손목 피벗 캡 제거 및 좌우 대향 보정

- 사용자 캡처 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-20 15-04-42.mp4`를 프레임 단위로 확인했다. 내려찍기와 정지 자세에서 손목 안쪽에 보이던 긴 삼각형은 칼날 열린 경계와 `Hand` 위치에 추가한 피벗 버텍스를 잇던 삼각 팬이었다.
- 파생 메시에서 양쪽 피벗 버텍스 2개와 피벗 캡 삼각 팬을 제거했다. 새 연결 오브젝트나 연결 기하는 추가하지 않았으며 `WristInteriorPivotCaps=0`, `AddedConnectorGeometry=0`이다.
- 칼날 회전축은 각 프레임의 실제 손목 쪽 열린 경계 중심으로 변경했다. 왼쪽 칼날 끝은 몸 중앙 방향의 오른쪽, 오른쪽 칼날 끝은 몸 중앙 방향의 왼쪽을 향하도록 최소 강체 회전만 적용했다.
- `62~77프레임`에서 회전하고 `78~93프레임`에서 서로 마주보는 수평 방향을 유지하며 `94~99프레임`에서 원래 자세로 복귀한다. `0~61`, `99~159프레임` 칼날 편차와 신체·손·전완 회전 편차는 모두 `0`이다.
- 독립 점검 결과 최대 강체 위치 오차 `0.000115`, 최대 변 길이 오차 `0.000029`, 손목 경계 중심 이탈 `0`, 수평 오차 `0.005603도`, 칼날 끝의 몸 중앙 방향 최소 정렬값 `1`이다.
- 정확 프레임 `50, 61, 62, 67, 72, 77, 78, 83, 93, 94, 99`를 원본/교정본 정면·3/4 시점으로 최종 1회 캡처해 손목 내부 이상 기하가 없고 칼날과 손목이 붙은 상태를 직접 확인했다.
- 원본 공격 FBX와 Unity 임포트 FBX의 SHA-256은 모두 `793B723A8861F5555D795BA4333705FB1EB34DAD8E1CA1856B8D2B0770106C70`이며 수정하지 않았다. 공격 속도는 `1`, Root Motion은 비활성 상태다.
- 적용·독립 점검·최종 캡처·영상 분석 기록은 `docs/validation/ostinato_attack_downstrike_blade_rotation_2026-07-20/`에 저장했다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackDownstrikeBladeRotation`, `InspectOstinatoAttackDownstrikeBladeRotation`, `CaptureOstinatoAttackDownstrikeBladeRotation`이다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 공격 FBX 직접 인스턴스 교체 및 정적 외형 동기화

- `Approved Ostinato Enemy Placement/Ostinato_04_Scissor_Attack` 아래의 기존 정적 FBX 기반 공격 개체를 삭제하고, `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_04_Scissor_Attack.fbx` 프리팹을 새 공격 개체로 직접 배치했다.
- Unity 공격 FBX는 사용자 파일 `enemies model/ostinato attack.fbx`와 바이트 해시가 동일하다. 양쪽 SHA-256은 `793B723A8861F5555D795BA4333705FB1EB34DAD8E1CA1856B8D2B0770106C70`이다.
- 새 씬 개체의 프리팹 원본은 공격 FBX다. 표시 메시만 정적 승인 모델 `Ostinato_ApprovedUnity.fbx`의 메시로 교체하고, 승인 모델의 본 순서와 루트 본을 공격 FBX 리그의 동일 이름 본에 재매핑했다.
- 승인 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 머티리얼 4종을 같은 순서로 연결했다. 표시 메시 외형 지문과 정적 승인 메시 지문은 모두 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`이다.
- 공격 Controller는 FBX의 `mixamo.com` 원본 테이크를 속도 `1`로 재생한다. 클립은 `0~159프레임`, `2.65초`, `60fps`이며 커브 지문 `72998B999D597917382DEDCDF39DEBD20986AF7C0B6AD319668DC81B30CA2E37`이 적용 전후 동일하다.
- 기존 수평 칼날 보정 클립과 파생 메시를 새 개체에서 사용하지 않는다. `LeftBladeControl`, `RightBladeControl`도 씬에 없으며 관련 보정 에셋 파일 자체는 삭제하지 않았다.
- 슬롯 Transform과 sibling index `3`, 다른 오스티나토 슬롯을 유지했다. Unity Play Mode에서 41개 연속 프레임을 정면·3/4로 확인했으며 정적 승인 외형이 전 구간 유지되고 메시 분리·늘어짐·개체 소실이 보이지 않았다. 관찰된 루트 이동과 회전은 모두 `0`이다.
- 적용·독립 점검·캡처 기록은 `docs/validation/ostinato_attack_fbx_direct_instance_2026-07-20/`에 저장했다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 내려찍기 이후 칼날만 수평 교정

- 사용자 캡처 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-20 13-10-07.mp4`에서 이전 칼날 제어 방식이 칼날을 손목에서 분리한 상태를 확인하고 해당 방식을 폐기했다.
- 내려찍기 전 `0~77프레임`은 원본 메시·포즈와 완전히 동일하게 유지했다. `78~93프레임`에만 양쪽 칼날 장축을 수평으로 교정하고, `94~99프레임`에서 원래 형상으로 복귀시켰다.
- `LeftBladeControl`·`RightBladeControl` 개체를 모두 제거했다. 본 추가, 칼날 재가중, 경계 버텍스 분리를 사용하지 않았으며 원본 토폴로지·인덱스·본 가중치·바인드 포즈와 손목/칼날 공유 경계 82개를 그대로 유지했다.
- 교정은 프레임별 연결형 BlendShape 32개로만 구성했다. 기존 `Hand`·`ForeArm` 및 모든 신체 위치·회전 커브는 바꾸지 않았고 최대 손 회전 편차는 `0도`다.
- 독립 점검 결과 사전 구간 최대 버텍스 편차는 `0`, `78~93프레임` 칼날 장축 수평 최대 오차는 `0.120301도`, 교정 구간 최대 버텍스 편차는 `0.977869`, 바운드 비율은 `1.183301`이다.
- 정확 프레임 `50, 61, 67, 77, 78, 83, 93, 94, 99`를 원본/교정본 정면·3/4 시점으로 대조했다. `77`까지는 동일하고 `78`부터 칼날만 수평으로 바뀌며, `83·93`에서 손목 연결을 유지하고 `99`에서 원본 형상으로 복귀한다.
- 씬에는 분리용 제어 개체가 없고 연결형 파생 메시가 저장돼 있다. 공격 컨트롤러는 교정 클립을 재생하며 다른 오스티나토 슬롯은 대상으로 삼지 않았다.
- 원본 공격 FBX와 Unity 복사본 SHA-256은 모두 `793B723A8861F5555D795BA4333705FB1EB34DAD8E1CA1856B8D2B0770106C70`이며 수정하지 않았다.
- 적용·점검·캡처 기록은 `docs/validation/ostinato_attack_wrist_orientation_2026-07-20/`에 저장했다. 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 최신 완료 - 오스티나토 공격 모션 구간 분석

- 원본 `mixamo.com` 공격 액션의 Unity `0~159프레임`에 대응하는 160프레임과 24개 본의 위치·회전을 Blender 5.1 백그라운드에서 읽기 전용으로 추출하고, Unity Play Mode 정면·3/4 연속 캡처 41장을 직접 확인했다.
- 동작은 `0~14` 시작 가드 압축, `15~49` 예비동작·양팔 들어 올리기, `50~61` 정점 전환, `62~77` 주 타격·양팔 내려치기, `78~93` 최대 신전·정지, `94~127` 회수, `128~159` 안정화·루프 종점으로 구분된다.
- 전체 활동량 최고점은 `67프레임 / 1.117초`이며 양쪽 Hand·ForeArm이 주도한다. 양손이 가장 멀리 뻗는 시점은 `83프레임 / 1.383초`이므로 시각적 접촉 후보는 `67~83프레임`으로 판단했다. FBX와 Controller에 타격 이벤트가 없어 단일 히트 프레임은 확정하지 않았다.
- 전체 본 끝점 이동량 중 상체·팔 계열 비중은 약 `80.8%`, 하체 계열은 약 `11.8%`다. Root 위치·회전 변화는 관찰되지 않았다.
- `159프레임`은 시작 자세와 평균 회전각 `0도`, 평균 본 머리 위치 차이 약 `0.0000043`으로 일치해 원본 동작 자체는 루프 종점에서 시작 자세로 돌아온다.
- 분석 보고서는 `docs/validation/ostinato_attack_motion_analysis_2026-07-20/Ostinato_AttackMotionAnalysis.md`, 원시 데이터는 같은 폴더의 `Ostinato_AttackBoneMotion.csv`, `Ostinato_AttackMotionSummary.json`에 저장했다.
- 분석 과정에서 FBX, 애니메이션 커브·포즈·타이밍·속도, 모델링·머티리얼, Controller, Scene, 게임플레이는 수정하지 않았다. 하네스 검증 및 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업도 실행하지 않았다.

## 최신 완료 - 오스티나토 공격 애니메이션 반복 재생

- `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_04_Scissor_Attack.fbx`의 `mixamo.com` 공격 테이크에 단일 clip override를 만들고 `LoopTime=True`만 설정했다. 기본 테이크의 `LoopTime=False` 외 설정은 그대로 복제했으며 `ClipOverrideCount=1`, `LoopOnlyOverride=True`다.
- 기본 테이크와 반복 override는 모두 `0~159프레임`, `2.65초`, `60fps`다. `LockRootRotation=False`, `LockRootHeightY=False`, `LockRootPositionXZ=False`를 비롯한 루트 옵션은 바꾸지 않았다.
- 공격 클립의 60개 커브 바인딩 지문은 적용 전후 모두 `72998B999D597917382DEDCDF39DEBD20986AF7C0B6AD319668DC81B30CA2E37`이며 `MovementCurveModified=False`다. Animator 상태 재생 속도는 `1`로 유지했다.
- `AnimatorApplyRootMotion=False`를 유지했고 코드에서 해당 값을 덮어쓰지 않았다. 표시 메시 외형 지문과 승인 정적 메시 지문은 모두 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`이며 승인 재질 4종도 그대로다.
- 원본 `enemies model/ostinato attack.fbx`와 Unity 복사본의 SHA-256은 모두 `793B723A8861F5555D795BA4333705FB1EB34DAD8E1CA1856B8D2B0770106C70`이다. FBX 바이트, 공격 동작, 프레임 범위, 타이밍, 커브, 포즈, 루트 모션, 슬롯 Transform, 다른 오스티나토 슬롯, 게임플레이는 변경하지 않았다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `EnableOstinatoAttackLoopPlayback`, `InspectOstinatoAttackLoopPlayback`이다. 컴파일 오류 수정본을 에디터에 인식시키기 위해 승인받은 Unity `Ctrl+R`을 1회 실행했다.
- 적용·독립 점검 기록은 `docs/validation/ostinato_attack_loop_2026-07-20/`의 `Ostinato_AttackLoopApply.txt`, `Ostinato_AttackLoopInspection.txt`와 각 bridge 로그에 저장했다.
- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, 캡처, Unity 재시작, 복원·재생성, Git 작업은 실행하지 않았다.

## 최신 완료 - 갱신된 사용자 오스티나토 공격 FBX 움직임 무수정 적용

- 사용자가 갱신한 `enemies model/ostinato attack.fbx`를 다시 확인했다. 새 원본 SHA-256은 `793B723A8861F5555D795BA4333705FB1EB34DAD8E1CA1856B8D2B0770106C70`이며, 이전 Unity 공격 FBX의 `F9F95F13D9F5F752BE56BB9BB69CE7EF51005609E9D43A6C61C07B13CC9463C9`와 달랐다.
- 새 원본을 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_04_Scissor_Attack.fbx`로 바이트 그대로 교체했다. 최종 원본과 Unity 복사본 SHA-256은 모두 `793B723A8861F5555D795BA4333705FB1EB34DAD8E1CA1856B8D2B0770106C70`이다.
- 새 FBX의 `mixamo.com` 공격 기본 테이크 전체를 사용했다. Blender 원본 범위는 `1~160프레임`, Unity 기본 임포트 범위는 `0~159프레임`, 길이는 `2.65초`, 프레임률은 `60fps`다.
- 사용자 clip override를 제거해 `ClipOverrideCount=0`으로 유지했다. 기본 테이크의 `LoopTime=False`, `LockRootRotation=False`, `LockRootHeightY=False`, `LockRootPositionXZ=False`를 덮어쓰지 않았다. 리타이밍, 프레임 자르기, 보간, 리샘플링, 포즈 보정도 수행하지 않았다.
- 공격 클립의 60개 커브 바인딩 지문은 적용 전후 모두 `72998B999D597917382DEDCDF39DEBD20986AF7C0B6AD319668DC81B30CA2E37`이다. `MovementCurveModified=False`를 확인했다.
- 새 승인 정적 모델 인스턴스의 Animator가 가진 기본 `ApplyRootMotion=False` 값은 코드에서 다시 대입하거나 강제하지 않았다. 최종 재생 보고는 `AnimatorApplyRootMotionOverridden=False`, `MovementOverride=False`이며 관찰된 모델 Root 이동·회전은 모두 `0`이다.
- 애니메이션 바인딩을 위해 슬롯 4 인스턴스의 유일한 `Hips` 부모 컨테이너 이름만 `Armature`로 맞췄다. 이는 곡선 값이나 본 움직임 수정이 아니라 원본 FBX 경로 `Armature/Hips/...`를 승인 정적 리그에 연결하기 위한 대상 인스턴스 경로 동기화다.
- 실제 표시 모델은 계속 `Ostinato_ApprovedUnity.fbx`를 사용한다. 슬롯 4 표시 메시와 정적 승인 메시의 외형 지문은 모두 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`이며 승인 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 재질 4종이 같은 순서로 연결돼 있다.
- 슬롯 4 Transform과 sibling index를 유지했고 다른 오스티나토 슬롯, AI, 피해·히트박스, 물리, 게임플레이는 변경하지 않았다.
- Unity Play Mode에서 원본 비루프 타임라인 전체를 41프레임, 정면·3/4 두 시점으로 한 번 캡처했다. 원본 공격 흐름과 승인 정적 외형이 전 프레임에서 유지되고 개체 소실이나 메시 분리는 보이지 않았다.
- 최종 비교 이미지는 `docs/validation/ostinato_attack_fbx_unmodified_2026-07-20/Ostinato_AttackAppearanceComparison.png`다. 적용·검사·재생 기록은 같은 폴더의 `Ostinato_AttackFbxUnmodifiedApply.txt`, `Ostinato_AttackFbxUnmodifiedInspection.txt`, `Ostinato_AttackRuntimePlayback.txt`에 저장했다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackFbxUnmodified`, `InspectOstinatoAttackFbxUnmodified`, `CaptureOstinatoAttackFbxUnmodifiedComparison`이다. 최종 비교 캡처는 한 번만 실행했다.
- 하네스 검증 및 관련 결과·로그·문서·생성물 작업, Unity 재시작, 복원·재생성, Git 작업은 실행하지 않았다.

## 최신 완료 - 사용자 지정 오스티나토 공격 FBX 교체와 정적 승인 외형 동기화

- 사용자 지정 `enemies model/ostinato attack.fbx`를 `Assets/_Project/Art/Enemies/Ostinato/Animations/Ostinato_04_Scissor_Attack.fbx`로 교체했다. 원본과 Unity 복사본 SHA-256은 `F9F95F13D9F5F752BE56BB9BB69CE7EF51005609E9D43A6C61C07B13CC9463C9`로 일치한다.
- 제공 FBX에는 `walking_man` 63프레임 테이크와 `mixamo.com|Layer0` 163프레임 테이크가 있다. 보행 테이크는 사용하지 않고 양팔·전완·척추 변화가 큰 `mixamo.com|Layer0`만 `Ostinato_04_Attack_Source` 공격 클립으로 임포트했다.
- 공격 클립은 `2.7초`, `60fps`, 커브 바인딩 `60개`, LoopTime 활성, Root Motion 비활성이다. 제공 FBX의 곡선 값이나 공격 동작은 수정하지 않았다.
- 제공 FBX와 승인 정적 모델은 본 이름 24개와 계층이 일치한다. Blender 기준 rest 행렬 성분 최대 차이는 `0.000783566`이다.
- 최초 적용에서는 Unity 클립 경로 `Armature/Hips/...`와 승인 정적 인스턴스의 Armature 컨테이너 이름이 달라 바인딩 19개가 해석되지 않았다. 승인 모델 자산이나 애니메이션 커브를 수정하지 않고 슬롯 4 인스턴스에서 유일한 `Hips` 본 부모 이름만 `Armature`로 맞춰 전체 바인딩을 연결했다.
- `Approved Ostinato Enemy Placement/Ostinato_04_Scissor_Attack`의 표시 모델은 `Ostinato_ApprovedUnity.fbx`를 사용한다. 실제 표시 메시 외형 지문과 승인 정적 메시 지문은 모두 `BD8D446D63E2479B647BC23470F85B350630718AF753B1C21859EE34DA7330F0`이다.
- 공격 개체에는 승인 재질 `Chitin`, `SoftTissue`, `HookBlade`, `CompoundEye` 4종을 같은 순서로 연결했다. 제공 공격 FBX의 단일 서브메시 외형은 애니메이션 공급원으로만 사용한다.
- 슬롯 4 Transform과 sibling index를 유지했고 다른 오스티나토 슬롯은 변경하지 않았다. AI, 피해·히트박스, 물리, 게임플레이도 변경하지 않았다.
- Unity Play Mode에서 공격 한 루프를 41프레임, 정면·3/4 두 시점으로 캡처했다. 전 구간에서 승인 정적 외형과 재질이 유지되고 개체 소실이나 표면 분리는 보이지 않았으며 모델 Root는 고정됐다.
- 최종 비교 이미지는 `docs/validation/ostinato_attack_fbx_2026-07-20/Ostinato_AttackAppearanceComparison.png`, 적용·검사·재생 기록은 같은 폴더의 `Ostinato_AttackFbxApply.txt`, `Ostinato_AttackFbxInspection.txt`, `Ostinato_AttackRuntimePlayback.txt`에 저장했다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyOstinatoAttackFbxReplacement`, `InspectOstinatoAttackAppearanceSync`, `CaptureOstinatoAttackAppearanceComparison`이다. 최종 비교 캡처는 한 번만 실행했다.
- 하네스 검증 `Run-HarnessValidation.ps1`과 관련 결과 조사·로그 확인·문서화·생성물 작업, Unity 재시작, 복원·재생성, Git 작업은 실행하지 않았다.
- 이번 완료 판정은 사용자 지정 FBX 교체, 실제 재생 연결, 정적 승인 외형 동기화 범위에 한정한다. 제공 FBX 공격 동작 자체의 별도 교정이나 원본 기획 모션과의 일치 판정은 수행하지 않았다.
