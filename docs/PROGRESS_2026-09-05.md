# 진행 기록 - 2026-09-05

## 기록 기준

- 이 문서는 `docs/PROGRESS_2026-09-04.md` 이후 현재 작업 트리에 남아 있는 미니 화염방사기 관련 변경 전체를 Git 반영 직전 상태로 정리한다.
- 원본 기획서 `docs/GAME_DESIGN_SOURCE.txt`의 미니 화염방사기 사양을 기준으로 공격 화염은 유효 사거리 `1~3m`, 최대 연속 분사 `10초`, 자동 종료 뒤 재사용 대기 `2.5초`를 사용한다.
- 원격 폭파는 반경 `6m`, 피해 `300`, 피아 식별 없음, 플레이어 체력 `70%` 감소, 보호막 피해 `2배`이며, 내구도 소진 자폭은 `4초` 뒤 반경 `15m`, 피해 `500`, 플레이어 체력 `90%` 감소, 보호막 피해 `2배`다.
- 기존 모델과 애니메이션을 임의로 다시 만들지 않고 원본 FBX, 원본 메시·텍스처·머티리얼, 원본 Mixamo Take를 보존했다. 자세와 장비 배치는 사용자께서 확정한 상태를 기준으로 했다.

## 진행 HTML 완료 표시

- `docs/PLAYER_ANIMATION_REQUIREMENTS.html`에서 다음 미니 화염방사기 6개 개체를 완료 상태로 표시했다.
  - `Flamethrower_Idle`
  - `Flamethrower_Fire`
  - `Flamethrower_Remote_Mode`
  - `Flamethrower_Tank_Place`
  - `Flamethrower_Remote_Detonate`
  - `Flamethrower_Tank_Recover`
- 앞서 완료된 샷건 9개 개체의 완료 표시도 현재 작업 트리에 함께 포함돼 있다.
- 문서에는 미니 화염방사기 리깅 목표, `Flamethrower_Tank_Place` 내려놓기 순서, `Flamethrower_Tank_Recover` 역재생 순서를 추가했다.

## 미니 화염방사기 원본 에셋과 리그

- 원본 `item model/mini flamethrower.fbx`를 `Assets/_Project/Art/Items/MiniFlamethrower/mini_flamethrower.fbx`로 가져오고 원본의 배낭 탱크, 두 연료통, 프레임·스트랩, 밸브, 주 호스·보조 호스, 분사 손잡이와 화염 형상을 누락 없이 유지했다.
- FBX에 포함된 `base_color.jpg`, `normal.jpg`, `texture_0_metallic.png`, `texture_0_roughness.png`와 `Material.001`을 Unity 에셋으로 보존했다.
- 원본 메시의 정점 `4,203`개, 삼각형 `4,178`개, 노멀, 탄젠트, UV, 머티리얼 참조를 유지한 파생 `mini_flamethrower_rigged.asset`에 본 가중치와 바인드 포즈를 추가했다.
- 리그는 탱크 본 1개, 호스 본 8개, 분사 손잡이 그립 본 1개의 총 10개 본이다. 탱크와 두 연료통은 등에 고정하고, 원본 호스는 탱크에서 분사 손잡이까지 연속되며, 사용자가 지정한 검은 권총형 그립이 오른손과 겹치도록 구성했다.
- 수송자 메시와 기존 골격 메시, 미니 화염방사기 원본 FBX의 정점·면·UV는 수정하지 않았다. 원본에 없는 대체 호스나 임의 형상을 만들지 않았다.

## 6개 화염방사기 상태

### `Flamethrower_Idle`

- 연료탱크는 수송자의 등 대부분을 덮는 가방 형태로 고정하고 두 연료통이 등 바깥쪽으로 세로로 나란히 향하게 했다.
- 분사 손잡이의 권총형 그립은 오른손에 놓고 방아쇠울과 방아쇠가 아래쪽을 향하도록 했다.
- Unity 실행 시작 구도는 `Flamethrower_Idle` 정면 전신을 바라보도록 `CargoRunMvp` 씬에 저장했다.

### `Flamethrower_Fire`

- `Flamethrower_Idle`과 같은 탱크·호스·손잡이 장착 상태에서 승인된 화염 방사 VFX를 분사구에 연결했다.
- 단일 발사 상태에서 `10초 분사 → 2.5초 대기 → 재분사`가 반복되며 상태 종료 시 새 방출을 중단한다.

### `Flamethrower_Remote_Mode`

- 양팔을 어깨너비로 나란히 전방으로 뻗고 양손이 연결된 전체 미니 화염방사기 조립체의 하단을 받치도록 했다.
- 탱크·연료통·밸브·호스·분사 손잡이는 서로 연결된 원본 상대 배치를 유지한다.
- 사용자가 Unity에서 직접 확정한 `MiniFlamethrower_Rig` 로컬 위치 `(-0.26830298, 1.4568014, 0.6099243)`를 편집 도구에 반영했다.

### `Flamethrower_Tank_Place`

- `Flamethrower_Remote_Mode` 자세를 `0.5초` 유지한 뒤 원본 `player model/transfer flame putting down.fbx`의 `mixamo.com` Take를 이어 재생하고 처음부터 반복한다.
- 원본 Take를 보존한 파생 `Flamethrower_Tank_Place_SymmetricPlace.anim`에서 왼팔도 오른팔과 함께 탱크를 받치며 내려가도록 구성했다.
- 양팔 최저점인 `80/146` 프레임, `2.667초`부터 연결된 전체 조립체를 수송자 앞 지면에 고정하고 오른손 추종을 해제한다. 반복 시작에서만 양손 앞 운반 자세로 복귀한다.
- 호스 스킨 부모 구조와 상대 Transform을 정상 상태와 맞춰 내려놓기 중 길게 늘어지던 직선 메시를 제거했다.

### `Flamethrower_Remote_Detonate`

- `Flamethrower_Tank_Place` 종료 자세의 연결된 조립체를 수송자 루트 기준 전방 총거리 `1m`의 지면에 배치했다.
- 원본 `item model/remote detonator.fbx`와 포함 텍스처를 `Assets/_Project/Art/Items/RemoteDetonator`에 가져왔다. 원격 기폭기 아랫부분을 오른손으로 받치고 버튼 면이 위를 향한다.
- 오른손은 시작 자세보다 전방 `30cm`, 위 `20cm` 이동하며 손바닥이 위를 향한다. 오른팔 전체의 상완 스위블과 팔꿈치 평면을 맞춰 손목·전완 메시의 꼬임을 해소했다.
- 현재 자산은 확정된 시작 정지 자세이며 후속 버튼 누르기 동작은 별도로 생성하지 않았다.

### `Flamethrower_Tank_Recover`

- 사용자가 손이 닿는 위치로 확정한 `Flamethrower_Tank_Place` 종료 지면 배치에서 시작한다. 탱크 하단 중심은 수송자 루트 기준 전방 약 `0.42221m`다.
- `Flamethrower_Tank_Place_SymmetricPlace.anim`의 전체 `4.867초`, `80`개 커브를 시간 순서만 반대로 복사한 `Flamethrower_Tank_Recover_Reverse.anim`을 재생한다.
- 최저점에서 양손이 조립체를 받치고 오른손 상대 위치를 따라 들어 올린다. 최고점에서는 탱크를 등에 고정하고 분사 손잡이만 오른손을 추종하는 `Flamethrower_Idle` 장비 상태로 전환한다.
- Idle 상태를 `0.5초` 유지한 뒤 지면 시작으로 돌아가 반복하며, 새 회차의 호스 본 기본값을 복원해 호스가 직선으로 늘어나지 않게 했다.

## 승인된 공격 화염 VFX의 Unity 적용

- `artSample/flamethrower_attack_vfx`의 승인 샘플을 Unity 파티클 프리팹 `Assets/_Project/Art/VFX/FlamethrowerAttack/FlamethrowerAttackVfx.prefab`으로 적용했다.
- 백열 중심, 고온 황색, 황금 압력층, 주황 외곽, 적색 연소층, 불티, 얇은 잔류 연기의 7개 파티클 레이어와 최대 파티클 `480`개를 사용한다.
- 화염은 분사구 바로 앞에서 수송자 전방으로만 시작하고 노랑·주황·적색 층이 이어진다. 검은 원형 구체와 뒤쪽 역분사는 포함하지 않았다.
- `FlamethrowerAttackVfx`와 `FlamethrowerAttackVfxStateBehaviour`가 사거리와 `10초/2.5초` 반복 주기를 담당하며 `Flamethrower_Fire.controller`와 씬에 연결돼 있다.
- 이번 범위는 시각 효과와 상태 재생까지이며 실제 피격, 피해, 연소 판정은 포함하지 않았다.

## 연료탱크 폭발 VFX 아트 샘플

- 신규 승인 검토용 샘플을 `artSample/flamethrower_tank_explosion_vfx`에 제작했다.
- 폭발 본체는 메시 덩어리가 아니라 머스켓 발사 계열과 같은 파티클 이펙트 문법으로 구성했다. 짧은 백열 코어, 황금·주황 고온 화염, 짙은 화약 연기, 불티와 소형 금속 파편을 사용하며 파편만 메시 사용 예외로 남겼다.
- 원격 폭파 모드는 탱크 중심에서 수평 반경과 상방 도달 높이가 모두 `6m`, 내구도 소진 자폭 모드는 모두 `15m`인 구형 시각 범위로 표현했다.
- 정점 단계와 감쇠 단계, 원격 `6m`와 자폭 `15m`를 비교할 수 있는 동적 HTML과 정적 프리뷰를 함께 보존했다. 마지막 부분에 고립된 검은 구체가 생기지 않도록 화염과 연기 전이를 연결했다.
- 이 항목은 아트 승인용 샘플만 완성한 상태다. 현재 Unity 씬, 프리팹, 실제 폭발 판정에는 적용하지 않았다.

## 주요 추가·변경 자산

- Unity 편집 도구: `Assets/_Project/Editor/PlayerHandsObjectAnimationTools.cs`
- Unity 에디터 브리지: `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
- 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- 미니 화염방사기 에셋: `Assets/_Project/Art/Items/MiniFlamethrower`
- 원격 기폭기 에셋: `Assets/_Project/Art/Items/RemoteDetonator`
- 화염방사기 애니메이션: `Assets/_Project/Art/Player/Animations/Flamethrower`
- 공격 VFX 프리팹·머티리얼·난류 자산: `Assets/_Project/Art/VFX/FlamethrowerAttack`
- 공격 VFX 런타임: `Assets/_Project/Scripts/VFX/FlamethrowerAttackVfx.cs`, `Assets/_Project/Scripts/VFX/FlamethrowerAttackVfxStateBehaviour.cs`
- 요구사항 문서: `docs/PLAYER_ANIMATION_REQUIREMENTS.html`
- 공격 화염 승인 샘플: `artSample/flamethrower_attack_vfx`
- 연료탱크 폭발 승인 검토 샘플: `artSample/flamethrower_tank_explosion_vfx`

## 확인 결과와 이번 Git 정리의 제외 범위

- 모델링·애니메이션의 1순위 검증은 각 작업 당시 실제 Unity 모델과 재생 화면을 정면·양측면·손 또는 호스 확대 구도에서 직접 확인한 결과를 따른다. 상세 캡처 경로와 2순위 보조 수치는 `docs/PROGRESS_2026-09-04.md`에 기록돼 있다.
- 연료탱크 폭발 VFX 샘플은 원격 모드의 화염·연기 본체가 수평 및 상방 `6m`, 자폭 모드가 수평 및 상방 `15m`에 도달하고 고립된 검은 구체가 없는 것을 직접 확인했다.
- 이번 Git 정리 단계에서는 현재 작업 트리의 내용을 문서화하는 것 외에 Unity 에셋, 모델, 애니메이션, 씬을 추가로 수정하거나 재생성하지 않았다.
- 사용자가 명령명과 대상 범위를 직접 지정하지 않은 Harness, EditMode, PlayMode, Windows Build 검증은 실행하지 않았고 결과 파일이나 로그도 확인하지 않았다.
- Git 커밋은 현재 작업 트리 전체와 이 진행 기록을 함께 포함하며 커밋 메시지는 `feat: record current Bellerophon development progress`로 관리한다.
