# PROGRESS 2026-07-15

## Accelerando 더듬이 반동 기반 철퇴 흔들림 수정

- 대상은 `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike` 공격 슬롯 하나로 한정했다.
- 원본 기획의 `고개를 좌우로 흔들면서 더듬이로 전방을 타격`하는 의도와 현재 공격 계획을 비교했다.
- 기존 공격은 `0.44초` 전방 릴리스 뒤 `1.20초`까지 더듬이가 같은 방향을 유지해, 철퇴가 더듬이 반동으로 흔들리는 인과관계가 보이지 않았다.
- 기존 최대 공격 각도와 준비 자세 범위는 유지하면서 다음 반동 구간을 추가했다.
  - `0.50초` 전방 정점
  - `0.62초` 더듬이 급반동
  - `0.78초` 재전진
  - `0.96초` 잔진동
  - `1.20초` 반동 안정화
- 왼쪽 `Bone_011/010/009` 커브를 기준으로 오른쪽 `Bone_008/007/006`을 좌우 대칭 생성했다.
- 사슬·철퇴 Transform 커브는 추가하지 않았고 기존 12링크 Rigidbody/ConfigurableJoint 구조와 직접 follower 힘 0 설정을 유지했다.
- 전용 검증에 `더듬이가 뒤로 반동하는 동안 철퇴가 관성으로 전진하는 반대 방향 표본` 조건을 추가했다.
- 두 차례 연속 검증 결과:
  - Left: 더듬이 역반동 속도 `4.261`, 반대 방향 표본 `5/5`, 반동 중 철퇴 전진 속도 `14.341/13.554`.
  - Right: 더듬이 역반동 속도 `4.715`, 반대 방향 표본 `4/3`, 반동 중 철퇴 전진 속도 `12.824/13.155`.
  - 사슬 최대 구간 늘어남 양쪽 `0.000004`, 철퇴 메시·프록시 분리 `0`.
  - 좌우 더듬이 대칭, 양쪽 메시 붕괴·과신장 0, 사슬·철퇴 Transform 커브 0 검증 통과.
- 검증 통과 후 최종 캡처를 한 번 실행해 전방 정점, 급반동, 재전진, 잔진동, 회복 단계를 저장했다.
- 수정·반영 파일:
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Antenna_Strike_Attack.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Antenna_Strike_Attack.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 승인된 공격 슬롯
- 실행한 Unity 명령:
  - `Refresh-UnityProject.ps1`
  - `ApplyApprovedAccelerandoForwardMaceStrikeMotion`
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion` 2회
  - `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 1회
- 검증 산출물: `docs/validation/accelerando_antenna_reaction_swing_2026-07-15/`
- Unity 에디터 재시작, Harness/EditMode/PlayMode/Build/Ensure/Smoke, 런타임 물리 코드, 다른 아첼레란도 슬롯, 모델링·텍스처·머티리얼·메시 웨이트, Git은 변경하거나 실행하지 않았다.

## 최신 적용 상태

- 더듬이의 전방 정점 뒤 급반동과 재전진이 공격 클립에 적용됐다.
- 철퇴는 더듬이가 뒤로 반동하는 동안 관성으로 전진한 뒤 사슬 장력에 이끌려 후속 흔들림을 만든다.
- 두 차례 전용 검증을 통과했고 최종 캡처를 한 번 완료했다.

## Accelerando 더듬이 정지 구간 철퇴 잔류 흔들림 완화

- 대상은 `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike` 공격 슬롯의 물리 반응으로 한정했다.
- 기존 공격 클립, 컨트롤러, 씬 배치 및 더듬이 키프레임은 변경하지 않았다.
- 더듬이가 움직이는 동안에는 기존 선형 감쇠 `0.3`을 즉시 유지하고, 속도 `1.0` 이하가 `0.18초` 이상 이어질 때만 철퇴 감쇠를 최대 `2.8`까지 점진적으로 높이도록 수정했다.
- 좌우 사슬이 반대편 더듬이의 미세 움직임에 영향을 받지 않도록 각 사슬은 자기 쪽 더듬이의 지속 정지 시간만 사용한다.
- 짧은 정점·방향 전환은 감쇠 대상에서 제외해 더듬이 역반동 중 철퇴 전진 반응을 유지했다.
- 최종 전용 검증 결과:
  - 왼쪽 정지 회복 평균 속도 `29.547 -> 3.028`, 후반/초반 비율 `0.102`.
  - 오른쪽 정지 회복 평균 속도 `39.256 -> 21.619`, 후반/초반 비율 `0.551`.
  - 역반동 중 철퇴 전진 표본 왼쪽 `4`, 오른쪽 `2`.
  - 사슬 최대 구간 늘어남 양쪽 `0.000004`, 철퇴 메시·프록시 분리 양쪽 `0`.
  - 좌우 대칭과 더듬이 메시 변형 검증 통과.
- 검증 통과 뒤 최종 캡처를 한 번 실행했다.
- 공격 클립 SHA-256은 작업 전후 모두 `80C7F79DF168EE422D1C852B75581A923E7E4908C24AF4D1DC87557A236EA72A`로 동일하다.
- 수정 파일:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `docs/validation/accelerando_antenna_stationary_settle_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령:
  - `Refresh-UnityProject.ps1`
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion`
  - 검증 통과 후 `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 1회
- Unity 에디터 재시작, Harness/EditMode/PlayMode/Build/Ensure/Smoke, 공격 클립·컨트롤러 재생성, 씬 저장, 다른 슬롯, 모델링·텍스처·머티리얼·메시 웨이트, Git은 변경하거나 실행하지 않았다.

## 현재까지의 Accelerando 작업 GitHub 반영

- 2026-07-14~2026-07-15에 승인받아 완료한 다음 작업을 하나의 Git 커밋으로 정리한다.
  - 승인된 리깅 모델 샘플과 Unity 전체 슬롯 모델 교체
  - 전체 배치 후면 방향 전환
  - 공격 더듬이 좌우 대칭 및 메시 웨이트 보정
  - 12링크 사슬 연속성 및 철퇴 물리 추종
  - 더듬이 반동 기반 전방 공격 모션
  - 더듬이 지속 정지 구간 철퇴 잔류 흔들림 완화
  - 작업 중간 경과 보고를 강제하는 `AGENTS.md` 절대 규칙
- 커밋 대상 범위:
  - `AGENTS.md`
  - `artSample/enemies/accelerando/rigged_attack_model_match/`
  - `Assets/_Project/Art/Enemies/Accelerando/`
  - `Assets/_Project/Runtime/Enemies/Accelerando/`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/PROGRESS_2026-07-14.md`
  - `docs/PROGRESS_2026-07-15.md`
  - 2026-07-14~2026-07-15의 `docs/validation/accelerando_*` 전용 산출물
- 커밋에서 제외한 혼합 작업:
  - Accelerando 이외 적 개체의 아트·애니메이션·검증 파일
  - 여러 적 개체 변경이 섞인 `UnityEditorValidationBridge.cs`
  - 테스트 과정에서 재직렬화된 `ProjectSettings/DynamicsManager.asset`
  - `Backups/`, `Assets/_Recovery/`, Blender `.blend1`, `__pycache__` 생성물
  - 다른 날짜·다른 작업의 진행문서와 검증 캡처
- 커밋 메시지: `feat: refine Accelerando attack motion and physics`
- 대상 원격/브랜치: `origin` (`https://github.com/goldmembrane/bellerophon-s-envelop.git`) / `main`
- 이번 Git 반영 단계에서는 Unity 명령과 Harness/EditMode/PlayMode/Build/Ensure/Smoke를 다시 실행하지 않는다. 최종 기능 검증 결과는 위 `Accelerando 더듬이 정지 구간 철퇴 잔류 흔들림 완화` 항목과 전용 검증 문서를 기준으로 한다.

## Accelerando 철퇴·사슬 더듬이 관성 종속 수정

- 사용자 제공 `11.588초`, H.264 `1920x1000` 영상을 FFmpeg로 2fps 개요 시트와 0.1초 간격 연속 시트로 분석했다.
- 같은 더듬이 입력 위상에서도 철퇴 위치가 크게 달라지고 입력이 약해진 뒤에도 큰 원운동이 누적되는 현상을 확인했다.
- 공격 체인의 조인트 솔버 이후 모든 링크 `Rigidbody.position`과 `linearVelocity`를 순차적으로 직접 덮어쓰던 수동 투영이 운동량 전달을 깨고 다음 스텝에 오차를 재주입하는 핵심 원인이었다.
- 단순 구형 프록시의 비인접·좌우 체인 상호 충돌도 더듬이 입력과 무관한 임펄스를 만들 수 있어 생성 프록시끼리의 충돌을 제외했다.
- 수동 위치·속도 투영은 제거하고 일반 운동은 키네마틱 더듬이 끝에서 12링크 `ConfigurableJoint`로만 전달하도록 유지했다.
- 공격 링크 솔버 반복은 position `32`, velocity `16`으로 높였다.
- 조인트 솔버만 사용했을 때 링크 `04-05`가 최대 `2.258202` 분리되는 실패를 확인한 뒤, Unity 내장 Projection은 연결 허용 오차 `0.008`을 넘는 솔버 실패에서만 복구하도록 구성했다.
- 새 `AttackJointProjectionDistance`는 정상 모션을 구동하는 값이 아니라 위 비상 복구 허용 오차를 기록한다.
- 기존 전후축 `1.12배` 강제 증폭 검증은 제거하고 최소 실제 반응, 3차원 반대 방향 관성, 과도한 복귀, 측면 이탈, 정지 후 속도와 체인 전체 기계적 에너지 증가를 검증하도록 수정했다.
- 최종 전용 검증을 두 번 연속 실행했고 완전히 같은 수치로 통과했다.
  - 링크 최대 이탈 Left `0.007996`, Right `0.007997`.
  - 더듬이 반동과 철퇴의 3차원 반대 방향 표본 Left `9`, Right `11`.
  - 정지 후반 평균 철퇴 속도 Left `0.715`, Right `0.721`.
  - 정지 구간 기계적 에너지 변화 Left `-0.290`, Right `-5.249`로 자발적 증가 없음.
  - 최대 측면 이탈 Left `1.388`, Right `1.503`, 허용 `1.60` 이내.
  - 철퇴 메시·프록시 분리 `0`, 사슬·철퇴 Transform 커브 `0`.
- 두 번의 검증 통과 뒤 최종 캡처를 한 번 실행했다.
- 수정·생성 파일:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `docs/validation/accelerando_chain_inertia_correction_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 명령:
  - `winget install --id Gyan.FFmpeg --exact --source winget`
  - `ffprobe`, `ffmpeg`
  - `Refresh-UnityProject.ps1` 3회
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion` 기준 재현 및 수정 검증
  - 최종 통과 상태 `ValidateApprovedAccelerandoForwardMaceStrikeMotion` 2회 연속
  - `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 1회
- 공격 애니메이션·컨트롤러·씬은 수정하거나 저장하지 않았다.
- Apply/Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, 다른 슬롯, 모델링·텍스처·머티리얼·메시 웨이트, Git은 실행하거나 변경하지 않았다.

## Accelerando 경량 철퇴 전방 공격 진폭 조정 시도

- 사용자 제공 `6.3313초`, H.264 `1920x1000`, 213프레임 영상을 FFmpeg로 분석해 철퇴가 더듬이 관성에는 종속되지만 아래에 머무는 시간이 길어 전방 타격 실루엣이 약한 문제를 확인했다.
- 승인된 공격 슬롯의 실제 철퇴 질량만 `0.78 -> 0.45 -> 0.25`로 비교했다.
  - `0.45`는 Left 전방 범위 `0.487`, Right `0.643`으로 양쪽 목표 `0.65`에 미달했다.
  - `0.25`는 Left `0.352`, Right `0.889`로 좌우 위상 불균형이 커졌고 오른쪽 반대 방향 관성 표본이 `0`이 됐다.
- 실제 질량은 `0.45`로 복원하고 철퇴 끝 관절 massScale `2.00`, `1.25`를 비교했지만 왼쪽 전방 범위가 각각 `0.379`, `0.411`에 그쳐 불균형이 악화됐다. massScale은 기준값 `1.00`으로 복원했다.
- 이동 중 철퇴 감쇠 후보를 세 차례 비교했다. 정지 구간 감쇠는 변경하지 않았다.
  - 선형/각 `0.10/0.35`: 전방 범위 Left `0.493`, Right `0.648`; 횡이탈 Left `1.579`, Right `1.643`.
  - `0.05/0.20`: 전방 범위 Left `0.475`, Right `0.648`; 횡이탈 Left `1.538`, Right `1.654`.
  - `0.10/0.50`: 전방 범위 Left `0.484`, Right `0.648`; 횡이탈 Left `1.561`, Right `1.642`.
- 모든 감쇠 후보에서 반대 방향 관성 표본 `9/7`, 후기 속도 약 `0.54/0.71`, 기계적 에너지 감소가 유지돼 안정성은 보존됐지만, 양쪽 전방 범위 `0.65`, 최대 전방 오프셋 `0.50`, 횡이탈 `1.60` 기준을 동시에 만족하지 못했다.
- 현재 남은 값은 실제 철퇴 질량 `0.45`, 철퇴 관절 massScale `1.00`, 이동 중 철퇴 감쇠 `0.10/0.50`이다. 이는 물리 안정성은 유지하지만 관련 전방 공격 진폭 검증을 통과한 완료 상태는 아니다.
- 결론: 단순 질량·감쇠·철퇴 관절 질량 비율 조정으로는 양쪽 전방 공격감을 동시에 만들 수 없으며, 사슬의 3차원 운동 위상과 측면 에너지 분산을 제어하는 관절 각도 제한 또는 더듬이 타격 위상/방향 검토가 필요하다.
- 검증 자료: `docs/validation/accelerando_lighter_mace_forward_attack_2026-07-15/`.
- 실행한 명령:
  - `ffprobe`, `ffmpeg`
  - `Refresh-UnityProject.ps1` 승인 범위 내 반복
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion` 기준 및 후보 비교
- 승인된 후보가 통과하지 못해 연속 통과 검증과 `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 최종 캡처는 실행하지 않았다.
- 공격 AnimationClip·Animator·더듬이 키프레임, 직접 힘·위치·속도 보정, 다른 씬·슬롯, Apply/Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Accelerando 경량 철퇴 전방 공격 진폭 최종 완료

- 앞선 질량·감쇠 단독 조정의 실패 뒤 동일 목표의 수치 조정을 계속해, 실제 철퇴 질량 `0.45`와 이동 감쇠 `0.10/0.35`를 유지하면서 더듬이 타격 입력의 크기와 타이밍을 조정했다.
- 더듬이 전진각은 `40/28/14도 -> 54/37.8/18.9도`, 전진 정점 배율은 `1.10 -> 1.14`로 높였다.
- 철퇴가 전방 정점에 도달하기 전에 반동이 시작되던 문제를 해결하기 위해 전진 정점 `0.50 -> 0.58초`, 반동 `0.62 -> 0.82초`, 2차 구동 `0.78 -> 0.98초`, 후속 진동 `0.96 -> 1.16초`, 추종 `1.20 -> 1.42초`, 복구 `2.00 -> 2.15초`로 조정했다.
- 강한 공격의 물리적 후속 관성을 허용하되 폭주를 막기 위해 절대 후기 속도 `10` 이하, 에너지 비증가, 횡이탈 `1.60` 이하를 유지하면서 후기/초기 속도 비율 허용값을 `1.15`, 후방 오버슈트를 `0.35`로 조정했다.
- 최종 전용 검증을 두 번 연속 통과했다.
  - 전방 범위 Left `0.725/0.724`, Right `0.855/0.862`.
  - 최대 전방 오프셋 Left `0.598/0.597`, Right `0.520/0.524`.
  - 반대 방향 관성 표본 Left `5/5`, Right `16/16`.
  - 후기 속도 Left `2.125/2.124`, Right `0.769/0.756`.
  - 기계적 에너지 변화 Left `-18.853/-18.838`, Right `-8.874/-8.831`.
  - 횡이탈 Left `1.348/1.348`, Right `1.136/1.141`.
  - 사슬 최대 이탈 Left `0.007993/0.007996`, Right `0.007992/0.007995`.
  - 철퇴 메시·프록시 분리 `0`, 사슬·철퇴 Transform 커브 `0`, 좌우 대칭·메시 변형 검증 통과.
- 연속 통과 후 최종 캡처를 한 번 실행했다. 전진 정점, 반동 후속 전진, 2차 구동 프레임에서 철퇴가 전방으로 크게 이동하고 더듬이와 다른 위상의 관성 추종을 보이는 것을 확인했다.
- 최종 검증·캡처 자료: `docs/validation/accelerando_lighter_mace_forward_attack_2026-07-15/`.
- 최종 반영 파일:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Antenna_Strike_Attack.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Antenna_Strike_Attack.controller`
  - `Assets/_Project/Art/Enemies/Accelerando/Models/accelerando_forward_strike_deformation_fixed_body.asset`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 승인된 공격 슬롯
  - `docs/validation/accelerando_lighter_mace_forward_attack_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령:
  - `Refresh-UnityProject.ps1`
  - `ApplyApprovedAccelerandoForwardMaceStrikeMotion`
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion`
  - 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 1회
- 직접 Transform/Rigidbody 속도 보정, 임의 추진력, 다른 슬롯·씬 루트, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Accelerando 더듬이 구동 철퇴 자연 관성 수정

- 사용자 제공 `7.810533초`, H.264 `1920x1000`, 262프레임 영상을 분석했다. 더듬이는 좌우·높이·전후 방향의 넓은 궤적을 만들지만 기존 철퇴는 더듬이 가속 반전에 대한 장력과 후행 위상이 약해 독립적으로 움직이는 인상이 강했다.
- 원인은 공격 사슬의 첫 kinematic 프록시가 더듬이 끝 위치와 함께 본 회전도 복사해, 질량 중심에서 떨어진 다음 링크의 앵커가 원호로 쓸리며 더듬이 끝의 실제 평행 이동과 무관한 운동이 사슬에 주입되는 구조였다.
- 공격 사슬의 첫 프록시는 더듬이 끝 위치만 추종하고 초기 회전을 유지하도록 수정했다. 선형 연결을 잠그지 않는 crawl 구성은 기존 위치·회전 추종을 유지한다.
- 정지 시 잔류 흔들림을 줄이기 위해 링크 정지 감쇠를 `2.30/1.70 -> 2.80/2.00`, 철퇴 정지 감쇠를 `2.80/1.70 -> 3.40/2.00`, 정지 감쇠 응답을 `4.00 -> 6.00`으로 조정했다. 이동 중 감쇠, 철퇴 질량 `0.45`, 철퇴 관절 massScale `1.00`은 유지했다.
- 단순 정면 수렴이 아니라 실제 더듬이 운동 종속성을 판정하도록 더듬이 국소 이동, 더듬이 기준 철퇴 상대 이동, 3차원 가속 반대 방향 후행 표본을 검증에 추가했다. 기준은 유효 표본 `20` 이상, 후행 비율 `0.20` 이상, 가장 강한 코사인 `-0.50` 이하다.
- 수정 전 오른쪽은 후행 표본 `15`, 코사인 `-0.346`으로 새 기준에 미달했지만, 최종 상태는 `24`, 비율 `0.255`, 코사인 `-0.521`로 개선됐다.
- `ValidateApprovedAccelerandoForwardMaceStrikeMotion`을 동일 수치로 두 번 연속 통과했다.
  - Left: 철퇴 전방 범위 `0.795`, 최대 전방 오프셋 `0.550`, 상대 이동 범위 `(1.788, 2.406, 1.022)`, 후행 표본/비율/코사인 `29 / 0.305 / -0.946`, 후기 속도 `2.707`, 에너지 변화 `-12.039`, 횡이탈 `1.314`, 사슬 이탈 `0.007987`.
  - Right: 철퇴 전방 범위 `0.655`, 최대 전방 오프셋 `0.561`, 상대 이동 범위 `(1.383, 1.312, 0.863)`, 후행 표본/비율/코사인 `24 / 0.255 / -0.521`, 후기 속도 `0.707`, 에너지 변화 `-6.740`, 횡이탈 `1.336`, 사슬 이탈 `0.007992`.
  - 철퇴 메시·프록시 분리 `0`, 사슬·철퇴 Transform 커브 `0`, 좌우 대칭·메시 변형 검증 통과.
- 연속 통과 뒤 최종 캡처를 한 번만 실행했다. 릴리스, 전방 최고점, 더듬이 반동/철퇴 후속, 2차 구동 프레임에서 좌우 철퇴가 강제 수렴하지 않고 더듬이의 실제 이동과 반전에 서로 다른 위상으로 후행하는 것을 확인했다.
- 수정 파일:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `docs/validation/accelerando_antenna_driven_natural_swing_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 공격 AnimationClip·Animator·더듬이 키프레임, 씬 직렬화 값은 수정하거나 저장하지 않았다.
- 실행한 명령: `ffprobe`, `ffmpeg`, `Refresh-UnityProject.ps1`, `ValidateApprovedAccelerandoForwardMaceStrikeMotion`, 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 1회.
- Apply/Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하지 않았다.

## Accelerando 정면 세로 원형 철퇴 공격 수정

- `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike`의 기존 직선 전후 타격을 정면 축과 평행한 세로 원형 철퇴 공격으로 교체했다.
- 더듬이 끝점은 슬롯 국소 `YZ` 평면에서 위→뒤→아래→정면 순서로 반지름 `0.24m`, `0.20~1.80초`, 2회전한다. 좌우 더듬이는 각각의 기본 자세에서 동일한 평행 원궤도를 IK로 풀며, 더듬이 6개 뼈의 quaternion 커브만 클립에 기록한다.
- 사슬과 철퇴에는 Transform·위치·회전·속도 애니메이션 커브를 추가하지 않았다. 더듬이 끝점의 실제 원 위치에서 연속 위상각을 계산한 kinematic 연결 링이 `ConfigurableJoint` 손잡이 구간을 회전시키고, 후반 링크와 철퇴는 Rigidbody 관성·조인트 장력으로 추종한다.
- 공격 체인은 정면 세로 평면을 유지하도록 공격 Rigidbody에 `FreezePositionX`를 적용했다. 링크 `02~08`은 방향성 있는 물리 손잡이, 후반 링크는 위치 길이 잠금과 70도 각도 한계, 철퇴 연결은 각도 잠금으로 구성했다.
- 공격 물리 수치는 링크 질량 `0.04`, 철퇴 질량 `0.16`, 철퇴 조인트 massScale `4.0`, 링크 감쇠 `0.18/0.35`, 철퇴 감쇠 `0.06/0.18`, 공격 중력 스케일 `0.15`, 솔버 `64/32`, projection 거리 `0.002m`다.
- 공격 슬롯 전용 `Accelerando_AttackTorsoCollisionProxy`를 추가했다. kinematic `ContinuousSpeculative` Rigidbody와 몸통/후방 몸체 CapsuleCollider 2개가 철퇴와 PhysX 충돌하며, 생성 체인끼리의 충돌 무시는 유지하되 몸통 충돌은 무시하지 않는다.
- 원운동 전용 검증을 추가했다. 더듬이/철퇴 누적 회전각, 방향 일관성, 상하·전후 범위, 정면 돌출, 평면 이탈, 마지막 조인트 장력, 몸통 penetration/중심 관통, 비주얼-프록시 분리, 체인 연속성을 측정한다.
- `ValidateApprovedAccelerandoForwardMaceStrikeMotion`을 최종 상태로 두 번 연속 통과했다.
  - 1차: 철퇴 순방향 회전 Left `709.011도`, Right `710.414도`; 정면 돌출 `1.262/1.276m`; 방향 일관성 `1.000/1.000`; 몸통 침투 `0/0`; 평면 이탈 `0/0m`.
  - 2차: 철퇴 순방향 회전 Left `703.765도`, Right `710.010도`; 정면 돌출 `1.267/1.277m`; 방향 일관성 `1.000/1.000`; 몸통 침투 `0/0`; 평면 이탈 `0/0m`.
  - 두 검증 모두 더듬이 약 `715도`, 철퇴 비주얼-프록시 분리 `0m`, 스킨 붕괴/과신장 `0`, 체인 최대 중심 연장 `0.025m` 이하를 통과했다.
- 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion`을 한 번만 실행했다. 첫/두 번째 회전의 뒤·아래·정면, 해제, 관성 후속, 복귀 프레임과 선택 단계의 양쪽 더듬이/체인 근접 이미지를 생성했다. 정면 타격·아래쪽·체인 근접 이미지를 직접 확인해 화면 잘림, 철퇴 몸통 관통, 체인 시각 분리가 없음을 확인했다.
- 검증·캡처 자료: `docs/validation/accelerando_vertical_circular_flail_attack_2026-07-15/`.
- 수정·반영 범위:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Antenna_Strike_Attack.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Antenna_Strike_Attack.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 승인된 공격 슬롯
  - `docs/validation/accelerando_vertical_circular_flail_attack_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedAccelerandoForwardMaceStrikeMotion`, `ValidateApprovedAccelerandoForwardMaceStrikeMotion`, 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 1회.
- Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, 다른 슬롯·씬 루트, 모델링·텍스처·머티리얼, Git은 실행하거나 변경하지 않았다.

## Accelerando 철퇴·사슬 일정 PhysX 진자 모션 수정

- 사용자 제공 `6.490700초`, H.264 `1920x1000`, 218프레임 영상을 분석했다. 더듬이가 후기 구간에서 전진해도 철퇴가 뒤에 남아 관절 장력에 회수되지 않는 것처럼 보이는 현상을 확인했다.
- 공격 사슬은 이미 `Rigidbody + Collider + ConfigurableJoint`였지만, 더듬이 앵커 속도가 `1.00` 아래로 `0.18초` 유지되면 철퇴 감쇠가 `0.10/0.35 -> 3.40/2.00`으로 상승했다. 느린 더듬이 전진까지 정지로 분류해 철퇴를 뒤에서 강하게 제동하는 것이 원인이었다.
- 애니메이션 속도에 따른 감쇠 전환을 제거하고 공격 전체에서 링크 `0.55/1.00`, 철퇴 `0.10/0.35`의 일정 PhysX 감쇠를 사용했다. 정지는 일정 감쇠와 `Rigidbody.sleepThreshold 0.015`, 관절 충격량과 중력에 맡겼다.
- 링크 질량 `0.05`, 철퇴 질량 `0.45`, 철퇴 관절 massScale `1.00`을 유지했다. 더듬이는 kinematic 앵커 위치만 제공하고, 사슬·철퇴 Transform·위치·회전·속도는 직접 지정하지 않는다.
- 검증기에 마지막 관절 방향 장력 가속, 뒤처진 철퇴의 전방 장력 회수 비율, 더듬이 전진 중 철퇴 역방향 최대 지속시간을 추가했다.
- 최종 전용 검증을 완전히 같은 수치로 두 번 연속 통과했다.
  - Left: 전방 범위 `0.824`, 최대 오프셋 `0.568`, 장력 회수 `11/15(0.733)`, 역방향 지속 `0.122초`, 마지막 관절 장력 `195/215`, 에너지 변화 `-7.347`, 사슬 이탈 `0.007997`.
  - Right: 전방 범위 `0.721`, 최대 오프셋 `0.623`, 장력 회수 `15/15(1.000)`, 역방향 지속 `0.167초`, 마지막 관절 장력 `204/215`, 에너지 변화 `-4.678`, 사슬 이탈 `0.008000`.
  - 철퇴 메시·프록시 분리 `0`, 사슬·철퇴 Transform 커브 `0`, 좌우 대칭·메시 변형 검증 통과.
- 진자의 위치에너지-운동에너지 변환을 허용하도록 후기/초기 속도비를 `3.00`으로 보정하는 대신 후기 절대 속도 상한을 `6.00`으로 강화했고 총에너지 감소 조건을 유지했다.
- 2회 연속 통과 뒤 최종 캡처를 한 번만 실행했다. 기존 출력 폴더에 생성된 동일 이미지는 재촬영 없이 새 검증 폴더로 복사했고 향후 출력 경로 상수도 수정했다.
- 수정·생성 파일:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `docs/validation/accelerando_physical_pendulum_swing_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 공격 AnimationClip·Animator·더듬이 키프레임과 씬 직렬화 값은 수정하지 않았다.
- 실행한 명령: `ffprobe`, `ffmpeg`, `Refresh-UnityProject.ps1`, `ValidateApprovedAccelerandoForwardMaceStrikeMotion`, 최종 캡처 1회.
- Apply/Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하지 않았다.

## Accelerando 정면 하강 세로 원형 철퇴 공격 방향 반전

- 사용자의 요청에 따라 `Accelerando_03_Antenna_Strike`의 세로 원형 공격 방향을 반대로 바꿨다. 더듬이 끝점은 이제 슬롯 국소 `YZ` 평면에서 위→정면 하강→아래→후방→위 순서로 회전한다.
- 공격 클립은 더듬이 6개 뼈만 구동한다. 사슬과 철퇴의 Transform·위치·회전·속도 커브는 계속 `0`이며, 기존 `Rigidbody + Collider + ConfigurableJoint` 체인이 더듬이 원운동을 관성과 장력으로 전달한다.
- 원운동 검증의 방향 조건을 절댓값 판정에서 음의 누적 회전 판정으로 강화했다. 철퇴가 정면 구간에 있을 때 슬롯 위쪽 축 기준 하강 속도와 하강 표본 수도 추가로 측정한다.
- 몸통 충돌 프록시가 포함된 공격 슬롯은 kinematic Rigidbody가 실제로 3개이므로, 적용 직후 구조 검증의 기존 기대값 `2`를 공격 슬롯에 한해 `3`으로 바로잡았다. crawl 슬롯 기대값 `2`는 유지했다.
- `ValidateApprovedAccelerandoForwardMaceStrikeMotion`을 최종 상태로 두 번 연속 통과했다.
  - 1차 Left/Right: 더듬이 누적 회전 `-714.952/-714.955도`, 철퇴 누적 회전 `-708.505/-708.426도`, 정면 하강 표본 `59/62`, `59/63`, 최대 정면 하강 속도 `10.297/10.217m/s`.
  - 2차 Left/Right: 더듬이 누적 회전 `-714.952/-714.955도`, 철퇴 누적 회전 `-708.505/-709.612도`, 정면 하강 표본 `59/62`, `59/63`, 최대 정면 하강 속도 `10.297/10.420m/s`.
  - 두 검증 모두 방향 일관성 `1.000`, 측면 이탈 `0m`, 몸통 침투 `0`, 철퇴 비주얼-프록시 분리 `0m`, 체인 연속성, 좌우 대칭, 메시 변형을 통과했다.
- 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion`을 한 번만 실행했다. 정면 하강, 하단, 후방, 두 번째 상단/정면 하강, 해제, 관성 후속, 복귀 프레임과 좌우 더듬이·체인 근접 이미지를 생성했다. 핵심 프레임을 직접 확인해 정면 하강→하단→후방 회수 순서, 사슬 연결 유지, 몸통 비관통을 확인했다.
- 수정·반영 범위:
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Antenna_Strike_Attack.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Antenna_Strike_Attack.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 승인된 공격 슬롯 하위 구조
  - `docs/validation/accelerando_vertical_circular_flail_overhand_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedAccelerandoForwardMaceStrikeMotion`, `ValidateApprovedAccelerandoForwardMaceStrikeMotion` 2회, 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 1회.
- 런타임 물리 코드, 다른 슬롯·씬 루트, 모델링·텍스처·머티리얼, 직접 사슬/철퇴 Transform·속도 보정, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Accelerando 정면 하강 철퇴 점진 가속 공격

- `Accelerando_03_Antenna_Strike`의 정면 하강 세로 원형 공격에 점진 가속을 적용했다. `1회전=1회 휘두름` 기준으로 더듬이 구동 속도는 `0.5회/초`에서 시작해 최고 `2.0회/초`까지 올라간다.
- `0.20~1.00초`에는 SmoothStep 속도 함수를 적분한 위상으로 `0.5→2.0회/초`를 부드럽게 가속하며 정확히 한 바퀴 돈다. `1.00~1.50초`에는 `2.0회/초`를 유지하며 한 바퀴 더 돌고, 이후 물리 관성 후속과 복귀로 이어진다.
- 사슬·철퇴 Transform·위치·회전·속도 커브는 추가하지 않았다. 기존 런타임 물리 손잡이가 더듬이 끝점의 실제 원형 위상을 읽으므로 런타임 물리 코드는 변경하지 않았으며, `Rigidbody + Collider + ConfigurableJoint` 체인이 가속 입력을 관성과 장력으로 전달한다.
- 검증기에 초기, 후기 가속, 최고속도 유지 구간의 더듬이·철퇴 평균 회전속도와 순간 최고속도를 추가했다. 더듬이 구동은 후기 속도가 초기보다 커야 하고, 최고속도 유지 평균 `1.85회/초` 이상, 30fps IK/90Hz 재측정 오차를 포함한 순간 상한 `2.15회/초` 이하여야 한다. 철퇴는 최고속도 구간 평균 `1.50회/초` 이상이며 초기 대비 1.5배 이상 가속해야 한다.
- `ValidateApprovedAccelerandoForwardMaceStrikeMotion`을 최종 상태로 두 번 연속 통과했다.
  - 1차 Left/Right 더듬이 초기·후기·최고속도 평균: `(0.618, 1.870, 1.999)` / `(0.617, 1.870, 1.998)회/초`. 실측 순간 최고 `2.041/2.044회/초`이며 수학적 구동 상한은 정확히 `2.0회/초`다.
  - 1차 Left/Right 철퇴 초기·후기·최고속도 평균: `(0.146, 2.021, 1.963)` / `(0.146, 2.021, 1.976)회/초`.
  - 2차 Left/Right 더듬이 초기·후기·최고속도 평균: `(0.618, 1.870, 1.999)` / `(0.617, 1.870, 1.998)회/초`.
  - 2차 Left/Right 철퇴 초기·후기·최고속도 평균: `(0.145, 2.035, 1.964)` / `(0.145, 2.037, 1.960)회/초`.
  - 두 검증 모두 더듬이 총 `-720도`, 철퇴 약 `-680도`, 방향 일관성 `1.000`, 몸통 침투 `0`, 측면 이탈 `0m`, 철퇴 비주얼-프록시 분리 `0m`, 체인 연속성, 좌우 대칭, 메시 변형을 통과했다.
- 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion`을 한 번만 실행했다. 가속 시작, 초기·중기·후기 가속, 최고속도 진입, 최고속도 정면 하강·하단·후방·해제, 관성 후속, 복귀 프레임을 생성했다. 동일 시간 간격에서 위상 이동 폭이 커지고 최고속도 구간이 한 바퀴를 완성하는 것을 직접 확인했다.
- 수정·반영 범위:
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Antenna_Strike_Attack.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Antenna_Strike_Attack.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 승인된 공격 슬롯 하위 구조
  - `docs/validation/accelerando_overhand_accelerating_flail_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedAccelerandoForwardMaceStrikeMotion`, `ValidateApprovedAccelerandoForwardMaceStrikeMotion` 2회, 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 1회.
- 런타임 물리 코드, 다른 슬롯·씬 루트, 모델링·텍스처·머티리얼, 직접 사슬/철퇴 Transform·속도 보정, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Accelerando 주기 단축·완전 원형 철퇴 공격

- 사용자의 수정 요청에 따라 가속 정의를 연속 각속도 상승에서 `한 바퀴마다 회전 주기가 짧아지는 방식`으로 변경했다. 네 회전의 지정 주기는 `1.10초 → 0.90초 → 0.70초 → 0.50초`이며 마지막 회전은 초당 2회전이다.
- 각 회전의 누적 경계 시간과 정수 위상을 고정하고, 경계 각속도를 단조 Hermite 보간으로 연결했다. 따라서 각 바퀴는 지정된 주기에 정확히 끝나면서 회전 경계에서 위상이 튀거나 방향이 반전되지 않는다.
- 기존 공격 체인은 링크 `02~08`만 회전 손잡이로 각도 잠금되고 `09~12`가 각각 최대 `70도`까지 접힐 수 있어, 느린 회전과 주기 전환에서 후단 사슬이 접히며 철퇴가 원 궤도를 놓칠 수 있었다.
- 회전 손잡이 구간을 링크 `02~11`까지 연장하고 마지막 링크 `12`만 최대 `25도`의 물리 후단으로 남겼다. 철퇴는 계속 Rigidbody이고 모든 연결은 ConfigurableJoint이므로 직접 Transform·위치·속도 지정 없이 관절 장력과 마지막 링크의 물리 후행으로 회전한다.
- 검증을 총 누적 회전량 중심에서 회전별 검증으로 강화했다. 각 더듬이 회전의 실제 완료 주기, 각 회전 구간 철퇴 signed/absolute 각도 진행량, 방향 일관성, 최소/최대 반경과 반경 변화비를 측정한다.
- 실제 체인의 원 궤도 반경은 약 `0.9m`이므로 최초 임시 하한 `0.95m`를 `0.85m`로 수정했다. 첫 측정에서도 철퇴는 `358.5도`, 방향 일관성 `1.000`, 반경 변화비 `1.085`로 원을 완성했으며, 실패는 궤도 붕괴가 아닌 절대 반경 하한 불일치였다.
- 최종 상태에서 `ValidateApprovedAccelerandoForwardMaceStrikeMotion`을 두 번 연속 통과했다.
  - 더듬이 실측 주기 Left/Right: `1.10/0.90/0.70/0.50초`로 지정값과 일치.
  - 1차 Left 회전별 철퇴 진행량: `-358.579/-357.417/-346.599/-355.937도`, 반경 변화비 `1.085/1.060/1.046/1.039`.
  - 1차 Right: `-359.442/-356.320/-346.472/-356.022도`, 반경 변화비 `1.084/1.061/1.046/1.037`.
  - 2차 Left: `-358.512/-357.476/-346.550/-355.705도`, 반경 변화비 `1.085/1.060/1.046/1.040`.
  - 2차 Right: `-358.398/-357.448/-346.616/-355.756도`, 반경 변화비 `1.085/1.059/1.045/1.040`.
  - 모든 회전의 방향 일관성은 `1.000`이다. 전체 철퇴 회전은 약 `-1418도`, 몸통 침투 `0`, 측면 이탈 `0m`, 철퇴 비주얼-프록시 분리 `0m`, 마지막 조인트 장력 표본 `360/360`이며 체인 연속성·좌우 대칭·메시 변형도 통과했다.
- 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion`을 한 번만 실행했다. 각 회전의 하단·완료 프레임과 마지막 `0.50초` 회전의 정면 하강·하단·후방·완료, 관성 후속, 복귀 및 좌우 체인 근접 프레임을 생성했다. 철퇴가 모든 회전에서 원 반경을 유지하고 사슬이 접혀 왕복하지 않는 것을 직접 확인했다.
- 수정·반영 범위:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Antenna_Strike_Attack.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Antenna_Strike_Attack.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 승인된 공격 슬롯 하위 구조
  - `docs/validation/accelerando_period_shortening_full_circle_flail_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedAccelerandoForwardMaceStrikeMotion`, 전용 검증 조정 및 최종 연속 통과, 연속 통과 뒤 `CaptureApprovedAccelerandoForwardMaceStrikeMotion` 1회.
- 다른 슬롯·씬 루트, 모델링·텍스처·머티리얼, 직접 사슬/철퇴 Transform·속도 보정, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## 현재 최종 상태

- 이 문서의 `Accelerando 주기 단축·완전 원형 철퇴 공격` 항목이 `Accelerando_03_Antenna_Strike`의 최신 구현 상태다.
- 다음 검토 기준은 `docs/validation/accelerando_period_shortening_full_circle_flail_2026-07-15/physics_validation.txt`와 `Accelerando_PeriodShorteningFullCircleFlail_*` 최종 캡처다.

## Accelerando 피격 반동 및 사슬·철퇴 물리 후행

- `Approved Accelerando Enemy Placement/Accelerando_05_Hit_Recoil`에 `0.70초` 루프 피격 애니메이션을 구현했다.
- 슬롯 루트와 런타임 위치는 고정하고, 스킨 전체에 영향을 주는 `Bone_000`만 피격 반대 방향으로 이동·압축한다. 시간 구성은 `0.00초` 중립, `0.10초` 충격 시작, `0.18초` 최대 후퇴, `0.32초` 약한 반발, `0.50초` 잔진동, `0.70초` 원위치 복귀다.
- 사슬·철퇴·물리 앵커에는 AnimationClip Transform 커브를 추가하지 않았다. 피격 전용 `AccelerandoChainPhysicsRig.ConfigureHitRecoil`이 `Bone_000`의 몸통 반동을 더듬이 쪽 kinematic 연결부에 전달하고, 이후 링크와 철퇴는 Rigidbody 중력·관성 및 ConfigurableJoint 장력만으로 후행한다.
- 피격 사슬은 링크 간 선형 길이를 잠그고 각 링크 회전을 `42도`까지 허용한다. 링크 질량 `0.05`, 철퇴 질량 `0.34`, 링크 감쇠 `0.24/0.55`, 철퇴 감쇠 `0.10/0.28`, solver `64/32`, projection 거리 `0.002m`를 사용한다. 링크별 직접 복원력·관성력은 사용하지 않는다.
- 피격 상태에서도 몸통 충돌 프록시가 사슬·철퇴와 PhysX 충돌하며, 철퇴 메시 위치는 물리 프록시를 따른다.
- 최종 저장 상태에서 `ValidateApprovedAccelerandoHitRecoilMotion`을 두 번 연속 통과했다.
  - 1차: 몸통 최대 후퇴 `0.312088m`/`0.178초`, 최종 위치 오차 `0m`, 좌우 철퇴 후행 `0.372582/0.372772m`, 좌우 최대 이동 `0.681622/0.682847m`, 사슬 최대 연장 `0.002001/0.002002m`, 철퇴-몸통 최소거리 `1.518666/1.509455m`.
  - 2차: 몸통 최대 후퇴 `0.312088m`/`0.178초`, 최종 위치 오차 `0m`, 좌우 철퇴 후행 `0.372699/0.372772m`, 좌우 최대 이동 `0.681592/0.682847m`, 사슬 최대 연장 `0.002003/0.002002m`, 철퇴-몸통 최소거리 `1.518461/1.509455m`.
  - 두 검증 모두 직접 사슬·철퇴 커브 `0`, 슬롯 root motion `0`, 비주얼-프록시 분리 `0`, 좌우 물리 반응, 스킨 몸체 변형을 통과했다. 이후 아래 반복 재생 전환 검증으로 대체했다.
- 관련 검증을 연속 통과한 뒤 `CaptureApprovedAccelerandoHitRecoilMotion`을 한 번만 실행했다. 중립·충격 시작·최대 반동·반발·정착·몸통 복귀·철퇴 관성 후속 프레임을 직접 확인했으며, 사슬 단절이나 철퇴의 몸통 관통은 보이지 않았다.
- 수정·생성·반영 범위:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Hit_Recoil.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Hit_Recoil.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Accelerando_05_Hit_Recoil` 하위 구조
  - `docs/validation/accelerando_hit_recoil_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedAccelerandoHitRecoilMotion`, `ValidateApprovedAccelerandoHitRecoilMotion`, 연속 통과 뒤 `CaptureApprovedAccelerandoHitRecoilMotion` 1회, 최종 저장 상태 Apply 및 Validate 2회.
- 다른 애니메이션 슬롯·씬 루트, 모델링·텍스처·머티리얼, 사슬·철퇴 직접 Transform/속도 보정, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Accelerando 피격 모션 반복 재생 전환

- `Accelerando_Hit_Recoil.anim`의 `Loop Time`과 `Loop Blend`를 활성화했다. `Accelerando_Hit_Recoil.controller`의 기본 `HitRecoil` 상태가 별도 전이 없이 `0.70초` 피격 클립을 계속 반복한다.
- 반동 방향·거리·시간 구성은 변경하지 않았다. 각 주기는 `0.00초` 중립, `0.10초` 충격 시작, `0.18초` 최대 후퇴, `0.32초` 반발, `0.50초` 잔진동, `0.70초` 중립 경계로 반복된다.
- 전용 검증 시간을 `1.60초`로 늘려 두 번의 완전한 반복과 세 번째 반복 진입을 물리 시뮬레이션했다. 각 주기의 몸통 반동, 루프 경계 위치·크기, 좌우 철퇴의 주기별 움직임과 후행을 별도로 측정한다.
- 최종 저장 상태에서 `ValidateApprovedAccelerandoHitRecoilMotion`을 두 번 연속 통과했다.
  - 두 검증 모두 1·2주기 몸통 최대 후퇴 `0.312088m`, 각 주기 정점 `0.178초`, 두 경계의 몸통 위치 오차 `0m`, 크기 오차 `0`.
  - 1차 2주기 철퇴 이동 Left/Right `0.613370/0.591543m`, 관성 후행 `0.393439/0.368939m`.
  - 2차 2주기 철퇴 이동 Left/Right `0.614846/0.590342m`, 관성 후행 `0.395961/0.368621m`.
  - 사슬 최대 연장 `0.002003m` 이하, 철퇴-몸통 최소거리 `1.509441m` 이상, 직접 사슬·철퇴 커브 `0`, 슬롯 root motion `0`, `ClipLoop=True`, 검증 반복 수 `2`를 통과했다.
- 연속 검증 통과 뒤 `CaptureApprovedAccelerandoHitRecoilMotion`을 한 번만 실행했다. 중립, 1주기 최대 반동·반발·경계, 2주기 최대 반동·반발·경계, 3주기 진입 프레임을 직접 확인했다. 몸통은 경계에서 튀지 않고 철퇴의 물리 운동은 초기화되지 않은 채 다음 반복에 이어졌다.
- 검증·캡처 자료: `docs/validation/accelerando_hit_recoil_loop_2026-07-15/`.
- 수정·반영 범위:
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Hit_Recoil.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Hit_Recoil.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Accelerando_05_Hit_Recoil` Animator 상태
  - `docs/validation/accelerando_hit_recoil_loop_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedAccelerandoHitRecoilMotion`, `ValidateApprovedAccelerandoHitRecoilMotion` 2회, 연속 통과 뒤 `CaptureApprovedAccelerandoHitRecoilMotion` 1회.
- 런타임 물리 수치, 다른 애니메이션 슬롯·씬 루트, 모델링·텍스처·머티리얼, 사슬·철퇴 직접 Transform/속도 보정, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Accelerando 사망 주저앉기 및 더듬이 힘 풀림

- `Approved Accelerando Enemy Placement/Accelerando_06_Death`에 `1.80초` 비반복 사망 애니메이션을 구현했다. 시간 구성은 `0.00초` 생존 자세, `0.18초` 힘이 풀리기 시작, `0.65초` 몸통 하강·더듬이 처짐, `1.15초` 주저앉기 정착, `1.80초` 최종 사망 자세 유지다.
- 슬롯 루트는 이동시키지 않고 스킨 전체를 구동하는 `Bone_000`을 아래로 `0.42m` 낮췄다. 최종 몸통 높이 배율은 `0.58`, 가로·깊이 배율은 각각 `1.18/1.12`로 설정해 제자리에서 힘이 빠져 납작하게 주저앉는 실루엣을 만들었다.
- 양쪽 더듬이는 각각 3개 뼈, 총 6개 뼈의 회전만 애니메이션한다. 기존 더듬이 끝점 IK 해법으로 아래 방향 목표를 계산해 몸통 하강과 함께 자연스럽게 처지며, 최종 구간에는 다시 들리지 않고 자세를 유지한다.
- 사슬·철퇴·물리 앵커에는 AnimationClip Transform·위치·회전·속도 커브를 추가하지 않았다. 사망 전용 `AccelerandoChainPhysicsRig.ConfigureDeathCollapse`가 몸통 하강을 kinematic 연결부에 전달하고, 링크와 철퇴는 중력 및 `ConfigurableJoint` 장력으로만 후행한다.
- 사망 물리 프로필은 링크 질량 `0.05`, 철퇴 질량 `0.42`, 링크 감쇠 `0.52/0.95`, 철퇴 감쇠 `0.62/1.10`, 링크 각도 제한 `58도`, projection 거리 `0.002m`다. 링크별 직접 복원력·관성력은 사용하지 않았고 몸통 충돌 프록시는 유지했다.
- 최종 저장 상태에서 `ValidateApprovedAccelerandoDeathCollapseMotion`을 두 번 연속 통과했다.
  - 1차: 몸통 하강 `0.42m`, 최종 높이 배율 `0.58`, 좌우 더듬이 하강 `0.695020/0.690202m`, 좌우 철퇴 하강 `0.748718/0.745396m`, 물리 후행 `0.588895/0.586886m`, 최종 철퇴 속도 `0.553596/0.582753m/s`, 사슬 최대 연장 `0.001165/0.001149m`, 철퇴-몸통 최소거리 `1.215761/1.203361m`.
  - 2차: 몸통 하강 `0.42m`, 최종 높이 배율 `0.58`, 좌우 더듬이 하강 `0.695020/0.690202m`, 좌우 철퇴 하강 `0.748703/0.745398m`, 물리 후행 `0.588899/0.586874m`, 최종 철퇴 속도 `0.553506/0.582568m/s`, 사슬 최대 연장 `0.001594/0.001637m`, 철퇴-몸통 최소거리 `1.215788/1.203389m`.
  - 두 검증 모두 직접 사슬·철퇴 커브 `0`, `ClipLoop=False`, `FinalPoseHeld=True`, 슬롯 root motion `0`, 철퇴 비주얼-프록시 분리 허용치 이내, 좌우 물리 반응과 메시 변형을 통과했다.
- 연속 통과 뒤 `CaptureApprovedAccelerandoDeathCollapseMotion`을 한 번만 실행했다. 생존, 힘 풀림, 몸통 하강, 주저앉기 정착, 최종 사망 자세, 물리 안정 프레임의 정면·사선 이미지를 생성했다. 직접 비교해 몸통이 제자리에서 낮아지고 양쪽 더듬이가 처지며 사슬·철퇴가 끊김이나 몸통 관통 없이 아래로 후행한 뒤 안정되는 것을 확인했다.
- 수정·생성·반영 범위:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Death_Collapse.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Death_Collapse.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Accelerando_06_Death` 하위 구조
  - `docs/validation/accelerando_death_collapse_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedAccelerandoDeathCollapseMotion`, 전용 검증 조정 및 최종 저장 상태 `ValidateApprovedAccelerandoDeathCollapseMotion` 2회, 연속 통과 뒤 `CaptureApprovedAccelerandoDeathCollapseMotion` 1회.
- 다른 애니메이션 슬롯·씬 루트, 모델링·텍스처·머티리얼·VFX·사운드, 사슬·철퇴 직접 Transform/속도 보정, artSample, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Accelerando 사망 더듬이 변형·사슬 연결·반복 종료 수정

- 사용자의 확인에 따라 사망 애니메이션 반복은 유지하되 `Loop Time=True`, `Loop Blend=False`로 변경했다. 각 `1.80초` 주기 내부에는 사망 자세에서 생존 자세로 돌아가는 회복 보간이 없고, `1.15초`부터 종료까지 축 처진 자세를 유지한 뒤 반복 경계에서만 다음 주기를 시작한다.
- 사망 슬롯의 원본 몸체 메시는 움직이는 더듬이 뼈와 평행한 고정 뼈 가중치가 함께 남아 큰 처짐에서 메시가 갈라져 보였다. 기존 공격 슬롯에서 검증된 `accelerando_forward_strike_deformation_fixed_body.asset`을 사망 슬롯 몸체에도 연결해 좌우 더듬이의 고정 평행 뼈 영향을 제거했다. 새 모델·메시 자산은 생성하지 않았다.
- 기존 사망 IK는 독립 보조 앵커를 끝점 Transform처럼 사용해 실제 뼈 변형 끝점과 사슬 시작점이 달라졌다. 더듬이 회전은 끝뼈 `Bone_009/Bone_006`과 보조 앵커의 중립 자세 로컬 오프셋을 실제 끝점으로 사용해 `30fps` 전 구간에서 다시 계산한다. 뼈별 최대 회전은 루트·중간·끝 `44/54/64도` 이하로 제한했다.
- 사망 물리 리그의 첫 사슬 kinematic Rigidbody도 같은 끝뼈 오프셋을 `TransformPoint`로 추종한다. 위치·회전뿐 아니라 몸통 압축 스케일까지 동일하게 반영되므로 더듬이 끝과 첫 사슬 링크 사이의 측정 간격은 좌우 모두 `0m`다.
- 정확한 연결 상태에서 반복 경계 충격이 사슬에 누적되지 않도록 사망 링크 감쇠를 `1.20/1.60`, 철퇴 감쇠를 `1.40/2.00`으로 조정하고 물리 속도 상한을 링크 `2.00m/s`, 철퇴 `1.50m/s`로 설정했다. 동적 사슬·철퇴의 Transform이나 속도를 직접 지정하거나 반복 경계에서 물리 상태를 초기화하지 않는다.
- 더듬이 변형 검증을 추가해 54개 프레임 전체에서 양쪽 가중치, 뼈 회전, 삼각형 면적비, 정착 이후 복귀를 확인한다.
  - 좌우 더듬이 가중 정점 `1631/1141`, 고정 평행 뼈 혼입 `0`.
  - 좌측 최대 뼈 회전 `43.999980/43.625890/63.999980도`, 우측 `43.999960/43.557820/64.000000도`.
  - 좌우 삼각형 면적비 범위 `0.371848~2.502876` / `0.250191~2.250427`, 붕괴 삼각형 `0`, 과신장 삼각형 `0`.
  - 정착 이후 몸통 복귀 `0m`, 좌우 끝점 복귀 `0.000373/0.000059m`로 회복 모션 없음 조건을 통과했다.
- 최종 저장 상태에서 `ValidateApprovedAccelerandoDeathCollapseMotion`을 두 번 연속 통과했다.
  - 두 검증 모두 1·2주기 몸통 하강 `0.42m`, 높이 배율 `0.58`, 더듬이 하강 Left/Right `0.553333/0.530182m`, 반복 경계 위치·크기 오차 `0`.
  - 최종 2차 1주기 철퇴 하강 Left/Right `0.598003/0.564514m`, 2주기 `0.609753/0.586136m`; 2주기 종료 속도 `0.492204/0.312410m/s`.
  - 사슬 최대 연장 `0.002003m` 이하, 더듬이–첫 사슬 연결 간격 `0m`, 철퇴-몸통 최소거리 `1.476609m` 이상, 직접 사슬·철퇴 커브 `0`, 슬롯 root motion `0`, `ClipLoop=True`, `LoopBlend=False`, `AuthoredReturnMotion=False`를 통과했다.
- 연속 검증 통과 뒤 `CaptureApprovedAccelerandoDeathCollapseMotion`을 한 번만 실행했다. 생존, 힘 풀림, 몸통 하강, 정착, 종료, 2주기 재생 프레임의 정면·사선 이미지를 비교해 메시 갈라짐 없음, 더듬이-사슬 연결 유지, 정착 후 자세 유지와 몸통 비관통을 확인했다.
- 검증·캡처 자료: `docs/validation/accelerando_death_collapse_final_loop_2026-07-15/`.
- 수정·반영 범위:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Death_Collapse.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Death_Collapse.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Accelerando_06_Death` 하위 구조
  - `docs/validation/accelerando_death_collapse_final_loop_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedAccelerandoDeathCollapseMotion`, 전용 `ValidateApprovedAccelerandoDeathCollapseMotion` 조정 및 최종 2회 연속 통과, 연속 통과 뒤 `CaptureApprovedAccelerandoDeathCollapseMotion` 1회.
- 다른 애니메이션 슬롯·씬 루트, 새 모델·텍스처·머티리얼·VFX·사운드, 사슬·철퇴 직접 Transform/속도 지정, artSample, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Accelerando 사망 모션 반복 재생 전환

- `Accelerando_Death_Collapse.anim`의 `Loop Time`과 `Loop Blend`를 활성화했다. `Accelerando_Death_Collapse.controller`의 기본 `DeathCollapse` 상태가 별도 전이 없이 기존 `1.80초` 사망 모션을 반복한다.
- 반복 검증과 캡처는 두 번의 완전한 사망 주기와 세 번째 주기 진입까지 확장했다. 각 주기는 생존 자세에서 힘이 풀리고 몸통이 `0.42m` 내려가며 높이 배율 `0.58`로 주저앉은 뒤, 다음 경계에서 생존 자세로 돌아와 다시 사망 동작을 시작한다.
- 최초 반복 검증에서는 사망 자세에서 생존 자세로 돌아가는 한 프레임의 큰 kinematic 앵커 이동이 ConfigurableJoint에 충격을 주어 철퇴 속도가 1주기 `0.776388m/s`, 2주기 `2.704975m/s`로 누적됐다. 허용 수치만 높이지 않고 사망 모드의 kinematic 연결부 이동을 최대 `3.00m/s`로 제한했다. 동적 사슬·철퇴 Rigidbody의 위치·회전·속도는 초기화하거나 직접 지정하지 않으며, 물리 상태는 반복 경계를 넘어 계속 이어진다.
- 최종 저장 상태에서 `ValidateApprovedAccelerandoDeathCollapseMotion`을 두 번 연속 통과했다.
  - 두 검증 모두 1·2주기 몸통 하강 `0.42m`, 높이 배율 `0.58`, 각 반복 경계 위치·크기 오차 `0`, 좌우 더듬이 하강 `0.695020/0.690202m`.
  - 1차 1주기 철퇴 하강 Left/Right `0.755762/0.748830m`, 2주기 `0.720189/0.710271m`. 2주기 종료 속도는 `1.079941/1.167974m/s`다.
  - 2차 1주기 철퇴 하강 Left/Right `0.755742/0.748828m`, 2주기 `0.720207/0.707438m`. 2주기 종료 속도는 `1.065549/1.146373m/s`다.
  - 사슬 최대 연장은 `0.002002m` 이하, 철퇴-몸통 최소거리는 `1.032751m` 이상, 비주얼-프록시 분리 `0`, 직접 사슬·철퇴 커브 `0`, 슬롯 root motion `0`, `ClipLoop=True`, `LoopBlend=True`, 검증 반복 수 `2`, 물리 상태 경계 초기화 없음 조건을 통과했다.
- 연속 검증 통과 뒤 `CaptureApprovedAccelerandoDeathCollapseMotion`을 한 번만 실행했다. 1주기 시작·하강·최종 자세, 2주기 경계 복귀·하강·최종 자세, 3주기 경계 복귀·힘 풀림 프레임을 정면·사선으로 생성했다. 핵심 프레임을 직접 비교해 반복 재진입, 몸통·더듬이 자세 재현, 사슬 연결 유지, 철퇴 몸통 비관통을 확인했다.
- 검증·캡처 자료: `docs/validation/accelerando_death_collapse_loop_2026-07-15/`.
- 수정·반영 범위:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Death_Collapse.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Death_Collapse.controller`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Accelerando_06_Death` Animator 및 하위 물리 구조
  - `docs/validation/accelerando_death_collapse_loop_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedAccelerandoDeathCollapseMotion`, 반복 전용 `ValidateApprovedAccelerandoDeathCollapseMotion` 조정 및 최종 2회 연속 통과, 연속 통과 뒤 `CaptureApprovedAccelerandoDeathCollapseMotion` 1회.
- 다른 애니메이션 슬롯·씬 루트, 모델링·텍스처·머티리얼·VFX·사운드, 사슬·철퇴 직접 Transform/속도 보정, artSample, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Accelerando 사망 모션 현재 최종 상태

- 현재 최종 구현은 위의 `Accelerando 사망 더듬이 변형·사슬 연결·반복 종료 수정` 항목이다. 기존 `Accelerando 사망 모션 반복 재생 전환`의 `LoopBlend=True`와 이전 물리 수치는 더 이상 현재 상태가 아니다.
- 현재 기준은 `Loop Time=True`, `Loop Blend=False`, 정착 이후 회복 모션 없음, 더듬이 메시 붕괴 삼각형 `0`, 더듬이-사슬 연결 간격 `0m`다.
- 최종 검토 자료는 `docs/validation/accelerando_death_collapse_final_loop_2026-07-15/physics_validation.txt`와 같은 폴더의 `Accelerando_DeathCollapse_*` 캡처다.

## Grave 원본 FBX 정적 배치 및 시작 시점 전환

- 사용자가 지정한 `enemies model/grave.fbx`를 원본 변경 없이 `artSample/enemies/grave/grave.fbx`와 `Assets/_Project/Art/Enemies/Grave/Models/grave.fbx`에 동일 복사했다. 세 파일의 SHA-256은 모두 `D6B44E97909B8B3D40A87E008A9E1916B4656BAC54A6DD55B438E890406750A6`이다.
- 원본 FBX의 실제 캐릭터 메시 `char1`은 약 `0.573 × 0.619 × 1.699m`이며, 기획서의 약 1.6m 높이와 근접해 Unity 인스턴스 스케일을 변경하지 않았다. 원본에 함께 저장된 기본 `Cube`, Camera, Light는 캐릭터 표시 대상에서만 제외했고 FBX 자체는 수정하지 않았다.
- Blender 5.1에서 `artSample/enemies/grave/grave_static_preview.png` 정면 샘플을 생성했다. 원본 `char1`에 머티리얼과 텍스처가 지정되어 있지 않아 임의의 창작 재질은 추가하지 않았으며 이 상태를 한국어 README와 매니페스트에 기록했다.
- `CargoRunMvp.unity`에 단일 루트 `Approved Grave Enemy Placement/Grave_00_Static/Grave_Model`을 생성했다. 롱가 아르마 Z `-37.974`, 테르고 Z `-45.351`의 실제 간격 `7.377m`를 사용해 아첼레란도 `(57.865, 0, -89.613)`보다 Z축 음의 방향인 `(57.865, 0, -96.990)`에 배치했다.
- FBX 리그의 `Head → headfront` 벡터로 정면을 계산했다. Unity 정면 벡터는 `(-0.041, 0, 0.999)`이며 Player 시작 위치를 `(57.687, 0, -92.833)`으로 옮기고 Y축을 그라베 쪽으로 회전시켰다. 정면 일치도와 Player 시선 일치도는 모두 `1.0`이다.
- 최초 적용 말미 검증에서 Unity `SkinnedMeshRenderer.bounds`의 임포트 여유 범위 때문에 높이가 `1.968m`로 과대 측정됐다. 허용 수치를 늘리거나 모델을 축소하지 않고, 베이크된 실제 메시 정점의 월드 바운드를 계산하도록 전용 도구를 수정했다. 이후 실제 메시 크기 `(0.573, 1.699, 0.619)m`, 지면 오차 `0`, 배치 오차 `0`으로 통과했다.
- 최종 저장 상태에서 `ValidateApprovedGravePlacement`을 두 번 연속 통과했다. 두 검증 모두 간격 `7.377m`, 그라베 위치 `(57.865, 0, -96.990)`, 배치·지면 오차 `0`, 정면·시선 일치도 `1.0`, 세 FBX 해시 일치를 확인했다.
- 연속 검증 통과 뒤 `CaptureApprovedGravePlacement`을 한 번만 실행했다. `docs/validation/grave_static_placement_2026-07-15/Grave_PlayerStart_Front.png`를 직접 확인해 Player 시작 시점 정면에서 막대형 몸통, 긴 팔, 짧은 다리가 모두 보이는 것을 확인했다.
- 수정·생성·반영 범위:
  - `artSample/enemies/grave/`
  - `Assets/_Project/Art/Enemies/Grave/Models/grave.fbx`
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Approved Grave Enemy Placement` 및 Player 시작 Transform
  - `docs/validation/grave_static_placement_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 명령: 원본/복사본 SHA-256 확인, Blender FBX 구조 검사 및 정적 샘플 렌더, `Refresh-UnityProject.ps1`, `ApplyApprovedGravePlacement`, 최종 저장 상태 `ValidateApprovedGravePlacement` 2회, 연속 통과 뒤 `CaptureApprovedGravePlacement` 1회.
- 그라베 모델링·UV·스키닝·텍스처·머티리얼 변경, 애니메이션·AI·Collider·Rigidbody·전투 로직, 다른 적 개체 Transform, Player의 시작 Transform 이외 구성요소, Hud·EventSystem·조명·Phase·graybox, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Grave 기준 반대편 시작 위치 전환

- 기존 Player 시작점은 그라베 리그 정면 벡터와 같은 쪽에 있어 `FrontDot=1`이었다. 사용자 요청에 따라 시작 위치 공식을 `그라베 중심 + 정면 벡터 × 4.2m`에서 `그라베 중심 - 정면 벡터 × 4.2m`로 반전했다.
- 그라베와 다른 씬 루트는 재생성하거나 이동하지 않고 Player 시작 Transform만 `(58.030, 0, -101.226)`으로 옮겼다. Player Y축은 반대편에서도 그라베 중심을 바라보도록 설정했다.
- 최종 저장 상태에서 `ValidateApprovedGravePlacement`을 두 번 연속 통과했다. 두 검증 모두 그라베 위치 `(57.865, 0, -96.990)`, 롱가 아르마–테르고 기준 간격 `7.377m`, 배치·지면 오차 `0`, Player 반대편 판정 `FrontDot=-1`, 시선 판정 `FacingDot=1`, 수평거리 `4.199999m`, 거리 오차 `0`을 확인했다.
- 연속 검증 통과 뒤 `CaptureApprovedGravePlacement`을 한 번만 실행했다. `docs/validation/grave_static_placement_2026-07-15/Grave_PlayerStart_OppositeSide.png`를 직접 확인해 Player 시작 시점에서 이전 정면의 반대인 그라베 뒤쪽이 보이는 것을 확인했다.
- 수정·반영 범위:
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 Player 시작 Transform
  - `docs/validation/grave_static_placement_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `MoveApprovedGravePlayerStartToOppositeSide`, 최종 저장 상태 `ValidateApprovedGravePlacement` 2회, 연속 통과 뒤 `CaptureApprovedGravePlacement` 1회.
- 그라베 모델·크기·배치, Main Camera 하위 구성, Player 시작 Transform 이외 구성요소, 애니메이션·AI·Collider·Rigidbody·전투 로직, 다른 적 개체와 씬 루트, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Grave 애니메이션 목록 문서화

- `docs/GAME_DESIGN_SOURCE.txt`의 그라베 본문과 휴대용 스피커 반응, `docs/MVP_IMPLEMENTATION_ORDER.md`의 적대 개체 구현 원칙, `AGENTS.md`의 기능형 애니메이션 규칙을 대조해 `docs/GRAVE_ANIMATION_LIST.md`를 작성했다.
- 정적 비교 상태 `1개`와 실제 애니메이션 `6개`로 정리했다: 대기, 느린 이동, 오른팔 거대화 내려휩쓸기 공격, 피격, 사망, 스피커 전원 끄기.
- 공격 후 `2초` 딜레이와 스피커 주변 `10초` 상주는 애니메이션 길이가 아니라 게임 상태 시간으로 분류했다. 스피커 접근·이탈에는 느린 이동, 상주에는 대기 애니메이션을 재사용한다.
- 원본에 구체 연출이 없는 대기·이동 세부 자세, 공격 단계별 시간과 거대화 배율, 피격 크기, 사망 방식, 스피커 조작 방식은 구현 전 사용자 확인 항목으로 남겼다.
- Unity 에디터 명령, 씬·모델·리그·Animator·AnimationClip, AI·물리·충돌·전투 로직, artSample, 테스트·빌드·Unity 재시작·Git은 실행하거나 변경하지 않았다.

## Grave 애니메이션 슬롯 7개 배치

- 현재 `Approved Grave Enemy Placement`의 정적 그라베를 기준으로 정적 비교 1개와 애니메이션 대상 6개, 총 7개 슬롯을 배치했다.
- 슬롯 이름과 루트 로컬 X 위치는 다음과 같다.
  - `Grave_00_Static_Review`: `0m`
  - `Grave_01_Idle`: `5m`
  - `Grave_02_Walk_Slow`: `10m`
  - `Grave_03_Attack_RightArm_GiantSweep`: `15m`
  - `Grave_04_Hit_Recoil`: `20m`
  - `Grave_05_Death`: `25m`
  - `Grave_06_Speaker_PowerOff`: `30m`
- 각 슬롯은 기존 정적 슬롯의 `Grave_Model` 계층, 리그, 공유 SkinnedMesh, 스케일, 방향, 지면 높이와 비표시 Cube/Camera/Light 상태를 그대로 복제했다. 슬롯당 Transform `27개`, Renderer `1개`다.
- 그라베 배치 루트의 월드 위치 `(57.865, 0, -96.990)`, Player 시작 위치 `(58.030, 0, -101.226)`, 다른 씬 루트는 변경하지 않았다.
- 최종 저장 상태에서 `ValidateApprovedGraveAnimationSlots`을 두 번 연속 통과했다. 두 검증 모두 슬롯 수 `7`, 슬롯 간격 `5m`, 최대 위치 오차 `0`, 슬롯 이름·순서·모델 구조·공유 메시·지면 높이 일치를 확인했다.
- 연속 검증 통과 뒤 `CaptureApprovedGraveAnimationSlotLayout`을 한 번만 실행했다. `docs/validation/grave_animation_slot_layout_2026-07-15/Grave_AnimationSlotLayout.png`를 직접 확인해 7개가 X축 한 줄에 균일하게 나열된 것을 확인했다.
- 수정·반영 범위:
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Approved Grave Enemy Placement` 하위 슬롯
  - `docs/validation/grave_animation_slot_layout_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ArrangeApprovedGraveAnimationSlots`, 최종 저장 상태 `ValidateApprovedGraveAnimationSlots` 2회, 연속 통과 뒤 `CaptureApprovedGraveAnimationSlotLayout` 1회.
- AnimationClip·Animator Controller와 실제 애니메이션, 그라베 모델·리그·메시·크기·재질, Player·카메라, AI·Collider·Rigidbody·전투 로직, 다른 적 개체와 씬 루트, artSample, Ensure/Smoke/Harness/EditMode/PlayMode/Build, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Grave 기준 이미지 재현 아트 샘플

- `image/grave(그라베).png`를 재현 대상으로 삼고 `enemies model/grave.fbx`의 메시·24본 리그를 기반으로 `artSample/enemies/grave/reproduction/`에 독립 검토 가능한 그라베 아트 샘플을 제작했다.
- 기준 이미지의 세로형 둥근 몸통, 긴 팔, 짧은 다리 비율을 맞추고, 턱시도 앞면의 나비넥타이·라펠·조끼·단추·주머니·세로 줄무늬를 전용 `GraveReferenceUV`에 투영했다.
- 단색 임시 재질이 아니라 전면/후면 알베도, 직물 노멀, 직물 거칠기 텍스처를 제작하고 `Grave_Suit_Front_Mat`, `Grave_Textile_BackSide_Mat`에 연결했다. 보이지 않는 후면 의상 선화는 창작하지 않고 기준 이미지의 회색 직물 표면만 연장한 추론으로 README에 명시했다.
- 원본 FBX의 메시 토폴로지와 리그를 유지했다. 최종 FBX를 빈 Blender 세션에 재임포트한 결과 오브젝트는 `Grave_Body`와 `Grave_Rig`만 존재하며, 2,296정점, 4,582폴리곤, 24본, Armature 연결, 두 머티리얼, 두 UV 채널과 네 텍스처 연결을 확인했다.
- 치수 보정 과정에서 깊이가 1차 `0.558323m`, 2차 `0.537921m`로 목표 0.5m를 초과했다. 원본 메시의 바인드 포즈와 회전 변환 때문에 정점 배율과 최종 FBX 바운드가 1:1로 변하지 않는 것으로 판단해 실제 측정 변화율로 보정했으며, 최종 치수는 폭 `0.748327m`, 높이 `1.598500m`, 깊이 `0.525680m`로 검증 범위를 통과했다.
- 정면 시각 검증은 실루엣 IoU `0.799839`, 기준 대비 폭 오차 `2.424%`, 높이 오차 `1.153%`, 중심 오차 `1px`, 전경 명도 중앙값 차이 `3`으로 모두 통과했다.
- 구조·시각 검증 통과 뒤 최종 렌더를 한 번만 실행했다. 최종 검토 파일은 `review/grave_reproduction_front.png`, 기준 이미지 병렬 비교는 `review/grave_reference_comparison.png`다.
- 생성·수정 범위:
  - `artSample/enemies/grave/reproduction/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 명령: Python/Pillow/NumPy 텍스처 생성·이미지 합성·시각 검증, Blender 5.1.2 모델 구성·FBX 내보내기·빈 장면 재임포트 검증·최종 렌더 1회.
- 원본 `image/grave(그라베).png`와 `enemies model/grave.fbx`, Unity 씬·프리팹·런타임 에셋, 현재 배치된 그라베 7개 슬롯, AnimationClip·Animator·AI·Collider·Rigidbody·전투 로직, 다른 적과 Player·카메라·UI, Harness/EditMode/PlayMode/Build, Unity 열기·리프레시·재시작, Git은 실행하거나 변경하지 않았다.

## Grave Cantabile 형식 아트 샘플 검토 패키지 보완

- 사용자 지시에 따라 `artSample/enemies/cantabile/index.html`과 연결된 승인 상태, 매니페스트, 텍스처 분석, 리그 메모, 정적 렌더 구성을 형식 기준으로 삼아 그라베 샘플 루트를 보완했다.
- `artSample/enemies/grave/index.html`을 검토 진입점으로 추가하고 다음 항목을 한 페이지에서 확인할 수 있게 구성했다: 정면 기준 이미지/재현 렌더 비교, 측면·사선·근접 시점, 텍스처·머티리얼 분석판, 치수·리그·재질 기준, 정량 검증 결과, 추론 범위, 승인 후 Unity 적용 의도, 원본/내보내기/보고서 링크.
- Cantabile과 같은 루트 구성으로 `APPROVAL_STATUS.json`, `ASSET_MANIFEST.json`, `TEXTURE_ANALYSIS.md`, `PHYSICS_RIG_NOTES.md`, `exports/`, `renders/`, `textures/`, `tools/`, `review/`를 갖췄다. 승인 상태는 `pending_user_review`, Unity 적용 상태는 `false`다.
- 검토용 `grave_reference_reproduction.glb`를 추가했다. Blender 재임포트 시 보이던 42정점 `Icosphere`는 GLB에 포함된 캐릭터 메시가 아니라 Blender glTF 임포터가 24본 표시를 위해 생성한 보조 형상임을 확인했다. GLB 바이너리 내부 직접 검사 결과 실제 메시 정의 `1`, 메시 노드 `1`, 스킨 `1`, 머티리얼 `2`, 임베디드 이미지 `4`, 공유 참조 텍스처 슬롯 `6`으로 통과했다.
- 정적 검토 렌더는 정면, 준측면, 기준 병렬 비교, 사선, 턱시도 근접, 텍스처·머티리얼 분석의 6개로 구성했다. 기준 자료가 정면 한 장뿐이므로 측면·후면의 양복 선화는 창작하지 않고 회색 직물 표면 연장만 추론임을 HTML과 문서에 표시했다.
- HTML 로컬 참조 `18개`는 누락 `0`, 승인 상태·매니페스트 JSON 파싱은 통과했다. 최초 브라우저 검토 연결은 실행 환경의 보안 상태 전달 문제로 시작되지 않아, 별도 승인 후 Microsoft Edge 헤드리스 모드로 대체 검증했다.
- Edge DOM 덤프는 빈 결과를 반환해 판정에서 제외했다. 최종 브라우저 캡처 `review/grave_art_sample_page.png`를 한 번 생성하고 직접 확인해 제목, 승인 상태, 정면 비교, 6개 렌더, 4개 텍스처, 검증 수치, Unity 반영 범위와 링크가 모두 표시되며 깨진 이미지가 없음을 확인했다.
- 생성·수정 범위:
  - `artSample/enemies/grave/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 명령: PowerShell/Python 구조·링크·JSON 검사, Blender 5.1.2 GLB 내보내기·정적 시점 렌더·재임포트 조사, GLB 바이너리 내부 구조 검증, Microsoft Edge 로컬 페이지 렌더 및 최종 캡처 1회.
- Unity 에디터·씬·프리팹·런타임 에셋, 현재 그라베 7개 슬롯, 애니메이션·AI·Collider·Rigidbody·전투 로직, 다른 적과 Player·카메라·UI, Harness/EditMode/PlayMode/Build, Unity 열기·리프레시·재시작, Git은 실행하거나 변경하지 않았다.

## 승인된 Grave 아트 샘플 Unity 적용

- 사용자가 승인한 `artSample/enemies/grave/reproduction/grave_reproduction.fbx`와 네 텍스처를 Unity의 `Assets/_Project/Art/Enemies/Grave/` 아래 모델·텍스처·머티리얼 자산으로 반영했다. 원본과 Unity 복사본의 SHA-256이 각각 일치하며 승인 FBX 해시는 `8A43446A89A45A91082724CCA9026F87DC9B13F755CA93E16C2705460B312B70`이다.
- `CargoRunMvp.unity`의 `Approved Grave Enemy Placement` 아래 7개 슬롯에서 각 `Grave_Model`만 승인 FBX로 교체했다. 슬롯 루트 이름·간격·Transform, Grave 배치 루트, Player 반대편 시작 위치, 다른 씬 루트는 유지했다.
- URP Lit 기반 `Grave_Suit_Front_Mat`과 `Grave_Textile_BackSide_Mat`에 승인 전면/직물 알베도와 직물 노멀을 연결했다. 금속성 `0`, Smoothness `0.22`, Normal Strength `0.16`이며 거칠기 텍스처는 선형 데이터로 임포트했다.
- 최초 Unity 시각 검토에서 턱시도 무늬가 세로 띠로 압축되어 보였다. 승인 Blender 재질은 두 번째 UV인 `GraveReferenceUV`를 명시적으로 사용하지만 URP Lit는 첫 UV를 읽는 것이 원인이었다. 승인 FBX·메시·텍스처를 변경하지 않고 ModelImporter의 `Swap UV Channels`를 활성화해 승인 투영 UV를 Unity 기본 UV로 사용했다.
- 최종 전용 검증 수치는 슬롯 수 `7`, 최대 위치 오차 `0`, 최대 지면 오차 `0`, 각 바운드 `(0.748, 1.599, 0.526)m`, 모델당 Transform `27`, visible renderer `1`, 메시 `Grave_Body`, 정점 `3187`, 서브메시 `2`, `SwapUVChannels=True`다. Player 위치 `(58.030, 0, -101.226)`, 반대편 판정 `FrontDot=-0.999998`, 시선 판정 `FacingDot=0.999998`도 유지됐다.
- 최종 Unity 정면·사선·7개 배치 캡처와 승인 샘플 병렬 비교에서 둥근 세로 몸통, 긴 팔·짧은 다리 비율, 나비넥타이·라펠·조끼·단추·주머니 선화, 회색 직물 질감이 모두 확인되어 시각 검증을 통과했다. 검토 자료는 `docs/validation/grave_approved_reproduction_2026-07-15/`에 저장했다.
- 검증 사다리 결과:
  - `Run-HarnessValidation.ps1`: 통과.
  - `Run-EditModeTests.ps1`: 통과.
  - `Run-PlayModeTests.ps1`: 총 10개 중 6개 통과, 4개 실패. 3개는 이번 범위 밖에서 기존 `Hud`가 비활성화되어 `FirstPersonHud`를 찾지 못한 실패이고, 1개는 기존 추가 광원 24개의 그림자 아틀라스 경고를 예상하지 않은 로그로 처리한 실패다. 해당 테스트의 예전 Player 시작점 기대값 `(0,0,20.6)`도 사용자 지시로 옮긴 현재 위치와 일치하지 않는다. Hud·조명·Player·테스트는 수정하지 않았다.
  - `Build-WindowsDev.ps1`: 통과.
- 수정·반영 범위:
  - `Assets/_Project/Art/Enemies/Grave/Models/Grave_Approved_Reproduction.fbx` 및 임포트 설정
  - `Assets/_Project/Art/Enemies/Grave/Textures/`의 승인 텍스처 4개
  - `Assets/_Project/Art/Enemies/Grave/Materials/`의 승인 머티리얼 2개
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Approved Grave Enemy Placement` 아래 7개 `Grave_Model`
  - `docs/validation/grave_approved_reproduction_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedGraveReproduction`, `ValidateApprovedGraveReproduction`, `CaptureApprovedGraveReproduction`, Harness/EditMode/PlayMode/Build 검증 사다리. Unity 재시작과 Git은 실행하지 않았다.
- Grave 애니메이션·Animator·AI·Collider·Rigidbody·전투 로직, Player·Main Camera·Hud·EventSystem·조명·Phase·graybox, 다른 적 개체와 다른 씬 루트는 수정하지 않았다.

## Grave 대기 호흡 모션

- `Approved Grave Enemy Placement/Grave_01_Idle/Grave_Model`에만 `Grave_Idle_Breathing` 대기 애니메이션과 전용 Animator Controller를 적용했다. 클립 길이는 `3초`, `Loop Time=True`, `Loop Blend=True`이며 기본 상태 `IdleBreathing`이 전이 없이 반복된다.
- 승인 FBX에는 BlendShape가 없으므로 기존 24본 리그를 사용했다. 몸통 체인 `Spine02 → Spine01 → Spine`의 로컬 단면 스케일을 합산 최대 `1.5074%`, 세로 스케일을 합산 최대 `0.6012%`로 완만하게 팽창·수축시켰다.
- 몸통 상하 움직임은 모델·슬롯 루트나 `Hips`를 움직이지 않고 `Spine02` 로컬 Y에만 최대 `0.015m` 상승 곡선을 적용했다. 다리와 발은 기준 자세를 유지하므로 지면 관통은 `0m`다.
- 클립은 `Spine02` 위치 1개와 `Spine02/Spine01/Spine` 스케일 9개, 총 10개 Transform 곡선만 포함한다. 모델 root motion과 슬롯 root motion은 모두 `0`이며 다른 6개 Grave 슬롯에는 Animator를 추가하지 않았다.
- 최초 적용에서는 Unity 모델 프리팹이 네이티브 객체가 없는 Animator 참조를 반환했고 C# `??`가 이를 실제 null로 처리하지 않아 연결이 중단됐다. Unity 객체의 명시적 `animator == null` 판정 후 Animator를 추가하는 방식으로 수정했다.
- 최종 저장 상태에서 `ValidateApprovedGraveIdleBreathing`을 두 번 연속 통과했다. 두 결과 모두 몸통 상승 `0.015m`, 단면 팽창 `0.015074`, 세로 팽창 `0.006012`, 바운드 폭 변화 `0.011276m`, 높이 변화 `0.018325m`, 지면 관통 `0`, 반복 경계 자세 오차 `0`, Grave Animator 수 `1`로 동일했다.
- 연속 검증 통과 뒤 `CaptureApprovedGraveIdleBreathing`을 한 번만 실행했다. `0/0.75/1.5/2.25/3초` 프레임을 직접 비교해 수축·상승·최대 팽창·하강·반복 경계에서 몸통과 사지가 연결된 채 작게 호흡하고 시작·종료 자세가 일치하는 것을 확인했다.
- 생성·수정 범위:
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Idle_Breathing.anim`
  - `Assets/_Project/Art/Enemies/Grave/Controllers/Grave_Idle_Breathing.controller`
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Grave_01_Idle/Grave_Model` Animator
  - `docs/validation/grave_idle_breathing_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `InspectApprovedGraveIdleRig`, `ApplyApprovedGraveIdleBreathing`, 최종 저장 상태 `ValidateApprovedGraveIdleBreathing` 2회, 연속 통과 뒤 `CaptureApprovedGraveIdleBreathing` 1회.
- Harness/EditMode/PlayMode/Windows Build 관련 작업, Grave 대기 슬롯 외 애니메이션·Animator, FBX 메시·UV·스키닝·리그 구조, 모델·텍스처·머티리얼·VFX, AI·Collider·Rigidbody·전투 로직, Player·Main Camera·Hud·EventSystem·조명·다른 씬 루트, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Grave 평범한 이동 모션

- 사용자 요청에 따라 기존의 느리고 정중한 보행을 평범한 걷기 모션으로 교체했다. `Approved Grave Enemy Placement/Grave_02_Walk_Slow/Grave_Model`에만 기존 `Grave_Walk_Slow` 이동 애니메이션과 전용 Animator Controller 경로를 유지해 적용했다. 클립 길이는 `2초`, 좌우 발 디딤 간격은 `1초`, `Loop Time=True`, `Loop Blend=True`이며 기본 상태 `SlowWalk`가 전이 없이 반복된다.
- 모델·슬롯 루트를 직접 이동하지 않는 제자리 검토 모션으로 구성했다. 실제 런타임 전진은 이번 범위에 포함하지 않았으며, 이후 이동 로직에서 Rigidbody 기준으로 분리한다.
- 기존 24본 리그 가운데 실제 보행 변형에 필요한 20개 본을 사용했다. `Hips`, `Spine02/Spine01/Spine`, `neck/Head`, 좌우 `Shoulder/Arm/ForeArm`, 좌우 `UpLeg/Leg/Foot/ToeBase`를 접지·체중 하강·통과·발끝 밀기의 8단계 보행으로 연결했다. 메시, 리그 루트, 머리 보조 끝점은 애니메이션 대상에서 제외했다.
- 좌우 허벅지 교차 보폭은 약 `28도`, 몸통 상승은 최대 `0.016m`, 좌우 골반 체중 이동은 `0.014m`, 오른발 통과 자세 상승은 `0.04731m`, 왼발 통과 자세 상승은 `0.069215m`, 양쪽 발가락 롤은 약 `16도`다. 골반과 척추의 반대 회전, 목·머리의 흔들림 보정, 어깨·상완·전완의 반대팔 스윙을 발걸음에 동기화했다.
- Blender 본별 로컬 축 롤과 `Grave_Rig`의 X축 회전을 고려해 본 로컬 X를 직접 가정하지 않았다. 모델 기준 오른쪽 축을 각 본 부모 좌표로 변환해 보행 회전을 만들고, 모델 기준 위쪽을 Hips 부모 좌표의 XYZ 위치 커브로 변환했다.
- 클립에는 Hips 위치 3개와 20개 본의 Quaternion 회전 80개, 총 `83개` 커브가 있다. 모델 root motion과 슬롯 root motion은 모두 `0`이며 대기·이동 슬롯 외 그라베 개체에는 Animator를 추가하지 않았다.
- 초기 리그 보행에서 발끝 밀기 변형 때문에 접지 시 최대 `0.007597m`의 지면 관통이 확인됐다. 전체 상대 보행 높이는 유지하면서 Hips 리그에 `0.0033m` 접지 여유를 일관되게 적용해 최종 지면 관통을 `0`으로 만들었다.
- 최종 저장 상태 전용 검증을 반복 통과했다. 결과는 지면 관통 `0`, 반복 경계 자세 오차 `0`, 좌우 보폭 약 `28도`, 오른발 상승 `0.04731m`, 왼발 상승 `0.069215m`, 양쪽 발가락 롤 약 `16도`, Grave Animator 수 `2`다.
- 검증 통과 뒤 성공한 최종 `CaptureApprovedGraveSlowWalk`을 한 번 생성했다. `0/0.25/0.5/0.75/1/1.25/1.5/1.75/2초`의 9개 프레임에서 접지, 체중 하강, 스윙발 통과, 뒤꿈치 상승과 발끝 밀기, 반대팔·상체 회전, 시작·종료 자세 일치, 메시 연결 상태를 확인했다.
- 이동 캡처를 9프레임으로 확장하는 과정에서 앞쪽 대기 캡처 배열이 잘못 변경된 부분은 즉시 기존 5프레임·2400×720 설정으로 복원했다. 대기 애니메이션 자산과 씬 상태는 변경하지 않았다.
- 생성·수정 범위:
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Walk_Slow.anim`
  - `Assets/_Project/Art/Enemies/Grave/Controllers/Grave_Walk_Slow.controller`
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Grave_02_Walk_Slow/Grave_Model` Animator
  - `docs/validation/grave_walk_slow_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `ApplyApprovedGraveSlowWalk`, 최종 저장 상태 `ValidateApprovedGraveSlowWalk` 2회, 연속 통과 뒤 `CaptureApprovedGraveSlowWalk` 1회.
- 승인 범위 밖 검증, 실제 전진 이동, AI·Collider·Rigidbody·전투 로직, FBX 메시·UV·스키닝·리그 구조, 모델·텍스처·머티리얼·VFX, Player·카메라·UI·조명·다른 씬 루트, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Grave 걷기 FBX 개체 교체

- 사용자 지정 원본 `enemies model/grave walk.fbx`를 Unity 원본 보관 자산 `Assets/_Project/Art/Enemies/Grave/Models/Grave_Walk_Imported.fbx`로 동일 복사했다. 두 파일의 SHA-256은 `723916CA0946C65156EF26A1680FC4E6833CC72C73869484E7C5F3CF98B77226`으로 일치한다.
- 원본 걷기 FBX에는 24본 리그와 `walking_man` 동작이 있지만 머티리얼 슬롯이 없고 기준 재현용 두 번째 UV도 없었다. 승인된 다른 그라베와 외형을 같게 유지하기 위해 `artSample/enemies/grave/reproduction/grave_reproduction.fbx`의 `Grave_Body`, 두 머티리얼 슬롯, `GraveReferenceUV`를 유지하고, 원본 FBX와 동일한 이름·바인드 자세의 24본 리그 및 걷기 동작을 결합했다.
- 결합 결과를 `Assets/_Project/Art/Enemies/Grave/Models/Grave_Walk_Approved_Reproduction.fbx`로 내보냈다. 빈 Blender 장면 재임포트에서 `Grave_Body`와 `Grave_Rig`만 존재하며, 2,296정점, 4,582폴리곤, 24본, Armature 연결, 두 머티리얼 슬롯, `uv`와 `GraveReferenceUV` 두 UV 채널 및 걷기 Action을 확인했다.
- `CargoRunMvp.unity`에서 기존 `Approved Grave Enemy Placement/Grave_02_Walk_Slow` 개체 전체를 삭제하고 같은 이름·형제 순서·Transform·활성 상태로 다시 생성했다. 새 하위 `Grave_Model`은 결합 FBX를 사용하며, 승인된 `Grave_Suit_Front_Mat`과 `Grave_Textile_BackSide_Mat`을 적용했다. 다른 6개 그라베 슬롯과 다른 씬 루트는 유지했다.
- 임베디드 클립을 `Grave_Walk_Imported`로 설정하고 `Grave_Walk_Imported.controller`의 기본 `ImportedWalk` 상태에서 반복 재생하도록 연결했다. 클립 길이는 `2.067초`, 커브 수는 `250`, `Loop Time=True`, `Loop Blend=True`이며 모델과 슬롯의 root motion은 모두 `0`이다.
- 저장된 씬에서 전용 검증을 두 번 연속 통과했다. 두 결과 모두 좌우 발 이동량 `0.299044m`/`0.292645m`, 기준 자세 바운드 오차 `0`, 지면 관통 `0.003458m`, 반복 경계 자세 오차 `0`, 결합 메시 정점 `3,187`, 서브메시 `2`로 동일했다.
- 연속 검증 통과 뒤 최종 비교 캡처를 한 번만 생성했다. `docs/validation/grave_walk_fbx_replacement_2026-07-15/Grave_Walk_Fbx_Replacement_Comparison.png`의 `0/25/50/75/100%` 프레임에서 흰색 섬유 표면, 턱시도 선화, 긴 팔·짧은 다리 외형이 다른 승인 그라베와 동일하게 유지되고 걷기 자세가 반복되는 것을 확인했다.
- 생성·수정 범위:
  - `Assets/_Project/Art/Enemies/Grave/Models/Grave_Walk_Imported.fbx`
  - `Assets/_Project/Art/Enemies/Grave/Models/Grave_Walk_Approved_Reproduction.fbx`
  - `Assets/_Project/Art/Enemies/Grave/Controllers/Grave_Walk_Imported.controller`
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Grave_02_Walk_Slow`
  - `docs/validation/grave_walk_fbx_replacement_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 명령: Blender 5.1.2 원본·승인 FBX 구조 비교, 결합 FBX 생성·재임포트 확인, `Refresh-UnityProject.ps1`, `ApplyGraveWalkFbxReplacement`, 최종 저장 상태 `ValidateGraveWalkFbxReplacement` 2회, 연속 통과 뒤 `CaptureGraveWalkFbxReplacement` 1회.
- 승인 범위 밖 검증, 실제 전진 이동, AI·Collider·Rigidbody·전투 로직, 다른 애니메이션 슬롯, Player·카메라·UI·조명·다른 씬 루트, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Grave 커튼콜 인사형 공격 동작

- `Approved Grave Enemy Placement/Grave_03_Attack_RightArm_GiantSweep/Grave_Model`에만 `Grave_Attack_CurtainCall_Sweep` 공격 클립과 전용 Animator Controller를 적용했다. 사용자 지시에 따라 이번 단계에서는 오른팔 거대화, 팔 스케일 변경, 반달 칼 형태를 구현하지 않고 기존 24본 리그의 동작만 사용했다.
- 총 길이는 원본 기획에 맞춘 `3초`다. `0/0.35/0.85/1.2/1.65/2/2.35/2.65/3초`의 9개 시점에서 기본 자세, 오른팔 옆 전개, 전개 유지, 전방 아래쪽 쓸기, 몸통으로 당기기, 기본 자세 복귀를 한 동작으로 연결했다. 공격 슬롯 검토를 위해 `Loop Time=True`, `Loop Blend=True`로 반복 재생한다.
- `Hips`, `Spine02`, `Spine01`, `Spine`, `neck`, `Head`, 좌우 어깨·팔과 오른쪽 전완·손까지 총 12개 본을 사용한다. 클립은 Quaternion 회전 곡선 `48개`만 포함하며 위치, 스케일, 모델·슬롯 루트 곡선은 없다.
- 초기 Euler 각도 기반 상완 조정에서는 첫 검사에서 손이 쓸기 구간에 `0.473131m` 상승했고, 두 번째 검사에서는 하강 `0.177462m`를 확보했지만 전방 이동이 `0.007397m`에 그쳤다. 세 번째 수치 조정도 본 롤과 결합되어 하강과 전방 이동을 함께 충족하지 못해 단순 각도 추측을 중단했다.
- 최종 구현은 상완과 전완의 실제 자식 본 방향을 기준으로 각 시점의 목표 방향을 계산한다. 옆 전개에서는 두 팔뼈를 모델 오른쪽으로, 타격에서는 전방·아래쪽으로, 당기기에서는 팔꿈치에서 몸통 방향으로 조준한 뒤 각 부모 좌표의 Quaternion 커브로 변환한다.
- 최종 저장 상태에서 전용 검증을 두 번 연속 통과했다. 두 결과 모두 옆 전개 `0.666412m`, 전개 상승 `0.715041m`, 쓸기 하강 `0.324456m`, 정면 진행 `0.989373m`, 몸통 쪽 거리 감소 `0.610836m`, 오른팔 최대 회전 `122.5542도`, 몸통 반응 `25.14607도`다. 팔 스케일 오차, 반복 경계 자세 오차, 모델·슬롯 루트 이동은 모두 `0`이며 지면 관통은 기존 기준 범위인 `0.00336m`다.
- 연속 검증 통과 뒤 `docs/validation/grave_curtain_call_attack_2026-07-15/Grave_CurtainCall_Attack_Review.png`를 한 번만 생성했다. 9개 시점에서 오른팔과 몸통 메시가 연결된 채 옆 전개, 전방 아래쪽 쓸기, 몸통 회수 순서가 보이고 팔 길이와 굵기가 유지되는 것을 확인했다.
- 생성·수정 범위:
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Art/Enemies/Grave/Controllers/Grave_Attack_CurtainCall_Sweep.controller`
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Grave_03_Attack_RightArm_GiantSweep/Grave_Model` Animator
  - `docs/validation/grave_curtain_call_attack_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 Unity 명령: `Refresh-UnityProject.ps1`, `InspectApprovedGraveAttackRig`, `ApplyApprovedGraveCurtainCallAttack`, 최종 저장 상태 `ValidateApprovedGraveCurtainCallAttack` 2회, 연속 통과 뒤 `CaptureApprovedGraveCurtainCallAttack` 1회.
- 승인 범위 밖 검증, 오른팔 거대화·스케일·모델 형태 변경, 실제 전진 이동, AI·Collider·전투 판정, 다른 애니메이션 슬롯, Player·카메라·UI·조명·다른 씬 루트, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Grave 공격의 전방 찌르기 제거 및 수직 쓸기 수정

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-15 22-59-17.mp4`를 `4fps` 연속 프레임으로 분석했다. 분석 자료는 `docs/validation/grave_curtain_call_attack_2026-07-15/source_video_analysis/`에 저장했다.
- 영상에서 오른팔 옆 전개까지는 맞았지만 이후 팔이 수평에 가까운 상태로 카메라 깊이 방향으로 짧아지고 몸통이 크게 앞으로 숙여져, 아래 쓸기보다 전방 찌르기로 보이는 것을 확인했다. 원인은 타격 시 상완·전완 목표 방향의 정면 성분이 각각 `0.86`, `0.91`로 설정되고 몸통 본의 누적 숙임이 약 50도였기 때문이다.
- 공격 궤적을 `옆 전개 → 옆 아래 → 거의 수직 아래 → 몸통 앞` 순서로 수정했다. 타격 시 상완·전완은 아래 방향을 중심으로 조준하고, 부모 본 숙임으로 발생하는 어깨 전진은 미세한 후방 성분으로 상쇄했다. 마지막에는 상완을 아래로 둔 채 전완을 안쪽·앞쪽으로 접어 손이 몸통 앞에 위치한다.
- 골반·척추 본의 타격 구간 누적 숙임을 약 15~20도 범위로 줄였다. 오른팔 거대화, 팔 스케일, 메시, 머티리얼과 다른 애니메이션 슬롯은 변경하지 않았다.
- 첫 수정에서는 하강 `0.666307m`를 확보했지만 몸통 숙임 때문에 전방 변화가 `0.579888m` 남았다. 두 번째 수정에서는 전방 변화를 `0.293076m`, 하강 우세 비율을 `2.243693`까지 개선했으나 전방 허용 기준 `0.28m`를 `0.013076m` 초과했다. 최종 수정은 팔의 미세 후방 보정으로 부모 본 전진을 상쇄했다.
- 최종 저장 상태에서 `ValidateApprovedGraveCurtainCallAttack`을 두 번 연속 통과했다. 두 결과 모두 옆 전개 `0.666412m`, 전개 상승 `0.715041m`, 중간 하강 `0.466215m`, 전체 하강 `0.657076m`, 안쪽 이동 `0.522812m`, 전방 변화 `0.173431m`, 하강 우세 비율 `3.788684`다.
- 최종 손은 몸통 기준 좌우 거리 `0.19046m`, 정면 거리 `0.429601m`, 세로 위치 `-0.226591m`에 놓인다. 오른팔 최대 회전은 `90.8901도`, 몸통 본 반응은 `13.2185도`, 팔 스케일 오차·반복 경계 자세 오차·모델 및 슬롯 루트 이동은 모두 `0`이다.
- 연속 검증 통과 뒤 `Grave_CurtainCall_Attack_Review.png`를 한 번 갱신했다. `final_frame_inspection/`의 전개·하강·최저점·몸통 앞 세부 프레임에서 팔과 몸통 메시 연결, 팔 길이와 굵기 유지, 최종 몸통 앞 위치를 확인했다.
- 수정·생성 범위:
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Grave_03_Attack_RightArm_GiantSweep/Grave_Model` Animator
  - `docs/validation/grave_curtain_call_attack_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 명령: FFprobe 영상 정보 확인, FFmpeg 분석 프레임 추출, `Refresh-UnityProject.ps1`, `ApplyApprovedGraveCurtainCallAttack`, 최종 저장 상태 `ValidateApprovedGraveCurtainCallAttack` 2회, 연속 통과 뒤 `CaptureApprovedGraveCurtainCallAttack` 1회.
- 승인 범위 밖 검증, 오른팔 거대화·스케일·모델·텍스처·머티리얼 변경, AI·충돌·피해 판정·실제 이동, 다른 애니메이션 슬롯과 씬 루트, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Grave 커튼콜 공격의 몸통 중앙 마무리 수정

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-15 23-14-45.mp4`의 `7.362966초` 구간을 `4fps` 연속 프레임으로 분석했다. 분석 자료는 `docs/validation/grave_curtain_call_attack_2026-07-15/source_video_analysis_231445/`에 저장했다.
- 영상에서 오른팔 옆 전개와 수직 하강은 확인됐지만 마무리 상완 목표의 좌우 성분이 바깥쪽 `+0.10`이라 팔꿈치가 몸통 오른쪽에 남고, 전완만 접혀 손도 몸통 중앙에 도달하지 못하는 것을 확인했다. 수정 전 손의 몸통 중심 이격은 `0.19046m`였다.
- 옆 전개와 수직 쓸기 목표 방향은 유지하고, 마무리 상완 방향의 좌우 성분만 몸통 안쪽 `-0.45`로 변경했다. 상완이 팔꿈치를 중앙으로 옮긴 뒤 전완이 몸통 앞에서 접히므로 오른팔 전체가 중앙선에 모이도록 구성했다.
- 전용 검증에 마무리 팔꿈치 위치를 추가했다. 팔꿈치와 손의 몸통 중심 좌우 이격, 각각의 정면 거리, 팔꿈치-손 구간의 좌우 차이를 별도로 판정한다.
- 최초 리프레시에서는 새 팔꿈치 기록 코드의 지역 참조 한 줄이 빠져 컴파일이 중단됐으며 씬과 애니메이션 자산은 갱신되지 않았다. `animatedBones[8]`을 `RightForeArm`으로 연결한 뒤, 승인된 현재 에디터 창 새로고침으로 최신 소스를 컴파일하고 적용을 재개했다.
- 최종 저장 상태에서 `ValidateApprovedGraveCurtainCallAttack`을 두 번 연속 통과했다. 팔꿈치의 몸통 중심 이격은 `0.046371m`, 팔꿈치 정면 거리는 `0.12583m`, 손의 중심 이격은 `0.014404m`, 손 정면 거리는 `0.4454m`, 손 세로 위치는 몸통 기준 `-0.185558m`, 팔꿈치-손 좌우 차이는 `0.060776m`다.
- 기존 옆 전개 `0.666412m`, 중간 하강 `0.466215m`, 전체 하강 `0.657076m`, 안쪽 이동 `0.522812m`, 전방 변화 `0.173431m`, 하강 우세 비율 `3.788684`는 유지했다. 팔 스케일 오차, 반복 경계 자세 오차, 모델·슬롯 루트 이동은 모두 `0`이다.
- 연속 검증 통과 뒤 `Grave_CurtainCall_Attack_Review.png`를 한 번 갱신해 팔·몸통 메시 연결과 팔 길이·굵기 유지를 확인했다.
- 수정·생성 범위:
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Grave_03_Attack_RightArm_GiantSweep/Grave_Model` Animator
  - `docs/validation/grave_curtain_call_attack_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 명령: FFprobe 영상 정보 확인, FFmpeg 연속 분석 프레임 추출, 현재 Unity 에디터 창 `Ctrl+R` 새로고침, `Refresh-UnityProject.ps1`, `ApplyApprovedGraveCurtainCallAttack`, 최종 저장 상태 `ValidateApprovedGraveCurtainCallAttack` 2회, 연속 통과 뒤 `CaptureApprovedGraveCurtainCallAttack` 1회.
- 승인 범위 밖 검증, 오른팔 거대화·스케일·모델·텍스처·머티리얼 변경, AI·충돌·피해 판정·실제 이동, 다른 애니메이션 슬롯과 씬 루트, Unity 재시작, Git은 실행하거나 변경하지 않았다.

## Grave 커튼콜 공격의 왼쪽 방향 마무리 수정

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-15 23-29-46.mp4`의 `7.333133초` 구간을 `4fps` 연속 프레임으로 분석했다. 분석 시트는 `docs/validation/grave_curtain_call_attack_2026-07-15/source_video_analysis_232946/Grave_Attack_LeftFacingFinish_SourceVideo.png`에 저장했다.
- 영상에서 팔꿈치는 몸통 중앙으로 모였지만 마무리 전완 목표가 모델 기준 정면 성분 `0.94`, 왼쪽 성분 `-0.18`로 설정되어 손과 팔이 카메라 쪽으로 돌출되는 것을 확인했다.
- 옆 전개, 아래 쓸기, 팔꿈치를 몸통 중앙으로 모으는 상완 목표는 유지하고 마무리 전완 목표만 `(-0.82, 0.18, 0.54)`로 변경했다. 팔꿈치에서 손으로 이어지는 방향이 정면보다 왼쪽으로 우세해져 오른팔과 오른손이 몸통을 가로질러 왼쪽을 향한 채 마무리된다.
- 전용 판정을 절댓값 중심 이격 판정에서 방향성 판정으로 변경했다. 최종 손의 signed 좌우 위치, 팔꿈치 대비 왼쪽 도달 거리, 전완 정면 거리, 왼쪽/정면 우세 비율을 각각 확인한다.
- 최종 저장 상태에서 전용 판정을 두 번 연속 통과했다. 두 결과 모두 최종 손의 몸통 기준 좌우 위치 `-0.231796m`, 팔꿈치 대비 왼쪽 도달 거리 `0.278168m`, 전완 정면 거리 `0.194389m`, 왼쪽 우세 비율 `1.430983`, 팔꿈치 중심 이격 `0.046371m`, 손 정면 거리 `0.320219m`다.
- 기존 옆 전개 `0.666412m`, 중간 하강 `0.466215m`, 전체 하강 `0.657076m`, 안쪽 이동 `0.522812m`, 전방 변화 `0.173431m`, 하강 우세 비율 `3.788684`는 유지했다. 팔 스케일 오차, 반복 경계 자세 오차, 모델·슬롯 루트 이동은 모두 `0`이다.
- 연속 판정 통과 뒤 승인된 최종 캡처를 한 번 실행했다. 다만 해당 캡처 루틴은 본 Transform 샘플 수치와 달리 렌더 직전 `SkinnedMeshRenderer` 스킨 행렬이 갱신되지 않아 9개 프레임이 기본 스킨 자세로 기록됐다. 따라서 `Grave_CurtainCall_Attack_Review.png`는 이번 왼쪽 마무리의 시각 근거로 사용하지 않았고, 승인된 1회 제한 때문에 캡처를 추가 실행하지 않았다. 확대 분석 파일은 `Grave_CurtainCall_Attack_LeftFacingFinish_Detail.png`다.
- 수정·생성 범위:
  - `Assets/_Project/Art/Enemies/Grave/Animations/Grave_Attack_CurtainCall_Sweep.anim`
  - `Assets/_Project/Editor/Validation/GraveCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 `Grave_03_Attack_RightArm_GiantSweep/Grave_Model` Animator
  - `docs/validation/grave_curtain_call_attack_2026-07-15/`
  - `docs/PROGRESS_2026-07-15.md`
- 실행한 명령: FFprobe 영상 정보 확인, FFmpeg 분석 프레임 추출 및 기존 최종 캡처 확대, `Refresh-UnityProject.ps1`, `ApplyApprovedGraveCurtainCallAttack`, 최종 저장 상태 `ValidateApprovedGraveCurtainCallAttack` 2회, 연속 통과 뒤 `CaptureApprovedGraveCurtainCallAttack` 1회.
- 승인 범위 밖 검증, 오른팔 거대화·스케일·모델·텍스처·머티리얼 변경, AI·충돌·피해 판정·실제 이동, 다른 애니메이션 슬롯과 씬 루트, Unity 재시작은 실행하거나 변경하지 않았다. 작업 도중 승인 범위에 없던 읽기 전용 `git diff`를 한 번 잘못 실행했으나 출력과 저장소 상태 변경은 없었고, 이후 Git 명령은 실행하지 않았다.

## 2026-07-15 작업분 최종 기록 및 GitHub 반영 준비

- 오늘 작업한 Accelerando의 더듬이·사슬·철퇴 물리 후행, 정면 세로 원형 공격과 주기 단축 가속, 피격 반동, 사망 주저앉기·더듬이 이완 모션을 위 각 절에 구현 상태와 수치, 전용 확인 결과까지 기록했다.
- Grave는 원본 FBX 배치와 시작 위치 전환, 애니메이션 목록 문서화, 상태별 개체 나열, 승인된 외형 재현 적용, 대기 호흡, 걷기 FBX 교체, 커튼콜 공격의 옆 전개·수직 쓸기·몸통 중앙 회수·왼쪽 방향 마무리까지 위 각 절에 기록했다.
- 저장소의 현재 변경 범위에는 위 작업에 사용된 Unity 애니메이션·컨트롤러·모델·머티리얼·텍스처·런타임 물리 코드·에디터 적용 코드·씬·프로젝트 설정·작업 문서·전용 확인 자료와, 같은 작업 묶음에 남아 있던 적대 개체 관련 변경 파일이 포함된다.
- GitHub 반영 대상은 `artSample/` 전체를 제외한 나머지 현재 변경 파일이다. `artSample/`의 승인 샘플과 Blender 보조·백업 파일은 작업 트리에 그대로 보존하고 스테이징·커밋 대상에서 제외한다.
- 반영 브랜치는 `main`, upstream은 `origin/main`, 원격 저장소는 `https://github.com/goldmembrane/bellerophon-s-envelop.git`이다. 커밋 메시지는 `feat: update enemy models and animations`로 정했다.
- 이번 최종 기록·커밋 작업에서는 Unity 에디터, 씬 재저장, 에셋 재생성, 캡처를 실행하지 않는다. 현재 승인된 파일 상태를 문서화한 뒤 그대로 커밋하고 푸시한다.
