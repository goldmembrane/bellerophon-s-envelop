# Bellerophon 진행 상황 — 2026-07-29

## 완료 - Revolution_08 새 115프레임 사망 FBX 재교체·Loop Pose 제거

- 사용자가 같은 경로
  `enemies model/révolution death.fbx`에 다시 배치한 새 원본은
  `673776`바이트이며 SHA-256은
  `978D21F60FCEC9E07DF284A6A4349064BA3D7962687CDB8402E3B4F615CBADEA`
  입니다. 직전 적용된 `734608`바이트·212프레임 파일과 다른
  원본임을 확인했습니다.
- Blender 읽기 전용 확인에서 본체는 `2307` authored 정점,
  `3945`면, 기존 레볼루션과 같은 24본 이름·순서를 유지합니다.
  액션은 `walking_man(1~63프레임)`과
  `Armature|mixamo.com|Layer0(1~115프레임)`이며 Mixamo 액션은
  정확히 하나입니다.
- `Revolution_08`의 기존 사망 모델을 삭제하고 새 FBX의 직접
  인스턴스 하나로 교체했습니다. 외부 원본과 프로젝트
  `RevolutionDeath.fbx`의 최종 SHA-256은 새 해시와 일치합니다.
- 외형은 생성하거나 추정하지 않았습니다. `Revolution_01` 정적
  모델이 사용하는 승인 Mesh와 기존 8개 머티리얼 참조를 새
  `Revolution_08`에 그대로 연결했습니다.
  `StaticAndDeathMeshSame=True`,
  `StaticAndDeathMaterialsSame=True`이며 새 Mesh·머티리얼·텍스처는
  생성하지 않았습니다.
- Unity는 새 FBX의 유일한 Mixamo 테이크 전체를
  `Revolution_08_Death_Mixamo`로 선택했습니다. 클립 범위는
  `firstFrame=0`, `lastFrame=114`, 길이 `1.9초`, `60fps`,
  `79`개 커브 바인딩입니다.
- 별도의 제자리 복귀처럼 보이게 했던 Unity `Loop Pose` 보정을
  제거했습니다. 최종 설정은 `Loop Time=True`,
  `Loop Pose=False(loopBlend=0)`, 속도 `1.0`, Root Motion
  비활성입니다. 마지막 프레임 뒤 시작 프레임으로 즉시 전환해
  반복하며 복귀 키나 보간 모션을 새로 만들지 않았습니다.
- 정면·사선에서 `0/0.25/0.5/0.75/1.0` 구간을 직접 확인했습니다.
  Mixamo 원본은 넘어지는 동작을 진행해 마지막에 바닥에 누운
  자세를 유지하며 `0.75~1.0` 구간에 제자리로 일어나는 복귀
  동작은 없습니다. 몸통·양팔·총신·다리는 연결 상태를 유지하고
  부품 공중 잔류, 메시 찢김이나 외형 붕괴는 보이지 않았습니다.
- `PlayCurrentRevolutionDeathLoop` 명령으로 최종 Play 상태에
  진입한 뒤 `4.1초` 이상 실제 재생해 두 번 이상의 반복을
  확인했습니다. 현재 Unity에서 `1.9초` 전체 Mixamo 클립을
  `Loop Pose` 없이 반복 재생하고 있습니다.
- 직접 확인 자료는
  `docs/validation/revolution_death_2026-07-29/Revolution_08_Death_VisualReview.png`,
  구조·설정 기록은 같은 폴더의
  `Revolution_08_Death_Inspection.txt`입니다.

### 수행하지 않은 항목

- 제자리 복귀 모션·키프레임·보간 동작 생성
- Unity `Loop Pose` 적용
- 제공 FBX의 애니메이션 키·속도·길이 수정
- 새 Mesh·머티리얼·텍스처·셰이더 생성
- `Revolution_01~07` 수정
- AI, 사망 판정, 드롭 및 전투 로직 연결
- 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Unity 재시작
- Git 작업

## 완료 - Revolution_08 새 212프레임 사망 FBX 재교체·전체 반복 재생

- 사용자가 같은 경로
  `enemies model/révolution death.fbx`에 다시 배치한 새 파일은
  `734608`바이트이며 SHA-256은
  `448922A917D7D0C7A52E361B98AD71E12CD32BA927FEBED2AA646301896D79E9`
  입니다. 직전 적용 파일의 `703024`바이트·
  `908F579A9080365A39B5D3EC5C1B61BE19ED842AEFB2D8B3A26C2F0C9FCD7B12`
  와 다른 파일임을 확인했습니다.
- Blender 읽기 전용 확인에서 새 FBX의 본체는 기존과 같은
  `2307` authored 정점, `3945`면, 동일한 24본 이름·순서를
  유지합니다. 내장 액션은 `walking_man(1~63프레임)`과
  `Armature|mixamo.com|Layer0(1~212프레임)`이며 Mixamo 액션은
  정확히 하나입니다.
- `Revolution_08`의 기존 사망 모델 인스턴스를 삭제하고 새 FBX의
  직접 인스턴스 하나로 교체했습니다. 외부 원본과 프로젝트의
  `RevolutionDeath.fbx` 최종 SHA-256은 모두 새 해시와 일치합니다.
- 외형을 새로 만들거나 추정하지 않았습니다. `Revolution_01` 정적
  모델의 현재 승인
  `Revolution_ApprovedAppearance.fbx` Mesh와 기존 8개 머티리얼
  참조를 새 사망 모델에 그대로 연결했습니다.
  `StaticAndDeathMeshSame=True`,
  `StaticAndDeathMaterialsSame=True`이며 새 Mesh·머티리얼·텍스처는
  생성하지 않았습니다.
- Unity는 새 FBX의 유일한 Mixamo 테이크 전체를
  `Revolution_08_Death_Mixamo`로 선택했습니다. 클립은
  `3.516667초`, `60fps`, `79`개 커브 바인딩이며 원본 속도
  `1.0`, Loop Time·Loop Pose·WrapMode.Loop, Root Motion 비활성
  상태로 반복 재생합니다.
- 정규화 시간 `0/0.25/0.5/0.75/1.0`을 정면·사선에서 직접
  확인했습니다. 정확한 마지막 프레임에서 몸체가 옆으로 완전히
  넘어지며 양팔·총신·다리가 몸통과 함께 유지됩니다. 부품 공중
  잔류, 메시 찢김이나 외형 붕괴는 보이지 않았습니다.
- 종료 자세 확인 뒤 Unity Play 상태에서 전체 길이보다 긴
  `4.1초` 이상 실제 재생했으며, 마지막 프레임 뒤 처음부터 반복
  재생하도록 유지했습니다.
- 적용 명령은 `ApplyRevolutionDeath`, 정확한 종료 확인 명령은
  `ReviewRevolutionDeathToEnd`, 캡처·재교체 없이 최종 반복 재생
  상태로 진입한 명령은 `PlayCurrentRevolutionDeathLoop`입니다.
  직접 확인 자료는
  `docs/validation/revolution_death_2026-07-29/Revolution_08_Death_ToEnd_VisualReview.png`,
  구조·시간 기록은 같은 폴더의
  `Revolution_08_Death_Inspection.txt`와
  `Revolution_08_Death_ToEnd_Inspection.txt`입니다.

### 수행하지 않은 항목

- 제공 FBX와 내장 Mixamo 애니메이션의 키·커브·속도·길이 수정
- 제공 FBX에 없는 자세·모션·물리 모션 생성
- 새 Mesh·머티리얼·텍스처·셰이더 생성
- `Revolution_01~07` 수정
- AI, 사망 판정, 드롭 및 전투 로직 연결
- 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Unity 재시작
- Git 작업

## 완료 - Revolution_08 사망 모션 정확한 종료까지 재생·직접 확인

- 기존 검토 자료의 마지막 열은 정규화 시간 `0.99`였으므로 정확한
  마지막 키를 확인한 자료가 아니었습니다. 전용 명령
  `ReviewRevolutionDeathToEnd`를 추가해 마지막 열을 정확히 `1.0`,
  즉 클립 전체 길이 `2.983334초`로 샘플링했습니다.
- 반복 클립을 `1.0`에서 시작 자세로 감아버리지 않도록 검토할 때만
  메모리 안의 비저장 복제 클립을 `ClampForever`·비반복으로
  샘플링했습니다. 프로젝트의
  `Revolution_08_Death_Mixamo` 클립, 컨트롤러, FBX, 애니메이션 커브,
  모델 외형과 씬 저장 내용은 변경하지 않았습니다.
- 정확한 종료 자세를 정면과 사선에서 직접 확인했습니다. 마지막
  프레임에서 몸통이 정면 기준 오른쪽으로 크게 기울고 양팔·총신·
  다리가 함께 따라가며, 시작 자세로 잘못 감기지 않습니다. 몸통과
  팔다리 연결이 유지되고 부품 공중 잔류, 메시 찢김이나 외형 붕괴는
  보이지 않았습니다.
- 종료 자세 캡처 뒤 Unity를 Play 상태로 전환하고 클립 한 사이클
  `2.983334초`보다 긴 `3.5초` 이상 실제 재생했습니다. 실제 런타임
  클립은 기존 `Loop Time`과 Root Motion 비활성 설정 그대로 반복
  재생 중입니다.
- 직접 확인 자료는
  `docs/validation/revolution_death_2026-07-29/Revolution_08_Death_ToEnd_VisualReview.png`,
  시간·샘플 방식 기록은 같은 폴더의
  `Revolution_08_Death_ToEnd_Inspection.txt`입니다.

### 수행하지 않은 항목

- 사망 FBX, 모델 외형, Mesh, 머티리얼, 텍스처 수정
- 사망 애니메이션 키·커브·길이·속도·반복 설정 수정
- `Revolution_01~07` 수정
- 씬 오브젝트 생성·삭제·교체 또는 씬 저장
- 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Unity 재시작
- Git 작업

## 완료 - Revolution_08 동일 경로의 새 사망 FBX로 재교체

- 사용자가 `enemies model/révolution death.fbx`를 같은 이름의 다른
  파일로 교체했다고 명시했습니다. 새 원본은 `703024`바이트이며
  SHA-256은
  `908F579A9080365A39B5D3EC5C1B61BE19ED842AEFB2D8B3A26C2F0C9FCD7B12`
  입니다. 이전 적용 파일의 SHA-256
  `08316AB8B02F2C0AAB5CA9C7D7E20934AA0D6F55390E4112D12385EDA7FBCD5D`와
  다른 파일임을 확인했습니다.
- `Revolution_08`의 이전 사망 모델 인스턴스를 다시 삭제하고, 새
  원본을 `Assets/_Project/Art/Enemies/Revolution/Models/RevolutionDeath.fbx`로
  덮어쓴 뒤 새 직접 인스턴스로 교체했습니다. 외부 원본과 프로젝트
  복사본의 최종 SHA-256은 모두 새 해시와 일치합니다.
- 새 FBX의 본체는 `2307` authored 정점, `3945`면, 기존 레볼루션과
  같은 24본 이름·순서를 유지합니다. 기존 `walking_man`과 별도로
  `Armature|mixamo.com|Layer0` 액션이 정확히 하나 있으며, 새 Mixamo
  액션 범위는 `1~180프레임`입니다.
- Unity는 새 Mixamo 액션만 `Revolution_08_Death_Mixamo`로 선택했습니다.
  새 클립은 `2.983334초`, `60fps`, `79`개 커브 바인딩이며
  `Loop Time`, `Loop Pose`, `WrapMode.Loop`, Root Motion 비활성
  상태로 `Revolution_08_Death.controller`에서 반복 재생됩니다.
- 외형은 새로 만들거나 추정하지 않았습니다. `Revolution_01`의
  `Revolution_ApprovedAppearance.fbx` Mesh와 기존 8개 머티리얼
  참조를 새 `Revolution_08` Renderer에 그대로 연결했습니다.
  `Revolution_01~07`의 서명은 변경되지 않았습니다.
- 자동으로 애니메이션 Bounds를 따라가는 첫 구도는 새 모션의 루트
  이동 때문에 개체를 지나치게 작게 보여 최종 판정에서 제외했습니다.
  최종 자료는 `Revolution_01`의 고정 중심과 동일한 넓은 배율을
  사망 모션 전 구간에 적용해 카메라가 모션을 따라가지 않게 했습니다.
- 최종 정면·사선 비교에서 새 모션은 약 3초 동안 정면 자세에서
  몸통과 양팔이 함께 내려가고 마지막에 한쪽으로 크게 기울어집니다.
  전 구간에서 몸통·양팔·총신·다리가 연결된 상태를 유지하며 부품
  공중 잔류, 메시 찢김이나 외형 붕괴는 보이지 않았습니다.
- 최종 자료는
  `docs/validation/revolution_death_2026-07-29/Revolution_08_Death_VisualReview.png`와
  `Revolution_08_Death_Inspection.txt`입니다. 적용 명령은
  `ApplyRevolutionDeath`이며, 적용 뒤 Unity는 Play 상태로 전환되어
  새 사망 모션을 반복 재생하고 있습니다.

### 수행하지 않은 항목

- 새 원본 `révolution death.fbx`와 내장 Mixamo 클립 수정
- 제공 FBX에 없는 자세·키프레임·물리 모션 생성
- 모델링 외형, Mesh, 머티리얼, 텍스처, 셰이더 생성 또는 수정
- `Revolution_01~07` 수정
- 게임플레이, AI, 사망 판정, 드롭과 전투 로직 연결
- 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Unity 재시작
- Git 작업

## 이전 적용 기록(현재 새 파일로 대체됨) - Revolution_08 제공 FBX 사망 모션 교체·정적 외형 동기화

- 확정된 사망 모션 슬롯
  `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_08`에서
  기존 모델 인스턴스를 삭제하고, 사용자가 지정한
  `enemies model/révolution death.fbx`를 프로젝트의
  `Assets/_Project/Art/Enemies/Revolution/Models/RevolutionDeath.fbx`로
  원본 그대로 복사해 직접 인스턴스로 교체했습니다.
- 원본과 프로젝트 복사본의 SHA-256은 모두
  `08316AB8B02F2C0AAB5CA9C7D7E20934AA0D6F55390E4112D12385EDA7FBCD5D`
  입니다. 제공 FBX에는 `walking_man` 액션과 별도로
  `Armature|mixamo.com|Layer0` 액션 하나가 있으며, 이름과 Take 정보로
  유일하게 식별된 Mixamo 액션만 `Revolution_08_Death_Mixamo`로
  가져왔습니다.
- Blender 읽기 전용 확인에서 제공 FBX의 본체는 `2307` authored 정점,
  `3945`면이며 기존 레볼루션과 같은 24개 본 이름·순서를 사용합니다.
  Unity 선택 클립은 길이 `1.9초`, `60fps`, `79`개 커브 바인딩이며
  `Loop Time`, `Loop Pose`, `WrapMode.Loop`를 사용합니다.
- `Revolution_08_Death.controller`의 유일한 기본 상태
  `Revolution_08_Death_Mixamo`가 선택한 클립 하나만 직접 참조합니다.
  Animator Root Motion은 비활성화했으며 적용 뒤 Unity를 Play 상태로
  전환해 반복 재생하도록 했습니다.
- 외형은 새로 만들거나 추정하지 않았습니다. `Revolution_01`의 현재
  승인 `Revolution_ApprovedAppearance.fbx` Mesh와 기존 8개 머티리얼
  참조를 `Revolution_08`의 대응 Renderer에 그대로 연결했습니다.
  새 Mesh·머티리얼·텍스처·셰이더는 생성하지 않았습니다.
- 정면·사선에서 정적 기준과 사망 모션의
  `0/0.25/0.5/0.75/0.99` 구간을 직접 비교했습니다. 몸통이 균형을
  잃고 한쪽으로 기울 때 양팔과 다리가 함께 움직이며, 몸통·팔·총신·
  다리가 연결된 상태를 유지합니다. 부품 공중 잔류, 메시 찢김,
  외형 교체나 붕괴는 보이지 않았습니다.
- 원본 기획서에는 레볼루션의 구체적인 사망 자세가 지정되어 있지
  않으므로 제공된 Mixamo 모션 외에 자세·속도·키를 임의로 만들거나
  보정하지 않았습니다.
- 최종 직접 비교 자료는
  `docs/validation/revolution_death_2026-07-29/Revolution_08_Death_VisualReview.png`,
  구조 기록은 같은 폴더의 `Revolution_08_Death_Inspection.txt`입니다.
  적용 명령은 `ApplyRevolutionDeath`입니다.

### 수행하지 않은 항목

- 원본 `révolution death.fbx`와 내장 Mixamo 클립 수정
- 제공 FBX에 없는 사망 자세·키프레임·물리 모션 생성
- 모델링 외형, Mesh, 머티리얼, 텍스처, 셰이더 생성 또는 수정
- `Revolution_01~07` 수정
- 게임플레이, AI, 사망 판정, 드롭과 전투 로직 연결
- 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Unity 재시작
- Git 작업

## 완료 - Revolution_07 3초 360도 방향 전환·정적 팔 각도 동기화

- `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_07`의 Animator가
  `Assets/_Project/Art/Enemies/Revolution/Animations/Revolution_07_Turn_360_3s_StaticArms.anim`을
  참조하도록 변경했습니다. 클립 길이는 정확히 `3초`, 프레임레이트는 `60fps`이며
  `Revolution_07_Turn.controller`의 유일한 기본 상태로 반복 재생됩니다.
- 제공된 `Revolution_07_Turn_Mixamo` 원본의 방향 전환 담당 커브는
  `Armature/Hips`의 로컬 위치 3개와 쿼터니언 회전 4개로 확인했습니다.
  원본의 수평 방향 전환량 `-176.1403도`를 같은 회전 방향의 `-180도`로
  정규화하고, 동일 동작을 두 구간으로 이어 정확히 `-360도`가 되도록 했습니다.
- 방향각은 `0초/0.75초/1.5초/2.25초/3초`에서 각각
  `0/-90/-180/-270/-360도`이며 최대 방향각 오차는 `0도`입니다.
  시작과 3초 끝의 Hips 위치 오차와 회전 오차도 모두 `0`입니다.
- 원본 Mixamo의 걷기·발 디딤·몸통 동작은 두 구간에 반복 적용했습니다.
  Hips를 제외한 보호 대상 Mixamo 커브의 최대 샘플 오차는
  `0.000000149`입니다. 두 번째 반 바퀴의 Hips 위치 궤적은 첫 번째
  궤적의 중심을 기준으로 180도 대칭시켜 중간 연결과 최종 루프가
  이어지도록 했습니다.
- 양팔은 `Revolution_01`의 현재 정적 모델을 직접 기준으로
  `Left/Right Shoulder → Arm → ForeArm → Hand` 8개 본의 로컬
  쿼터니언 회전을 3초 전체에 고정했습니다. 외형 Mesh·8개 머티리얼
  참조는 변경하지 않았고 `Revolution_01~06`, `Revolution_08`도
  변경하지 않았습니다.
- 정면·사선에서 정적 기준과 `0/0.75/1.5/2.25/3초` 자세를 직접
  비교한 결과, 정면→측면→후면→반대 측면→정면으로 회전하며 양팔
  형태가 정적 모델 각도를 유지하고 메시 붕괴나 분리된 부품은
  보이지 않았습니다.
- 직접 확인 자료는
  `docs/validation/revolution_turn_2026-07-29/Revolution_07_Turn_VisualReview.png`,
  수치·구조 기록은 같은 폴더의
  `Revolution_07_Turn_360_3s_StaticArms_Inspection.txt`입니다.
- 적용 뒤 Unity는 Play 상태로 전환되어 수정된 방향 전환 모션을
  반복 재생하고 있습니다.

### 실행하지 않은 항목

- 원본 `révolution turn.fbx`와 내부 Mixamo 클립 수정
- 모델링 외형, Mesh, 머티리얼, 텍스처, 셰이더 수정
- `Revolution_01~06`, `Revolution_08` 수정
- 게임플레이, AI, 물리, 실제 전투 로직 연결
- 하네스 검증
- EditMode/PlayMode 테스트와 Windows 빌드
- Unity 재시작
- Git 작업

## 완료 - Revolution_07 제공 FBX 방향 전환 교체·정적 외형 동기화

- 확정 목록의 방향 전환 슬롯
  `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_07`에서
  기존 모델 인스턴스를 제거하고, 사용자가 지정한
  `enemies model/révolution turn.fbx`를 프로젝트의
  `Assets/_Project/Art/Enemies/Revolution/Models/RevolutionTurn.fbx`로
  원본 그대로 복사해 직접 인스턴스로 교체했습니다.
- 원본과 프로젝트 복사본의 SHA-256은 모두
  `74745CC9DF32B29B47BF101092F605B72AD5E9B0389DF7B372C8CAFD320E246D`
  입니다. 제공 FBX에는 기존 `walking_man` 액션과 별도로
  `Armature|mixamo.com|Layer0` 액션 하나가 있으며, 사용자가 지정한
  Mixamo 액션만 `Revolution_07_Turn_Mixamo`로 가져왔습니다.
- 선택 클립은 길이 `1.616667초`, `60fps`, `79`개 커브 바인딩이며
  `Loop Time`, `Loop Pose`, `WrapMode.Loop`를 사용합니다.
  `Revolution_07_Turn.controller`의 기본 상태
  `Revolution_07_Turn_Mixamo`가 이 클립 하나만 직접 참조합니다.
  Animator Root Motion은 비활성화했습니다.
- Blender 읽기 전용 대조에서 제공 FBX와 기존 레볼루션 FBX는
  `2307` authored 정점, `3945`면, 같은 24본 이름·순서와 동일한
  메시 좌표·면 인덱스를 사용했습니다. FBX 7700과 7400의 정밀도
  차이에 따른 본 정적 행렬 체크섬 차이만 존재했습니다.
- 외형은 새로 만들거나 추정하지 않았습니다.
  `Revolution_01`의 현재 승인
  `Revolution_ApprovedAppearance.fbx` Mesh와 기존 8개 머티리얼
  참조를 `Revolution_07`의 대응 Renderer에 그대로 연결했습니다.
  새 Mesh·머티리얼·텍스처·셰이더는 생성하지 않았습니다.
- Unity 직접 비교 시트는 정면 정적 기준, 정면 방향 전환 5시점,
  사선 정적 기준, 사선 방향 전환 5시점을 같은 행 구성으로 보여줍니다.
  제공 모션은 정면에서 측면을 거쳐 후면으로 돌아서는 약 180도 방향
  전환이며 몸통·다리·양팔이 함께 회전합니다. 전 구간에서 승인 외형과
  재질이 유지되고 부품 공중 잔류, 메시 찢김, 외형 붕괴는 보이지
  않습니다.
- 원본 기획서의 방향 전환 기준은 `360도 3초`이지만, 이번 사용자
  지시는 제공 FBX의 Mixamo 모션을 그대로 반복 재생하는 것이므로
  실제 `1.616667초·약 180도` 테이크의 키나 속도를 임의 변경하지
  않았습니다.
- 최종 직접 비교 자료는
  `docs/validation/revolution_turn_2026-07-29/Revolution_07_Turn_VisualReview.png`,
  구조 기록은 같은 폴더의
  `Revolution_07_Turn_Inspection.txt`입니다.
- 적용 명령은 `ApplyRevolutionTurn`이며, 적용 뒤 Unity를 Play
  상태로 전환해 `Revolution_07`의 방향 전환 모션이 반복 재생되게
  했습니다.

### 수행하지 않은 항목

- `Revolution_01~06`, `Revolution_08` 수정
- 제공 FBX 애니메이션 키·회전량·재생 속도 수정 또는 새 `.anim` 생성
- 새 모델링·Mesh·머티리얼·텍스처·셰이더 생성
- AI 방향 판단, 실제 이동·회전과 전투 로직 연결
- 하네스 검증
- EditMode/PlayMode 테스트와 빌드
- Unity 재시작
- Git 작업

## 완료 - Revolution_06 피격 양팔 정적 각도 고정

- 사용자 확인에 따라 `Revolution_06` 피격 모션의 양팔 범위를
  `LeftShoulder → LeftArm → LeftForeArm → LeftHand`와
  `RightShoulder → RightArm → RightForeArm → RightHand`로
  확정했습니다.
- 원본 `Revolution_06_Hit_Mixamo` 클립은 수정하지 않았습니다.
  전체 클립을
  `Animations/Revolution_06_Hit_StaticArms.anim`으로 복제한 뒤,
  위 8개 본의 회전 바인딩만 `Revolution_01` 정적 모델링의 로컬
  쿼터니언 값으로 0초부터 1.116667초까지 고정했습니다.
- 원본의 팔 회전 Euler 바인딩 24개를 제거하고 같은 8개 본의
  쿼터니언 바인딩 32개로 교체해 보정 클립은 총 87개 바인딩을
  사용합니다. 팔 위치 커브, 몸통·머리·다리 등 비팔 커브,
  오브젝트 참조 커브와 애니메이션 이벤트는 원본 Mixamo 클립과
  동일한 서명임을 적용 전후에 확인했습니다.
- `Revolution_06_Hit.controller`의 단일 기본 상태
  `Revolution_06_Hit_StaticArms`가 보정 클립을 반복 재생합니다.
  길이 `1.116667초`, `60fps`, Loop Time·Loop Pose·WrapMode.Loop,
  Root Motion 비활성 상태를 유지했습니다.
- 직접 비교 시트는 다른 슬롯을 숨긴 상태에서 24본 범위로 프레이밍한
  정면·사선 정적 기준과 피격 5시점을 보여줍니다. 피격 중 몸통이
  회전·후퇴해 양팔 체인 전체가 몸통과 함께 방향을 바꾸지만,
  어깨부터 손까지의 상대 각도와 총신 방향은 정적 자세를 유지합니다.
  원본처럼 팔이 독립적으로 위로 들리거나 꺾이지 않으며, 부품 분리,
  허공 잔류, 메시 찢김이나 외형 붕괴도 보이지 않습니다.
- 최종 자료는
  `docs/validation/revolution_hit_2026-07-29/Revolution_06_Hit_VisualReview.png`와
  `Revolution_06_Hit_Inspection.txt`입니다.
- 적용 명령은 `ApplyRevolutionHitStaticArmPose`이며, 적용 후 Unity를
  Play 상태로 전환해 보정된 피격 모션을 반복 재생하도록 했습니다.

### 수행하지 않은 항목

- 원본 `révolution hit.fbx`와 내장 Mixamo 클립 수정
- 양팔 8개 본의 회전 이외 애니메이션 커브 수정
- `Revolution_01~05`, `Revolution_07~08` 수정
- 모델링·Mesh·머티리얼·텍스처·셰이더 수정
- AI, 피격 판정, 피해량과 전투 로직 수정
- 하네스 검증
- EditMode/PlayMode 테스트와 빌드
- Unity 재시작
- Git 작업

## 완료 - Revolution_06 제공 FBX 피격 교체·정적 외형 동기화

- 확정 목록의 피격 슬롯
  `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_06`에서
  기존 모델 인스턴스를 제거하고, 사용자가 지정한
  `enemies model/révolution hit.fbx`를 프로젝트의
  `Assets/_Project/Art/Enemies/Revolution/Models/RevolutionHit.fbx`로
  원본 그대로 복사해 직접 인스턴스로 교체했습니다.
- 원본과 프로젝트 복사본의 SHA-256은 모두
  `F8637B24BA6F08A94D5FAFB9A51A61BFDAD57C2666F40CDD3E893303CE429F8D`
  입니다. 제공 FBX에는 기존 `walking_man` 액션과 별도로
  `Armature|mixamo.com|Layer0` 액션 하나가 있으며, 사용자가 지정한
  Mixamo 액션만 `Revolution_06_Hit_Mixamo`로 가져왔습니다.
- 선택 클립은 길이 `1.116667초`, `60fps`, `79`개 커브 바인딩이며
  `Loop Time`, `Loop Pose`, `WrapMode.Loop`를 사용합니다.
  `Revolution_06_Hit.controller`의 기본 상태
  `Revolution_06_Hit_Mixamo`가 이 클립 하나만 직접 참조합니다.
  Animator Root Motion은 비활성화했습니다.
- Blender 읽기 전용 대조에서 제공 FBX와 기존 레볼루션 FBX는
  `2307` authored 정점, `3945`면, 같은 24본 이름·순서와 동일한
  메시 좌표·면 인덱스를 사용했습니다. FBX 7700과 7400의 정밀도
  차이에 따른 본 정적 행렬 체크섬 차이만 존재했습니다.
- 외형은 새로 만들거나 추정하지 않았습니다.
  `Revolution_01`의 현재 승인
  `Revolution_ApprovedAppearance.fbx` Mesh와 기존 8개 머티리얼
  참조를 `Revolution_06`의 대응 Renderer에 그대로 연결했습니다.
  새 Mesh·머티리얼·텍스처·셰이더는 생성하지 않았습니다.
- Unity 직접 비교 시트는 정면 정적 기준, 정면 피격 5시점,
  사선 정적 기준, 사선 피격 5시점을 같은 행 구성으로 보여줍니다.
  피격 중 몸통이 크게 뒤로 젖혀지고 양팔이 위로 들렸다가 복귀하며,
  전 구간에서 원형 몸통·두 다리·양팔·총신 외형과 재질 배치가 정적
  기준과 일치합니다. 부품 공중 잔류, 메시 찢김, 외형 교체나 붕괴는
  보이지 않습니다.
- 최종 직접 비교 자료는
  `docs/validation/revolution_hit_2026-07-29/Revolution_06_Hit_VisualReview.png`,
  구조 기록은 같은 폴더의
  `Revolution_06_Hit_Inspection.txt`입니다.
- 적용 뒤 Unity를 Play 상태로 전환하도록 예약해 현재 에디터에서
  `Revolution_06`의 피격 모션이 반복 재생되게 했습니다.

### 수행하지 않은 항목

- `Revolution_01~05`, `Revolution_07~08` 수정
- 제공 FBX 애니메이션 키 수정 또는 새 `.anim` 생성
- 새 모델링·Mesh·머티리얼·텍스처·셰이더 생성
- AI, 피격 판정, 피해량, 이동 및 전투 로직 연결
- 하네스 검증
- EditMode/PlayMode 테스트와 빌드
- Unity 재시작
- Git 작업

## 완료 - Revolution_05 제공 FBX 근접 공격 교체·정적 외형 동기화

- 확정 목록의 근접 공격 슬롯
  `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_05`에서
  기존 모델 인스턴스를 제거하고, 사용자가 지정한
  `enemies model/révolution slash.fbx`를 프로젝트의
  `Assets/_Project/Art/Enemies/Revolution/Models/RevolutionSlash.fbx`로
  원본 그대로 복사해 직접 인스턴스로 교체했습니다.
- 원본과 프로젝트 복사본의 SHA-256은 모두
  `599A1203FCD360D6401F6AD168823FF1702DBFAAF050C26D01FF71BCA991A52B`
  입니다. 제공 FBX에는 기존 `walking_man` 액션과 별도로
  `Armature|mixamo.com|Layer0` 액션 하나가 있으며, 사용자가 지정한
  Mixamo 액션만 `Revolution_05_MeleeAttack_Mixamo`로 가져왔습니다.
- 선택 클립은 길이 `1.566667초`이며 `Loop Time`, `Loop Pose`,
  `WrapMode.Loop`를 사용합니다.
  `Revolution_05_MeleeAttack.controller`의 기본 상태
  `Revolution_05_MeleeAttack_Mixamo`가 이 클립 하나만 직접
  참조합니다. Animator Root Motion은 비활성화했습니다.
- Blender 읽기 전용 대조에서 제공 FBX와 기존 레볼루션 FBX는
  `2307` authored 정점, `3945`면, 같은 24본 이름·순서와 동일한
  메시 좌표·면 인덱스를 사용했습니다. FBX 7700과 7400의 정밀도
  차이에 따른 본 정적 행렬 최대 차이는 `0.000045776`입니다.
- 외형은 새로 만들거나 추정하지 않았습니다.
  `Revolution_01`의 현재 승인
  `Revolution_ApprovedAppearance.fbx` Mesh와 기존 8개 머티리얼
  참조를 `Revolution_05`의 대응 Renderer에 그대로 연결했습니다.
  새 Mesh·머티리얼·텍스처·셰이더는 생성하지 않았습니다.
- Unity 직접 비교 시트는 정면 정적 기준, 정면 공격 5시점,
  사선 정적 기준, 사선 공격 5시점을 같은 행 구성으로 보여줍니다.
  정적 기준과 공격 전 구간에서 몸통·다리·양팔 부품, 색과 재질
  배치가 일치합니다. 몸통 회전과 양팔의 큰 휘두르기가 분명하며,
  부품 공중 잔류, 메시 찢김, 외형 교체나 붕괴는 보이지 않습니다.
  최종 직접 비교 자료는
  `docs/validation/revolution_melee_attack_2026-07-29/Revolution_05_MeleeAttack_VisualReview.png`,
  구조 기록은 같은 폴더의
  `Revolution_05_MeleeAttack_Inspection.txt`입니다.
- 적용 뒤 Unity를 Play 상태로 전환해 현재 에디터에서
  `Revolution_05`의 근접 공격 모션이 반복 재생되도록 했습니다.

### 수행하지 않은 항목

- `Revolution_01~04`, `Revolution_06~08` 수정
- 제공 FBX 애니메이션 키 수정 또는 새 `.anim` 생성
- 새 모델링·Mesh·머티리얼·텍스처·셰이더 생성
- AI, 공격 판정, 피해량, 이동 및 전투 로직 연결
- 하네스 검증
- EditMode/PlayMode 테스트와 빌드
- Unity 재시작
- Git 작업

## 완료 - Revolution_03 FBX 내장 이동 모션 반복 재생

- `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_03`에만
  현재 `Revolution.fbx`의 내장 테이크
  `Armature|Armature|Armature|Armature|walking_man|baselayer`를
  연결했습니다.
- 새 애니메이션과 키프레임을 만들지 않았습니다. FBX 바이너리는
  변경하지 않고 Unity 임포터에서 기존 0~62프레임 내장 테이크의
  애니메이션 가져오기와 `Loop Time`, `Loop Pose`, `WrapMode.Loop`만
  활성화했습니다.
- `Revolution_03_Move.controller`의 기본 상태
  `RevolutionEmbeddedMove`는 길이 `1.033333초`인 해당 FBX 내장 클립을
  직접 참조하고 반복 재생합니다.
- `Revolution_03` 모델에만 Animator를 연결했으며 Root Motion은
  비활성화했습니다. 적용 과정에서 기존 메시·머티리얼·외형·본 계층과
  배치 Transform은 변경하지 않았고 다른 Revolution 슬롯도 변경하지
  않았습니다.
- 적용 후 Unity를 Play 상태로 전환해 현재 에디터에서 이동 모션이
  재생되도록 했습니다.

### 수행하지 않은 항목

- 새 `.anim` 또는 키프레임 생성
- FBX 바이너리, 모델링 외형, 메시, 머티리얼, 텍스처와 본 구조 수정
- `Revolution_03` 외 다른 개체 수정
- 하네스 검증
- EditMode/PlayMode 테스트와 빌드
- Unity 재시작
- Git 작업

## Revolution_04 오른팔 총신 링 반경 재교정

### 사용자 영상으로 무효화된 이전 완료 판정

- 사용자 제공 `2026-07-29 00-37-45.mp4`를 확인한 결과, 이전 후보는
  오른팔 총신의 기울기와 일부 선택 문제만 해결됐습니다.
- 오른팔 다섯 총신 묶음의 공전 반경이 원반보다 커서 둥근 총구 부품이
  원반 아래와 바깥으로 크게 분리되어 도는 현상이 남아 있었습니다.
- 따라서 2026-07-29의 이전 완료 판정과 이전 검토 이미지는 무효입니다.
  무효 이미지는 `Logs/RevolutionMachineGunInvalidDiagnostics/`로
  격리했습니다.

### 확인한 원인

- 오른팔 총신 묶음마다 부모 링 회전과 반대 방향의 자식 보정 회전을
  적용해 총신 기울기는 막았지만, 각 묶음의 원래 중심 위치를 그대로
  공전 피벗으로 사용했습니다.
- 정상 왼팔의 링 반경을 오른팔에 반영하는 처리가 없었기 때문에,
  오른팔의 넓은 원래 피벗 반경이 유지됐습니다.

### 적용

- 정상 왼팔의 메시와 애니메이션은 변경하지 않았습니다.
- 정상 왼팔 총신 구성요소 중심의 링 반경을 읽기 전용 기준값으로
  사용했습니다.
- 오른팔 다섯 총신 묶음의 피벗과 실제 메시 위치를 그 반경까지 안쪽으로
  이동시켰습니다.
- 부모 링 회전과 자식 역회전 구조는 유지해 오른팔 총신 전체가
  기울지 않고 평행한 상태로 회전하게 했습니다.
- `ApplyRevolutionMachineGunAttackMotion`으로
  `CargoRunMvp/Approved Revolution Enemy Placement/Revolution_04`에
  다시 적용하고 씬을 저장했습니다.

### 직접 시각 비교

- 기준 영상 `2026-07-28 16-02-35.mp4`의 정상 왼팔과 수정 전 영상
  `2026-07-29 00-37-45.mp4`의 오른팔을 각각 10fps 접촉 시트로
  직접 대조했습니다.
- 수정 전 영상에서 총신 하나가 원반 아래·바깥으로 크게 공전하는 것을
  확인했습니다.
- 수정 후보의 정면·확대 6프레임, 30fps 연속 프레임, 색상 분리 화면을
  직접 확인했습니다.
- 오른팔 다섯 총신 묶음은 모두 선택돼 있으며 서로 평행합니다.
- 총신 묶음은 고정 원반 중심부의 좁은 링 안에서만 위치를 바꾸며,
  수정 전의 원반 밖 큰 원형 공전은 나타나지 않습니다.

### 최종 자료

- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_VisualReview.png`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_DirectVisualReview.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_RightCloseup.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_DirectVisualReview.txt`

### 수행하지 않은 항목

- `InspectRevolutionMachineGunAttackMotion`
- 일반 자동 검증, EditMode/PlayMode 테스트, 빌드
- Unity 재시작
- Git 작업
- 정상 왼팔 및 다른 개체 수정

## 완료 - Revolution_04 정적 모델 기준 오른팔 사격 형태 복구

### 사용자 영상과 원인

- 사용자 제공 `2026-07-29 11-41-27.mp4`의 6.32초 전체 구간을
  8fps 프레임과 4fps 접촉 시트로 직접 확인했습니다.
- 오른팔 총신 회전 모션 자체는 정상이나, 오른팔 상완·전완·손 하우징이
  몸통 가까이에서 뭉치고 총신 케이지는 바깥에 남아 허공에 떠 보이는
  형태 붕괴를 확인했습니다.
- 씬의 정적 기준인 `Revolution_01`과 현재 `Revolution_04`를
  정면·사선에서 같은 카메라로 비교했습니다.
- 정적 오른팔은 어깨–상완–전완–손 하우징–총신이 연속된 형태지만,
  기존 공격 자세는 왼팔 방향을 오른팔에 반사한 뒤 오른팔 상완·전완·손을
  개별 정렬해 좌우 본 로컬축 차이로 정적 오른팔의 상대 형태가
  무너졌습니다.

### 복구

- `Revolution_01`의 `RightArm/RightForeArm/RightHand` 로컬 회전을
  오른팔 형태 기준으로 사용했습니다.
- 세 본의 정적 상대 회전은 그대로 유지하고 `RightArm` 체인 전체만
  어깨에서 회전시켜 기존 오른팔 총구축이 `Revolution_04`의 정면
  발사 방향을 향하게 했습니다.
- `Revolution_04_MachineGun_Attack.anim`에서는
  `RightArm/RightForeArm/RightHand`의 고정 자세 커브만 교체했습니다.
- 오른팔 `Revolution_Right_Barrel_Ring_Bone` 회전 커브의 키·각도·
  방향·속도·위상은 수정하지 않았습니다.
- 왼팔 본·메시·자세·총신 회전·섬광을 포함한 나머지 애니메이션
  커브는 수정하지 않았습니다.

### 직접 시각 확인

- 정적 기준, 수정 전 공격 자세, 정적 기반 복구 자세를 정면·사선
  3열 비교 이미지로 구성했습니다.
- 적용 후 현재 공격 자세와 정적 기반 복구 자세가 같은 오른팔 형태로
  일치하는 것을 직접 확인했습니다.
- 원본 재질 고정 사선 30fps 연속 프레임의 `0·10·25·45` 프레임과
  전체 접촉 시트를 직접 확인했습니다.
- 총신이 회전하는 동안 오른팔의 어깨–상완–전완–손 하우징 연결과
  정면 사격 자세가 유지됐으며, 총신 케이지가 하우징에서 분리되거나
  허공으로 이동하지 않았습니다.

### 최종 자료

- `docs/validation/revolution_machinegun_attack_2026-07-28/UserFeedback_114127_ContactSheet.png`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_RightArmShape_StaticComparison.png`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_RightArmShapeMotion_ContactSheet.png`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_RightArmShapeMotion.mp4`

### 수행하지 않은 항목

- 하네스 검증
- 별도 자동 검사·EditMode/PlayMode 테스트·빌드
- 오른팔 총신 회전 애니메이션 수정 또는 재생성
- 왼팔 수정
- 다른 Revolution 슬롯과 다른 개체 수정
- Unity 재시작
- Git 작업

## Git 체크포인트

- 현재 `main`의 `origin/main` 기준 레볼루션 기관총 공격 작업을
  `fix: restore Revolution machine gun barrel ring motion` 메시지로
  커밋하고 같은 원격 브랜치에 푸시합니다.
- 커밋에는 `Revolution_04` 기관총 적용 도구와 브리지 연결, 현재 씬,
  최종 애니메이션·컨트롤러·파생 메시, 진행 문서와 직접 검토 자료를
  포함합니다.
- 현재 씬이 사용하는 왼팔 자산은
  `Revolution_04_Left_MachineGun_Barrels.asset` 하나이며, 오른팔은
  `Revolution_04_Right_MachineGun_BarrelGroup_01~05.asset`을 사용합니다.
- 과거 실패 구현에서 남은 미사용
  `Revolution_04_Left_MachineGun_BarrelGroup_01~12.asset`, Blender 자동
  백업 `.blend1` 파일과 Python `__pycache__`는 커밋에서 제외합니다.
- 강제 푸시, rebase, amend, 이력 재작성은 수행하지 않습니다.

## 다음 작업 확정 계획 — 오른팔 총신을 왼팔 방식으로 재구성

### 계획 확정 근거

- 사용자 제공 `2026-07-29 00-47-39.mp4`의 6.66초 전체 구간과 첫
  1.2초의 좌우 총신 연속 확대 프레임을 직접 대조했습니다.
- 개체 기준 왼팔은 화면 오른쪽이며, 정상 왼팔은 개별 총신이 따로
  공전하는 구조가 아닙니다. 팔·손·기관총 외부 하우징은 고정되고,
  원반 중심축에 결합된 총신 케이지 전체가 하나의 강체처럼 회전합니다.
- 왼팔 총신들의 길이 방향은 원반 면 법선이자 발사 방향인 공통 축과
  평행합니다. 따라서 총신 케이지 전체를 회전해도 총신이 대각선으로
  기울거나 원뿔 형태로 세차하지 않습니다.
- 왼팔의 발사 섬광은 회전 케이지의 자식이 아니라 손 기준의 고정 발사
  위치에 머뭅니다. 총신 케이지는 발사 순간 잠시 멈춘 뒤 다음 발사
  위치로 이동하는 인덱스 회전을 합니다.
- 현재 오른팔은 `Revolution_Right_Barrel_Ring_Bone` 아래에
  `Revolution_Right_MachineGun_BarrelGroup_01~05`를 각각 두고, 부모
  링 회전과 같은 크기의 반대 방향 자식 회전을 적용합니다. 이 구조는
  총신 자세를 화면상 고정하지만 총신 케이지 전체가 회전하는 왼팔의
  작동 원리와 다릅니다.
- 과거의 단순 강체 결합 방식은 오른팔 총신 각각의 길이 방향이 공통
  축과 평행하지 않은 상태에서 전체를 회전시켰기 때문에, 회전 각도에
  따라 총신이 대각선으로 기울고 원뿔을 훑듯 움직였습니다.

### 폐기할 접근

- 오른팔 총신마다 개별 공전 피벗을 두는 방식을 사용하지 않습니다.
- 부모 링 회전을 자식 역회전으로 상쇄하는 현재 방식을 최종 구조로
  사용하지 않습니다.
- 기울어진 오른팔 총신들을 원래 형상 그대로 단일 메시로 결합해
  회전하지 않습니다.
- 왼팔의 Transform, 축 벡터 또는 평균 반경 수치를 오른팔에 단순
  복사하거나 모델 중심면으로 반사하지 않습니다.
- 총신 하나씩 임의의 반경으로 이동해 겉보기 위치만 맞추지 않습니다.
- 자동 수치나 오브젝트 존재 여부를 시각 일치의 완료 근거로 사용하지
  않습니다.

### 변경 금지 기준

- 정상 왼팔의 메시, 링 본, 애니메이션 커브, 총신 선택, 총신 배치와
  섬광 위치는 수정하지 않습니다.
- `Revolution_04`의 팔 조준 자세, 몸체, 다리, 머리, 다른 공격 부품과
  다른 Revolution 슬롯은 이번 오른팔 총신 교정 대상에 포함하지
  않습니다.
- 원본 기획의 전환 시간, 연사 딜레이와 최대 사격 지속 시간은 오른팔
  링 회전 수정과 별개이므로 임의로 변경하지 않습니다.

### 구현 순서

1. 오른팔 원반의 고유 좌표계를 확정합니다.
   - 오른팔 원반 정면의 실제 중심과 원반 면 법선을 오른팔 형상에서
     구합니다.
   - 원반 면 법선과 발사 방향이 일치하도록 오른팔 전용 공통 축을
     확정합니다.
   - 왼팔 축 반사나 손·전완 방향만으로 축을 추정하지 않습니다.

2. 오른팔 다섯 총신을 완전한 단위로 다시 선택합니다.
   - 각 총신의 원반 삽입부, 긴 몸체, 앞쪽 총구와 연결 캡을 모두
     포함합니다.
   - 총신 일부가 빠지거나 고정 원반·허브·하우징이 포함되면 다음
     단계로 진행하지 않습니다.

3. 애니메이션 전에 각 총신을 정적으로 평행 정렬합니다.
   - 각 총신이 원반에 꽂힌 뒤쪽 삽입점을 고정 피벗으로 사용합니다.
   - 삽입점을 움직이지 않고 각 총신의 길이 방향을 오른팔 공통 축과
     평행하게 맞춥니다.
   - 앞뒤 깊이를 유지해 총신이 원반에서 빠지거나 안쪽으로 파묻히지
     않게 합니다.
   - 이 정렬은 실행 중 개별 Transform 보정으로 남기지 않고 파생
     메시의 정점 배치에 한 번만 굽습니다.

4. 오른팔 자체 원반을 기준으로 링 반경을 통일합니다.
   - 총신 삽입점들이 오른팔 원반 중심과 동심인 하나의 링 위에 놓이게
     합니다.
   - 왼팔 평균 반경을 그대로 사용하지 않고 오른팔 원반의 실제 내부
     범위를 기준으로 합니다.
   - 반경 조정이 필요하면 다섯 총신에 동일한 방식으로 적용하고,
     총신 사이의 각도 간격과 케이지 형태를 유지합니다.

5. 정렬된 오른팔 총신 전체를 하나의 결합 메시로 만듭니다.
   - 다섯 총신의 축 평행, 삽입점 링 배치, 앞뒤 깊이와 원반 내부
     배치를 정지 상태에서 먼저 직접 확인합니다.
   - 확인된 형상을
     `Revolution_04_Right_MachineGun_Barrels.asset` 하나로 결합합니다.
   - 결합 메시를 `Revolution_Right_Barrel_Ring_Bone` 바로 아래에
     연결합니다.
   - 기존 `Revolution_Right_MachineGun_BarrelGroup_01~05` 계층과
     개별 애니메이션 커브는 제거 대상입니다.

6. 애니메이션을 넣기 전에 단일 축 회전의 대표 자세를 확인합니다.
   - 시작 위치와 72도, 144도, 216도, 288도 위치를 차례대로 직접
     확인합니다.
   - 각 대표 위치 사이의 중간 각도도 확인합니다.
   - 한 총신이라도 회전 각도에 따라 길이 방향이 바뀌거나 원뿔·타원
     궤적, 앞뒤 출렁임, 원반 이탈이 보이면 애니메이션 단계로 넘어가지
     않고 2~4단계로 돌아갑니다.

7. 정적 회전이 정상인 뒤에만 왼팔의 시간 흐름을 오른팔에 적용합니다.
   - 왼팔과 같은 발사 순간 위치 유지와 다음 발사 위치로의 이동 순서를
     사용합니다.
   - 현재 공격 클립의 발사당 72도 인덱스 회전을 오른팔 링 본 하나에만
     적용합니다.
   - 왼팔 Quaternion 값을 복사하지 않고 오른팔 로컬축에서 정면상 같은
     회전 방향이 나오도록 직접 비교해 회전 부호를 확정합니다.
   - 개별 총신 자식에는 회전 또는 역회전 커브를 생성하지 않습니다.

8. 오른팔 섬광은 회전체와 분리된 고정 발사 위치를 유지합니다.
   - 섬광 피벗은 오른팔 손 또는 고정 하우징을 기준으로 둡니다.
   - 섬광이 링 본을 따라 공전하지 않게 합니다.
   - 회전해 들어온 총신이 고정 발사 위치에서 차례로 발사하는 모습이
     왼팔과 같은지 직접 대조합니다.

### 직접 시각 확인 기준

- 같은 시점의 왼팔과 오른팔을 나란히 놓고 한 회전 주기 전체를
  연속 프레임으로 대조합니다.
- 오른팔 총신 전체가 빠짐없이 하나의 케이지로 회전해야 합니다.
- 모든 총신의 길이 방향, 사이 간격, 상대 자세와 앞뒤 깊이가 회전
  전 구간에서 유지되어야 합니다.
- 원반과 허브는 고정되고 총신 케이지는 원반 중심과 동심으로
  회전해야 합니다.
- 대표 각도와 모든 중간 각도에서 총신 및 연결 캡이 오른팔 원반
  범위를 벗어나면 안 됩니다.
- 총신 끝이 원뿔 또는 타원 궤적을 만들거나 회전 각도에 따라
  대각선으로 기울면 실패입니다.
- 개별 총신이 따로 미끄러지거나 회전 방향을 상쇄하면 실패입니다.
- 섬광은 고정 발사 위치에 머물러야 하며 총신과 함께 공전하면
  실패입니다.
- 위 조건을 만족하더라도 정상 왼팔에 변화가 생기면 실패입니다.

### 다음 작업의 중단 조건

- 오른팔 총신의 완전한 구성요소 경계, 원반 삽입점 또는 고정 하우징
  경계를 영상과 형상에서 확정할 수 없으면 추측해서 선택하지 않고
  사용자에게 확인합니다.
- 정적 평행 정렬과 단일 축 대표 자세가 정상임을 직접 확인하기 전에는
  공격 클립 재생성과 씬 최종 반영으로 넘어가지 않습니다.
- 이전 실패와 같은 기울어짐이 한 번이라도 보이면 역회전을 다시
  추가하지 않고, 해당 총신의 정적 축 정렬과 공통 축부터 재검토합니다.

## 완료 - Revolution_04 오른팔 총신 단일 케이지 재구성

### 구조 변경

- 7월 29일 확정 계획에 따라 오른팔의
  `Revolution_Right_MachineGun_BarrelGroup_01~05` 자식 계층과
  개별 역회전 애니메이션 커브를 제거했습니다.
- 오른팔에서 선별된 10개 연결 성분·80개 정점을 다섯 총신 단위로
  유지하면서, 각 총신의 뒤쪽 원반 삽입점을 고정 기준으로 잡았습니다.
- 각 총신 길이축을 오른팔 자체 원반의 공통 축과 평행하게 정렬하고,
  오른팔 삽입점들이 만드는 고유 링 중심과 평균 반경에 배치했습니다.
  정상 왼팔의 반경 수치는 복사하지 않았습니다.
- 정렬과 링 배치는 실행 중 자식 Transform 보정으로 남기지 않고
  `Assets/_Project/Art/Enemies/Revolution/Animations/Revolution_04_Right_MachineGun_Barrels.asset`
  정점 배치에 한 번만 굽습니다.
- 결합 메시 하나를 `Revolution_Right_Barrel_Ring_Bone`에 직접
  연결했습니다. 공격 클립은 왼쪽·오른쪽 링 본 하나씩에만 발사당
  `72°` 인덱스 회전을 적용합니다.
- 정상 왼팔 메시·본·애니메이션·총신 배치·섬광과 팔 조준 자세는
  수정하지 않았습니다.

### 직접 시각 확인

- 최종 비교 이미지는 원본 재질 전신, 양팔 확대와 오른팔 총구축 정면
  고대비 행으로 구성했습니다.
- 오른팔 총구축 정면에서 `0°`, 중간 `36°`, `72°`, `144°`,
  `216°`, `288°`, `360°` 자세를 직접 확인했습니다.
- 다섯 총신은 모든 대표 각도와 중간 각도에서 동일 간격과 상대 자세를
  유지하며 하나의 케이지로 회전합니다.
- 개별 총신 미끄러짐, 역회전, 대각선 기울어짐, 원뿔·타원 궤적과
  앞뒤 출렁임은 보이지 않습니다.
- 총신 링은 오른팔 원반과 동심이며 고정 원반·허브·하우징 범위 안에
  머뭅니다.
- 원본 재질 30fps 연속 프레임에서 정상 왼팔, 팔·몸체와 다른 부품은
  고정 상태를 유지하고 오른팔 총신 케이지 전체만 회전합니다.
- 오른팔 섬광은 손 기준 고정 발사 위치에 머물며 회전체와 함께
  공전하지 않습니다.

### 최종 자료

- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_VisualReview.png`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_DirectVisualReview.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_RightCloseup.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_DirectVisualReview.txt`
- 이전 구조와 중간 구도 실패 자료는
  `Logs/RevolutionMachineGunInvalidDiagnostics/`에 격리했습니다.

### 수행하지 않은 항목

- `InspectRevolutionMachineGunAttackMotion`
- 하네스 검증
- 일반 테스트·빌드
- Unity 재시작
- Git 작업
- 정상 왼팔 및 다른 개체 수정

## 정정 완료 - Revolution_04 오른팔 총신을 왼팔과 같은 경로로 재구성

### 이전 완료 판정 무효

- 위 `10개 연결 성분·80개 정점 재정렬` 완료 판정은 무효입니다.
- 사용자 제공 `2026-07-29 10-43-30.mp4`에서 오른팔 회전체가 원반과
  팔 아래로 크게 분리되어 공전하는 현상을 직접 확인했습니다.
- 오른팔 전용 로직은 실제 좌우 공통 구성인 앞쪽 캡 2개와 긴 성분
  10개 중 앞쪽 캡을 누락했고, 나머지 성분을 왼팔에 없는 별도
  평행 정렬·반경 재배치 경로로 보냈습니다.
- 이전 총구 정면 캡처는 매 프레임 회전 메시 평균 중심을 따라가
  축 이탈을 화면 중앙에 숨겼습니다.
- 이전 직접 검토 문서와 영상은
  `Logs/RevolutionMachineGunInvalidDiagnostics/`에 격리했습니다.

### 최종 수정

- 좌우 모두 같은 `SelectBarrelComponents` 선택 경로와
  `CreateExtractedBarrelMesh` 결합 경로를 사용하도록 변경했습니다.
- 왼팔은 기존 12성분·113정점을 그대로 유지합니다.
- 오른팔 원본의 유효한 4성분은 `RightHand` 가중치가
  `0.375~0.443`이므로, 비총신 후보를 추가하지 않고 12성분 전체를
  보존하는 오른팔 선택 경계 `0.35`를 사용합니다.
- 최종 오른팔 회전체는 12성분·108정점입니다.
- 오른팔 전용 평행 정렬·삽입점 평균 링·반경 재배치는 사용하지
  않습니다. 원본 형상을 그대로 하나의 메시로 결합해
  `Revolution_Right_Barrel_Ring_Bone`에 직접 연결했습니다.
- 좌우 링 본은 같은 키 시점, 같은 발사당 `72°` 인덱스 각도, 같은
  로컬 회전축을 사용합니다. 개별 자식 역회전 커브는 없습니다.
- 정상 왼팔 메시·선택·회전·섬광과 다른 Revolution 슬롯은 수정하지
  않았습니다.

### 직접 시각 확인

- 총구 정면 카메라를 메시 평균 중심이 아닌 각 링 본의 고정 중심,
  고정 축, 고정 화면 상단 방향에 고정했습니다.
- 실제 Player 카메라 행, 원본 재질 전신·양팔 행, 왼팔 청록색 총구
  정면 행, 오른팔 자홍색 총구 정면 행을 같은 시점으로 구성했습니다.
- 고정축 좌우 나란히 30fps 영상에서 `0°`, 중간 `36°`, `72°`,
  `144°`, `216°`, `288°`, `360°`를 직접 비교했습니다.
- 좌우 모두 같은 방향·같은 각도·같은 발사 시점으로 회전하며,
  회전체 중심이 고정되고 각 성분의 상대 위치와 앞뒤 깊이가
  유지됩니다.
- 오른팔의 팔 아래 대형 공전, 원반 이탈, 성분 누락, 개별 미끄러짐,
  역회전, 세차와 타원 궤적은 보이지 않습니다.
- 원본 재질 30fps 영상에서도 오른팔 회전체가 팔에서 분리되지 않고
  왼팔과 같은 인덱스 회전을 수행합니다.

### 최종 자료

- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_VisualReview.png`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_DirectVisualReview.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_LeftRightParity.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_RightCloseup.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_DirectVisualReview.txt`

### 수행하지 않은 항목

- 하네스 검증
- 별도 자동 검사·테스트·빌드
- Unity 재시작
- Git 작업
- 정상 왼팔 및 다른 개체 수정

## 최종 재정정 - 정상 왼팔 상태와 모션을 유일한 기준으로 오른팔 재구성

### 이전 결과 무효 및 원인

- 위 `12성분·108정점 원본 형상 보존` 완료 판정은 무효입니다.
- 사용자 제공 `2026-07-29 11-15-45.mp4`에서 오른팔 총신이 앞 원반만
  따라가는 것처럼 보이면서 뒤 원반 밖으로 나가고, 정상 왼팔과 달리
  총신 길이축이 회전축에 대해 기울어진 채 회전하는 것을 확인했습니다.
- 오른팔 원본의 기울어진 총신 형상을 강체로 결합한 것만으로는 각 총신의
  길이축이 공통 회전축과 평행해지지 않아 원뿔형 궤적이 남았습니다.
- 첫 왼팔 형상 매핑 시도에서는 오른팔 후보 12성분을 모두 원본에서
  제거해, 그중 고정돼야 하는 앞·뒤 원반 2성분까지 사라지는 문제가
  있었습니다. 해당 중간 결과도 채택하지 않았습니다.

### 최종 구조

- 정상 왼팔의 현재 상태와 모션만을 정답으로 사용했습니다. 별도의
  원반 경계 수치나 자동 판정 조건을 완료 기준으로 두지 않았습니다.
- 오른팔 원본 12성분은 길이 방향 총신 10성분·80정점과 고정 원반
  2성분으로 구분했습니다.
- 원본 스킨 메시에서는 기울어진 총신 10성분만 제거하고, 앞·뒤 원반
  2성분은 기존 오른손 하우징에 고정된 상태로 유지했습니다.
- 실제 회전 표시 메시에는 정상 왼팔의 승인된 총신 케이지 형상을
  사용했습니다. 왼팔 회전 본의 월드 형상을 오른팔 앞·뒤 원반 공통축
  좌표계로 변환하고, 좌우 손 부모의 스케일·좌표계 차이까지
  `TransformPoint`/`InverseTransformPoint`로 보정했습니다.
- 오른팔 회전 본은 오른팔 자체 앞·뒤 삽입부의 공통축과 중심을
  사용합니다. 개별 총신 피벗·역회전·별도 공전 커브는 사용하지
  않습니다.
- 좌우 총신 케이지는 같은 키 시점, 같은 발사당 `72°` 회전,
  같은 로컬 위상 변화를 사용합니다. 정상 왼팔과 다른 슬롯·모델,
  공격 시간과 섬광 타이밍은 변경하지 않았습니다.

### 정상 왼팔과 직접 비교

- 원본 재질의 좌우 앞쪽 사선 연속 프레임과 뒤쪽 사선 연속 프레임을
  각각 같은 시점·같은 배율로 나란히 구성했습니다.
- 앞쪽 사선 `0·5·10·15` 프레임에서 양팔 총신의 길이 방향, 간격,
  상대 자세, 앞 원반 삽입 상태와 회전 위상이 일치하는지 직접
  확인했습니다.
- 뒤쪽 사선 `0·10·15·25` 프레임에서 오른팔 총신이 뒤 원반 중심을
  통과한 상태를 유지하며, 정상 왼팔처럼 총신 전체가 축과 평행하게
  회전하는지 직접 확인했습니다.
- 전체 회전 구간에서 오른팔 총신은 정상 왼팔과 같은 모션으로
  움직이며 앞·뒤 원반 밖으로 빠지지 않습니다. 총신의 대각선 기울기,
  원뿔형 공전, 팔 아래 이탈과 원반 소실은 보이지 않습니다.
- 이 판정은 정상 왼팔과의 화면상 동일성만으로 내렸으며 별도 검증
  범위로 축소하지 않았습니다.

### 최종 자료

- `docs/validation/revolution_machinegun_attack_2026-07-28/Revolution_04_MachineGunAttack_VisualReview.png`
- `docs/validation/revolution_machinegun_attack_2026-07-28/DirectVisualReview.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/LeftRightParity.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/LeftRightObliqueParity.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/LeftRightRearObliqueParity.mp4`
- `docs/validation/revolution_machinegun_attack_2026-07-28/RightCloseup.mp4`

### 수행하지 않은 항목

- 하네스 검증
- 별도 자동 검사·테스트·빌드
- Unity 재시작
- Git 작업
- 정상 왼팔 및 다른 개체 수정

## 완료 - 원본 기획서 기반 우주 해적 개체 목록 문서화

- `docs/GAME_DESIGN_SOURCE.txt`의 `적대 개체 - 해적` 구간을 기준으로
  우주 해적 소속 개체를 `파후르`, `쿠르사`, `이슈판트`, `아타`의
  총 4종으로 정리했습니다.
- 상세 목록은 `docs/SPACE_PIRATE_ENTITY_LIST_2026-07-29.md`에 기록했습니다.
  각 개체의 역할, 외형, 크기, 체력, 이동속도, 무장과 행동을 원문 범위
  안에서 정리했습니다.
- 원본 전체 목록의 `쿠르사스`와 개별 설명의 `쿠르사` 표기 차이는
  숨기지 않고 병기했으며, 현재 프로젝트에서 사용자 확인으로 확정된
  표준명 `쿠르사`를 사용했습니다.
- 파후르·쿠르사·이슈판트·아타 전용 침입선은 개체가 아닌 외부 수송
  수단으로 분리해 확정 개체 4종에 포함하지 않았습니다.
- Unity 실행·수정·저장, 모델링·애니메이션·게임 로직 작업, 하네스 검증
  및 관련 작업, EditMode/PlayMode 테스트, 빌드, Smoke/Validate/Ensure
  계열 명령과 Git 작업은 실행하지 않았습니다.

## 완료 - 사용자 확정 파후르 애니메이션 9개 문서화

- `docs/GAME_DESIGN_SOURCE.txt`의 파후르 단독 행동과 아타의 수호·돌파
  태세 설명을 대조해 `docs/PAHUR_ANIMATION_LIST_2026-07-29.md`를
  작성했습니다.
- 사용자 확인에 따라 파후르 애니메이션은 대기, 이동, 미니 화염방사기
  발사, 돌파태세 화염방사, 수호태세 전용 발사, 정지, 대형 전환, 피격,
  사망의 총 9개로 확정했습니다.
- 기존의 `3개 확정` 판정과 대기·피격·사망·수호태세 전용 발사·정지·
  대형 전환의 미확정 판정은 이번 사용자 확정으로 대체했습니다.
- 원문에 구체 자세가 없는 대기·정지·대형 전환·피격·사망과 수호태세
  전용 발사의 세부 형태는 목록 확정과 분리했으며, 구현 단계에서
  사용자 확인 없이 추측하지 않는 것으로 기록했습니다.
- 과거 진행문서의 `로켓 공격` 표현은 원본의 `미니 화염방사기`와
  충돌하므로 이번 애니메이션 목록의 근거로 사용하지 않았습니다.
- Unity 실행·수정·저장, 모델링·애니메이션 제작·적용, 하네스 검증 및
  관련 작업, EditMode/PlayMode 테스트, 빌드, Smoke/Validate/Ensure
  계열 명령과 Git 작업은 실행하지 않았습니다.

## 완료 - 파후르 원본 FBX 10개 Unity 배치와 정면 시작 시점

- 사용자 지정 `enemies model/pāḫḫur.fbx`를
  `Assets/_Project/Art/Enemies/Pahur/Models/Pahur.fbx`로 직접
  복사·임포트했습니다. 원본과 Unity 자산의 SHA-256은 모두
  `797D3663D6249F4924322D8487DD308F364CC5663A454D72123824C117AC66A9`
  로 일치하며 파일 크기는 `479,452바이트`입니다.
- `CargoRunMvp`에 `Approved Pahur Enemy Placement` 루트를 생성하고
  `Pahur_01_Static_Review`부터 `Pahur_10_Death`까지 사용자 확정
  애니메이션 목록에 대응하는 정적 슬롯 10개를 배치했습니다.
- 파후르 루트는 레볼루션 루트 `(57.86535, 0, -148.629)`에서 월드
  `-Z` 방향으로 롱가 아르마–테르고 간격과 같은 `7.376999m` 떨어진
  `(57.86535, 0, -156.006)`에 있습니다.
- 슬롯은 월드 `+X` 방향으로 나열했으며 각 간격은 현재 레볼루션
  `Revolution_01–02` 간격과 같은 `2.444763m`입니다.
- 각 슬롯은 공급 FBX의 직접 인스턴스 하나를 사용합니다. 모델 원본
  비율을 유지한 균일 스케일로 기획 높이 `1.5m`에 맞추고 바닥에
  접지했으며 모델·메시·리그·재질과 내장 애니메이션은 수정하지
  않았습니다. Animator와 Animation은 정적 배치를 위해 재생하지
  않습니다.
- Player는 파후르 10개 행의 중심을 정면으로 보도록
  `(68.81515, 0, -168.2432)`에 배치하고 월드 `+Z` 방향을 바라보게
  저장했습니다. 카메라는 `(68.81515, 1.62, -168.2432)`이며 전체
  파후르 행 경계가 화면 안에 들어옵니다.
- 첫 적용은 프로젝트 자산 프리팹의 `activeInHierarchy=false`를 가시성
  실패로 잘못 판정해 씬 변경 전에 중단됐습니다. 자산과 씬 인스턴스
  모두에서 활성 Renderer를 검사하도록 수정한 뒤 재적용했습니다.
- `ApplyPahurPlacement`, `InspectPahurPlacement`,
  `CapturePahurPlacementReview`를 실행했습니다. 독립 점검은 직접 FBX
  인스턴스 10개, 간격, 높이, 접지, 정적 상태, Player 정면 구도와
  다른 씬 루트 무변경을 통과했습니다.
- 최종 정면 이미지를 직접 확인한 결과 파후르 10개가 같은 높이와
  접지선으로 X축에 나열되고 모두 카메라 정면을 향합니다. 승인된
  행 전체 구도 때문에 개체 뒤의 기존 레볼루션 등 이전 배치가 슬롯
  사이로 함께 보이지만, 기존 개체는 이동·비활성화·수정하지 않았습니다.
- 검사 기록은
  `docs/validation/pahur_placement_2026-07-29/Pahur_Placement_Inspection.txt`,
  최종 이미지는 같은 폴더의 `Pahur_Placement_VisualReview.png`입니다.
- 파후르 애니메이션 제작·연결·재생, AI·전투·화염 VFX·충돌·물리,
  하네스 검증 및 관련 작업, EditMode/PlayMode 테스트, 빌드,
  Smoke/Validate/Ensure 계열 명령, Unity 재시작과 Git 작업은
  실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 현재 FBX 외형 동기화 아트 샘플

- `image/pāḫḫur(파후르).png`와 원본 기획서의 후드형 안드로이드,
  이마 불 문양, 후방 연료통, 화염방사기 기준을 분석해 현재 Unity
  배치 모델 `Assets/_Project/Art/Enemies/Pahur/Models/Pahur.fbx`의
  색·머티리얼·텍스처 샘플을
  `artSample/enemies/pahur/appearance_reference_sync/`에 제작했습니다.
- 검토 진입점은 `index.html`, 같은 Cantabile HTML 형식의 요약은
  `summary.html`입니다. 승인 상태는 `PENDING_USER_REVIEW`,
  `approved_for_unity=false`입니다.
- 남색 후드, 주황 화염 표식, 청색 광학부, 청회색 마모 장갑, 짙은
  관절·손·발, 건메탈 총신, 열변색 총구 끝, 검은 호스, 마모 강철
  연료통을 알베도·러프니스·메탈릭·미세 노멀 텍스처로 구성했습니다.
- 현재 FBX는 머티리얼이 없는 단일 스킨 메시이며 정점 `3,554`,
  면 `7,043`, 루프 `21,129`, 본 `24`, UV 레이어 `uv`입니다.
  새 메시나 부품을 만들지 않고 기존 연결 메시 섬, 폴리곤 중심,
  본 가중치 기준으로 머티리얼 인덱스만 배정했습니다.
- `GEOMETRY_PRESERVATION.json`의 소스 전후 해시 비교와
  `EXPORT_INSPECTION.json`의 원본 FBX·Blend·재내보낸 FBX 재임포트
  비교에서 정점·에지·면·루프·UV·본·본 그룹이 모두 유지되어
  `PASS`했습니다. GLB는 4개 본 영향 제한과 primitive 분할이 있는
  범용 시각 검토본으로 분리하고 Blend·FBX를 구조 보존 기준으로
  기록했습니다.
- 정면, 사선, 측면, 후면 렌더와 원화 병렬 비교 이미지를 직접
  확인했습니다. 정면에서 후드·표식·광학부·장갑·총신의 색 분리가
  보이고, 사선과 후면에서 연료통·호스 재질이 일관되게 유지됩니다.
- 원화의 정밀 판금 장갑 실루엣과 화염 분사 VFX는 모델·메시를
  변경하지 않는 이번 범위로 재현할 수 없어 HTML에 한계로
  명시했습니다. 후면은 기준 이미지가 없어 신규 문양이나 부품을
  추측하지 않고 정면 재질만 연장했습니다.
- 로컬 브라우저 표시 검증을 시도했으나 현재 브라우저 연결
  메타데이터 단계에서 탐색 전에 차단됐습니다. 대신
  `HTML_VALIDATION.json`에서 두 HTML의 제목·구역·링크·이미지 경로와
  실제 이미지 크기를 검사해 깨진 이미지 `0`, 깨진 링크 `0`으로
  `PASS`했고, 생성 렌더는 별도로 직접 확인했습니다.
- Unity 씬·프리팹·런타임 에셋·배치 개체·애니메이션·VFX는 수정하거나
  연결하지 않았습니다. 하네스 검증 및 관련 작업, EditMode/PlayMode
  테스트, 빌드, Smoke/Validate/Ensure 계열 명령, Unity 새로고침·저장,
  Git 작업도 실행하지 않았습니다.
