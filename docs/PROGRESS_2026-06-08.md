# Progress 2026-06-08

## Latest Handoff For Next Session

- Start the next session by reading `docs/PROGRESS_2026-06-02.md`, then this file.
- Detailed implementation steps 13 through 21 are complete and verified. Step 21 was implemented as balance/playtest guardrails rather than unapproved value tuning.
- Next implementation target requires user confirmation: `docs/DETAILED_IMPLEMENTATION_PLAN.md` step 22 is a multiplayer design gate only. Do not start multiplayer implementation without a separate approval.
- Today added Detailed Step 21 EditMode coverage for source-valued economy pins, hazard cadence, high-risk special-route behavior, debt/failure recovery, and repeatable early/mid/high-risk playtest scenario definitions.
- Today added `DetailedStep21BalancePlaytestHardeningEditorValidation`, bridge command `ValidateDetailedStep21BalancePlaytestHardening`, `Run-DetailedStep21BalancePlaytestHardeningSmoke.ps1`, and `Run-DetailedStep21FullSmokeSuite.ps1`.
- Today documented the 21-step validation path in `docs/HARNESS.md` and `docs/MVP_PLAYTEST_CHECKLIST.md`.
- Today fixed the association contract decision layout so `No` is left and `Yes` is right, and added Phase7 validation for that layout.
- Today adjusted the planet-stay hub layout so placeholder map marker labels no longer overlap the body text.
- Today fixed planet hub navigation: entering `Repair` from the planet hub now has a `Planet` back button that returns to the hub.
- Today restored and hardened the planet hub `Shop` route. The hub `Shop` entry is linked to the existing Phase15 `EquipmentShopController`; it is not a separate placeholder shop.
- Today fixed the shop background issue: the Phase15 shop root is now a full-screen opaque UI layer, so the cargo ship interior no longer shows behind the shop.
- Today hardened lower-phase smoke cleanup. Successful Phase10 and Phase15 play smoke runs now restore the latest Phase20 scene state so `CargoRunMvp` is not left downgraded with missing shop/presentation assets.
- Today hardened inactive UI relinking: Phase10 bootstrap now finds the inactive repair-screen `Planet` back button and also links it directly during generation.
- Current saved scene expectation: `CargoRunMvp` contains `Phase 20 Presentation Polish`, the Phase20 planet hub, and the Phase15 equipment shop with non-null planet/shop links.
- Latest validation completed: `.\scripts\Run-HarnessValidation.ps1`, `.\scripts\Run-EditModeTests.ps1`, `.\scripts\Run-DetailedStep21BalancePlaytestHardeningSmoke.ps1`, `.\scripts\Run-PlayModeTests.ps1`, `.\scripts\Run-DetailedStep21FullSmokeSuite.ps1`, `.\scripts\Build-WindowsDev.ps1`, `git diff --check`.
- `git diff --check` currently passes with CRLF warnings only.
- If Unity shows a reload/import prompt next session, that is expected after external file edits. Let Unity reload/import before running bridge smoke tests.

## Detailed Step 21 Complete Update

- Detailed implementation step 21, "Balancing and playtest hardening," is complete.
- No source-valued rewards, repair/towing costs, hazard probabilities, enemy stats, or item prices were tuned without user confirmation. The new coverage pins those values so future balancing changes surface as explicit test failures.
- Added `DetailedStep21BalancePlaytestHardeningTests` for economy pins, hazard and special-route risk pins, debt grace/final game-over/total-loss recovery, and early/mid/high-risk repeatable playtest scenario definitions.
- Added `DetailedStep21BalancePlaytestHardeningEditorValidation` and `Run-DetailedStep21BalancePlaytestHardeningSmoke.ps1`. The smoke can recover from a stale open-editor bridge by running the already-known Detailed Step 20 presentation smoke first, then retrying the new Step 21 bridge command.
- Added `Run-DetailedStep21FullSmokeSuite.ps1`, which runs the Phase 1-18 MVP smoke sweep followed by detailed steps 13 through 21 in order.
- Verification completed:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed
  - `.\scripts\Run-DetailedStep21BalancePlaytestHardeningSmoke.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Run-DetailedStep21FullSmokeSuite.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed

## Equipment Shop Fullscreen And Smoke Restore Hardening

- Confirmed the planet hub `Shop` entry is wired to the existing Phase15 `EquipmentShopController`, not a separate placeholder shop.
- Fixed the visual issue where entering the shop showed the cargo ship interior behind it: the Phase15 shop root is now a full-screen opaque UI layer instead of an 860x520 centered panel.
- Added Phase15 editor validation that the shop root covers the full screen, so this regression is caught before play.
- Hardened Phase10 planet maintenance relinking so the inactive repair-screen `Planet` back button is found and preserved. The controller is also linked directly from the newly created button during bootstrap.
- Hardened Phase10 and Phase15 play smoke cleanup so successful runs restore the latest Phase20 scene state instead of leaving `CargoRunMvp` downgraded to an older phase and disconnecting the shop.
- Verification completed: `.\scripts\Run-HarnessValidation.ps1`, `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`, `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`, `.\scripts\Run-PlayModeTests.ps1`, `.\scripts\Run-DetailedStep20PresentationSmoke.ps1`, `.\scripts\Run-EditModeTests.ps1`, `git diff --check`.

## Planet Hub Repair And Shop Return Fix

- Added a `Planet` back button to the repair/maintenance screen so entering `Repair` from the planet hub can return to the planet surface hub.
- Reconnected existing `EquipmentShopController` instances when Phase10 planet/maintenance assets are regenerated, so the planet hub `Shop` button stays enabled after Phase10 smoke/bootstrap runs.
- Added smoke coverage for `Planet Hub -> Shop -> Close -> Planet Hub` and `Planet Hub -> Repair -> Planet -> Planet Hub`.
- Verification completed: `.\scripts\Run-HarnessValidation.ps1`, `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`, `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`, `.\scripts\Run-PlayModeTests.ps1`, `.\scripts\Run-DetailedStep20PresentationSmoke.ps1`, `.\scripts\Run-EditModeTests.ps1`, `git diff --check`.

## Start Contract And Planet Hub UI Adjustment

- Swapped the association contract decision layout so `No` is on the left and `Yes` is on the right.
- Added Phase7 editor/play smoke checks that the `Yes` button is placed to the right of the `No` button.
- Adjusted the planet-stay hub body area and placeholder map marker positions so marker labels do not overlap the body text.
- Added a Phase10 planet maintenance smoke check that planet map markers do not cover the body text area.
- Verification completed: `.\scripts\Run-HarnessValidation.ps1`, `.\scripts\Run-Phase7NewGameStartSmoke.ps1`, `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`, `git diff --check`.

## 튜토리얼 스킵 전환 버그 수정

- returning-player `Skip Tutorial` 클릭 후 시작 UI가 닫히지 않고 같은 화면에 멈추던 문제를 수정했다.
- `NewGameStartFlowController.SkipTutorialForReturningPlayer()`가 `$1100` 보상과 Completed 세션을 적용한 뒤, 씬에 `PlanetStayController`가 있으면 바로 행성 체류 허브를 열고 시작 UI를 닫도록 했다.
- `PlanetStayUxPlayModeSmoke`에 스킵 후 시작 UI 비활성화와 행성 허브 표시를 검증하는 PlayMode 테스트를 추가했다.
- 검증 완료:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Run-DetailedStep20PresentationSmoke.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed

## 상세 20단계 완료 업데이트

- 상세 구현 20단계 `아트, 사운드, 연출, 레벨 완성도` 1차 완료.
- 튜토리얼/운송 정산 이후 바로 정비 화면으로 들어가던 흐름을 `PlanetStayController` 기반 행성 체류 허브로 분리했다. 이제 settlement continue는 행성 화면을 먼저 열고, 행성 화면에서 Repair / Contracts / Shop / Cargo Depot / Ship 진입점을 선택한다.
- 기존 계약 보드, 상점, 개인 화물, 업그레이드 화면은 행성 허브에서 열렸을 때 행성 허브로 돌아갈 수 있는 경로를 추가했다. 정비 화면에서 열리는 기존 경로는 기존처럼 정비 화면으로 돌아간다.
- Phase10 부트스트랩에 `Phase 20 Planet Stay Screen`을 추가하고, 지도 마커 4종(Shop, Repair, Ship, Cargo)과 행성 상태/계약/수리비/화물 정보를 표시하도록 연결했다.
- Phase15 부트스트랩에서 나중에 생성되는 `EquipmentShopController`를 행성 허브 목적지에 재연결하도록 했다.
- Phase20 프레젠테이션 부트스트랩을 추가해 조종석 전면 유리 프레임, 엔진 도넛 링, 관제실 화면 accent, 무기고 터렛 accent, 보급실 사출 경고 스트라이프, 화물칸 고정 스트랩, 저시야 통로 beacon placeholder를 씬에 생성한다.
- 사운드는 실제 음원 에셋이 아니라 기존 `ShipSignalAudioHooks`의 damage / external danger / intruder cue hook을 20단계 smoke에서 검증하는 placeholder 범위로 유지했다.
- 새 검증 경로 `.\scripts\Run-DetailedStep20PresentationSmoke.ps1`를 추가했다.
- 검증 완료:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-DetailedStep20PresentationSmoke.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Run-Phase10PlanetMaintenanceSmoke.ps1`: passed
  - `.\scripts\Run-Phase15EquipmentLoopSmoke.ps1`: passed
  - `.\scripts\Run-Phase18MvpPlaytestLoopSmoke.ps1`: passed
  - `.\scripts\Build-WindowsDev.ps1`: passed
- 검증 중 열린 에디터가 새 브리지 명령을 모르는 stale assembly 상태라 한 번 닫고 batchmode로 20단계 smoke를 실행했다. 이후 Unity 에디터를 다시 열고 Phase10/15/18 smoke를 열린 에디터 경로로 재검증했다.
- 다음 구현 대상은 원본 상세 계획 기준 21단계 이후 범위로 별도 확인이 필요하다.

## 상세 19단계 완료 업데이트

- 상세 구현 19단계 `저장, 설정, 플랫폼 경계` 완료.
- `GameSettingsState`를 추가해 해상도, 전체화면, master/music/effects volume, mouse sensitivity, high contrast UI, camera shake reduction 후보 설정을 저장 가능한 순수 상태로 분리했다.
- `SaveGameDocument` 버전 `1`과 저장 DTO를 추가했다. 저장 대상은 시작 흐름, 세션 상태, 지갑/채무, 화물선 6구역 상태, 장비/비품창고, 개인 화물, 업그레이드, 유명세, 협회 상태, 특수 의뢰 진행도/해금, 위험 해금, 현재/방문 행성 기록, 계약 목록이다.
- `SaveGameMapper`, `SaveGameSerializer`, `SaveGameMigration`을 추가해 런타임 상태와 JSON 저장 문서 사이를 변환하고, 구버전/누락 필드 저장 문서를 현재 버전으로 보정한다.
- `ISaveGameRepository`, `SaveGameService`, `InMemorySaveGameRepository`, `FileSaveGameRepository`, `PlatformCloudSaveGameRepository`를 추가했다. 테스트/로컬 파일/플랫폼 클라우드 경계를 같은 저장 서비스 계약으로 다룬다.
- `GameSessionState.Restore`와 `NewGameStartFlowState.Restore`를 추가해 저장 문서에서 세션과 시작 흐름을 재구성할 수 있게 했다.
- 튜토리얼 완료/스킵 가능 여부는 저장 프로필에 남긴다. 튜토리얼 완료 후 Play Mode를 중지했다가 다시 시작해도 실제 씬 컨트롤러는 저장된 프로필 플래그를 읽어 새 게임 흐름에서 tutorial skip을 열 수 있다.
- tutorial skip 보상은 사용자 확인 사항에 맞춰 계속 `$1100`을 지급한다.
- `NewGameStartFlowController`는 실제 Phase7 부트스트랩 씬에서 로컬 저장을 활성화한다. 코드로 생성한 테스트 컨트롤러는 기본적으로 로컬 파일 저장을 끄므로 PlayMode 테스트가 로컬 세이브 파일에 의존하지 않는다.
- `IPlatformServices`를 업적/클라우드/통계 세부 인터페이스로 확장했다:
  - `IPlatformAchievementServices`
  - `IPlatformCloudSaveServices`
  - `IPlatformStatsServices`
- `NullPlatformServices`는 새 플랫폼 경계를 메모리 기반 Null 구현으로 제공한다. 실제 Steamworks SDK, Steam Cloud, Steam 통계 연결은 아직 하지 않는다.
- 새 검증 경로 `.\scripts\Run-DetailedStep19SaveSettingsPlatformSmoke.ps1`를 추가했다.
- 새 검증:
  - `SaveGamePersistenceTests`: 저장/로드, 프로필 기반 새 게임 스킵, 버전 마이그레이션, 파일 저장소, Null 플랫폼 업적/클라우드/통계 경계 검증.
  - `DetailedStep19SaveSettingsPlatformEditorValidation`: 저장 프로필, `$1100` 스킵 보상, 설정 복구, legacy migration, 플랫폼 경계 smoke 검증.
- 다음 구현 대상은 `docs/DETAILED_IMPLEMENTATION_PLAN.md` 20단계 `아트, 사운드, 연출, 레벨 완성도`이다.

## 상세 18단계 완료 업데이트

- 상세 구현 18단계 `행성 내 화면과 UX 완성도` 완료.
- `PlanetStayModels`/`PlanetStayRules`를 추가해 행성 체류 허브를 순수 상태로 분리했다.
- 행성 지도 마커 4종을 데이터화했다:
  - Shop
  - Repair Shop
  - Ship
  - Cargo Supply Depot
- 행성 체류 허브가 수리점, 계약 사무소, 상점, 개인 화물 보급소, 함선 준비 진입 가능 여부와 출발 readiness를 계산한다.
- 정비 화면의 계약/지도 요약 문구를 `Repair / Contract Office / Shop / Cargo Depot / Ship` 루프로 정리했다.
- 계약 보드는 기존 Association/Private/Special 분리를 유지하고, Special 표시는 17단계 특수 의뢰 조건이 실제로 열렸을 때 포함되도록 연결했다.
- `GameSessionState.AcceptSpecialContract`를 추가해 행성 체류 중 특수 의뢰 수락이 세션의 활성 특수 의뢰 상태로 반영된다.
- 시작 계약서 흐름을 원본 조건에 맞춰 보강했다:
  - 자동 스크롤은 60초에 하단 도달.
  - Down 입력은 3초에 하단 도달.
  - 하단 도달 후 No는 `이미 잠정적으로 동의한 상태입니다`로 차단.
  - Ctrl+C로 스크롤을 멈춘 뒤 Ctrl+X를 눌러야 private business hidden route가 시작된다.
  - 튜토리얼 완료 이력이 있는 시작 상태에서는 tutorial skip이 가능하고, skip 시 튜토리얼 고정 보상 $1000과 수리 지원 $100을 합쳐 $1100을 지급한 뒤 post-tutorial 계약을 노출한다.
- Phase7 시작 UI 부트스트랩에 No/Skip Tutorial 버튼을 추가했고, 기존 Phase7~18 smoke들은 Yes 클릭 전에 계약서 하단 도달 helper를 호출하도록 갱신했다.
- 새 검증 경로 `.\scripts\Run-DetailedStep18PlanetUxSmoke.ps1`를 추가했다. 열린 에디터가 없을 때는 batchmode fallback으로 실행된다.
- 새 검증:
  - `PlanetStayRulesTests`: 행성 지도/시설/계약/상점 탭과 특수 의뢰 수락 연결 검증.
  - `PlanetStayUxPlayModeSmoke`: 실제 `NewGameStartFlowController`와 Unity UI Button/Text로 No 차단과 returning-player skip 버튼 상태 검증.
- 검증 완료:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `231/231`
  - `.\scripts\Run-DetailedStep18PlanetUxSmoke.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed, `8/8`
  - `git diff --check`: passed with CRLF warnings only
- 검증 중 열린 에디터가 stale bridge assembly와 stale ScriptAssemblies를 계속 사용해 새 명령/테스트를 로드하지 못했다. 메인 Unity 에디터를 종료하고 stale `Temp/UnityLockfile` 생성물을 제거한 뒤 batchmode 검증으로 새 어셈블리를 컴파일했다.
- batchmode 과정에서 생성된 `ProjectSettings/SceneTemplateSettings.json` 생성물은 18단계 범위 밖이라 정리했다.
- 다음 구현 대상은 `docs/DETAILED_IMPLEMENTATION_PLAN.md` 19단계 `저장, 설정, 플랫폼 경계`이다.

## 상세 17단계 완료 업데이트

- 상세 구현 17단계 `특수 의뢰와 해금 루프` 완료.
- `SpecialContractModels`에 특수 의뢰 종류, 조건 진행도, 특수 장비 해금 상태, 적 처치/목표 아이템 회수, 고정 루트 modifier, 도착 정산 결과 규칙을 추가했다.
- 구현된 특수 의뢰:
  - Presence Detector Unlock: 유명세 500, OrganicRich 방문 1회, Resistance chip 2개와 Revolution chip 1개 회수, 보상 $2000, presence detector 구매 해금.
  - Light Blade Unlock: 유명세 1000, VolcanicActive 방문 3회, VeryHard 완료 2회, Istante power core와 Ata control module 회수, 보상 light blade 1개와 $2500.
  - Electric Mine Unlock: 유명세 3000, CommonMineralRich/RareMineralRich 방문, Revolution 처치 3회, 창고 절반 크기 화물 내구도 55% 이상 도착, 보상 $3000과 electric mine 구매 해금.
  - Corridor Purifier Unlock: 유명세 5000, Monstrum/Dolore/Revolution/Ata 처치 1회 이상, 4분 44초 고정 침입 루트와 침입 확률 x3 modifier, 보상 corridor purifier 설치/충전 1회와 $7500.
- 특수 의뢰 실패 시 명성은 외부 `ReputationState`에 남기고, 방문/처치/회수 조건과 활성 특수 의뢰는 초기화되도록 했다.
- `GameSessionState`에 `SpecialContractProgressState`를 저장하고, 운송 도착 정산에서 특수 의뢰 완료/실패, 보너스 지급, 장비 보상 지급, 장비 구매 해금이 자동 처리되도록 연결했다.
- `EquipmentRules`에 특수 해금 장비 `LightBlade`, `ElectricMine`, `CorridorPurifier`를 추가하고, 특수 해금 상태가 있을 때만 상점 구매가 가능하도록 했다. 보상 장비는 구매품이 아니므로 `GrantItem` 경로로 지급한다.
- `EquipmentShopController`는 세션의 특수 해금 상태를 반영해 구매 가능 여부를 판정한다.
- 새 검증 경로 `.\scripts\Run-DetailedStep17SpecialContractsSmoke.ps1`를 추가했다.
- 검증 완료:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `225/225`
  - `.\scripts\Run-DetailedStep17SpecialContractsSmoke.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed, `7/7`
- 검증 중 열린 에디터가 처음에는 stale bridge assembly를 사용해 새 `ValidateDetailedStep17SpecialContracts` 명령을 모르고 실패했다. 기존 Phase13 smoke로 `AssetDatabase.Refresh()`를 유도한 뒤 재실행해 통과했다.
- Phase13 refresh/PlayMode 과정에서 발생한 `CargoRunMvp.unity`와 graybox material 재직렬화, `ProjectSettings/SceneTemplateSettings.json` 생성물은 17단계 범위 밖 변경으로 정리했다.
- 다음 구현 대상은 `docs/DETAILED_IMPLEMENTATION_PLAN.md` 18단계 `행성 내 화면과 UX 완성도`이다.

## 상세 16단계 완료 업데이트

- 상세 구현 16단계 `우주 해적 상세 구현` 완료.
- 새 순수 로직 모델 `SpacePirateModels`에 우주 해적 4종 데이터를 추가했다:
  - Pahur
  - Kurus
  - Istante
  - Ata
- 우주 해적 침입선 수치를 원본 기준으로 데이터화했다:
  - Pahur 침입선 HP 350, 4개체 탑승.
  - Kurus 침입선 HP 600, 4개체 탑승.
  - Istante 침입선 HP 900, 1개체 탑승.
  - Ata 침입선 HP 1200, 1개체 탑승.
- `SpacePirateRegion` 위험에서 생성되는 외부 목표를 `ExternalTargetType.SpacePirateBoardingCraft`로 연결했고, seed로 선택된 해적 타입의 침입선 체력을 사용한다.
- 무기고 수동 포탑 기본 피해량 `50` 기준 Ata 침입선은 24발에 파괴되며, 격추 시 hazard가 `Neutralized`된다.
- 각 개체의 대표 행동을 데이터/순수 규칙 중심으로 연결했다:
  - Pahur: 0.5초당 5 피해 로켓 공격, 1초 tick 기준 구역 내구도 10 피해.
  - Kurus: 방어 태세와 조우 시 방패 밀치기 10 피해.
  - Istante: 머스킷 60 피해, 단검 40 피해.
  - Ata: 보호/돌파 진형 지휘, 사망 시 부하 정지, 방별 방해 공작 hook.
- Ata 방해 공작 hook을 원본 지속 시간 기준으로 추가했다:
  - 동력실 출력 저하: 60초 시전, 부스터 비활성/5개 구역 정전 hook.
  - 통제실 해킹: 60초 시전, 통제실 기능 오프라인/무작위 복도 7개 폐쇄 hook.
  - 조종실: 60초 시전, 자동 조종 비활성 hook.
  - 무기실: 60초 시전, 포탑 비활성 hook.
  - 비품실/운송창고 폭탄: 45초 설치 후 해당 구역 내구도 0%.
- 세력 관계는 원본 기준으로 우주 해적끼리 `Bonded`, 외계 생명체와 `Competitive`, 화물 자유 연대 및 씨앗체와 `Hostile`, Ata 지휘 대상은 `Commanded`로 검증했다.
- 새 검증 경로 `.\scripts\Run-DetailedStep16SpacePirateSmoke.ps1`를 추가했다.
- 검증 완료:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `218/218`
  - `.\scripts\Run-DetailedStep16SpacePirateSmoke.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed, `7/7`
  - `git diff --check`: passed with CRLF warnings only
- 검증 중 열린 에디터가 처음에는 stale bridge assembly를 사용해 새 `ValidateDetailedStep16SpacePirate` 명령을 모르고 실패했다. 기존 Phase13 smoke로 `AssetDatabase.Refresh()`를 유도한 뒤 재실행해 통과했다.
- Phase13 refresh 과정에서 생긴 `CargoRunMvp.unity`/graybox material 재직렬화와 `ProjectSettings/SceneTemplateSettings.json` 생성물은 16단계 범위 밖이라 정리했다.
- 다음 구현 대상은 `docs/DETAILED_IMPLEMENTATION_PLAN.md` 17단계 `특수 의뢰와 해금 루프`이다.

## 상세 15단계 완료 업데이트

- 상세 구현 15단계 `화물 자유 연대 상세 구현` 완료.
- 새 순수 로직 모델 `CargoFreedomLeagueModels`에 화물 자유 연대 4종 데이터를 추가했다:
  - Negatif
  - Rebellion
  - Resistance
  - Revolution
- 화물 자유 연대 침입선 수치를 원본 기준으로 데이터화했다:
  - Negatif 침입선 HP 300, 5개체 x 3그룹.
  - Rebellion 침입선 HP 450, 3개체 x 2그룹.
  - Resistance 침입선 HP 700, 2개체 x 2그룹.
  - Revolution 침입선 HP 1000, 1개체 x 1그룹.
- `CargoFreedomLeagueRegion` 위험에서 생성되는 외부 목표를 `ExternalTargetType.CargoFreedomLeagueBoardingCraft`로 연결했고, 무기고 수동 포탑으로 격추 시 hazard가 `Neutralized`가 된다.
- 각 개체의 대표 행동을 데이터/순수 규칙 중심으로 연결했다:
  - Negatif: 운송 화물 피해, 저장 화물, 피격 시 공격자 재타겟, 제거 시 저장 화물 70% 회수.
  - Rebellion: 화물 보호막 설치, 보호막 이후 화물칸 구역 공격.
  - Resistance: 비품실 우선 목표, 머스켓 약탈 placeholder, 제거 시 약탈 장비와 특별 의뢰 칩 hook.
  - Revolution: 비품실 30% 이하 폭탄 설치, 10초 후 비품실 0%, 이후 다른 구역 재타겟, 특별 의뢰 칩 hook.
- 새 검증 경로 `.\scripts\Run-DetailedStep15CargoFreedomLeagueSmoke.ps1`를 추가했다.
- 검증 완료:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `211/211`
  - `.\scripts\Run-DetailedStep15CargoFreedomLeagueSmoke.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Run-Phase16HudMapAtmosphereSmoke.ps1`: passed, 최신 HUD/map 씬 상태 복원 확인
- 다음 구현 대상은 `docs/DETAILED_IMPLEMENTATION_PLAN.md` 16단계 `우주 해적 상세 구현`이다.

## 상세 14단계 완료 업데이트

- 상세 구현 14단계 `외계 생명체 상세 구현` 완료.
- 새 순수 로직 모델 `AlienLifeformModels`에 외계 생명체 7종 데이터를 추가했다:
  - Cantabile
  - Con Spirito
  - Accelerando
  - Grave
  - Smorzando
  - Ostinato
  - Dolore
- 사용자 확인에 따라 외계 생명체의 외부 침입 물체 체력은 전부 `350`으로 통일했다.
- `AlienLifeRegion` 위험에서 생성되는 외부 목표는 `ExternalTargetType.AlienLifeform`으로 분리했고, 무기고 수동 포탑 기본 피해량 `50` 기준 7발에 파괴된다.
- 각 생명체의 원본 수치와 대표 특수 행동을 데이터/placeholder 중심으로 연결했다:
  - Cantabile: 음파 정지와 기관실 공명 피해 hook.
  - Con Spirito: 비외계 세력 조우 시 돌진, 정지 후 휴식.
  - Accelerando: 시야 유지 시간에 따른 이동 속도/공격 딜레이 보간.
  - Grave: 상태 이상 피해와 지속 시간 증폭.
  - Smorzando: 액체 상태 피해 감소, 흡수 누적, 좀비 변환 placeholder.
  - Ostinato: 50% 이하 포효/광폭/회복 주기.
  - Dolore: 40% 이하 처형 판정.
- 새 검증 경로 `.\scripts\Run-DetailedStep14AlienLifeformSmoke.ps1`를 추가했다.
- 검증 완료:
  - `.\scripts\Run-HarnessValidation.ps1`: passed
  - `.\scripts\Run-EditModeTests.ps1`: passed, `203/203`
  - `.\scripts\Run-DetailedStep14AlienLifeformSmoke.ps1`: passed
  - `.\scripts\Run-PlayModeTests.ps1`: passed
  - `.\scripts\Run-Phase16HudMapAtmosphereSmoke.ps1`: passed, 최신 HUD/map 씬 상태 복원 확인
- 다음 구현 대상은 `docs/DETAILED_IMPLEMENTATION_PLAN.md` 15단계 `화물 자유 연대 상세 구현`이다.

이 문서는 2026-06-08 상세 구현 13단계 종료 시점의 handoff 기록이다. 다음 작업자는 먼저 `docs/PROGRESS_2026-06-02.md`를 읽고, 그 포인터를 따라 이 문서까지 확인한 뒤 이어서 진행한다.

## 최신 상태 요약

- 상세 구현 13단계 `씨앗체 상세 구현` 완료.
- 다음 구현 대상은 `docs/DETAILED_IMPLEMENTATION_PLAN.md`의 14단계, `외계 생명체 상세 구현`.
- 온라인/멀티플레이, Steam 로비, 실제 네트워크 기능은 아직 시작하지 않는다.
- `은폐 블랙홀`은 기존 사용자 확인대로 후반 위험 확장으로 보류한다.

## 오늘 완료한 주요 구현

### 씨앗체 8종 데이터와 공통 규칙

- `SeedIntruderKind`를 원본 기준 8종으로 확장했다:
  - `Parvum`
  - `Fuga`
  - `LongaArma`
  - `Tergo`
  - `Urzere`
  - `Societas`
  - `Monstrum`
  - `Mimesis`
- `Fuga`는 사용자 확인 명칭 `푸가` 기준으로 유지한다. `무스카`로 되돌리지 않는다.
- `SeedIntruderProfile`을 추가해 씨앗체별 체력, 이동속도, 공격 사거리, 공격 딜레이, 목표 성향, 금속/화물선 상호작용, 특수 행동 placeholder를 데이터화했다.
- `CreateSeedIntrusionForSeed`와 `TickSeedIntruder`를 추가해 Parvum 외 씨앗체도 검증용으로 생성/틱할 수 있게 했다.
- 기존 `CreateParvumIntrusion`, `CreateParvumIntrusionForSeed`, `TickParvum` API는 유지했다.
- 기존 운송 중 자동 씨앗체 발생은 Phase14/18 회귀를 피하기 위해 아직 Parvum 경로를 유지한다. 8종 자동 선택은 외형/전투 연결 단계에서 전환할 수 있다.

### 금속/화물선 상호작용

- 금속 선호 씨앗체는 `CargoMaterial.CommonMetal`, `CargoMaterial.RareMetal` 화물이 있을 때 화물 목표를 우선 선택할 수 있다.
- 금속 화물 손상 수치는 기존 침입자 화물 손상 기본값 `IntruderRules.DefaultCargoDamagePercent`를 재사용했다.
- 금속 화물이 아닌 경우에는 화물선 구역 손상 목표를 사용한다.
- 금속 섭취 중 공격받은 씨앗체를 플레이어 목표로 바꿀 수 있도록 `RetargetMetalFeederToPlayer`와 `IntruderEntityState.WithTarget`를 추가했다.

### 테르고 자세 상태

- 사용자 확인에 따라 `자세 무너짐`은 원본 7종 상태 이상에 추가하지 않았다.
- `PlayerPostureState`를 별도 자세 상태로 추가했다:
  - `Standing`
  - `KnockedDownByTergo`
  - `PinnedByTergo`
- `FirstPersonPlayerStatus`는 자세 상태가 `Standing`이 아니면 이동/질주/행동을 차단하고 이동 배율을 0으로 처리한다.
- Tergo 대표 공격 결과는 `PlayerPostureState.KnockedDownByTergo`를 반환한다.

### 미메시스 placeholder

- 사용자 확인에 따라 실제 음성 저장/흉내 기능은 구현하지 않았다.
- `Mimesis` 프로필에는 음성 흉내 기능 보유 placeholder와 주입 중 공격받을 때 `정지` 연결용 placeholder만 남겼다.

### 카탈로그와 검증 경로

- 기본 상세 카탈로그의 적대 개체 목록을 씨앗체 8종으로 확장했다.
- 신규 smoke 스크립트 `.\scripts\Run-DetailedStep13SeedEntitySmoke.ps1`를 추가했다.
- 신규 브리지 명령 `ValidateDetailedStep13SeedEntity`를 추가했다.
- 신규 editor validation `DetailedStep13SeedEntityEditorValidation`을 추가했다.

### PlayMode 테스트 브리지 수정

- generic `.\scripts\Run-PlayModeTests.ps1`의 open-editor Unity Test Runner callback timeout 문제를 수정했다.
- PlayMode 테스트 시작 전에 `EditorSceneManager.playModeStartScene`을 비워 Test Framework가 생성한 InitTestScene으로 진입하도록 했다.
- PlayMode 테스트 timeout 시 Unity Test Runner 실행을 cancel하고 PlayMode에서 빠져나오도록 해, 실패 후 editor가 깨진 실행 상태에 남지 않게 했다.
- PlayMode 도메인 리로드 후 API callback을 놓치는 경우를 대비해 `TestRunCallbackAttribute` 기반 결과 저장 fallback을 추가했다.
- 복구 결과 파일로 완료 처리되는 경우 active request 상태도 함께 정리하도록 했다.

## 검증 상태

2026-06-08 작업 종료 전 통과한 검증:

- `.\scripts\Run-HarnessValidation.ps1`: passed
- `.\scripts\Run-EditModeTests.ps1`: passed, latest `231/231`
- `.\scripts\Run-DetailedStep13SeedEntitySmoke.ps1`: passed
- `.\scripts\Run-DetailedStep14AlienLifeformSmoke.ps1`: passed
- `.\scripts\Run-DetailedStep15CargoFreedomLeagueSmoke.ps1`: passed
- `.\scripts\Run-DetailedStep16SpacePirateSmoke.ps1`: passed
- `.\scripts\Run-DetailedStep17SpecialContractsSmoke.ps1`: passed
- `.\scripts\Run-DetailedStep18PlanetUxSmoke.ps1`: passed
- `.\scripts\Run-DetailedStep19SaveSettingsPlatformSmoke.ps1`: passed
- `.\scripts\Run-Phase13IntruderFrameworkSmoke.ps1`: passed
- `.\scripts\Run-Phase14ParvumIntruderSmoke.ps1`: passed
- `.\scripts\Run-Phase1To18Smokes.ps1`: passed, Phase 1 through Phase 18 in order
- `.\scripts\Run-EditModeTests.ps1`: passed, latest `236/236`
- `.\scripts\Run-PlayModeTests.ps1`: passed, latest `8/8`
- `.\scripts\Build-WindowsDev.ps1`: passed
- `git diff --check`: passed with CRLF warnings only

주의:

- `Run-Phase13IntruderFrameworkSmoke.ps1`와 `Run-Phase14ParvumIntruderSmoke.ps1`를 병렬 실행했을 때 Phase14가 씬 컨트롤러를 찾지 못해 실패했다. Phase13이 씬을 다시 여는 동안 Phase14가 PlayMode에 들어간 병렬 실행 충돌로 판단되며, Phase14 단독 재실행은 통과했다.

## 다음 작업

다음은 상세 구현 20단계 `아트, 사운드, 연출, 레벨 완성도`이다.

20단계 시작 전 확인할 점:

- 6구역과 복도 구조를 원본 묘사에 맞춰 다듬는다.
- graybox와 placeholder를 실제 플레이 가능한 수준의 아트/사운드/연출로 단계적으로 교체한다.
- 미메시스 실제 음성 저장/흉내는 아직 사용자 확인 전이므로 임의 구현하지 않는다.
- Steam/클라우드/업적 실제 SDK 연결은 19단계에서 만든 인터페이스 뒤에 남겨두고 아직 연결하지 않는다.
