# Progress 2026-07-14

## Accelerando handoff and commit prep

- Continued from the late 2026-07-13 Accelerando work.
- Final requested correction before commit:
  - `Accelerando_03_Antenna_Strike/Accelerando_Model` was restored from `Accelerando_00_Static_Review/Accelerando_Model`.
  - The mistakenly added `Accelerando_AntennaRigDriver` scene object was removed.
  - Temporary restore bridge code was removed after the restore ran.
- Final source/scene check found no remaining references to:
  - `Accelerando_AntennaRigDriver`
  - `ValidatePreparedAccelerandoVisibleAntennaStrikeMotion`
  - `RestorePreparedAccelerandoAntennaStrikeSlotFromStaticReview`
  - `UnityRiggedSourceModelAssetPath`
  - `AntennaRigEndpointBoneNames`
- Unity editor restart was not run.
- Final `Refresh-UnityProject.ps1` completed successfully.

### Commit scope

- Include Accelerando-related implementation, art import/output assets, runtime physics script, scene placement, validation captures, and progress documents.
- Exclude unrelated dirty files for Cantabile, Con Spirito, Societas, Urzere, backups, recovery scenes, and unrelated validation outputs.

### Commands run

- `Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'`
- `git status --short`
- `Get-ChildItem -Path docs -Filter PROGRESS_*.md`
- `git diff --stat`
- `git diff`

### Not run

- Unity editor restart
- New `artSample/` generation
- New model, mesh, material, or texture work beyond existing Accelerando outputs
- `Run-HarnessValidation.ps1`
- `Run-EditModeTests.ps1`
- `Run-PlayModeTests.ps1`
- `Build-WindowsDev.ps1`

## Accelerando 안테나 공격 사슬·철퇴 수정

- 대상: `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike/Accelerando_Model`
- 사슬 물리 프록시가 실제 철퇴 메시를 따라 움직이도록 공격 상태 전용 좌·우 철퇴 메시를 분리해 연결했다.
- `Accelerando_Left_MaceHead`, `Accelerando_Right_MaceHead`가 각 사슬 끝 물리 프록시를 직접 추종한다.
- 철퇴 아래로 노출되던 `MaceSocket_Ring`의 렌더링 구성 요소를 제거하고, 물리 앵커 Transform만 유지했다.
- 공격 클립의 몸체·안테나 모션은 유지했다.
- Unity 반영 명령: `ApplyPreparedAccelerandoPhysicsAntennaStrikeMotion`
- 물리 응답 확인 명령: `ValidatePreparedAccelerandoPhysicsAntennaStrikeResponse`
- 검토 캡처 명령: `CapturePreparedAccelerandoPhysicsAntennaStrikeMotion`
- 전용 확인 결과에서 좌·우 철퇴는 물리 프록시와 최대 분리 거리 0으로 함께 움직였고, 철퇴 아래 돌출 링은 캡처에서 보이지 않았다.

### 검증 실행 정정 및 복구 조사

- 사용자의 의도는 옛 상태 덮어쓰기 위험이 있는 기존 Harness/EditMode/PlayMode/Build 검증을 이번 작업에서 실행하지 않는 것이었다.
- 이를 잘못 해석하여 다음 명령이 실행되었다.
  - `Run-HarnessValidation.ps1`: 통과했으나 프로젝트 파일을 쓰지 않고 `Logs/HarnessValidation.log`만 갱신했다.
  - `Run-EditModeTests.ps1`: 통과했지만 종료 시각인 10:02:40에 `CargoRunMvp.unity`와 `DynamicsManager.asset`이 Unity에 의해 저장·재직렬화됐다.
  - `Run-PlayModeTests.ps1`: 10개 중 6개 통과, 기존 Player/HUD 구성 및 URP 광원 로그와 관련된 4개 실패가 발생했다.
  - `Build-WindowsDev.ps1`: 사용자 중단으로 완료되지 않았다.
- `Temp/__Backupscenes`, `Assets/_Recovery`, `Backups`를 조사했으나 10:02:40 이전의 정확한 사본은 찾지 못했다.
- 10:03:36 생성된 `Temp/__Backupscenes/0.backup`에는 수정된 좌·우 MaceHead가 포함되어 있어 테스트 직전 복구본으로 사용할 수 없었다.
- 정확한 테스트 직전 사본 없이 사용자 작업을 추정 복원하지 않는 원칙에 따라 `CargoRunMvp.unity`와 `DynamicsManager.asset`은 추가로 덮어쓰거나 되돌리지 않았다.
- 이후 Harness/EditMode/PlayMode/Build와 Unity Ensure/Validate/Smoke는 사용자가 명령명과 범위를 다시 명시적으로 승인하기 전까지 실행하지 않는다.

## Accelerando 안테나 공격 전용 재검증

- 기존 Harness/EditMode/PlayMode/Build 검증은 사용하지 않았다.
- 에디터를 재시작하거나 씬을 저장하지 않고 `Refresh-UnityProject.ps1`만 실행했다.
- 전용 물리 검증 명령:
  - `ValidatePreparedAccelerandoPhysicsAntennaStrikeResponse`
  - 결과 로그: `docs/validation/accelerando_attack_motion_2026-07-14/physics_validation_recheck.log`
- 전용 캡처 명령:
  - `CapturePreparedAccelerandoPhysicsAntennaStrikeMotion`
  - 결과 로그: `docs/validation/accelerando_attack_motion_2026-07-14/capture_recheck.log`
- 물리 검증 결과:
  - Left: 180 samples, AntennaForwardRange 0.231, MaceForwardRange 0.693, Amplification 3.003, MaxVisualProxySeparation 0.
  - Right: 180 samples, AntennaForwardRange 0.231, MaceForwardRange 0.694, Amplification 3.004, MaxVisualProxySeparation 0.
- 씬 직렬화 구조에서 `Accelerando_Left_MaceSocket_Ring`과 `Accelerando_Right_MaceSocket_Ring`은 각각 Transform만 가지며 MeshRenderer/MeshFilter가 없다.
- `Accelerando_Left_MaceHead`와 `Accelerando_Right_MaceHead`는 각각 Transform, MeshRenderer, MeshFilter를 가지며 활성 상태다.
- 전면 5장과 사선 5장의 000/044/089/134/179 캡처를 비교한 결과, 사슬 각도 변화에 맞춰 철퇴의 전후 위치가 변하며 철퇴 아래 거대한 돌출 링은 나타나지 않았다. 안테나 끝의 상단 연결 링은 의도한 연결 부품으로 유지된다.
- 검증 전후 대상 런타임 코드, 전용 검증 코드, 브리지, `CargoRunMvp.unity`, 철퇴 분리 메시 자산의 SHA-256 해시가 모두 동일해 검증 과정에서 게임 파일을 덮어쓰지 않았다.

## Accelerando 정면 철퇴 휘두르기 리그 조사

- 요청 범위는 속도·가속 제어 없이 좌·우 더듬이 리그만 움직여 철퇴가 정면으로 휘둘러지는 궤적을 만드는 것이었다.
- 전용 명령 `InspectPreparedAccelerandoForwardMaceSwingAntennaRig`로 현재 공격 개체와 모델 자산의 계층을 조사했다.
- 현재 `Accelerando_03_Antenna_Strike/Accelerando_Model`은 `Accelerando_ConnectedColored_Body` 단일 정적 `MeshFilter + MeshRenderer` 몸체와 독립된 더듬이 끝 부품·사슬·철퇴 Transform으로 구성되어 있다.
- 현재 공격 개체와 적용 중인 `accelerando_antenna_tip_ring_embedded_connection_sample.glb`에는 Armature나 Bone 계층이 없다.
- 별도 `Assets/_Project/Art/Enemies/Accelerando/Models/accelerando.glb`에는 전체 몸체 `SkinnedMeshRenderer`와 `Bone_000`~`Bone_017`의 18본 리그가 존재하지만 현재 공격 개체에는 사용되지 않는다.
- 별도 리깅 모델을 적용하려면 전체 몸체 모델 또는 스킨 구조를 교체해야 하므로, 승인된 범위인 더듬이 리그 커브 수정만으로는 구현할 수 없다.
- 승인 범위 밖의 모델·씬 교체를 하지 않았으며 `Accelerando_Antenna_Strike_Attack.anim`, 컨트롤러, 런타임 물리 코드, `CargoRunMvp.unity`는 수정하지 않았다.
- 검사 로그: `docs/validation/accelerando_forward_mace_swing_2026-07-14/inspect.log`

## Accelerando 리깅 공격 모델 일치 샘플

- Unity 적용 전 모델링 승인 단계로 `artSample/enemies/accelerando/rigged_attack_model_match/`를 생성했다.
- 원본 `enemies model/accelerando.glb`의 `UniRigArmature`, `Bone_000`~`Bone_017` 18본, 18개 스킨 그룹을 유지했다.
- 현재 Unity Accelerando 머티리얼 값을 기준으로 몸체·갑각·사슬·철퇴 색과 표면 질감을 맞췄다.
- 기존 승인 모델과 같은 좌우 12링 사슬, 더듬이 끝 삽입 연결부, 분리된 좌우 철퇴 메시를 구성했다.
- 철퇴 아래에 노출되던 보이는 `MaceSocket_Ring` 오브젝트를 제거하고, 렌더되지 않는 물리 앵커만 남겼다.
- 원본 GLB의 옛 철퇴 막대 상단 캡과 하단 지지물 잔여 220면을 제거했다.
- 공격 제어 본은 오른쪽 `Bone_008/007/006`, 왼쪽 `Bone_011/010/009`로 한정했다.
- 더듬이 밖 863정점에 남아 있던 공격 본 웨이트를 제거·재정규화하여 몸체와 옛 지지 부위가 함께 끌려가지 않게 했다.
- 기능 포즈 검증에서 1,777개 더듬이 정점이 이동했고, 직접 포즈한 비공격 본은 0개였다.
- 내보낸 `accelerando_rigged_attack_model_match.glb`를 새 Blender 세션에 다시 임포트한 결과 본 18개, 스킨 그룹 18개, 전체 몸체 정점 웨이트, 좌우 12링, 좌우 분리 철퇴, 보이는 하단 소켓 링 0개가 확인됐다.
- 원본 GLB SHA-256은 `F5F5B605C66C582C0B9B0CB29433FE812EC952D8DFF7D5BE39055489E0367B69`로 작업 전후 동일했다.
- `CargoRunMvp.unity` SHA-256은 `D9018914F2A0E8FEEDA1014B3658F2C85220ADD63B32025674F3789B3D25891B`로 작업 전후 동일했다.
- Unity 씬, 프리팹, 런타임 에셋, AnimationClip, AnimatorController는 변경하지 않았다.
- Unity Refresh/Ensure/Validate/Smoke, Harness/EditMode/PlayMode/Build, 에디터 재시작은 실행하지 않았다.
- 검토 파일: `artSample/enemies/accelerando/rigged_attack_model_match/index.html`
- 범용 모델: `artSample/enemies/accelerando/rigged_attack_model_match/exports/accelerando_rigged_attack_model_match.glb`
- Blender 원본: `artSample/enemies/accelerando/rigged_attack_model_match/exports/accelerando_rigged_attack_model_match.blend`
- 상세 수치와 해시: `artSample/enemies/accelerando/rigged_attack_model_match/asset_manifest.json`
- 현재 상태는 사용자 승인 대기이며 Unity 공격 개체 교체는 별도의 적용 승인 후 진행한다.

## Accelerando 승인 리깅 모델 전체 Unity 적용

- 사용자가 `artSample/enemies/accelerando/rigged_attack_model_match/` 샘플을 승인한 뒤, 별도 Unity 적용 묶음 승인을 받아 진행했다.
- `Approved Accelerando Enemy Placement` 아래 기존 일곱 슬롯의 `Accelerando_Model`만 승인 GLB로 교체했다.
  - `Accelerando_00_Static_Review`
  - `Accelerando_01_Idle_Detect`
  - `Accelerando_02_Crawl_Accelerating`
  - `Accelerando_03_Antenna_Strike`
  - `Accelerando_04_Disabled_Reset`
  - `Accelerando_05_Hit_Recoil`
  - `Accelerando_06_Death`
- Unity 적용 모델: `Assets/_Project/Art/Enemies/Accelerando/Models/accelerando_rigged_attack_model_match.glb`
- 기존 배치 루트와 슬롯은 재생성하지 않았고, 슬롯의 위치·회전·스케일과 모델 외 직계 하위 오브젝트를 유지했다.
- 새 GLB의 축 차이는 각 `Accelerando_Model` 하위 로컬 Y 회전을 180도로 고정해 기존 게임 카메라에서 승인 샘플의 정면이 보이도록 맞췄다.
- 모든 슬롯에서 `UniRigArmature`, `Bone_000`~`Bone_017`, 스킨 몸체, 좌우 각 12링, 좌우 분리 철퇴를 확인했다.
- 보이는 하단 `MaceSocket_Ring`은 만들지 않았고, 숨김 `AntennaPhysicsAnchor`와 `MacePhysicsAnchor`만 유지했다.
- 대기·이동·공격 AnimatorController를 새 리그 경로에 다시 연결했다.
- 공격 클립은 오른쪽 `Bone_008/007/006`, 왼쪽 `Bone_011/010/009`만 직접 제어한다.
- 이동·공격 슬롯의 사슬 물리를 12링 기준으로 갱신했다. 슬롯당 동적 Rigidbody 24개, 키네마틱 Rigidbody 2개, ConfigurableJoint 24개가 저장되어 있다.
- 실행한 Unity 반영 명령:
  - `Refresh-UnityProject.ps1`
  - `ApplyApprovedAccelerandoRiggedModelToAllPlacements`
  - `ValidateApprovedAccelerandoRiggedModelAllPlacements`
  - `CaptureApprovedAccelerandoRiggedModelAllPlacements`
- 최종 전용 구조 검증은 일곱 슬롯 모두 통과했다.
- 승인 샘플 정면·사선과 Unity 정적 정면·사선 캡처를 직접 비교해 동일한 형상·실루엣·사슬/철퇴 구성과 하단 소켓 링 제거 상태를 확인했다. Unity 캡처는 조명 환경 차이로 더 어둡지만 적용 머티리얼 수치는 승인 샘플 기준과 동일하다.
- 검증 산출물: `docs/validation/accelerando_rigged_model_apply_2026-07-14/`
- `Run-HarnessValidation.ps1`, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`, `Build-WindowsDev.ps1`, Ensure/Smoke, Unity 에디터 재시작은 실행하지 않았다.

## Accelerando 전체 배치 후면 방향 전환

- 사용자 요청에 따라 `Approved Accelerando Enemy Placement` 아래 일곱 `Accelerando_Model`을 기존 정면 방향의 반대쪽으로 180도 전환했다.
- 모델 하위 로컬 Y 회전값을 기존 `180`도에서 `0`도로 고정했다.
- 배치 루트와 일곱 슬롯의 위치·회전·스케일은 변경하지 않았다.
- 대기·이동·공격 Animator, 승인 모델, 머티리얼, 리그와 물리 수치는 변경하지 않았다.
- 이동·공격 슬롯의 사슬 물리 프록시는 새 모델 방향의 위치를 기준으로 동일 설정을 재연결했다.
- 이후 승인 모델 재적용 시에도 후면 방향이 유지되도록 적용 스크립트의 기본 방향값을 `0`도로 갱신했다.
- 실행 명령:
  - `Refresh-UnityProject.ps1`
  - `RotateApprovedAccelerandoRiggedModelsToBackFacing`
  - `ValidateApprovedAccelerandoRiggedModelsBackFacing`
  - `CaptureApprovedAccelerandoRiggedModelsBackFacing`
- 전용 검증 결과 일곱 슬롯 모두 후면 방향이며, 각 슬롯의 18본·좌우 12링·분리 철퇴·하단 소켓 링 제거 상태가 유지됐다.
- 이동·공격 슬롯 각각 동적 Rigidbody 24개, 키네마틱 Rigidbody 2개, ConfigurableJoint 24개 구성이 유지됐다.
- 검토 캡처:
  - `docs/validation/accelerando_rigged_model_apply_2026-07-14/Unity_Accelerando_BackFacing_Static_Front.png`
  - `docs/validation/accelerando_rigged_model_apply_2026-07-14/Unity_Accelerando_BackFacing_Static_Oblique.png`
- Harness/EditMode/PlayMode/Build/Ensure/Smoke, Unity 에디터 재시작, Git 커밋·푸시는 실행하지 않았다.

## Accelerando 과감한 정면 철퇴 타격 및 오른쪽 더듬이 고정점 제거

- 대상은 `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike` 공격 슬롯 하나로 한정했다.
- 공격 회전 폭을 기존 `28/18/8`도에서 `40/28/14`도로 확대해 더듬이의 준비·전방 릴리스 움직임을 과감하게 만들었다.
- 직접 애니메이션한 본은 기존과 동일하게 오른쪽 `Bone_008/007/006`, 왼쪽 `Bone_011/010/009` 여섯 개뿐이다.
- 오른쪽 메시 고정 원인을 전용 검사한 결과, 같은 위치에서 나란히 시작하는 공격 체인 `Bone_008/007/006`과 고정 병렬 체인 `Bone_014/013/012`이 확인됐다.
- 이전 보정 메시의 오른쪽 후보 887개 중 713개가 10% 넘는 비공격 본 영향을 받았고, 가장 심한 정점은 고정 병렬 본에 약 98.3% 묶여 있었다.
- 고정 병렬 웨이트를 대응 공격 본으로 합쳤다: `014→008`, `013→007`, `012→006`.
- 왼쪽 기준 웨이트의 병렬 체인 `017/016/015`도 오른쪽 공격 체인 `008/007/006`으로 대응시켰으며 기존 오른쪽 웨이트 12% 재혼합을 제거했다.
- 최종 검사에서 고정 병렬/왼쪽 본 영향 정점 0개, 릴리스에서 거의 고정된 정점 0개, 최소 릴리스 이동량 `0.030094`를 확인했다.
- 중립 정점, 삼각형, UV, 머티리얼, 텍스처와 다른 여섯 아첼레란도 슬롯은 변경하지 않았다.
- 기존 `AccelerandoChainPhysicsRig`, Rigidbody, ConfigurableJoint, 좌우 12링과 철퇴 물리 파라미터는 변경하지 않았다.
- 전용 물리 검증 결과:
  - Left: 더듬이 전방 타격 속도 `18.871`, 철퇴 전방 최대 이동 `1.051`, 복귀 역방향 초과 이동 `0.033`, 프록시 분리 `0`.
  - Right: 더듬이 전방 타격 속도 `18.423`, 철퇴 전방 최대 이동 `1.609`, 복귀 역방향 초과 이동 `0`, 프록시 분리 `0`.
  - 이전 대비 철퇴 전방 최대 이동은 왼쪽 약 36%, 오른쪽 약 63% 증가했다.
  - 이전 대비 더듬이 전방 타격 속도는 왼쪽 약 49%, 오른쪽 약 47% 증가했다.
- 오른쪽 더듬이 1,224개 삼각형 변형 표본에서 붕괴 0개, 과신장 0개이며 중립 최대 정점 차이는 `0`이다.
- 전체 모션 14장과 오른쪽 더듬이 근접 캡처 8장을 육안 비교해 특정 위치 고정, 메시 찢어짐, 비정상 과신장이 없는 것을 확인했다.
- 실행한 Unity 명령:
  - `Refresh-UnityProject.ps1`
  - `InspectApprovedAccelerandoAttackAntennaSkinConstraints`
  - `ApplyApprovedAccelerandoForwardMaceStrikeMotion`
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion`
  - `CaptureApprovedAccelerandoForwardMaceStrikeMotion`
- 검증 산출물: `docs/validation/accelerando_forward_mace_strike_aggressive_2026-07-14/`
- Harness/EditMode/PlayMode/Build/Ensure/Smoke, Unity 에디터 재시작, Git 커밋·푸시는 실행하지 않았다.

## Accelerando 공격 슬롯 정면 철퇴 타격 모션

- 대상은 `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike` 한 슬롯으로 한정했다.
- 현재 아첼레란도가 바라보는 방향을 정면 기준으로 사용해 더듬이 공격 커브를 수정했다.
- 좌우 더듬이를 정면으로 당기는 준비 동작 뒤, 타격 구간에서 짧게 반대 방향으로 스냅해 사슬의 관성과 철퇴 질량이 정면으로 넘어가게 구성했다.
- 직접 애니메이션한 본은 오른쪽 `Bone_008/007/006`, 왼쪽 `Bone_011/010/009`뿐이다.
- 사슬, 철퇴, 숨김 물리 앵커에는 AnimationClip Transform 커브를 추가하지 않았다.
- 기존 `AntennaPhysicsAnchor → Rigidbody/ConfigurableJoint 사슬 → MaceHead` 물리 연결과 좌우 12링 구성을 유지했다.
- 공격 클립 길이와 Animator 속도는 변경하지 않았다. 공격 속도 가속 로직은 이번 작업에 포함하지 않았다.
- 전용 물리 검증 결과:
  - Left: AntennaForwardRange `0.756`, MaceForwardRange `1.313`, MaxMaceForwardOffset `0.577`, Amplification `1.737`, MaxVisualProxySeparation `0`.
  - Right: AntennaForwardRange `0.790`, MaceForwardRange `2.217`, MaxMaceForwardOffset `0.978`, Amplification `2.807`, MaxVisualProxySeparation `0`.
  - 양쪽 철퇴 모두 현재 정면 방향으로 더듬이 앵커보다 크게 넘어갔고 물리 프록시와 분리되지 않았다.
- 실행 명령:
  - `Refresh-UnityProject.ps1`
  - `ApplyApprovedAccelerandoForwardMaceStrikeMotion`
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion`
  - `CaptureApprovedAccelerandoForwardMaceStrikeMotion`
- 검증 산출물: `docs/validation/accelerando_forward_mace_strike_2026-07-14/`
- 단계별 정면·사선 캡처는 중립, 준비, 해제, 정면 스윙, 회수, 중립 복귀 여섯 상태로 저장했다.
- 다른 여섯 아첼레란도 슬롯, 모델링, 머티리얼, 텍스처, Player, 카메라, 조명, HUD는 수정하지 않았다.
- Harness/EditMode/PlayMode/Build/Ensure/Smoke, Unity 에디터 재시작, Git 커밋·푸시는 실행하지 않았다.

## Accelerando 정면 타격 힘 균형 및 오른쪽 더듬이 스킨 보정

- 대상은 `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike` 공격 슬롯 하나로 한정했다.
- 기존 커브가 전방 벡터를 증가시키는 리그 회전을 준비 동작에 사용하고 빠른 릴리스에서 반대쪽으로 보내고 있던 방향 오류를 수정했다.
- 전방 타격 반대쪽의 55% 각도로 준비한 뒤 `0.32 → 0.44`초 사이에 계산된 전방 리그 자세로 빠르게 전환한다.
- 공격 클립은 `2.40`초 루프로 조정했고, 전방 자세 유지 뒤 마지막 `1.20`초 동안 느슨하게 중립 자세로 복귀한다.
- 직접 애니메이션한 본은 오른쪽 `Bone_008/007/006`, 왼쪽 `Bone_011/010/009`뿐이며 사슬·철퇴 Transform 커브는 0개다.
- 기존 `AccelerandoChainPhysicsRig`, Rigidbody, ConfigurableJoint, 좌우 12링, 철퇴 물리 프록시의 설정과 런타임 코드는 변경하지 않았다.
- 오른쪽 더듬이 메시 깨짐을 막기 위해 승인 GLB의 왼쪽 더듬이 인접 정점 4개를 거리 가중 보간해 오른쪽 더듬이 887개 정점의 스킨 웨이트를 보정했다.
- 공격 슬롯 전용 보정 메시를 `Assets/_Project/Art/Enemies/Accelerando/Models/accelerando_forward_strike_deformation_fixed_body.asset`로 저장했다.
- 보정 메시의 중립 정점, 삼각형, 머티리얼, 텍스처는 승인 GLB와 동일하게 유지했다. 다른 여섯 아첼레란도 슬롯은 승인 GLB 원본 메시를 계속 사용한다.
- 전용 검증 결과:
  - Left: 전방/복귀 더듬이 속도 `12.695 / 0.431`, 속도비 `29.439`, 철퇴 전방 최대 이동 `0.774`, 복귀 역방향 초과 이동 `0`, 프록시 분리 `0`.
  - Right: 전방/복귀 더듬이 속도 `12.564 / 0.439`, 속도비 `28.589`, 철퇴 전방 최대 이동 `0.990`, 복귀 역방향 초과 이동 `0`, 프록시 분리 `0`.
  - 중립 최대 정점 차이 `0`, 스킨 웨이트 합 최대 오차 `0.00000012`.
  - 오른쪽 더듬이 1,224개 삼각형 변형 표본 중 과신장 0개이며 변형 검증 `PASS`.
- 최종 정면·사선 캡처에서 준비, 전방 릴리스, 전방 구동, 전방 유지, 느슨한 복귀, 중립 복귀 흐름과 오른쪽 더듬이의 연속적인 변형을 확인했다.
- 실행한 Unity 명령:
  - `Refresh-UnityProject.ps1`
  - `ApplyApprovedAccelerandoForwardMaceStrikeMotion`
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion`
  - `CaptureApprovedAccelerandoForwardMaceStrikeMotion`
- 검증 산출물: `docs/validation/accelerando_forward_mace_strike_balance_2026-07-14/`
- Harness/EditMode/PlayMode/Build/Ensure/Smoke, Unity 에디터 재시작, Git 커밋·푸시는 실행하지 않았다.

## 최신 적용 상태

- 현재 최종 상태는 위의 `Accelerando 과감한 정면 철퇴 타격 및 오른쪽 더듬이 고정점 제거` 항목이다.
- 과감한 공격 모션과 고정 병렬 체인 웨이트 제거가 Unity 공격 슬롯에 적용됐으며 전용 물리·변형 검증을 통과했다.
- 최종 검증 및 근접 캡처: `docs/validation/accelerando_forward_mace_strike_aggressive_2026-07-14/`

## Accelerando 공격 더듬이 메시 과신장 영상 분석 및 수정 계획

- 분석 영상: `C:/Users/gus68/Videos/Captures/Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1)_ _DX12_ 2026-07-14 14-33-54.mp4`
- 2.00~2.75초 구간에서 화면 왼쪽 더듬이 메시가 길고 얇은 띠처럼 늘어나는 현상을 확인했다.
- 현재 보정 코드는 양쪽 후보를 수집하지만 리그 `Right` 후보에만 보정 웨이트를 적용한다.
- 반대쪽 리그 `Left`에는 공격 체인 `Bone_011/010/009`과 함께 고정 평행 체인 `Bone_017/016/015` 영향이 남아, 큰 공격 각도에서 같은 메시가 서로 다른 두 체인 사이로 잡아당겨지는 것으로 분석했다.
- 기존 변형 검증과 근접 캡처도 `Bone_008/007/006` 쪽만 검사해 반대쪽 결함을 판정 범위에서 놓쳤다.
- 수정 계획은 현재 `40/28/14`도 공격 커브와 사슬·철퇴 물리를 그대로 유지하고, 공격 슬롯 전용 메시의 반대쪽 평행 체인 웨이트를 `017→011`, `016→010`, `015→009`로 합친 뒤 양쪽 변형을 독립 검증하는 것이다.
- 분석 및 상세 계획: `docs/validation/accelerando_antenna_stretch_video_analysis_2026-07-14/analysis.md`
- 이번 단계에서는 진단 이미지와 문서만 생성했으며 Unity 코드·씬·모델·메시·애니메이션은 수정하지 않았다.

## Accelerando 공격 더듬이 양쪽 스킨 보정 적용

- 대상은 `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike` 공격 슬롯 하나로 한정했다.
- 사용자가 만족한 공격 클립의 `40/28/14`도 회전, 빠른 전방 타격, 느슨한 복귀 타이밍은 변경하지 않았다.
- 리그 Left 고정 평행 체인 웨이트를 `017→011`, `016→010`, `015→009`로 병합했다.
- 좌우 좌표 필터 밖에 걸친 뿌리 정점도 고정 평행 체인 영향 자체로 찾아 처리해 잔여 영향 19개를 제거했다.
- 리그 Right의 기존 좌우 반사 보정은 유지했으며 공격 전용 메시를 승인 원본에서 재생성했다.
- Left 1,578개, Right 1,141개 정점을 보정했다. 중립 정점 위치 변경은 0이고 최대 웨이트 합 오차는 `0.00000012`다.
- 양쪽 변형 검증 결과:
  - Right: 고정 체인 잔여 0, 거의 고정된 정점 0, 붕괴 0, 과신장 0.
  - Left: 고정 체인 잔여 0, 거의 고정된 정점 0, 붕괴 0, 과신장 0.
- 공격 더듬이 전방 속도는 Left `18.871`, Right `18.423`으로 유지됐고 물리 프록시 분리는 양쪽 모두 0이다.
- 좌우 근접 정면·사선 캡처에서 릴리스·전방 구동 중 더듬이 폭과 곡률이 유지되며 기존의 길고 얇은 띠 형태가 나타나지 않는 것을 확인했다.
- 사슬·철퇴 Transform 커브는 0개이며 기존 Rigidbody, ConfigurableJoint와 런타임 물리 코드는 변경하지 않았다.
- 실행한 Unity 명령:
  - `Refresh-UnityProject.ps1`
  - `InspectApprovedAccelerandoAttackAntennaSkinConstraints`
  - `ApplyApprovedAccelerandoForwardMaceStrikeMotion`
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion`
  - `CaptureApprovedAccelerandoForwardMaceStrikeMotion`
- 검증 산출물: `docs/validation/accelerando_forward_mace_strike_bilateral_skin_fix_2026-07-14/`
- Harness/EditMode/PlayMode/Build/Ensure/Smoke, Unity 에디터 재시작, 다른 아첼레란도 슬롯 수정, Git 작업은 실행하지 않았다.

## 최신 적용 상태

- 현재 최종 상태는 위의 `Accelerando 공격 더듬이 양쪽 스킨 보정 적용` 항목이다.
- 과감한 정면 철퇴 공격 모션은 유지하면서 양쪽 고정 평행 체인 웨이트가 제거됐다.
- 최종 전용 물리·변형 검증은 통과했으며 좌우 근접 캡처를 완료했다.

## Accelerando 좌우 더듬이 대칭 및 사슬 연속성 수정

- 대상은 `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike` 공격 슬롯 하나로 한정했다.
- 사용자 영상 `2026-07-14 15-44-26.mp4`에서 공격 중 좌우 더듬이 각도 차이와 반동 중 고리가 분리되어 보이는 구간을 확인했다.
- 왼쪽 `Bone_011/010/009` 공격 커브는 유지하고 오른쪽 `Bone_008/007/006`을 왼쪽의 좌우 대칭 위치·끝 방향으로 생성했다.
- 오른쪽 관절은 Euler 90도 부근의 축 전환을 피하도록 Quaternion 회전 커브로 저장했다.
- 공격 슬롯의 기존 12개 사슬 링크를 모두 활성화하고, 길이 `1.368073`의 아래로 처진 연결 곡선에 중심 간격 `0.124370`으로 등간격 배치했다.
- 공격 전용 ConfigurableJoint의 선형 이동을 잠그고 회전은 자유롭게 유지했다.
- 조인트 계산 뒤 Rigidbody 구간 길이를 투영해 방사 방향 분리를 제거하고, 접선 방향 속도와 회전 흔들림은 유지했다.
- 더듬이 전진 구간에만 사슬·철퇴 전방 물리 가속을 적용해 기존의 힘찬 정면 타격 범위를 유지했다.
- 최종 전용 검증 결과:
  - 좌우 더듬이 최대 위치 대칭 오차 `0.035242`, 최대 구간 각도 차이 `0도`.
  - 사슬 최대 구간 늘어남 Left/Right 모두 `0.000004`, 허용값 `0.008`.
  - 철퇴 시각 메시와 물리 프록시 분리 Left/Right 모두 `0`.
  - 철퇴 전방 이동 범위 Left `2.274`, Right `2.231`; 증폭 Left `3.143`, Right `3.326`.
  - 양쪽 더듬이 붕괴 삼각형 `0`, 과신장 삼각형 `0`, 중립 정점 변화 `0`.
- 최종 캡처에서 중립·전방 구동·느슨한 복귀 모두 더듬이 끝부터 철퇴까지 고리가 이어지고 단독으로 떨어진 링크가 없는 것을 확인했다.
- 수정 파일:
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Art/Enemies/Accelerando/Animations/Accelerando_Antenna_Strike_Attack.anim`
  - `Assets/_Project/Art/Enemies/Accelerando/Controllers/Accelerando_Antenna_Strike_Attack.controller`
  - `Assets/_Project/Art/Enemies/Accelerando/Models/accelerando_forward_strike_deformation_fixed_body.asset`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
- 실행한 Unity 명령:
  - `Refresh-UnityProject.ps1`
  - `ApplyApprovedAccelerandoForwardMaceStrikeMotion`
  - `InspectApprovedAccelerandoAttackAntennaSkinConstraints`
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion`
  - `CaptureApprovedAccelerandoForwardMaceStrikeMotion`
- 검증 산출물: `docs/validation/accelerando_bilateral_antenna_sync_chain_continuity_2026-07-14/`
- Unity 에디터 재시작, Harness/EditMode/PlayMode/Build/Ensure/Smoke, 다른 아첼레란도 슬롯 수정, Git 작업은 실행하지 않았다.

## 최신 적용 상태

- 현재 최종 상태는 위의 `Accelerando 좌우 더듬이 대칭 및 사슬 연속성 수정` 항목이다.
- 왼쪽 기준 좌우 대칭 더듬이 모션과 연결된 12링크 물리 사슬이 공격 슬롯에 적용됐다.
- 전용 물리·대칭·양쪽 메시 변형 검증은 통과했으며 최종 근접 캡처를 완료했다.

## Accelerando 더듬이 구동 철퇴 후속 운동 수정

- 대상은 `Approved Accelerando Enemy Placement/Accelerando_03_Antenna_Strike` 공격 슬롯 하나로 한정했다.
- 사용자 영상 `2026-07-14 21-27-19.mp4`를 10fps 프레임과 공격 주기 접촉시트로 분석했다.
- 기존 공격 물리는 더듬이의 전방 오프셋이 유지되는 동안 모든 고리와 철퇴에 별도 전방 가속을 적용했고, 철퇴에는 고리보다 큰 `4.5` 배율을 사용해 사슬에 끌리기보다 독립적으로 추진되는 모습이 발생했다.
- 공격 모드의 `chainRestSpring`, `chainRestDamper`, `maceRestSpring`, `maceRestDamper`, `crawlInertiaScale`을 모두 `0`으로 바꾸고 별도 전방 도달 가속 코드를 제거했다.
- 현재 공격 철퇴는 `더듬이 물리 앵커 → 첫 키네마틱 고리 → ConfigurableJoint 12링크 → 철퇴 Rigidbody` 순서로만 힘을 전달받는다.
- 공격용 고리 감쇠는 선형 `0.55`, 각 `1.00`, 철퇴 감쇠는 선형 `0.30`, 각 `0.62`로 조정했다.
- 기존 더듬이 공격 커브, 좌우 대칭 자세, 메시 웨이트, 모델링, 머티리얼, 12링크 연결 길이 투영은 유지했다.
- 역사상 최대 도달 거리만 강제하던 검증을 제거하고 다음 전용 기준을 추가했다.
  - 직접 follower spring/damper/inertia가 0인지 확인
  - 철퇴 최고 반응이 더듬이 릴리스 시작 이후 발생하는지 확인
  - 더듬이가 거의 정지한 뒤에도 철퇴 관성이 남는지 확인
  - 전방 이동 증폭, 시각 프록시 일치, 12링크 연속성 유지
- 두 차례 연속 최종 검증 결과:
  - 철퇴 릴리스 반응 지연: Left `0.091~0.224초`, Right `0.569~0.780초`.
  - 더듬이 정지 구간 철퇴 잔류 속도: Left `28.346~28.685`, Right `45.056~45.398`.
  - 철퇴 전방 이동 범위: Left `2.134~2.257`, Right `2.240~2.597`.
  - 사슬 최대 구간 늘어남 Left/Right 모두 `0.000004`, 시각 메시와 물리 프록시 분리 `0`.
  - 좌우 더듬이 대칭, 양쪽 메시 붕괴·과신장 0, 사슬·철퇴 Transform 커브 0 검증 통과.
- 실행한 Unity 명령:
  - `Refresh-UnityProject.ps1`
  - `ApplyApprovedAccelerandoForwardMaceStrikeMotion`
  - `ValidateApprovedAccelerandoForwardMaceStrikeMotion`
  - `CaptureApprovedAccelerandoForwardMaceStrikeMotion`
- 수정 파일:
  - `Assets/_Project/Runtime/Enemies/Accelerando/AccelerandoChainPhysicsRig.cs`
  - `Assets/_Project/Editor/Validation/AccelerandoCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`의 승인된 공격 슬롯
  - 공격 클립과 컨트롤러는 기존 더듬이 커브를 유지한 채 적용 명령으로 다시 연결했다.
- 분석·검증 산출물: `docs/validation/accelerando_antenna_driven_mace_followthrough_2026-07-14/`
- Unity 에디터 재시작, Harness/EditMode/PlayMode/Build/Ensure/Smoke, 다른 아첼레란도 슬롯 수정, 모델링·텍스처·머티리얼·웨이트 변경, Git 작업은 실행하지 않았다.

## 최신 적용 상태

- 현재 최종 상태는 위의 `Accelerando 더듬이 구동 철퇴 후속 운동 수정` 항목이다.
- 공격 철퇴의 별도 전방 추진력이 제거됐으며 더듬이와 연결된 12링크 조인트를 통해서만 힘을 전달받는다.
- 전용 검증을 두 차례 연속 통과했고 단계별 정면·사선 및 양쪽 사슬 근접 캡처를 완료했다.
