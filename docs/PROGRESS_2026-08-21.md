# 2026-08-21 진행 기록

## 이슈판트 현재 모델 3~6번 누적 작업 GitHub 반영

상태: `ISPANT_CURRENT_MODEL_MOTIONS_03_TO_06_READY_FOR_GITHUB`

- 2026-08-19의 3번 제자리 걷기와 4번 발검·장검 실시간 추종·왼팔 메시 결함 제거, 2026-08-20의 5번 Slash 상체와 Running 하체 합성·기존 장검 궤적 및 오른손 파지 복원, 아래 6번 납도·머스켓 조준 이식을 하나의 현재 장면 상태로 기록한다.
- Unity 반영 대상은 `CargoRunMvp/Approved Ispant Enemy Placement`의 3~6번 모션 개체이며, 각 작업의 상세 수정·검사 수치는 `docs/PROGRESS_2026-08-19.md`, `docs/PROGRESS_2026-08-20.md`, 본 문서와 해당 날짜 `docs/validation/` 기록을 따른다.
- GitHub 반영에는 현재 장면, 3~6번 전용 AnimationClip·AvatarMask·AnimatorController, Unity Editor 재현 도구와 브리지 명령, 장검 강체 추종 런타임 컴포넌트, 파생 몸체·머스켓 메시, Unity 내부 원본 FBX 복사본과 최종 직접 비교 자료를 포함한다.
- 브랜치는 `main`, 원격은 `origin`, 커밋 메시지는 `feat: transfer finalized Ispant motions`로 기록한다.
- 작업 범위 밖 Ata 자료, `.analysis-temp/`, `__pycache__/`, 루트 `enemies model/`의 사용자 제공 FBX, 사용자 원본 삭제 표시는 커밋에서 제외한다.

## 이슈판트 6번 기존 납도·머스켓 조준 모션 현재 모델 이식 완료

상태: `ISPANT_06_LEGACY_SHEATH_TO_RIFLE_TRANSFER_PASS`

### 적용 대상과 동작 순서

- `CargoRunMvp/Approved Ispant Enemy Placement/Ispant_06_SheathSwordDrawMusket/Ispant_New_Direct_Model` 한 개체에만 적용했다.
- 기존 확정 6번의 `장검 납도 → 0.5초 유지 → 머스켓 전환 Bridge → 등 뒤 머스켓을 꺼내 정면 조준 → 즉시 처음부터 반복` 순서를 유지했다.
- 기존·현재 네 구간 길이는 각각 `1.633333 / 0.5 / 0.8166667 / 3.516667초`로 일치한다.
- 현재 24본 중 원본과 대응하는 22본 곡선을 이식하고, 기존 최종 조준에 있던 양쪽 `Shoulder` 위치 곡선도 부모 스케일을 포함한 모델 공간으로 변환했다.
- 최종 어깨 위치 곡선 변환 최대 오차는 `0.00000021m`이며, 현재 체형 비율에 따른 마지막 양쪽 어깨 이동량은 각각 `0.1504861m`다.
- 현재 모델·머티리얼과 원본 본체 메시는 유지하고, 다른 11개 이슈판트 슬롯과 다른 장면 루트는 변경하지 않았다.

### 머스켓 강체 분리와 무기 연결

- 현재 `char1`에 합쳐져 있던 머스켓의 실제 연결 요소 11개를 진단해 하나의 강체 머스켓으로 분리했다.
- 원본 본체 `9,798`삼각형을 본체 `9,623`삼각형과 머스켓 `175`삼각형으로 정확히 분할했다. 강체 머스켓은 `150`정점이며 본체 파생 메시의 정점 배열·본 웨이트·바인드포즈·머티리얼은 원본과 동일하다.
- 등 뒤와 오른손 머스켓은 같은 강체 메시와 기존 머티리얼을 공유한다. 잡는 순간 `BackMusket OFF → HandMusket ON`으로 전환되며 두 Renderer가 중복 표시되지 않는다.
- 장검은 납도 중 현재 오른손을 따라가고 납도 뒤 골반 기준 위치를 유지한다. 머스켓은 전환 후 오른손 움직임과 기존 확정 궤적을 따라 정면을 조준한다.

### 검사와 직접 시각 대조

- 저장 장면 독립 검사: `PASS`, `SceneChanged=False`
- 장검 손잡이 최대 오차: `0.00001706m`
- BakeMesh 최대 정점 크기: `2.321313m`, 비정상·비유한 정점 없음
- 등 머스켓→손 머스켓 최대 전환 연속 오차: `0.00143522m`
- 오른손과 머스켓 표면 최대 거리: `0.00002123m`
- 마지막 왼손과 전방 지지면 거리: `0.02135442m`
- 기존 확정 머스켓 각도 궤적 최대 오차: `0°`
- 마지막 총구 정면 오차: `0°`
- 기존 최종 11구간과 현재 동일 11구간을 한 이미지의 위·아래 행으로 캡처해 직접 대조했다. 현재 모델의 다른 체형·리그 비율 안에서 납도 종료, 유지, Bridge, 등 머스켓 접근, 손 전환, 정면 조준, 루프 복귀의 순서와 대응 무기 이동이 같은 흐름이며 머스켓은 모든 구간에서 끊어지지 않는다.
- 검사 기록: `docs/validation/ispant_06_weapon_split_2026-08-21/Ispant_06_WeaponSplit_Inspection.txt`
- 직접 비교 이미지: `docs/validation/ispant_06_weapon_split_2026-08-21/Ispant_06_WeaponSplit_Comparison.png`

### 중간 실패 후보 폐기

- Humanoid `HumanPose` 기반 후보는 현재 모델이 원본과 다르게 옆으로 돌아 직접 비교에 실패했고, 등→손 머스켓 전환 연속 오차도 `0.01090241m`였다.
- 해당 후보의 생성 곡선과 장면 상태를 폐기하고, 직접 비교가 더 정확했던 본 방향 기반 리타게팅을 다시 적용·저장·검사했다. 최종 장면에는 실패 후보가 남아 있지 않다.

## 이번 범위에서 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·조사·로그·문서·생성물 작업
- EditMode·PlayMode 테스트 및 Windows 빌드
- 원본 `Ispant_New_Direct_Source.fbx` 본체 메시·본 웨이트·바인드포즈·머티리얼 수정
- 다른 이슈판트 슬롯과 배치 밖 장면 루트 수정
- AI·내비게이션·전투 판정·히트박스·물리 이동 구현
- Unity 종료·재시작
