# 진행 기록 2026-08-30

## 실행 시작 시 Stick_Carry 정면 배치

- 실행 직후 플레이어 카메라가 `Stick_Carry` 개체의 정면을 화면 중앙에서 바라보도록 `CargoRunMvp`의 플레이어 시작 위치와 방향을 변경했다.
- 기존 `Hands_Empty_Idle` 시작 정렬에 사용하던 카메라 평면 거리 `5.4m`와 중심 조준 로직을 그대로 재사용했다. 새 거리값은 만들지 않았으며 대상만 `Stick_Carry`로 바꿨다.
- 플레이어 시작 위치는 `(71.30554, 0, -205.82838)`에서 `(71.30554, 0, -213.20538)`로 이동했다. 회전과 카메라 로컬 구도는 기존 방식을 유지했다.
- 기존 `ConfigurePlayerStartFacingEmpty`는 공통 `ConfigurePlayerStartFacingTarget`을 호출하도록 정리하고, `ApplyPlayerStickCarryStartView`와 `CapturePlayerStickCarryStartViewReview` 편집기 브리지 명령을 추가했다. 기존 Empty 시작 정렬 호출은 같은 결과를 유지한다.
- 첫 적용 시 새 로그 문자열에서 정의되지 않은 `Vec(Vector3)` 호출 때문에 컴파일 오류가 발생해 씬 변경 전에 중단됐다. 새 보조 함수를 만들지 않고 기존 `Num(float)` 포맷을 재사용해 교정한 뒤 리프레시와 적용이 정상 완료됐다.
- 1순위 직접 확인에서는 실제 Unity Play Mode 시작 화면 한 장을 확인했다. `Stick_Carry`가 정면 전신으로 화면 중앙에 보였고 다른 개체가 목표를 가리지 않았다.
- 직접 확인 뒤 보조 지표에서 카메라 평면 거리는 약 `5.399994m`, 플레이어 루트와 대상 간 평면 거리는 약 `5.560413m`, 화면 중심 오차는 가로 `0`, 세로 약 `0.000000358`, 플레이어 방향 오차는 `0°`였다. 적용 위치와 Play Mode 위치가 일치했고 모든 보조 기준을 통과했다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerStickCarryStartView`, `CapturePlayerStickCarryStartViewReview`를 사용했다. 적용·검토 지표와 직접 확인 이미지는 `docs/validation/player_stick_carry_start_view_2026-08-30/`에 저장했다.
- 모델, 애니메이션 클립, 다른 플레이어 애니메이션 개체는 수정하지 않았다. 별도 테스트·빌드와 Git 작업은 실행하지 않았다.

## 모든 수송자 왼팔 미국 국기 연보라색 사각형 적용

- 사용자 직접 확인에서 최종 Unity 화면의 몸 전체 연보라색 오염이 제거되고, 수송자 왼팔 미국 국기 자리만 연보라색 사각형으로 표시되는 상태를 확인했다.
- 기존 미국 국기 크기를 기준으로 한 사각형 내부를 동일한 연보라색으로 채우고 알파 `1.0`(투명도 `0%`)을 적용했다. 사용자가 기존 모델 수정으로 지정해 `artSample/`은 제작하지 않았다.
- `player.fbx`를 공유하는 수송자 `143`개가 사용하는 `Assets/_Project/Art/Player/Textures/texture_0.png`와 `Assets/_Project/Art/Player/player.fbm/texture_0.png`를 동일하게 갱신했다.
- 최종 보조 지표는 왼팔 삼각형 `558`개와 미국 국기 시드 삼각형 `16`개를 기준으로, 팔 표면 사각 범위 약 `0.10743m × 0.08525m`, 변경 픽셀 `4,647`개, 완전 불투명, 두 텍스처 해시 일치, 전체 수송자 공유 적용 통과다.
- 첫 적용에서는 흩어진 UV 섬의 전역 최솟값·최댓값을 하나로 묶어 몸통과 장비까지 포함하는 큰 연보라색 띠가 생겼다. 이를 실패로 판정하고 원본 기준 텍스처에서 복구한 뒤, 스킨된 `LeftArm` 로컬 표면 좌표 안에서만 국기 사각 범위를 채우도록 교정했다.
- 직접 검토용 10개 수송자 자세 접촉 시트는 `docs/validation/player_transporter_left_arm_flag_rectangle_2026-08-30/final.png`, 최종 수치와 해시는 같은 폴더의 `apply_metrics.json`에 기록했다. 접촉 시트 이후 UV 이음새 채움 보정을 반영했으며, 최종 완료 판정은 보정 후 열린 Unity 화면에 대한 사용자 직접 확인을 우선 근거로 삼았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`과 `ApplyAllTransporterLeftArmFlagRectangleOpaque`를 사용했다. 별도 Harness·EditMode·PlayMode·Windows Build와 Git 작업은 실행하지 않았다.

## Stick_Carry 한손 운반 애니메이션 복제와 오른손 막대 배치

- 1순위 직접 확인에서 정면·측면·오른손 확대 구도의 8개 애니메이션 위상을 확인했다. 오른손이 막대 아래쪽 약 `1/8` 지점을 계속 잡고, 막대 긴 축은 팔과 교차하는 수직 자세를 유지했으며, 손과 막대의 분리나 몸통의 큰 관통은 보이지 않았다.
- `Hands_Carry_OneHand`의 현재 2레이어 구성을 기준으로 팔 레이어 애니메이션을 `Assets/_Project/Art/Player/Animations/Stick_Carry_OneHand.anim`에 별도 복제하고, 전용 `Stick_Carry_OneHand.controller`를 만들어 `Stick_Carry`의 `Animator`에 연결했다. 원본 컨트롤러와 원본 애니메이션은 변경하지 않았다.
- `item model/stick.fbx`를 `Assets/_Project/Art/Items/Stick/stick.fbx`에 동일 해시로 가져와 오른손 본의 `Stick_Carry_Item`으로 연결했다. 모델의 최장 로컬 축은 `LocalZ`, 실제 길이는 약 `1.054752m`이며 아래쪽에서 `12.5%` 지점을 그립 기준으로 사용했다.
- 손가락 본이 없는 `Stick_Carry` 리그 구조에 맞춰 오른손과 아래팔 본으로 손바닥 중심을 계산하고, 24개 애니메이션 위상의 평균 로컬 손바닥 지점에 그립을 정렬했다.
- 직접 확인 뒤 보조 지표에서 복제 애니메이션 76프레임의 위치·회전 차이는 모두 `0`, 최종 8위상 원본 자세 차이도 모두 `0`이었다. 손 접점 최대 오차는 약 `0.009479m`, 팔과 막대의 직각 오차는 최대 약 `2.230553°`였으며 원본/가져온 FBX SHA-256은 `59FECA1C0AADE476A59ED907FED43C828EE43654E07CB04EDCA9DF4A5D8BA664`로 일치했다.
- 적용·검토 지표와 최종 직접 확인 접촉 시트는 `docs/validation/player_stick_carry_one_hand_animation_grip_2026-08-30/`에 저장했다. Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyStickCarryOneHandAnimationAndStickGrip`, `CaptureStickCarryOneHandAnimationAndStickGrip`을 사용했다.
- 별도 Harness·EditMode·PlayMode·Windows Build와 Git 작업은 실행하지 않았다.

## Stick_Carry 오른손 손바닥 그립과 전체 재질 교정

- 사용자 실제 실행 캡처에서 이전 완료 판정이 잘못됐음을 확인했다. 막대가 손바닥이 아니라 손목 보호대에 남아 있었으므로 완료 판정을 철회하고, 기존의 오른손 로컬 `+Y` 오프셋 추정식을 제거했다.
- 교정 후 1순위 직접 확인에서 전신 정면과 오른손 정면·측면 확대의 8개 애니메이션 위상을 다시 확인했다. 막대 아랫부분이 오른손 손바닥·손가락 영역에 유지되고, 이전처럼 커프/손목 안으로 들어가거나 손에서 분리되는 모습은 보이지 않았다.
- 손바닥 기준은 `RightHand` 본에 `50%` 이상 스킨 웨이트가 실린 실제 메시 정점 `272`개의 가중 중심으로 계산한다. 손목 본에서 실제 가중 중심까지 거리는 약 `0.13203m`로, 잘못 사용했던 추정 거리 약 `0.03365m`보다 실제 장갑 손 위치에 맞게 교정됐다.
- 후속 사용자 실제 실행 캡처를 기준으로 높이·회전·애니메이션은 유지한 채 정면 화면 오른쪽 축으로만 `0.035m` 추가 이동했다. 적용 전후 그립 높이 변화는 약 `0.0000029m`, 막대 회전 변화는 `0°`였다.
- 추가 이동 후 1순위 직접 확인에서 전신 정면과 오른손 정면·측면 확대의 8개 위상을 다시 확인했다. 막대 아랫부분이 오른손 손바닥 중앙과 손가락 안쪽에 유지되고 새 관통·부유는 보이지 않았다.
- 막대는 아래쪽 `12.5%` 그립과 팔에 수직인 자세를 유지했다. 최종 보조 지표는 손바닥 표면 접촉 오차 최대 약 `0.000191m`, 계산상 관통 약 `0.000682m`, 간격 `0m`, 팔과 직각 오차 최대 약 `2.230553°`이며 8위상 원본 자세 차이는 위치·회전 모두 `0`이다.
- `stick.fbx` 내장 머티리얼 `Material.001`을 `Assets/_Project/Art/Items/Stick/Materials/Material.001.mat`으로 추출·리맵했다. 원본 `base_color.jpg`, `normal.jpg`, `texture_0_metallic.png`, `texture_0_roughness.png`를 모두 `Textures/`에 가져왔다.
- URP Lit에서 원본 메탈릭과 러프니스 채널을 함께 적용하기 위해 메탈릭 RGB와 `1-러프니스` 알파를 보존한 `texture_0_metallic_smoothness.png`를 기술적으로 패킹해 연결했다. 기본색·노멀·메탈릭·러프니스의 실제 표면 반응을 최종 이미지에서 확인했고 자동 생성된 중복 `stick.fbm` 폴더는 남기지 않았다.
- 적용·검토 지표, 가중 정점 기반 진단 이미지와 최종 직접 확인 이미지는 `docs/validation/player_stick_carry_grip_material_fix_2026-08-30/`에 저장했다. Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyStickCarryOneHandAnimationAndStickGrip`, `CaptureStickCarryGripDiagnostic`, `CaptureStickCarryOneHandAnimationAndStickGrip`을 사용했다. `artSample/`, Harness·EditMode·PlayMode·Windows Build, Git 커밋·푸시는 실행하지 않았다.
