# PROGRESS 2026-07-22

## 완료 - 돌로레 3번 공격 촉수 리깅 애니메이션

- 대상은 `Approved Dolore Enemy Placement/Dolore_04_Tentacle_Stab_Attack`이다. 사용자가 지정한 최종 구성은 기존 모델링의 촉수 시작점 위치 고정, 시작점 생성 `0.5초`, 해당 시작점부터 상승 `1.5초`, 전방 내려찍기 `0.5초`, 다시 위쪽 준비 자세로 복귀 `0.8초`, 이후 한 번 더 공격하고 복귀 중 숨긴 뒤 전체 생성부터 반복하는 구조다.
- 승인된 촉수의 내장 리그는 `Bone_000 -> Bone_012 -> ... -> Bone_001` 순서의 단일 13본 체인이다. 전용 `Intro`, `AttackLoop`, Animator Controller와 적용·점검·캡처 도구를 생성하고 대상 개체에 연결했다.
- 첫 구현은 `Bone_000`만 고정하고 나머지 12본을 사용했다. 본 루트 이동은 `0`이었지만 기존 모델링의 실제 5정점 절단 고리가 최대 `0.0468080454m` 움직여 사용자 요구에 맞지 않는 상태로 판정했다.
- 절단 고리 5정점의 스킨 가중치를 조사한 결과 `Bone_012`, `Bone_011`, `Bone_010`에만 연결되어 있었다. 이에 `Bone_000`, `Bone_012`, `Bone_011`, `Bone_010`을 고정하고 `Bone_009`부터 `Bone_001`까지 9본에 애니메이션 커브를 적용했다.
- 사용자가 제공한 실제 게임 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 10-33-20.mp4`를 대조한 결과, 이전 문서의 "촉수 메시 압축" 판정은 실제 런타임 현상이 아니었다. 격리 복제본에 `AnimationClip.SampleAnimation`을 적용한 기존 정적 캡처의 아티팩트였으며, 영상의 실제 문제는 액자 정면 출현이 없고 촉수가 위에서 옆으로 찌르는 방향이었다.
- 방향 기준을 기존 촉수 수평축에서 액자가 바라보는 바깥 방향으로 교체했다. 고정 본 다음의 첫 가동 본 `Bone_009` 로컬 위치도 자세별 바깥/위 방향으로 구동하고, Intro 확장 순서를 `Bone_001` 끝부분부터 시작점 쪽으로 뒤집어 촉수 끝부터 액자 정면에 나타나도록 했다.
- 최종 전용 수치 점검은 `Intro=2.0초(시작점 생성 0.5초 + 상승 1.5초)`, `Strike=0.5초`, `Recover=0.8초`, 촉수 끝 상승 `1.05851364m`, 바깥 방향 이동 `0.8078073m`, 낙차 `1.062072m`, 측면 이동 `0.000019208228m`, 준비 높이 `1.0585134m`, 반복 복귀 오차 `0`, 본 루트 이동 `0`, 실제 절단면 이동 `0`으로 통과했다.
- 위쪽 자세에서 움직이는 촉수의 스킨드 메시가 기존 정적 로컬 바운드 밖에서 컬링되는 문제를 발견했다. 원본 메시 바운드와 전체 핵심 자세의 13본 경로를 합친 커스텀 로컬 바운드를 대상 렌더러에 저장했고, 자동 바운드 재설정을 막아 실제 Animator 재생 전 구간에서 메시가 표시되도록 했다.
- 후속 요청에 따라 반복 구조를 `1.5초 솟아오름 -> 0.5초 1차 내려찍기 -> 0.8초 준비 자세 복귀 -> 0.5초 2차 내려찍기 -> 0.8초 복귀 중 숨김 -> 다시 솟아오름`으로 변경했다. 별도 회수 모션은 추가하지 않았다.
- `AttackLoop` 클립은 2.6초 비반복 시퀀스로 바꾸고 Controller를 `Intro -> AttackLoop -> Intro`로 연결했다. 두 번째 복귀 종료의 9개 가동 본 위치·회전은 Intro 0초의 숨김 자세와 정확히 일치한다.
- 저장 상태 검사에서 1·2차 공격은 각각 바깥 이동 `0.8078073m`, 낙차 `1.062072m`, 측면 이동 `0.000019208228m`로 일치했다. 1차 준비 자세 복귀 오차, 2차 숨김 끝점 오차, 2차 숨김 전체 본 자세 오차, 본 루트 이동, 실제 절단면 이동은 모두 `0`이다.
- 사용자 화면에서 숨김 자세의 축소된 촉수 시작점이 계속 노출되는 문제가 확인됐다. Intro 0프레임과 2차 복귀 종료에서는 촉수 `SkinnedMeshRenderer`를 끄도록 가시성 커브를 적용했다.
- 후속 실제 게임 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 11-37-16.mp4`를 대조한 결과, 기존 숨김 자세는 각 본의 로컬 위치를 단순히 `1.5%`로 축소해 액자 내부에 압축된 붉은 덩어리를 남겼고, 실제 모델링 시작점에서부터 전개되지 않았다. 첫 가동 본 `Bone_009`를 실제 시작 표면 중심에 정확히 놓고 `Bone_008`부터 `Bone_001`까지 같은 지점에 겹친 숨김 자세로 교체했다.
- 실제 게임 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 12-49-53.mp4`에서 시작점 표면이 Intro와 반복 대기 구간에 전체 크기로 계속 보이고, 첫 전개가 화면 왼쪽으로 진행되는 문제가 다시 확인됐다. 원인은 시작점 표면이 고정 본 `Bone_012~010`의 영향을 받는데 `Bone_009`만 축소했던 점과, 첫 가동 본의 목표 위치를 실제 시작 표면이 아니라 부모 본 중심에서 계산했던 점이다.
- Intro 앞부분 `0.5초`는 공격 소스 전체를 실제 모델링 시작 표면 중심에 고정한 채 `0.001배 -> 0.5배 -> 1배`로 연속 확대한다. 시작 프레임에는 Renderer도 꺼져 있어 붉은 시작점이 전혀 보이지 않고, 0.25초에는 작은 시작점이 생성 중이며 0.5초에 원래 크기가 된다. 두 번째 복귀 중에는 반대로 축소된 뒤 Renderer가 꺼진다.
- 상승 전개 순서를 `Bone_009 -> Bone_001`로 바꾸고 최초 `0.3초`에는 `Bone_009`만 위쪽으로 뻗게 했다. 준비·예비·내려찍기 위치도 모두 실제 모델링 시작 표면을 기준으로 계산해 부모 본 중심과의 `0.111m` 측면 차이를 제거했다.
- 저장 상태 검사에서 표면 중심 이동은 최대 `0.000007633119m`, Intro 숨김·루프 숨김의 가동 체인 시작점 오차는 `0.0000473775581m`, 0.5초 생성 완료 오차는 `0.000000238418579m`이다. 생성 스케일 비율은 시작 `0.001`, 중간 `0.5`, 완료 오차 `0`이며 최초 상승은 위쪽 `0.0410666466m`, 측면 `0.000000368039338m`이다.
- 실제 장면의 Animator를 플레이 모드에서 직접 재생한 최종 캡처는 16개 자세와 3개 시점으로 총 48장 생성됐다. 실제 재생에서 최초 상승 측면 이동은 `0`, 1·2차 공격은 각각 바깥 이동 `0.807808m`, 낙차 `1.062072m`, 측면 이동 `0.000058m`이고, 1차 복귀·2차 숨김·고정점 이동 오차는 모두 `0`이다.
- 실제 가시성 측정은 `IntroHiddenVisible=False`, `SourceRevealVisible=True`, `CycleHiddenVisible=False`, `RepeatRevealVisible=True`로 통과했다. 0.25초에는 시작점 생성 중, 0.5초에는 시작점 생성 완료, 이후에는 같은 지점에서 촉수 전개가 시작되며 2차 복귀 완료 뒤 다시 완전히 숨겨진다. 생성 완료·루프 숨김·반복 생성 완료 시 가동 체인 퍼짐은 모두 `0`이다.
- 최종 시트에서 완전 숨김, 0.25초 시작점 생성 중, 0.5초 생성 완료, 같은 시작점에서 촉수 출현·상승, 머리 위 준비, 1차 내려찍기, 준비 복귀, 2차 내려찍기, 복귀 중 숨김, 시작점까지 숨김 완료, 다음 반복의 0.25초·0.5초 생성과 재출현이 시간순으로 확인된다. 진단 48장과 최종 48장의 SHA-256 이미지 해시는 전부 일치한다.
- 생성 애니메이션 자산은 `Assets/_Project/Art/Generated/Enemies/Dolore/AttackAttachment/Animations/`에 있다. 수치 보고서는 `Assets/_Project/Art/Generated/Enemies/Dolore/AttackAttachment/Review/Dolore_04_TentacleStab_Inspection.txt`, 실제 Animator 최종 48장은 `Assets/_Project/Art/Generated/Enemies/Dolore/AttackAttachment/Review/Dolore_04_TentacleStab_FullMotion_Final/`에 있다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `InspectDolore04TentacleStabFullMotionTarget`, `ApplyDolore04TentacleStabFullMotion`, `InspectDolore04TentacleStabFullMotion`, `CaptureDolore04TentacleStabFullMotionDiagnostic`, `CaptureDolore04TentacleStabFullMotionFinal`이다. 바운드 적용 실패 중 남은 미저장 변경은 사용자 승인 후 `OpenCargoRunMvpScene`으로 마지막 저장 장면을 다시 열어 폐기했다.
- `Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업, 승인 범위 밖 검증·테스트·빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 후속 완료 - 고리 선행 생성과 연결된 촉수 출현 보정

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 13-24-43.mp4`와 고리 형태 참조 이미지 `1111111.png`를 기준으로 다시 판독했다. 기존 결과는 고리가 액자 오른쪽 아래에서 이동하며 나타나고, 촉수 끝이 이미 위에 있거나 고리와 분리되어 보이며, 종료 때 전체가 오른쪽 아래로 사라지는 문제가 있었다.
- `Bone_012`가 담당하는 고리 표면은 승인된 위치와 회전을 유지한다. `0.0초=0.001배`, `0.25초=0.5배`, `0.5초=1배`로 생성하면서 스킨 정점 기준 위치 보정을 적용해 실제 고리 중심의 최대 이동을 `0.00000011920929m`로 제한했다. 촉수 가동 체인은 고리가 완성되는 `0.5초` 전까지 액자 뒤에 숨긴다.
- 압축된 가동 뼈를 한 점에서 순서대로 늘리던 방식은 최종 Animator 이미지에서 고리와 촉수 끝 사이가 끊겨 보이는 결함을 만들었으므로 폐기했다. 현재 Intro는 `Bone_009`부터 `Bone_001`까지 원래 뼈 간 길이를 유지한 온전한 체인을 액자 뒤에 배치한다. `0.5초`에는 뾰족한 끝이 고리 표면에서 바깥 반대 방향으로 `0.03m` 숨고, `0.65초`에는 고리 중심에 진입하며, `0.8초`에는 바깥 `0.06m`·위 `0.10m` 지점으로 통과한다. 이후 연결된 몸통이 같은 경로를 따라 상승해 `2.0초`에 머리 위 준비 자세에 도달한다.
- 공격 루프는 `0.5초` 1차 내려찍기, `0.8초` 준비 자세 복귀, `0.5초` 2차 내려찍기, `0.8초` 복귀 중 숨김 순서다. 별도 회수 이동과 오른쪽 아래 소멸은 없다. 몸통은 두 번째 위쪽 복귀 경로에서 먼저 숨고, 고리는 승인된 위치에서 축소된 뒤 Renderer가 꺼지며 다음 반복은 고리 생성부터 시작한다.
- 최신 저장 상태 검사는 `Intro=2.0초`, `SourceGeneration=0.5초`, `Rise=1.5초`, `Strike=0.5초`, `Recover=0.8초`로 통과했다. 초기 고리 통과는 위 `0.09999955m`, 측면 편차 `0.000009488781m`; 1·2차 공격은 각각 바깥 이동 `0.8857863m`, 낙차 `1.31501877m`, 측면 이동 `0.0000167400522m`; 뾰족한 끝 방향 일치도 `0.9997272`, 선행 거리 `0.0575027466m`; 본 루트 이동 `0`이다.
- 두 번째 진단 캡처 57장과 최종 캡처 57장은 압축 체인 방식의 분리 결함을 찾는 데 사용했다. 그 이미지들은 현재 연결 체인 수정 이전 결과이므로 최신 구현의 최종 근거로 사용하지 않는다. 승인된 최종 캡처 1회와 진단 캡처 최대 2회를 모두 사용했기 때문에 연결 체인 수정 뒤 추가 캡처는 실행하지 않았고, 최신 근거는 `Dolore_04_TentacleStab_Inspection.txt`의 `Phase=Inspect`, `Result=PASS` 결과다.
- 이번 후속 작업에서도 `Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 후속 완료 - 촉수 상승 중 액자 겹침 제거

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 14-13-55.mp4`를 분석했다. 고리는 승인된 위치에서 정상적으로 생성됐지만, `0.8초` 이후에도 촉수 몸통 대부분이 액자 뒤에 남아 있어 약 `1.1~1.8초` 상승 구간이 액자에 가려졌다가 상단에서 드러나는 상태였다.
- 고리 위치·회전과 `0.5초` 생성 시간은 유지했다. 뾰족한 끝은 `0.8초`에 액자 바깥 방향 `0.25m`, 위 `0.12m` 지점으로 먼저 통과하고, `0.95초`에는 `Bone_009~Bone_001` 가동 체인 전체가 액자 앞쪽 기준 `0.30m` 평면으로 빠져나오게 했다.
- 촉수는 액자 앞쪽 평면을 유지한 채 `1.35초` 중간 상승 자세와 `2.0초` 위쪽 준비 자세로 이어진다. 준비·예비·내려찍기 자세에도 같은 앞쪽 기준을 적용해 상승 뒤 액자 깊이로 다시 들어가지 않게 했다.
- 전용 저장 상태 검사에서 고리 통과 이후 가동 뼈 전체의 실제 모델링 표면 기준 최소 앞쪽 이격거리는 `0.340871364m`다. 초기 상승은 위 `0.119999528m`, 측면 편차 `0.00001852894m`; 고리 표면 이동 `0.00000011920929m`; 본 루트 이동 `0`으로 통과했다.
- 1·2차 공격은 각각 바깥 이동 `0.8857863m`, 낙차 `1.31501877m`, 측면 이동 `0.0000167400522m`이며 기존 `0.5초` 공격·`0.8초` 복귀와 마지막 숨김 구조를 유지했다.
- 실제 Animator 진단 캡처는 등장 세부 자세를 추가한 20개 시점과 3개 카메라 방향으로 총 60장을 생성했다. 고리 생성 완료, 끝부분 통과, 전체 체인 앞쪽 이격, 초기 상승, 중간 상승, 머리 위 준비, 두 차례 공격과 종료 숨김이 확인됐고 캡처 후 Inspect도 `SceneChanged=False`, `Result=PASS`였다.
- 실행하지 않은 항목은 `Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업이다.

## 후속 완료 - 고리를 통과하는 뾰족한 끝 선행 전개

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 14-36-51.mp4`를 분석했다. 기존 결과는 고리 생성 뒤 뾰족한 끝이 고리에서 나오는 대신, 액자 아래에 접혀 있던 촉수 몸통 전체가 앞쪽·위쪽으로 함께 이동해 아래에서 솟아오르는 것처럼 보였다.
- 원인은 `0.8초` 자세에서 모든 가동 본을 동시에 굽히고 `0.95초`에 첫 가동 본 `Bone_009`를 앞쪽으로 이동시켜, 끝부분이 고리를 통과하기 전에 연결된 몸통 전체가 노출된 것이었다.
- 고리의 승인된 위치·회전과 `0.5초` 생성 시간은 변경하지 않았다. 고리가 완전히 생성된 뒤 `Bone_001` 쪽 말단부터 순차적으로 전개되도록 자세를 분리했다. `0.65초`에는 마지막 말단만 고리에 진입하고, `0.8초`에는 말단 3개 본만 고리 바깥·위쪽으로 통과하며, `1.05초`와 `1.35초`에는 말단에서 시작점 방향으로 몸통이 차례로 따라 나온다. `1.65초`에 가동 체인 전체가 액자 앞쪽 평면을 확보하고 `2.0초`에 위쪽 준비 자세에 도달한다.
- 실제 스킨 가중치를 반영한 정점 검사를 추가했다. `Bone_009~Bone_001`의 합산 가중치가 `0.75` 이상이고 액자 앞쪽에 나온 정점을 상승 구간별로 검사해, 고리보다 아래에서 선행 노출되는 가동 메시가 없음을 확인했다. 보고값 `MinimumEmergenceVisibleVertexHeight=0`은 검사 표본에서 고리 아래쪽 전방 노출이 검출되지 않았다는 뜻이다.
- 실제 Animator 진단 캡처 60장을 정면·사선·측면으로 대조했다. 고리 완성 프레임에는 촉수 몸통이 보이지 않고, 이후 고리와 같은 축에서 뾰족한 끝이 먼저 나타난 다음 연결된 몸통이 그 경로를 따라 위로 전개된다. 이전 영상의 액자 아래쪽 몸통 선행 노출은 확인되지 않았다.
- 진단 뒤 전용 저장 상태 검사는 `Result=PASS`, `SceneChanged=False`였다. 기존 수치는 `Intro=2.0초`, `SourceGeneration=0.5초`, `Rise=1.5초`, `Strike=0.5초`, `Recover=0.8초`, 가동 체인 최소 앞쪽 이격거리 `0.340871364m`, 1·2차 공격 바깥 이동 `0.8857863m`, 낙차 `1.31501877m`, 측면 이동 `0.0000167400522m`, 뾰족한 끝 방향 일치도 `0.99972713`으로 유지됐다.
- 이번 후속 작업에서도 `Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 후속 완료 - 액자 전면 이격 후 상승

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 14-51-42.mp4`를 프레임 단위로 분석했다. 기존 구현은 고리 통과와 상승을 동시에 시작하면서 끝부분의 앞쪽 이격이 `0.02m -> 0.15m -> 0.25m`로 늦게 증가해, 촉수가 액자 상단 높이에 도달할 때까지 메시가 액자 깊이를 완전히 벗어나지 못했다.
- Intro 전개를 `고리 통과 -> 높이를 거의 유지한 전면 이격 -> 상승` 순서로 분리했다. `0.8초` 고리 통과에서는 끝부분을 액자 바깥 방향 `0.18m`, 위 `0.04m`에 두고, `1.05초`에는 바깥 `0.48m`, 위 `0.08m`까지 먼저 이동한다. 이후 `1.30초`부터 상승을 시작해 `1.55초` 중간 상승, `1.78초` 전체 체인 이격을 거쳐 `2.0초` 준비 자세에 도달한다.
- 고리 통과 자세의 말단 3본 굽힘이 몸통 일부를 고리 아래로 끌어내는 원인이어서, 이 구간은 내장 리그를 액자 깊이 방향으로 곧게 펴서 통과하도록 변경했다. 시점별 전방 노출 정점 높이는 진입 `0`, 고리 통과 `0`, 전면 이격 `0.0846068859m`, 초기 상승 `0.11029768m`, 중간 상승 `0.161006808m`, 전체 이격 `0.161347866m`로 고리 아래 선행 노출이 없다.
- 전용 저장 상태 점검 결과 상승 전 끝부분의 액자 전면 이격은 `0.4799879m`, 전면 이격 완료 전 상승량은 `0.08000004m`, 상승 완료 이후 가동 체인 최소 앞쪽 이격은 `0.340871364m`다. 고리 표면 이동은 `0.00000011920929m`, 본 루트 이동은 `0`이다.
- 실제 Animator 진단 캡처 60장을 정면·사선·측면에서 대조했다. 고리가 완전히 생성된 뒤 뾰족한 끝이 고리를 통과하고, 측면에서 액자 앞쪽으로 먼저 길게 빠진 다음 그 깊이를 유지한 채 위로 상승한다. 중간·전체 상승에서도 액자 프레임과 촉수 메시의 공간상 관통은 확인되지 않았다.
- 기존 `Intro=2.0초`, 고리 생성 `0.5초`, 상승 `1.5초`, 공격 `0.5초`, 복귀 `0.8초`, 두 차례 공격 뒤 숨김 구조를 유지했다. 1·2차 공격은 각각 바깥 이동 `0.8857863m`, 낙차 `1.31501877m`, 측면 이동 `0.0000167400522m`로 변경되지 않았다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `ApplyDolore04TentacleStabFullMotion`, `InspectDolore04TentacleStabFullMotion`, `CaptureDolore04TentacleStabFullMotionDiagnostic`이다. 실패 적용 뒤 남은 장면 상태는 승인된 조건에 따라 `OpenCargoRunMvpScene`으로 마지막 저장 장면을 다시 열어 폐기했다. 진단 뒤 최종 점검은 `Result=PASS`, `SceneChanged=False`였다.
- 이번 후속 작업에서도 `Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 고리 연결을 유지한 비스듬한 전면 출구

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 15-35-35.mp4`를 프레임 단위로 분석했다. 기존 결과는 고리 통과 뒤 상승 자세의 `Bone_009` 기준 위치 전체를 끝 목표점에 맞춰 이동시켜, 촉수 몸통이 고리에서 끊긴 뒤 액자 상단 쪽에서 새로 나타나는 것처럼 액자 면을 관통했다.
- 전개 구조를 `고리 통과 -> 고리 연결 길이를 유지한 비스듬한 출구 -> 연결 상태의 초기 상승 -> 중간 상승`으로 변경했다. `1.18초`에 첫 가동 본을 고리 높이에 두고 이후 본을 액자 바깥 방향 기준 `20도`로 펼쳐, 촉수가 고리에서 액자 앞쪽으로 비스듬히 이어져 나오게 했다.
- `1.30초` 초기 상승은 본 각도 `12, 18, 26, 36, 48, 60, 74, 86도`, `1.55초` 중간 상승은 `20, 30, 42, 54, 66, 76, 84, 89도`로 연속 증가시켰다. 이전처럼 앞쪽 본 여러 개가 깊이축에서 포개져 정면에서 붉은 덩어리로 보이지 않고, 고리 가까운 본부터 끝 방향으로 연속해서 위로 휘어진다.
- 고리 연결성 점검을 추가했다. 비스듬한 출구에서 첫 가동 본과 고리 중심의 거리는 실제 본 길이 수준인 `0.119508676m`이며, 초기·중간 상승 중 전방 가동 메시의 최저 높이는 각각 `0.07454598m`, `0.07774699m`로 고리 근처 연결을 유지한다. 고리 아래 선행 노출값은 `0`이다.
- 액자 앞쪽 이격은 상승 전 `0.4799879m`, 이격 완료 전 높이 변화는 `0.08000004m`, 전체 상승 뒤 최소 앞쪽 이격은 `0.340871364m`다. 고리 표면 이동은 `0.00000011920929m`, 본 루트 이동은 `0`이다.
- 실제 Animator 진단 캡처 63장의 측면에서 고리 통과 후 앞쪽 출구와 상승 경로의 연결을 확인했다. 해당 진단에서 정면 출구가 깊이축에 눌려 보이는 점을 발견해 출구 각도와 상승 본 분배를 마지막으로 조정했다. 승인된 진단 캡처 1회를 사용했기 때문에 최종 각도 조정 뒤 추가 캡처는 실행하지 않았으며, 최종 저장 상태 근거는 `Dolore_04_TentacleStab_Inspection.txt`의 `Result=PASS`, `Phase=Inspect`, `SceneSaved=False` 결과다.
- 기존 고리 생성 `0.5초`, 전체 상승 `1.5초`, 공격 `0.5초`, 복귀 `0.8초`, 두 차례 공격 뒤 숨김 구조와 공격 수치는 유지했다. 1·2차 공격은 각각 바깥 이동 `0.8857863m`, 낙차 `1.31501877m`, 측면 이동 `0.0000167400522m`다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `ApplyDolore04TentacleStabFullMotion`, `InspectDolore04TentacleStabFullMotion`, `CaptureDolore04TentacleStabFullMotionDiagnostic`이다. 이번 후속 작업에서는 `Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처를 실행하지 않았다.

## 후속 완료 - 힘을 모아 목표를 강하게 찌르는 전방 공격

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 15-52-55.mp4`를 프레임 단위로 분석했다. 기존 공격은 `0.08초`에 예비 자세로 이동한 뒤 `0.31초` 중간 자세와 `0.5초` 타격 자세까지 거의 균등하게 이어져, 힘을 모으는 정지감과 타격 직전 가속이 없고 촉수가 아래로 접히는 인상이 강했다.
- 공격 루프의 준비 구간을 `0.16초`까지 뒤·위로 크게 말아 올리고 `0.28초`까지 유지하도록 변경했다. `0.38초`부터 전방 진행을 시작해 `0.44초`에 타격 자세의 88%까지 접근하고 `0.46초`에 충돌 자세에 도달하도록 마지막 `0.08초`에 이동을 집중했다. 충돌 자세는 `0.58초`까지 유지한 뒤 기존 `1.3초` 준비 자세 복귀 시점으로 돌아간다.
- 내장 리그 9본의 말단 쪽 전진 비율을 높이고 타격 기준 길이를 `2.05배`로 확장했다. 뾰족한 끝부분은 타격 방향과 `0.999844253`로 일치하며 몸통보다 `0.0575027466m` 앞서 목표를 향한다. 1·2차 공격은 각각 전방 이동 `1.09588027m`, 낙차 `1.44445491m`, 측면 이동 `0.00000532480863m`다.
- 준비 자세는 기존 공격 준비점보다 위로 `0.229243517m`, 뒤로 `0.5981156m` 이동하며, 가속 표본 뒤 타격까지 전방 이동 `0.8102287m`가 집중된다. 1·2차 충돌 유지 오차는 모두 `0`, 첫 공격 복귀 오차는 `0`이다. 두 번째 공격의 숨김 축소는 충돌 유지가 끝난 `1.88초` 이후 시작하도록 늦춰 타격 실루엣이 유지되게 했다.
- 고리 생성, 고리 통과, 고리 연결을 유지한 비스듬한 전면 출구, 상승 경로와 고정 시작점은 변경하지 않았다. 고리 표면 이동은 `0.00000011920929m`, 본 루트 이동은 `0`, 상승 뒤 액자 앞쪽 최소 이격은 `0.340871364m`로 유지됐다.
- 실제 Animator 진단 캡처는 27개 자세를 정면·사선·측면에서 촬영해 81장을 생성했다. 준비, 힘 모음, 가속, 1차 타격·충돌 유지·복귀, 2차 타격·충돌 유지 프레임을 대조해 뾰족한 끝이 전방 선두에서 길게 뻗고 옆으로 새지 않는 것을 확인했다. 촬영 후 판정은 반복 시작 표본 제외 조건이 예전 번호에 남아 다음 주기 고리 생성량을 고정점 이동으로 오인해 실패했으며, 조건을 현재 `24~26`번 표본으로 수정했다. 승인된 진단 캡처 1회 한도에 따라 재촬영하지 않았다.
- 적용 후와 캡처 코드 수정 후 전용 저장 상태 검사는 모두 `Result=PASS`, `SceneChanged=False`였다. 최종 보고서는 `Dolore_04_TentacleStab_Inspection.txt`이며 씬과 애니메이션 자산은 적용 성공 시 저장됐다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `OpenCargoRunMvpScene`, `ApplyDolore04TentacleStabFullMotion`, `InspectDolore04TentacleStabFullMotion`, `CaptureDolore04TentacleStabFullMotionDiagnostic`이다. 이번 후속 작업에서는 `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처를 실행하지 않았다.

## 완료 - 돌로레 처형 모션 전반부 복제

- 대상은 `Approved Dolore Enemy Placement/Dolore_05_Execution_Pull_In`이다. 사용자 지시에 따라 처형 모션의 관통 이전 구간을 4번 공격과 동일하게 구성했다. 현재 범위는 숨김 상태, 고리 `0.5초` 생성, 고리를 통한 뾰족한 끝 출현, 액자 앞쪽 비스듬한 출구, 전체 `1.5초` 상승, 위쪽 준비, 힘 모음, 첫 전방 찌르기와 충돌 유지까지다. 관통 이후 대상 끌어오기와 액자 내부 수납은 구현하지 않았다.
- 기존 `Dolore_04_TentacleStab_Intro.anim`을 5번 전용 `Dolore_05_ExecutionPullIn_Intro.anim`으로 복제하고, `Dolore_04_TentacleStab_AttackLoop.anim`의 첫 충돌 유지 종료 시점인 `0.58초`까지를 `Dolore_05_ExecutionPullIn_PierceHold.anim`으로 잘라냈다. 원본 공격 클립과 컨트롤러는 수정하지 않았다.
- 5번 전용 컨트롤러는 `Intro -> PierceHold`로 구성했다. Intro `2.0초`가 끝나면 첫 공격 구간으로 즉시 전환하고, `PierceHold`에는 출구 전환을 두지 않아 찌른 충돌 자세에서 멈춘다. 이후 처형 끌어오기 모션은 이 상태 뒤에 연결할 수 있다.
- 첫 적용에서는 5번 슬롯 위치에서 플레이어 방향을 새로 계산해 동일한 로컬 모션을 측면 이동 `0.290768564m`로 잘못 판정했다. 사용자 기준은 슬롯별 플레이어 방향이 아니라 액자가 바라보는 바깥 방향이므로, 4번에서 승인된 액자 바깥축을 5번에도 그대로 사용하는 방식으로 수정했다. 실패 중 남은 미저장 장면 상태는 승인된 조건에 따라 `OpenCargoRunMvpScene`으로 폐기한 뒤 다시 적용했다.
- 최종 전용 검사는 `Result=PASS`, `SceneChanged=False`다. 4번 원본 대비 Intro 커브 차이와 첫 찌르기 커브 차이는 모두 `0`, Intro와 찌르기 준비 연결 오차는 `0`이다. 상승 `1.04043579m`, 힘 모음 상승 `0.229243755m`, 후퇴 `0.5981156m`, 마지막 전방 가속 `0.8102287m`, 전체 전방 이동 `1.09588027m`, 낙차 `1.44445467m`, 측면 이동 `0.00000531970545m`, 충돌 유지 오차 `0`, 본 루트 이동 `0`, 시작점 표면 이동 `0.0000152592547m`다.
- 실제 Animator 진단 캡처는 16개 자세를 정면·사선·측면으로 촬영해 총 48장을 생성했다. 숨김, 고리 생성, 고리 통과, 비스듬한 전면 출구, 상승, 준비, 힘 모음, 가속, 찌르기와 충돌 유지가 순서대로 확인됐다. 실제 재생의 고정 본 이동은 `0`, 마지막 충돌 자세 오차도 `0`이다. 이미지는 `Dolore_05_ExecutionPullIn_Opening_Diagnostic/`에 있다.
- 생성 자산은 `Dolore_05_ExecutionPullIn_Intro.anim`, `Dolore_05_ExecutionPullIn_PierceHold.anim`, `Dolore_05_ExecutionPullIn.controller`이며, 보고서는 `Dolore_05_ExecutionPullIn_Opening_Inspection.txt`다. `CargoRunMvp.unity`의 5번 처형 개체에 전용 Animator Controller를 연결하고 저장했다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionOpening`, `InspectDolore05ExecutionOpening`, `CaptureDolore05ExecutionOpeningDiagnostic`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 완료 - 돌로레 처형 대상 transfer 배치

- 사용자 지정 원본 `enemies model/transfer.fbx`(423,244 bytes)를 바이트 변경 없이 `Assets/_Project/Art/Generated/Enemies/Dolore/ExecutionTarget/transfer.fbx`로 반입했다. 원본과 Unity 반입본의 SHA-256은 모두 `292ED21D21E4FDBCEEC2F515B1FB94F8055D61CBC0A6D2BB16FCC68F8B0381CE`다. 모델의 메시·머티리얼·리그는 수정하지 않았다.
- `Approved Dolore Enemy Placement/Dolore_05_Execution_Pull_In` 아래에 `Dolore_05_Execution_Target_Transfer`를 생성했다. 처형 개체와 같은 로컬 X/Y `0, 0`, 기본 회전 `(0,0,0,1)`, 기본 스케일 `(1,1,1)`을 유지하고 촉수가 찌르는 앞쪽 로컬 Z `2.311592`에 배치했다.
- `PierceHold` 종료 자세의 촉수 끝은 로컬 `(-0.09925101, 0.6875298, 2.44436646)`이고, 대상 렌더러 범위는 중심 `(0.0433347225, 0.829666138, 2.44436646)`, 크기 `(1.46760559, 2.17471337, 1.1280061)`다. 촉수 끝은 대상 범위 안에 있으며 범위까지의 거리는 `0`이다.
- 전용 배치 검사는 `Result=PASS`, 캡처 후 재검사는 `SceneChanged=False`다. 렌더러는 1개이고 콜라이더는 추가하지 않았다. 처형 애니메이션, 다른 돌로레 모션 개체, 다른 씬 루트는 변경하지 않았다. 보고서는 `Dolore_05_ExecutionTarget_Inspection.txt`다.
- 승인된 진단 캡처 1회로 `PierceHold`의 정면·사선·측면 3장을 생성했다. 정면에서는 대상이 처형 중심선에 정렬되고, 사선·측면에서는 촉수 끝이 대상 몸통 범위까지 도달한 상태를 확인했다. 이미지는 `Dolore_05_ExecutionTarget_Diagnostic/`에 있다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, 실패 적용 뒤 미저장 상태를 폐기하기 위한 `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionTargetTransfer`, `InspectDolore05ExecutionTargetTransfer`, `CaptureDolore05ExecutionTargetTransferDiagnostic`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 촉수 메시의 운송자 몸통 완전 관통

- 기존 로컬 Z `2.311592` 배치는 촉수 끝 본을 운송자 전체 렌더러 경계 중심에 맞춘 상태라 실제 몸통 메시의 반대편 표면 통과를 보장하지 못했다. 운송자 X/Y `0, 0`, 회전 `(0,0,0,1)`, 스케일 `(1,1,1)`은 유지하고 Z만 돌로레 쪽으로 이동했다.
- `PierceHold`의 촉수 진행선과 운송자 실제 메시 삼각형의 교점을 계산해 몸통 진입면과 최종 이탈면을 구하도록 전용 배치 검사를 변경했다. 첫 본 좌표 기준 배치는 Z `2.178078`이었으나 진단 측면에서 실제 뾰족한 렌더 메시 노출이 충분히 분명하지 않아, FBX 본 가중치와 bind pose로 촉수 정점을 직접 스키닝한 실제 메시 최전방점을 최종 기준으로 사용했다.
- 최종 운송자 로컬 위치는 `(0, 0, 2.0459826)`이며 최초 배치보다 돌로레 쪽으로 `0.2656094m` 이동했다. 몸통 메시 진입면은 Z `2.01942253`, 이탈면은 Z `2.22280717`, 실제 뾰족한 촉수 메시 최전방은 Z `2.312271`이다.
- 뾰족한 촉수 메시가 몸통 이탈면 밖으로 나온 거리는 `0.08946395m`이고, 촉수 말단 한 마디 길이 `0.08946373m` 전체가 반대편 외부로 노출된다. 촉수 체인 최소 Z는 `0.9497223`으로 진입면보다 뒤에 있어 진입면부터 이탈면까지 연속 관통한다. 최종 전용 검사는 `FullTorsoPenetration=True`, `PointedTentacleMeshBeyondExit=True`, `Result=PASS`, `SceneChanged=False`다.
- 승인된 진단 캡처 1회로 정면·사선·측면 3장을 확인했으며, 이 화면 확인을 근거로 끝 본 대신 실제 렌더 메시 기준 보정을 적용했다. 승인된 1회 한도를 사용했기 때문에 최종 보정 뒤 추가 캡처는 실행하지 않았고 최종 저장 상태는 실제 메시 교차 수치로 재검사했다.
- 원본 및 반입 `transfer.fbx`의 SHA-256은 `292ED21D21E4FDBCEEC2F515B1FB94F8055D61CBC0A6D2BB16FCC68F8B0381CE`로 유지됐다. FBX 메시·리그·머티리얼, 처형 애니메이션, 콜라이더, 다른 돌로레 모션 개체와 다른 씬 루트는 변경하지 않았다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, 실패 적용의 미저장 상태를 폐기하기 위한 `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionTargetTransfer`, `InspectDolore05ExecutionTargetTransfer`, `CaptureDolore05ExecutionTargetTransferDiagnostic`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 촉수 끝 노출을 유지한 최대 거리 배치

- 이전 로컬 Z `1.40883482`는 촉수 끝부분과 실제로 겹치지 않는 수송자 장비·팔다리까지 포함한 전체 렌더러 최대 깊이를 기준으로 삼아 수송자를 돌로레 쪽으로 과도하게 당겼다. 사용자 지시에 따라 끝부분 전체가 보이는 조건을 유지하면서 가능한 가장 먼 위치를 다시 계산했다.
- `Bone_004`가 지배하는 촉수 끝부분 5,359개 정점을 각각 수송자 메시로 투영했다. 각 정점의 X/Y에서 실제로 겹치는 수송자 삼각형 중 가장 앞선 Z만 구하고, 모든 겹침 정점이 해당 표면보다 `0.001m` 앞에 남도록 하는 제약 중 최대 로컬 Z를 선택했다. 끝부분과 무관한 수송자 전체 경계는 거리 계산에서 제외했다.
- 수송자 최종 로컬 위치는 `(0, 0, 1.87494838)`이다. 이전보다 돌로레에서 `0.46611356m` 멀어졌다. 몸통 진입면은 Z `1.84838688`, 이탈면은 Z `2.05177927`이며 촉수 체인 최소 Z `0.9497223`이 진입면 뒤에 있어 관통은 유지된다.
- 끝부분 5,359개 정점 모두 수송자 투영과 겹쳤고, 가장 작은 앞쪽 여유는 `0.0009942055m`다. 부동소수점 허용 오차 안에서 설정 여유 `0.001m`를 만족하며 `EntirePointedSectionVisible=True`, `FarthestVisibleLocalZ=True`, `FullTorsoPenetration=True`다.
- 승인된 진단 캡처 1회로 정면·사선·측면 3장을 확인했다. 이전보다 수송자와 돌로레 사이 거리가 벌어졌고 정면·사선에서 붉은 끝부분이 몸통 앞쪽에 남아 있다. 촬영 후 재검사는 `Result=PASS`, `SceneChanged=False`다.
- 수송자 X/Y `0, 0`, 회전 `(0,0,0,1)`, 스케일 `(1,1,1)`과 원본·반입 `transfer.fbx`, 촉수·처형 애니메이션, 콜라이더, 다른 돌로레 모션 개체 및 다른 씬 루트는 변경하지 않았다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `ApplyDolore05ExecutionTargetTransfer`, `InspectDolore05ExecutionTargetTransfer`, `CaptureDolore05ExecutionTargetTransferDiagnostic`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 수송자 몸통 앞에 촉수 끝부분 전체 노출

- 이전 로컬 Z `2.0459826`은 실제 촉수 메시의 최전방 정점만 몸통 중심선 이탈면보다 앞에 두는 기준이었다. 이 방식은 뾰족한 끝부분의 뒤쪽 정점과 수송자 앞쪽 실루엣의 다른 부위가 겹칠 수 있어, 촉수 끝부분 전체가 수송자 몸통 앞에서 보인다는 조건을 보장하지 못했다.
- 말단 위치 본 `Bone_001`은 메시 정점을 직접 지배하지 않는 보조 본이다. `Bone_001`에서 부모 방향으로 올라가 실제 지배 정점이 있는 가장 가까운 `Bone_004`를 끝부분 메시 구동 본으로 자동 선택했다. `Bone_004`가 지배하는 5,359개 정점을 FBX 본 가중치와 bind pose로 직접 스키닝해 끝부분 범위 Z `2.195076 ~ 2.312271`을 계산했다.
- 첫 보정은 수송자 로컬 Z `1.92878759`에서 몸통 중심선 이탈면보다 끝부분 전체를 `0.08946395m` 앞에 뒀다. 승인된 정면·사선·측면 진단 캡처 1회에서 중심선 조건은 충족했지만, 수송자 전체 앞쪽 실루엣의 최대 Z가 끝부분보다 앞서 측면 가림이 남는 것을 확인했다.
- 최종 기준은 몸통 중심선 한 점이 아니라 수송자 렌더러 전체의 가장 앞선 Z다. 수송자 최종 로컬 위치를 `(0, 0, 1.40883482)`로 옮겼다. 몸통 진입면은 Z `1.38227367`, 이탈면은 Z `1.58566117`, 수송자 전체 앞쪽 표면은 Z `2.10561371`이다.
- 촉수 끝부분의 가장 뒤쪽 정점 Z `2.195076`은 수송자 전체 앞쪽 표면보다 `0.08946228m` 앞에 있다. 말단 한 마디 길이 `0.08946373m`만큼의 노출 여유를 확보해 끝부분 전체가 수송자 앞쪽 형상에 가려지지 않는다. 최종 검사는 `EntirePointedSectionVisible=True`, `FullTorsoPenetration=True`, `Result=PASS`, `SceneChanged=False`다.
- 수송자 X/Y `0, 0`, 회전 `(0,0,0,1)`, 스케일 `(1,1,1)`은 유지했다. 원본 및 반입 `transfer.fbx`, 촉수·처형 애니메이션, 콜라이더, 다른 돌로레 모션 개체와 다른 씬 루트는 변경하지 않았다. 승인된 진단 캡처 1회 한도를 사용했기 때문에 최종 수치 보정 뒤 추가 캡처는 실행하지 않았다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, 실패 적용의 미저장 상태를 폐기하기 위한 `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionTargetTransfer`, `InspectDolore05ExecutionTargetTransfer`, `CaptureDolore05ExecutionTargetTransferDiagnostic`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 처형 대상 교체와 2초 액자 회수 반복

- 사용자 지정 `transfer.fbx`와 `transfer lying.fbx`를 처형 대상의 세운 상태와 관통 직후 누운 상태로 사용했다. Unity 반입본은 원본과 바이트 단위로 동일하며 SHA-256은 각각 `292ED21D21E4FDBCEEC2F515B1FB94F8055D61CBC0A6D2BB16FCC68F8B0381CE`, `AD8C1493915797EE20011174CC1491D8EA37852669C81BDBC0F9916C1283DECD`다. 두 FBX의 메시·리그·머티리얼은 수정하지 않았다.
- 수송자의 원본 비율을 유지하는 공통 균일 배율 `0.9104649`를 적용했다. 돌로레 액자 렌더 범위 높이는 `1.8m`, 세운 수송자 높이는 `1.98m`로 정확히 `1.10배`다. 세운 수송자 최종 로컬 위치는 `(0, 0, 1.971102)`다.
- 누운 FBX의 메시 중심이 세운 FBX와 다른 원점에 있으므로 임의 좌표 대신 관통 시점 촉수 뾰족부 투영 중심과 누운 메시 중심을 계산해 맞췄다. 누운 상태의 관통 시점 로컬 위치는 `(-0.09367777, 0.8808584, 1.713703)`이며, 촉수 체인이 메시 앞면 뒤에 있고 뾰족부가 겹치는 앞면 밖으로 나오는 조건을 통과했다.
- 처형 슬롯 Animator를 `Intro -> PierceHold -> PullIn -> Intro` 순환 구조로 재구성했다. `Intro`는 기존 고리 생성과 상승 전반부, `PierceHold`는 기존 첫 찌르기 `0.58초`까지 유지한다. 관통 직후 `PullIn` 0초에서 세운 모델을 숨기고 누운 모델을 즉시 표시하며, 전환 전후 촉수 끝 위치 오차는 `0`이다.
- `PullIn`은 정확히 `2.0초`다. 누운 수송자는 촉수가 관통된 상태에서 고정 고리 중심으로 이동하면서 원본 비율을 유지한 채 균일 축소되고, 종료 시 렌더러가 숨겨진다. 촉수 리그도 동일 구간에 관통 자세에서 Intro의 숨김 시작 자세로 회수된다. 이후 즉시 세운 수송자 상태로 복구되고 고리 생성부터 다음 반복이 시작된다.
- 전용 저장 상태 검사는 `Result=PASS`, `StandingHeightRatio=1.1`, `PullSeconds=2`, `PierceToPullTipError=0`, `PullStartLyingPenetrated=True`, `PullEndHidden=True`, `SceneChanged=False`다. 보고서는 `Dolore_05_ExecutionPullIn_Loop_Inspection.txt`다.
- 승인된 실제 Animator 진단 캡처 1회로 세운 상태, 고리 생성, 관통, 누운 상태 교체, 회수 중간, 회수 종료 직전의 6개 시점을 생성했다. 표적 교체와 액자 방향 이동·균일 축소가 순서대로 확인됐고 캡처 뒤 장면은 변경되지 않았다. 추가 진단 및 최종 캡처는 실행하지 않았다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, 실패 적용의 미저장 상태를 폐기하기 위한 `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionPullInLoop`, `InspectDolore05ExecutionPullInLoop`, `CaptureDolore05ExecutionPullInLoopDiagnostic`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 80% 수송자 회수 중 촉수 끝 추종

- 수송자 배율을 실제 황동 액자의 `80%` 안에 맞춘 뒤 기존 `PullIn`은 수송자와 촉수 리그를 각각 독립적으로 숨김 자세까지 보간해, 끌려가는 동안 촉수 끝과 작은 수송자 몸통의 관통 지점이 분리될 수 있었다.
- 실제 뾰족한 끝 메시를 지배하는 정점 집합에서 중심에 가장 가까운 기준 정점을 선택했다. `PullIn` 0초의 수송자 상대 위치를 저장하고, 수송자의 실제 이동량과 같은 만큼 기준 정점이 따라가도록 첫 가동 본 `Bone_009`의 로컬 위치 커브를 `0.1초` 간격으로 보정했다.
- 촉수 끝 추종은 수송자 렌더러가 유지되는 `1.94초`까지 계속되고, 마지막 `0.06초`에는 기존 Intro 숨김 자세로 복귀한다. 수송자 이동·크기·클리핑 커브는 변경하지 않았다.
- 0.0~1.9초 20개 표본에서 실제 촉수 끝 정점과 수송자 상대 관통 지점의 최대 오차는 `0.000012358m`다. 관통 지점의 몸통 앞면이 액자 평면 앞에 있어 화면에 보이는 15개 표본에서는 촉수 끝이 몸통 앞면 밖에 있고 연결 체인이 몸통 내부까지 이어지는 상태를 모두 유지했다.
- 몸통 관통 지점이 액자 평면 `Z=0.9497223` 뒤로 넘어간 이후에는 픽셀 클리핑으로 해당 단면이 보이지 않으므로, 보이지 않는 후반 단면의 완전 관통은 요구하지 않았다. 촉수 끝의 수송자 상대 위치 추종 자체는 계속 유지된다.
- 최종 전용 검사는 `Result=PASS`, `PointedTipFollowMaximumError=0.000012358`, `PointedTipFollowSampleCount=20`, `PointedTipVisibleImpaledSampleCount=15`, `PointedTipRemainsImpaledAtAllSamples=True`, `PullSeconds=2`, `PullScaleVariation=0`, `SceneChanged=False`다.
- 승인된 실제 Animator 진단 1회로 누운 모델 교체, 회수 중간, 액자 진입 뒤 숨김 순서를 확인했다. 실패 적용으로 남은 미저장 장면 변경은 승인된 `OpenCargoRunMvpScene`으로 마지막 저장 상태에서 복구했다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionPullInLoop`, `CaptureDolore05ExecutionPullInLoopDiagnostic`, `InspectDolore05ExecutionPullInLoop`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업과 최종 캡처는 실행하지 않았다.

## 후속 완료 - 실제 액자 기준 수송자 80% 균일 배율

- 수송자 원본 비율을 유지하면서 세운 모델과 누운 모델의 너비·높이가 모두 액자의 `80%` 안에 들어가도록 공통 균일 배율을 다시 계산했다. 두 FBX 자세의 단위 배율 경계를 모두 비교하고, 너비·높이 네 조건 중 가장 작은 배율을 선택한다.
- 첫 계산에서 돌로레 다리·몸체까지 포함한 전체 렌더 범위를 액자 크기로 사용한 문제가 실제 Animator 진단에서 확인됐다. 이 기준은 폐기하고 `Dolore_Oxidized_Brass_Frame` 서브메시만 직접 베이크해 얻은 실제 황동 액자 경계 `0.7707214m × 0.8060298m`를 최종 기준으로 사용했다.
- 최종 공통 균일 배율은 `0.2965097`이다. 세운 자세는 액자 너비의 `56.46209%`, 높이의 `80%`; 누운 자세는 액자 너비의 `71.52446%`, 높이의 `60.98877%`다. 두 자세 모두 실제 액자 너비와 높이의 `80%`를 넘지 않으며 회수 2초 전 구간의 배율 변화는 `0`이다.
- 작은 모델에서 전체 렌더 경계 중심을 촉수 끝에 맞추면 팔·다리 사이 빈 공간이 관통점에 올 수 있었다. 세운·누운 수송자의 몸통 중심 부근 실제 투영 삼각형을 선택하고 촉수 끝 정점을 그 삼각형 내부에 정렬하도록 변경했다. 최종 검사는 `PullStartLyingPenetrated=True`, 관통 전후 촉수 끝 위치 오차 `0`이다.
- 기존 `PullIn=2.0초`, 액자 평면 픽셀 클리핑, 촉수 회수, `Intro -> PierceHold -> PullIn -> Intro` 반복 구조를 유지했다. 회수 종료 최대 Z는 `0.9397278`로 액자 평면 `0.9497223` 뒤에 있고, 최종 검사는 `Result=PASS`, `BothPosesFitWithinFrame=True`, `PullEndHidden=True`, `SceneChanged=False`다.
- 실제 Animator 진단 1회는 잘못된 전체 돌로레 경계 기준을 발견하는 데 사용했다. 이후 실제 황동 액자 기준 최종 보정에는 승인된 추가 진단·최종 캡처를 실행하지 않고 저장 수치 검사로 마무리했다.
- 실패 적용의 미저장 변경은 승인된 `OpenCargoRunMvpScene`으로 각각 마지막 저장 상태에서 복구했다. 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionPullInLoop`, `CaptureDolore05ExecutionPullInLoopDiagnostic`, `InspectDolore05ExecutionPullInLoop`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업과 최종 캡처는 실행하지 않았다.

## 후속 완료 - 수송자 축소 제거와 액자 경계 부분 가림

- 사용자 제공 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 18-21-06.mp4`를 대조했다. 기존 `PullIn` 클립은 누운 수송자의 로컬 배율을 `0.9104649`에서 `0.03`까지 직접 보간해, 액자로 회수되는 동안 몸통이 눈에 띄게 작아졌다.
- 누운 수송자의 `m_LocalScale.x/y/z`를 회수 2초 전 구간에서 `0.9104649`로 고정했다. 9개 시점의 세 축 배율 변화량은 모두 `0`이며 원본 비율과 액자 대비 크기는 유지된다. 이전 진행 항목의 균일 축소 설명은 이번 수정으로 폐기됐다.
- 고정된 고리 위치 `Z=0.9497223`에 색은 그리지 않고 깊이만 기록하는 전용 액자 입구 평면을 배치했다. 액자와 기존 장면은 기본 렌더 순서를 유지하고, 누운 수송자에만 원본 머티리얼 속성을 복제한 전용 머티리얼을 연결해 가림 평면 뒤에서 렌더되게 했다. 가림 평면과 수송자 렌더 큐는 각각 `2010`, `2020`이다.
- 회수 중 몸통이 입구 평면을 넘은 픽셀은 즉시 깊이 가림되고, 평면 앞쪽에 남은 부분은 원래 크기로 계속 표시된다. 실제 모델 경계는 관통 직후부터 일부가 평면에 걸쳐 있으므로 첫 교체 시점부터 넘어간 부분만 가려지며, 부분 교차 상태는 `0.1초` 표본에서 확인됐다.
- 회수 종점은 몸통 중심을 고리에 맞추는 방식에서 몸통 최전방 표면까지 완전히 액자 뒤로 보내는 방식으로 변경했다. 2초 종료의 몸통 최대 Z는 `0.9397279`로 액자 평면보다 약 `0.01m` 뒤에 있고, 그 뒤 렌더러가 숨겨진다. 회수 시간, 관통 직후 누운 모델 교체, 촉수 포즈 연속성 오차 `0`, 세운 상태와 고리 생성부터의 반복은 유지됐다.
- 실제 Animator 진단 캡처 1회에서 관통 직후 원래 크기, 회수 중간의 부분 가림, 종료 직전의 액자 뒤 진입을 확인했다. 전용 최종 검사는 `Result=PASS`, `PullScaleVariation=0`, `PullScaleConstant=True`, `CrossedPortionHiddenImmediately=True`, `PullEndHidden=True`, `SceneChanged=False`다.

## 후속 완료 - 수송자 액자 진입부 픽셀 클리핑

- 사용자 제공 실제 게임 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 20-52-56.mp4`를 대조했다. 이전 깊이 전용 오클루더는 실제 Game 뷰에서 수송자 픽셀을 가리지 못했고, 수송자가 액자 몸체와 겹친 채 액자 너머에서도 계속 보였다. 따라서 위 항목의 깊이 오클루더 방식이 실제 화면에서 동작했다는 판정은 폐기했다.
- 누운 수송자 전용 `DoloreExecutionPortalClip.shader`를 추가했다. 고정된 고리 위치 `Z=0.9497223`을 통과하는 월드 평면을 머티리얼의 `_PortalClipPlane`에 저장하고, 수송자 각 픽셀의 월드 위치가 액자 뒤로 넘어가면 해당 픽셀을 셰이더에서 즉시 폐기한다. 이 방식은 별도 오클루더의 렌더 순서나 카메라 깊이 결과에 의존하지 않는다.
- 씬의 `Dolore_05_Execution_Portal_Occluder` 개체는 제거했다. 누운 수송자 전용 머티리얼은 새 클리핑 셰이더와 기본 Geometry 렌더 큐 `2000`을 사용한다. 세운 수송자와 돌로레 본체 머티리얼은 변경하지 않았다.
- 수송자 균일 배율 `0.9104649`, 원본 비율, 관통 직후 누운 모델 교체, `PullIn=2.0초`, 촉수 회수, `Intro -> PierceHold -> PullIn -> Intro` 반복 구조는 유지했다. 9개 배율 표본의 변화량은 `0`, 관통 전후 촉수 끝 위치 오차는 `0`이다.
- 승인된 실제 Animator 진단 1회에서 회수 중간에는 액자 앞쪽에 남은 신체 부분만 보이고 평면을 넘은 부분은 잘렸으며, 종료 직전에는 수송자가 완전히 보이지 않는 것을 확인했다. 최종 저장 상태 검사는 `Result=PASS`, `LegacyDepthOccluderPresent=False`, `PixelClipIndependentOfRenderOrder=True`, `PullEndHidden=True`, `SceneChanged=False`다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `ApplyDolore05ExecutionPullInLoop`, `CaptureDolore05ExecutionPullInLoopDiagnostic`, `InspectDolore05ExecutionPullInLoop`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, 실패 적용의 미저장 상태를 폐기하기 위한 `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionPullInLoop`, `InspectDolore05ExecutionPullInLoop`, `CaptureDolore05ExecutionPullInLoopDiagnostic`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 수송자 30도 기울기와 액자 진입 전 정렬

- 관통 직후 누운 수송자는 로컬 Y축 `+30도`로 기운 자세에서 회수를 시작한다. 이 시작 자세에서 몸통 관통점을 다시 정렬했으며, 씬의 기본 자세는 `0도`로 유지하고 `PullIn` 클립만 회전을 담당한다.
- 회수 중 수송자는 액자 평면과 처음 접촉하기 직전까지 부드럽게 Y축 `0도`로 복귀하고 이후 회전 없이 액자를 통과한다. 실제 진입 시점은 `1.068876초`, 시작 Y 회전은 `30도`, 진입 시점 Y 회전과 진입 이후 최대 절대 Y 회전은 모두 `0도`다.
- 촉수 끝 추종 기준을 단순 위치 차이에서 수송자 로컬 관통점으로 변경했다. 따라서 수송자가 회전하는 동안에도 촉수의 뾰족한 끝이 같은 몸통 지점을 따라가며, 20개 표본의 최대 오차는 `0.00001079m`다. 액자 앞에서 관통면이 보이는 6개 표본은 모두 관통 상태를 유지했다.
- 수송자 공통 균일 배율 `0.2965097`, 원본 비율, `PullIn=2초`, 액자 픽셀 클리핑, 종료 숨김, 세운 상태와 고리 생성부터 반복하는 구조는 유지했다. 원본과 Unity 반입 FBX의 SHA-256은 각각 `292ED21D21E4FDBCEEC2F515B1FB94F8055D61CBC0A6D2BB16FCC68F8B0381CE`, `AD8C1493915797EE20011174CC1491D8EA37852669C81BDBC0F9916C1283DECD`로 동일하다.
- 승인된 실제 Animator 진단 1회에서 관통 직후, 회수 중간, 회수 종료 상태를 생성했고 `ActualAnimatorStates=6`, `SceneChanged=False`를 확인했다. 이어진 저장 상태 검사는 `Result=PASS`, `PullStartLyingPenetrated=True`, `PointedTipRemainsImpaledAtAllSamples=True`, `PullEndHidden=True`, `SceneChanged=False`다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `ApplyDolore05ExecutionPullInLoop`, `CaptureDolore05ExecutionPullInLoopDiagnostic`, `InspectDolore05ExecutionPullInLoop`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, `OpenCargoRunMvpScene`, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 수송자 30도 기울기 축을 X축으로 교체

- 사용자 확인에 따라 직전 항목의 로컬 Y축 `+30도` 기울기는 폐기하고 로컬 X축 `+30도`로 교체했다. 관통 직후 누운 수송자는 X축 `+30도`에서 회수를 시작하며, 씬의 기본 자세는 `0도`이고 `PullIn` 클립만 회전한다.
- 수송자는 액자 평면에 처음 접촉하기 직전까지 X축 `0도`로 부드럽게 복귀한 뒤 회전 없이 액자를 통과한다. 실제 진입 시점은 `1.070115초`, 시작 X 회전은 `30도`, 진입 시점 X 회전과 진입 이후 최대 절대 X 회전은 모두 `0도`다.
- 촉수의 뾰족한 끝은 축에 독립적인 수송자 로컬 관통점을 추종한다. 0.0~1.9초 20개 표본의 최대 추종 오차는 `0.000015259m`이며, 액자 앞에서 관통면이 보이는 6개 표본은 모두 관통 상태를 유지했다.
- 수송자 공통 균일 배율 `0.2965097`, 원본 비율, `PullIn=2초`, 픽셀 클리핑, 종료 숨김과 고리 생성부터 반복하는 구조는 유지했다. 원본과 Unity 반입 FBX의 SHA-256은 각각 `292ED21D21E4FDBCEEC2F515B1FB94F8055D61CBC0A6D2BB16FCC68F8B0381CE`, `AD8C1493915797EE20011174CC1491D8EA37852669C81BDBC0F9916C1283DECD`로 동일하다.
- 최초 적용은 기존 미저장 장면 변경 때문에 저장 전에 중단됐다. 사용자 추가 승인에 따라 `OpenCargoRunMvpScene`으로 마지막 저장 장면을 다시 열어 미저장 상태를 폐기한 뒤 적용했으며, 성공 적용은 `SceneSaved=True`다.
- 승인된 실제 Animator 진단 1회는 `ActualAnimatorStates=6`, `SceneChanged=False`로 완료됐다. 이어진 저장 상태 검사는 `Result=PASS`, `PullStartLyingPenetrated=True`, `PointedTipRemainsImpaledAtAllSamples=True`, `PullEndHidden=True`, `SceneChanged=False`다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionPullInLoop`, `CaptureDolore05ExecutionPullInLoopDiagnostic`, `InspectDolore05ExecutionPullInLoop`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 수송자 X축 시작 기울기 60도 강화

- 사용자 지시에 따라 직전 X축 `+30도` 시작 자세는 폐기하고 X축 `+60도`로 변경했다. 관통 직후 누운 수송자는 X축 `+60도`에서 회수를 시작하며, 액자에 접근하면서 `0도`로 부드럽게 전환된다.
- 실제 액자 평면 최초 진입 시점은 `1.064581초`다. 시작 X 회전은 `60도`, 진입 시점 X 회전과 진입 이후 최대 절대 X 회전은 모두 `0도`이므로 수송자는 정렬된 상태로 액자를 통과한다.
- 촉수의 뾰족한 끝은 수송자 로컬 관통점을 계속 추종한다. 0.0~1.9초 20개 표본의 최대 추종 오차는 `0.00000763m`이며, 액자 앞에서 관통면이 보이는 5개 표본은 모두 관통 상태를 유지했다.
- 공통 균일 배율 `0.2965097`, 원본 비율, `PullIn=2초`, 픽셀 클리핑, 종료 숨김과 고리 생성부터 반복하는 구조는 변경하지 않았다. 원본과 Unity 반입 FBX의 SHA-256은 각각 `292ED21D21E4FDBCEEC2F515B1FB94F8055D61CBC0A6D2BB16FCC68F8B0381CE`, `AD8C1493915797EE20011174CC1491D8EA37852669C81BDBC0F9916C1283DECD`로 동일하다.
- 실제 Animator 진단 1회는 `ActualAnimatorStates=6`, `SceneChanged=False`로 완료됐다. 저장 상태 검사는 `Result=PASS`, `PullStartLyingPenetrated=True`, `PointedTipRemainsImpaledAtAllSamples=True`, `PullEndHidden=True`, `SceneChanged=False`다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, `ApplyDolore05ExecutionPullInLoop`, `CaptureDolore05ExecutionPullInLoopDiagnostic`, `InspectDolore05ExecutionPullInLoop`이다. 조건부 `OpenCargoRunMvpScene`은 필요하지 않아 실행하지 않았다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 수송자 회전에 동기화된 관통 촉수 체인

- 기존 회수 보정은 `Bone_009`의 위치만 이동해 뾰족한 끝을 수송자 관통점에 붙였고 촉수 체인의 회전은 원래 회수 자세를 유지했다. 이에 수송자가 X축 `60도 -> 0도`로 회전해도 촉수 방향은 함께 변하지 않아 시각적으로 관통이 분리돼 보일 수 있었다.
- 내장 리그의 첫 가동 본 `Bone_009`에 수송자의 현재 X축 각도를 같은 방향으로 더했다. 촉수 체인을 먼저 회전한 뒤 회전으로 이동한 뾰족 정점을 수송자 로컬 관통점에 다시 맞추므로, 수송자와 촉수가 함께 `60도 -> 0도`로 전환되면서 동일한 몸통 지점을 계속 관통한다.
- 위치와 쿼터니언 커브를 함께 기록한 뒤 완성된 클립을 두 번 재표본화해 실제 스킨드 메시 뾰족 정점의 잔여 오차를 `Bone_009` 위치 커브에 보정했다. 또한 원본 관통 자세에서 선택한 하나의 정점 인덱스를 생성·추종·연속성 검사 전 구간에 공통 사용해 회전 후 AABB 변화가 다른 정점을 선택하지 않게 했다.
- 첫 두 적용은 기존 `TipBone`/자세별 AABB 중심 정점 기준의 전환 연속성 검사에서 `0.002m` 허용치를 넘겨 중단됐다. 동일 정점 기준을 넣기 전 확인된 실제 오차는 `0.006765206m`였고, 최종 전환 오차는 `0.00000006m`다. 실패 적용의 미저장 장면은 승인된 `OpenCargoRunMvpScene`으로 마지막 저장 상태에서 복구했다.
- 0.0~1.9초 20개 표본에서 수송자 각도에 따른 촉수 리그의 기대 회전과 실제 회전의 최대 오차는 `0도`다. 뾰족 정점의 수송자 로컬 관통점 추종 최대 오차는 `0.000007631m`이며, 액자 앞에서 관통면이 보이는 5개 표본 모두 촉수 끝이 몸통 앞에 있고 체인이 내부까지 이어진 상태를 유지했다.
- 수송자 시작 X 회전 `60도`, 액자 최초 진입 `1.064581초`, 진입 시점과 이후 X 회전 `0도`, 공통 균일 배율 `0.2965097`, `PullIn=2초`, 픽셀 클리핑, 종료 숨김과 반복 구조는 유지했다. 원본과 반입 FBX 해시도 이전 승인값과 동일하다.
- 실제 Animator 진단 1회는 `ActualAnimatorStates=6`, `SceneChanged=False`로 완료됐다. 최종 저장 상태 검사는 `Result=PASS`, `TentacleRotationFollowMaximumErrorDegrees=0`, `PointedTipRemainsImpaledAtAllSamples=True`, `PullEndHidden=True`, `SceneChanged=False`다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, 실패 상태 복구를 위한 `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionPullInLoop`, `CaptureDolore05ExecutionPullInLoopDiagnostic`, `InspectDolore05ExecutionPullInLoop`이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 수송자를 걸지 않고 몸통을 관통한 채 회수하는 촉수 각도

- 사용자 지정 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 22-09-04.mp4`를 프레임 단위로 확인했다. 기존 방식은 수송자의 X축 회전을 `Bone_009` 전체에 그대로 더해 뾰족한 끝의 위치 수치는 맞았지만, 끝 직전 촉수 구간이 몸통 앞·옆으로 늘어져 수송자를 꿰뚫기보다 걸어서 끌고 가는 윤곽을 만들었다.
- 수송자 회전 복사 방식을 제거하고 내장 리그의 `Bone_009`를 기하학적으로 보정했다. 매 표본에서 `Bone_002`에서 실제 스킨드 메시 뾰족 정점으로 향하는 끝부분 축을 액자가 바라보는 바깥 방향인 슬롯 로컬 `+Z`에 정렬한 뒤, 동일한 뾰족 정점을 기존 수송자 로컬 관통점에 다시 맞췄다. 따라서 수송자가 X축 `60도 -> 0도`로 자세를 바꾸는 동안에도 촉수 끝 직전 구간은 몸통 뒤쪽에서 앞쪽으로 나오는 관통선에 놓인다.
- 첫 시도에서 끝부분 가까운 `Bone_003`만 회전한 결과 실제 스킨드 메시 방향 오차가 `71.80863도`로 남아 폐기했다. 두 번째 시도는 관통 방향 오차를 `0.2825486도`로 줄였으나, 수송자가 회전·이동한 뒤 기존 관통점의 화면 깊이선이 메시 투영면과 교차하지 않는 표본까지 표면 교차를 요구해 0.5초에서 잘못 실패했다. 두 경우 모두 미저장 장면 상태를 `OpenCargoRunMvpScene`으로 마지막 승인 저장 상태에서 복구했다.
- 최종 전용 검사는 `Result=PASS`다. `TentaclePenetrationDirectionMaximumErrorDegrees=0.2825486`, `TentaclePenetrationSampleCount=20`, 실제 몸통 투영 교차가 존재하는 `TentaclePenetrationCrossingSampleCount=5` 전부에서 `TentaclePreTipRemainsBehindBodySurface=True`를 확인했다. 뾰족 정점의 수송자 로컬 관통점 추종 최대 오차는 `0.00001706m`, 관통 전환 오차는 `0.00000763m`다.
- 수송자 시작 X 회전 `60도`, 액자 최초 진입 `1.064581초`, 진입 시점과 이후 X 회전 `0도`, 균일 배율 `0.2965097`, `PullIn=2초`, 픽셀 클리핑, 종료 숨김과 고리 생성부터 반복하는 구조는 유지했다. 원본과 반입 FBX SHA-256은 각각 `292ED21D21E4FDBCEEC2F515B1FB94F8055D61CBC0A6D2BB16FCC68F8B0381CE`, `AD8C1493915797EE20011174CC1491D8EA37852669C81BDBC0F9916C1283DECD`로 동일하다.
- 실제 Animator 진단 명령은 승인된 1회만 호출했으나 선행 전용 검사 실패로 캡처 생성 전에 중단됐다. 1회 제한을 지켜 수정 후 재실행하지 않았다. 최종 저장 상태 `InspectDolore05ExecutionPullInLoop`는 `SceneChanged=False`로 통과했다.
- 실행한 범위는 지정 영상의 `ffprobe`/`ffmpeg` 분석, `Refresh-UnityProject.ps1`, `ApplyDolore05ExecutionPullInLoop`, 실패 상태 복구용 `OpenCargoRunMvpScene`, 1회 호출 후 중단된 `CaptureDolore05ExecutionPullInLoopDiagnostic`, `InspectDolore05ExecutionPullInLoop`, 원본·반입 FBX SHA-256 확인이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업, 최종 캡처는 실행하지 않았다.

## 후속 완료 - 몸통 앞을 감싸던 회수 촉수 체인을 실제 메시 관통선으로 교체

- 사용자 지정 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 22-35-30.mp4`를 프레임 단위로 확인했다. 직전 구현은 `Bone_002 -> 뾰족 정점`의 짧은 구간만 슬롯 `+Z`로 맞췄기 때문에, 그보다 위의 굵은 체인이 수송자 가슴부터 골반 앞면을 대각선으로 타고 내려와 관통이 아니라 갈고리나 끈으로 잡아 끄는 실루엣을 만들었다.
- 처음에는 `Bone_005 -> Bone_004 -> Bone_003 -> Bone_002 -> 뾰족 정점` 전체를 직선화했으나, 실제 스킨드 메시를 직접 지배하지 않는 명목상 끝 본을 회전시켜 `Bone_002 -> 뾰족 정점`이 1.8초에 `164.2939도` 뒤집혔다. `Bone_001`을 별도 회전한 시도도 1.7초에 `168.9379도` 역전되어 폐기했다. 실패 적용의 미저장 장면은 각각 `OpenCargoRunMvpScene`으로 마지막 저장 상태에서 복구했다.
- 메시 웨이트를 기준으로 뾰족 구간의 실제 주 드라이버가 `Bone_004`임을 확인했다. 최종 구현은 몸통 뒤쪽 진입 본 `Bone_005`와 실제 드라이버 `Bone_004`만 사용한다. `Bone_005 -> Bone_004`를 액자 바깥 방향의 몸통 진입선으로 정렬하고, `Bone_004 -> 실제 뾰족 정점`을 같은 방향으로 반복 보정한 뒤 `Bone_009` 위치로 기존 수송자 로컬 관통점을 추종한다. 따라서 굵은 말단 메시가 수송자 앞면을 타지 않고 몸통 뒤와 내부를 지나 앞쪽 끝으로 이어진다.
- 최종 저장 상태 검사는 `Result=PASS`, `TentaclePenetrationChainMaximumErrorDegrees=0.3690354`, `TentaclePenetrationSampleCount=20`, `TentaclePenetrationCrossingSampleCount=5`, `TentacleEntryBoneRemainsBehindBodyBackSurface=True`다. 뾰족 정점의 회수 중 최대 추종 오차는 `0.000007633m`, 관통 자세에서 회수 시작 자세로 넘어갈 때의 오차는 `0.000000119m`다.
- 수송자 시작 X 회전 `60도`, 액자 최초 진입 `1.064581초`, 진입 이후 X 회전 `0도`, 균일 배율 `0.2965097`, `PullIn=2초`, 픽셀 클리핑, 종료 숨김과 고리 생성부터 반복하는 구조는 유지했다. 원본과 반입 FBX SHA-256은 각각 `292ED21D21E4FDBCEEC2F515B1FB94F8055D61CBC0A6D2BB16FCC68F8B0381CE`, `AD8C1493915797EE20011174CC1491D8EA37852669C81BDBC0F9916C1283DECD`로 동일하다.
- 저장 상태 검사 통과 뒤 승인된 실제 Animator 진단 캡처를 1회 실행해 `ActualAnimatorStates=6`, `SceneChanged=False`를 확인했다. 이 전용 진단 카메라는 기존 `PierceStanding`에서도 촉수 재질이 보이지 않는 구도라 사용자 영상과 같은 촉수 외형 비교에는 사용하지 않았고, 추가 캡처는 실행하지 않았다.
- 실행한 범위는 지정 영상의 `ffprobe`/`ffmpeg` 분석, `Refresh-UnityProject.ps1`, `ApplyDolore05ExecutionPullInLoop`, 실패 상태 복구용 `OpenCargoRunMvpScene`, `InspectDolore05ExecutionPullInLoop`, 최종 `CaptureDolore05ExecutionPullInLoopDiagnostic` 1회, 원본·반입 FBX SHA-256 확인이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 후속 완료 - 수송자 로컬 몸통 축을 따르는 연속 관통 촉수선

- 사용자 지정 영상 `Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-07-22 22-53-30.mp4`를 프레임 단위로 확인했다. 직전 슬롯 `+Z` 기준 관통선은 누운 수송자 메시의 실제 앞뒤 축과 다르므로 촉수가 몸통 앞면을 타고 골반 쪽을 잡는 실루엣이 됐다. `transfer lying.fbx`의 몸 길이는 로컬 Z, 몸통 두께와 가슴 앞 방향은 로컬 `+Y`임을 기준으로 교체했다.
- 관통 시점의 수송자는 X `60도`이므로 몸통 로컬 `+Y`는 슬롯에서 대략 `(0, 0.5, 0.866)` 방향이고, 수송자가 X `0도`로 펴지면 `(0, 1, 0)` 방향이 된다. 촉수는 고정 슬롯 방향을 사용하지 않고 매 표본의 수송자 로컬 `+Y`를 따라 회전한다.
- 실제 스킨 혼합 결과를 보존하면서 충분한 직선 관통 길이를 만들기 위해 `Bone_008 -> Bone_007 -> Bone_006 -> Bone_005 -> Bone_004 -> 실제 뾰족 정점`을 하나의 연속 관통선으로 정렬했다. 상류 `Bone_009`와 고리 `Bone_010`은 기존 굴곡과 고정 시작점을 유지한다.
- 몸통 중심부 후보를 실제 로컬 Y 메시 단면으로 교차 계산하고, 정렬 후 말단 스킨 길이 안에서 앞뒤 각각 `0.005` 여유를 확보할 수 있는 두께 `0.10` 이하의 단면만 선택했다. 뾰족 끝은 해당 단면의 가슴 앞 표면 밖에 유지되고 `Bone_008`은 포털 진입 전 몸통 뒤 표면 뒤에 유지된다.
- 최종 저장 상태 검사는 `Result=PASS`, `PierceToPullTipError=0.00000006`, `PointedTipFollowMaximumError=0.00001079`, `PointedTipVisibleImpaledSampleCount=11`, `PointedTipRemainsImpaledAtAllSamples=True`, `TentacleBodyLocalNormalMaximumErrorDegrees=0.05233916`, `TentaclePenetrationCrossingSampleCount=11`, `TentacleEntryBoneRemainsBehindTorsoBackSurface=True`, `SceneChanged=False`다. 포털 진입 시점은 `1.098817초`, 진입 시 X 회전은 `0도`다.
- 수송자 균일 배율 `0.2965097`, `PullIn=2초`, 픽셀 클리핑, 종료 숨김과 고리 생성부터 반복하는 구조는 유지했다. 원본과 반입 FBX SHA-256은 서 있는 모델 `292ED21D21E4FDBCEEC2F515B1FB94F8055D61CBC0A6D2BB16FCC68F8B0381CE`, 누운 모델 `AD8C1493915797EE20011174CC1491D8EA37852669C81BDBC0F9916C1283DECD`로 각각 동일하다.
- 저장 상태 검사 통과 뒤 승인된 실제 Animator 진단 캡처를 한 번만 실행해 6개 상태와 `SceneChanged=False`를 확인했다. 전용 진단 카메라는 촉수 렌더러가 화면에 거의 잡히지 않아 관통 외형 판정에는 사용하지 않았으며 추가 캡처는 실행하지 않았다.
- 실행한 범위는 지정 영상의 `ffprobe`/`ffmpeg` 분석, `Refresh-UnityProject.ps1`, `ApplyDolore05ExecutionPullInLoop`, 실패 상태 복구용 `OpenCargoRunMvpScene`, `InspectDolore05ExecutionPullInLoop`, 최종 `CaptureDolore05ExecutionPullInLoopDiagnostic` 1회, 원본·반입 FBX SHA-256 확인이다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 후속 완료 - 뾰족 끝 아래 촉수 구간으로 수송자 관통 위치 이동

- 사용자의 정정에 따라 촉수 뾰족 끝의 길이를 늘리지 않고, 수송자 몸통이 뾰족 끝 아래의 촉수 구간을 관통하도록 기준점을 변경했다. 촉수 각도·길이·타이밍은 변경하지 않았다.
- 직전 구현은 `Bone_004` 지배 뾰족 구간의 중앙 정점을 수송자 앞면에 맞춰 뾰족 구간의 뒤쪽 절반이 몸통 안에 들어갔다. 수정 구현은 현재 관통 각도로 정렬된 뾰족 구간의 가장 뒤쪽 밑동 평면을 계산하고, 수송자 몸통 앞면을 그 밑동보다 로컬 `0.005` 뒤에 배치한다.
- 포털 앞에서 수송자가 완전히 보이는 11개 표본마다 실제 스킨 정점으로 뾰족 구간 밑동을 다시 계산했다. 최종 검사는 `PointedBaseMinimumFrontClearance=0.004997719`, `PointedBaseVisibleSampleCount=11`, `PointedBaseExposedAtAllVisibleSamples=True`로 통과해 뾰족 부분 전체가 몸통 앞에 남고 그 아래 촉수선이 관통함을 확인했다.
- 기존 관통선은 유지됐다. `PierceToPullTipError=0.00000006`, `PointedTipFollowMaximumError=0.000007633`, `TentacleBodyLocalNormalMaximumErrorDegrees=0.05233916`, `TentacleEntryBoneRemainsBehindTorsoBackSurface=True`다. 수송자 시작 X 회전 `60도`, 포털 진입 X 회전 `0도`, `PullIn=2초`, 균일 배율 `0.2965097`, 포털 클리핑·종료 숨김·고리 생성부터 반복도 유지했다.
- `Refresh-UnityProject.ps1`, `ApplyDolore05ExecutionPullInLoop`, `InspectDolore05ExecutionPullInLoop`만 실행했고 최종 결과는 `Result=PASS`, `SceneChanged=False`다. 진단·최종 캡처, `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, Git 작업은 실행하지 않았다.

## 후속 완료 - 가슴 관통 상태의 수송자 상체·팔다리 저항 동작

- `transfer lying.fbx`의 실제 리그 계층을 사용해 골반과 수송자 전체 회전은 유지하고 `Spine02`, `Spine01`, `Spine`에 각각 `16도`, `14도`, `10도`를 분산해 상체만 합계 약 `40도` 세웠다. 관통 단면도 고정 몸 길이 비율 대신 실제 `Spine` 뼈 높이에서 찾도록 변경해 골반이 아니라 가슴을 촉수선이 통과하게 했다.
- 양팔은 수송자의 올라간 시선 방향으로 직선 정렬하고 좌우 `5도` 반대 위상으로 흔들며, 양다리는 좌우 `8도` 반대 위상으로 움직이게 했다. 동작 주기는 `0.5초`이고 관통 직후부터 포털 진입까지 반복한다. 촉수 추종 곡선은 이 리그 동작이 적용된 몸통 로컬 관통점을 다시 표본화하므로 회수 중에도 가슴 관통을 유지한다.
- 전용 검사 결과는 `UpperBodyRiseDegrees=39.99989`, `MaximumArmStraightnessErrorDegrees=0.02797646`, 팔 최고점 `+4.998457/-5.001004도`, 다리 최고점 `+7.125877/-7.443025도`, 후반 팔 흔들림 `5.001004도`, 반복 자세 오차와 시작·종료 뼈 자세 오차 모두 `0도`, `ChestLengthFractionError=0.07500097`이다.
- 기존 조건도 유지됐다. `PierceToPullTipError=0.00000006`, `PointedTipFollowMaximumError=0.00001079`, `PointedBaseMinimumFrontClearance=0.004994333`, `TentacleBodyLocalNormalMaximumErrorDegrees=0.05233916`, 수송자 시작 X 회전 `60도`, 포털 진입 X 회전 `0도`, 균일 배율 `0.2965097`, `PullIn=2초`, 포털 클리핑과 종료 숨김이 모두 통과했다.
- 첫 전용 적용은 실제 다리 표면 각도 `7.125877/7.443025도`를 설정값 8도에 과도하게 가까이 요구한 검사 하한 때문에 중단됐다. 동작값은 바꾸지 않고 반대 위상 동작을 충분히 판별하는 `6.5도` 하한으로 검사만 수정했으며, 실패 중 미저장 장면은 승인된 `OpenCargoRunMvpScene`으로 마지막 저장 상태에서 복구했다.
- 최종 저장 상태 검사는 `Result=PASS`, `SceneChanged=False`다. 검사 통과 뒤 실제 Animator 진단 캡처를 한 번만 실행해 `ActualAnimatorStates=6`, `SceneChanged=False`를 확인했다. 진단 카메라의 정면 구도에서는 팔의 전방 신전이 축약되어 보이므로 최종 판정은 실제 뼈 방향과 반복 표본 수치를 함께 사용했다.
- 실행한 전용 명령은 `Refresh-UnityProject.ps1`, 실패 상태 복구용 `OpenCargoRunMvpScene`, `ApplyDolore05ExecutionPullInLoop`, `InspectDolore05ExecutionPullInLoop`, 최종 `CaptureDolore05ExecutionPullInLoopDiagnostic` 1회다. `Run-HarnessValidation.ps1` 및 Harness 검증 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, FBX 수정, Git 작업은 실행하지 않았다.
