# 진행 기록 — 2026-09-15

## Lightsaber_Thrust 오른팔 메시 떨림 수정

- 적용 대상: `Lightsaber_Thrust`
- 적용 애니메이션: `LightsaberThrust_Stabbing_SourceExact`, `LightsaberThrust_EnterLast_To_StabbingFirst_0_2s`, `LightsaberThrust_StabbingLast_Hold_0_5s`
- 변경 내용:
  - 기존 손 위치와 찌르기 동작을 유지한 채 오른쪽 팔꿈치의 굽힘면을 프레임 간 연속되게 선택했습니다.
  - 상완·전완의 뼈 길이축 롤 회전을 인접 프레임과 이어지도록 정규화해 메시 떨림을 제거했습니다.
  - 원본 구간, 마지막 홀드, 반복 브리지의 오른팔 회전 접점을 일치시켰습니다.
  - 광선검의 오른손 추종, 기존 자세, 원본 동작 시간과 반복 구조는 유지했습니다.

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyLightsaberThrustArmPopCorrection ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectLightsaberThrustArmPopCorrection ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command AssertNoUnityConsoleErrors ...`

## 검증 결과

- 1순위 직접 확인: 사용자 제공 영상과 수정 후 Unity 실행 모드 연속 녹화를 같은 오른팔 확대 구도와 자연 재생 속도로 비교했습니다. 이전 영상의 상완 메시 떨림과 전완 축 뒤집힘이 제거되고 손·팔꿈치가 한 굽힘면을 따라 이어지는 것을 확인했습니다.
- 연속 재생 기록: `Temp/LightsaberThrust/JitterFinalContinuous.mp4`
- 보조 검증: 전체 반복 2회, 실제 게임 프레임 54개를 연속 추적했습니다.
- 보조 수치: 전체 최대 오른팔 회전 간격 `18.5265026°/60Hz`, 반복 경계 `0.318810135°/60Hz`, 오른손 추종 위치·회전 오차 `0`입니다.
- 정적 보조 수치: 원본 찌르기 구간 최대 `19.248°/프레임`, 브리지 최대 `23.49689°/프레임`입니다.
- 원본 보호: `player model/transfer lightsaber stabbing.fbx`와 프로젝트 내 복사본의 SHA-256이 `A4A6E1A687E594ACA0486B117648581205F6F22A15CD12F40114003BB23B0D25`로 일치합니다.
- Unity 콘솔: 오류 `0`, 경고 `0`을 확인했습니다.

## 실행하지 않은 검증과 작업

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 메시·렌더러·머티리얼·VFX 및 원본 FBX 수정
- 진행 HTML 문서 수정
- Git 커밋 및 푸시

## ElectricMine 3개 상태 모델·파지 적용

- 적용 대상:
  - `ElectricMine_Idle`
  - `ElectricMine_Activate`
  - `ElectricMine_Armed_Idle`
- 원본 모델: `item model/electric current mine.fbx`
- 원본 및 프로젝트 복사본 SHA-256: `033FE11268E4C36AA6BEA9AE79A4E8C83C802DED928E28E5C9EFB906EE40631F`
- 변경 내용:
  - 원본 FBX를 값 변경 없이 `Assets/_Project/Art/Items/ElectricMine/ElectricCurrentMine.fbx`로 복사했습니다.
  - 내장 텍스처 4개와 내장 머티리얼 1개를 모두 외부화하고 URP Lit에 연결했습니다. 메탈릭·러프니스 원본 채널은 Unity용 메탈릭·스무드니스 채널 어댑터에 결합했습니다.
  - 기획 기준에 따라 지뢰 지름을 `0.30m`로 맞췄습니다.
  - 세 개체 모두 오른손 자식으로 직접 배치해 런타임 보정 스크립트 없이 오른손을 추종하게 했습니다.
  - 사용자가 표시한 외곽 측면·아래쪽을 쥐고 상면 중앙의 빨간 스위치를 수송자가 볼 수 있도록 오른팔·아래팔·손목 회전을 분배했습니다.
  - 기존 캐릭터의 닫힌 손 파지 리깅을 기준으로 오른손가락 15개 본을 모두 사용했습니다.

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity Play Mode 자연 재생에서 세 개체를 전신과 오른손 확대 구도로 나란히 확인했습니다. 오른팔은 자연스럽게 굽혀지고 손목이 꺾이거나 소매·지뢰에 파고들지 않았으며, 오른손과 손가락이 표시된 외곽 부위를 감싸고 지뢰 상면과 빨간 스위치가 수송자 쪽으로 노출됐습니다. 세 개체 모두 메시 뒤틀림과 가시적 관통 없이 동일한 파지 상태로 통과했습니다.
- 최종 직접 확인 이미지: `docs/validation/ElectricMineSetup/Final.png`
- 자연 재생 확인 시간: `2.5초`
- 검증 중 대상 조작: 없음 (`Animator.Play`, `Animator.Rebind`, 강제 시간 지정 없음)
- 오른손 추종 위치·회전·스케일 최대 오차: 모두 `0`
- 손바닥–외곽 파지 접촉 오차: `0m`
- 손목 직선성 오차: `0°`
- 팔꿈치 굽힘: `73.25629°`
- 스위치 가시 방향 일치값: `0.969094336`
- 모델 지름: `0.299999952m`
- 내장 텍스처 외부화 수: `4`
- 내장 머티리얼 외부화 수: `1`
- 원본 FBX 변경: 없음
- 대상 외 씬 개체 변경: 없음
- Unity 콘솔: 오류 `0`, 경고 `0`

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectElectricMineSetupSources ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyElectricMineSetup ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectElectricMineSetup ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureElectricMineSetupFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command AssertNoUnityConsoleErrors ...`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 원본 FBX 및 대상 외 모델·메시·렌더러·머티리얼·애니메이션 수정
- 진행 HTML 문서 수정
- Git 커밋 및 푸시

## ElectricMine_Activate 빨간 버튼 누르기 모션

- 적용 대상: `ElectricMine_Activate`
- 적용 모델: 기존 `RightHand/ElectricMine_Prop/ElectricCurrentMine_Model`
- 변경 내용:
  - 현재 전신 자세와 오른손 파지·지뢰 로컬 상태를 보존한 상태에서, 왼팔 두 관절과 왼손 검지 3관절 리깅으로 빨간 중앙 버튼 접촉 자세를 계산했습니다.
  - 실제 씬 대상을 계산 중 조작하지 않도록 동일 개체의 임시 복제본에서 접촉 자세를 산출한 뒤 회전 커브로 구웠습니다.
  - 검지는 버튼을 향해 펴고 중지·약지·소지·엄지는 손바닥 쪽으로 말아 검지만 버튼에 닿는 실루엣으로 구성했습니다.
  - `0.4초` 접근, `0.2초` 접촉 유지, `0.5초` 복귀 후 전체 `1.1초`를 반복하는 클립과 단일 상태 컨트롤러를 연결했습니다.
  - 버튼 모델 자체에는 애니메이션 커브를 연결하지 않았습니다.

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectElectricMineActivateSources ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyElectricMineActivateAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectElectricMineActivateAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureElectricMineActivateAnimationFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command AssertNoUnityConsoleErrors ...`

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity Play Mode 자연 재생에서 시작·접근·버튼 접촉·접촉 유지·복귀·복귀 완료를 전신과 버튼 정면 확대 구도로 확인했습니다. 왼손 검지만 빨간 중앙 버튼에 닿고 다른 손가락은 손바닥 쪽으로 말려 있으며, 왼팔·손목의 부자연스러운 꺾임, 몸통 관통, 메시 뒤틀림이 보이지 않았습니다.
- 최종 직접 확인 이미지: `docs/validation/ElectricMineActivate/Final.png`
- 최종 이미지 SHA-256: `62CAA68D229D3D46877AC36A94E5B8DFB651D1E6346877E0202E61F4BF954BDC`
- 자연 재생 검증 중 대상 조작: 없음 (`Animator.Play`, `Animator.Rebind`, 강제 재생 시간 지정 미사용)
- 자연 재생 반복 확인: `2회`, 관측 주기 `1.14410615초`
- 검지 끝–버튼 중심 보조 거리: 양 회차 `0.0110083306m`
- 복귀 자세 최대 편차: `0.5510728°`
- 오른팔 파지 최대 편차: `0°`
- 지뢰 오른손 추종 위치·회전·스케일 최대 오차: 모두 `0`
- 버튼 모델 애니메이션: 없음
- Unity 콘솔: 오류 `0`, 경고 `0`, 로그 `6`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 전류 지뢰 모델·텍스처·머티리얼·버튼 모델 변형
- `ElectricMine_Idle`, `ElectricMine_Armed_Idle` 및 다른 개체 수정
- 진행 HTML 문서 수정
- Git 커밋 및 푸시

## ElectricMine_Activate 왼팔 점멸·불연속 수정

- 적용 대상: `ElectricMine_Activate`의 왼팔·왼손 버튼 누르기 전체 반복 모션
- 사용자 확인 영상: `C:/Users/gus68/Videos/Captures/Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-09-15 21-35-22.mp4`
- 원인:
  - 원본 영상 `3.84~4.02초` 구간에서 버튼 접촉 자세가 복귀할 때 왼쪽 아래팔이 한두 렌더 프레임 사이에 바뀌는 현상을 직접 확인했습니다.
  - 생성 클립의 `0.2500→0.2542초` 사이에서 `LeftForeArm` 로컬 회전이 `135.635531°` 변했습니다.
  - 지뢰 관통을 막는 조건부 2본 IK가 처음 개입하는 시점에 다른 팔꿈치 해로 전환된 것이 원인이며, 복귀가 접근 경로를 역순 사용하므로 같은 불연속이 복귀 중에도 재현됐습니다.
- 변경 내용:
  - 기존에 무관통 검증을 통과한 자세의 공간 순서와 시작·버튼 접촉 자세는 유지했습니다.
  - 기존 안전 자세 경로를 인접 자세의 누적 회전 거리로 재샘플링해 큰 회전 구간에 더 많은 키를 배정했습니다.
  - 접근과 복귀에는 최고 속도를 제한한 완만한 이징을 적용하고, `0.5초` 복귀 구간의 마지막 `0.05초`는 정확한 시작 자세로 정착시켰습니다.
  - 직접 캡처 부하가 자연 재생 판정에 섞이지 않도록 캡처를 서로 다른 반복 주기에 한 번씩 분산하고, 이후 별도의 두 주기를 캡처 없이 관찰하도록 검증 흐름을 수정했습니다.
  - 버튼, 오른팔 파지, 오른손 자식 지뢰의 Transform과 다른 씬 개체는 변경하지 않았습니다.

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity 실행 모드의 실제 렌더링에서 시작 → 접근 → 버튼 접촉 → 접촉 유지 → 복귀 → 복귀 완료를 같은 구도로 확인했습니다. 왼팔이 사라지거나 반대 자세로 튀는 프레임이 없고, 검지만 빨간 버튼에 닿으며 왼손·엄지·손목이 지뢰 메시를 관통하지 않았습니다.
- 최종 직접 확인 이미지: `docs/validation/ElectricMineActivateLeftArmContinuity/Final.png`
- 자연 재생: 강제 시간 이동 없이 캡처 후 별도의 `2주기`를 확인했으며 관찰 주기 시간은 `1.09845초`입니다.
- 최대 인접 왼팔 회전: 수정 전 `135.635531°/240Hz`, 수정 후 `4.671805°/240Hz`
- 수정 후 최대 회전 구간: `LeftForeArm`, `0.3375→0.341667초`
- 두 주기 버튼 접촉 거리: 각각 `0.006343334m`
- 실행 모드 최소 비검지 손-지뢰 표면 여유: `+0.000460975m`
- 480Hz 구조 검사 최소 표면 여유: `+0.000002585m`
- 시작 자세 복귀 오차: `0°`
- 오른팔 회전 편차: `0°`
- 지뢰 로컬 위치·회전·스케일 오차: 모두 `0`
- Unity 콘솔: 오류 `0`, 경고 `58`

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyElectricMineActivateAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectElectricMineActivateAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureElectricMineActivateAnimationFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command AssertNoUnityConsoleErrors ...`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 전류 지뢰 모델·텍스처·머티리얼·버튼 및 오른팔 수정
- 다른 개체·애니메이션·진행 HTML 수정
- 기존 `docs/validation/ElectricMineActivateNoOverlap/**` 결과 덮어쓰기
- Git 커밋 및 푸시

## 남은 확인 사항

- 현재 승인 범위 안에서 남은 작업 없음

## ElectricMine_Activate 왼손·전류 지뢰 관통 수정

- 적용 대상: `ElectricMine_Activate`
- 사용자 확인 영상: `C:/Users/gus68/Videos/Captures/Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1)_ _DX12_ 2026-09-15 21-05-08.mp4`
- 원인:
  - 기존 클립은 최종 검지 접촉 거리만 검사하고 손바닥·손목·나머지 손가락의 지뢰 표면 여유 거리를 검사하지 않았습니다.
  - 시작 자세와 접촉 자세를 뼈별 Quaternion Slerp로 바로 보간해 약 `0.26초`의 접근 구간에서 왼손이 지뢰 메시 내부를 통과했습니다.
- 변경 내용:
  - 손목을 지뢰 전면 안전면과 외곽 방향으로 우회시키고 손바닥 면을 지뢰 전면과 평행하게 정렬한 뒤, 왼손 검지 끝만 빨간 버튼에 접촉하도록 접촉 자세를 다시 계산했습니다.
  - 시작→접촉 경로를 `240Hz` 키로 순차 계산하고, 각 자세에서 왼손을 지뢰 표면 바깥으로 보정했습니다. 복귀는 검증된 접근 경로를 역방향으로 사용해 같은 관통 방지 경로를 유지했습니다.
  - 손바닥·손목·엄지·중지·약지·소지의 뼈 구간에 보수적인 반경을 둔 표면 여유 거리 검사를 추가하고, `480Hz` 보조 표본과 Unity 자연 재생 전 구간에서 음수 여유 거리를 실패로 판정하도록 했습니다.
  - 기존 `0.4초` 접근, `0.2초` 접촉, `0.5초` 복귀와 전체 반복 구조를 유지했습니다.
  - 오른팔 파지, `RightHand/ElectricMine_Prop` 추종 계층, 전류 지뢰 Transform·모델·텍스처·머티리얼과 버튼 정적 상태는 변경하지 않았습니다.

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyElectricMineActivateAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectElectricMineActivateAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureElectricMineActivateAnimationFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command AssertNoUnityConsoleErrors ...`

## 직접 확인 및 보조 검증

- 1순위 직접 확인: 사용자 영상과 같은 전류 지뢰 정면 확대 구도에서 Unity Play Mode 자연 재생을 두 번 반복해 시작·접근 중간·접촉·접촉 유지·복귀 초반·복귀 완료를 확인했습니다. 접근과 복귀 중 왼손이 지뢰 뒤로 파고들거나 사라지는 구간이 없고, 접촉 중에는 검지 끝만 빨간 버튼에 닿으며 손바닥·손목·나머지 손가락은 지뢰 전면 바깥에 유지됐습니다.
- 최종 직접 확인 이미지: `docs/validation/ElectricMineActivateNoOverlap/Final.png`
- 자연 재생 중 대상 조작: 없음 (`Animator.Play`, `Animator.Rebind`, 강제 재생시간 지정 미사용)
- 자연 재생 순환: `2회`, 관측 순환 시간 `1.09766936초`
- 검지 끝 최소 버튼 접촉 거리: 두 순환 모두 `0.00634333445m`
- 비검지 손 부위 최소 표면 여유 거리: 자연 재생 `0.000157870352m`, `480Hz` 보조 표본 `0.00029328838m`
- 오른팔 파지 최대 회전 편차: `0°`
- 전류 지뢰 로컬 위치·회전·스케일 최대 오차: 모두 `0`
- Unity 콘솔: 오류 `0`, 경고 `0`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- EditMode·PlayMode 테스트 및 Windows Build 관련 작업
- Unity 에디터 재시작
- 전류 지뢰 모델·메시·텍스처·머티리얼·버튼 수정
- 오른팔 파지와 전류 지뢰 Transform 수정
- 다른 개체 및 애니메이션 수정
- 진행 HTML 문서 수정
- Git 커밋 및 푸시

## 남은 확인 사항

- 현재 승인 범위 안에서 남은 작업 없음

## ElectricMine_Idle 2D 이동 모션 순환

- 적용 대상: `ElectricMine_Idle`
- 원본 모션:
  - `Player_Idle`
  - `Player_Walk_Forward`
  - `Player_Walk_Backward`
  - `Player_Sidestep`
  - `Player_Walk_Diagonal`
  - `Player_Run_Forward`
- 변경 내용:
  - 다섯 원본 `AnimationClip`의 곡선·이벤트·클립 설정을 값 변경 없이 복사했습니다.
  - `Player_Walk_Diagonal`의 기존 두 자식 모션과 임계값을 가진 `Simple1D` Blend Tree를 그대로 복제했습니다.
  - 여섯 모션을 상위 `FreeformCartesian2D` Blend Tree에 연결하고 `Idle → WalkForward → WalkBackward → Sidestep → WalkDiagonal → RunForward`를 각 1초씩 재생한 뒤 처음부터 반복하도록 구성했습니다.
  - 현재 `ElectricMine_Idle`의 `RightArm` 이하 18개 본 회전을 그대로 캡처한 전용 Override 레이어로 파지를 유지했습니다. 척추와 `RightShoulder`는 원본 이동 모션을 그대로 통과시켜 상체 흔들림이 유지됩니다.
  - 전류 지뢰의 현재 로컬 위치·회전·스케일과 `RightHand/ElectricMine_Prop` 추종 계층은 변경하지 않았습니다.
  - 공용 Unity 브리지가 전용 명령을 먼저 소비하는 경쟁 조건에서도 완성된 `Unknown bridge command` 로그를 다시 회수하도록 전용 브리지의 최근 로그 재확인 경로를 보완했습니다.

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectElectricMineIdleLocomotionSources ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyElectricMineIdleLocomotion ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectElectricMineIdleLocomotion ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureElectricMineIdleLocomotionFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command AssertNoUnityConsoleErrors ...`

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity Play Mode 자연 재생에서 대기·전진·후진·옆걸음·대각선·달리기를 전신과 오른팔·지뢰 확대 구도로 나란히 확인했습니다. 여섯 하체 동작이 구분되고 상체·어깨가 동작에 따라 자연스럽게 흔들리면서도 오른팔·손목·손가락 파지가 풀리지 않았습니다. 지뢰 이탈, 메시 뒤틀림, 새 관통은 보이지 않았습니다.
- 최종 직접 확인 이미지: `docs/validation/ElectricMineIdleLocomotion/Final.png`
- 최종 이미지 SHA-256: `ADFBF8ECC9028824E827A3D6EDD8D40DC2E885FA108B80D74D5036E65E4FFDDD`
- 자연 재생 검증 중 대상 조작: 없음 (`Animator.Play`, `Animator.Rebind`, 강제 재생 시간 지정 미사용)
- 순환 횟수: `2회`, 달리기 종료 뒤 대기 복귀 확인
- 관측 전환 간격: 최소 `0.9426934초`, 최대 `1.06315255초`
- 오른팔·손목·손가락 파지 최대 편차: `0°`
- 척추 자연 흔들림: 최대 `18.5733757°`
- 오른쪽 어깨 자연 흔들림: 최대 `37.64201°`
- 지뢰 오른손 추종 위치·회전·스케일 최대 오차: 모두 `0`
- 원본 모션 곡선·이벤트 및 대각선 Blend Tree 변경: 없음
- Root Motion 적용: 없음
- Unity 콘솔: 오류 `0`, 로그 `0`. 전체 경고 `58`건은 이 작업 전 Detector 검증 때부터 존재하던 타 파일의 기존 컴파일 경고이며, `ElectricMineIdleLocomotion` 구현에서 새로 남은 경고는 `0`건입니다. 현재 적용본의 파지 편차는 `0°`입니다.

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 원본 여섯 애니메이션 개체·에셋 수정
- `ElectricMine_Activate`, `ElectricMine_Armed_Idle` 수정
- 전류 지뢰 모델·Transform·메시·렌더러·머티리얼·텍스처 수정
- 진행 HTML 문서 수정
- Git 커밋 및 푸시

## 남은 확인 사항

- 현재 승인 범위 안에서 남은 작업 없음

## Detector_Attached_Static 2D 이동 모션 순환

- 적용 대상: `Detector_Attached_Static`
- 원본 모션:
  - `Player_Idle`
  - `Player_Walk_Forward`
  - `Player_Walk_Backward`
  - `Player_Sidestep`
  - `Player_Walk_Diagonal`
  - `Player_Run_Forward`
- 변경 내용:
  - 원본 다섯 `AnimationClip`의 곡선·이벤트·클립 설정을 그대로 복사했습니다.
  - `Player_Walk_Diagonal`의 기존 두 자식 모션과 임계값을 가진 1D Blend Tree를 그대로 복제했습니다.
  - 복제한 여섯 모션을 상위 `FreeformCartesian2D` Blend Tree에 연결하고 `Idle → WalkForward → WalkBackward → Sidestep → WalkDiagonal → RunForward`를 각 1초씩 재생한 뒤 처음부터 반복하도록 구성했습니다.
  - Root Motion은 끄고 대상의 Transform, 렌더러, 메시, 머티리얼과 머리 자식 `PresenceDetector_Attached`의 로컬 위치·회전·스케일 및 외형은 변경하지 않았습니다.

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectDetectorAttachedStaticLocomotionSources ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyDetectorAttachedStaticLocomotion ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectDetectorAttachedStaticLocomotion ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureDetectorAttachedStaticLocomotionFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command AssertNoUnityConsoleErrors ...`

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity 실행 모드의 자연 재생을 두 번 순환시켜 12패널로 확인했습니다. 여섯 동작이 지정 순서로 식별되고 수송자 메시 소실·루트 이탈·눈에 띄는 메시 왜곡이 없으며 탐지기가 모든 자세에서 머리를 추종했습니다.
- 최종 직접 확인 이미지: `docs/validation/DetectorAttachedStaticLocomotion/Final.png`
- 자연 재생 검증 중 대상 조작: 없음 (`Animator.Play`, `Animator.Rebind`, 강제 재생시간 지정 미사용)
- 관측 전환 간격: 최소 `0.9721698초`, 최대 `1.01007116초`
- 순환 횟수: `2회`, 달리기 종료 뒤 대기 복귀 확인
- Root Motion 적용: 없음
- 탐지기 머리 기준 로컬 위치·회전·스케일 최대 오차: 모두 `0`
- 원본 모션 곡선·이벤트·대각선 Blend Tree 변경: 없음
- Unity 콘솔: 오류 `0`, 경고 `0`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 원본 여섯 애니메이션 개체·에셋 수정
- AvatarMask·리타게팅·포즈 보정·신규 모션 곡선 생성
- 탐지기 모델·텍스처·머티리얼·Transform 수정
- 진행 HTML 통과 처리
- Git 커밋 및 푸시

## 남은 확인 사항

- 현재 승인 범위 안에서 남은 작업 없음

## Lightsaber_Thrust 오른쪽 아래팔 압착 변형 수정

- 적용 대상: `Lightsaber_Thrust`
- 원인:
  - 사용자 표시 구간과 동일한 오른쪽 팔꿈치·아래팔을 원본 Mixamo 및 현재 보정 클립의 같은 프레임으로 비교했습니다.
  - 기존 프레임 연속화가 아래팔 길이축 회전을 누적 고정하면서 약 `179.302°` 반전시켰고, 같은 `char1` 스킨 메시의 최소 두께가 원본 대비 `16.7314%`까지 압착됐습니다.
  - 팔꿈치 굽힘과 공용 스킨 웨이트는 주원인이 아니었습니다.
- 변경 내용:
  - `LightsaberThrust_Stabbing_SourceExact`를 보호된 원본 Mixamo 복사본에서 다시 생성하게 했습니다.
  - 상완·아래팔의 길이축 회전은 원본 프레임의 롤을 보존하고, 기존 손 위치·손 자세·광선검 추종 조건에 필요한 방향 보정만 적용하게 했습니다.
  - 마지막 홀드와 `0.2초` 반복 브리지의 오른팔 접점을 새 원본 기반 보정 결과에 다시 맞췄습니다.
  - 공용 `player.fbx`, `char1` 메시, 스킨 웨이트, 렌더러, 머티리얼과 씬 정적 자세는 변경하지 않았습니다.

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity 실행 모드의 자연 재생 전체 루프에서 사용자 이미지와 같은 오른쪽 팔꿈치·아래팔 부위를 측면 확대했습니다. 이전의 삼각형 압착과 뒤집힌 주름이 사라지고 어깨에서 손목까지 연속된 두께와 매끄러운 외곽선이 유지되는 것을 확인했습니다.
- 최종 직접 확인 이미지: `Assets/_Project/Art/Player/Animations/LightsaberThrust/Review/LightsaberThrust_ForearmDeformation_Final.png`
- 원본 대비 최소 두께 비율: `0.864459` (`0.098997m` 대비 `0.085579m`)
- 원본 대비 최대 아래팔 축 회전 차이: `32.069°` (수정 전 `179.302°`)
- 실행 모드 측정 최소 아래팔 두께: `0.105427m`
- 자연 재생 연속 회전 최대 변화: `19.871°/60Hz`
- 반복 경계 회전 변화: `4.448°/60Hz`
- 광선검 손잡이 오른손 추종 위치·회전 오차: `0`
- 원본 및 프로젝트 복사본 SHA-256: `A4A6E1A687E594ACA0486B117648581205F6F22A15CD12F40114003BB23B0D25`
- Unity 콘솔: 오류 `0`, 경고 `0`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 공용 플레이어 메시·스킨 웨이트·원본 FBX 수정
- 다른 개체, 머티리얼, 텍스처, VFX, 진행 HTML 수정
- Git 커밋 및 푸시

## Lightsaber_Thrust 오른쪽 손목 표시창 방향 수정

- 적용 대상: `Lightsaber_Thrust`의 오른쪽 아래팔과 손목 표시창
- 원인:
  - 오른손등이 수송자 기준 오른쪽을 향하도록 보정된 상태에서도 `RightForeArm`의 길이축 회전은 손 방향과 독립적으로 남아 있었습니다.
  - 그 결과 손바닥이 왼쪽을 향하는 구간의 표시창 면이 오른쪽을 향하지 않고 최대 `130.5145°` 어긋났습니다.
- 변경 내용:
  - `LightsaberThrust_EnterLast_To_StabbingFirst_0_2s`, `LightsaberThrust_Stabbing_SourceExact`, `LightsaberThrust_StabbingLast_Hold_0_5s`에서 손바닥이 왼쪽을 향하는 구간만 아래팔 길이축 회전을 부드럽게 보정했습니다.
  - 아래팔 보정 직후 오른손의 월드 회전을 원래 값으로 복원해 손 위치, 손바닥 방향, 손 자세와 광선검 추종을 유지했습니다.
  - 상완, 광선검, 다른 뼈, 컨트롤러, 씬 정적 자세와 기존 파생 메시에는 손대지 않았습니다.
  - 기존 팔 회전 변화량 검증이 의도적인 아래팔 축 보정까지 떨림으로 오인하지 않도록, 이번 표시창 검증은 전 구간 표시창 방향·손 추종·직접 확대 확인을 별도 조건으로 판정하게 했습니다.

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity 실행 모드의 자연 재생 전체 반복에서 사용자 이미지와 같은 오른쪽 손목 표시창 부위를 측면 확대했습니다. 표시창 면이 수송자 기준 오른쪽으로 노출되고, 표시창 주변 메시가 새로 접히거나 비틀리지 않은 것을 확인했습니다.
- 최종 직접 확인 이미지: `Assets/_Project/Art/Player/Animations/LightsaberThrust/Review/LightsaberThrust_WristPanelOrientation_Final.png`
- 자연 재생 측정: 손바닥이 왼쪽을 향한 `125`개 실제 재생 프레임에서 표시창 오른쪽 방향 최대 오차 `0.5791218°`
- 정적 전 구간 측정: 관련 `100`개 표본에서 표시창 오른쪽 방향 최대 오차 `0°` (수정 전 `130.5145°`)
- 보호된 동작 변화량: 오른손 위치 `0m`, 오른손 회전 `0°`, 상완 회전 `0°`, 광선검 위치 최대 `0.000001m`, 광선검 회전 `0°`
- 광선검 손잡이 오른손 추종 위치·회전 오차: `0`
- 반복 시간 유지: 중간 동작 `0.2초`, 마지막 자세 유지 `0.5초`
- 컨트롤러 SHA-256 유지: `0B6EA60294C2388902A30EEF1AEEDF502566A3A1C25BB4C2C8B0587950F88494`
- 기존 파생 메시 SHA-256 유지: `CA98681FEDE4381AA38350AC43B24C35FC6A1ADD1C8A6B9C60B20CC204AF8EE6`
- Unity 콘솔: 오류 `0`, 경고 `0`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 공용 플레이어 메시·스킨 웨이트·원본 FBX, 렌더러, 머티리얼 수정
- 다른 개체, 다른 애니메이션 동작, 진행 HTML 수정
- Git 커밋 및 푸시

## Lightsaber_Thrust 광선검 상부 파지 및 손목 관절 수정

- 적용 대상: `Lightsaber_Thrust` 전체 반복 구간의 오른손·오른손가락·오른쪽 아래팔과 `Lightsaber_Prop`
- 원인:
  - 최초 보정은 손잡이 길이축을 손바닥 가로축에 강제로 맞추고 손을 기존 손잡이 위치로 끌어당겨, 손목 굽힘이 최대 `151.0463°`까지 발생하고 오른손 메시가 소매 안으로 파고들었습니다.
  - 손 회전 도중 자식인 광선검이 임시로 이동한 위치를 접촉점 계산에 사용한 순서 오류도 있어 손바닥과 손잡이가 최대 `0.146824m` 떨어졌습니다.
- 변경 내용:
  - 실패 결과 위에 보정을 누적하지 않도록 보호된 Mixamo 찌르기 소스와 승인된 시작·유지 자세에서 세 클립의 기준을 다시 구성했습니다.
  - 원본 어깨·팔꿈치·오른손 위치와 찌르기 동작은 유지하고, 손목 굽힘을 제거한 뒤 손바닥을 아래로 돌리는 회전의 `75%`를 아래팔 회내 회전으로 분배했습니다.
  - `Flashlight_Idle`의 기존 주먹 파지 손가락 리깅을 그대로 복사해 전체 구간에 유지했습니다.
  - 광선검 각도는 유지하고 손잡이 위치만 오른손의 상부 파지 접촉점으로 최대 `0.108325m` 재중심화했습니다.

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity 실행 모드의 자연 재생 전체 반복에서 사용자 지정 오른손·손잡이 부위를 측면 확대했습니다. 오른손 전체가 소매 밖에 유지되고 손목–아래팔 연결이 끊기거나 파고들지 않으며, 손바닥이 손잡이 상부에 놓이고 굽힌 손가락이 손잡이를 감싸는 것을 확인했습니다.
- 최종 직접 확인 이미지: `Assets/_Project/Art/Player/Animations/LightsaberThrust/Review/LightsaberThrust_TopGrip_Final.png`
- 자연 재생 손목 굽힘 최대값: `0°`
- 자연 재생 손바닥–손잡이 상부 접촉 오차: 최대 `0.000661911m`
- 자연 재생 손가락–손잡이 접촉 오차: 최대 `0.0854703m`
- 원본 오른손 위치 변화: `0m`
- 광선검 각도 변화: `0°`
- 광선검 손잡이 오른손 추종 위치·회전 오차: `0`
- 자연 재생 연속 회전 최대 변화: `18.9256°/60Hz`
- 반복 경계 회전 변화: `11.5343°/60Hz`, 반복 경계 `2회` 확인
- 반복 시간 유지: 중간 동작 `0.2초`, 마지막 자세 유지 `0.5초`
- 컨트롤러 SHA-256 유지: `0B6EA60294C2388902A30EEF1AEEDF502566A3A1C25BB4C2C8B0587950F88494`
- 기존 파생 메시 SHA-256 유지: `CA98681FEDE4381AA38350AC43B24C35FC6A1ADD1C8A6B9C60B20CC204AF8EE6`
- Unity 콘솔: 오류 `0`, 경고 `0`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 공용 플레이어 메시·스킨 웨이트·원본 FBX, 렌더러, 머티리얼 수정
- 다른 개체, 다른 애니메이션 동작, 진행 HTML 수정
- Git 커밋 및 푸시

## Lightsaber_ThrustMode_Exit 역재생 모션 구현

- 적용 대상: `Lightsaber_ThrustMode_Exit`
- 원본: `Lightsaber_ThrustMode_Enter`에 연결된 1초 클립의 전환 구간 `0~0.5초`
- 변경 내용:
  - Enter 클립의 처음 `31프레임`을 값 변경 없이 역순 복사해 `0.5초` 퇴장 동작으로 구성했습니다.
  - Enter의 양손 모은 자세 유지 `0.5초`는 복사 대상에서 제외했습니다.
  - 역재생 종료 뒤 `Lightsaber_Off_Idle` 자세를 `0.5초` 유지하고 시작 자세로 즉시 복귀하는 전체 `1초` 반복 클립을 연결했습니다.
  - 단일 상태·무전이 Animator Controller를 사용하고 Root Motion을 끈 채 오른손 자식인 `Lightsaber_Prop` 추종 구조를 유지했습니다.
  - 최종 캡처에서 간헐적으로 빈 패널이 저장되지 않도록 실제 캐릭터 픽셀을 확인하고 빈 렌더 결과는 저장하지 않는 검증 보호 장치를 추가했습니다.

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity Play Mode 자연 재생을 강제 샘플링 없이 확인했습니다. 여섯 패널에서 양손 모은 시작 → 역재생 중간 → Off Idle 도달 → Off Idle 유지 2시점 → 다음 루프 시작 복귀가 순서대로 보였습니다.
- 직접 확인 결과: 팔·손·몸통 메시의 뒤틀림이나 관통이 없고, 광선검 손잡이가 오른손에서 이탈하지 않았으며, Off Idle 유지 세 시점의 자세가 동일했습니다.
- 최종 직접 확인 이미지: `Assets/_Project/Art/Player/Animations/LightsaberThrustModeExit/Review/LightsaberThrustModeExit_Final.png`
- 자연 재생: `Animator.Play`·`Animator.Rebind` 호출 없이 전체 `1초` 루프와 반복 경계를 확인했습니다.
- 유지 구간 편차: 위치 `0m`, 회전 `0°`, 스케일 `0`
- 원본 Enter 클립 SHA-256: `4CC6B04BF070E1F6026011ADAB3F9896D224B8277A4557A7A178CBB571FD118B`
- 원본 Enter 컨트롤러 SHA-256: `0E9505E00E71DCBA5D294E0AFAED98E651816E98A5E2AE4D630FD0BF19E6B286`
- 원본 Off Idle 클립 SHA-256: `CC1260979C01C123B7A94B3F5D6D3EAC07D2F428C9E8A51F4EC40EA39242B37F`
- Unity 콘솔: 오류 `0`, 경고 `0`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 원본 Enter·Off Idle 애니메이션 수정
- 모델·메시·렌더러·머티리얼·VFX 및 다른 개체 수정
- 진행 HTML 문서 수정
- Git 커밋 및 푸시

## 광선검 모션 통과 처리 및 Detector 시작 시점 변경

- 진행 HTML 통과 처리 대상:
  - `Lightsaber_Off_Idle`
  - `Lightsaber_DiagonalSlash`
  - `Lightsaber_Grip_OneHand`
  - `Lightsaber_ThrustMode_Enter`
  - `Lightsaber_Thrust`
  - `Lightsaber_ThrustMode_Exit`
- 시작 화면 대상: `Detector_Attached_Static`
- 변경 내용:
  - `docs/PLAYER_ANIMATION_REQUIREMENTS.html`에서 지정된 광선검 6개 행에 기존 완료 행과 동일한 `animation-complete` 및 `data-completion="완료"` 표시를 적용했습니다.
  - `Detector_Attached_Static`의 위치·회전·스케일과 다른 씬 개체를 유지하고, 실행 시작점을 결정하는 `Player` 루트의 위치만 대상 정면 축으로 옮겼습니다.
  - 시작 `Player` 위치는 `(71.307860, 0.000000, -300.829300)`, 회전은 `(0.000000, 0.000000, 0.000000, 1.000000)`으로 저장했습니다.
  - 대상의 전체 렌더러 경계가 들어오는 가장 가까운 검증 지점인 정면 수평 거리 `4.5m`를 사용했습니다.

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectDetectorAttachedStaticStartViewSources ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyDetectorAttachedStaticStartView ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectDetectorAttachedStaticStartView ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureDetectorAttachedStaticStartViewFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command AssertNoUnityConsoleErrors ...`

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity 실행 모드의 실제 시작 화면에서 `Detector_Attached_Static`이 화면 중앙에 머리부터 발끝까지 들어오고 정면이 보이는 것을 확인했습니다.
- 최종 직접 확인 이미지: `docs/validation/DetectorAttachedStaticStartView/DetectorAttachedStaticStartView_Final.png`
- 대상 정면 축 일치값: `1.000000`
- 카메라-대상 시선 일치값: `0.986779`
- 화면 가장자리 최소 여백: `0.071590`
- 카메라와 대상의 수평 거리: `4.500000m`
- `Detector_Attached_Static` Transform 변경: 없음
- 실행 모드 검증 중 `Player` Transform 조작: 없음
- Unity 콘솔: 오류 `0`, 경고 `0`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- 검증 명령 선제 금지 목록의 나머지 명령과 관련 결과 조사
- Unity 에디터 재시작
- 모델·텍스처·머티리얼·VFX·애니메이션 수정 또는 재생성
- 지정되지 않은 진행 HTML 행과 씬 개체 수정
- Git 커밋 및 푸시

## 남은 확인 사항

- 현재 승인 범위 안에서 남은 작업 없음

## Detector_Attached_Static 기척 감지기 모델 적용

- 적용 대상: `Detector_Attached_Static`
- 원본 모델: `item model/presence detector.fbx`
- 변경 내용:
  - 원본 FBX를 값 변경 없이 `Assets/_Project/Art/Items/PresenceDetector/PresenceDetector.fbx`로 복사했습니다.
  - 내장 텍스처 4개와 내장 머티리얼 1개를 모두 가져와 URP Lit 머티리얼에 연결했습니다.
  - `Armor_Head_Idle`과 같은 `Armature/Hips/Spine02/Spine01/Spine/neck/Head` 자식 장착 구조를 사용해 머리 움직임을 직접 추종하게 했습니다.
  - 대상 개체의 기존 Transform과 참조 개체는 변경하지 않았습니다.

## 직접 확인 및 보조 검증

- 1순위 직접 확인: Unity 실제 장면에서 감지기가 수송자 머리 보호대에 부착되고 머리 움직임을 따라가며, 모델·텍스처·머티리얼이 빠짐없이 보이는 것을 확인했습니다.
- 최종 직접 확인 이미지: `docs/validation/DetectorAttachedStaticPresenceDetector/DetectorAttachedStaticPresenceDetector_Final.png`
- 원본 및 프로젝트 복사본 SHA-256: `49F9E7B8A5DCC93FADBB2B9718F5C4E4A6562CF5503FB17B8C7746A0A542566C`
- 렌더러 수: `1`, 외부화 머티리얼 수: `1`, 내장 텍스처 수: `4`
- 머리 기준 로컬 위치: `(0.0077131195, 0.144181624, 0.06534819)`
- 머리 기준 로컬 회전: `(-0.9635937, 0.0232555941, 0.008851763, 0.2662105)`
- 메시·UV·참조 개체 변경: 없음

## ElectricMine_Activate 왼팔 연속성·몸통 겹침 최종 수정

- 적용 대상: `ElectricMine_Activate`의 왼팔·왼손 버튼 누르기 전체 반복 모션
- 변경 내용:
  - 버튼 접근과 복귀 경로를 최소 저크 곡선과 연속된 팔꿈치 굽힘면으로 다시 구성해 왼팔이 프레임 사이에서 튀거나 끊기는 구간을 제거했습니다.
  - 왼팔·아래팔·손을 몸통 전방과 바깥쪽으로 통과시키고 전 구간에 몸통 여유 검사를 적용했습니다.
  - 검지만 빨간 버튼에 접촉하고 나머지 손가락과 손목은 전류 지뢰 표면 밖에 유지했습니다.
  - 기존 `0.4초` 접근, `0.2초` 접촉, `0.5초` 복귀와 전체 `1.1초` 반복을 유지했습니다.
  - 오른팔 파지와 오른손 자식 전류 지뢰의 위치·회전·스케일은 변경하지 않았습니다.

## 직접 확인 및 보조 검증

- 1순위 직접 확인: 사용자가 Unity에서 현재 자연 재생 모션을 직접 확인하고 현 상태로 사용해도 된다고 승인했습니다.
- Unity 자연 재생 확인: 강제 시간 이동 없이 `2회` 반복, 관측 주기 `1.06827366초`
- 버튼 접촉 거리: 양 회차 `0.006349274m`
- 실행 모드 최소 왼팔–몸통 여유: `0.00254197419m`
- 구조 검사 최소 왼팔–몸통 여유: `0.00244796276m`
- 최대 인접 왼팔 회전: `2.53064513°/240Hz` (허용 기준 `3°`)
- 최대 회전 가속 변화: `0.5004584°` (허용 기준 `0.65°`)
- 시작 자세 복귀 편차와 오른팔 파지 편차: `0°`
- 전류 지뢰 오른손 추종 위치·회전·스케일 오차: 모두 `0`

## ElectricMine_Activate 검증 도구 콘솔 오류 수정

- 원인: 연속 캡처 보조 코드의 목록을 교체하는 과정에서 삭제된 `ContinuousFrameTimes` 참조 한 곳이 남아 `CS0103` 컴파일 오류가 발생했습니다.
- 수정: 해당 참조를 현재 프레임 목록인 `ContinuousFrames.Count`로 교정했습니다.
- 애니메이션·씬·모델·머티리얼 변경: 없음
- Unity 콘솔 직접 확인: 오류 `0`, 기존 경고 `58`, 로그 `2`

## 최종 저장소 반영

- 광선검, 탐지기, 전류 지뢰 관련 구현·애니메이션·모델 에셋·씬 연결·진행 문서를 하나의 완료 커밋으로 기록하고 `origin/main`에 푸시합니다.
- `Temp`, `Library`, `Logs`, `UserSettings` 및 `docs/validation` 생성물은 저장소 규칙에 따라 커밋하지 않습니다.

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 관련 작업
- 검증 명령 선제 금지 목록의 다른 작업
- Unity 에디터 재시작 및 추가 캡처
- 애니메이션·모델·씬의 추가 수정
- 관련 없는 사용자 변경 스테이징
- 기존 커밋 수정, 강제 푸시, 리베이스, 리셋
