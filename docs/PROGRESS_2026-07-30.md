# 2026-07-30 진행 상황

## 완료 작업 정리 및 Git 체크포인트

### 이번 체크포인트에 포함할 완료 작업

- 레볼루션 이동 모션 개체에 공급 FBX의 내장 이동 애니메이션을 반복
  재생하도록 구성했습니다.
- 레볼루션 오른팔 총신을 정상 왼팔의 상태와 모션을 유일한 비교
  기준으로 재구성하고, 최종적으로 총신 회전 모션을 유지한 채 정적
  사격 자세의 오른팔 형태를 복구했습니다.
- 레볼루션 근접 공격, 피격, 방향 전환, 사망 모션 개체를 각각 사용자가
  지정한 FBX로 교체하고 정적 모델 외형과 동기화했습니다.
- 피격 모션의 양팔은 어깨부터 손까지 정적 모델 각도로 고정했습니다.
- 방향 전환은 360도 회전에 3초가 걸리도록 맞추고 양팔을 정적 모델
  각도로 고정했습니다.
- 사망 모션은 사용자가 마지막으로 제공한 동일 경로의 새 FBX로
  재교체하고 Mixamo 애니메이션 전체 구간을 반복 재생하도록
  구성했습니다.
- 우주 해적 개체 목록과 사용자 확정 파후르 애니메이션 9개를 문서화했습니다.
- 공급 `pāḫḫur.fbx`를 Unity 프로젝트에 임포트하고 파후르 정적 비교와
  9개 애니메이션 슬롯에 대응하는 총 10개 인스턴스를 레볼루션 아래쪽에
  배치했습니다. X축 간격과 Z축 행 간격은 기존 배치에서 측정한 값을
  사용하고 Player 시작 시점을 파후르 정면으로 저장했습니다.
- 현재 Unity 파후르 FBX를 기준으로 원화의 색·머티리얼·텍스처를
  분석한 승인 대기 아트 샘플을
  `artSample/enemies/pahur/appearance_reference_sync/`에 제작했습니다.
  상태는 `PENDING_USER_REVIEW`, `approved_for_unity=false`이며 Unity
  런타임에는 연결하지 않았습니다.

### 포함할 주요 산출물

- 레볼루션 애니메이션, 컨트롤러, 교체 FBX, 관련 Editor 도구와
  `CargoRunMvp` 씬 변경
- 파후르 프로젝트 FBX, 배치 Editor 도구와 씬 배치
- `docs/PROGRESS_2026-07-29.md`
- `docs/SPACE_PIRATE_ENTITY_LIST_2026-07-29.md`
- `docs/PAHUR_ANIMATION_LIST_2026-07-29.md`
- 레볼루션 및 파후르의 최종 검사 기록과 진행문서에서 직접 참조하는
  시각 검토 이미지·영상
- 파후르 외형 동기화 Blender/FBX/GLB, 텍스처, 렌더, HTML,
  구조 보존 및 HTML 검사 기록
- 사용자가 공급한 레볼루션 근접·피격·방향 전환·사망 원본 FBX

### Git 포함 제외

- Blender 자동 백업 `*.blend1`
- Python `__pycache__`와 `*.pyc`
- 최종 영상·접촉 시트 생성에 사용했지만 진행문서가 직접 참조하지 않는
  원시 PNG 프레임 시퀀스
- 다른 적대 개체 아트 샘플 폴더에 남은 자동 생성 백업

위 파일은 완료 산출물이 아니므로 이번 체크포인트에 스테이징하지
않습니다. 사용자 작업으로 판단되는 파일은 수정하거나 삭제하지 않습니다.

### 확인 결과

- 현재 브랜치: `main`
- 현재 upstream: `origin/main`
- 원격 저장소: `origin`
- 파후르 아트 샘플 원본 FBX·Blend·재내보낸 FBX 구조 비교:
  `PASS`
- 파후르 HTML 정적 DOM·이미지·링크 검사: `PASS`
- 파후르 원본 프로젝트 FBX SHA-256:
  `797D3663D6249F4924322D8487DD308F364CC5663A454D72123824C117AC66A9`
- 파후르 아트 샘플은 사용자 승인 전 상태이므로 Unity 미적용을
  유지합니다.

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·로그·문서·생성물 작업
- EditMode/PlayMode 테스트
- Windows 빌드
- Smoke/Validate/Ensure 계열 Unity 명령
- Unity 실행·수정·저장·새로고침
- 강제 푸시, 이력 재작성, `git reset --hard`
