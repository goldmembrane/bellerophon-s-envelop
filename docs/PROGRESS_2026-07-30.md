# 2026-07-30 진행 상황

## 완료 작업 정리 및 Git 체크포인트

### 이번 체크포인트에 포함할 완료 작업

- 레볼루션 이동 모션 개체에 공급 FBX의 내장 이동 애니메이션을 반복
  재생하도록 구성했습니다.
- 레볼루션 오른팔 총신을 정상 왼팔의 상태와 모션을 유일한 비교
  기준으로 재구성하고, 최종적으로 총신 회전 모션을 유지한 채 정적
  사격 자세의 오른팔 형태를 복구했습니다.
- 레볼루션 근접 공격, 피격, 방향 전환, 사망 모션 개체를 각각 사용자가
  지정한 FBX로 교체하고 정적 모델 외형과 동기화했습니다.
- 피격 모션의 양팔은 어깨부터 손까지 정적 모델 각도로 고정했습니다.
- 방향 전환은 360도 회전에 3초가 걸리도록 맞추고 양팔을 정적 모델
  각도로 고정했습니다.
- 사망 모션은 사용자가 마지막으로 제공한 동일 경로의 새 FBX로
  재교체하고 Mixamo 애니메이션 전체 구간을 반복 재생하도록
  구성했습니다.
- 우주 해적 개체 목록과 사용자 확정 파후르 애니메이션 9개를 문서화했습니다.
- 공급 `pāḫḫur.fbx`를 Unity 프로젝트에 임포트하고 파후르 정적 비교와
  9개 애니메이션 슬롯에 대응하는 총 10개 인스턴스를 레볼루션 아래쪽에
  배치했습니다. X축 간격과 Z축 행 간격은 기존 배치에서 측정한 값을
  사용하고 Player 시작 시점을 파후르 정면으로 저장했습니다.
- 현재 Unity 파후르 FBX를 기준으로 원화의 색·머티리얼·텍스처를
  분석한 승인 대기 아트 샘플을
  `artSample/enemies/pahur/appearance_reference_sync/`에 제작했습니다.
  상태는 `PENDING_USER_REVIEW`, `approved_for_unity=false`이며 Unity
  런타임에는 연결하지 않았습니다.

### 포함할 주요 산출물

- 레볼루션 애니메이션, 컨트롤러, 교체 FBX, 관련 Editor 도구와
  `CargoRunMvp` 씬 변경
- 파후르 프로젝트 FBX, 배치 Editor 도구와 씬 배치
- `docs/PROGRESS_2026-07-29.md`
- `docs/SPACE_PIRATE_ENTITY_LIST_2026-07-29.md`
- `docs/PAHUR_ANIMATION_LIST_2026-07-29.md`
- 레볼루션 및 파후르의 최종 검사 기록과 진행문서에서 직접 참조하는
  시각 검토 이미지·영상
- 파후르 외형 동기화 Blender/FBX/GLB, 텍스처, 렌더, HTML,
  구조 보존 및 HTML 검사 기록
- 사용자가 공급한 레볼루션 근접·피격·방향 전환·사망 원본 FBX

### Git 포함 제외

- Blender 자동 백업 `*.blend1`
- Python `__pycache__`와 `*.pyc`
- 최종 영상·접촉 시트 생성에 사용했지만 진행문서가 직접 참조하지 않는
  원시 PNG 프레임 시퀀스
- 다른 적대 개체 아트 샘플 폴더에 남은 자동 생성 백업

위 파일은 완료 산출물이 아니므로 이번 체크포인트에 스테이징하지
않습니다. 사용자 작업으로 판단되는 파일은 수정하거나 삭제하지 않습니다.

### 확인 결과

- 현재 브랜치: `main`
- 현재 upstream: `origin/main`
- 원격 저장소: `origin`
- 파후르 아트 샘플 원본 FBX·Blend·재내보낸 FBX 구조 비교:
  `PASS`
- 파후르 HTML 정적 DOM·이미지·링크 검사: `PASS`
- 파후르 원본 프로젝트 FBX SHA-256:
  `797D3663D6249F4924322D8487DD308F364CC5663A454D72123824C117AC66A9`
- 파후르 아트 샘플은 사용자 승인 전 상태이므로 Unity 미적용을
  유지합니다.

### 실행하지 않은 항목

- 하네스 검증 및 관련 실행·재실행·로그·문서·생성물 작업
- EditMode/PlayMode 테스트
- Windows 빌드
- Smoke/Validate/Ensure 계열 Unity 명령
- Unity 실행·수정·저장·새로고침
- 강제 푸시, 이력 재작성, `git reset --hard`

## 사용자 검토 대기 - 파후르 기계 표면 텍스처 재작업 2차

- 사용자의 `모델링 메시 자체는 변형시키지마` 지시에 따라 기존의
  지오메트리 추가 계획을 폐기하고, 원본과 파생 모델의 메시·토폴로지·
  UV·스킨 웨이트·리그·실루엣을 유지했습니다.
- 직전 샘플의 잔기스 위주 표면을 전신 하드서피스 텍스처로 교체했습니다.
  정면 투영 맵에는 안면판, 흉부 프레임, 적층 복부, 팔 장갑 밴드,
  허벅지판, 무릎 캡, 정강이 통풍구와 발 덮개를 구성했습니다.
- 투영 패널과 동일한 높이 맵을 범프에 연결하고 장갑·기계부·무기·
  연료통별 매크로 노멀을 추가해, 메시를 변경하지 않고도 판재 단차,
  깊은 이음 홈, 리벳, 리브, 통풍구와 탱크 밴드가 조명에 반응하도록
  재작업했습니다.
- 정면, 사선, 측면, 후면 렌더와 기준 이미지 나란히 비교본을
  `artSample/enemies/pahur/appearance_reference_sync/renders/`에
  다시 생성했습니다. 상태는
  `2026-07-30_mechanical_surface_texture_rework_02`,
  `PENDING_USER_REVIEW`입니다.
- 원본 FBX, 검토 Blend, 재내보낸 FBX는 정점 `3,554`, 에지 `10,585`,
  면 `7,043`, 루프 `21,129`, 본 `24`, UV 레이어 `uv`로 일치하며
  구조 보존 검사를 `PASS`했습니다. 원본 FBX SHA-256은
  `797D3663D6249F4924322D8487DD308F364CC5663A454D72123824C117AC66A9`
  로 이전과 동일합니다.
- 현재 FBX의 근육형 저폴리 실루엣과 원화의 판금 안드로이드 실루엣
  차이는 메시 보존 지시 때문에 남습니다. 이번 결과는 그 경계 안에서
  색·머티리얼·텍스처·노멀·범프만으로 기계 표면을 강화한 샘플입니다.
- Unity 원본 FBX, 씬, 프리팹, 런타임 에셋은 수정하지 않았으며 사용자
  승인 전 Unity 미적용 상태를 유지합니다.
- 하네스 검증과 관련 실행·조사·로그·문서·생성물 작업, EditMode·
  PlayMode 테스트, Windows 빌드는 실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 두건·가스통 표면 재작업 3차

- 사용자가 몸통은 더 이상 건드리지 않도록 지정해 2차 샘플의 몸통 장갑
  알베도·메탈릭·러프니스·노멀과 전신 기계 패널 투영 맵의 SHA-256을
  수정 전후 비교했고 모두 동일함을 확인했습니다.
- 가스통의 반복 세로 립과 밴드 높이 정보를 제거하고, 낮은 미세 요철,
  넓은 금속 반사와 제한된 잔기스만 남겨 기준 이미지의 매끈한 원통
  표면에 맞췄습니다.
- 두건은 몸통용 기계 패널 범프에서 분리하고 짙은 남색 비금속 천,
  낮은 대비의 가로 감김 주름, 둘레 봉제선과 이마 불 문양으로 다시
  구성했습니다. 메시 보존 지시에 따라 실제 외곽선은 공급 FBX 형상을
  유지합니다.
- 정면·사선·측면·후면 및 기준 이미지 비교 렌더를 다시 생성했습니다.
  상태는 `2026-07-30_hood_tank_surface_rework_03`,
  `PENDING_USER_REVIEW`입니다.
- Unity 원본 FBX·씬·프리팹·런타임 에셋은 수정하지 않았습니다.

## 사용자 검토 대기 - 파후르 두건·얼굴 분리 재작업 4차

- 사용자가 몸통과 가스통을 더 이상 수정하지 않도록 지정해 해당
  알베도·메탈릭·러프니스·노멀 및 전신 기계 패널 투영 맵을 수정 전
  SHA-256 기준으로 유지했습니다.
- 기존에는 두건 아래 얼굴 폴리곤이 몸통 관절과 같은
  `dark_mechanics` 재질과 전신 기계 범프를 공유해 두건과 얼굴의
  경계가 흐려졌습니다.
- 얼굴 폴리곤 `182`개를 전용 `Pahur_Faceplate_Dark_Metal`
  재질로 분리하고, 두건 폴리곤 `114`개는 기존 짙은 남색 비금속 천
  재질로 유지했습니다.
- 얼굴 전용 투영 텍스처에 밝은 상단 금속 경계선, 가늘고 청색인 양쪽
  광학 눈, 관자 장갑, 중앙 콧날판, 분절 턱과 세로 환기구를 구성하고
  전용 높이 맵과 제한된 청색 발광을 적용했습니다.
- 정면에서 두건이 눈 위에서 끝나고 별도 금속 얼굴이 시작되며,
  사선에서도 중간 건메탈 얼굴과 짙은 천 두건이 구별되는 것을
  확인했습니다.
- 몸통·가스통 관련 기준 텍스처 `23`개의 SHA-256은 수정 전과 모두
  동일했습니다.
- 원본 FBX, 검토 Blend, 재내보낸 FBX는 정점 `3,554`, 에지 `10,585`,
  면 `7,043`, 루프 `21,129`, 본 `24`로 일치했으며 좌표·토폴로지·
  UV·스킨 웨이트 보존 검사를 `PASS`했습니다. 원본 FBX SHA-256은
  `797D3663D6249F4924322D8487DD308F364CC5663A454D72123824C117AC66A9`
  로 유지되었습니다.
- HTML 정적 DOM·이미지·링크 검사와 최종 렌더 직접 검토는 `PASS`했고,
  깨진 이미지와 링크는 없었습니다.
- 상태는 `2026-07-30_hood_face_separation_rework_04`,
  `PENDING_USER_REVIEW`이며 Unity에는 적용하지 않았습니다.
- 하네스 검증과 관련 실행·재실행·조사·로그·문서·생성물 작업,
  EditMode·PlayMode 테스트, Windows 빌드는 실행하지 않았습니다.

## 사용자 반려 - 파후르 밀착 두건·광학 눈 위치 재작업 5차

- 기준 이미지 머리를 확대해 두건이 챙 있는 모자가 아니라 윗머리에
  밀착해 감긴 천이고, 화면 오른쪽 관자 뒤에 묶음과 두 갈래 꼬리가
  있다는 점을 다시 기준으로 삼았습니다.
- 기존 높이 하나로 나눈 대칭 두건 분류를 폐기하고, 윗머리와 화면
  오른쪽 묶음에 해당하는 머리 면 `90`개만 천으로 유지했습니다.
  나머지 머리 면 `206`개는 전용 건메탈 얼굴로 분리했습니다.
- 기존 청색 눈이 두건보다 지나치게 낮아 목 가까이에 보이던 투영
  오류를 수정했습니다. 얼굴 상단 경계·양쪽 눈·관자 장갑·콧날판·
  분절 턱·환기구 투영 전체를 위로 올려 눈이 두건 바로 아래에서
  보이도록 재정렬했습니다.
- 두건은 가로 감김 주름, 하단 겹침 띠, 미세 직조, 화면 오른쪽의
  비대칭 묶음과 꼬리로 구성했습니다. 몸통과 가스통은 수정하지
  않았습니다.
- 머리 정면 확대 렌더와 기준 이미지 직접 비교본을
  `renders/07_head_front_detail.png`,
  `renders/08_head_detail_comparison.png`로 추가했습니다.
- 메시 보존 지시에 따라 공급 FBX의 각지고 넓은 윗머리 외곽선은
  유지했으며, 정점·토폴로지·UV·웨이트·리그는 변경하지 않았습니다.
- 몸통·가스통 관련 기준 텍스처 `23`개의 SHA-256은 4차 작업 완료
  상태와 모두 동일했습니다.
- 원본 FBX, 검토 Blend, 재내보낸 FBX는 정점 `3,554`, 에지 `10,585`,
  면 `7,043`, 루프 `21,129`, 본 `24`로 일치해 구조 보존 검사를
  `PASS`했습니다. 원본 FBX SHA-256은
  `797D3663D6249F4924322D8487DD308F364CC5663A454D72123824C117AC66A9`
  로 유지되었습니다.
- HTML 정적 DOM·이미지·링크 검사는 `PASS`했고 깨진 이미지와 링크는
  없었습니다.
- 상태는 `2026-07-30_fitted_headwrap_and_visible_optics_rework_05`,
  `PENDING_USER_REVIEW`이며 Unity에는 적용하지 않았습니다.
- 하네스 검증과 관련 실행·재실행·조사·로그·문서·생성물 작업,
  EditMode·PlayMode 테스트, Windows 빌드는 실행하지 않았습니다.

- 이후 사용자 직접 시각 검토에서 좌표 추측으로 두건 면을 잘못
  분류해 모자처럼 보인다는 판정을 받아 이 결과를 폐기했습니다.

## 사용자 반려 - 파후르 원본 머리 표면 마스크 재작업 6차

- 5차에서 사용한 머리 높이·좌우·전후 좌표 임계값을 전부 제거했습니다.
- 원본 FBX 머리 `296`면이 하나의 연결 메시임을 확인하고,
  `renders/09_head_topology_front.png`,
  `renders/09_head_topology_three_quarter.png`,
  `renders/09_head_topology_side.png`,
  `renders/09_head_topology_rear.png`에서 실제 머리 표면을 직접
  대조했습니다.
- 기존 머리 `296`면 전체에는
  `Pahur_Headwrap_And_Face_Masked`를 배정했습니다. 짙은 남색 천이
  윗머리와 좌우로 내려오는 두건 표면을 계속 덮고, 중앙의 안면 형태만
  별도 투영 마스크로 건메탈화해 좌우 두건 면이 모자 챙으로 읽히던
  오류를 제거했습니다.
- 얼굴 마스크 상단을 두건 바로 아래까지 올리고 청색 양안 광학부를
  가늘게 줄여 완전히 노출했습니다. 중앙 콧날판·분절 턱·세로
  환기구도 같은 마스크 안에 유지했습니다.
- 이마에는 기준 이미지의 주황 불 문양과 낮은 대비의 천 주름을
  배치했습니다. 불 문양은 비발광 도색, 눈만 청색 발광으로 분리했습니다.
- 몸통과 가스통 관련 텍스처·머티리얼은 수정하지 않았으며, 원본
  메시·토폴로지·UV·스킨 웨이트·리그·애니메이션도 변경하지 않았습니다.
- 원본 FBX, 검토 Blend, 재내보낸 FBX는 정점 `3,554`, 에지 `10,585`,
  면 `7,043`, 루프 `21,129`, 본 `24`로 일치했으며 구조 보존 검사를
  `PASS`했습니다. 원본 FBX SHA-256은
  `797D3663D6249F4924322D8487DD308F364CC5663A454D72123824C117AC66A9`
  로 유지되었습니다.
- HTML 정적 DOM·이미지·링크 검사는 `PASS`했고 깨진 이미지와 링크는
  없었습니다. `renders/07_head_front_detail.png`와
  `renders/08_head_detail_comparison.png`를 직접 대조해 두건 천,
  중앙 금속 얼굴, 노출된 청색 눈의 순서를 확인했습니다.
- 상태는 `2026-07-30_head_surface_mask_rework_06`,
  `PENDING_USER_REVIEW`이며 Unity에는 적용하지 않았습니다.
- 하네스 검증과 관련 실행·재실행·조사·로그·문서·생성물 작업,
  EditMode·PlayMode 테스트, Windows 빌드는 실행하지 않았습니다.
- 이후 사용자 직접 시각 검토에서 두건과 얼굴을 하나의 통합 재질로
  처리해 정면에서 여전히 모자처럼 보인다는 판정을 받아 폐기했습니다.

## 사용자 검토 대기 - 파후르 두건·얼굴 별도 재질 복원 7차

- 저장소 최초 샘플의 머리 분류와 렌더를 다시 확인했습니다. 최초
  샘플은 머리 전체를 같은 천으로 덮지 않고 상단 `114`면만
  `hood_navy_cloth`로 사용했습니다.
- 6차의 `Pahur_Headwrap_And_Face_Masked` 통합 재질을 제거하고,
  상단 `114`면은 `Pahur_Hood_Navy_Cloth`, 하단 `182`면은
  `Pahur_Faceplate_Dark_Metal`로 분리했습니다.
- 두건과 얼굴은 알베도·메탈릭·러프니스·노멀 텍스처를 공유하지
  않습니다. 두건은 짙은 남색 비금속 천과 미세 직조, 얼굴은 별도
  건메탈과 패널 텍스처를 사용합니다.
- 얼굴 상단의 굵은 흰색·검은색 수평 띠를 제거하고 얇은 금속 경계만
  남겨 모자 챙처럼 보이는 인상을 줄였습니다. 가는 청색 양안 광학부는
  두건 바로 아래에 완전히 노출했습니다.
- 이마 주황 불 문양은 비발광 도색으로, 눈은 청색 발광으로 분리했습니다.
- 몸통·가스통 파일을 재생성하지 않도록 텍스처 생성 도구에
  `--head-only` 경로를 추가하고 머리 관련 텍스처만 다시 만들었습니다.
- 6차 매니페스트를 기준으로 몸통·가스통 보호 텍스처 `23`개의
  SHA-256을 비교했고 불일치 `0`개를 확인했습니다.
- 원본 FBX, 검토 Blend, 재내보낸 FBX는 정점 `3,554`, 에지 `10,585`,
  면 `7,043`, 루프 `21,129`, 본 `24`로 일치해 구조 보존 검사를
  `PASS`했습니다. 원본 FBX SHA-256은
  `797D3663D6249F4924322D8487DD308F364CC5663A454D72123824C117AC66A9`
  로 유지되었습니다.
- HTML 정적 DOM·이미지·링크 검사와 정면 확대 비교 렌더의 직접
  시각 검토는 `PASS`했습니다.
- 상태는 `2026-07-30_separate_headwrap_face_materials_rework_07`,
  `PENDING_USER_REVIEW`이며 Unity에는 적용하지 않았습니다.
- 하네스 검증과 관련 실행·재실행·조사·로그·문서·생성물 작업,
  EditMode·PlayMode 테스트, Windows 빌드는 실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 원본 머리·매끈한 무구강 얼굴 재작업 8차

- 사용자 직접 시각 검토에서 7차 얼굴의 패널 무늬와 범프, 아래쪽
  세로 통풍구가 기준 이미지에 없는 입처럼 보인다는 판정을 받아
  7차 결과를 폐기했습니다.
- 매번 원본
  `Assets/_Project/Art/Enemies/Pahur/Models/Pahur.fbx`를 새로 불러오며,
  메시 정점·면·실루엣·UV·스킨 웨이트·리그는 수정하지 않았습니다.
- 원본 메시 위에 별도 두건 옆자락과 꼬리를 덧그리던 투영 도형을
  제거했습니다. 기존 머리 면 중 최초 샘플에서 확인된 상단 `114`면은
  짙은 남색 `Pahur_Hood_Navy_Cloth`, 하단 `182`면은 회색
  `Pahur_Faceplate_Dark_Metal`로 다시 칠했습니다.
- 얼굴 알베도의 스크래치·입자·패널선을 제거하고 넓고 낮은 대비의
  명도 변화만 남겼습니다. 얼굴 노멀과 높이 맵은 평탄화했으며
  머티리얼 범프 연결도 제거했습니다.
- 얼굴 투영 맵에는 기준 이미지에서 확인되는 가는 청색 양안
  광학부만 남겼습니다. 입, 세로 환기구, 코·턱 패널은 생성하지
  않았습니다.
- 몸통과 가스통 관련 텍스처는 `--head-only` 경로로 재생성 대상에서
  제외했습니다.
- 7차 매니페스트 기준 몸통·가스통 보호 텍스처 `23`개의 SHA-256을
  비교했고 불일치 `0`개를 확인했습니다.
- 얼굴 높이 맵은 전체 픽셀 `(128, 128)`, 얼굴 노멀은 전체 픽셀
  `(128, 128, 255)`로 평탄함을 확인했습니다.
- 원본 FBX, 검토 Blend, 재내보낸 FBX는 정점 `3,554`, 에지 `10,585`,
  면 `7,043`, 루프 `21,129`, 본 `24`, UV 레이어와 본 그룹이
  일치해 구조 보존 검사를 `PASS`했습니다. 원본 FBX SHA-256은
  `797D3663D6249F4924322D8487DD308F364CC5663A454D72123824C117AC66A9`
  입니다.
- HTML 정적 DOM·이미지·링크 검사와 기준 이미지/최종 머리 확대본
  직접 시각 비교를 `PASS`했습니다.
- 상태는
  `2026-07-30_original_mesh_smooth_mouthless_face_rework_08`,
  `PENDING_USER_REVIEW`이며 Unity에는 적용하지 않았습니다.
- 하네스 검증과 관련 실행·재실행·조사·로그·문서·생성물 작업,
  EditMode·PlayMode 테스트, Windows 빌드는 실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 양쪽 어깨·왼팔 기계 표면 재작업 9차

- 사용자 검토에서 양쪽 어깨와 파후르 자신의 왼팔이 기계 장갑이
  아니라 매끈한 생체 근육처럼 보인다는 판정을 확인했습니다.
- 원인은 해당 부위가 몸통과 같은 범용 `armor_bluegray` 재질을
  공유해, 원본 메시의 둥근 음영을 분절 장갑으로 구별할 전용 표면
  정보가 없었던 점입니다.
- 양쪽 어깨 상단 `148`면을
  `Pahur_Shoulder_Mechanical_Blue`, 왼쪽 위팔·아래팔 `274`면을
  `Pahur_LeftArm_Segmented_Mechanical`, 왼손 `224`면을
  `Pahur_LeftHand_Articulated_Mechanical`로 분리했습니다.
- 세 재질에 각각 전용 알베도·메탈릭·러프니스·노멀 맵을 만들고,
  기준 이미지에서 확인되는 청회색 장갑판, 어두운 관절 경계,
  금속 팔 분절선과 체결부를 표면 정보로 표현했습니다.
- `renders/10_shoulders_left_arm_detail.png`에 정면 상반신 확대본,
  `renders/11_shoulders_left_arm_comparison.png`에 기준 이미지와의
  직접 비교본을 추가했습니다. 정면 비교에서 파후르 자신의 왼팔은
  화면 오른쪽에 표시됩니다.
- 원본 메시 정점·좌표·토폴로지·UV·스킨 웨이트·실루엣·리그는
  변경하지 않았습니다. 보호 영역의 기존 텍스처 `63`개를 8차
  매니페스트와 SHA-256으로 대조했고 불일치 `0`개를 확인했습니다.
- 원본 FBX, 검토 Blend, 재내보낸 FBX는 정점 `3,554`, 에지 `10,585`,
  면 `7,043`, 루프 `21,129`, 본 `24`, UV 레이어와 버텍스 그룹이
  일치해 구조 보존 검사를 `PASS`했습니다.
- HTML 정적 DOM·이미지·링크 검사와 기준 이미지/어깨·왼팔 확대본의
  직접 시각 비교를 `PASS`했으며 깨진 이미지와 링크는 없습니다.
- 상태는
  `2026-07-30_shoulders_left_arm_mechanical_surface_rework_09`,
  `PENDING_USER_REVIEW`이며 Unity에는 적용하지 않았습니다.
- 하네스 검증과 관련 실행·재실행·조사·로그·문서·생성물 작업,
  EditMode·PlayMode 테스트, Windows 빌드는 실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 어깨·왼팔 몸통 조화 재작업 10차

- 사용자 직접 시각 검토에서 9차 어깨·왼팔의 검은 반복 띠와 강한
  별도 표면 반응이 다른 몸통 부분과 비교해 이질적으로 튄다는 판정을
  받아 9차 결과를 폐기했습니다.
- 양쪽 어깨 상단 `148`면과 파후르 자신의 왼쪽 상완·전완 `274`면의
  전용 재질 배정은 유지하되, 팔레트·메탈릭·러프니스·노멀 강도·
  텍스처 축척을 몸통 `armor_bluegray`와 같은 기준으로 맞췄습니다.
- 왼손 `224`면은 기존 검은 관절·손·발에 사용되는
  `dark_mechanics`와 같은 표면 기준으로 맞췄습니다.
- 9차의 순검정 절차적 반복 띠를 제거하고, 분절선의 폭·대비와
  범프 강도를 낮춰 몸통 패널선보다 튀지 않는 보조 정보만 남겼습니다.
- 어깨와 왼팔의 알베도·메탈릭·러프니스·노멀 `4`종은 몸통 장갑
  맵과 각각 일치하고, 왼손의 같은 `4`종은 기존 검은 관절부 맵과
  각각 일치함을 SHA-256으로 확인했습니다.
- 대상 `12`개 맵을 제외한 보호 텍스처 `63`개를 9차 매니페스트와
  비교했고 불일치 `0`개를 확인했습니다.
- `renders/10_shoulders_left_arm_detail.png`와
  `renders/11_shoulders_left_arm_comparison.png`를 다시 생성해
  정면 화면 오른쪽의 왼팔과 양쪽 어깨가 몸통 표면 계열로 이어지는
  것을 직접 대조했습니다.
- 원본 메시 정점·좌표·토폴로지·UV·스킨 웨이트·실루엣·리그는
  변경하지 않았습니다. 원본 FBX, 검토 Blend, 재내보낸 FBX는 정점
  `3,554`, 에지 `10,585`, 면 `7,043`, 루프 `21,129`, 본 `24`,
  UV 레이어와 버텍스 그룹이 일치해 구조 보존 검사를 `PASS`했습니다.
- HTML 정적 DOM·이미지·링크 검사와 기준 이미지/어깨·왼팔 비교본의
  직접 시각 검토를 `PASS`했으며 깨진 이미지와 링크는 없습니다.
- 상태는
  `2026-07-30_shoulders_left_arm_body_cohesion_rework_10`,
  `PENDING_USER_REVIEW`이며 Unity에는 적용하지 않았습니다.
- 하네스 검증과 관련 실행·재실행·조사·로그·문서·생성물 작업,
  EditMode·PlayMode 테스트, Windows 빌드는 실행하지 않았습니다.

## Unity 적용 완료 - 지정 파후르 FBX 전체 모델 교체

- 사용자 지시에 따라 기존 아트 샘플 재작업을 중단했으며, 아트 샘플
  파일·텍스처·머티리얼은 이번 작업에서 수정하지 않았습니다.
- 외부 원본
  `D:/Bellerophon2/Bellerophon/enemies model/pāḫḫur.fbx`를 파일명이나
  기존 해시가 같다는 이유로 생략하지 않고
  `Assets/_Project/Art/Enemies/Pahur/Models/Pahur.fbx`에 강제
  덮어쓰기·임포트했습니다.
- 외부 원본과 프로젝트 임포트 FBX는 모두 `416,412`바이트이며
  SHA-256
  `9D376E7BD5262E9DF347D8CCC37675DDBCF647D063011C0A71E80CDDA4A9DEA3`
  으로 정확히 일치합니다. 기존 FBX 메타 GUID
  `f4abedb45727f0d4cb04ba0dbb219298`은 유지했습니다.
- 현재 열린 `CargoRunMvp`의 `Approved Pahur Enemy Placement` 아래
  `10`개 슬롯에 있던 모델 자식을 모두 새 FBX 인스턴스로
  교체했습니다. 슬롯 이름·Transform·역할, Player, 파후르 외 다른
  씬 루트는 변경하지 않았습니다.
- Unity 교체 검사에서 직접 FBX 인스턴스 `10`개, 목표 높이 `1.5m`,
  전체 라인업 표시, 씬 저장을 확인했습니다. 저장된 씬 파일에서도
  유지된 FBX GUID의 직접 프리팹 참조 `10`개와 슬롯 이름 `10`개를
  다시 확인했습니다.
- `Refresh-UnityProject.ps1`과 전용
  `ReplacePlacedPahurModels` 브리지 명령은 오류 없이 완료됐습니다.
  최종 캡처는 교체 후 검사 뒤 승인된 `1`회만 실행했으며
  `docs/validation/pahur_placement_2026-07-29/Pahur_Placement_VisualReview.png`
  에 저장했습니다. 캡처 로그는 `FullLineupVisible=True`,
  `SceneChanged=False`입니다.
- `Run-HarnessValidation.ps1`, EditMode·PlayMode 테스트, Windows
  빌드, Smoke·Validate·Ensure 계열 명령과 Git 쓰기 작업은 실행하지
  않았습니다.

## 사용자 검토 대기 - 교체 파후르 FBX 외형 동기화 아트 샘플 11차

- 현재 Unity 배치에 사용된 교체 모델
  `Assets/_Project/Art/Enemies/Pahur/Models/Pahur.fbx`와
  `image/pāḫḫur(파후르).png`를 기준으로
  `artSample/enemies/pahur/appearance_reference_sync` 샘플을 전면
  갱신했습니다.
- 교체 FBX는 `416,412`바이트, SHA-256
  `9D376E7BD5262E9DF347D8CCC37675DDBCF647D063011C0A71E80CDDA4A9DEA3`
  이며 단일 스킨 메시 `char1`, 정점 `2,445`, 에지 `6,575`, 면
  `4,348`, 루프 `13,044`, UV 레이어 `1`, 본 `24`, 내장 보행 액션
  `1`개입니다.
- 기존 `114`개 연결 표면, 로컬 폴리곤 위치와 본 가중치를 기준으로
  전체 면을 두건·얼굴·장갑·관절·화염방사기·탱크·피팅·호스의
  `13`개 사용 재질군에 배정했습니다. 메시 생성·삭제·이동·변형,
  실루엣·UV·스킨 웨이트·리그 변경은 하지 않았습니다.
- 기준 이미지의 짙은 남색 두건과 작은 주황 화염 문양, 분리된 청색
  광학부, 매끄러운 짙은 금속 얼굴, 밝은 흉부·허벅지 강철판,
  청회색 어깨·팔 장갑, 어두운 정강이와 검은 관절·손·발을
  반영했습니다.
- 화염방사기는 짙은 건메탈, 후방 탱크는 반복 홈 없는 매끄러운
  마모 강철, 호스는 거친 검은 고무로 분리했습니다. 황동·열변색은
  작은 탱크 피팅에만 제한했습니다.
- 첫 비교 렌더에서 총구 전체의 잘못된 구리색과 과대한 두건 문양을
  확인해 총구를 건메탈로 되돌리고 문양을 작은 세로형 화염으로
  수정했습니다. 후면 검토에서 발견한 두건 후면 중복 문양도 전면
  마스크를 좁혀 제거했습니다.
- `index.html`과 `summary.html`은 기존 지정 문서의 어두운 녹색
  페이지·비교 그리드·렌더 갤러리·반영 내용·한계·요약 구성을
  그대로 따르며 새 교체 모델 기준 설명과 수치로 갱신했습니다.
- 원본 FBX, 검토 Blend, 내보낸 FBX는 정점·에지·면·루프·본 수,
  UV 레이어와 버텍스 그룹이 일치해 구조 보존 검사를 `PASS`했습니다.
  정적 HTML DOM·이미지·링크 검사도 누락 `0`개로 `PASS`했습니다.
- 인앱 브라우저 렌더 검토는 브라우저 연결 도구의 환경 메타데이터
  오류로 시작되지 않아 `BLOCKED_BY_BROWSER_CONNECTION`으로
  기록했습니다. 정면·사선·측면·후면·머리 확대·직접 비교 이미지는
  별도로 직접 시각 검토했습니다.
- 상태는
  `2026-07-30_replacement_fbx_appearance_reference_sync_11`,
  `PENDING_USER_REVIEW`이며 Unity 모델·씬·Player·카메라에는
  적용하거나 수정하지 않았습니다.
- `Run-HarnessValidation.ps1` 및 관련 작업, EditMode·PlayMode 테스트,
  Windows 빌드, Smoke·Validate·Ensure 계열 명령과 Git 쓰기 작업은
  실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 기계형 표면 재작업 아트 샘플 12차

- 사용자 피드백에 따라
  `artSample/enemies/pahur/appearance_reference_sync`의 교체 FBX 외형
  샘플을 기준 이미지와 다시 직접 대조했습니다.
- 기존 갑옷 알베도에 있던 굵은 세로·가로 격자, 대각선, 볼트 무늬가
  분절된 UV마다 늘어나고 강한 노멀·전신 패널 범프와 겹치면서
  몸통이 아래로 흐르거나 무너져 보이는 원인을 확인했습니다.
- 갑옷과 다리 텍스처를 저주파 명도 변화, 짧고 제한된 긁힘, 미세
  수평 브러시드 메탈 결로 다시 만들었습니다. 강한 가짜 패널선과
  패널 범프는 제거하고 금속 노멀 강도와 과도한 반사를 낮췄습니다.
- 기준 이미지의 밝은 중앙 흉부 쉘, 청회색 외장, 짙은 적층 복부·
  골반 모듈, 어두운 청회색 다리판과 제한된 청색·주황 표시가
  구조적으로 읽히도록 기존 면의 재질 배정과 색 분포를 조정했습니다.
  전면 기계 구조 오버레이는 색상에만 사용하고 높이·범프·노멀에는
  연결하지 않았습니다.
- 몸통용 오버레이에서는 머리 영역을 투명 처리해 흉부 윗부분에
  잘못 투영되던 흰 고리 도형을 제거했습니다. 후면에는 새 문양이나
  기계 부품을 만들지 않았고 탱크의 매끄러운 금속 표면을 유지했습니다.
- 정면, 사선, 측면, 후면과 기준 이미지 직접 비교본을 다시
  생성했습니다. 직접 시각 검토에서 이전의 세로 번짐·젖은 광택·
  재질 혼합이 제거되고 흉부·복부·골반·다리의 명도 경계가 분명히
  구분되는 것을 확인했습니다.
- `index.html`, `summary.html`, `README.md`,
  `TEXTURE_ANALYSIS.md`를 기존 지정 HTML의 형식에 맞춰 12차 원인,
  반영 내용, 메시 보존 범위와 한계로 갱신했습니다. 상태는
  `2026-07-30_rigid_mechanical_surface_rework_12`,
  `PENDING_USER_REVIEW`입니다.
- 구조 보존 검사는 `PASS`했습니다. 원본 FBX, 검토 Blend, 내보낸
  FBX가 정점 `2,445`, 에지 `6,575`, 면 `4,348`, 루프 `13,044`,
  본 `24`, UV 레이어와 버텍스 그룹에서 일치하며 좌표·토폴로지·
  UV·스킨 웨이트·리그가 변경되지 않았습니다.
- HTML 정적 DOM·이미지·링크 검사는 `PASS`했으며 깨진 이미지와
  링크는 `0`개입니다. 샘플에는 텍스처 `76`개, 최종 렌더 `6`개,
  분석 렌더 `14`개, Blend `1`개와 내보내기 파일 `2`개가 있습니다.
- Unity 런타임에는 적용하지 않았습니다. 프로젝트 원본
  `Assets/_Project/Art/Enemies/Pahur/Models/Pahur.fbx`는
  `416,412`바이트, SHA-256
  `9D376E7BD5262E9DF347D8CCC37675DDBCF647D063011C0A71E80CDDA4A9DEA3`,
  마지막 수정 `2026-07-30 16:45:19`로 유지됐고,
  `CargoRunMvp.unity`도 `15,883,165`바이트, SHA-256
  `EC9B371A9E0A55A4D59812D382AEE7F4867FC6B290B2258B7D82A2F7E2C54DC9`,
  마지막 수정 `2026-07-30 16:55:34`로 유지됐습니다.
- `Run-HarnessValidation.ps1` 및 관련 작업, EditMode·PlayMode 테스트,
  Windows 빌드, Smoke·Validate·Ensure 계열 명령, Unity 리프레시·
  씬 저장·런타임 적용과 Git 쓰기 작업은 실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 몸통 구조 통일 아트 샘플 13차

- 사용자 피드백에 따라 몸통이 기준 이미지처럼 정형화되지 않고
  아래로 무너져 보이는 원인을 몸통 연결 표면과 정면 렌더에서 다시
  직접 확인했습니다.
- 주 몸통 연결 표면 하나에 청회색 장갑 `114`면, 짙은 기계부
  `39`면, 얼굴 금속 `15`면, 어깨 금속 `74`면이 섞여 불규칙한
  삼각 파편처럼 보이는 상태를 확인했습니다.
- 해당 연결 표면과 몸통 연결부를 포함한 `398`면을
  `torso_rigid_shell` 전용 재질로 통일했습니다. 전신 오버레이는
  몸통과 다리에서 제거하고 몸통 전용 색상·발광 맵으로 밝은 중앙
  흉갑, 청회색 측면 장갑, 네 단의 수평 복부 모듈, 좁은 골반
  하우징을 구분했습니다.
- 아래로 흐르는 것처럼 보이던 세로 주황 표시를 기준 이미지와 같은
  짧은 가로형 상태 표시로 바꿨습니다. 몸통 전용 맵은 높이·범프·
  노멀에 연결하지 않았고 외곽선이나 가짜 입체 패널도 만들지
  않았습니다.
- 정면·사선·측면·후면 렌더와 함께 몸통 전용 근접 렌더
  `renders/12_torso_front_detail.png`, 기준 이미지 직접 비교본
  `renders/13_torso_detail_comparison.png`를 생성해 시각 확인했습니다.
- `index.html`, `summary.html`, `README.md`,
  `TEXTURE_ANALYSIS.md`를 기존 지정 HTML 형식에 맞춰 갱신했습니다.
  상태는 `2026-07-30_torso_rigid_component_unification_13`,
  `PENDING_USER_REVIEW`입니다.
- 구조 보존 검사는 `PASS`했습니다. 원본 FBX, 검토 Blend, 내보낸
  FBX가 정점 `2,445`, 에지 `6,575`, 면 `4,348`, 루프 `13,044`,
  본 `24`, UV 레이어와 버텍스 그룹에서 일치합니다. 새 메시 생성,
  기존 메시 삭제·이동·변형, 실루엣·UV·스킨 웨이트·리그 변경은
  하지 않았습니다.
- HTML 정적 DOM·이미지·링크 검사는 `PASS`했으며 깨진 이미지와
  링크는 `0`개입니다. 몸통 전용 렌더와 직접 비교본도 검토 목록에
  포함했습니다.
- Unity 런타임에는 적용하지 않았습니다. 프로젝트 원본 FBX와
  `CargoRunMvp.unity`는 수정하지 않았습니다.
- `Run-HarnessValidation.ps1` 및 관련 작업, EditMode·PlayMode 테스트,
  Windows 빌드, Smoke·Validate·Ensure 계열 명령, Unity 리프레시·
  씬 저장·런타임 적용과 Git 쓰기 작업은 실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 몸통 축·표면 반응 재배치 아트 샘플 14차

- 사용자가 제공한 현재 샘플 캡처와
  `image/pāḫḫur(파후르).png`를 직접 비교해, 흉갑이 왼쪽으로
  밀리고 목 주변의 색 조각이 세로로 늘어져 몸통이 무너져 보이는
  상태를 다시 확인했습니다.
- 주 몸통 구성 요소의 로컬 중심은 `x≈18.6`이며 전체 오브젝트
  Generated 좌표 약 `0.648`, 1024 픽셀 투영 맵의 `664px`에
  해당합니다. 기존 몸통 맵 중심은 `624px`로 설정돼 흉갑·복부·
  골반이 실제 몸통 축보다 왼쪽에 배치돼 있었습니다.
- 몸통 투영 중심을 `664px`로 바로잡고 목·쇄골의 조각형 투영과
  큰 평면 흉갑 배치를 제거했습니다. 기존 면 위에서 어두운 상부
  기계 하부 구조, 좌우 중앙 흉갑, 네 단의 수평 복부 모듈, 짧은
  골반 하우징이 한 축으로 연속되도록 다시 배열했습니다.
- 몸통 전용 알베도·발광 맵에 더해 금속성·거칠기 맵을 추가하고,
  밝은 강철판·청회색 장갑·어두운 내부 기계부가 서로 다른 반사와
  거칠기로 읽히게 했습니다. 몸통 전용 맵은 높이·범프·노멀에
  연결하지 않았습니다.
- 정면·사선·몸통 근접 렌더를 세 차례 직접 확인하며 중심축, 복부
  적층, 흉갑 테두리 두께와 상부 명도를 보정했습니다.
  `renders/13_torso_detail_comparison.png`에서 기준 이미지와 새
  몸통 배열을 나란히 검토했습니다.
- 상태는
  `2026-07-30_torso_axis_surface_response_relayout_14`,
  `PENDING_USER_REVIEW`입니다. `index.html`, `summary.html`,
  `README.md`, `TEXTURE_ANALYSIS.md`와 에셋 매니페스트를 기존
  샘플 형식에 맞춰 갱신했습니다.
- 구조 보존 검사는 `PASS`했습니다. 원본 FBX, 검토 Blend, 내보낸
  FBX가 정점 `2,445`, 에지 `6,575`, 면 `4,348`, 루프 `13,044`,
  본 `24`, UV 레이어와 버텍스 그룹에서 일치합니다. 메시 생성·
  삭제·이동·변형, 실루엣·토폴로지·UV·본·스킨 웨이트·애니메이션
  변경은 하지 않았습니다.
- HTML 정적 DOM·이미지·링크 검사는 `PASS`했으며 깨진 이미지와
  링크는 `0`개입니다. 샘플에는 텍스처 `84`개, 최종 렌더 `6`개,
  분석 렌더 `16`개, Blend `1`개와 내보내기 파일 `2`개가 있습니다.
- Unity 런타임에는 적용하지 않았습니다. 프로젝트 원본 FBX와
  `CargoRunMvp.unity`는 수정하지 않았습니다.
- `Run-HarnessValidation.ps1` 및 관련 작업, EditMode·PlayMode 테스트,
  Windows 빌드, Smoke·Validate·Ensure 계열 명령, Unity 리프레시·
  씬 저장·런타임 적용과 Git 쓰기 작업은 실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 몸통 연결 표면 갑주화 아트 샘플 15차

- 사용자 피드백에 따라 팔과 다리는 작업 전 상태로 고정하고, 몸통에
  투영한 무늬가 아니라 실제 모델 표면을 따라 갑주가 구성되도록
  몸통 범위만 다시 분석했습니다.
- 몸통 `442`면은 실제로 분리된 `7`개 연결 표면
  (`1`, `27`, `61`, `63`, `84`, `96`, `97`)으로 구성되어 있음을
  확인했습니다. 법선 인접 각도 `32°`로 나누면 `122`개의 작은
  조각이 되어 삼각 파편과 얼룩이 생기므로 법선 클러스터 방식은
  사용하지 않았습니다.
- 이전 2D 몸통 색상·발광·금속성·거칠기 투영 연결을 셰이더에서
  전부 제거했습니다. 실제 연결 표면 단위로 큰 청회색 외장, 밝은
  상부 중앙판, 중간 명도의 중앙·골반 갑주, 짙은 내부 기계부를
  배정했으며 임의 패널 선·상태 표시·복부 무늬는 만들지 않았습니다.
- 중앙 하부 연결 표면은 처음에 내부 기계 재질로 배정했을 때 검은
  공백처럼 보이는 것을 직접 확인해, 같은 연결 표면 전체를 중간
  명도의 강철 갑주로 바꿨습니다. 정면·사선·몸통 근접 렌더와
  `renders/13_torso_detail_comparison.png`를 다시 생성해 직접
  비교했습니다.
- 팔·다리 보호 재질 `leg_steel`, `shoulder_machine_blue`,
  `left_arm_machine`, `left_hand_machine`의 배정 `1,319`면 SHA-256은
  작업 전후 모두
  `905369811883C28D0BACEAED7910D27A5340D806604760C551EB341BC213C857`
  이며, 관련 알베도·메탈릭·노멀·러프니스 텍스처 `16`개의 SHA-256도
  전부 작업 전과 일치합니다.
- 상태는 `2026-07-30_torso_connected_surface_armor_15`,
  `PENDING_USER_REVIEW`입니다. `index.html`, `summary.html`,
  `README.md`, `TEXTURE_ANALYSIS.md`, 연결 표면 분석 JSON과 렌더,
  내보내기·매니페스트를 갱신했습니다.
- 구조 보존 검사는 `PASS`했습니다. 원본 FBX, 검토 Blend, 내보낸
  FBX가 정점 `2,445`, 에지 `6,575`, 면 `4,348`, 루프 `13,044`,
  본 `24`, UV 레이어와 버텍스 그룹에서 일치합니다. 메시 생성·
  삭제·이동·변형, 실루엣·토폴로지·UV·본·스킨 웨이트·애니메이션
  변경은 하지 않았습니다.
- HTML 정적 DOM·이미지·링크 검사는 `PASS`했으며 깨진 이미지와
  링크는 `0`개입니다. 샘플에는 텍스처 `96`개, 최종 렌더 `6`개,
  분석 렌더 `19`개, Blend `1`개와 내보내기 파일 `2`개가 있습니다.
- Unity 런타임에는 적용하지 않았습니다. 프로젝트 원본 FBX SHA-256
  `9D376E7BD5262E9DF347D8CCC37675DDBCF647D063011C0A71E80CDDA4A9DEA3`,
  `CargoRunMvp.unity` SHA-256
  `EC9B371A9E0A55A4D59812D382AEE7F4867FC6B290B2258B7D82A2F7E2C54DC9`
  는 유지됐습니다.
- `Run-HarnessValidation.ps1` 및 관련 실행·재실행·조사·로그·문서·
  생성물 작업, `Run-EditModeTests.ps1`, `Run-PlayModeTests.ps1`,
  `Build-WindowsDev.ps1`, Smoke·Validate·Ensure 계열 명령, Unity
  리프레시·씬 저장·런타임 적용과 Git 쓰기 작업은 실행하지
  않았습니다.

## 사용자 검토 대기 - 파후르 중앙 상단 갑주 배치 수정 16차

- 사용자가 지적한 흰 네모판은 몸통 연결 표면 `97`의 `18`면으로,
  실제 중심 갑주 `63`보다 위쪽에 작고 불규칙하게 따로 놓여
  있었습니다. 밝은 재질 자체뿐 아니라 이 표면을 중앙 갑주로
  지정한 역할 배치가 어색함의 원인이었습니다.
- 연결 표면 `97`을 `torso_center_plate`에서 주변 상부 외장과 같은
  `torso_rigid_shell`로 재배정했습니다. 중심축을 따라 이어지는 큰
  연결 표면 `63`에만 중앙 갑주 역할을 남겼으며, 메시나 표면 위치를
  이동·변형하지 않았습니다.
- 정면·3/4·몸통 확대 렌더를 직접 확인해 상단의 흰 네모 강조가
  사라지고, 상부 외장에서 중앙 갑주로 이어지는 배치가 정리된 것을
  확인했습니다. `renders/13_torso_detail_comparison.png`의 문구와
  비교본도 새 배치에 맞춰 갱신했습니다.
- 팔·다리 보호 재질 배정 `1,319`면 SHA-256은
  `905369811883C28D0BACEAED7910D27A5340D806604760C551EB341BC213C857`
  로 작업 전과 동일합니다. 중앙 하단 연결 표면 `63·84`의 `60`면
  SHA-256도
  `FE967A501D3FFED1A2BEC6EF742783F3596D8847FD6879DC59883835CD5BDBA7`
  로 작업 전과 동일하며 관련 텍스처는 수정하지 않았습니다.
- 상태는 `2026-07-30_upper_torso_plate_relayout_16`,
  `PENDING_USER_REVIEW`입니다. `index.html`, `summary.html`,
  `README.md`, `TEXTURE_ANALYSIS.md`, 검토 Blend·FBX·GLB와 렌더,
  에셋 매니페스트를 갱신했습니다.
- 구조 보존 검사는 `PASS`했습니다. 원본 FBX, 검토 Blend, 내보낸
  FBX가 정점 `2,445`, 에지 `6,575`, 면 `4,348`, 루프 `13,044`,
  본 `24`, UV 레이어와 버텍스 그룹에서 일치합니다.
- HTML 정적 DOM·이미지·링크 검사는 `PASS`했으며 깨진 이미지와
  링크는 `0`개입니다. 직접 시각 검토 목록의 정면·3/4·몸통
  비교본도 새 렌더로 갱신했습니다.
- Unity 런타임에는 적용하지 않았습니다. 프로젝트 원본 FBX SHA-256
  `9D376E7BD5262E9DF347D8CCC37675DDBCF647D063011C0A71E80CDDA4A9DEA3`,
  `CargoRunMvp.unity` SHA-256
  `EC9B371A9E0A55A4D59812D382AEE7F4867FC6B290B2258B7D82A2F7E2C54DC9`
  는 유지됐습니다.
- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·
  로그·문서·생성물 작업은 실행하지 않았습니다. Unity 반영·
  리프레시·씬 저장과 Git 쓰기 작업도 실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 중앙 상단 독립 판 실제 삭제 17차

- 사용자는 재질 통합으로 상단 판을 숨기는 방식이 아니라 배치를
  조정하거나 판 자체를 삭제하도록 지시했고, 위치를 임의 추측하지
  않는 방식으로 독립 연결 표면 `97`을 실제 삭제했습니다.
- 삭제 대상은 원본 FBX의 연결 표면 `97`에 속한 정점 `11`, 에지
  `27`, 면 `18`, 루프 `54`입니다. 이 표면은 다른 연결 표면과
  정점을 공유하지 않는 독립 섬임을 확인한 뒤 검토용 Blend·FBX·
  GLB에서만 제거했습니다.
- 삭제 전후 남은 메시의 정점 좌표·본 가중치 SHA-256
  `47DCDA261342E5E53904AAD1AF42686C4090842466998BE02A3AF6DD1B7018FE`,
  에지 SHA-256
  `3E05637F85678B495BC540BE56ECA4E949C64BCC6E4297C8764E51C1CEC217A1`,
  면·UV SHA-256
  `BB0A7BF300D5528052707F60A2395FFD557CF5DF63E66F8F7172F1032C1D1121`
  이 각각 삭제 전후 일치합니다.
- 검토 Blend와 FBX는 승인 삭제 후 예상 구조인 정점 `2,434`, 에지
  `6,548`, 면 `4,330`, 루프 `12,990`, 본 `24`와 일치하며 샘플
  전용 삭제 범위 구조 검사는 `PASS`했습니다.
- 정면·3/4·몸통 확대 렌더에서 중앙 상단 독립 판이 실제로 사라진
  것을 직접 확인했습니다. `renders/13_torso_detail_comparison.png`
  에 기준 이미지와 삭제 후 몸통을 나란히 정리했습니다.
- 팔·다리 보호 재질 배정 `1,319`면 SHA-256
  `905369811883C28D0BACEAED7910D27A5340D806604760C551EB341BC213C857`,
  중앙 하단 `63·84`의 `60`면 SHA-256
  `FE967A501D3FFED1A2BEC6EF742783F3596D8847FD6879DC59883835CD5BDBA7`
  은 작업 전과 동일합니다.
- 상태는 `2026-07-30_upper_torso_plate_deleted_17`,
  `PENDING_USER_REVIEW`입니다. `index.html`, `summary.html`,
  `README.md`, `TEXTURE_ANALYSIS.md`, 검토 Blend·FBX·GLB, 렌더,
  삭제 범위 구조 검사 자료와 에셋 매니페스트를 갱신했습니다.
- HTML 정적 DOM·이미지·링크 검사는 `PASS`했으며 깨진 이미지와
  링크는 `0`개입니다.
- 프로젝트 원본 FBX SHA-256
  `9D376E7BD5262E9DF347D8CCC37675DDBCF647D063011C0A71E80CDDA4A9DEA3`,
  `CargoRunMvp.unity` SHA-256
  `EC9B371A9E0A55A4D59812D382AEE7F4867FC6B290B2258B7D82A2F7E2C54DC9`
  는 유지됐고 Unity에는 적용하지 않았습니다.
- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·
  로그·문서·생성물 작업은 실행하지 않았습니다. Unity 반영·
  리프레시·씬 저장과 Git 쓰기 작업도 실행하지 않았습니다.

## 사용자 검토 대기 - 파후르 얼굴 표면 고정·광학부 재작업 18차

- 기준 이미지의 머리를 직접 확대해 두건 바로 아래에 분리된 두 개의
  청색 광학부가 있고 얼굴은 입·세로 환기구·추가 패널이 없는
  매끄러운 중암도 건메탈임을 다시 기준으로 삼았습니다.
- 머리 면을 `height >= 164.0`으로 나누던 좌표 임계값을 폐기했습니다.
  정면·3/4·측면 토폴로지에서 확정한 원본 머리 폴리곤 `126`개를
  두건 표면으로 명시하고 나머지 기존 금속 안면 표면과 분리했습니다.
- 얼굴 알베도에 실제로 추가되던 7픽셀 간격의 가로선과 러프니스
  파형을 제거했습니다. 얼굴 노멀과 높이 맵은 평탄하고, 메탈릭과
  러프니스에는 반복 무늬가 없습니다.
- 눈과 이마 문양은 전신 바운딩박스 `Generated` 좌표 투영에서
  기존 머리 로컬 표면 전용 투영으로 변경했습니다. 눈은 두건 바로
  아래에서 중앙 금속 간격을 두고 좌우로 분리되며 얼굴 중앙·하단에는
  발광 픽셀이 없습니다.
- 이마 화염 문양은 임의 도형으로 생성하지 않고 원본 기준 이미지의
  화염 문양을 직접 크롭해 투명 데칼로 사용했습니다.
- `renders/07_head_front_detail.png`,
  `renders/07_head_three_quarter_detail.png`,
  `renders/08_head_detail_comparison.png`를 생성해 기준 이미지·정면·
  3/4를 직접 대조했습니다. 두건 천, 분리된 두 눈, 매끄러운 무구강
  금속 얼굴 순서를 확인했습니다.
- 검토 Blend와 FBX는 기존 승인 삭제 상태인 정점 `2,434`, 에지
  `6,548`, 면 `4,330`, 루프 `12,990`, 본 `24`를 유지했습니다.
  이번 작업에서 새 메시 생성·이동·변형·삭제나 UV·스킨 웨이트·
  리그 변경을 하지 않았습니다.
- 몸통·팔·다리·가스통·무기 재질 배정과 텍스처는 작업 전 상태를
  유지했습니다. 보호 텍스처 `80`개의 통합 SHA-256은 작업 전후
  `CAC4277783A05F7863F5B767E46177B83640E382071EE98A194CE22F51E3F5E0`,
  비머리·비두건 표면 배정 `3,887`면의 통합 SHA-256은
  `FFBEDFD78F5B9A92280DC2D93E239E269F60CB380D6D6B455A0B0023EEC87AE8`
  로 일치했습니다. Unity에는 적용하지 않았습니다.
- 얼굴 노멀은 전체 픽셀 `(128, 128, 255)`, 얼굴 높이 맵은 전체
  픽셀 `(128, 128, 128)`, 얼굴 러프니스는 전체 픽셀 `(88, 88, 88)`,
  메탈릭은 전체 픽셀 `(218, 218, 218)`로 평탄함을 확인했습니다.
  발광 맵의 연결 영역은 좌우 두 개뿐이며 각각의 경계는
  `(156, 581, 345, 639)`, `(475, 581, 664, 639)`입니다.
- 구조 보존과 내보내기 검사는 `PASS`했습니다. HTML 정적 DOM·이미지·
  링크 검사는 `PASS`, 깨진 이미지·링크는 `0`개입니다. 인앱 브라우저
  연결은 환경 메타데이터 부재로 차단되어 정면·3/4·직접 비교 렌더를
  대신 직접 확인했습니다.
- Blender 실행 중 설치된 Auto Rig Pro의 `load_pre` 정리 핸들러
  경고가 출력됐지만 샘플 생성·렌더·Blend 저장·FBX/GLB 내보내기와
  내보내기 검사는 종료 코드 `0`으로 완료됐습니다.
- 상태는 `2026-07-30_face_surface_locked_optics_rework_18`,
  `PENDING_USER_REVIEW`입니다.
- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·
  로그·문서·생성물 작업, EditMode·PlayMode 테스트, Windows 빌드,
  Unity 반영·리프레시·씬 저장과 Git 쓰기 작업은 실행하지
  않았습니다.

## 사용자 검토 대기 - 파후르 좌우 안면판 추종 눈 재작업 19차

- 기준 이미지와 18차 정면·3/4 머리 렌더를 직접 비교해, 기존 두 눈이
  얼굴 전체의 한 X/Y 평면에 놓여 3/4에서 가까운 눈이 늘어나고 먼
  눈이 눌리는 원인을 확인했습니다.
- 원본 얼굴 `208`면의 정적 좌표·법선·UV·인접 관계와 렌더 프레임에서
  본 포즈가 적용된 평가 좌표·법선을 `EYE_SURFACE_ANALYSIS.json`에
  기록했습니다. 분석 스크립트는 원본 FBX를 읽기만 하며 메시를
  저장하거나 변경하지 않습니다.
- 눈 그래픽은 하나의 렌즈 원본만 담도록 바꾸고, 렌더 프레임에서
  평가된 좌우 상단 안면판의 접선축에 각각 독립 배치했습니다. 얼굴
  전체의 공통 평면 사각 투영은 제거했습니다.
- `renders/07_head_front_detail.png`,
  `renders/07_head_three_quarter_detail.png`,
  `renders/08_head_detail_comparison.png`를 갱신했습니다. 정면에서는
  중앙 금속 간격을 유지하고, 3/4에서는 가까운 눈과 먼 눈이 각각
  해당 표면의 경사와 원근을 따르는 것을 직접 확인했습니다.
- 눈은 중앙 금속·관자놀이·두건·얼굴 하단을 침범하지 않습니다.
  얼굴 금속, 두건, 화염 문양과 몸통·팔·다리·가스통·무기 텍스처와
  재질 배정은 수정하지 않았습니다.
- 정면 청색 눈 영역은 `(824, 484, 888, 504)`와
  `(976, 484, 1038, 502)`, 3/4는 `(729, 479, 772, 501)`와
  `(849, 476, 924, 494)`의 서로 분리된 두 연결 영역으로
  확인했습니다.
- 눈 오버레이·발광 원본 두 파일을 제외한 보호 텍스처 `94`개의
  통합 SHA-256은 작업 전후
  `63689B13EA62CE9CAE924CE1B4C1F2A9054C306703383435B587C77B72E84125`
  로 일치했습니다. 검토 Blend와 직전 백업 Blend의 전체 `4,330`면
  재질 배정 통합 SHA-256도
  `F2AEEF13E95C79CB54DFEBB4D2FC6F568D147069FD291268B38B58880C081520`
  로 일치했습니다.
- 메시 이동·변형·생성·추가 삭제, UV·본 가중치·리그·애니메이션
  변경은 하지 않았습니다. Unity에도 적용하지 않았습니다.
- 구조 보존과 내보내기 검사는 `PASS`했고, 원본 FBX에서 승인된
  독립 연결 표면 `97`만 빠진 정점 `2,434`, 에지 `6,548`,
  면 `4,330`, 루프 `12,990`, 본 `24`를 유지했습니다. HTML 정적
  DOM·이미지·링크 검사도 `PASS`했으며 깨진 이미지·링크는 `0`개입니다.
- 프로젝트 원본 FBX SHA-256
  `9D376E7BD5262E9DF347D8CCC37675DDBCF647D063011C0A71E80CDDA4A9DEA3`,
  `CargoRunMvp.unity` SHA-256
  `EC9B371A9E0A55A4D59812D382AEE7F4867FC6B290B2258B7D82A2F7E2C54DC9`
  는 유지됐습니다.
- Blender 실행 중 설치된 Auto Rig Pro의 `load_pre` 정리 핸들러
  경고가 출력됐지만 분석·샘플 생성·렌더·Blend 저장·FBX/GLB
  내보내기와 구조 검사는 모두 종료 코드 `0`으로 완료됐습니다.
- 상태는 `2026-07-30_face_surface_fitted_eyes_rework_19`,
  `PENDING_USER_REVIEW`입니다.
- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·
  로그·문서·생성물 작업, EditMode·PlayMode 테스트, Windows 빌드,
  Unity 반영·리프레시·씬 저장과 Git 쓰기 작업은 실행하지
  않았습니다.

## 사용자 검토 대기 - 파후르 화난 눈 각도 재작업 20차

- 기준 이미지의 눈은 양쪽 모두 바깥쪽 눈꼬리가 높고 얼굴 중앙 쪽
  끝이 낮아 화난 인상을 내지만, 19차 정면 렌더는 화면 왼쪽 눈
  `-6.5°`, 오른쪽 눈 `+3.6°`로 반대 방향에 가까운 것을 확인했습니다.
- 좌우 눈의 기존 중심·크기·색·소켓·발광과 각 안면판의 접선축은
  유지하고, 접선 평면 안에서 화면 왼쪽 눈 `-16°`, 오른쪽 눈
  `-14°`의 회전만 추가했습니다. 두 U축은 바깥쪽에서 얼굴 중앙을
  향하므로 두 값 모두 중앙 쪽 끝을 낮춥니다.
- 수정 후 정면 청색 영역 주축은 화면 왼쪽 `+8.9°`, 오른쪽
  `-11.6°`, 3/4는 각각 `+12.8°`, `-11.8°`로 확인됐습니다.
  두 눈은 각 안면판 표면과 원근을 유지하면서 중앙을 향해 내려갑니다.
- 얼굴 금속·렌즈 텍스처·두건·화염 문양과 몸통·팔·다리·가스통·
  무기 재질 및 텍스처는 수정하지 않았습니다.
- 메시 생성·이동·변형·추가 삭제, UV·본 가중치·리그·애니메이션
  변경은 하지 않았습니다. Unity에도 적용하지 않았습니다.
- `renders/07_head_front_detail.png`,
  `renders/07_head_three_quarter_detail.png`,
  `renders/08_head_detail_comparison.png`를 갱신해 기준 이미지와
  정면·3/4를 직접 대조했습니다.
- 보호 텍스처 `94`개의 통합 SHA-256은 작업 전후
  `63689B13EA62CE9CAE924CE1B4C1F2A9054C306703383435B587C77B72E84125`
  로 일치했습니다. 눈 오버레이
  `853F4DFE51FD9C6575436A714A4CD0C3DD21CAF2DA948C3E9F4CA51965DB8B83`,
  발광 원본
  `9ACC8FC9CF1A08B41625C82E332B24EF017CB8EA0B61D861B645924FE9F5AAB1`
  도 작업 전후 일치했습니다.
- 검토 Blend와 직전 19차 백업 Blend의 전체 `4,330`면 재질 배정
  통합 SHA-256은 모두
  `F2AEEF13E95C79CB54DFEBB4D2FC6F568D147069FD291268B38B58880C081520`
  로 일치했습니다.
- 구조 보존과 내보내기 검사는 `PASS`했고 정점 `2,434`, 에지
  `6,548`, 면 `4,330`, 루프 `12,990`, 본 `24`를 유지했습니다.
  HTML 정적 DOM·이미지·링크 검사도 `PASS`했으며 깨진 이미지와
  링크는 `0`개입니다.
- 프로젝트 원본 FBX SHA-256
  `9D376E7BD5262E9DF347D8CCC37675DDBCF647D063011C0A71E80CDDA4A9DEA3`,
  `CargoRunMvp.unity` SHA-256
  `EC9B371A9E0A55A4D59812D382AEE7F4867FC6B290B2258B7D82A2F7E2C54DC9`
  는 유지됐습니다.
- Blender 실행 중 설치된 Auto Rig Pro의 `load_pre` 정리 핸들러
  경고가 출력됐지만 샘플 생성·렌더·Blend 저장·FBX/GLB 내보내기와
  구조 검사는 모두 종료 코드 `0`으로 완료됐습니다.
- 상태는 `2026-07-30_face_surface_fitted_angry_eyes_rework_20`,
  `PENDING_USER_REVIEW`입니다.
- `Run-HarnessValidation.ps1` 하네스 검증 및 관련 실행·재실행·조사·
  로그·문서·생성물 작업, EditMode·PlayMode 테스트, Windows 빌드,
  Unity 반영·리프레시·씬 저장과 Git 쓰기 작업은 실행하지
  않았습니다.

## 파후르 모델 교체·외형 샘플 작업 종료 체크포인트

- 사용자는 20차 화난 눈 각도 수정 결과를 확인한 뒤 `좋아. 일단
  여기까지`라고 답하고, 현재까지의 작업을 진행문서에 기록해
  GitHub에 커밋·푸시하도록 요청했습니다.
- Unity의 `Approved Pahur Enemy Placement` 아래 배치된 파후르
  `10`개는 `enemies model/pāḫḫur.fbx`를 기준으로 교체했습니다.
  슬롯 이름·배치 Transform과 파후르 외 씬 루트는 유지했습니다.
- 교체 모델을 기준으로 `image/pāḫḫur(파후르).png`의 외형을 분석해
  `artSample/enemies/pahur/appearance_reference_sync/`에 검토 가능한
  Blender·FBX·GLB·정적 렌더·HTML·텍스처·머티리얼·분석 자료를
  누적 제작했습니다.
- 외형 샘플은 기준 이미지의 기계 갑주 인상을 목표로 몸통 연결
  표면별 재질을 재구성했고, 중앙 상단의 어색한 독립 연결 표면
  `97`은 사용자 지시에 따라 검토 샘플에서 삭제했습니다. 그 외
  메시의 좌표·UV·본 가중치·리그·애니메이션은 유지했습니다.
- 머리는 기존 모델 표면을 기준으로 두건과 매끄러운 금속 얼굴을
  구분하고, 기준 이미지에 없는 입·환기구·추가 얼굴 무늬는
  제거했습니다. 좌우 눈은 각 안면판의 접선과 원근을 따르면서
  바깥쪽 눈꼬리가 높고 중앙 쪽 끝이 낮은 화난 인상이 되도록
  최종 조정했습니다.
- 최종 아트 샘플 리비전은
  `2026-07-30_face_surface_fitted_angry_eyes_rework_20`입니다.
  샘플 메타데이터는 `PENDING_USER_REVIEW`이며, 이 외형 샘플을
  Unity 머티리얼·텍스처로 적용하는 작업은 수행하지 않았습니다.
- 기존에 완료한 구조 보존·내보내기 검사는 `PASS`, HTML 정적
  DOM·이미지·링크 검사는 `PASS`였고 깨진 이미지·링크는
  `0`개였습니다. 이번 종료 기록 및 Git 반영 단계에서는
  `Run-HarnessValidation.ps1` 하네스 검증과 관련 작업,
  EditMode·PlayMode 테스트, Windows 빌드, Unity 실행·리프레시·
  씬 저장을 수행하지 않습니다.
- 이번 Git 반영 범위는 파후르 모델 교체, 파후르 배치 교체 도구,
  해당 씬 변경, 파후르 검토 캡처, 외형 동기화 아트 샘플과 본
  진행문서입니다. Resistance·Revolution 생성물, Blender 백업
  `.blend1`, Python `__pycache__`는 관련 없는 작업 또는 임시
  생성물이므로 커밋에서 제외합니다.
