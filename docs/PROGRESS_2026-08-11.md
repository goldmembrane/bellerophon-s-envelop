# Bellerophon Progress — 2026-08-11

## Current status

`ISPANT_SLOT8_FULL_DRAW_START_PLAYMODE_VERIFIED`

이슈판트 8번 모션에서 오른팔을 내린 뒤 발검으로 넘어갈 때 발생하던 시각적 멈춤을 제거했고, 사용자의 추가 확인에 따라 4번 발검 모션을 잘린 구간 없이 원본 0프레임부터 이어재생하도록 수정했습니다. 이번 상태가 현재 기준입니다.

## 이번 GitHub 반영까지의 누적 작업

- 작업 규칙에 사용자가 설명한 명확한 지시를 임의 추론 없이 그대로 적용한다는 절대 규칙을 추가했습니다.
- 이슈판트 애니메이션 목록의 7번 설명에서 사용자가 지정하지 않은 동작 표현을 삭제했습니다.
- 6번 장검 납도 후 머스켓 조준 모션의 양손과 머스켓 접촉, 방아쇠 아래 오른손 배치, 팔과 머스켓 0.1m 상향, 몸통을 따라 움직이는 왼쪽 허리 장검을 반영했습니다.
- 7번 개체를 제공된 `išpant firing.fbx`로 교체하고 정적 이슈판트 외형·크기·Y축 높이를 동기화했습니다. 등 뒤 중복 머스켓을 제거하고 6번 마지막 양손 조준 자세, 원본 기획 공격 간격, 총구 발사 이펙트와 왼쪽 허리 장검을 적용했습니다.
- 8번 개체를 제공된 `išpant changing to sword.fbx`로 교체하고 정적 외형과 장비를 동기화했습니다. 140프레임에 손 머스켓이 사라지고 등 뒤 머스켓이 나타나도록 했으며, 오른팔 복귀와 손 내리기 뒤 4번 발검으로 연결했습니다.
- 8번의 분리된 Animator 상태 경계를 제거하고 `원본 8번 3.4초 → 별도 멈춤 0초 → 중간 동작 0.3초 → 4번 발검 전체 1.76초`를 단일 5.46초 루프로 결합했습니다.
- 실제 CargoRunMvp Play Mode에서 8번을 2회 이상 정상 속도와 0.5배속 영상으로 확인했으며, 최종 저장 검사에서 외형·장비·배치 오차 없이 `SceneChanged=False`를 확인했습니다.

### Git 커밋 포함 범위

- 이슈판트 6·7·8번 애니메이션 자산, 생성·검사 도구, Unity 브리지 명령과 CargoRunMvp 장면
- 실제 사용한 7번 발사 및 8번 머스켓에서 장검으로 전환하는 원본/프로젝트 FBX
- 2026-08-10~11 진행문서와 관련 검증 산출물
- 6·7·8번 작업에 필요한 기존 진행문서의 표현 정정과 사용자 지정 에이전트 규칙
- 임시 `__pycache__`, 폐기 산출물, 사용하지 않은 다른 이슈판트 원본 모션은 제외

## 원인

- 8번 동작이 `머스켓 전환`, `0.3초 중간 동작`, `4번 발검 이어재생`의 세 Animator 상태와 종료 시점 전환으로 분리되어 있었습니다.
- 중간 동작의 보간만 바꾸면 멈추는 위치가 상태 경계 사이에서 이동할 뿐, 경계 평가 자체는 남았습니다.
- 생성 시에는 정상 길이였던 보조 클립의 `AnimationClipSettings.stopTime`이 저장 후 원본 설정값으로 직렬화되는 문제도 함께 확인했습니다.
- 발검 이어재생 시작값이 14프레임으로 설정되어 있어 원본 발검의 첫 0.56초가 제외되어 있었습니다.

## 적용

- 세 구간의 실제 포즈를 60fps로 결합한 `Ispant_08_StowMusketDrawSword_ContinuousSequence` 클립을 생성했습니다.
- AnimatorController를 전환 없는 단일 상태·단일 4.9초 루프로 변경했습니다.
- 사용자 지정 구성은 그대로 유지했습니다.
  - 원본 8번 동작: 3.4초
  - 오른팔을 내린 뒤 별도 유지: 0초
  - 즉시 이어지는 중간 동작: 0.3초
  - 4번 발검 이어재생 시작: 원본 0프레임/0초
  - 잘라낸 발검 앞부분: 0초
  - 4번 발검 이어재생: 원본 전체 1.76초
- 머스켓 140프레임 전환, 등 뒤 머스켓, 장검, 정적 외형 동기화, Y축 배치와 장비 종속 관계는 변경하지 않았습니다.
- 보조 클립과 연속 클립의 `startTime`/`stopTime`을 명시해 저장 후에도 3.4초/0.3초/1.76초/5.46초 길이가 유지되도록 했습니다.

## 검증

- 저장 후 `InspectIspant08ChangingToSwordReplacement`: PASS
  - `ClipLength=3.4`
  - `TransitionBridgeSeconds=0.3`
  - `DrawContinuationSourceStartFrame=0`
  - `DrawContinuationSourceStartTime=0`
  - `DrawContinuationRemovedDuplicatePreDrawSeconds=0`
  - `DrawContinuationSeconds=1.76`
  - `SequenceCycleSeconds=5.46`
  - `LoweredPoseHoldSeconds=0`
  - `SceneChanged=False`
- CargoRunMvp 실제 Play Mode에서 8번만 전용 레이어로 렌더링하고 2.04617회 반복 녹화했습니다.
- GPU 비동기 읽기를 사용해 재생 루프를 정지시키지 않고 296프레임, 약 26.41fps로 기록했습니다.
- 정상 속도 영상에서 0.25초 이상, 0.5배속 영상에서 0.5초 이상 완전히 같은 화면이 유지되는 구간은 없었습니다.
- 연속성 완료 판정은 정지 캡처가 아니라 실제 두 반복 MP4를 기준으로 했습니다.

검증 산출물:

- `docs/validation/ispant_changing_to_sword_2026-08-11/actual_playmode_motion/Ispant_08_ContinuousMotion_TwoLoops.mp4`
- `docs/validation/ispant_changing_to_sword_2026-08-11/actual_playmode_motion/Ispant_08_ContinuousMotion_TwoLoops_HalfSpeed.mp4`
- `docs/validation/ispant_changing_to_sword_2026-08-11/actual_playmode_motion/Ispant_08_ContinuousMotion_Frames.csv`
- `docs/validation/ispant_changing_to_sword_2026-08-11/actual_playmode_motion/Ispant_08_ContinuousMotion_Analysis.txt`

## 실행하지 않은 항목

- 하네스 검증
- EditMode 테스트
- PlayMode 테스트
- Windows 개발 빌드
- Ensure, Validate, Smoke 계열 명령
- Unity 재시작 또는 복구
- Git 변경 작업
