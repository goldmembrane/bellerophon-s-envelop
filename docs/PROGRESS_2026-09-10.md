# 2026-09-10 진행 기록

> 현재 상태: **사용자 지시로 손가락 작업 중단.** 기존 공용 양손 리그와 네 소비품 모션·소품 배치는 로컬에 적용돼 있다. 이번 오른손 형상/가중치 복구는 후보에만 있고, 공용 원본과 144개 사용처에는 적용하지 않았다. 마지막 파지 재계산은 종료됐으나 직접 확인하지 않은 상태다. 손가락 수정 완료로 판정하지 않는다.

## 00:43 사용자 중단 지시와 작업 기록 확정

### 이미 적용된 작업

- 대상은 `Battery_Aux_Plug`, `Converter_Shield_Plug`, `Buffer_Shield_Plug`, `Nanomachine_Inject`다. 폐기 지시를 받은 이전 공용 애니메이션 작업은 제거하고, 모델의 본과 Unity Animation Rigging을 사용하는 공용 모션으로 재구현했다.
- 사용자 영상 피드백에 따라 왼팔은 약 176.36° 신전을 유지하며 앞으로 올라간다. 오른손목은 기존 중립 자세를 유지하고, 아래를 향한 소품 플러그/바늘이 왼쪽 손목 뒤에서 팔꿈치 방향 약 65mm 위치의 소켓으로 접근·접촉·분리한다. 몸통의 기존 척추 세 본과 팔 IK/회전 제약을 사용한다.
- 5초 시간표는 왼팔 제시(0~0.5), 오른팔 준비(0.5~1), 상승 준비(1~1.2), 접촉 접근(1.2~1.7), 접촉 유지(1.7~3), 분리(3~3.5), 분리 유지(3.5~5), 5→0 무보간 복귀다. 사용자에게 확인받은 모션은 손가락 복구 중 변경하지 않았다.
- 공용 `player.fbx`에 양손 손가락 본 30개를 추가해 총 54본을 적용했고 수송자 렌더러 144개 연결을 갱신했다. 산탄총 손목 보정은 추가 본을 포함한 55본, 호흡 보정은 54본과 기존 BlendShape 1개를 유지했다. 기존 대표 여섯 동작의 직접 비교는 2026-09-09 20:35 기록을 따른다. 모든 기존 클립의 전수 검토를 뜻하지 않는다.
- 배터리는 `auxiliary battery.fbx`, 전환기는 `shield switch.fbx`, 완충제는 `Rapid protective barrier buffer.fbx`, 나노머신은 `Nanomachine therapeutics.fbx`의 Unity 사본을 사용한다. 각 소품을 오른손에 쥐고 플러그/바늘이 아래를 향하도록 배치했다. 사용자 지정 원본 모델은 보존했다.
- `RightHandTarget`의 `RigTransform` 누락으로 목표와 실제 손가락의 세계 회전이 달라지던 문제를 수정했다. 검토 잔여 모델·카메라로 겹쳐 보이던 문제와 보정 Mesh의 영속 데이터 재구성 문제도 조사·정정했다. 과거 잘못된 중복 객체 부재/완료 주장은 이전 진행 기록에서 정정했다.
- `AGENTS.md`에 속도 우선 지시가 없으면 검증 기준·범위·절차·판정 조건을 임의로 완화하지 않는 규칙이 추가돼 있다. 속도 우선 표현이 있어도 다른 승인·금지 규칙이 자동 해제되지는 않는다.

### 손가락 복구 후보와 직접 확인 결과

- **직접 확인:** 원본과 같은 구도의 기본 자세 비교 06~07에서 원래 칼날처럼 뾰족한 손끝을 둥근 독립 손가락 형태로 재구성했다. 초기 후보의 블록형 UV 끊김은 기존 UV 차트 안에서 연결하는 방식으로 줄였다. 새 텍스처·재질은 만들지 않았다.
- **직접 확인:** 마지막 완료된 자연 관찰 `diagnostic_25`의 손등/엄지 확대에서는 표면 갈라짐과 날카로운 접힘이 줄었지만, 일부 손가락의 소품 접촉 이격·관통이 남아 전체 파지는 미통과다. 이 결과를 손가락 작업 완료로 보고하지 않는다.
- 보조 관찰 25는 네 개체 모두 면 뒤집힘·축소·과도한 늘어남 실패가 없었으나, 손-소품 면 교차가 Battery/Converter/Buffer/Nano 순서로 **28/0/33/28개**였다. 일부 손가락 접촉 실패도 있어 전체 판정은 `FAIL`이다. 이는 아래 마지막 재파지 이전 프로필에 대한 결과다.
- 원래 두 구멍만 메우는 방법으로는 시각 문제가 해결되지 않아, 승인받은 오른손 다섯 손가락 및 손등/손바닥 연결부를 재구성했다. 65mm 원래 연결 경계와 몸체·왼손·손목·54개 본은 보호한다. Blender 후보는 13,473 정점/27,008 면이며 이 수치는 시각 통과 근거가 아니다.
- 관절 가중치의 급격한 변화와 연결 경계의 작은 기존 손가락 영향을 없앤 불연속을 보완했다. 실제 네 자연 자세를 입력으로 하는 제작 계산에서 면 보존 조건을 강화했으며 독립 관찰 판정 기준은 완화하지 않았다.
- 가중치 보정 FBX 재내보내기에서 바인드 포즈 차이가 감지돼 후보 준비가 차단됐다. 허용치를 늘리지 않고 기존 재구성 FBX를 되살린 뒤 별도 가중치 JSON만 갱신하도록 수정했다. 이후 기존 본·바인드 포즈 보존 검사를 통과했다. Unity 임포트는 `player_hands_candidate_weights.json`/`player_hands_skin_weights.json`을 권위 있는 가중치 데이터로 사용한다.

### 중단 시 마지막 저장 상태

- 중단 지시 전에 시작한 `RefitConsumableHandWeightReview`는 Unity의 동기식 계산이어서 안전한 즉시 취소 경로가 없었다. 추가 계산을 시작하지 않고 기존 호출의 종료만 확인했다. 네 후보 파지 프로필과 프리팹 저장이 완료됐으며 `candidate_grip_authoring.txt` 끝은 `authoring=COMPLETE; directReviewRequired=True`다.
- **마지막 재파지 결과는 직접 관찰하지 않았다.** 새 진단·최종 캡처를 시작하지 않았고, 이전 25번 결과를 이 마지막 프로필의 검증 결과로 재사용하지 않는다.
- `ApplySharedRightFingerRepair`와 기존 보정 전이/복구 경로를 구현했으나 **실행하지 않았다.** 임시 메시만 사용하는 사전 전이 확인에서 손목 55본과 호흡 54본/BlendShape 1개 유지가 가능했으나, 실제 전체 적용·적용 후 회귀를 완료한 것은 아니다.
- 이번 손가락 결함 복구 시작 전 백업 `finger_weights_before/` 대비 실제 공용 FBX와 씬의 바이트 동일성을 재확인했다. 마지막 파지 계산도 후보에만 저장한다. 원래 네 실제 파지 프로필·두 보정 메시·실제 공용 가중치는 이번 복구 후보로 승격하지 않았다.
- `StopConsumableRiggedSharedMotionReview`, `StopPlayerHandRigReview`, `InspectPlayerHandRigReviewState`로 종료 상태를 확인했다. Edit Mode, 일시정지/프레임 디버거/애니메이션 프리뷰 꺼짐, GPUBatched, 복구 대기 없음, 임시 검토 루트·잔여 모델·잔여 검토 카메라 0이다. **실제 키보드·마우스 조작 정상 여부는 직접 확인하지 않았다.**
- 마무리 콘솔은 오류 **0**, 경고 **58**이다. 경고 전체를 새로 조사하지 않았으며 무경고라고 보고하지 않는다. 중단 이후 모델·코드를 수정하지 않았고 새 캡처·자동 테스트·빌드도 실행하지 않았다.

### 파일과 Git 인계

- 적용 코드/에셋: `Assets/_Project/Scripts/Animation/Consumable/`, `Assets/_Project/Scripts/Animation/PlayerHands/`, `Assets/_Project/Art/Items/Consumable/`, `Assets/_Project/Art/Player/Animations/Consumable/`, `Assets/_Project/Art/Player/HandsRig/`, 관련 두 에디터 도구와 기존 브리지.
- 제작 스크립트: `scripts/player_hands_rig/`. 불변 입력·복구 원본은 로컬 `Backups/PlayerHandsRig_2026-09-09/`, `Backups/ConsumableItemGrips_2026-09-09/`다. 백업은 업로드 대상이 아니므로 재제작 시 이 로컬 의존성을 확인해야 한다.
- 관찰 근거는 `docs/validation/consumable_item_grips_2026-09-09/`와 이전 두 손 리그/배터리 검토 폴더에 보존했다. 대용량 캡처와 백업은 Git 업로드 대상에서 제외한다.
- Git 선별 중 `CargoRunMvp.unity`에 이번 공용 리그/소비품 변경과 기존 방패 파괴·방어구 모델 변경이 함께 저장된 것을 확인했다. 씬은 Git LFS 대상으로 일반적인 부분 스테이징으로 작업을 분리할 수 없고, 브리지에도 관련 에디터 명령이 함께 있다. 사용자가 기존 `Assets/_Project/` 게임 변경까지 함께 커밋·푸시하는 범위를 추가 승인했다. 공유 씬과 참조 에셋을 현재 상태 그대로 포함하며 씬이나 코드를 다시 작성하지 않는다.
- 이번 체크포인트 범위는 `Assets/_Project/`의 기존 변경, `scripts/player_hands_rig/`, 요청된 검증 임의 완화 금지 규칙이 있는 `AGENTS.md`, 9월 9일·10일 진행문서와 소비품 파지 검토 README다. 실제 적용본과 미적용 복구 후보를 함께 보존하는 중간 저장이며, 손가락 복구 완료판이 아니다. 기존 `docs/PLAYER_ANIMATION_REQUIREMENTS.html`과 `docs/PROGRESS_2026-09-06.md`의 다른 작업 수정은 이번 선별에서 제외하고 로컬에 그대로 둔다.
- 로컬 백업·대용량 진단 캡처·`artSample/shield_break_2026-09-08/`·Unity 생성물·로컬 훅 상태는 포함하지 않는다. 이 문서는 커밋 직전의 기록이며 실제 커밋 식별자와 원격 반영 여부는 Git 이력 및 작업 완료 응답으로 확인한다.
- Git 선별 확인: 276개 파일을 스테이징했고 새 에셋의 `.meta` 누락과 제외 경로 혼입은 없다. `git diff --cached --check`는 Unity 직렬화 파일의 빈 필드 뒤 공백 957건으로 실패했다. 코드·제작 스크립트·문서의 공백 오류나 다른 종류의 진단은 없었다. 두 확인의 결과가 같으며, 체크포인트 보존 작업에서 이 에셋 형식을 다시 쓰거나 점검 기준을 완화하지 않는다. 이 결과를 전체 점검 통과라고 표시하지 않는다.

## 00:28 오른손 표면 재구성 후보 직접 비교

아래는 00:28 당시 이력이며, 최신 중단 상태는 위 절을 따른다.

- 사용자가 기존 손가락 정점과 관절 주변 면의 재구성, 가중치·네 파지, 기존 두 보정 메시와 전체 수송자 연결의 일관된 반영을 승인했다. 승인 범위 안의 반복 보완에는 추가 승인을 요청하지 않는다.
- **직접 확인:** `hand_weight_review/diagnostic_06`~`diagnostic_07` 원본/후보의 같은 구도 기본 자세에서 기존 칼날 모양 손끝은 둥근 독립 손가락으로 바뀌었다. 06의 블록형 UV 끊김은 07의 기존 UV 차트 안 투영으로 줄었다. 원본 장갑 무늬와 기존 재질을 재사용하며 새 텍스처를 만들지 않았다.
- **직접 확인:** `diagnostic_22`~`diagnostic_23`의 자연 재생 손바닥·손등 확대는 관절 주변 접힘과 파지 접촉 문제로 아직 미통과다. 23에서 급격한 손등 벌어짐은 줄었지만 수치 개선을 시각 통과로 인정하지 않았다.
- 보조 관찰: 22의 뒤집힌 면은 Battery/Converter/Buffer/Nano 148/151/131/183개, 23은 204/202/166/233개. 23에서 과도한 늘어남 실패는 사라졌으나 교차·일부 손가락 접촉 실패가 남는다. 관찰 기준은 변경하지 않았다.
- `rebuild_right_finger_surface.py`는 65mm 원래 손바닥 연결 경계를 유지하고 원래 54개 본을 따라 손가락 관절 루프와 둥근 손끝을 만든다. 원래 몸체·왼손·손목은 보존한다. `finger_shape_before/`를 불변 입력으로 사용한다.
- 반복 가중치 제작에서 고정된 실패 면 3개는 65mm 연결 경계의 기존 작은 손가락 영향을 새 Hand 영향으로 바꾼 불연속이었다. 기존 경계 영향을 이어 주도록 수정하고 24번 자연 재생을 관찰 중이다. 두 계산은 미통과 상태에서 중단했으며 후보를 내보내지 않았다.
- `PlayerHandRigTools`에 형상 재구성용 보존 대응 검사를 별도로 추가했다. 기존 가중치 전용/두 구멍 보수 검사는 완화하지 않았다. 새 경로는 변경 허용 오른손 영역 밖 정점·UV·삼각형·비대상 가중치와 54개 본·바인드 포즈를 보호한다.
- Unity 반영은 후보 준비와 자연 검토만 실행했다. 전체 적용 명령 `ApplySharedRightFingerRepair`는 아직 구현/실행 전이다. 후보 통과 후 두 기존 보정 효과와 144개 연결을 보호하며 실제로 반영하고, 네 파지와 기존 여섯 동작을 직접 회귀 확인해야 한다.
- 최종 캡처·자동 테스트·빌드·Git 쓰기는 실행하지 않았다. 현재 검토 재생 중이며 종료 시 원래 검토 환경을 복구해야 한다. 실제 키보드·마우스 조작은 아직 직접 검증하지 않았다.
