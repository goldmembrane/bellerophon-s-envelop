# 진행 기록 — 2026-09-16

## 무기실 현재 Unity 편집 상태 스크립트 반영

- 대상 루트: `Approved Armory 01 Shell`
- 현재 열린 `CargoRunMvp` 씬에서 사용자 편집 상태를 다시 캡처해 `Assets/_Project/Editor/Validation/ApprovedArmoryShellCurrentState.cs`에 반영했습니다.
- 총 `151`개 Transform의 계층 sibling path, `activeSelf`, 로컬 위치, 로컬 회전, 로컬 스케일을 그대로 저장했습니다.
- 이번 사용자 편집으로 반영된 실제 차이는 다음과 같습니다.
  - 기존 잘못 배치된 `AR-05 right thumb switch top cap` 상태를 제거했습니다.
  - 현재 씬의 `AR-05 left thumb switch top cap (1)` 이름·sibling path·로컬 변환을 그대로 기록했습니다.
  - `AR-05 right vertical thumb grip`의 현재 sibling path를 그대로 기록했습니다.
- `InspectWeaponRoomSnapshotReflection`으로 현재 Unity 상태와 복원 스크립트를 다시 대조했으며, 계층·활성 상태·로컬 위치·회전·스케일이 모두 정확히 일치했습니다.
- 모델·메시·렌더러·머티리얼·텍스처는 수정하지 않았습니다.

## Turret_Enter 양손 조종대 파지 모션

- 적용 대상: `Turret_Enter`
- 시작 자세: `Player_Idle` 기본 클립의 첫 프레임을 그대로 사용했습니다.
- 동작 구성:
  - `0.7초` 동안 왼손은 왼쪽 세로 막대, 오른손은 오른쪽 세로 막대로 이동합니다.
  - 손목 방향과 손가락 리깅을 사용해 각 막대를 감싸 쥡니다.
  - 파지 자세를 `0.5초` 유지합니다.
  - 유지 종료 뒤 즉시 `Player_Idle` 시작 자세로 복귀해 전체 `1.2초` 구간을 반복합니다.
- 생성 자산:
  - `Assets/_Project/Animations/TurretEnter/Turret_Enter_TwoHandGrip.anim`
  - `Assets/_Project/Animations/TurretEnter/Turret_Enter_TwoHandGrip.controller`
- 연결 결과:
  - `Turret_Enter` Animator에 새 컨트롤러를 연결했습니다.
  - Root Motion은 사용하지 않습니다.
  - `Turret_Enter` 루트와 실제 무기실 조종대 Transform은 변경하지 않았습니다.
  - 양팔·양손·손가락 외 뼈는 `Player_Idle` 자세를 유지합니다.

## 직접 확인 및 보조 검증

- 1순위 직접 확인: `Temp/TurretEnter/Final.png`
  - 실제 수송자 메시와 실제 AR-05 조종대 메시 복제본을 사용한 한 장의 4패널 시트로 시작, 중간, 파지 정면 확대, 파지 측면 확대를 확인했습니다.
  - 양팔이 각각 같은 쪽 세로 막대로 이동하고 막대가 장갑 앞을 가리는 실제 파지 중첩이 형성됩니다.
  - 팔의 몸통 관통, 메시 찌그러짐, 좌우 손 대상 뒤바뀜은 보이지 않았습니다.
- 2순위 수치·스크립트 보조 검증:
  - 전체 길이 `1.2초`, 전환 `0.7초`, 유지 `0.5초`, Loop Time 활성화
  - 시작 자세 대 `Player_Idle` 회전 오차 `0°`
  - 시작 자세 위치 오차 최대 `0.0000005665843m`, 스케일 오차 `0`
  - 유지 구간 회전 오차 `0°`
  - 비대상 뼈 최대 회전 오차 `0°`
  - 왼손 손목 목표 오차 `0.0000155246926m`
  - 오른손 손목 목표 오차 `0.00590173062m`
  - 최소 손가락 굽힘: 왼손 `63.6920052°`, 오른손 `63.70918°`
  - 왼팔·오른팔 몸통 바깥쪽 여유 `0.425292969m` / `0.392745972m`
  - Unity 콘솔 새 오류 `0`

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureWeaponRoomCurrentState ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectWeaponRoomSnapshotReflection ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyTurretEnterAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectTurretEnterAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureTurretEnterFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureTurretEnterWristCorrectionFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureTurretEnterFingerGripCorrectionFinal ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyTurretExitAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectTurretExitAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureTurretExitFinal ...`

## Turret_Enter 양손 손목 파지 방향 교정

- 사용자 기준 이미지에서 양손이 전완 축과 어긋나 아래로 꺾인 원인을 확인했습니다.
  - 기존 손 정렬은 손가락 기준축을 수송자 기준 아래쪽으로 강제했습니다.
  - 기존 수치 검증도 같은 아래 방향을 성공 조건으로 사용해 결함을 검출하지 못했습니다.
- 팔 목표 위치, 손가락의 막대 감쌈, `0.7초` 전환, `0.5초` 유지는 그대로 보존하고 다음 항목만 교정했습니다.
  - 왼손 손등은 수송자 기준 왼쪽, 오른손 손등은 수송자 기준 오른쪽을 향합니다.
  - 양손의 기준축은 각 전완의 자연스러운 연장선으로 정렬해 아래쪽 손목 꺾임을 제거했습니다.
  - 검증 기준을 `손가락 아래 방향`에서 `전완 연속성 + 손등 바깥 방향 + 손가락 감쌈`으로 교체했습니다.
- 1순위 직접 확인: `Temp/TurretEnter/WristCorrectionFinal.png`
  - 실제 수송자 메시와 실제 조종대 메시 복제본으로 파지 완료 구간을 정면·정면 확대·좌측 확대·우측 확대에서 확인했습니다.
  - 양 전완에서 손목까지 형태가 자연스럽게 이어지고, 양손이 각 막대를 감싸며, 손목 메시 뒤틀림과 몸통·조종대 관통이 보이지 않았습니다.
- 2순위 수치·스크립트 보조 검증:
  - 손등 바깥 방향 오차: 왼손 `0°`, 오른손 `0°`
  - 전완 대비 손목 굽힘: 왼손 `19.5436573°`, 오른손 `28.3157845°`
  - 중립 손 방향 오차: 왼손 `0°`, 오른손 `0°`
  - 최소 손가락 굽힘: 왼손 `63.71096°`, 오른손 `63.70916°`
  - 시작 자세 대 `Player_Idle` 회전 오차 `0°`, 유지 구간 회전 오차 `0°`, 비대상 뼈 회전 오차 `0°`
  - 무기실 스냅샷 `151`개 Transform의 계층·활성 상태·로컬 위치·회전·스케일 일치
  - Unity 콘솔 새 오류 `0`

## Turret_Enter 양손 손가락 정면 감쌈 파지 교정

- 사용자 기준 이미지에서 양손 손가락이 조종간 아래쪽으로 꺾여 보이는 원인을 확인했습니다.
  - 기존 손가락 리깅 계산은 세로 조종간 축을 기준으로 첫 마디를 아래쪽, 끝 마디를 위쪽으로 향하게 해 손가락을 세로 평면에서 굽혔습니다.
- 손목·전완·상완·몸통·하체·조종간 위치와 기존 타이밍을 유지한 채 손가락 굽힘 평면만 수정했습니다.
  - 각 손가락은 조종간 뒤쪽에서 수송자 정면 방향으로 나와 막대 앞면을 감싼 뒤 다시 뒤쪽으로 말립니다.
  - 엄지는 다른 손가락과 반대쪽에서 감싸는 파지를 유지합니다.
- 1순위 직접 확인: `Temp/TurretEnter/FingerGripCorrectionFinal.png`
  - 실제 수송자 메시와 실제 조종간 메시 복제본을 사용해 전체, 측면, 왼손 확대, 오른손 확대를 확인했습니다.
  - 양손 손가락의 아래쪽 꺾임이 제거되고 조종간 정면을 감싸는 방향으로 이어집니다.
  - 손가락·손바닥의 조종간 관통, 손목·장갑 메시 뒤틀림, 기존 양팔 자세 변화는 보이지 않았습니다.
- 2순위 수치·스크립트 보조 검증:
  - 최대 손가락 수직 처짐: 왼손 `0.00356292725°`, 오른손 `0.004272461°`
  - 최소 조종간 앞뒤 감쌈 각도: 왼손 `64.2453°`, 오른손 `67.09014°`
  - 손등 바깥 방향 오차: 왼손 `0°`, 오른손 `0°`
  - 시작 자세 대 `Player_Idle` 회전 오차 `0°`, 유지 구간 회전 오차 `0°`, 비대상 뼈 회전 오차 `0°`
  - 무기실 스냅샷 `151`개 Transform의 계층·활성 상태·로컬 위치·회전·스케일 일치
  - Unity 콘솔 새 오류 `0`

## Turret_Exit 원본 역재생 모션

- 적용 대상: `Turret_Exit`
- 원본: `Assets/_Project/Animations/TurretEnter/Turret_Enter_TwoHandGrip.anim`
- 원본의 모든 float 커브를 60fps 프레임 단위로 직접 읽어 다음 순서로 복사했습니다.
  - `0~0.7초`: `Turret_Enter`의 `0.7~0초` 구간을 정확히 역순 재생
  - 원본 파지 자세의 0.5초 유지 구간은 제외
  - `0.7~1.2초`: 원본 0초이자 `Player_Idle`인 자세를 0.5초 유지
  - 유지 종료 뒤 즉시 처음 파지 자세로 복귀해 전체 반복
- 생성 자산:
  - `Assets/_Project/Animations/TurretExit/Turret_Exit_Reverse.anim`
  - `Assets/_Project/Animations/TurretExit/Turret_Exit_Reverse.controller`
- `Turret_Exit` Animator에 새 컨트롤러를 연결했으며 Root Motion은 사용하지 않습니다.
- `Turret_Enter` 원본 클립·컨트롤러·루트와 실제 조종간 Transform은 수정하지 않았습니다.
- 1순위 직접 확인: `Temp/TurretExit/Final.png`
  - 파지 시작, 역재생 중간, `Player_Idle` 도달, `Player_Idle` 유지의 4패널을 확인했습니다.
  - 시작 파지는 `Turret_Enter`의 마지막 자세와 같고, 양손·손가락이 조종간에서 자연스럽게 빠져나오며 메시 튐·뒤틀림·관통이 보이지 않았습니다.
  - `0.7초`와 `1.2초` 패널이 같은 `Player_Idle` 자세를 유지합니다.
- 2순위 수치·스크립트 보조 검증:
  - 43개 역재생 전환 프레임의 회전·위치·스케일 원본 대조 오차 `0`
  - 시작 자세 대 `Turret_Enter` 파지 완료 자세의 회전·위치·스케일 오차 `0`
  - `Player_Idle` 회전 오차 `0°`, 위치 오차 최대 `0.0000005665843m`, 스케일 오차 `0`
  - `Player_Idle` 0.5초 유지 구간의 회전·위치·스케일 오차 `0`
  - 첫 프레임 커브 변화량 `0.00011292845`로 초기 파지 유지 구간 없음
  - 무기실 스냅샷 `151`개 Transform의 계층·활성 상태·로컬 위치·회전·스케일 일치
  - Unity 콘솔 새 오류 `0`

## Turret 진행표 완료 및 ShipRepair 시작 시점 이동

- `docs/PLAYER_ANIMATION_REQUIREMENTS.html`에서 다음 두 항목만 완료 상태로 변경했습니다.
  - `PLR-TUR-001` / `Turret_Enter`
  - `PLR-TUR-010` / `Turret_Exit`
- Unity 실행 직후 시작 시점을 `ShipRepair` 개체 정면을 보는 위치로 옮겼습니다.
  - 이동 대상은 `Player` 루트뿐이며 `ShipRepair` Transform과 플레이어 자식·카메라 설정은 변경하지 않았습니다.
  - 카메라와 대상 사이 수평 거리 `4.5m`
  - 대상 정면축 일치도 `1.000000`, 카메라 시선 일치도 `0.986779`
  - 화면 가장자리 최소 여백 `0.071590`
- 1순위 직접 확인: `Temp/ShipRepairStart/Final.png`
  - 자연 실행 모드에서 `ShipRepair` 수송자가 화면 중앙에 정면 전신으로 보이고 다른 개체에 가려지지 않는 것을 확인했습니다.
  - 대상 위치·회전·스케일과 기존 주변 개체 상태는 그대로 유지됐습니다.
- 2순위 수치·스크립트 보조 검증:
  - 자연 실행 모드 진입 `True`
  - 대상 Transform 변경 `False`
  - 플레이어 자식 또는 카메라 구성 변경 `False`
  - 대상 전체 바운드 화면 내 표시 `True`
  - 대상 정면 확인 `True`
  - Unity 콘솔 새 오류 `0`
- Unity 반영 명령:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyShipRepairReviewStart`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectShipRepairReviewStart`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureShipRepairReviewStart`

## ShipRepair·SabotageRepair 공용 수리 모션

- 적용 대상: `ShipRepair`, `SabotageRepair`
- 공급 원본: `player model/transfer repairing.fbx`
  - SHA-256: `FBDF21C7ED8581D76C3710FACCD5375939B02D1EFA1E5AB6BFFC94FFB70F72D0`
  - Unity 사본: `Assets/_Project/Animations/RepairingShared/Source/TransferRepairing.fbx`
  - 공급 원본과 Unity 사본의 바이너리 해시가 일치합니다.
- 공용 생성 자산:
  - `Assets/_Project/Animations/RepairingShared/RepairingShared_UpperSource_LowerPlayerIdle.anim`
  - `Assets/_Project/Animations/RepairingShared/RepairingShared.controller`
- 원본 수리 클립의 `Spine02` 이하 상체·양팔·양손 커브와 재생시간을 그대로 복사했습니다.
  - 원본 및 공용 클립 길이 `16.466670초`
  - 원본 및 공용 클립 프레임레이트 `30fps`
- `Hips`, 왼쪽 `UpLeg` 하위 체인, 오른쪽 `UpLeg` 하위 체인은 `Player_Idle` 첫 프레임 자세를 전체 구간에 상수값으로 적용했습니다.
  - 최대 하체 위치 오차 `0m`
  - 최대 하체 회전 오차 `0°`
  - 최대 하체 스케일 오차 `0`
- 두 개체 Animator에 같은 공용 컨트롤러를 연결했습니다.
  - Loop Time 활성화
  - Root Motion 비활성화
  - 두 대상 루트 Transform·렌더러·Avatar 변경 없음
  - 다른 씬 Transform 변경 없음

## 직접 확인 및 보조 검증

- 1순위 직접 확인: `docs/validation/ShipRepairSharedAnimation/Final.png`
  - 자연 실행 모드에서 한 주기 이상 재생한 뒤 정규화 시점 `0.10`, `0.35`, `0.60`, `0.85`를 한 장에 확인했습니다.
  - 위 행 `ShipRepair`와 아래 행 `SabotageRepair`의 같은 시점 포즈가 일치합니다.
  - 상체·팔·손은 원본 수리 동작에 따라 움직이고, 골반·양다리는 네 시점 모두 같은 `Player_Idle` 첫 프레임 자세를 유지합니다.
  - 보이는 메시의 뒤틀림·관통·소실은 없습니다.
- 2순위 수치·스크립트 보조 검증:
  - 두 Animator 정규화 시간 최대 차이 `0`
  - 두 개체 모두 한 주기 이상 자연 반복재생 `True`
  - 상체 원본 커브 키·탄젠트·가중치 일치 `True`
  - 하체 전 구간 오차 `0`
  - 실행 모드 중 대상 루트 Transform 변경 `False`
  - Unity 콘솔 새 오류 `0`

## Unity 반영 명령

- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectShipRepairSharedAnimationSources ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyShipRepairSharedAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectShipRepairSharedAnimation ...`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureShipRepairSharedAnimationFinal ...`

## 이번 작업에서 실행하지 않은 항목

- `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
- `./scripts/Run-EditModeTests.ps1`
- `./scripts/Run-PlayModeTests.ps1`
- `./scripts/Build-WindowsDev.ps1`
- Unity 에디터 재시작
- 모델·메시·렌더러·머티리얼·텍스처 수정
- `ShipRepair`, `SabotageRepair` 외 다른 애니메이션 개체와 진행 HTML 행 수정
- Git 커밋 및 푸시

## 남은 확인 사항

- 현재 승인 범위 안에서 남은 작업 없음

## ShipRepair·SabotageRepair 공용 수리 모션 교정 (기존 적용 결과 대체)

- 사용자 제공 영상 `2026-09-16 21-03-28.mp4`를 직접 확인해 기존 결과의 양팔 꼬임과 타이핑 동작 소실을 재현했습니다.
- 원인:
  - 기존 구현은 서로 다른 FBX 골격의 Generic 로컬 Transform 회전값을 같은 경로에 절대값으로 복사했습니다.
  - 뼈 이름과 경로는 같아도 바인드 축이 달라 원본 회전이 플레이어 골격에서 다른 자세로 해석됐습니다.
  - 기존 수치 검사는 잘못 복사된 커브의 값 일치만 검사했기 때문에 실제 자세 오류를 검출하지 못했습니다.
- 교정:
  - 공급 원본 `player model/transfer repairing.fbx`와 플레이어 바이너리 사본을 각각 Humanoid로 임포트했습니다.
  - 공급 원본 타이핑 동작을 플레이어 Humanoid 프록시에 리타게팅한 뒤 상체 회전만 Generic 대상용 클립으로 베이크했습니다.
  - 잘못된 기존 자산 `RepairingShared_UpperSource_LowerPlayerIdle.anim`은 제거했습니다.
  - 생성 자산:
    - `Assets/_Project/Animations/RepairingShared/RepairingShared_HumanoidRetargetedUpper.anim`
    - `Assets/_Project/Animations/RepairingShared/RepairingShared_UpperBody.mask`
    - `Assets/_Project/Animations/RepairingShared/RepairingShared.controller`
    - `Assets/_Project/Animations/RepairingShared/Source/PlayerHumanoidProxy.fbx`
  - 컨트롤러의 기본 레이어는 `Player_Idle` 첫 프레임을 속도 `0`으로 유지하고, 상체 전용 Override 레이어가 리타게팅된 타이핑 동작만 재생합니다.
  - `ShipRepair`, `SabotageRepair`의 기존 Generic Avatar, 루트 Transform, 렌더러, Root Motion 설정과 다른 씬 Transform은 변경하지 않았습니다.
- 1순위 직접 확인: `docs/validation/ShipRepairSharedAnimationCorrection/Final.png`
  - 자연 실행 모드에서 두 개체가 한 주기 이상 재생한 뒤 정규화 시점 `0.10`, `0.35`, `0.60`, `0.85`를 확인했습니다.
  - 두 개체 모두 양팔과 손이 몸통 앞의 작업 자세로 정상 연결되고, 기존 영상의 머리 위로 들린 팔과 아래로 뒤틀린 반대 팔이 사라졌습니다.
  - 네 시점에서 양손 위치가 달라져 타이핑 동작이 보이며, 하체는 같은 `Player_Idle` 자세를 유지합니다.
- 2순위 수치·스크립트 보조 검증:
  - 원본 및 리타게팅 클립 길이 `16.466670초`, 프레임레이트 `30fps`
  - 왼손 이동 범위 `0.111195m`, 오른손 이동 범위 `0.301730m`
  - 두 Animator 정규화 시간 최대 차이 `0`
  - 두 개체 모두 한 주기 이상 자연 반복재생 `True`
  - 실행 모드 중 대상 루트 Transform 변경 `False`
  - Unity 콘솔 새 오류 `0`
- Unity 반영 명령:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectShipRepairSharedAnimationCorrectionSources ...`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyShipRepairSharedAnimationCorrection ...`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectShipRepairSharedAnimationCorrection ...`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureShipRepairSharedAnimationCorrectionFinal ...`
- 실행하지 않은 검증:
  - `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
  - `./scripts/Run-EditModeTests.ps1`
  - `./scripts/Run-PlayModeTests.ps1`
  - `./scripts/Build-WindowsDev.ps1`

## ShipRepair 진행 바 · 용접 이펙트 아트 샘플

- 샘플 경로: `artSample/ShipRepairProgressAndWelding/`
- `ShipRepair`, `SabotageRepair`의 현재 Unity 검증 이미지를 기준으로 두 개체 머리 위에 각각 50% 진행 바를 배치했습니다.
  - 내부 트랙 206px 중 103px를 채워 정확히 50%로 표현했습니다.
  - 어두운 반투명 트랙, 청록색 채움, 중앙 눈금을 사용했습니다.
- `ShipRepair` 앞 양손 사이 작업점에 용접 이펙트 샘플을 배치했습니다.
  - 청백색 아크 중심, 얇은 청색 외곽광, 아래쪽으로 떨어지는 주황색 스파크와 짧고 옅은 연기로 구성했습니다.
  - 내장 이미지 생성 도구로 투명 VFX 레이어만 생성하고 현재 Unity 캡처에 합성해 수송자 모델 픽셀과 자세를 유지했습니다.
- 직접 확인 자료:
  - `artSample/ShipRepairProgressAndWelding/ProgressBars_50Percent_ArtSample.png`
  - `artSample/ShipRepairProgressAndWelding/ShipRepair_WeldingVFX_Comparison.png`
  - `artSample/ShipRepairProgressAndWelding/ShipRepair_WeldingVFX_Detail.png`
  - `artSample/ShipRepairProgressAndWelding/index.html`
- 상태: 사용자 승인 완료, 아래 Unity 적용 결과로 대체

## ShipRepair·SabotageRepair 진행 바 · ShipRepair 용접 이펙트 Unity 적용

- 대상 씬: `Assets/_Project/Scenes/CargoRunMvp.unity`
- `ShipRepair`, `SabotageRepair` 각각의 머리 위에 승인 샘플 기반 월드 공간 진행 바를 추가했습니다.
  - 수리 시간은 원본 기획서의 `5초`를 사용합니다.
  - 실행 모드에서 `0 → 100%` 진행 후 즉시 처음부터 반복합니다.
  - 두 개체의 진행값은 같은 실행 시점에서 일치합니다.
  - 편집 모드 미리보기는 승인 샘플과 같은 `50%`입니다.
- `ShipRepair`에만 승인 용접 이펙트를 추가했습니다.
  - `ShipRepair` 루트 정면 `0.5m` 평면을 유지하면서 양손 사이 높이를 추종합니다.
  - 승인 투명 소스를 변형 없이 복사했습니다.
  - 원본 및 Unity 사본 SHA-256:
    `486CA659F0F695BB223E634B2687763A0A8DC1BF9165632CA619C14B4BB2FF9C`
- 추가 자산과 코드:
  - `Assets/_Project/Runtime/Repair/RepairProgressBarPresenter.cs`
  - `Assets/_Project/Runtime/Repair/RepairWeldingVfxPresenter.cs`
  - `Assets/_Project/Art/VFX/RepairWelding/WeldingVFX_Approved.png`
  - `Assets/_Project/Editor/ShipRepairProgressAndWeldingSetupTools.cs`
- 기존 `ShipRepair`, `SabotageRepair` 루트 Transform, Animator Controller, 모델 렌더러와 머티리얼은 변경하지 않았습니다.
- 1순위 직접 확인:
  - `docs/validation/ShipRepairProgressAndWelding/Final.png`
  - 승인 샘플과 나란히 비교해 진행 바의 왼쪽→오른쪽 채움, 캐릭터 대비 폭, 머리 위 간격을 확인했습니다.
  - 승인 용접 이미지의 청백색 중심, 청색 외곽광, 주황 하강 불꽃이 `ShipRepair`의 양손 앞에 표시됩니다.
- 2순위 수치·스크립트 보조 검증:
  - 자연 실행 모드 전체 주기 및 재시작 확인 `True`
  - 최종 캡처 진행률: 두 대상 모두 `0.496003`
  - 두 진행 바 최대 진행률 차이 `0`
  - 용접 이펙트 정면 거리 `0.5m`
  - Unity 콘솔 새 오류 `0`
  - 저장 씬 SHA-256:
    `A08E5B524E31AFBD389D7195C86D9B52AD7FF835EC1AFB175FEC3EBA569F22E9`
- Unity 반영 명령:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Refresh-UnityProject.ps1`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectShipRepairProgressAndWeldingSources ...`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command ApplyShipRepairProgressAndWelding ...`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command InspectShipRepairProgressAndWelding ...`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Invoke-UnityEditorBridge.ps1 -Command CaptureShipRepairProgressAndWeldingFinal ...`
- 실행하지 않은 항목:
  - `./scripts/Run-HarnessValidation.ps1` 및 Harness 검증 관련 작업
  - Unity 에디터 재시작
  - 모델·애니메이션·기존 렌더러·기존 머티리얼 수정
  - 진행 HTML 완료 처리
  - Git 커밋 및 푸시

## ShipRepair 용접 이펙트 월드 위치 고정 교정

- 기존 `RepairWeldingVfxPresenter`가 매 프레임 양손 중간점의 X/Y를 다시 계산해 용접 이펙트가 손 애니메이션을 따라가던 원인을 제거했습니다.
- 기존 씬에 배치된 첫 용접점의 월드 좌표를 그대로 보존했습니다.
  - 고정 월드 좌표: `(71.30782, 0.837461, -318.8)`
  - `ShipRepair` 정면 거리: `0.5m`
- 실행 중에는 위치를 다시 손에서 계산하지 않고 저장된 월드 좌표를 유지하며, 카메라를 향한 표시 회전과 승인 크기만 갱신합니다.
- 승인 용접 이미지 자체는 변경하지 않았습니다.
  - 승인 소스와 Unity 사본 SHA-256 일치:
    `486CA659F0F695BB223E634B2687763A0A8DC1BF9165632CA619C14B4BB2FF9C`
- 1순위 직접 확인:
  - `docs/validation/ShipRepairProgressAndWelding/Final.png`
  - 승인 샘플과 비교해 청백색 아크 중심, 청색 외곽광, 주황색 하강 스파크가 기존 첫 용접점에 표시되는 것을 확인했습니다.
  - `ShipRepair`에만 용접 이펙트가 있고 `SabotageRepair`에는 없는 상태를 확인했습니다.
- 2순위 실행 모드 보조 검증:
  - 자연 실행 모드에서 5초 진행 한 주기와 재시작 확인 `True`
  - 전체 관찰 구간 용접 이펙트 월드 위치 최대 이동량 `0m`
  - 두 진행 바 최대 진행률 차이 `0`
  - Unity 콘솔 새 오류 `0`
  - 저장 씬 SHA-256:
    `F2C681BAED9EF65485EC298447ED82250A72206384F63D24DDEAD5D13A60E0C3`
- 변경하지 않은 범위:
  - `ShipRepair`, `SabotageRepair`의 기존 애니메이션과 모델
  - 기존 렌더러·머티리얼·텍스처
  - 진행 바의 5초 반복 동작과 승인된 시각 디자인
  - Git 이력

## ShipRepair 용접 샘플 반복 VFX 구현 (폐기된 이전 방식)

- 사용자 지시에 따라 별도 아트 샘플을 추가하지 않고, 기존 승인 용접 샘플을 실제 반복 VFX로 구현했습니다.
- 기존 승인 PNG는 변형하지 않은 기본 레이어로 유지했습니다.
  - 승인 소스와 Unity 사본 SHA-256:
    `486CA659F0F695BB223E634B2687763A0A8DC1BF9165632CA619C14B4BB2FF9C`
- 추가된 반복 VFX 레이어:
  - `WeldingArcBursts`: 청백색 아크 중심의 짧고 빠른 점멸
  - `WeldingSparks`: 중력과 하향 속도를 적용한 주황색 스파크 및 짧은 잔광
  - `WeldingSmoke`: 위로 퍼지며 투명해지는 옅은 청회색 연기
- 세 레이어는 월드 공간에서 반복 재생되며, 방출점은 기존 고정 용접점에서만 계산합니다.
  - 애니메이션 반복 기준 시간 `0.72초`
  - 고정 월드 좌표 `(71.30782, 0.837461, -318.8)`
  - `ShipRepair` 정면 거리 `0.5m`
- 생성 자산:
  - `Assets/_Project/Art/VFX/RepairWelding/WeldingParticleStar.png`
  - `Assets/_Project/Art/VFX/RepairWelding/WeldingParticleSoft.png`
  - `Assets/_Project/Art/VFX/RepairWelding/WeldingAdditive.mat`
  - `Assets/_Project/Art/VFX/RepairWelding/WeldingSmoke.mat`
- 1순위 직접 확인:
  - `docs/validation/ShipRepairProgressAndWelding/Final.png`
  - 자연 실행 중 서로 다른 세 시점에서 아크 밝기와 스파크 위치·궤적이 달라지는 것을 한 장에서 확인했습니다.
  - 승인 샘플의 청백색 중심, 청색 외곽광, 주황색 낙하 스파크 형태가 유지되며 `SabotageRepair`에는 용접 VFX가 없습니다.
- 2순위 실행 모드 보조 검증:
  - 기본 승인 레이어 알파 범위 `0.620111~0.996831`
  - 최대 활성 파티클: 스파크 `19`, 아크 `5`, 연기 `9`
  - 자연 실행 모드에서 5초 진행 한 주기와 재시작 확인 `True`
  - 전체 관찰 구간 용접 VFX 월드 위치 최대 이동량 `0m`
  - 두 진행 바 최대 진행률 차이 `0`
  - Unity 콘솔 새 오류 `0`
  - 저장 씬 SHA-256:
    `0F06833BCA3A61FD253C111556EBA9EC0F5EFE1FB33C5091739FE3C582DAA502`
- 변경하지 않은 범위:
  - `ShipRepair`, `SabotageRepair`의 기존 애니메이션·모델·기존 렌더러·기존 머티리얼
  - 진행 바의 5초 반복 동작
  - 용접 이펙트의 고정 좌표
  - Git 이력

> 이 절의 절차적 파티클 방식과 생성 자산은 승인 샘플을 임의 해석한 잘못된 구현이어서 폐기했습니다. 현재 적용 상태는 아래 교정 기록을 기준으로 합니다.

## ShipRepair 승인 용접 샘플 기반 VFX 교정

- 승인된 `WeldingVFX_Approved.png` 자체의 픽셀만 사용하도록 용접 VFX를 다시 구현했습니다.
  - 청백색 아크 영역: 승인 이미지의 해당 픽셀을 점멸시킵니다.
  - 주황색 스파크 영역: 승인 이미지의 해당 픽셀을 아래로 이동시키며 감쇠합니다.
  - 상단 연기 영역: 승인 이미지의 해당 픽셀을 위로 이동시키며 감쇠합니다.
  - 애니메이션 반복 기준 시간은 `0.72초`입니다.
- 이전에 임의 생성했던 파티클 텍스처·머티리얼과 파티클 시스템을 모두 제거했습니다.
  - 제거: `WeldingParticleStar.png`, `WeldingParticleSoft.png`, `WeldingAdditive.mat`, `WeldingSmoke.mat`
  - 편집기 설정 도구에 남아 있던 비활성 절차적 파티클 생성 코드도 제거했습니다.
  - 현재 파티클 시스템 수: `0`
- 현재 VFX는 승인 PNG를 공유하는 스프라이트 레이어 6개로 구성됩니다.
  - 기본 잔상 1개, 아크 1개, 스파크 2개, 연기 2개
  - 사용 셰이더: `Assets/_Project/Art/VFX/RepairWelding/WeldingApprovedSampleAnimated.shader`
  - 사용 머티리얼: `Assets/_Project/Art/VFX/RepairWelding/WeldingApprovedSampleAnimated.mat`
- 승인 이미지 원본은 변경하지 않았습니다.
  - SHA-256: `486CA659F0F695BB223E634B2687763A0A8DC1BF9165632CA619C14B4BB2FF9C`
- 기존 용접점과 진행 바 동작을 유지했습니다.
  - 고정 월드 좌표: `(71.30782, 0.837461, -318.8)`
  - `ShipRepair` 정면 거리: `0.5m`
  - 실행 중 최대 위치 이동량: `0m`
  - 진행 바 시간: `5초`, 두 진행 바 최대 진행률 차이: `0`
- 1순위 직접 확인:
  - `docs/validation/ShipRepairProgressAndWelding/Final.png`
  - 승인 샘플과 동일한 청백색 아크 중심, 청색 외곽광, 주황색 하강 불꽃과 상단 연기 구성이 `ShipRepair` 앞 고정 위치에서 유지되는 것을 확인했습니다.
  - 서로 다른 세 실행 시점에서 아크 밝기, 불꽃 하강 위치, 연기 상승 위치가 달라지는 것을 확인했습니다.
  - `SabotageRepair`에는 용접 VFX가 없고 진행 바만 표시되는 것을 확인했습니다.
- 2순위 수치·스크립트 보조 검증:
  - 승인 PNG 외 시각 소스 사용: `False`
  - 생성 파티클 자산 사용: `False`
  - 승인 샘플 스프라이트 레이어 수: `6`
  - 아크 밝기 범위: `0.580584~0.999929`
  - 스파크 최대 하강 진행률: `0.158564`
  - 연기 최대 상승 진행률: `0.084237`
  - 자연 실행 모드 전체 주기 및 재시작 확인: `True`
  - Unity 콘솔 새 오류: `0`
  - 저장 씬 SHA-256: `D8E4D423B24E661435507E36B86E9763FE3CB10D0605E6C0A3B16E51475837EC`
- 변경하지 않은 범위:
  - `ShipRepair`, `SabotageRepair`의 기존 애니메이션·모델·기존 렌더러·기존 머티리얼
  - 진행 바의 승인된 디자인과 5초 반복 동작
  - 용접 이펙트의 고정 좌표
  - Git 이력

## ShipRepair 용접 VFX 정적 PNG 표시 제거

- 사용자 확인에 따라 용접점에 승인 PNG 전체와 동적 VFX가 함께 표시되던 구조를 수정했습니다.
- `ApprovedRepairWeldingVfx` 루트의 정적 `SpriteRenderer`와 `baseLayer` 직렬화 참조를 제거했습니다.
- 셰이더의 정적 전체 PNG 출력 모드를 제거했습니다.
  - 승인 PNG 파일은 삭제하거나 변형하지 않고, 동적 VFX 픽셀 추출용 소스 텍스처로만 사용합니다.
  - 화면에는 아크 1개, 스파크 2개, 연기 2개의 동적 VFX 레이어만 표시됩니다.
- 기존 동작은 유지했습니다.
  - VFX 반복 시간: `0.72초`
  - 고정 월드 좌표: `(71.30782, 0.837461, -318.8)`
  - `ShipRepair` 정면 거리: `0.5m`
  - 진행 바 시간: `5초`
- 1순위 직접 확인:
  - `docs/validation/ShipRepairProgressAndWelding/Final.png`
  - 자연 실행 모드의 서로 다른 세 시점에서 정적 PNG 전체 형상이 남지 않고 용접 아크·스파크·연기 VFX만 변하는 것을 확인했습니다.
  - `SabotageRepair`에는 용접 VFX가 없고 진행 바만 유지되는 것을 확인했습니다.
- 2순위 수치·스크립트 보조 검증:
  - 승인 샘플 동적 VFX 레이어 수: `5`
  - 루트 정적 `SpriteRenderer`: `0`
  - 파티클 시스템: `0`
  - 아크 밝기 범위: `0.580369~0.999846`
  - 스파크 최대 하강 진행률: `0.159908`
  - 연기 최대 상승 진행률: `0.084951`
  - 실행 중 최대 위치 이동량: `0m`
  - 두 진행 바 최대 진행률 차이: `0`
  - Unity 콘솔 새 오류: `0`
  - 승인 PNG SHA-256: `486CA659F0F695BB223E634B2687763A0A8DC1BF9165632CA619C14B4BB2FF9C`
  - 저장 씬 SHA-256: `83B2B8795105A53C5191E8BEF3005A469548D75AFE8C855B09AC5664AAD877FB`
- 변경하지 않은 범위:
  - `ShipRepair`, `SabotageRepair`의 모델과 기존 수리 동작
  - 진행 바 디자인과 재생시간
  - 용접 이펙트의 고정 좌표
  - Git 이력

## 현재 누적 작업 Git 인계 요약

- 이번 인계에는 마지막 원격 커밋 이후 현재 작업 트리에 누적된 다음 작업을 함께 반영합니다.
  - `Detector_Attached_Static`: 장착 상태와 이동 반복 구성을 반영하고 진행표에서 완료 처리했습니다.
  - `ElectricMine_Idle`, `ElectricMine_Activate`, `ElectricMine_Armed_Idle`: 휴대·활성화·무장 상태 애니메이션, 이동 반복, 승인된 중앙 버튼 점멸 VFX를 반영하고 진행표에서 완료 처리했습니다.
  - `Helm_Enter`, `Helm_Exit`: 조타륜 양손 파지·해제 애니메이션과 손목·손가락 방향 교정을 반영하고 진행표에서 완료 처리했습니다.
  - `Turret_Enter`, `Turret_Exit`: 무기실 현재 상태 스냅샷, 양손 조종대 파지·해제 애니메이션, 손목·손가락 파지 교정을 반영하고 진행표에서 완료 처리했습니다.
  - `ShipRepair`, `SabotageRepair`: 공용 수리 애니메이션의 Humanoid 재타기팅과 하체 `Player_Idle` 고정, 5초 반복 진행 바를 반영했습니다.
  - `ShipRepair`: 승인된 용접 샘플 픽셀만 사용하는 0.72초 반복 VFX를 고정 월드 좌표에 적용하고, 정적 PNG 전체 표시와 폐기된 절차적 파티클 자산을 제거했습니다.
- 현재 씬 보호와 구형 작업 접근 방지 변경도 포함합니다.
  - 오래된 `Assets/_Recovery` 및 `Backups` 씬 사본을 제거했습니다.
  - 폐기된 구형 씬 검증 명령을 제거했습니다.
  - `scripts/Open-UnityProject.ps1`가 현재 `CargoRunMvp` 씬의 경로·크기·SHA-256·필수 마커를 확인한 뒤에만 에디터 진입을 허용하도록 갱신했습니다.
- 승인 요청 누락 방지를 위해 `AGENTS.md`에 여섯 단계 사전 점검 게이트와 현재 로컬 훅 활성 범위를 명시했습니다.
- 진행 상태 문서 반영:
  - `docs/PLAYER_ANIMATION_REQUIREMENTS.html`에서 `Detector_Attached_Static`, `ElectricMine_Idle`, `ElectricMine_Activate`, `ElectricMine_Armed_Idle`, `Helm_Enter`, `Helm_Exit`, `Turret_Enter`, `Turret_Exit`을 완료 처리했습니다.
- 이번 Git 인계 점검:
  - 원격 `origin/main`과 작업 시작 시점의 로컬 `main`이 동일한 커밋임을 확인했습니다.
  - 기존 추적 파일의 작업 트리 차이에 대한 `git diff --check`에서는 줄바꿈 변환 경고만 확인했습니다.
  - 신규 Unity 직렬화 파일까지 포함한 `git diff --cached --check`에서는 Unity가 빈 `userData`·`assetBundle` YAML 값 뒤에 기록한 후행 공백을 확인했습니다. 기능·자산 내용 오류가 아니며 Unity 생성 파일은 임의 재작성하지 않았습니다.
  - Unity 생성물과 세션 상태 경로인 `Library`, `Temp`, `Logs`, `UserSettings`, `Builds`, `TestResults`, `.codex/state`는 커밋 대상에서 제외합니다.
  - 사용자가 직접 명령명과 대상 범위를 지정하지 않은 Harness, EditMode, PlayMode, Windows Build 검증은 실행하지 않았습니다.
