# 진행 기록 2026-06-23

## SR-07 사출대 단말기 HSK 화면 Unity 적용 시작

- 사용자가 승인한 SR-07 HSK 화면 샘플을 Unity 비품실에 적용하라고 요청했다.
- 적용 범위:
  - `SR-07 visible ejection terminal inactive screen` 앞면에 `HSK_Open_Close.png` 텍스처 면만 추가한다.
  - SR-07 단말기 화면 기준으로 승인 샘플의 화면 크기와 UV 크롭을 반영한다.
  - 적용 후 `ApprovedSupplyRoomShellCurrentState.cs`를 현재 비품실 상태 복구 기준으로 저장한다.
- 수정 대상:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Editor/Validation/ApprovedSupplyRoomShellCurrentState.cs`
  - `docs/PROGRESS_2026-06-23.md`
  - `docs/SUPPLY_ROOM_OBJECTS.md`
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "AddApprovedSupplyRoomSr07HskScreenOnly" -LogPath "Logs\AddApprovedSupplyRoomSr07HskScreenOnly.log" -TimeoutSeconds 360`
- 실행하지 않을 작업:
  - 비품실 다른 오브젝트 수정
  - 무기실, 동력실, 운송창고 등 다른 구역 수정
  - 샘플 재제작
  - Validate/Test/Build
  - 전체 비품실 재생성 또는 복원

## SR-07 사출대 단말기 HSK 화면 크기 조정 시작

- 사용자가 사출대 단말기 화면에 `HSK_Open_Close.png`가 꽉 차도록 조정하라고 요청했다.
- 수정 범위:
  - `scripts/GenerateSupplyRoomShellSample.py`
  - `docs/PROGRESS_2026-06-23.md`
  - `artSample/supply_room_shell/` 샘플 산출물
- 수정 내용:
  - SR-07 단말기 화면 앞면의 HSK 텍스처 평면을 디스플레이 크기에 맞추고 UV 여백을 조정한다.
  - 사출대 단말기 프레임, 버튼, 위치, 비품실 다른 오브젝트는 수정하지 않는다.
- 실행 예정 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 실행하지 않을 작업:
  - Unity 에디터 리프레시/브리지 실행
  - `Assets/` 수정
  - 실제 Unity 씬 배치
  - 사출대 개폐, 쿨타임, 사출 판정, 상호작용 로직 구현
  - Validate/Test/Build

## SR-07 사출대 단말기 HSK 화면 크기 조정 완료

- 실행 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
  - 첫 실행은 124초 제한으로 중단되어 샘플 산출물이 중간 상태가 됐다.
  - 파일 잠금이 풀린 뒤 같은 명령을 재실행했고, 좌측 아이콘 크롭 보정 후 한 번 더 재실행했다.
- 결과:
  - 마지막 샘플 생성 명령은 종료 코드 `0`으로 완료됐다.
  - SR-07 HSK 텍스처 평면을 단말기 화면 영역 크기에 맞추고, UV를 `uv_min=(0.0, 0.075)`, `uv_max=(0.86, 0.925)`로 조정했다.
  - `artSample/supply_room_shell/renders/10_ejection_terminal_hsk_screen.png`에서 HSK 화면이 단말기 디스플레이 판을 채우고 `OPEN/CLOSE [E]`가 정상 방향으로 보이는 것을 확인했다.
- 실행하지 않은 작업:
  - Unity 에디터 리프레시/브리지 실행
  - `Assets/` 수정
  - 실제 Unity 씬 배치
  - 사출대 개폐, 쿨타임, 사출 판정, 상호작용 로직 구현
  - Validate/Test/Build

## SR-07 사출대 단말기 HSK Open/Close 화면 샘플 제작 시작

- 사용자가 사출대 단말기 화면에 `HSK_Open_Close.png`를 넣은 샘플 제작을 요청했다.
- 텍스처 원본 위치:
  - `Assets/Heavy Station Kit/_common/Textures/GUI/HSK_Open_Close.png`
- 샘플 반영 범위:
  - `artSample/supply_room_shell/`의 비품실 샘플에서 SR-07 사출대 단말기 화면 앞면에 HSK Open/Close 이미지를 텍스처 평면으로 추가한다.
  - 기존 비품실 구조, 사물함, 사출대 단말기 프레임/버튼, SR-08, SR-11, SR-12 샘플 구조는 유지한다.
  - `index.html`에 SR-07 HSK 화면 요약과 10번 근접 렌더를 추가한다.
- 수정 대상:
  - `scripts/GenerateSupplyRoomShellSample.py`
  - `scripts/Run-SupplyRoomShellSample.ps1`
  - `docs/SUPPLY_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-23.md`
  - `artSample/supply_room_shell/` 샘플 산출물
- 실행 예정 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 실행하지 않을 작업:
  - Unity 에디터 리프레시/브리지 실행
  - `Assets/` 수정
  - 실제 Unity 씬 배치
  - 사출대 개폐, 쿨타임, 사출 판정, 상호작용 로직 구현
  - Validate/Test/Build

## SR-07 사출대 단말기 HSK Open/Close 화면 샘플 제작 완료

- 실행 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
  - 텍스처 UV 좌우 반전 수정 후 `.\scripts\Run-SupplyRoomShellSample.ps1` 재실행
- 결과:
  - 두 번의 샘플 생성 명령 모두 종료 코드 `0`으로 완료됐다.
  - `artSample/supply_room_shell/index.html`을 SR-07 HSK 화면 요약이 먼저 보이도록 갱신했다.
  - `artSample/supply_room_shell/renders/10_ejection_terminal_hsk_screen.png`를 추가했다.
  - SR-07 사출대 단말기 화면 앞면에 `HSK_Open_Close.png`를 텍스처 평면으로 얹었다.
  - 첫 렌더에서 텍스트가 좌우 반전되어 보여 UV를 수정했고, 재렌더에서 `OPEN/CLOSE [E]`가 정상 방향으로 보이는 것을 확인했다.
- 실행하지 않은 작업:
  - Unity 에디터 리프레시/브리지 실행
  - `Assets/` 수정
  - 실제 Unity 씬 배치
  - 사출대 개폐, 쿨타임, 사출 판정, 상호작용 로직 구현
  - Validate/Test/Build

## SR-12 비품실 CCTV 앵커 Unity 적용 시작

- 사용자가 조건부 승인된 SR-12 CCTV 샘플을 Unity에 배치하라고 요청했다.
- 적용 조건:
  - SR-12 CCTV 위치는 샘플의 북서쪽 코너 기준을 따른다.
  - CCTV는 사물함 오브젝트와 겹치지 않아야 한다.
  - 겹침을 피하기 위해 사물함 오브젝트 자체는 수정하지 않는다.
- 적용 범위:
  - `Approved Supply Room 01 Shell` 하위에 SR-12 CCTV 오브젝트만 추가한다.
  - SR-12 기존 오브젝트가 있으면 SR-12만 삭제 후 재생성한다.
  - SR-12 외 비품실 하위 오브젝트는 보호 스냅샷으로 변경 여부를 확인한다.
  - 비품실 밖 모든 기존 오브젝트는 보호 스냅샷으로 변경 여부를 확인한다.
- 수정 대상:
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/ApprovedSupplyRoomShellCurrentState.cs`
  - `docs/SUPPLY_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-23.md`
- 추가한 Unity 브리지 명령:
  - `AddApprovedSupplyRoomSr12CctvOnly`
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr12CctvOnly -LogPath .\Logs\ApprovedSupplyRoomSr12CctvOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateRestore.log -TimeoutSeconds 300`
- 실행하지 않을 작업:
  - 사물함, 사출대, SR-08 바닥 패널, SR-11 복도 문구 등 기존 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - 통제실 CCTV 화면 전환, 구매/해금, RenderTexture 로직 구현
  - Validate/Test/Build

## SR-12 비품실 CCTV 앵커 Unity 적용 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr12CctvOnly -LogPath .\Logs\ApprovedSupplyRoomSr12CctvOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateRestore.log -TimeoutSeconds 300`
- 결과:
  - 다섯 명령 모두 종료 코드 `0`으로 완료됐다.
  - `Approved Supply Room 01 Shell` 하위에 `SR-12 CCTV northwest corner anchor`를 추가했다.
  - SR-12 하위에는 코너 벽/천장 브래킷, 케이블, 관절 암, 힌지 조인트, CCTV 본체, 보호 후드, 렌즈, 감시 방향 표시 레이를 배치했다.
  - SR-12 물리 파츠와 사물함 렌더러의 경계가 겹치지 않는지 브리지에서 검사했고 통과했다.
  - SR-12 외 비품실 하위 오브젝트와 비품실 밖 기존 오브젝트는 보호 스냅샷 검사 대상으로 유지했다.
  - 적용 후 `ApprovedSupplyRoomShellCurrentState.cs`를 다시 캡처하고 복원/저장해 SR-12 포함 상태를 최종 복구 기준으로 반영했다.
- 실행하지 않은 작업:
  - 사물함, 사출대, SR-08 바닥 패널, SR-11 복도 문구 등 기존 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - 통제실 CCTV 화면 전환, 구매/해금, RenderTexture 로직 구현
  - Validate/Test/Build

## SR-12 비품실 CCTV 앵커 샘플 제작 시작

- 사용자가 비품실을 위에서 내려다볼 때 왼쪽 상단 모서리에 배치할 CCTV 오브젝트 샘플 제작을 요청했다.
- 좌상단은 기존 비품실 샘플 평면 기준 북서쪽 코너로 해석한다.
- 샘플 반영 범위:
  - `artSample/supply_room_shell/`의 비품실 샘플에 SR-12 CCTV 오브젝트를 추가한다.
  - 벽/천장 코너 브래킷, 짧은 관절 암, 소형 CCTV 본체, 렌즈, 감시 방향 표시 레이를 포함한다.
  - `index.html`, `README.md`, `ASSET_MANIFEST.json`, `APPROVAL_STATUS.json`에 SR-12 샘플 설명을 반영한다.
- 수정 대상:
  - `scripts/GenerateSupplyRoomShellSample.py`
  - `scripts/Run-SupplyRoomShellSample.ps1`
  - `docs/SUPPLY_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-23.md`
  - `artSample/supply_room_shell/` 샘플 산출물
- 실행 예정 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 실행하지 않을 작업:
  - Unity 에디터 리프레시/브리지 실행
  - `Assets/` 수정
  - 실제 Unity 씬 배치
  - 통제실 CCTV 화면 전환, 구매/해금, RenderTexture 로직 구현
  - 비품실 밖 오브젝트 수정
  - Validate/Test/Build

## SR-12 비품실 CCTV 앵커 샘플 제작 완료

- 실행 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 결과:
  - 명령이 종료 코드 `0`으로 완료됐다.
  - `artSample/supply_room_shell/index.html`을 SR-12 승인용 요약이 먼저 보이도록 갱신했다.
  - `artSample/supply_room_shell/renders/09_cctv_corner.png`를 추가했다.
  - SR-12 CCTV는 비품실 평면 기준 왼쪽 상단, 즉 북서쪽 벽/천장 코너에 배치했다.
  - CCTV 파츠는 코너 브래킷, 짧은 관절 암, 소형 본체, 보호 후드, 검은 렌즈, 감시 방향 표시 레이로 구성했다.
- 실행하지 않은 작업:
  - Unity 에디터 리프레시/브리지 실행
  - `Assets/` 수정
  - 실제 Unity 씬 배치
  - 통제실 CCTV 화면 전환, 구매/해금, RenderTexture 로직 구현
  - 비품실 밖 오브젝트 수정
  - Validate/Test/Build

## SR-12 비품실 CCTV 앵커 조건부 승인 기록

- 사용자가 SR-12 CCTV 샘플을 조건부 승인했다.
- 승인 조건:
  - Unity 적용 시 CCTV가 사물함 오브젝트와 겹치지 않아야 한다.
  - 겹침을 피하기 위해 사물함 오브젝트 자체를 수정하지 않는다.
- 반영 내용:
  - `artSample/supply_room_shell/APPROVAL_STATUS.json`의 승인 상태를 `조건부 승인`으로 변경했다.
  - `artSample/supply_room_shell/ASSET_MANIFEST.json`에 승인 조건을 추가했다.
  - `docs/SUPPLY_ROOM_OBJECTS.md`의 SR-12 상태를 `조건부 승인`으로 변경했다.
- 실행하지 않은 작업:
  - Unity 에디터 리프레시/브리지 실행
  - `Assets/` 수정
  - 실제 Unity 씬 배치
  - 샘플 재생성
  - Validate/Test/Build

## 비품실 전체 현재 편집 상태 재반영 시작

- 사용자가 Unity에서 직접 편집한 비품실 전체 오브젝트 상태를 스크립트에 반영하라고 요청했다.
- 적용 범위는 `Approved Supply Room 01 Shell` 루트와 그 하위 전체 Transform/activeSelf 상태로 제한한다.
- 작업 방식:
  - 현재 Unity 씬의 `Approved Supply Room 01 Shell` 전체 상태를 `ApprovedSupplyRoomShellCurrentState.cs`에 캡처한다.
  - 캡처한 상태를 다시 복원 명령으로 적용하고 저장해 최종 복구 기준으로 둔다.
- 수정/생성 대상:
  - `Assets/_Project/Editor/Validation/ApprovedSupplyRoomShellCurrentState.cs`
  - `docs/PROGRESS_2026-06-23.md`
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateRestore.log -TimeoutSeconds 300`
- 실행하지 않을 작업:
  - 비품실 전체 재생성
  - 비품실 밖 오브젝트 수정
  - 무기실, 동력실, 운송창고, 통제실, 조종실, 복도, 카메라, 조명, UI, 매니저 수정
  - SR-11 문구 추가 재작업
  - 샘플 재생성
  - Validate/Test/Build

## 비품실 전체 현재 편집 상태 재반영 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateRestore.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - 현재 Unity 씬의 `Approved Supply Room 01 Shell` 루트와 하위 전체 Transform/activeSelf 상태를 `ApprovedSupplyRoomShellCurrentState.cs`에 반영했다.
  - 캡처된 상태를 복원 명령으로 다시 적용하고 저장해 비품실의 최종 복구 기준으로 확정했다.
- 실행하지 않은 작업:
  - 비품실 전체 재생성
  - 비품실 밖 오브젝트 수정
  - 무기실, 동력실, 운송창고, 통제실, 조종실, 복도, 카메라, 조명, UI, 매니저 수정
  - SR-11 문구 추가 재작업
  - 샘플 재생성
  - Validate/Test/Build

## SR-11 복도 문구 글자별 배치 수정 시작

- 사용자가 글자 크기를 줄여도 효과가 없는 이유가 문구 전체가 하나의 오브젝트 취급이기 때문이라고 지적했고, 접근 방식을 바꾸라고 지시했다.
- 수정 대상은 SR-11 복도 문구 2개만으로 제한한다.
- 수정 내용:
  - `SR-11 armory direction wall text`는 빈 부모 오브젝트로 두고, 하위에 `A/R/M/O/R/Y` 글자를 개별 TextMesh로 생성한다.
  - `SR-11 cargo hold direction wall text`도 빈 부모 오브젝트로 두고, 하위에 `C/A/R/G/O/H/O/L/D` 글자를 개별 TextMesh로 생성한다.
  - 글자는 입구 상부 패널 폭 안에 들어가도록 문자 슬롯 간격으로 균등 배치한다.
  - 기존 통짜 TextMesh 방식은 사용하지 않는다.
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr11TextOnly -LogPath .\Logs\ApprovedSupplyRoomSr11TextOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
- 실행하지 않을 작업:
  - 비품실 전체 재생성
  - SR-11 문구 외 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - SR-11 표지판 판넬, 색상 스트립, 바닥 화살표, 바닥 문구 생성
  - Validate/Test/Build

## SR-11 복도 문구 글자별 배치 수정 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr11TextOnly -LogPath .\Logs\ApprovedSupplyRoomSr11TextOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
- 결과:
  - 세 명령 모두 종료 코드 `0`으로 완료됐다.
  - 브리지 로그 기준 SR-11 문구 부모 그룹은 `2`개 생성됐다.
  - `ARMORY`는 부모 아래 개별 글자 `6`개로 배치된다.
  - `CARGO HOLD`는 공백 슬롯을 간격으로만 사용하고 부모 아래 개별 글자 `9`개로 배치된다.
  - 각 글자는 입구 상부 패널 폭 안에 들어가도록 문자 슬롯 간격으로 균등 배치된다.
  - 비품실 내 SR-11 문구 외 오브젝트와 비품실 밖 기존 루트는 보호 검사 대상이었다.
- 실행하지 않은 작업:
  - 비품실 전체 재생성
  - SR-11 문구 외 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - SR-11 표지판 판넬, 색상 스트립, 바닥 화살표, 바닥 문구 생성
  - Validate/Test/Build

## SR-11 복도 문구 상부 패널 맞춤 수정 시작

- 사용자가 SR-11 문구 크기를 입구 위 패널 크기에 맞추라고 지시했다.
- 수정 대상은 SR-11 복도 문구 2개만으로 제한한다.
- 수정 내용:
  - `SR-11 armory direction wall text`를 무기실 입구 상부 패널 중심에 맞춘다.
  - `SR-11 cargo hold direction wall text`를 운송창고 입구 상부 패널 중심에 맞춘다.
  - `CARGO HOLD`까지 상부 패널 폭 안에 들어가도록 문구 크기를 줄인다.
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr11TextOnly -LogPath .\Logs\ApprovedSupplyRoomSr11TextOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
- 실행하지 않을 작업:
  - 비품실 전체 재생성
  - SR-11 문구 외 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - SR-11 표지판 판넬, 색상 스트립, 바닥 화살표, 바닥 문구 생성
  - Validate/Test/Build

## SR-11 복도 문구 상부 패널 맞춤 수정 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr11TextOnly -LogPath .\Logs\ApprovedSupplyRoomSr11TextOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
- 결과:
  - 세 명령 모두 종료 코드 `0`으로 완료됐다.
  - 브리지 로그 기준 `SR-11` 문구 Transform은 `2`개 생성됐다.
  - `SR-11 armory direction wall text` 위치를 무기실 입구 상부 패널 중심에 맞췄다.
  - `SR-11 cargo hold direction wall text` 위치를 운송창고 입구 상부 패널 중심에 맞췄다.
  - 문구 크기는 `0.18`에서 `0.12`로 줄여 상부 패널 안에 들어가도록 조정했다.
  - 비품실 내 SR-11 문구 외 오브젝트와 비품실 밖 기존 루트는 보호 검사 대상이었다.
- 실행하지 않은 작업:
  - 비품실 전체 재생성
  - SR-11 문구 외 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - SR-11 표지판 판넬, 색상 스트립, 바닥 화살표, 바닥 문구 생성
  - Validate/Test/Build

## SR-11 복도 문구 재생성 수정 시작

- 사용자가 이전 SR-11 적용 결과가 문구가 아니라 이상한 오브젝트로 생성됐고, 해당 오브젝트를 직접 삭제했다고 말했다.
- 수정 대상은 SR-11 복도 문구 2개만으로 제한한다.
- 원인 판단:
  - 기존 구현은 `TextMesh`에 일반 Lit 머티리얼을 붙여 글자가 아니라 글리프 면처럼 보일 수 있었다.
  - 기존 위치는 동쪽 벽 두께 안쪽에 가까워 문구가 벽에 묻힐 수 있었다.
- 수정 내용:
  - SR-11 전용 문구 생성은 폰트 기반 머티리얼을 쓰도록 바꾼다.
  - 문구 위치를 동쪽 벽 내부가 아니라 방 안쪽 면 앞으로 옮긴다.
  - 문구가 방 안쪽을 향하도록 회전을 조정한다.
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr11TextOnly -LogPath .\Logs\ApprovedSupplyRoomSr11TextOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
- 실행하지 않을 작업:
  - 비품실 전체 재생성
  - SR-11 문구 외 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - SR-11 표지판 판넬, 색상 스트립, 바닥 화살표, 바닥 문구 생성
  - Validate/Test/Build

## SR-11 복도 문구 재생성 수정 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr11TextOnly -LogPath .\Logs\ApprovedSupplyRoomSr11TextOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
- 결과:
  - 세 명령 모두 종료 코드 `0`으로 완료됐다.
  - 브리지 로그 기준 `SR-11` 문구 Transform은 `2`개 생성됐다.
  - `SR-11 armory direction wall text`, `SR-11 cargo hold direction wall text`만 생성 대상으로 유지했다.
  - 문구 전용 생성은 폰트 기반 머티리얼을 쓰도록 수정했다.
  - 문구 위치를 동쪽 벽 내부가 아니라 방 안쪽 면 앞으로 옮겼고, 방 안쪽을 향하도록 회전을 바꿨다.
  - 비품실 내 SR-11 문구 외 오브젝트와 비품실 밖 기존 루트는 보호 검사 대상이었다.
- 실행하지 않은 작업:
  - 비품실 전체 재생성
  - SR-11 문구 외 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - SR-11 표지판 판넬, 색상 스트립, 바닥 화살표, 바닥 문구 생성
  - Validate/Test/Build

## SR-11 복도 문구 한정 Unity 적용 시작

- 사용자가 SR-11 샘플 중 복도 문구만 승인했다.
- Unity 적용 대상:
  - `SR-11 armory direction wall text`
  - `SR-11 cargo hold direction wall text`
- 제외 대상:
  - SR-11 표지판 백플레이트
  - SR-11 색상 스트립
  - SR-11 바닥 화살표
  - SR-11 바닥 문구
  - SR-09/SR-10 출입구 구조
  - SR-08 패널
  - 비품실 내 나머지 전체 오브젝트
  - 비품실 밖 모든 오브젝트
- 승인 상태 파일에는 `approvedUnityScope: corridor_text_only`로 범위를 좁혀 기록했다.
- 추가한 Unity 브리지 명령:
  - `AddApprovedSupplyRoomSr11TextOnly`
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr11TextOnly -LogPath .\Logs\ApprovedSupplyRoomSr11TextOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
- 실행하지 않을 작업:
  - 비품실 전체 재생성
  - SR-11 문구 외 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - SR-11 길찾기 로직 또는 UI 구현
  - 샘플 재렌더링/Blender 재실행
  - Validate/Test/Build

## SR-11 복도 문구 한정 Unity 적용 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr11TextOnly -LogPath .\Logs\ApprovedSupplyRoomSr11TextOnly.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
- 결과:
  - 세 명령 모두 종료 코드 `0`으로 완료됐다.
  - Unity 씬의 `Approved Supply Room 01 Shell` 하위에 SR-11 벽면 복도 문구 텍스트 2개만 추가했다.
  - 추가 대상은 `SR-11 armory direction wall text`, `SR-11 cargo hold direction wall text`로 제한했다.
  - SR-11 표지판 백플레이트, 색상 스트립, 바닥 화살표, 바닥 문구는 적용하지 않았다.
  - SR-11 문구 외 비품실 하위 오브젝트와 비품실 밖 기존 씬 루트는 보호 스냅샷 검사 대상으로 두었다.
- 실행하지 않은 작업:
  - 비품실 전체 재생성
  - SR-11 문구 외 비품실 오브젝트 수정
  - 비품실 밖 오브젝트 수정
  - SR-11 길찾기 로직 또는 UI 구현
  - 샘플 재렌더링/Blender 재실행
  - Validate/Test/Build

## SR-11 복도 방향 표시 레이블 샘플 제작 시작

- 사용자가 비품실 오브젝트 목록 중 11번 샘플 제작을 지시했다.
- SR-11은 모델링 샘플 승인 대상이며, 실제 Unity 씬/프리팹/런타임 자산에는 아직 연결하지 않는다.
- 샘플 기준:
  - `ARMORY`, `CARGO HOLD` 방향을 플레이 중 확인할 수 있는 독립 방향 표시 오브젝트를 만든다.
  - 기존 SR-09 무기실 방향 출입구와 SR-10 운송창고 방향 출입구는 구조 변경하지 않는다.
  - 벽면 방향 표지판, 색상 스트립, 바닥 방향 화살표를 SR-11 이름대의 독립 오브젝트로 추가한다.
  - `index.html`에 SR-11 요약본을 포함한다.
- 승인된 수정 범위:
  - `scripts/GenerateSupplyRoomShellSample.py`
  - `scripts/Run-SupplyRoomShellSample.ps1`
  - `artSample/supply_room_shell/` 샘플 산출물
  - `docs/SUPPLY_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-23.md`
- 실행 예정 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 실행하지 않을 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 수정
  - SR-11 길찾기 로직 또는 UI 구현
  - Validate/Test/Build

## SR-11 복도 방향 표시 레이블 샘플 제작 완료

- 실행 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/supply_room_shell/` 샘플 산출물이 SR-11 복도 방향 표시 레이블 기준으로 재생성됐다.
- 반영 내용:
  - `SR-11 armory direction wall sign backplate`와 `SR-11 cargo hold direction wall sign backplate`를 추가했다.
  - 각 표지판에 출입구 색상과 맞춘 색상 스트립과 `ARMORY`, `CARGO HOLD` 텍스트를 넣었다.
  - 각 출입구 앞 바닥에 SR-11 방향 화살표와 바닥 방향 텍스트를 추가했다.
  - 새 확인 렌더 `artSample/supply_room_shell/renders/08_corridor_direction_labels.png`를 추가했다.
  - `artSample/supply_room_shell/index.html`에 SR-11 요약 섹션을 추가했다.
- 실행하지 않은 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 수정
  - SR-11 길찾기 로직 또는 UI 구현
  - Validate/Test/Build

## 비품실 현재 편집 상태 스크립트 반영

- 사용자가 Unity에서 직접 편집한 비품실 전체 상태를 스크립트에 반영하기로 했다.
- 작업 대상은 `Approved Supply Room 01 Shell` 루트의 현재 Transform 상태 저장/복원 경로로 제한한다.
- 무기실, 동력실, 화물칸, 복도, 룸 셀, 카메라, 조명, UI, 매니저 오브젝트는 수정 대상에서 제외한다.
- `CaptureApprovedSupplyRoomShellCurrentState` 브리지 명령을 추가해 현재 비품실 Transform 상태를 `ApprovedSupplyRoomShellCurrentState.cs`에 저장하도록 했다.
- `RestoreApprovedSupplyRoomShellCurrentState` 브리지 명령을 추가해 저장된 비품실 상태를 최종 복구 시점으로 다시 적용하도록 했다.
- 승인된 순서대로 Unity 리프레시, 비품실 상태 캡처, Unity 리프레시, 비품실 상태 복원 명령을 실행했다.
- 비품실 전체 현재 상태 반영 외의 재생성, 모델링 수정, Validate/Test/Build는 실행하지 않았다.

## SR-08 사출 위험 구역 철판 샘플 제작 시작

- 사용자가 SR-04는 UI로 처리하므로 모델링하지 않고 SR-08을 진행하라고 지시했다.
- SR-08은 사출당할 영역이 바닥에 표시되는 형태로 보여야 한다.
- 사용자 요청에 따라 경고선만 그리는 방식이 아니라 사출대 앞 바닥을 철판으로 까는 형태로 샘플 제작한다.
- 승인된 수정 범위:
  - `scripts/GenerateSupplyRoomShellSample.py`
  - `scripts/Run-SupplyRoomShellSample.ps1`
  - `artSample/supply_room_shell/` 샘플 산출물
  - `docs/SUPPLY_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-23.md`
- 실행 예정 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 실행하지 않을 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 수정
  - SR-04 모델링
  - SR-08 사출 판정/로직 구현
  - Validate/Test/Build

## SR-08 사출 위험 구역 철판 샘플 제작 완료

- 실행 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/supply_room_shell/` 샘플 산출물이 SR-08 철판형 사출 위험 구역 기준으로 재생성됐다.
- 반영 내용:
  - 사출대 앞 바닥에 `SR-08 ejection hazard steel floor plate main` 철판 구역을 추가했다.
  - 철판 구역에 패널 이음선, 가장자리 경고 트림, 검은 사선 경고 무늬, 볼트, 사출 방향 체브론을 추가했다.
  - 새 확인 렌더 `artSample/supply_room_shell/renders/07_ejection_hazard_floor.png`를 추가했다.
  - SR-04 분류 표시부는 사용자 지시에 따라 물리 모델링에서 제외하고 UI 작업으로 문서화했다.
- 실행하지 않은 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 수정
  - SR-08 사출 판정/로직 구현
  - Validate/Test/Build

## SR-08 사출 위험 구역 Unity 전용 적용 시작

- 사용자가 SR-08 바닥 철판 패널 샘플을 승인했다.
- 사용자 지시:
  - 승인된 바닥 패널만 사출대 위치에 옮긴다.
  - 다른 오브젝트는 건드리지 않는다.
- 승인된 적용 범위:
  - `Approved Supply Room 01 Shell` 하위 SR-08 사출 위험 구역 바닥 철판 패널 계열만 추가 또는 갱신한다.
  - 현재 비품실에서 사출대는 이전 위치/각도 교체 작업 이후 음수 Z 쪽에 있으므로, SR-08 패널도 그 사출대 앞 바닥에 맞춰 배치한다.
- 보호 대상:
  - SR-08 외 모든 비품실 하위 오브젝트
  - 비품실 밖 모든 기존 씬 루트
- 수정 파일:
  - `artSample/supply_room_shell/APPROVAL_STATUS.json`
  - `artSample/supply_room_shell/ASSET_MANIFEST.json`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `docs/SUPPLY_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-23.md`
- 추가한 Unity 브리지 명령:
  - `AddApprovedSupplyRoomSr08Only`
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr08Only -LogPath .\Logs\ApprovedSupplyRoomSr08Only.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
- 실행하지 않을 작업:
  - 비품실 전체 재생성
  - SR-08 외 비품실 오브젝트 수정
  - 무기실, 동력실, 운송창고, 통제실, 조종실, 복도, 카메라, 조명, UI, 매니저 수정
  - SR-04 모델링
  - SR-08 사출 판정/로직/쿨타임 구현
  - Validate/Test/Build

## SR-08 사출 위험 구역 Unity 전용 적용 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr08Only -LogPath .\Logs\ApprovedSupplyRoomSr08Only.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - Unity 씬의 `Approved Supply Room 01 Shell` 하위에 SR-08 사출 위험 구역 바닥 철판 패널 계열만 추가했다.
  - SR-08 패널은 현재 비품실 사출대 위치에 맞춰 사출대 앞 바닥에 배치했다.
  - SR-08 외 비품실 하위 오브젝트와 비품실 밖 기존 씬 루트는 보호 스냅샷 검사 대상으로 두었다.
  - 적용 후 `ApprovedSupplyRoomShellCurrentState.cs`를 다시 캡처해 SR-08 포함 상태를 복구 기준으로 반영했다.
- 실행하지 않은 작업:
  - 비품실 전체 재생성
  - SR-08 외 비품실 오브젝트 수정
  - 무기실, 동력실, 운송창고, 통제실, 조종실, 복도, 카메라, 조명, UI, 매니저 수정
  - SR-04 모델링
  - SR-08 사출 판정/로직/쿨타임 구현
  - Validate/Test/Build

## 비품실 전체 현재 편집 상태 재반영 시작

- 사용자가 Unity에서 직접 편집한 비품실 전체 오브젝트 상태를 스크립트에도 반영하라고 요청했다.
- 사용자가 범위를 `Approved Supply Room 01 Shell` 루트와 그 하위 전체 오브젝트로 정정했다.
- 적용 대상:
  - `Approved Supply Room 01 Shell` 루트
  - `Approved Supply Room 01 Shell` 하위 전체 Transform/activeSelf 상태
- 비품실 밖 오브젝트 처리:
  - 수정하지 않는다.
  - 삭제하지 않는다.
  - 생성하지 않는다.
  - 복구 명령에서는 비품실 밖 씬 루트를 보호 스냅샷 검사 대상으로 둔다.
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateRestore.log -TimeoutSeconds 300`
- 실행하지 않을 작업:
  - 비품실 전체 재생성
  - 비품실 밖 오브젝트 수정
  - 무기실, 동력실, 운송창고, 통제실, 조종실, 복도, 카메라, 조명, UI, 매니저 수정
  - SR-08 사출 판정/로직 구현
  - Validate/Test/Build

## 비품실 전체 현재 편집 상태 재반영 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateRestore.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - 현재 Unity 씬의 비품실 전체 Transform/activeSelf 상태가 `ApprovedSupplyRoomShellCurrentState.cs`에 반영됐다.
  - 반영된 상태 수는 루트 포함 `141`개 Transform이다.
  - 반영된 스크립트 기준으로 복구 명령을 실행했고 종료 코드 `0`으로 완료됐다.
  - 이 상태를 비품실의 최종 복구 기준으로 둔다.
- 실행하지 않은 작업:
  - 비품실 전체 재생성
  - 비품실 밖 오브젝트 수정
  - 무기실, 동력실, 운송창고, 통제실, 조종실, 복도, 카메라, 조명, UI, 매니저 수정
  - SR-08 사출 판정/로직 구현
  - Validate/Test/Build

## 오늘 작업 범위

- 현재 날짜를 `2026-06-23`으로 확인했다.
- 오늘 날짜 진행문서가 없어 최신 진행문서 `docs/PROGRESS_2026-06-22.md`를 먼저 확인했다.
- 사용자가 Unity 에디터를 열라고 요청해 승인 후 `.\scripts\Open-UnityProject.ps1`만 실행했다.
- 사용자가 무기실 오브젝트 목록을 요청해 승인 후 `docs/ARMORY_OBJECTS.md`만 읽고 목록을 요약했다.

## AR-05 포탑 핸들 샘플 제작

- 사용자가 AR-01 제작 과정에서 AR-02, AR-03, AR-04, AR-05, AR-06, AR-07, AR-08, AR-09, AR-10이 함께 구현되었고, AR-11은 AR-05에 넣으면 된다고 정리했다.
- 사용자가 AR-12를 진행하기 전에 AR-05 포탑 핸들 모양을 바꾸라고 요청했다.
- 사용자 지정 외형:
  - 기존 타륜형이 아니다.
  - 양손으로 잡는 U자형 그립이다.
  - 스위치가 위쪽에 달려 엄지로 누를 수 있는 형태다.
- 사용자는 Unity 오브젝트를 건드리지 말아야 한다고 명시했다.
- 승인된 작업 범위:
  - `artSample/armory_shell/` 승인용 샘플 산출물만 갱신한다.
  - `scripts/GenerateArmoryShellSample.py`, `scripts/Run-ArmoryShellSample.ps1`, `docs/ARMORY_OBJECTS.md`, 이 진행문서만 수정한다.
- 실행하지 않을 작업:
  - Unity Refresh
  - Unity 브리지 명령
  - Unity 씬/프리팹/런타임 자산 수정
  - `Assets/` 하위 파일 수정
  - Validate/Test/Build
  - AR-12 파손/폭파 상태 작업

## AR-05 샘플 수정 내용

- `scripts/GenerateArmoryShellSample.py`의 AR-05 생성부에서 기존 타륜형 토러스와 가로/세로 바를 제거했다.
- 새 AR-05는 다음 구조로 샘플링한다.
  - 콘솔 베이스
  - 힌지 하우징
  - U자형 하단 크로스바
  - 좌우 세로 양손 그립
  - 각 그립 상단의 빨간 엄지 스위치 캡
  - 상단 스위치 옆 보호 레일
  - 중앙 피벗 핀
- `artSample/armory_shell/renders/07_turret_handle_detail.png`를 추가 렌더로 생성해 AR-05 형상을 별도 확인할 수 있게 했다.
- `docs/ARMORY_OBJECTS.md`에는 AR-02~AR-10이 AR-01 샘플/현재 무기실 구성에 포함되었고, AR-11은 AR-05에 포함되는 기준으로 갱신했다.

## 실행 결과

- 실행한 명령:
  - `.\scripts\Run-ArmoryShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/armory_shell/` 샘플 산출물이 재생성됐다.
  - 새 확인 렌더 `artSample/armory_shell/renders/07_turret_handle_detail.png`가 생성됐다.
- 실행하지 않은 작업:
  - Unity Refresh
  - Unity 브리지 명령
  - Unity 씬/프리팹/런타임 자산 수정
  - `Assets/` 하위 파일 수정
  - Validate/Test/Build
  - AR-12 파손/폭파 상태 작업

## AR-05 엄지 스위치 크기 축소

- 사용자가 AR-05 U자형 포탑 핸들의 엄지 버튼이 너무 크고 조종대 밖으로 삐져나온다고 지적했다.
- 사용자 요청에 따라 엄지 버튼만 크기를 줄이고, 다른 무기실 샘플 구조와 Unity 오브젝트는 건드리지 않는 범위로 진행했다.
- 수정한 파일:
  - `scripts/GenerateArmoryShellSample.py`
  - `docs/PROGRESS_2026-06-23.md`
- 수정 내용:
  - `AR-05 left/right thumb switch top cap`의 크기를 `(0.20, 0.12, 0.055)`에서 `(0.12, 0.085, 0.045)`로 줄였다.
- 수정하지 않은 항목:
  - AR-05 U자형 그립 위치와 구조
  - AR-05 콘솔, 힌지, 보호 레일, 피벗
  - AR-01~AR-04, AR-06~AR-12
  - Unity 씬/프리팹/런타임 자산
  - `Assets/` 하위 파일

## AR-02/AR-03 계단 단수 감소 샘플 제작

- 사용자가 중앙 기둥 높이를 조금 더 낮추고 계단 수도 줄이라고 요청했다.
- Unity 오브젝트를 건드리지 않고 `artSample/armory_shell/` 샘플만 수정하는 범위로 진행했다.
- 수정한 파일:
  - `scripts/GenerateArmoryShellSample.py`
  - `docs/PROGRESS_2026-06-23.md`
- 수정 내용:
  - AR-02 중앙 기둥 높이를 `1.36`에서 `1.08`로 더 낮췄다.
  - AR-03 계단 단수를 `12`단에서 `8`단으로 줄였다.
  - AR-03 계단 상승 높이를 `1.18`에서 `0.90`으로 낮췄다.
  - AR-03 난간 끝 높이도 낮춘 계단 높이에 맞췄다.
  - AR-04 상부 발판과 안전 림은 모델링 형태, 반지름, 두께를 바꾸지 않고 높이 위치만 더 낮췄다.
  - AR-05 포탑 핸들은 U자형 엄지 스위치 모델링 형태를 바꾸지 않고 발판 높이에 맞춰 위치만 더 낮췄다.
- 수정하지 않은 항목:
  - AR-04 발판 모델링 형태, 반지름, 두께
  - AR-05 U자형 핸들 형태와 엄지 스위치 형태
  - AR-01 룸 셸
  - AR-06 커브 스크린
  - AR-07~AR-10 출입구/복도/방향표시
  - Unity 씬/프리팹/런타임 자산
  - `Assets/` 하위 파일
- 실행한 명령:
  - `.\scripts\Run-ArmoryShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/armory_shell/` 샘플 산출물이 재생성됐다.
- 실행하지 않은 작업:
  - Unity Refresh
  - Unity 브리지 명령
  - Unity 씬/프리팹/런타임 자산 수정
  - `Assets/` 하위 파일 수정
  - Validate/Test/Build
- 실행한 명령:
  - `.\scripts\Run-ArmoryShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/armory_shell/` 샘플 산출물이 재생성됐다.
- 실행하지 않은 작업:
  - Unity Refresh
  - Unity 브리지 명령
  - Unity 씬/프리팹/런타임 자산 수정
  - `Assets/` 하위 파일 수정
  - Validate/Test/Build
  - AR-12 파손/폭파 상태 작업

## AR-05 엄지 스위치 부착 위치 수정

- 사용자가 AR-05 엄지 스위치가 조종대에 붙어 있지 않다고 지적했다.
- 사용자 승인에 따라 Unity 오브젝트를 건드리지 않고 `artSample/armory_shell/` 샘플 생성 기준만 수정했다.
- 수정한 파일:
  - `scripts/GenerateArmoryShellSample.py`
  - `docs/PROGRESS_2026-06-23.md`
- 수정 내용:
  - `AR-05 left/right thumb switch top cap`의 중심 위치를 `(x, 0.795, 3.15)`에서 `(x, 0.84, 3.122)`로 옮겨 좌우 세로 그립 상단에 닿게 했다.
  - 스위치 캡 회전을 제거해 그립 상단에 평평하게 붙어 보이도록 했다.
  - 스위치 보호 레일도 새 스위치 위치에 맞춰 최소 조정했다.
- 수정하지 않은 항목:
  - AR-05 U자형 그립 크기와 구조
  - AR-05 콘솔, 힌지, 피벗
  - AR-01~AR-04, AR-06~AR-12
  - Unity 씬/프리팹/런타임 자산
  - `Assets/` 하위 파일
- 실행한 명령:
  - `.\scripts\Run-ArmoryShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/armory_shell/` 샘플 산출물이 재생성됐다.
- 실행하지 않은 작업:
  - Unity Refresh
  - Unity 브리지 명령
  - Unity 씬/프리팹/런타임 자산 수정
  - `Assets/` 하위 파일 수정
  - Validate/Test/Build
  - AR-12 파손/폭파 상태 작업

## AR-05 U자형 엄지 스위치 샘플 승인 기록

- 사용자가 `artSample/armory_shell/`의 AR-05 U자형 포탑 핸들 샘플을 승인했다.
- 승인 조건:
  - AR-05 포탑 핸들은 타륜형이 아니라 U자형 양손 그립이다.
  - 각 그립 위쪽에는 엄지로 누를 수 있는 스위치가 붙어 있어야 한다.
  - 엄지 스위치는 조종대 밖으로 삐져나오지 않고 그립 상단에 붙어 있어야 한다.
- Unity 적용 조건:
  - 이 승인 기록은 Unity 적용 실행 승인이 아니다.
  - 추후 Unity 적용 시에는 AR-05 포탑 핸들만 수정한다.
  - 현재 저장된 무기실의 다른 오브젝트는 절대 수정하지 않는다.
  - `Approved Armory 01 Shell` 루트 Transform은 수정하지 않는다.
  - AR-01, AR-02, AR-03, AR-04, AR-06, AR-07, AR-08, AR-09, AR-10, AR-11, AR-12는 수정하지 않는다.
  - 무기실 밖 기타 씬 오브젝트도 수정하지 않는다.
- 수정한 파일:
  - `artSample/armory_shell/APPROVAL_STATUS.json`
  - `artSample/armory_shell/ASSET_MANIFEST.json`
  - `docs/ARMORY_OBJECTS.md`
  - `docs/PROGRESS_2026-06-23.md`
- 실행하지 않은 작업:
  - Unity 에디터 조작
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1`
  - Unity 브리지 명령 전체
  - `Assets/` 하위 파일 읽기/수정
  - `Assets/_Project/Scenes/CargoRunMvp.unity` 읽기/수정
  - `artSample/armory_shell/` 샘플 재생성
  - `.\scripts\Run-ArmoryShellSample.ps1`
  - Validate/Test/Build

## AR-05 Unity 전용 적용 시작

- 사용자가 승인된 AR-05 U자형 엄지 스위치 샘플을 Unity에 적용하라고 요청했다.
- 사용자는 AR-05만 수정하고 현재 저장된 무기실의 다른 오브젝트는 모두 건드리지 말라고 지시했다.
- 승인된 읽기 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellCurrentState.cs`
  - `docs/PROGRESS_2026-06-23.md`
- 승인된 수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `docs/PROGRESS_2026-06-23.md`
- 추가한 Unity 브리지 명령:
  - `UpdateApprovedArmoryAr05Only`
- 적용 방식:
  - `Approved Armory 01 Shell` 하위에서 이름이 `AR-05`로 시작하는 오브젝트만 삭제하고 승인 샘플 기준 U자형 포탑 핸들로 재생성한다.
  - AR-05 외 무기실 하위 오브젝트와 무기실 밖 씬 루트는 작업 전 Transform/activeSelf를 캡처한 뒤, 변경되면 명령이 실패하도록 보호한다.
- 보호 대상:
  - `Approved Armory 01 Shell` 루트 Transform
  - AR-01, AR-02, AR-03, AR-04
  - AR-06, AR-07, AR-08, AR-09, AR-10, AR-11, AR-12
  - AR-05가 아닌 모든 무기실 하위 오브젝트
  - 무기실 밖 기타 씬 오브젝트
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command UpdateApprovedArmoryAr05Only -LogPath .\Logs\ApprovedArmoryAr05Only.log -TimeoutSeconds 300`
- 실행하지 않을 명령:
  - `EnsureApprovedArmoryShell`
  - `MoveApprovedArmoryShellToZBelowControlRoom`
  - `UpdateApprovedArmoryAr03Only`
  - `CaptureApprovedArmoryShellCurrentState`
  - `RestoreApprovedArmoryShellCurrentState`
  - `Run-HarnessValidation.ps1`
  - `Run-EditModeTests.ps1`
  - `Run-PlayModeTests.ps1`
  - `Build-WindowsDev.ps1`
  - `Open-UnityProject.ps1 -Restart`

## AR-05 Unity 전용 적용 결과

- 실행한 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command UpdateApprovedArmoryAr05Only -LogPath .\Logs\ApprovedArmoryAr05Only.log -TimeoutSeconds 300`
- 결과:
  - 두 명령 모두 종료 코드 `0`으로 완료됐다.
  - Unity 씬의 `Approved Armory 01 Shell` 하위에서 AR-05 포탑 핸들만 U자형 양손 그립과 상단 엄지 스위치 형태로 재생성했다.
  - AR-05 외 무기실 하위 오브젝트와 무기실 밖 씬 루트는 보호 스냅샷 검사 대상이었다.
- 실행하지 않은 명령:
  - `EnsureApprovedArmoryShell`
  - `MoveApprovedArmoryShellToZBelowControlRoom`
  - `UpdateApprovedArmoryAr03Only`
  - `CaptureApprovedArmoryShellCurrentState`
  - `RestoreApprovedArmoryShellCurrentState`
  - `Run-HarnessValidation.ps1`
  - `Run-EditModeTests.ps1`
  - `Run-PlayModeTests.ps1`
  - `Build-WindowsDev.ps1`
  - `Open-UnityProject.ps1 -Restart`
- 수정하지 않은 항목:
  - `Approved Armory 01 Shell` 루트 Transform
  - AR-01, AR-02, AR-03, AR-04
  - AR-06, AR-07, AR-08, AR-09, AR-10, AR-11, AR-12
  - AR-05가 아닌 모든 무기실 하위 오브젝트
  - 무기실 밖 기타 씬 오브젝트

## 무기실 전체 현재 상태 스크립트 반영

- 사용자가 Unity에서 직접 편집한 무기실 전체 상태를 스크립트에 반영하라고 요청했다.
- 사용자는 무기실 내 오브젝트 외의 다른 오브젝트는 건드리지 말라고 명시했다.
- 적용 대상:
  - `Approved Armory 01 Shell` 루트
  - `Approved Armory 01 Shell` 하위 전체 오브젝트
- 무기실 밖 오브젝트 처리:
  - 수정하지 않는다.
  - 삭제하지 않는다.
  - 생성하지 않는다.
  - Transform, activeSelf, 컴포넌트, 머티리얼을 변경하지 않는다.
  - 복구 명령에서는 무기실 밖 씬 루트를 보호 스냅샷 검사 대상으로 둔다.
- 수정된 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellCurrentState.cs`
  - `docs/PROGRESS_2026-06-23.md`
- 실행한 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateRestore.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - 현재 Unity 씬의 무기실 전체 Transform/activeSelf 상태가 `ApprovedArmoryShellCurrentState.cs`에 반영됐다.
  - 반영된 스크립트 기준으로 복구 명령을 실행했고 종료 코드 `0`으로 완료됐다.
  - 이 상태를 무기실의 최종 복구 기준으로 둔다.
- 실행하지 않은 명령:
  - `EnsureApprovedArmoryShell`
  - `MoveApprovedArmoryShellToZBelowControlRoom`
  - `UpdateApprovedArmoryAr03Only`
  - `UpdateApprovedArmoryAr05Only`
  - `Run-HarnessValidation.ps1`
  - `Run-EditModeTests.ps1`
  - `Run-PlayModeTests.ps1`
  - `Build-WindowsDev.ps1`
  - `Open-UnityProject.ps1 -Restart`

## AR-02/AR-03 낮은 높이 샘플 제작

- 사용자가 중앙 기둥 높이가 너무 높으므로 기둥 높이를 낮추고, 계단 높이도 같이 낮추라고 요청했다.
- 사용자는 발판 자체의 모델링은 건드릴 필요가 없다고 уточ정했다.
- Unity 오브젝트를 건드리지 않고 `artSample/armory_shell/` 샘플만 수정하는 범위로 진행했다.
- 수정한 파일:
  - `scripts/GenerateArmoryShellSample.py`
  - `docs/PROGRESS_2026-06-23.md`
- 수정 내용:
  - AR-02 중앙 기둥 높이를 `2.24`에서 `1.72`로 낮췄다.
  - AR-03 계단 상승 높이를 `2.06`에서 `1.54`로 낮췄다.
  - AR-03 난간 끝 높이도 낮춘 계단 높이에 맞췄다.
  - AR-04 상부 발판과 안전 림은 모델링 형태, 반지름, 두께를 바꾸지 않고 높이 위치만 낮췄다.
  - AR-05 포탑 핸들은 U자형 엄지 스위치 모델링 형태를 바꾸지 않고 발판 높이에 맞춰 위치만 낮췄다.
- 수정하지 않은 항목:
  - AR-04 발판 모델링 형태, 반지름, 두께
  - AR-05 U자형 핸들 형태와 엄지 스위치 형태
  - AR-01 룸 셸
  - AR-06 커브 스크린
  - AR-07~AR-10 출입구/복도/방향표시
  - Unity 씬/프리팹/런타임 자산
  - `Assets/` 하위 파일
- 실행한 명령:
  - `.\scripts\Run-ArmoryShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/armory_shell/` 샘플 산출물이 재생성됐다.
- 실행하지 않은 작업:
  - Unity Refresh
  - Unity 브리지 명령
  - Unity 씬/프리팹/런타임 자산 수정
  - `Assets/` 하위 파일 수정
  - Validate/Test/Build
- 실행한 명령:
  - `.\scripts\Run-ArmoryShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/armory_shell/` 샘플 산출물이 재생성됐다.
- 실행하지 않은 작업:
  - Unity Refresh
  - Unity 브리지 명령
  - Unity 씬/프리팹/런타임 자산 수정
  - `Assets/` 하위 파일 수정
  - Validate/Test/Build

## AR-02/AR-03 추가 하향 샘플 제작

- 사용자가 중앙 기둥과 계단을 더 내리라고 요청했다.
- Unity 오브젝트를 건드리지 않고 `artSample/armory_shell/` 샘플만 수정하는 범위로 진행했다.
- 수정한 파일:
  - `scripts/GenerateArmoryShellSample.py`
  - `docs/PROGRESS_2026-06-23.md`
- 수정 내용:
  - AR-02 중앙 기둥 높이를 `1.72`에서 `1.36`으로 더 낮췄다.
  - AR-03 계단 상승 높이를 `1.54`에서 `1.18`로 더 낮췄다.
  - AR-03 난간 끝 높이도 낮춘 계단 높이에 맞췄다.
  - AR-04 상부 발판과 안전 림은 모델링 형태, 반지름, 두께를 바꾸지 않고 높이 위치만 더 낮췄다.
  - AR-05 포탑 핸들은 U자형 엄지 스위치 모델링 형태를 바꾸지 않고 발판 높이에 맞춰 위치만 더 낮췄다.
- 수정하지 않은 항목:
  - AR-04 발판 모델링 형태, 반지름, 두께
  - AR-05 U자형 핸들 형태와 엄지 스위치 형태
  - AR-01 룸 셸
  - AR-06 커브 스크린
  - AR-07~AR-10 출입구/복도/방향표시
  - Unity 씬/프리팹/런타임 자산
  - `Assets/` 하위 파일

## AR-02/AR-03 Unity 전용 적용 시작

- 사용자가 AR-02/AR-03 낮은 높이 샘플을 승인했고, Unity에 옮기되 AR-02와 AR-03을 제외한 나머지 무기실 오브젝트는 건드리지 말라고 지시했다.
- Unity에서 수정할 대상:
  - `Approved Armory 01 Shell` 하위 AR-02 중앙 기둥
  - `Approved Armory 01 Shell` 하위 AR-03 계단/난간
- Unity에서 수정하지 않을 대상:
  - `Approved Armory 01 Shell` 루트 Transform
  - AR-01
  - AR-04 발판/림
  - AR-05 포탑 핸들
  - AR-06~AR-12
  - AR-02/AR-03이 아닌 모든 무기실 하위 오브젝트
  - 무기실 밖 기타 씬 오브젝트
- 수정한 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `docs/PROGRESS_2026-06-23.md`
- 추가한 Unity 브리지 명령:
  - `UpdateApprovedArmoryAr02Ar03Only`
- 적용 방식:
  - 이름이 `AR-02`로 시작하는 기존 기둥 오브젝트만 삭제하고 낮은 기둥으로 재생성한다.
  - 이름이 `AR-03`으로 시작하는 기존 계단/난간 오브젝트만 삭제하고 8단 계단으로 재생성한다.
  - AR-02/AR-03 외 무기실 오브젝트와 무기실 밖 씬 루트는 작업 전 Transform/activeSelf를 캡처한 뒤, 변경되면 명령이 실패하도록 보호한다.
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command UpdateApprovedArmoryAr02Ar03Only -LogPath .\Logs\ApprovedArmoryAr02Ar03Only.log -TimeoutSeconds 300`
- 실행하지 않을 명령:
  - `EnsureApprovedArmoryShell`
  - `MoveApprovedArmoryShellToZBelowControlRoom`
  - `UpdateApprovedArmoryAr03Only`
  - `UpdateApprovedArmoryAr05Only`
  - `CaptureApprovedArmoryShellCurrentState`
  - `RestoreApprovedArmoryShellCurrentState`
  - `Run-HarnessValidation.ps1`
  - `Run-EditModeTests.ps1`
  - `Run-PlayModeTests.ps1`
  - `Build-WindowsDev.ps1`
  - `Open-UnityProject.ps1 -Restart`

## AR-02/AR-03 Unity 전용 적용 결과

- 실행한 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command UpdateApprovedArmoryAr02Ar03Only -LogPath .\Logs\ApprovedArmoryAr02Ar03Only.log -TimeoutSeconds 300`
- 결과:
  - 두 명령 모두 종료 코드 `0`으로 완료됐다.
  - Unity 씬의 `Approved Armory 01 Shell` 하위에서 AR-02 중앙 기둥과 AR-03 계단/난간만 수정했다.
  - AR-02는 승인 샘플 기준 낮은 중앙 기둥으로 재생성했다.
  - AR-03은 승인 샘플 기준 8단 계단과 낮은 난간으로 재생성했다.
  - AR-02/AR-03 외 무기실 하위 오브젝트와 무기실 밖 씬 루트는 보호 스냅샷 검사 대상이었다.
- 실행하지 않은 명령:
  - `EnsureApprovedArmoryShell`
  - `MoveApprovedArmoryShellToZBelowControlRoom`
  - `UpdateApprovedArmoryAr03Only`
  - `UpdateApprovedArmoryAr05Only`
  - `CaptureApprovedArmoryShellCurrentState`
  - `RestoreApprovedArmoryShellCurrentState`
  - `Run-HarnessValidation.ps1`
  - `Run-EditModeTests.ps1`
  - `Run-PlayModeTests.ps1`
  - `Build-WindowsDev.ps1`
  - `Open-UnityProject.ps1 -Restart`
- 수정하지 않은 항목:
  - `Approved Armory 01 Shell` 루트 Transform
  - AR-01
  - AR-04 발판/림
  - AR-05 포탑 핸들
  - AR-06, AR-07, AR-08, AR-09, AR-10, AR-11, AR-12
  - AR-02/AR-03이 아닌 모든 무기실 하위 오브젝트
  - 무기실 밖 기타 씬 오브젝트

## 무기실 전체 현재 편집 상태 재반영

- 사용자가 Unity에서 직접 편집한 현재 상태를 그대로 스크립트에 반영하라고 요청했다.
- 적용 대상:
  - `Approved Armory 01 Shell` 루트
  - `Approved Armory 01 Shell` 하위 전체 오브젝트
- 무기실 밖 오브젝트 처리:
  - 수정하지 않는다.
  - 삭제하지 않는다.
  - 생성하지 않는다.
  - Transform, activeSelf, 컴포넌트, 머티리얼을 변경하지 않는다.
  - 복구 명령에서는 무기실 밖 씬 루트를 보호 스냅샷 검사 대상으로 둔다.
- 수정된 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellCurrentState.cs`
  - `docs/PROGRESS_2026-06-23.md`
- 실행한 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateRestore.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - 현재 Unity 씬의 무기실 전체 Transform/activeSelf 상태가 `ApprovedArmoryShellCurrentState.cs`에 다시 반영됐다.
  - 반영된 스크립트 기준 복구 명령도 종료 코드 `0`으로 완료됐다.
  - 이 상태를 무기실의 최신 최종 복구 기준으로 둔다.
- 실행하지 않은 명령:
  - `EnsureApprovedArmoryShell`
  - `MoveApprovedArmoryShellToZBelowControlRoom`
  - `UpdateApprovedArmoryAr03Only`
  - `UpdateApprovedArmoryAr05Only`
  - `UpdateApprovedArmoryAr02Ar03Only`
  - `Run-HarnessValidation.ps1`
  - `Run-EditModeTests.ps1`
  - `Run-PlayModeTests.ps1`
  - `Build-WindowsDev.ps1`
  - `Open-UnityProject.ps1 -Restart`

## AR-12 파손 조종대 샘플 제작 시작

- 사용자가 AR-12 포탑 조종대 파손/폭파 상태를 구현하기 위한 샘플 제작을 요청했다.
- 사용자 지정 내용:
  - 조종대는 왼쪽으로 45도 기울어진 상태다.
  - 연기 이펙트를 넣는다.
  - 모니터는 파손된 채로 꺼져 있어야 한다.
  - 액정 깨짐은 `C:\Users\gus68\OneDrive\바탕 화면\111.jpg` 참고 이미지처럼 중심 충격점, 방사형 균열, 잔금, 흰 유리 파편 형태로 제작한다.
- Unity 적용 전 `artSample/` 승인용 샘플을 먼저 제작하는 범위로 진행했다.
- 읽은 파일:
  - `docs/ARMORY_OBJECTS.md`
  - `docs/PROGRESS_2026-06-23.md`
  - `scripts/GenerateArmoryShellSample.py`
  - `scripts/Run-ArmoryShellSample.ps1`
- 추가한 파일:
  - `scripts/GenerateArmoryDamageSample.py`
  - `scripts/Run-ArmoryDamageSample.ps1`
- 샘플 생성 대상:
  - `artSample/armory_damage_state/`
- 샘플 구성:
  - 왼쪽으로 45도 기울어진 파손 포탑 조종대.
  - 꺼진 검은 모니터 화면.
  - 참고 이미지 기반 중심 충격점, 긴 방사형 균열, 짧은 잔금, 흰 유리 파편.
  - 조종대 주변 반투명 연기, 얇은 연기 줄기, 바닥 그을림.
  - 파손된 U자형 포탑 핸들 일부와 느슨한 케이블.
- 실행하지 않을 작업:
  - Unity Refresh
  - Unity 브리지 명령
  - Unity 씬/프리팹/런타임 자산 수정
  - `Assets/` 하위 파일 수정
  - Validate/Test/Build
  - 기존 무기실 현재 상태 변경
## AR-12 파손 조종대 샘플 생성 완료

- 실행 명령:
  - `.\scripts\Run-ArmoryDamageSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - `artSample/armory_damage_state/` 아래에 AR-12 파손 조종대 승인용 샘플을 생성했다.
  - Unity 씬, `Assets/` 하위 파일, 기존 무기실 오브젝트, 검증/테스트/빌드 명령은 실행하거나 수정하지 않았다.
- 생성된 주요 확인 파일:
  - `artSample/armory_damage_state/index.html`
  - `artSample/armory_damage_state/renders/01_overview.png`
  - `artSample/armory_damage_state/renders/02_monitor_crack_detail.png`
  - `artSample/armory_damage_state/renders/03_left_tilt_profile.png`
  - `artSample/armory_damage_state/renders/04_smoke_effect.png`

## AR-12 파손 샘플 방향 정정 및 재생성 완료

- 사용자가 기존 AR-12 샘플 방향이 잘못됐다고 지적했다.
- 정정된 기준:
  - 조종대 본체가 45도로 기울어진 것이 아니다.
  - AR-05 핸들 부분만 왼쪽으로 45도 꺾인 상태여야 한다.
  - 파손 화면은 조종대에 달린 소형 모니터가 아니라 AR-06 커브형 대형 모니터다.
  - AR-06 커브형 대형 모니터에 사용자 제공 참고 이미지처럼 중심 충격점, 방사형 균열, 짧은 잔금, 흰 유리 파편을 세세하게 배치해야 한다.
- 수정한 파일:
  - `scripts/GenerateArmoryDamageSample.py`
  - `docs/PROGRESS_2026-06-23.md`
- 재생성 대상:
  - `artSample/armory_damage_state/`
- 실행 명령:
  - `.\scripts\Run-ArmoryDamageSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 조종대 본체는 정상 배치로 두고 AR-05 핸들만 왼쪽 45도 파손 각도로 꺾이도록 샘플을 재구성했다.
  - AR-06 커브형 대형 모니터를 추가하고, 그 표면에 참고 이미지형 중심 충격점, 방사형 균열, 잔금, 유리 파편을 다시 제작했다.
  - Unity Refresh, Unity 브리지 명령, `Assets/` 하위 파일 수정, 실제 무기실 오브젝트 수정, Validate/Test/Build는 실행하지 않았다.

## 무기실 모델링 제외 항목 정리 및 비품실 오브젝트 문서 작성

- 사용자가 AR-13, AR-14, AR-15, AR-16, AR-17, AR-18, AR-19는 모델링 작업이 아니므로 패스하라고 지시했다.
- 사용자가 원본 기획서를 보고 비품실 오브젝트 목록 문서를 만들라고 지시했다.
- 정리한 무기실 기준:
  - AR-12는 별도 파손 모델링을 진행하지 않는다.
  - AR-12는 조종대 주변 연기 이펙트와 AR-06 커브형 대형 스크린 꺼짐 상태로 후속 처리한다.
  - AR-13~AR-19는 무기실 고정 모델링 대상에서 제외하고 UI, 기능, 상태값, 사운드, 외부 목표 작업으로 분리한다.
  - AR-05 상태는 진행문서 기준 Unity 반영 완료로 정리했다.
- 생성한 비품실 문서:
  - `docs/SUPPLY_ROOM_OBJECTS.md`
- 비품실 문서에 정리한 주요 대상:
  - SR-01 비품실 룸 셸
  - SR-02 비품창고 벽
  - SR-03 기본 보관 슬롯
  - SR-04 비품창고 분류 표시부
  - SR-05 사출대 프레임
  - SR-06 사출대 위아래 문
  - SR-07 사출대 단말기
  - SR-08 사출 위험 구역
  - SR-09 무기실 방향 출입구
  - SR-10 운송창고 방향 출입구
  - SR-11 복도 방향 표시 레이블
  - SR-12 비품실 CCTV 앵커
  - SR-13~SR-19 UI, 데이터, 로직, VFX 항목
- 수정한 파일:
  - `docs/ARMORY_OBJECTS.md`
  - `docs/SUPPLY_ROOM_OBJECTS.md`
  - `docs/PROGRESS_2026-06-23.md`
- 실행하지 않은 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 읽기/수정
  - `artSample/` 샘플 생성/수정
  - Validate/Test/Build

## SR-01 비품실 룸 셸 샘플 제작 시작

- 사용자가 비품실 룸 셸 샘플 제작을 요청했다.
- 원본 기획서 기준으로 다음 구조를 반영한다.
  - 비품창고 벽, 빈 벽, 사출대 벽이 ㄷ자 형태다.
  - 남은 한 면에는 무기실 방향 출입구와 운송창고 방향 출입구가 함께 존재한다.
  - 비품창고 벽과 사출대 벽은 룸 셸 판단용 위치 표시로 넣는다.
- 샘플 범위:
  - `SR-01` 비품실 룸 셸
  - 비품창고 벽 위치 표시
  - 사출대 벽 위치 표시
  - 무기실 방향 출입구 위치 표시
  - 운송창고 방향 출입구 위치 표시
  - 방향/구역 라벨
- 추가한 파일:
  - `scripts/GenerateSupplyRoomShellSample.py`
  - `scripts/Run-SupplyRoomShellSample.ps1`
- 샘플 생성 대상:
  - `artSample/supply_room_shell/`
- 실행 예정 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 실행하지 않을 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 읽기/수정
  - 실제 비품실/무기실/운송창고 오브젝트 수정
  - 사출대 로직, 보관 로직, UI 구현
  - Validate/Test/Build

## SR-01 비품창고 참고 이미지형 닫힌 2문 사물함 수정 완료

- 실행 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/supply_room_shell/` 산출물이 참고 이미지형 닫힌 2문 금속 사물함 기준으로 재생성됐다.
- 반영 내용:
  - 기존의 살짝 열린 좌우 문 구조를 제거했다.
  - 사물함을 정면에서 닫힌 좌우 2문 금속 캐비닛 형태로 수정했다.
  - 연녹색 도장 금속 느낌의 문과 프레임 재질을 추가했다.
  - 중앙 세로 이음선을 추가했다.
  - 각 문 상단에 작은 가로형 명패 손잡이를 추가했다.
  - 문 중앙 하단 쪽에 검은 세로형 매립 손잡이와 잠금판을 추가했다.
  - 바깥쪽 좌우 힌지 레일과 힌지 배럴을 추가했다.
  - 문 네 모서리에 삼각형 타공/환기 구멍 패턴을 추가했다.
  - 문서와 HTML 미리보기 설명을 참고 이미지형 닫힌 2문 금속 사물함 기준으로 정정했다.
- 실행하지 않은 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 읽기/수정
  - 실제 비품실/무기실/운송창고 오브젝트 수정
  - 사출대 로직, 보관 로직, UI 구현
  - Validate/Test/Build

## SR-01 비품실 캐비닛/사출대 위치 교체 시작

- 사용자가 비품실의 캐비닛 오브젝트와 사출대 위치를 서로 바꾸라고 요청했다.
- 사용자 지시:
  - 위치만 바꾸는 것이 아니라 각도도 보이는 쪽으로 바꾼다.
  - 이 두 가지 외에는 다른 오브젝트를 건드리지 않는다.
- 수정 범위:
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `docs/PROGRESS_2026-06-23.md`
- 추가한 Unity 브리지 명령:
  - `SwapApprovedSupplyRoomCabinetAndEjectionBay`
- 교체 대상:
  - 캐비닛 계열: `SR-02 supply storage wall placement marker only`, `SR-02 visible gap behind freestanding locker`, `SR-02 freestanding...`, `SR-03...`
  - 사출대 계열: `SR-05 ejection...`, `SR-06 ejection...`, `SR-07 visible ejection terminal...`
- 적용 방식:
  - 위 두 계열의 로컬 Z 위치를 반대로 미러링해 서로 반대편 벽 쪽으로 이동한다.
  - 위 두 계열의 로컬 Y축 회전을 180도 더해 방 내부에서 보이는 방향으로 돌린다.
  - 비품실 루트, 바닥, 벽, 출입구, 복도 스텁, 방향 라벨, 바닥 마커 등 비대상 비품실 오브젝트는 보호 스냅샷으로 확인한다.
  - 비품실 밖 기존 씬 루트도 보호 스냅샷으로 확인한다.
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command SwapApprovedSupplyRoomCabinetAndEjectionBay -LogPath .\Logs\ApprovedSupplyRoomSwapCabinetEjection.log -TimeoutSeconds 300`
- 실행하지 않을 작업:
  - 무기실 수정
  - 동력실 수정
  - 운송창고 수정
  - 기존 복도/방/카메라/라이트/UI/매니저 등 모든 기존 오브젝트 수정
  - 비품실 바닥, 벽, 출입구, 복도 스텁, 방향 라벨, 바닥 마커 수정
  - Validate/Test/Build

## SR-01 비품실 캐비닛/사출대 위치 교체 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command SwapApprovedSupplyRoomCabinetAndEjectionBay -LogPath .\Logs\ApprovedSupplyRoomSwapCabinetEjection.log -TimeoutSeconds 300`
- 결과:
  - 두 명령 모두 종료 코드 `0`으로 완료됐다.
  - 비품실 캐비닛 계열 오브젝트와 사출대 계열 오브젝트의 위치를 서로 반대편으로 교체했다.
  - 두 계열 모두 로컬 Y축 180도 회전을 더해 방 내부에서 보이는 방향으로 돌렸다.
  - 비품실 루트, 바닥, 벽, 출입구, 복도 스텁, 방향 라벨, 바닥 마커 등 비대상 비품실 오브젝트는 보호 검사 대상이었다.
  - 비품실 밖 기존 씬 루트도 보호 스냅샷으로 확인했고, 대상 두 계열 외 Transform/activeSelf 변경 없이 완료했다.
- 실행하지 않은 작업:
  - 무기실 수정
  - 동력실 수정
  - 운송창고 수정
  - 기존 복도/방/카메라/라이트/UI/매니저 등 모든 기존 오브젝트 수정
  - 비품실 바닥, 벽, 출입구, 복도 스텁, 방향 라벨, 바닥 마커 수정
  - Validate/Test/Build

## SR-01 비품실 동력실 아래 이동 시작

- 사용자가 비품실 위치를 동력실 아래로 옮기라고 요청했다.
- 수정 범위:
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `docs/PROGRESS_2026-06-23.md`
- 추가한 Unity 브리지 명령:
  - `MoveApprovedSupplyRoomShellBelowEngineRoom`
- 이동 방식:
  - 기존 `Approved Supply Room 01 Shell` 루트 위치만 이동한다.
  - 비품실 내부 자식 모델링, 회전, 스케일, activeSelf는 변경하지 않는다.
  - 동력실 렌더러 경계의 Z 아래에서 시작하고, X/Y는 동력실 루트와 맞춘 뒤 다른 기존 루트와 겹치지 않는 Z 위치를 선택한다.
  - 모든 기존 씬 루트는 보호 스냅샷으로 확인한다.
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command MoveApprovedSupplyRoomShellBelowEngineRoom -LogPath .\Logs\ApprovedSupplyRoomShellMoveBelowEngineRoom.log -TimeoutSeconds 300`
- 실행하지 않을 작업:
  - 무기실 수정
  - 동력실 수정
  - 운송창고 수정
  - 기존 복도/방/카메라/라이트/UI/매니저 등 모든 기존 오브젝트 수정
  - 비품실 내부 자식 모델링 수정
  - Validate/Test/Build

## SR-01 비품실 동력실 아래 이동 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command MoveApprovedSupplyRoomShellBelowEngineRoom -LogPath .\Logs\ApprovedSupplyRoomShellMoveBelowEngineRoom.log -TimeoutSeconds 300`
- 결과:
  - 두 명령 모두 종료 코드 `0`으로 완료됐다.
  - 기존 `Approved Supply Room 01 Shell` 루트 위치만 동력실 아래로 이동했다.
  - 비품실 내부 자식 모델링, 회전, 스케일, activeSelf는 변경하지 않았다.
  - 기존 씬 루트는 보호 스냅샷으로 확인했고, 비품실 루트 외 기존 오브젝트 Transform/activeSelf 변경 없이 완료했다.
- 실행하지 않은 작업:
  - 무기실 수정
  - 동력실 수정
  - 운송창고 수정
  - 기존 복도/방/카메라/라이트/UI/매니저 등 모든 기존 오브젝트 수정
  - 비품실 내부 자식 모델링 수정
  - Validate/Test/Build

## SR-01 비품실 Unity 배치 시작

- 사용자가 참고 이미지형 닫힌 2문 사물함 샘플을 승인했다.
- 사용자 지시:
  - 비품실을 샘플대로 모델링 제작해서 Unity에 배치한다.
  - 비품실은 동력실 Z축 아래에 둔다.
  - 무기실과 동일선상에 맞춘다.
  - 다른 구역과 겹치지 않게 배치한다.
  - 무기실뿐 아니라 모든 다른 기존 오브젝트를 건드리지 않는다.
- 승인 반영:
  - `artSample/supply_room_shell/APPROVAL_STATUS.json`을 승인/Unity 적용 허용 상태로 갱신했다.
  - `artSample/supply_room_shell/ASSET_MANIFEST.json`에 Unity 적용 승인 메모를 추가했다.
- 수정한 파일:
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `artSample/supply_room_shell/APPROVAL_STATUS.json`
  - `artSample/supply_room_shell/ASSET_MANIFEST.json`
  - `docs/PROGRESS_2026-06-23.md`
- 추가한 Unity 브리지 명령:
  - `CreateApprovedSupplyRoomShell`
- 구현 방식:
  - 새 루트 `Approved Supply Room 01 Shell`만 생성/수정한다.
  - 기존 씬 오브젝트는 보호 스냅샷으로 캡처하고 Transform/activeSelf 변경 여부를 확인한다.
  - 비품실 모델은 승인 샘플의 바닥, ㄷ자 벽, 두 출입구, 독립 사물함, 사출대 벽, 사출대 단말기를 Unity 프리미티브로 재구성한다.
  - 배치는 동력실 렌더러 경계의 Z 아래에서 시작하고, X/Y는 무기실 루트와 맞춘 뒤, 다른 기존 루트와 겹치면 Z 아래 방향으로 재시도한다.
- 실행 예정 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CreateApprovedSupplyRoomShell -LogPath .\Logs\ApprovedSupplyRoomShellCreate.log -TimeoutSeconds 300`
- 실행하지 않을 작업:
  - 기존 무기실 수정
  - 동력실 수정
  - 운송창고 수정
  - 기존 복도/방/카메라/라이트/UI/매니저 등 모든 기존 씬 오브젝트 수정
  - `Assets/_Project/Scenes/CargoRunMvp.unity` 직접 편집
  - 기존 오브젝트 재생성/복구/Ensure 계열 명령
  - Validate/Test/Build

## SR-01 비품실 Unity 배치 완료

- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CreateApprovedSupplyRoomShell -LogPath .\Logs\ApprovedSupplyRoomShellCreate.log -TimeoutSeconds 300`
- 결과:
  - 두 명령 모두 종료 코드 `0`으로 완료됐다.
  - Unity 씬에 새 루트 `Approved Supply Room 01 Shell`을 생성했다.
  - 비품실은 승인 샘플 기준으로 바닥, ㄷ자 벽, 두 출입구, 참고 이미지형 닫힌 2문 사물함, 사출대 벽, 사출대 단말기를 포함한다.
  - 배치 로직은 동력실 렌더러 경계의 Z 아래에서 시작하고, 무기실 루트와 X/Y 동일선상에 맞춘 뒤, 기존 루트와 겹치지 않는 Z 위치를 선택했다.
  - 기존 씬 루트는 보호 스냅샷으로 확인했고, 새 비품실 루트 외 기존 오브젝트 Transform/activeSelf 변경 없이 완료했다.
- 실행하지 않은 작업:
  - 기존 무기실 수정
  - 동력실 수정
  - 운송창고 수정
  - 기존 복도/방/카메라/라이트/UI/매니저 등 모든 기존 씬 오브젝트 수정
  - `Assets/_Project/Scenes/CargoRunMvp.unity` 직접 편집
  - 기존 오브젝트 재생성/복구/Ensure 계열 명령
  - Validate/Test/Build

## SR-01 비품창고 좌우 2문 사물함 수정 완료

- 실행 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 결과:
  - 첫 실행은 렌더 시간이 길어 명령 제한 시간에 도달해 완료 상태를 확인하지 못했다.
  - 같은 승인 명령을 더 긴 실행 시간으로 재실행했고 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/supply_room_shell/` 산출물이 좌우 2문 사물함 기준으로 재생성됐다.
- 반영 내용:
  - SR-03 사물함 전면의 3개 슬롯 패널, 슬롯 번호 라벨, 세로 분할선을 제거했다.
  - SR-03 사물함 전면을 좌우 2개 문으로 나눴다.
  - 각 문 바깥쪽에 힌지 레일과 힌지 노브를 추가했다.
  - 중앙에는 양쪽 문 손잡이와 중앙 이음선을 추가했다.
  - 문은 약간 열린 각도로 배치해 양옆으로 열리는 사물함 구조를 드러냈다.
  - 문서와 미리보기 설명도 `기본 3칸 위치`에서 `좌우 2문 구조`로 정정했다.
- 실행하지 않은 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 읽기/수정
  - 실제 비품실/무기실/운송창고 오브젝트 수정
  - 사출대 로직, 보관 로직, UI 구현
  - Validate/Test/Build

## SR-01 비품창고 참고 이미지형 닫힌 2문 사물함 수정 시작

- 사용자가 참고 이미지와 같은 모양으로 비품창고 사물함을 다시 만들라고 요청했다.
- 참고 이미지 기준:
  - 좌우 2문은 열린 형태가 아니라 정면에서 닫힌 금속 캐비닛 형태다.
  - 중앙 세로 이음선이 있다.
  - 각 문 상단에는 작은 가로형 명패 손잡이가 있다.
  - 문 중앙 하단 쪽에는 검은 세로형 매립 손잡이와 잠금판이 있다.
  - 바깥쪽 좌우에는 힌지가 보인다.
  - 문 모서리에는 삼각형 타공/환기 구멍 패턴이 있다.
- 수정 범위:
  - `scripts/GenerateSupplyRoomShellSample.py`
  - `docs/PROGRESS_2026-06-23.md`
  - `artSample/supply_room_shell/` 샘플 산출물 재생성
- 실행 예정 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 실행하지 않을 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 읽기/수정
  - 실제 비품실/무기실/운송창고 오브젝트 수정
  - 사출대 로직, 보관 로직, UI 구현
  - Validate/Test/Build

## SR-01 비품실 룸 셸 샘플 생성 완료

- 실행 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/supply_room_shell/` 아래에 비품실 룸 셸 승인용 샘플을 생성했다.
- 생성된 주요 확인 파일:
  - `artSample/supply_room_shell/index.html`
  - `artSample/supply_room_shell/renders/01_overview.png`
  - `artSample/supply_room_shell/renders/02_floor_plan.png`
  - `artSample/supply_room_shell/renders/03_supply_storage_wall.png`
  - `artSample/supply_room_shell/renders/04_ejection_wall.png`
  - `artSample/supply_room_shell/renders/05_corridor_entries.png`
  - `artSample/supply_room_shell/renders/06_room_shell_markers.png`
- 실행하지 않은 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 읽기/수정
  - 실제 비품실/무기실/운송창고 오브젝트 수정
  - 사출대 로직, 보관 로직, UI 구현
  - Validate/Test/Build

## SR-01 비품창고 좌우 2문 사물함 수정 시작

- 사용자가 비품창고 사물함 전면 해석을 정정했다.
  - 독립 오브젝트로 분리한 것은 맞다.
  - 전면을 3개 슬롯으로 명확히 보이게 만드는 형태가 아니다.
  - 문이 2개로 나뉘고 양옆으로 문이 열리는 사물함 형태여야 한다.
- 수정 범위:
  - `scripts/GenerateSupplyRoomShellSample.py`
  - `docs/PROGRESS_2026-06-23.md`
  - `artSample/supply_room_shell/` 샘플 산출물 재생성
- 수정 내용:
  - SR-03 사물함 전면의 3개 슬롯 패널, 슬롯 번호 라벨, 세로 분할선을 제거한다.
  - SR-03 사물함 전면을 좌우 2개 문으로 나눈다.
  - 문 바깥쪽에 힌지와 힌지 노브를 추가한다.
  - 중앙에는 양쪽 문 손잡이와 중앙 이음선을 둔다.
  - 양쪽 문은 살짝 열린 각도로 보여 양옆으로 열리는 구조를 드러낸다.
  - 기본 3칸 보관 기준은 외형 슬롯으로 드러내지 않고 후속 UI/데이터 기준으로 남긴다.
- 실행 예정 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 실행하지 않을 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 읽기/수정
  - 실제 비품실/무기실/운송창고 오브젝트 수정
  - 사출대 로직, 보관 로직, UI 구현
  - Validate/Test/Build

## SR-01 비품창고 사물함화 및 사출대 단말기 가시성 수정 시작

- 사용자가 비품창고 해석을 정정했다.
  - 비품창고는 벽에 붙은 패널이 아니라 사물함처럼 별도 오브젝트로 만들어 벽 앞에 배치해야 한다.
  - 사출대 단말기가 샘플에서 보이지 않으므로 보이게 수정해야 한다.
- 수정 범위:
  - `scripts/GenerateSupplyRoomShellSample.py`
  - `docs/PROGRESS_2026-06-23.md`
  - `artSample/supply_room_shell/` 샘플 산출물 재생성
- 수정 내용:
  - SR-02 비품창고를 벽 앞 독립 사물함형 오브젝트로 변경한다.
  - SR-03 기본 3칸 보관 슬롯을 사물함 전면에 보이도록 변경한다.
  - SR-07 사출대 단말기를 더 앞으로 빼고 크기를 키워 렌더에서 보이도록 조정한다.
  - 사출대 렌더 카메라 구도를 단말기가 함께 보이도록 조정한다.
- 실행 예정 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 실행하지 않을 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 읽기/수정
  - 실제 비품실/무기실/운송창고 오브젝트 수정
  - 사출대 로직, 보관 로직, UI 구현
  - Validate/Test/Build

## SR-01 비품창고 사물함화 및 사출대 단말기 가시성 수정 완료

- 실행 명령:
  - `.\scripts\Run-SupplyRoomShellSample.ps1`
- 결과:
  - 종료 코드 `0`으로 완료됐다.
  - 실행 래퍼의 필수 산출물 존재 확인이 통과했다.
  - `artSample/supply_room_shell/` 산출물이 수정 기준으로 재생성됐다.
- 반영 내용:
  - SR-02 비품창고를 벽면 패널이 아니라 벽 앞 독립 사물함형 오브젝트로 바꿨다.
  - SR-03 기본 3칸 보관 슬롯을 사물함 전면에 보이도록 바꿨다.
  - SR-07 사출대 단말기를 더 앞으로 빼고 크기를 키워 렌더에서 보이게 수정했다.
  - `04_ejection_wall.png` 구도에서 사출대 벽과 오른쪽 단말기가 함께 보이도록 카메라를 조정했다.
- 실행하지 않은 작업:
  - Unity 실행
  - Unity Refresh
  - Unity 브리지 명령
  - `Assets/` 하위 파일 읽기/수정
  - 실제 비품실/무기실/운송창고 오브젝트 수정
  - 사출대 로직, 보관 로직, UI 구현
  - Validate/Test/Build

## 비품실 복구 및 전체 오브젝트 복구 백업 기록

- 사용자 지시에 따라 git 기준으로 기존 조종실/동력실/통제실/무기실이 포함된 씬 상태를 먼저 복원한 뒤, 비품실을 다시 끼워 넣는 순서로 복구했다.
- `artSample/supply_room_shell/APPROVAL_STATUS.json`의 깨진 승인 상태 때문에 `CreateApprovedSupplyRoomShell`이 실패했으므로, 사용자가 승인한 비품실 샘플 기준으로 승인 상태를 복구했다.
- `Approved Supply Room 01 Shell`을 생성한 뒤, 기존 순서 기반 `RestoreApprovedSupplyRoomShellCurrentState`가 hierarchy mismatch로 실패하는 문제를 확인했다.
- 비품실 스냅샷 적용을 위해 `RestoreApprovedSupplyRoomShellCurrentStateByName` 복구 명령을 추가했다.
  - 대상 루트는 `Approved Supply Room 01 Shell`로 제한했다.
  - 스냅샷에 포함된 `SR-08`, `SR-11`, `SR-12`가 없을 경우 비품실 내부에서만 재생성한 뒤 이름 기준으로 transform/active 값을 적용하도록 했다.
  - 적용 결과 `TransformStates=161`, `Applied=161` 로그를 확인했다.
- 사용자 지시에 따라 비품실 전체 오브젝트 상태를 다시 캡처했다.
  - 실행 명령: `CaptureApprovedSupplyRoomShellCurrentState`
  - 결과: `TransformStates=160`
  - 출력: `Assets/_Project/Editor/Validation/ApprovedSupplyRoomShellCurrentState.cs`
- 비품실 복구 직후 상태 백업을 생성했다.
  - 경로: `Backups/supply_room/`
  - 포함: `CargoRunMvp.unity`, `ApprovedSupplyRoomShellCurrentState.cs`, `.meta`, 캡처 로그
- 전체 오브젝트 복구용 백업을 생성했다.
  - 경로: `Backups/full_object_recovery/2026-06-23_232803-current-state/`
  - 포함: `CargoRunMvp.unity`, `ApprovedSupplyRoomShellCurrentState.cs`, `ApprovedArmoryShellCurrentState.cs`, `ApprovedArmoryShellBootstrap.cs`, `UnityEditorValidationBridge.cs`, `README.md`
- 추가된 작업 규칙:
  - 응답 전 현재 적용 규칙 확인을 절대 규칙으로 추가했다.
  - 규칙 위반 시 해당 위반 작업을 파기하고 사용자 승인 기존 상태로 되돌리는 규칙을 추가했다.
- 이 기록 작업 중에는 Unity 실행, Unity 브리지, Ensure, Restore, 테스트, 검증, 빌드를 실행하지 않았다.