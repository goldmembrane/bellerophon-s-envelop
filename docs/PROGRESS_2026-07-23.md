# PROGRESS 2026-07-23

## 통합 기록 - 돌로레 4번 촉수 공격

- 대상은 `Approved Dolore Enemy Placement/Dolore_04_Tentacle_Stab_Attack`입니다. 기존 모델링의 13본 촉수 리그를 사용하며 고정 시작점 본은 `Bone_000`, `Bone_012`, `Bone_011`, `Bone_010`, 실제 가동 본은 `Bone_009`부터 `Bone_001`까지 9개입니다.
- 승인된 순서는 `고리 0.5초 생성 -> 고리에서 촉수 끝 출현 -> 액자 앞 비스듬한 출구 -> 1.5초 상승 -> 0.5초 1차 전방 내려찍기 -> 0.8초 준비 복귀 -> 0.5초 2차 내려찍기 -> 0.8초 복귀 중 숨김 -> 고리 생성부터 반복`입니다. 별도 회수 또는 액자 오른쪽 아래로 사라지는 모션은 없습니다.
- 촉수는 고정된 고리를 통과한 뒤 액자 바깥쪽으로 먼저 빠져나와 액자와 겹치지 않은 상태로 상승합니다. 공격 시 뾰족한 끝이 목표를 향하도록 힘 모음과 가속 구간을 분리했습니다.
- 최종 검사값은 `IntroSeconds=2`, `SourceGenerationSeconds=0.5`, `RiseSeconds=1.5`, `StrikeSeconds=0.5`, `RecoverSeconds=0.8`, `AttackLoopSeconds=2.6`입니다. 고리 표면 이동은 `0.000000119m`, 촉수 끝 상승은 `1.040436m`, 공격 전방 이동은 `1.09588027m`, 낙차는 `1.44445491m`, 측면 이동은 `0.000005325m`, 본 루트 이동은 `0`입니다.
- 적용 자산은 `Dolore_04_TentacleStab_Intro.anim`, `Dolore_04_TentacleStab_AttackLoop.anim`, `Dolore_04_TentacleStab.controller`이며 검사 보고서는 `Dolore_04_TentacleStab_Inspection.txt`입니다.

## 통합 기록 - 돌로레 5번 처형과 수송자 회수

- 대상은 `Approved Dolore Enemy Placement/Dolore_05_Execution_Pull_In`입니다. 4번 공격의 고리 생성·촉수 출현·상승·첫 전방 찌르기까지 이어받은 뒤 수송자를 관통해 액자 안으로 회수합니다.
- `transfer.fbx`와 `transfer lying.fbx`는 원본 비율을 유지한 공통 균일 배율 `0.2965097`로 사용합니다. 세운 자세와 누운 자세 모두 액자 너비·높이의 `80%` 범위에 들어가며, 원본 FBX 바이트는 변경하지 않았습니다.
- 상태 순서는 `Intro -> PierceHold -> PullIn -> Intro`입니다. 관통 직후 누운 모델로 교체한 뒤 정확히 `2초` 동안 끌어오고, 액자 평면을 통과한 픽셀은 `DoloreExecutionPortalClip.shader`로 즉시 가립니다. 수송자 크기는 회수 중 변하지 않으며 종료 후 세운 자세와 고리 생성으로 복귀합니다.
- 회수 시작 시 수송자는 X축 `60도`로 기울고 액자 진입 시 `0도`가 됩니다. 상체는 리그를 사용해 약 `40도` 세웠으며 팔은 시선 방향으로 뻗어 좌우 `5도`, 다리는 좌우 약 `7도` 반대 위상으로 흔듭니다. 동작은 관통 직후부터 액자 진입까지 반복됩니다.
- 촉수는 뾰족한 끝의 길이를 늘리지 않고 뾰족한 밑부분 아래가 가슴을 관통하도록 정렬했습니다. 회수 20개 표본의 촉수 끝 추종 최대 오차는 `0.00001079m`, 촉수선 방향 최대 오차는 `0.05233916도`이며, 보이는 11개 표본 모두 몸통 뒤에서 진입해 뾰족한 끝 전체가 몸통 앞에 남습니다.
- 적용 자산은 `Dolore_05_ExecutionPullIn.controller`, 전용 `IntroSlot`, `PierceHoldSlot`, `PullInSlot` 클립, 두 수송자 FBX, 클리핑 셰이더와 전용 머티리얼입니다. 최종 검사 보고서는 `Dolore_05_ExecutionPullIn_Loop_Inspection.txt`입니다.

## 완료 - 돌로레 6번 피격 반동, 머리와 액자의 왼쪽 회전

- 대상은 `Approved Dolore Enemy Placement/Dolore_06_Hit_Reaction`입니다. 전체 재생시간은 `2초`이며, 뒤로 움찔한 뒤 원래 자세로 복귀하는 동작을 반복 재생합니다.
- 리그의 중심 계층 `Hips -> chest -> head`를 사용했습니다. 모델 루트는 움직이지 않고 `chest` 본을 로컬 뒤쪽으로 `0.10m` 이동시키며 `8도` 젖힙니다. 실제 합동 액자 중심의 최대 후방 이동은 `0.1389046m`입니다.
- 피격 최대 지점에서 액자 전체가 돌로레 로컬 왼쪽으로 `14도` 회전합니다. 머리는 같은 방향으로 `25도` 돌아가므로, 액자는 머리보다 작은 각도로 함께 반응합니다.
- `head` 본은 액자와 초상 정점에도 가중치가 있어 직접 돌리지 않았습니다. 대신 `head` 스킨 가중치를 머리 회전 마스크로 사용한 BlendShape `Dolore_Hit_HeadLeft`를 생체 조직 서브메시에만 적용했습니다. 생체 조직 영향 정점은 `835개`, 액자·초상 이동 정점은 `0개`입니다.
- 적용 자산은 `Dolore_06_Hit_Reaction.anim`, `Dolore_06_Hit_Reaction.controller`, `Dolore_06_HitReactionMesh.asset`이며 6번 피격 모션 개체의 Animator에 연결되어 있습니다. 키 시점은 `0, 0.12, 0.32, 0.65, 1.1, 1.55, 2초`입니다.
- 저장 상태 검사는 `Result=PASS`, `DurationSeconds=2`, `LoopEnabled=True`, `BackwardRecoilMeters=0.1389046`, `FrameSignedYawDegrees=-14.00001`, `HeadTurnDegrees=25`, `ImpactBlendShapeWeight=100`, `LoopBoundaryError=0`, `SceneChanged=False`로 통과했습니다.
- 실제 Animator 복제본 측정도 `ActualAnimatorCloneBackwardRecoil=0.1389013`, `ActualAnimatorCloneFrameSignedYawDegrees=-14.00001`로 클립 직접 샘플과 일치했습니다. 슬롯 루트 애니메이션 곡선, 다른 Dolore 슬롯 변경, 원본 FBX 변경은 없습니다.
- 최종 진단 캡처는 실제 복제본 Animator의 `HitReaction` 상태를 재생해 정면 3장과 사선 3장을 한 번 생성했습니다. 정지, 피격 최대, 복구 자세가 서로 구분되며 피격 순간 액자와 머리의 왼쪽 회전이 확인됩니다.
- Unity에 실행한 명령은 `Refresh-UnityProject.ps1`, `ApplyDolore06HitReaction`, `InspectDolore06HitReaction`, 최종 `CaptureDolore06HitReactionDiagnostic` 1회입니다. `Run-HarnessValidation.ps1` 및 Harness 관련 실행·재실행·조사·로그·문서·생성물 작업, EditMode/PlayMode 테스트, 빌드, Unity 재시작, 원본 FBX 수정, 다른 Dolore 모션 변경, Git 변경 작업은 실행하지 않았습니다.

## 완료 - 돌로레 7번 사망 모션

- 대상은 `Approved Dolore Enemy Placement/Dolore_07_Death`입니다. `Hips`를 돌로레 로컬 왼쪽으로 0.9초 동안 90도 회전시키고, 베이크된 스킨 바운드 기준으로 바닥 높이를 보정했습니다.
- 전체 루프는 `0.0~0.9초 기존 초상 유지 및 왼쪽 낙하 -> 0.9~1.9초 TV 백색 노이즈 -> 1.9~2.9초 완전한 검정 -> 2.9초 시작 자세로 즉시 복귀`입니다. 0.9초 이후 위치·회전 유지 오차는 모두 0입니다.
- 적용 자산은 `Dolore_07_Death.anim`, `Dolore_07_Death.controller`, `Dolore_07_Death_PortraitFill.mat`, `DoloreDeathPortraitSignal.shader`, `Dolore_DeathPortraitSignalFill.asset`입니다. 원본 렌더러의 재질은 기준 Dolore 개체와 동일하게 유지하고, 신호는 `head` 본 자식인 액자 내부 중앙 초상 면에만 적용했습니다. 액자 틀과 몸통까지 신호가 번지던 전체 스킨 복제 오버레이 `Dolore_DeathPortraitSignalOverlay`, `Dolore_07_Death_Portrait.mat`, `Dolore_07_Death_PortraitSignalMesh.asset`은 제거했습니다.
- 저장 후 검사값은 `Result=PASS`, `DurationSeconds=2.9`, `FallDurationSeconds=0.9`, `WhiteNoiseDurationSeconds=1`, `BlackDurationSeconds=1`, `FallDirection=ScreenLeft`, `LeftDisplacementMeters=0.3395538`, `FallAngleDegrees=90`, `GroundContactErrorMeters=0.04026675`, `HoldPositionErrorMeters=0`, `HoldRotationErrorDegrees=0`입니다.
- 실제 Animator 복제본에서도 초상 단계 `0`, 백색 노이즈 단계 `1`, 검정 단계 `2`가 확인됐고, 다른 Dolore 슬롯·슬롯 루트 곡선·원본 FBX 변경은 없습니다. 검사 보고서는 `docs/validation/dolore_death_2026-07-23/Dolore_07_Death_Inspection.txt`입니다.
- 사용자 런타임 영상에서 액자 틀과 몸통 일부가 함께 백색·검정으로 바뀌는 문제를 확인한 뒤 구조를 수정했습니다. 수정 후 최종 캡처 12장에서 시작 외형과 낙하 중 기존 초상 유지, 완전 낙하 후 백색 노이즈, 검정 화면을 확인했습니다. 정면과 사선 모두 중앙 신호 면만 전환되고 외곽 프레임·청록 몸체는 원래 재질을 유지합니다. 캡처 보고서는 `Dolore_07_Death_Capture.txt`, 최종 이미지는 `docs/validation/dolore_death_2026-07-23/Dolore_07_Death_Final`에 있습니다.
- 두 번째 런타임 영상에서 신호 화면과 정상 초상 개구부의 크기 불일치를 확인했습니다. 신호 마스크를 현재 Animator 재생 위치가 아닌 `Dolore_01_Static_Review`의 정적 기준 자세에서 생성하도록 바꾸고, 낙하 후 화면 축을 기준으로 초상 개구부와 같은 크기·중심이 되도록 가로 반폭 `초상 바운드의 71%`, 세로 반폭 `62%`, 세로 중심 오프셋 `-20%`를 적용했습니다. 검토 캡처는 기본 스킨 메시의 베이크 바운드에 카메라를 고정하고 동일한 `1.05초` 자세에서 정상 초상과 신호 화면을 비교하도록 수정했습니다.
- 크기 보정 후 최종 캡처 12장에서 정상 초상, 백색 노이즈, 검은 화면이 같은 액자 내부 경계를 사용하며 액자 틀과 몸통을 침범하지 않는 것을 정면·사선에서 확인했습니다.
- 일반 테스트·빌드, Unity 재시작, 원본 FBX 수정, 다른 Dolore 모션 변경, Git 작업은 실행하지 않았습니다.

## 완료 - 니게티프 Unity 정적 배치 및 애니메이션 슬롯 준비

- 사용자 지정 원본 `enemies model/négatif.fbx`를 `Assets/_Project/Art/Enemies/Negatif/Models/Negatif.fbx`로 복사해 임포트했습니다. 원본과 임포트본의 SHA-256은 `90AC1346B7C7E71E3A605667C4858816B9F2BD2C7F164721FDF88A544C75AC25`로 일치하며 원본 FBX는 수정하지 않았습니다.
- `Approved Negatif Enemy Placement` 아래에 정적 검토, 대기, 이동, 앞발 할퀴기, 피격, 도주, 사망용 슬롯을 각각 1개씩 총 7개 생성했습니다. 모든 슬롯은 원본 FBX의 직접 인스턴스이며 애니메이션은 아직 적용하지 않았습니다.
- 니게티프 루트 위치는 `(57.86535, 0, -126.498)`입니다. 돌로레의 Z 위치에서 롱가 아르마와 테르고 사이 Z 간격 `7.376999m`만큼 -Z 방향으로 배치했으며, 돌로레 행과 같은 X 슬롯 간격 `2.444763m`로 나열했습니다.
- 원본 기획서의 높이 약 `0.5m`에 맞춰 균일 배율을 적용하고 바닥에 정렬했습니다. 전체 7개체 바운드는 `(15.27916, 0.5, 0.779678)`이며 사용 머티리얼은 원본 FBX의 `Material_1`입니다.
- 첫 카메라 확인에서 FBX의 기본 전방 축이 기존 개체와 달라 측면이 보이는 것을 확인했습니다. 이후 `tailstart -> head` 리그 축을 기준으로 각 슬롯을 자동 정렬해 최종 시각 전방을 `(0.00061, 0, -1)`로 맞췄습니다.
- 실행 시작용 `Player` 위치는 `(65.186, 0, -135.4847)`로 이동했습니다. 저장 상태 검사에서 `PlayerFacesNegatif=True`, `AllNegatifVisible=True`, `DirectFbxInstances=7`, `OtherSceneRootsUnchanged=True`를 확인했습니다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyNegatifPlacement`, `InspectNegatifPlacement`, `CaptureNegatifPlayerStartView`입니다. 마지막 전방 축 수정 후 `ApplyNegatifPlacement`와 `InspectNegatifPlacement`를 다시 실행했습니다.
- Harness/EditMode/PlayMode 테스트, 빌드, Smoke/Validate/Ensure 계열 명령, Unity 재시작, 니게티프 애니메이션·AI·전투 기능 구현, 다른 적대 개체 수정, Git 커밋·푸시는 실행하지 않았습니다.

## 완료 - 니게티프 승인 샘플 및 Unity 외형 동기화

- 기준 이미지는 `image/négatif(네거티프).png`, 원본 모델은 `enemies model/négatif.fbx`입니다. `artSample/enemies/negatif/appearance_reference_sync`에 Unity 반영 전 승인용 샘플을 생성했으며 현재 상태는 `PENDING_USER_APPROVAL`입니다.
- 원본 저폴리 형상 위에 마모된 회갈색 금속, 어두운 기계 관절·다리·꼬리, 먼지 낀 베이지 캔버스 적재부, 갈색 가죽 스트랩, 구리색 코 포인트와 작은 주황 발광 눈의 색·거칠기·요철 분포를 구성했습니다. 새 메시, 장식, 눈 데칼은 추가하지 않았습니다.
- 원본 FBX SHA-256과 샘플 내 원본 복사본 SHA-256은 모두 `90AC1346B7C7E71E3A605667C4858816B9F2BD2C7F164721FDF88A544C75AC25`입니다. 정점 `3218`, 면 `6330`, 본 `27`과 형상 서명 `3A4A2B2AB23CD75FBBBBEA77003C731F093FD17D57A9EC6BC94703A0ED4ADC4F`가 전후 일치해 `GEOMETRY_VALIDATION.json`의 결과는 `PASS`입니다.
- 샘플 복제본에만 통합 머티리얼, `Negatif_MaterialUV`, `Negatif_MasksA`, `Negatif_MasksB`를 추가했습니다. Armature Modifier, 리그, 스킨 가중치는 보존하되 Unity의 애니메이션 비활성 정적 검토와 같은 바인드 메시를 보여주기 위해 프레젠테이션 렌더에서 변형 평가를 끄고, Blender에서 수입 분할 노멀이 깨져 보이는 문제를 피하려고 프레젠테이션용 플랫 셰이딩을 사용했습니다.
- 주요 검토 산출물은 `renders/05_reference_comparison.png`, 4방향 렌더, `renders/06_material_texture_breakdown.png`, `blender/Negatif_Appearance_ReferenceSync.blend`, `index.html`입니다. GLB도 보조 산출물로 생성했지만 사용자 정의 색상 마스크 내보내기 경고가 있어 재질 검토 기준은 Blender 파일과 렌더입니다.
- 사용자 검토에서 가방 끈의 표현이 약하고 눈이 볼에 위치한 문제를 확인해 수정했습니다. 이후 하단 끈이 끈처럼 보이지 않는 문제를 다시 확인해 원본 메시의 실제 융기 중심을 `-0.060 / -0.009 / 0.055`로 측정하고, 반폭 `0.013`, 전이 폭 `0.006`의 마스크를 윗면에서 측면과 아래 경계까지 연장했습니다. 추가 진단에서 하단 띠 영역의 다리 본 가중치가 평균 `0.844`라 기존 제외 조건이 가죽 마스크를 평균 `0.156`까지 지우는 것을 확인했습니다. 원본 스킨 가중치는 유지하고 외측 가방 면에만 재질 분류 우회를 적용해 세 띠를 짙은 적갈색 무광 가죽으로 연결했으며, 돌출 높이와 거칠기를 강화했습니다. 원본 토폴로지 기준 최종 가죽 마스크 코너 수는 `3982`입니다.
- 사용자 요청에 따라 몸통의 추가 장갑판·원형 관절·환기구 메시 변형은 취소했습니다. 샘플은 다시 원본과 동일한 정점 `3218`, 면 `6330`, 본 `27` 및 형상 서명을 유지하며, 기계 느낌은 전용 `armor` 절차적 텍스처의 패널 홈·리벳형 요철·가장자리 마모와 금속성·거칠기 차이로만 표현합니다.
- 눈은 좌표 기반 코너 보간을 제거하고 머리 측면 위쪽의 외향 소형 폴리곤을 좌우 2개씩 총 4개만 선택했습니다. 발광 코너 수는 `12`이며 큰 볼·눈썹 삼각면으로 번지지 않고 정면에서도 좌우 대칭입니다. 새 눈 메시나 데칼은 추가하지 않았습니다.
- 사용자가 메시 변형을 취소한 최종 샘플을 승인했습니다. `APPROVAL_STATUS.json`은 `APPROVED`, `approved_for_unity=true`, `modeling_modified=false`로 기록했습니다.
- 승인 샘플의 원본 토폴로지와 `Negatif_MaterialUV`, 표면 분류를 6개 재질 슬롯으로 고정한 `Negatif_ApprovedAppearance.fbx`를 Unity용 파생 모델로 내보냈습니다. Blender 내보내기 기준 정점 `3218`, 면 `6330`, 본 `27`이며 승인 샘플에 없던 형상이나 부품은 추가하지 않았습니다.
- 승인 샘플의 알베도·거칠기·높이 텍스처 18개를 `Assets/_Project/Art/Enemies/Negatif/Textures`로 그대로 복사했습니다. 샘플과 Unity 복사본의 SHA-256은 18개 모두 일치합니다. 샘플의 금속성·요철 강도·눈 발광 값을 읽는 `Bellerophon/Negatif/ApprovedAppearance` URP 셰이더와 재질 6개를 생성했습니다.
- `ApplyNegatifApprovedAppearance`로 `Approved Negatif Enemy Placement` 아래 7개 슬롯의 `Negatif_Model`만 승인 파생 FBX로 교체했습니다. 루트·슬롯 Transform, Player, 다른 씬 루트는 유지했고 `CargoRunMvp.unity`를 저장했습니다.
- 적용 및 독립 검사 결과는 각각 `Result=PASS`입니다. 모든 7개체가 승인 모델과 6개 승인 재질을 사용하며 모델당 면 `6330`, 본 `27`, 높이 `0.5m`입니다. 배치 위치는 `(57.86535, 0, -126.498)`이고 `RootAndSlotTransformsUnchanged=True`, `OtherSceneRootsUnchanged=True`, `SourceHashPreserved=True`입니다.
- 최종 Unity 캡처는 `Logs/Negatif_ApprovedAppearance_Unity.png`입니다. 현재 씬 조명에서는 승인 샘플의 스튜디오 렌더보다 어둡지만, 어두운 기계부·갈색 화물부·가죽 스트랩·주황 눈의 승인 색 분포가 적용된 것을 확인했습니다.
- 실행한 Unity 명령은 `Refresh-UnityProject.ps1`, `ApplyNegatifApprovedAppearance`, `InspectNegatifApprovedAppearance`, `CaptureNegatifApprovedAppearance`입니다. Harness/EditMode/PlayMode 테스트, 빌드, Smoke/Validate/Ensure 계열 명령, Unity 재시작, 애니메이션·AI·전투 구현, Git 커밋·푸시는 실행하지 않았습니다.

## 승인 대기 - 니게티프 GLB 외형 동기화 샘플

- 사용자 지정 새 원본은 `enemies model/négatif.glb`이며 SHA-256은 `BD27F171F84E212273C841A2BAE44832519CEA0F0201C0D3FBD5707F870CE8E8`입니다. 기존 FBX와 구조가 달라 Unity 교체를 중단한 뒤 별도 샘플 승인을 받는 절차로 전환했습니다.
- `artSample/enemies/negatif/glb_appearance_sync`에 GLB 전용 샘플을 생성했습니다. 주 메시 `17771`정점·`6397`면, 보조 `Icosphere` `42`정점·`80`면, 리그 `46본`과 각 형상 서명이 전후 일치합니다. 원본 GLB 형상은 바꾸지 않았고, 아래 눈 파생 메시 추가를 포함한 샘플 상태는 `modeling_changed=true`, `source_geometry_changed=false`입니다.
- 새 GLB에 없던 UV만 추가하고 기존 승인 샘플의 텍스처 18개와 6개 재질을 대응시켰습니다. 가방끈은 실제 화물 융기 중심 `0.04 / 0.50 / 0.76`을 따라 좁고 연속된 세 띠로 다시 분류하고, 가죽에만 적갈색 명암과 요철 대비를 강화했습니다. 양쪽 눈 추가 후 최종 재질 면 수는 어두운 기계부 `2286`, 회갈색 금속 `2485`, 캔버스 `726`, 가죽 스트랩 `863`, 구리 `401`, 주황 눈 `400`입니다.
- 기존 사각 발광 면은 원래 금속 재질로 복구했습니다. 처음 좌우 눈을 어깨 관절에 가까운 `Y=-0.927139`에 배치한 문제를 기준 이미지와 대조해 수정하고, 약 `0.50m` 앞쪽인 코 뒤·귀 앞의 얼굴 판 중심 `(+/-0.262566, -1.427052, 1.211955)`으로 이동했습니다. 두 눈 모두 얼굴 지배 본 `Bone_025`에 결합했고, 각 눈은 `342`정점·`342`면입니다. 렌즈 형태·크기와 실제 재질 발광 강도 `9.0`은 유지했습니다.
- Blender 샘플에서 두 눈의 대칭 오차는 X 합 `0`, Y 차 `0.00000012`, Z 차 `0.00000012`입니다. 내보낸 GLB를 빈 씬에 재수입해도 얼굴 위치의 눈 개체 두 개와 리그 46본이 유지됩니다.
- 기준 이미지와 양쪽 눈 형태·위치를 대조하는 최종 비교 이미지는 `renders/06_reference_eye_comparison.png`, 기존 승인 외형과의 비교 이미지는 `renders/05_approved_sample_comparison.png`입니다. Unity에는 아직 반영하지 않았습니다.
