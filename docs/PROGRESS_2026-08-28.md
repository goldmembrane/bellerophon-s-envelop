# 진행 기록 2026-08-28

## 수송자 앉은 대각선 Blend Tree와 일어서기 Mixamo 반복

- `Player_Crouch_Diagonal`은 기존 승인 결과인 `Player_Crouch_Forward_Mixamo_InPlace.anim`과 `Player_Crouch_Sidestep_Mixamo_InPlace.anim`을 각각 `50%`로 사용하는 1D Blend Tree로 구성했다. 현재 기본값은 오른쪽 전방 대각선이며 두 입력 클립은 수정하지 않았다.
- 실제 Unity Play Mode의 같은 정면·측면 전신 구도에서 전진 원본, 오른쪽 옆걸음 원본과 50:50 결과를 위상별로 직접 비교했다. 대각선 결과에는 전진의 앞뒤 보폭과 옆걸음의 좌우 스텝이 함께 나타났고, 두 루프 동안 개체 중심은 제자리에 남았다. 메시 늘어짐, 관절 파손과 개체 소실은 보이지 않았다.
- `Player_Crouch_Exit`은 사용자 지정 원본 `player model/transfer crouched to stand.fbx`를 수정하지 않고 `Assets/_Project/Art/Player/Animations/Player_Crouch_Exit_Mixamo.fbx`로 복사했다. 두 파일의 SHA-256은 `0A8AA9A38A0A85135A05847A9725E82AF5D9E59BF8742FC27F13CF7796D888D7`로 일치한다.
- Unity에서 노출된 단일 내장 Take `mixamo.com`은 30fps, `0.6333333초`, float curve 60개다. `Player_Crouch_Exit_Mixamo_Hold.anim`은 이 원본 구간의 곡선 값, 속도, 프레임과 이벤트를 그대로 유지하고 마지막 완료 자세만 `0.5초` 연장해 총 `1.1333333초` 반복 클립으로 만들었다. 리타기팅, 미러링, 자세 보정과 원본 루트 곡선 수정은 하지 않았다.
- 같은 Play Mode 구도에서 Exit 원본과 적용 결과를 정면·측면의 같은 위상으로 직접 비교했다. 일어나기 원본 구간은 서로 동일했고 마지막 두 유지 표본도 같은 완료 자세였으며, 두 루프에서 반복과 처음 자세 복귀를 확인했다.
- 2순위 보조 확인에서 대각선은 `43프레임×2루프`, Blend 값 `0.5`, 루트 변위 `0m`였다. Exit는 `34프레임×2루프`, 유지 구간의 전 본 위치·회전 최대 차이 `0`, 루트 변위 `0m`였다. 두 상태는 `Loop=True`, `ApplyRootMotion=False`, `AlwaysAnimate`다.
- 입력 두 제자리 클립의 적용 전후 SHA-256은 각각 동일했다. 두 대상 루트 Transform과 다른 플레이어 Animator도 변경되지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerCrouchDiagonalAndExit`, `CapturePlayerCrouchDiagonalAndExitReview`, `CapturePlayerCrouchDiagonalAndExitFinal`을 사용했다.
- 적용·검토 자료는 `docs/validation/player_crouch_diagonal_exit_2026-08-28/player_crouch_diagonal_exit_apply_metrics.json`, `player_crouch_diagonal_exit_review_metrics.json`, 두 `*_review_contact_sheet.png`에 저장했다. 직접 확인과 보조 검증을 통과한 뒤 두 `*_final.png`를 대상별 한 번만 생성했다.
- `docs/PLAYER_ANIMATION_REQUIREMENTS.html`의 완료 표시는 승인 범위 밖이어서 변경하지 않았다. Harness, EditMode 테스트, PlayMode 테스트, Windows Build와 Git 작업은 실행하지 않았다.

## Player_Crouch_Exit의 Player_Idle 첫 프레임 도착 보정

- 실제 Unity Play Mode의 같은 정면·측면 전신 구도에서 Exit 원본, 보정 결과, `Player_Idle` 첫 프레임을 동작 시작부터 유지 구간까지 나란히 직접 확인했다. 보정 결과는 원본의 낮은 시작 자세에서 출발해 머리·척추·골반·팔·다리가 프레임마다 연속적으로 Idle 첫 프레임 자세로 수렴했고, 마지막 동작 프레임과 두 유지 표본은 Idle 기준 자세와 일치했다. 관절 꺾임, 메시 파손과 순간이동은 보이지 않았다.
- `Player_Crouch_Exit_Mixamo_Hold.anim`은 원본 Take `mixamo.com`을 30fps 기준 20개 시점에서 그대로 샘플링한 뒤, 동작 시작의 원본 가중치 `100%`에서 종료의 Idle 첫 프레임 가중치 `100%`까지 Smoothstep으로 전환했다. 25개 Armature Transform의 위치는 선형 보간하고 회전은 Quaternion 보간했으며, 원본 종료 시각 `0.6333333초`에 Idle 첫 프레임에 도달한 뒤 `0.5초` 동안 그대로 유지해 총 `1.1333333초`로 반복한다.
- 원본 FBX와 Unity 복사본의 SHA-256은 `0A8AA9A38A0A85135A05847A9725E82AF5D9E59BF8742FC27F13CF7796D888D7`로 계속 일치했고, `Player_Idle.anim`의 적용 전후 SHA-256도 `F835EA47600940846039E4BD323D3D6FEC6B05C676E9DEBB9293344B839AA853`로 동일하다. 두 입력 에셋은 수정하지 않았고 `Player_Crouch_Diagonal`과 다른 Animator도 변경하지 않았다.
- 2순위 보조 확인에서 원본 시작 위치 오차는 약 `0.000000321m`, 회전 오차는 `0°`였고, Idle 도착 위치·회전 오차와 `0.5초` 유지 구간의 위치·회전 차이는 모두 `0`이었다. 실제 상태는 `34프레임×2루프`, 루트 변위 `0m`, `Loop=True`, `ApplyRootMotion=False`였다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerCrouchExitIdleTransition`, `CapturePlayerCrouchExitIdleTransitionReview`, `CapturePlayerCrouchExitIdleTransitionFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_crouch_diagonal_exit_2026-08-28/player_crouch_exit_idle_transition_*`에 저장했고, 직접 확인과 보조 검증을 통과한 뒤 최종 이미지를 한 번만 생성했다.
- Harness, EditMode 테스트, PlayMode 테스트, Windows Build와 Git 작업은 실행하지 않았다.

## 범용 손과 물체 Idle 애니메이션 연결

- 실제 Unity Play Mode의 같은 정면·측면 전신 구도에서 `Hands_Empty_Idle`과 `Player_Idle` 원본, `Hands_Carry_OneHand`와 1Hand FBX 내장 Take, `Hands_Carry_TwoHand`와 2Hand FBX 내장 Take를 각각 8개 위상으로 나란히 직접 확인했다. 세 적용 결과는 기준 행과 손·팔·척추·하체 자세 및 동작이 일치했고, 개체 소실, 관절 파손, 메시 늘어짐과 임의 보정 흔적은 보이지 않았다.
- `Hands_Empty_Idle.anim`은 `Player_Idle.anim`의 별도 직렬화 복사본으로 만들고 3초·60fps·48개 float curve와 반복 설정을 그대로 유지했다. 전용 `Hands_Empty_Idle.controller`가 이 복사본을 직접 참조하며 원본 `Player_Idle.anim`은 변경하지 않았다.
- `player model/transfer 1hand idle.fbx`는 `Hands_Carry_OneHand_Mixamo.fbx`로 정확 복사했고 SHA-256은 양쪽 모두 `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`이다. 유일한 내장 Take `mixamo.com`은 2.533333초·30fps·60개 float curve이며 전용 Controller가 이 FBX sub-asset을 수정 없이 직접 참조한다.
- `player model/transfer 2hand Idle.fbx`는 `Hands_Carry_TwoHand_Mixamo.fbx`로 정확 복사했고 SHA-256은 양쪽 모두 `2CB9CA6EBE34C22770FB2BB23FE026CDCA47EF481324D8D6097CA23E6048D752`다. 유일한 내장 Take `mixamo.com`은 4.766667초·30fps·60개 float curve이며 전용 Controller가 이 FBX sub-asset을 수정 없이 직접 참조한다.
- 2순위 보조 확인에서 Empty는 `180프레임×2루프`, OneHand는 `76프레임×2루프`, TwoHand는 `143프레임×2루프`를 표본화했다. 세 대상 모두 기준 대비 최대 회전 차이 `0°`, 위치 차이 `0.000000061m` 이하, 루트 변위 `0m`, `Loop=True`, `ApplyRootMotion=False`였다. 세 대상 루트 Transform과 다른 Animator도 변경되지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsObjectAnimations`, `CapturePlayerHandsObjectAnimationsReview`, `CapturePlayerHandsObjectAnimationsFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_objects_2026-08-28/`에 저장했고 직접 확인과 보조 검증을 통과한 뒤 세 최종 이미지를 한 번의 최종 명령으로 생성했다.
- Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## Carry 개체의 Empty Idle 몸통 정렬과 팔 각도 보존

- 실제 Unity Play Mode의 같은 8개 시점·정면·측면 전신 구도에서 `Hands_Empty_Idle`, 각 Carry 원본 Take와 최종 결과를 나란히 직접 확인했다. `Hands_Carry_OneHand`와 `Hands_Carry_TwoHand`의 최종 골반·양다리·허리·척추·목·머리는 Empty 기준과 일치했고, 양쪽 어깨부터 손까지는 각 Carry 원본의 상대 각도를 유지했다. 반복 전 구간에서 관절 붕괴, 메시 찢김, 개체 소실과 루트 이동은 보이지 않았다.
- 각 전용 Controller는 기본 레이어 `HandsEmptyIdleBase`에서 3초짜리 `Hands_Empty_Idle.anim`을 반복하고, Override 레이어 `Carry Arms`에서 원본 FBX의 내장 Take `mixamo.com`을 수정 없이 독립 반복한다. OneHand 팔은 원본 길이 `2.533333초`, TwoHand 팔은 원본 길이 `4.766667초`와 속도를 그대로 유지한다.
- 공용 `Hands_Carry_Arms.mask`는 실제 클립 바인딩과 같은 `Armature/Hips/...` 상대 경로 26개를 사용한다. Empty가 담당하는 골반·다리·척추·목·머리 경로는 비활성이고, `LeftShoulder`/`RightShoulder`부터 각 `Hand`까지 8개 경로만 활성이다. 대상 리그에는 Hand 아래 별도 손가락 Transform이 없어 추가 손가락 경로는 없다.
- 2순위 보조 확인에서 OneHand는 360프레임 동안 Empty 기본 2회·Carry 팔 2회, TwoHand는 572프레임 동안 Empty 기본 3회·Carry 팔 2회를 표본화했다. 두 대상 모두 몸통 위치 최대 차이 약 `0.000000015m`, 몸통과 팔의 최대 회전 차이 `0°`, 루트 변위 `0m`, `Loop=True`, `ApplyRootMotion=False`였다. Generic Animator의 팔 위치 진단값은 최대 `0.003m`였으며, 사용자 기준인 팔 각도 보존 판정에는 포함하지 않았다.
- `Hands_Empty_Idle.anim`과 두 Mixamo FBX의 적용 전후 SHA-256은 각각 `1C3901F1F2D5C0F75EA6C6D65FC5350F5637470A56A50297515AD59D97701D1E`, `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`, `2CB9CA6EBE34C22770FB2BB23FE026CDCA47EF481324D8D6097CA23E6048D752`로 동일했다. 두 대상 루트와 다른 Animator도 변경하지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsCarryBodyAlignment`, `CapturePlayerHandsCarryBodyAlignmentReview`, `CapturePlayerHandsCarryBodyAlignmentFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_body_alignment_*`에 저장했고 직접 확인과 보조 검증을 통과한 뒤 OneHand와 TwoHand 최종 이미지를 한 번의 최종 명령으로 생성했다.
- Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## Carry 팔 자세의 반복 전 구간 보정

- 실제 Unity Play Mode에서 각 원본 Carry 자세와 보정 결과를 같은 8개 시점의 전신·확대 정면/측면 구도로 직접 비교했다. `Hands_Carry_OneHand`의 왼팔은 어깨에서 손까지 몸 옆 아래로 자연스럽게 내려가고 팔꿈치가 약간 굽은 상태를 유지했다. `Hands_Carry_TwoHand`의 양손은 기존 간격과 상대 자세를 유지한 채 캐릭터 기준 오른쪽 가슴으로 함께 이동했다. 반복 전 구간에서 손목 뒤틀림, 관절 꺾임, 메시 찢김과 루트 이동은 보이지 않았다.
- 원본 FBX는 수정하지 않았다. 각 내장 Take를 30fps 전 프레임으로 샘플링하고 원본의 미세한 움직임을 유지한 채 승인된 손 목표에 양팔 IK를 적용해 `Hands_Carry_OneHand_ArmAdjusted.anim`과 `Hands_Carry_TwoHand_ArmAdjusted.anim`을 만들었다. 파생 클립은 Shoulder부터 Hand까지 8개 Transform의 위치·회전 곡선 56개만 가지며 원본 길이 `2.533333초`, `4.766667초`와 반복 속도를 그대로 유지한다.
- 전용 Controller의 기본 레이어는 계속 `Hands_Empty_Idle.anim`을 재생하고 `Carry Arms` Override 레이어만 각 파생 클립을 반복한다. 공용 양팔 마스크, Empty 몸통·골반·다리·척추·목·머리와 `ApplyRootMotion=False` 설정은 유지했다.
- 2순위 보조 확인에서 OneHand 왼손은 모든 표본에서 팔 길이의 최소 `0.919998배`만큼 어깨 아래에 있고 골반보다 최소 `0.023216m` 아래, 몸 바깥쪽 `0.259659m`에 있었다. TwoHand 양손 중심은 최소 어깨너비의 `0.269986배`만큼 오른쪽에 있었고 원본 대비 두 손 간격 최대 차이는 `0.000049m`였다.
- 원본 손 움직임 범위는 OneHand `0.031253m`에서 `0.031178m`, TwoHand `0.052429m`에서 `0.052424m`로 유지됐다. OneHand는 360프레임에서 기본·팔 루프 각 2회, TwoHand는 572프레임에서 기본 3회·팔 루프 2회를 확인했다. 두 대상 모두 몸통 위치 차이 약 `0.000000015m`, 몸통 회전 차이 `0°`, 루트 변위 `0m`, `Loop=True`였다.
- `Hands_Empty_Idle.anim`과 두 Mixamo FBX의 적용 전후 SHA-256은 각각 `1C3901F1F2D5C0F75EA6C6D65FC5350F5637470A56A50297515AD59D97701D1E`, `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`, `2CB9CA6EBE34C22770FB2BB23FE026CDCA47EF481324D8D6097CA23E6048D752`로 동일하다. 대상 루트와 다른 Animator도 변경하지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsCarryPoseAdjustment`, `CapturePlayerHandsCarryPoseAdjustmentReview`, `CapturePlayerHandsCarryPoseAdjustmentFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_pose_adjustment_*`에 저장했고 직접 확인과 보조 검증을 통과한 뒤 두 최종 이미지를 한 번의 최종 명령으로 생성했다.
- Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## OneHand 왼팔 간격과 오른손 세로 그립 보정

- 실제 Unity Play Mode에서 이전 OneHand 결과와 새 결과를 같은 8개 시점의 전신·확대 정면/측면 구도로 직접 비교했다. 왼팔은 기존의 자연스러운 하강·팔꿈치 굽힘을 유지하면서 위팔·아래팔 실루엣이 몸통에서 분리됐다. 오른쪽 어깨·위팔·아래팔은 그대로이고 오른손목·손만 세워져, 손바닥이 몸 안쪽을 향한 채 가상의 막대기를 수직으로 잡는 자세가 됐다. 손목·팔꿈치 뒤틀림, 메시 겹침·찢김과 루트 이동은 보이지 않았다.
- `Hands_Carry_OneHand_ArmAdjusted.anim` 생성 시 왼손 목표를 이전 자세보다 팔 길이의 약 `10%`만큼 캐릭터 왼쪽 바깥으로 옮겼다. 오른손은 기준 프레임에서 Hand의 세로축을 캐릭터 Up에 맞추고 손바닥 축을 몸통 방향에 맞춘 고정 로컬 회전 오프셋만 전 프레임에 적용해 기존 미세 움직임을 유지했다.
- 2순위 보조 확인에서 왼쪽 아래팔은 몸통 중심에서 최소 `0.264748m`, 왼손은 골반에서 바깥쪽으로 최소 `0.307930m` 떨어졌다. 오른쪽 Shoulder·Arm·ForeArm의 위치·회전 차이는 모두 `0`이고 적용 전후 곡선 SHA-256도 `C8442F2E2FC4C1DA0263FB88D7E181709E876D24020784466B9C6D134071C42B`로 동일하다.
- 오른손 세로축의 최대 편차는 `5.33737°`, 손바닥 안쪽 방향 최대 편차는 `5.648404°`였다. 오른손 위치 차이는 최대 `0.000199m`, 움직임 범위는 `0.0399261m`에서 `0.0399259m`로 유지됐다. 360프레임에서 Empty 기본과 OneHand 팔 루프를 각각 2회 확인했고 몸통 회전 차이와 루트 변위는 `0`이었다.
- `Hands_Empty_Idle.anim`과 OneHand Mixamo FBX의 적용 전후 SHA-256은 각각 `1C3901F1F2D5C0F75EA6C6D65FC5350F5637470A56A50297515AD59D97701D1E`, `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`로 동일하다. TwoHand 파생 클립과 Controller의 적용 전후 SHA-256도 각각 `5B67B71EA4585EE6FF32B25C68381C74B9FF42EEB83B18D1060E0C8C5794A371`, `43966669BA474F749CEB53372EF0305EDB24C5CB2F8E4609864E2E48080FB276`로 동일하며 다른 Animator도 변경하지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsCarryOneHandGripClearance`, `CapturePlayerHandsCarryOneHandGripClearanceReview`, `CapturePlayerHandsCarryOneHandGripClearanceFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_onehand_grip_clearance_*`에 저장했고 직접 확인과 보조 검증을 통과한 뒤 최종 이미지를 한 번만 생성했다.
- Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## OneHand 오른손목 세로 그립 축 수정

- 이전 결과와 수정 결과를 실제 Unity Play Mode의 같은 8개 시점·전신/확대·정면/측면 구도로 먼저 직접 비교했다. 이전 결과에서 손바닥이 정면을 향하고 손가락이 위로 서 있던 `멈춤` 형태가 사라졌으며, 수정 결과는 반복 전 구간에서 오른손의 옆면이 보이고 손목이 가상의 막대기를 지면에 수직으로 쥘 수 있는 방향을 유지했다. 오른쪽 어깨·위팔·아래팔 자세, 왼팔 간격, 손의 위치 움직임과 몸통 자세는 변하지 않았고 새 관절 꺾임이나 메시 파손은 보이지 않았다.
- 원인은 `RightHand.up`을 막대기 세로축, `RightHand.forward`를 손바닥 축으로 판정했던 로컬 축 해석 오류였다. 생성 기준을 `RightHand.forward = 캐릭터 Up`, `RightHand.right = 몸통 방향`으로 수정해 손목 로컬 회전 오프셋만 전 프레임에 다시 적용했다. 손가락 전용 곡선은 추가하거나 수정하지 않았고 `RightShoulder`, `RightArm`, `RightForeArm` 곡선도 건드리지 않았다.
- 직접 확인 뒤 수행한 2순위 보조 확인에서 6초·360프레임 동안 Empty 기본과 OneHand 팔 레이어를 각각 2회 표본화했다. 막대기 세로축 최대 편차는 `5.337407°`, 손바닥 몸통 방향 최대 편차는 `5.648370°`, 오른손 위치 차이는 최대 `0.000199m`였고 원본/수정 오른손 움직임 범위는 각각 `0.0399261m`, `0.0399259m`였다. 오른쪽 어깨·위팔·아래팔 위치/회전 차이는 모두 `0`, 루트 변위는 `0m`, `Loop=True`, `ApplyRootMotion=False`였다.
- 오른쪽 어깨·위팔·아래팔 곡선 SHA-256은 적용 전후 모두 `C8442F2E2FC4C1DA0263FB88D7E181709E876D24020784466B9C6D134071C42B`로 동일하다. `Hands_Empty_Idle.anim`, OneHand 원본 FBX, TwoHand 파생 클립과 Controller의 해시도 각각 `1C3901F1F2D5C0F75EA6C6D65FC5350F5637470A56A50297515AD59D97701D1E`, `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`, `5B67B71EA4585EE6FF32B25C68381C74B9FF42EEB83B18D1060E0C8C5794A371`, `43966669BA474F749CEB53372EF0305EDB24C5CB2F8E4609864E2E48080FB276`로 유지됐다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsCarryOneHandWristGripCorrection`, `CapturePlayerHandsCarryOneHandWristGripCorrectionReview`, `CapturePlayerHandsCarryOneHandWristGripCorrectionFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_onehand_wrist_grip_correction_*`에 저장했고 직접 확인과 보조 확인을 통과한 뒤 최종 이미지를 한 번만 확정했다.
- Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## OneHand 오른손목 수직축 180도 반전

- 이 단계의 손목 단독 반전은 사용자의 후속 정정에 따라 잘못 해석된 적용으로 판정됐으며 현재 최종 상태가 아니다. 기록과 검증 자료는 삭제하지 않고 아래 `OneHand 오른팔 자연스러운 수직 그립 재보정` 단계로 대체했다.
- 이전 손목 상태와 새 결과를 실제 Unity Play Mode의 같은 8개 시점·전신/확대·정면/측면 구도로 먼저 직접 비교했다. 오른손은 같은 위치와 수직 그립을 유지하면서 수직축 주위로 반대 면이 보이도록 뒤집혔고, 반복 전 구간에서 같은 방향을 유지했다. 오른쪽 어깨·위팔·아래팔 자세와 손가락 모양은 그대로였으며 손목 튐, 관절 꺾임과 메시 파손은 보이지 않았다.
- 기존 세로 그립 기준인 `RightHand.forward = 캐릭터 Up`을 유지한 뒤 해당 축을 중심으로 `180°` 회전을 추가했다. 이 고정 로컬 회전 오프셋만 OneHand 파생 클립의 전 프레임에 적용했고, 손가락 전용 곡선이나 `RightShoulder`, `RightArm`, `RightForeArm` 곡선은 추가·수정하지 않았다.
- 직접 확인 뒤 수행한 2순위 보조 확인에서 6초·360프레임 동안 Empty 기본과 OneHand 팔 레이어를 각각 2회 표본화했다. 손바닥 방향은 기존 몸통 방향에서 `174.351639°~180°` 반전됐고 목표 180도 대비 최대 편차는 `5.648370°`였다. 수직 그립 최대 편차는 `5.337407°`, 오른손 위치 차이는 최대 `0.000199m`, 원본/수정 오른손 움직임 범위는 각각 `0.0399261m`, `0.0399259m`였다.
- 오른쪽 어깨·위팔·아래팔 위치/회전 차이는 모두 `0`이며 곡선 SHA-256은 적용 전후 `C8442F2E2FC4C1DA0263FB88D7E181709E876D24020784466B9C6D134071C42B`로 동일하다. `Hands_Empty_Idle.anim`, OneHand 원본 FBX, TwoHand 파생 클립과 Controller의 해시도 각각 `1C3901F1F2D5C0F75EA6C6D65FC5350F5637470A56A50297515AD59D97701D1E`, `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`, `5B67B71EA4585EE6FF32B25C68381C74B9FF42EEB83B18D1060E0C8C5794A371`, `43966669BA474F749CEB53372EF0305EDB24C5CB2F8E4609864E2E48080FB276`로 유지됐다. 루트 변위는 `0m`, `Loop=True`, `ApplyRootMotion=False`였다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsCarryOneHandWrist180Flip`, `CapturePlayerHandsCarryOneHandWrist180FlipReview`, `CapturePlayerHandsCarryOneHandWrist180FlipFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_onehand_wrist_180_flip_*`에 저장했고 직접 확인과 보조 확인을 통과한 뒤 최종 이미지를 한 번만 확정했다.
- Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## OneHand 오른팔 자연스러운 수직 그립 재보정

- 첫 자연스러운 팔 보정본은 수치상 손바닥 안쪽으로 잘못 판정했으나, 사용자의 직접 확인에서 실제 손바닥이 몸통 밖을 향한 실패 상태로 판정됐다. 원인은 장갑 메시의 실제 손바닥 법선을 `RightHand.right`로 해석한 것이며, 실제 기준은 `-RightHand.right`였다. 이 직접 판정을 우선해 기존 성공 판정과 최종 캡처를 무효화하고 손 방향을 다시 교정했다.
- 교정 결과를 실제 Unity Play Mode의 같은 8개 시점·전신/확대·정면/측면 구도로 실패본과 먼저 직접 비교했다. 해부학적 오른손의 손등 장갑 면은 몸 바깥쪽에 보이고 실제 손바닥은 몸통 안쪽을 향했다. 오른쪽 팔꿈치는 몸 바깥쪽·어깨 아래에 놓이고 위팔에서 아래팔, 손목까지 자연스럽게 이어졌으며, 오른손은 기존 가슴 앞 높이와 지면 수직 그립을 유지했다. 반복 전 구간에서 손목 비틀림, 팔꿈치 꺾임, 몸통 교차와 메시 파손은 보이지 않았고 손가락·왼팔·몸통 자세도 유지됐다.
- 각 원본 프레임의 오른손 위치를 IK 목표로 고정하고 `RightArm`과 `RightForeArm`을 함께 풀었다. 팔꿈치 pole은 캐릭터 오른쪽 바깥·약간 아래로 두고, 수직 그립에 필요한 잔여 회전은 아래팔 축 회전과 `RightHand` 로컬 회전의 최대 편차가 작아지도록 분산했다. 마지막에 `RightHand.forward = 캐릭터 Up`, 실제 손바닥 법선인 `-RightHand.right = 몸통 방향`을 맞췄으며 손가락 전용 곡선은 추가·수정하지 않았다.
- 직접 확인 뒤 수행한 2순위 보조 확인에서 6초·360프레임 동안 Empty 기본과 OneHand 팔 레이어를 각각 2회 표본화했다. 오른쪽 팔꿈치는 몸 중심에서 바깥쪽 최소 `0.398834m`, 어깨보다 아래 최소 `0.118280m`였고, 오른손 위치 차이는 최대 `0.000192m`였다. 손목 로컬 회전의 원본 대비 최대 차이는 `10.773995°`, 실제 손바닥의 몸통 안쪽 방향 최대 오차는 `0.019782°`, 수직 그립 축 최대 오차는 `0°`였다. 원본/수정 오른손 움직임 범위는 각각 `0.0399261m`, `0.0399264m`였다.
- 오른팔 곡선 SHA-256은 실제 손바닥이 바깥을 향한 실패본의 `698AB56744713487DB9BD7012D43E3FBA7C2BBBAAA785E6D209D8EECDB8AA4F8`에서 교정본의 `6D0999016757D120CB1F167233CD3C24EFD05ED934F4000622F28ED76C209F38`로 변경됐다. 왼팔 곡선 SHA-256은 적용 전후 `898F6EEAA483104EAED5A65CB6787EF3E4EBE6B97DEAA1165CA1B0C54BA9A45E`로 동일하다. Empty Idle, OneHand 원본 FBX, TwoHand 파생 클립과 Controller의 해시도 각각 `1C3901F1F2D5C0F75EA6C6D65FC5350F5637470A56A50297515AD59D97701D1E`, `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`, `5B67B71EA4585EE6FF32B25C68381C74B9FF42EEB83B18D1060E0C8C5794A371`, `43966669BA474F749CEB53372EF0305EDB24C5CB2F8E4609864E2E48080FB276`로 유지됐다. 루트 변위는 `0m`, `Loop=True`, `ApplyRootMotion=False`였다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsCarryOneHandNaturalVerticalGrip`, `CapturePlayerHandsCarryOneHandNaturalVerticalGripReview`, `CapturePlayerHandsCarryOneHandNaturalVerticalGripFinal`을 사용했다. Play Mode 종료 직후 첫 리프레시는 전환 중 상태라 거부됐고, Edit Mode 안정 후 같은 명령 재시도는 정상 완료됐다. 적용·검토 자료는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_onehand_natural_vertical_grip_*`에 저장했다. 사용자 판정으로 무효화된 기존 최종 이미지는 교정본의 직접 확인과 보조 확인을 통과한 뒤 새 최종 이미지로 갱신했다.
- Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## OneHand 전완–손목 해부학적 연결 재수정

- 사용자가 제공한 확대 이미지에서 이전 결과의 오른손이 손목 관절을 기준으로 전완에서 옆으로 접혀 매달린 형태임을 직접 확인해 이전 성공 판정을 무효화했다. 원인은 손바닥 안쪽과 수직 그립 방향만 맞추고 전완이 손목으로 들어오는 방향을 제한하지 않은 고정 pole IK였다. 수치상 손바닥 방향이 맞아도 실제 손목 실루엣이 실패한 사례이므로 사용자 이미지의 직접 판정을 최우선 기준으로 삼았다.
- 새 결과는 손바닥 안쪽 방향과 지면 수직 그립축으로 해부학적 전완 진행 방향을 계산하고, 원본 FBX의 매 프레임에서 어깨–팔꿈치와 팔꿈치–손목 길이를 보존한 채 팔꿈치와 손 위치를 함께 다시 풀었다. 전완축 회전은 원본 손목 국부 회전과의 차이가 최소가 되도록 분산했으며 손가락 전용 곡선은 추가하거나 수정하지 않았다.
- 실제 Unity Play Mode에서 실패본과 수정본을 같은 8개 위상·전신/오른손 확대·정면/측면 구도로 직접 비교했다. 수정본은 전완 장갑 소매와 손목 커프가 같은 진행축으로 자연스럽게 이어졌고, 이전처럼 손이 관절에서 직각으로 접히거나 매달리지 않았다. 손바닥은 몸통 안쪽을 향하고 수직 그립을 유지했으며, 팔꿈치는 몸 바깥쪽·어깨 아래에서 손으로 이어졌다. 2회 반복 전 구간에 관절 튐, 메시 찢김, 몸통 교차와 루트 이동은 보이지 않았다.
- 직접 확인 뒤 2순위 보조 검증으로 6초·360프레임에서 Empty 기본과 OneHand 팔 레이어를 각각 2회 표본화했다. 전완–손목 연속각은 최대 `7.616153°`, 원본 대비 손목 국부 회전 차이는 최대 `1.992059°`, 실제 손바닥의 몸통 안쪽 오차는 최대 `0.027976°`, 수직 그립축 오차는 `0°`였다. 오른쪽 팔꿈치는 몸 중심에서 바깥쪽 최소 `0.365173m`, 어깨보다 아래 최소 `0.172094m`였고 루트 변위와 몸통 회전 차이는 `0`이었다.
- 오른팔 곡선 SHA-256은 실패본의 `6D0999016757D120CB1F167233CD3C24EFD05ED934F4000622F28ED76C209F38`에서 교정본의 `51E0504D3FB2DFFE4E2CE3D111FEF938ED55A218DCAA86841D9D90348F5659A4`로 변경됐다. 왼팔 곡선은 적용 전후 `898F6EEAA483104EAED5A65CB6787EF3E4EBE6B97DEAA1165CA1B0C54BA9A45E`로 동일하다. Empty Idle, OneHand 원본 FBX, TwoHand 파생 클립과 Controller의 해시도 각각 `1C3901F1F2D5C0F75EA6C6D65FC5350F5637470A56A50297515AD59D97701D1E`, `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`, `5B67B71EA4585EE6FF32B25C68381C74B9FF42EEB83B18D1060E0C8C5794A371`, `43966669BA474F749CEB53372EF0305EDB24C5CB2F8E4609864E2E48080FB276`로 유지됐다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsCarryOneHandAnatomicalWristGrip`, `CapturePlayerHandsCarryOneHandAnatomicalWristGripReview`, `CapturePlayerHandsCarryOneHandAnatomicalWristGripFinal`을 사용했다. 검토 단계와 실제 Play Mode 상태가 어긋난 두 호출은 캡처 전에 중단됐고 단계값을 지운 뒤 정상 재진입했다. 적용·검토 자료와 원본 해상도 손목 확대 정면/측면 이미지는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_onehand_anatomical_wrist_grip_*`에 저장했으며, 직접 확인과 보조 확인을 통과한 뒤 최종 이미지를 한 번만 생성했다.
- Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## OneHand 실제 오른손바닥 몸통 방향 재교정

- 이전 `전완–손목 해부학적 연결 재수정` 결과는 Transform 축을 실제 손바닥 면으로 간주한 잘못된 판정이었으며, 사용자의 직접 지적에 따라 완료 상태를 무효화했다. 실제 오른손은 정면 기준 화면 왼쪽 손이고, 손바닥은 손가락이 굽혀지는 안쪽·손가락 패드와 엄지가 있는 면이다. 이번 단계에서는 각도, 해시, Transform 축과 자동 스크립트 결과를 자세 성공 근거로 사용하지 않았다.
- 실제 오른손바닥이 몸통을 향하도록 수직 그립축 주위의 손 방향을 반전하고, 손목만 비틀지 않도록 같은 방향을 기준으로 어깨–팔꿈치–전완–손목 관절 연쇄를 원본 FBX의 매 프레임에서 함께 다시 생성했다. 손가락 전용 곡선과 원본 Mixamo FBX, 왼팔, 몸통 및 `Hands_Carry_TwoHand`는 수정하지 않았다.
- 첫 몸통 시점 촬영은 카메라가 손과 몸 사이가 아니라 몸 뒤에서 촬영해 백팩과 몸통이 손바닥을 가렸으므로 직접 확인 불가로 판정하고 통과시키지 않았다. 자세는 그대로 둔 채 카메라를 손과 몸통 사이에 배치해 오른손을 몸통 방향에서 직접 바라보도록 재촬영했다.
- 재촬영한 실제 Unity Play Mode 원본 해상도 몸통 시점에서 손목은 화면 오른쪽, 손가락은 왼쪽을 향하고 오른손 엄지는 손가락 아래쪽에 놓였다. 손가락 안쪽 패드와 손바닥 중앙 패드가 카메라를 향하고 반대편 손등 관절판은 보이지 않아 실제 오른손바닥이 몸통 쪽을 향함을 직접 확인했다. 같은 면은 8개 위상 전체에서 유지됐고, 정면·측면 확대에서는 전완 소매–손목 커프–손이 꺾이거나 비틀리지 않고 이어졌다. 두 반복 동안 관절 튐, 메시 찢김, 몸통 교차와 루트 이동은 보이지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsCarryOneHandActualPalmInwardGrip`, `CapturePlayerHandsCarryOneHandActualPalmInwardGripReview`, `CapturePlayerHandsCarryOneHandActualPalmInwardGripFinal`을 사용했다. 검토 자료는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_onehand_actual_palm_inward_grip_*`에 저장했고, 실제 손바닥 면을 직접 확인한 뒤 검토 접촉 시트를 최종 이미지로 한 번만 확정했다.
- 수치·각도·해시·스크립트 기반 자세 성공 판정, Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## Hands_Carry_OneHand 지정 FBX 내장 Take 직접 연결

- 사용자 지시에 따라 `Hands_Carry_OneHand`의 기존 `Base Empty Idle + Carry Arms` 2레이어와 `Hands_Carry_OneHand_ArmAdjusted.anim` 연결을 제거했다. 파생 `.anim` 파일 자체는 삭제하지 않았으며 다른 플레이어 Animator도 변경하지 않았다.
- 지정 원본 `player model/transfer 1hand idle.fbx`와 Unity 복사본 `Assets/_Project/Art/Player/Animations/Hands_Carry_OneHand_Mixamo.fbx`의 SHA-256은 모두 `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`로 일치했다. 두 FBX는 수정하거나 재생성하지 않았다.
- `Hands_Carry_OneHand.controller`는 단일 `Base Layer`와 단일 상태 `HandsCarryOneHand`만 사용하며, 유일한 내장 Take `mixamo.com`을 직접 참조한다. 별도 리타기팅, 미러링, 키프레임·곡선·자세 보정은 적용하지 않았다. Take는 `2.533333초`, 30fps, float curve 60개, 이벤트 0개이며 `Speed=1`, `Loop=True`, `ApplyRootMotion=False`, `AlwaysAnimate`다.
- 실제 Unity Play Mode에서 원본 Take와 적용 결과를 정면·측면 8개 동일 위상으로 나란히 직접 확인했다. 모든 위상에서 자세·팔다리 움직임·실루엣이 일치했고 메시 파손, 관절 이상, 개체 소실과 루트 이동은 보이지 않았다.
- 2순위 보조 확인에서 76프레임씩 2루프, 총 152프레임의 최대 위치 차이는 약 `0.000000008m`, 회전 차이는 `0°`, 루트 변위는 `0m`였다. 원본 FBX 해시, 대상 루트와 다른 Animator는 적용 전후 동일했다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`과 `ApplyPlayerHandsCarryOneHandEmbeddedTakeExact`을 사용했다. 검토 자료는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_onehand_embedded_take_*`에 저장했고, 직접 확인과 보조 확인을 통과한 뒤 최종 이미지를 한 번만 확정했다. 승인 범위 밖 자동 검증, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## Hands_Carry_OneHand Empty 몸통·왼팔 하강·오른손바닥 왼쪽 보정

- `Hands_Carry_OneHand.controller`를 `Hands_Empty_Idle.anim` 기본 레이어와 양팔 전용 Override 레이어로 구성했다. 몸통·골반·다리·목·머리는 Empty Idle을 따르고 `Hands_Carry_OneHand_ArmAdjusted.anim`에는 Shoulder부터 Hand까지 양팔 8개 Transform 곡선만 포함했다.
- 왼팔은 원본의 위상별 미세 움직임을 유지하면서 몸 옆 아래로 내리고 몸통 바깥 간격을 확보했다. 오른손 위치와 원본 움직임은 유지하고 전완과 손목에 회전을 분산해 실제 오른손바닥 면인 `-RightHand.right`가 캐릭터 자신의 왼쪽을 향하도록 했다.
- 실제 Unity Play Mode의 같은 8개 위상에서 원본과 결과를 전신 정면·측면 및 오른팔 확대 정면·측면으로 직접 비교했다. 결과의 왼팔은 몸통과 겹치지 않고 자연스럽게 내려갔으며, 오른팔은 어깨–팔꿈치–전완–손목이 끊기거나 꺾이지 않고 이어졌다. 캐릭터 왼쪽에 배치한 전용 카메라에서 실제 손바닥 패드 면이 정면으로 보여 손바닥 방향도 직접 확인했다.
- 2순위 보조 확인에서 6초·360프레임 동안 Empty 기본과 팔 레이어를 각각 2회 표본화했다. 몸통 위치 차이는 약 `0.000000015m`, 회전 차이는 `0°`, 왼전완–몸통 최소 간격은 `0.264748m`, 왼손–골반 최소 바깥 간격은 `0.307930m`였다.
- 오른손바닥의 캐릭터 왼쪽 목표 오차와 전완 진행 방향 차이는 모두 `0°`, 오른손 위치 차이는 최대 `0.000199m`, 원본/보정 손 움직임 범위는 `0.039926m / 0.039927m`, 손목 로컬 회전 차이는 최대 `3.743788°`였다. 오른팔꿈치는 몸 중심 바깥 최소 `0.300621m`, 어깨 아래 최소 `0.215860m`였고 루트 변위는 `0m`였다.
- 지정 원본과 Unity FBX의 SHA-256은 `90DFFCC45C58B5BF0876F6A3409026D9E6BC6BF5268068D12516803FBADC6E78`로 유지됐다. `Hands_Empty_Idle.anim`, TwoHand 파생 클립·Controller, 다른 Animator와 대상 루트도 변경되지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`과 `ApplyPlayerHandsCarryOneHandEmptyBodyPalmLeft`을 사용했다. 검토 자료는 `docs/validation/player_hands_objects_2026-08-28/player_hands_carry_onehand_empty_body_palm_left_*`에 저장했고, 직접 확인과 보조 확인을 통과한 뒤 최종 이미지를 한 번만 확정했다. 승인 범위 밖 자동 검증, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## Hands_Draw_Back·Hands_Stow_Back 지정 FBX 내장 Take 직접 연결

- `Hands_Draw_Back`에는 `player model/transfer grab from behind.fbx`, `Hands_Stow_Back`에는 `player model/transfer put back.fbx`의 유일한 내장 Take `mixamo.com`을 각각 전용 단일 상태 Controller로 직접 연결했다. 별도 `.anim` 생성, 리타기팅, 미러링, 키프레임·곡선·자세 보정은 적용하지 않았다.
- Draw 원본과 Unity 복사본 `Assets/_Project/Art/Player/Animations/Hands_Draw_Back_Mixamo.fbx`의 SHA-256은 모두 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`다. Stow 원본과 Unity 복사본 `Assets/_Project/Art/Player/Animations/Hands_Stow_Back_Mixamo.fbx`의 SHA-256은 모두 `ECAA2FCE857BD9E5275ECDDDFFA220F26C3AA802354A36C9980B51D1026A01D9`다.
- `Hands_Draw_Back.controller`의 `HandsDrawBack` 상태는 2.3초·30fps·float curve 60개·이벤트 0개의 원본 FBX sub-asset을 참조한다. `Hands_Stow_Back.controller`의 `HandsStowBack` 상태는 3.966667초·30fps·float curve 60개·이벤트 0개의 원본 FBX sub-asset을 참조한다. 두 상태는 `Speed=1`, `Loop=True`, `ApplyRootMotion=False`, `AlwaysAnimate`다.
- 실제 Unity Play Mode에서 각 원본 Take와 적용 결과를 정면·측면 8개 동일 위상으로 나란히 직접 확인했다. Draw의 뒤로 손을 가져가는 동작과 Stow의 양팔·몸통 동작이 각 원본 행과 동일했고, 메시 소실, 리그 파손과 임의 보정 흔적은 보이지 않았다.
- 직접 확인 뒤 2순위 보조 확인에서 Draw는 69프레임씩 2루프, 총 138프레임의 원본 대비 최대 위치 차이가 약 `0.000000121m`, 회전 차이가 `0°`였다. Stow는 119프레임씩 2루프, 총 238프레임의 최대 위치 차이가 약 `0.000000335m`, 회전 차이가 `0°`였다. 두 대상 모두 루트 변위 `0m`, `Loop=True`, `ApplyRootMotion=False`였으며 대상 루트와 다른 Animator는 변경되지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsDrawAndStowBackExactTakes`, `CapturePlayerHandsDrawAndStowBackExactReview`, `CapturePlayerHandsDrawAndStowBackExactFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_back_2026-08-28/`에 저장했고, 직접 확인과 보조 확인을 통과한 뒤 두 최종 이미지를 한 번의 최종 명령으로 확정했다. 승인 범위 밖 자동 검증, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## Hands_Draw_Back 오른팔 정면 각도 보정

- 실제 Unity Play Mode에서 원본과 수정본을 기본 8개 위상과 최대 전진 위상의 전신·오른팔 확대 정면/측면으로 나란히 직접 확인했다. 원본의 최대 전진 자세는 오른팔이 캐릭터 정면축에서 옆으로 벌어진 대각선이었고, 수정본은 어깨에서 오른손까지 정면축을 향했다. 어깨–팔꿈치–전완–손목은 꺾이거나 끊기지 않고 이어졌으며 팔꿈치 굽힘과 손의 보이는 방향, 동작 위상은 원본과 같았다. 메시 파손과 루트 이동도 보이지 않았다.
- 원본 `Hands_Draw_Back_Mixamo.fbx`를 수정하지 않고 `Hands_Draw_Back_ForwardAdjusted.anim`을 만들었다. 2.3초·30fps·이벤트 0개와 원본의 최대 전진 프레임 66을 유지하고, `RightArm`, `RightForeArm`, `RightHand`의 회전 바인딩만 Quaternion 곡선으로 교체했다. 나머지 곡선과 이벤트는 원본과 동일하다.
- 최대 전진 프레임의 어깨→손 정면 각도는 `34.84513°`에서 `0°`로 바뀌었다. 팔꿈치 굽힘은 `60.73546° / 60.73512°`, 오른손 월드 방향 차이는 `0°`로 유지했다. 원본 FBX에 `RightHand` 자체 회전 곡선이 없어, 편집 상태의 기본 손 로컬 회전을 각 원본 표본 전에 복원하고 생성된 클립의 실제 부모 관절 평가 결과로 손 회전을 2차 보정했다.
- 직접 확인 뒤 2순위 보조 확인에서 69프레임씩 2루프, 총 138프레임을 표본화했다. 수정 클립과 실제 Animator 자세의 최대 위치 차이는 약 `0.000000147m`, 회전 차이는 `0°`, 원본과 수정본의 오른팔 외 자세 차이는 위치·회전 모두 `0`, 루트 변위는 `0m`였다. `Loop=True`, `ApplyRootMotion=False`다.
- 원본 `player model/transfer grab from behind.fbx`와 Unity FBX의 SHA-256은 모두 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`로 유지됐다. `Hands_Stow_Back.controller`, 다른 Animator와 대상 루트도 변경하지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsDrawBackForwardAngle`, `CapturePlayerHandsDrawBackForwardAngleReview`, `CapturePlayerHandsDrawBackForwardAngleFinal`을 사용했다. 검토 자료는 `docs/validation/player_hands_draw_back_forward_angle_2026-08-28/`에 저장했고 직접 확인과 보조 확인을 통과한 뒤 최종 이미지를 한 번만 확정했다. 승인 범위 밖 자동 검증, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## Hands_Draw_Back 명치 높이·30도 팔꿈치·손바닥 왼쪽 보정

- 실제 Unity Play Mode에서 원본과 수정본을 기본 8개 위상과 최대 전진 위상의 전신 및 오른팔 확대 정면·측면으로 나란히 직접 확인했다. 최대 전진 시 오른손은 명치 높이로 내려왔고, 오른팔은 캐릭터 정면을 향하면서 팔꿈치가 완전 신전되지 않은 약한 굽힘을 유지했다. 어깨–팔꿈치–전완–손목의 실루엣은 역꺾임이나 단절 없이 이어졌고, 손바닥 전용 캐릭터 왼쪽 시점에서 전진 구간의 실제 손바닥 면이 왼쪽을 향하는 흐름을 확인했다. 동작 위상, 반복 속도, 메시와 루트에도 이상이 보이지 않았다.
- 원본 `Hands_Draw_Back_Mixamo.fbx`는 수정하지 않고 기존 `Hands_Draw_Back_ForwardAdjusted.anim`의 `RightArm`, `RightForeArm`, `RightHand` 회전 곡선만 다시 생성했다. 원본 전방 진행량을 보정 가중치로 사용해 동작 타이밍을 유지하고, 최대 전진 프레임에서 `Spine01` 높이를 명치 기준으로 삼아 손 높이와 팔꿈치 굽힘을 맞춘 뒤 전완과 손목에 손바닥 왼쪽 회전을 분산했다.
- 직접 확인 뒤 2순위 보조 확인에서 최대 전진 프레임은 원본과 수정본 모두 `66`으로 유지됐다. 오른손–명치 높이 차이는 약 `0.00000298m`, 팔꿈치 굽힘은 `29.99812°`, 수평 정면 각도와 손바닥 왼쪽 목표 오차는 각각 `0°`였다. 클립은 `2.3초`, 30fps, 이벤트 0개, `Loop=True`, `ApplyRootMotion=False`를 유지했다.
- 69프레임씩 2루프, 총 138프레임에서 수정 클립과 실제 Animator 자세의 최대 위치 차이는 약 `0.000000147m`, 회전 차이는 `0°`였다. 원본과 수정본의 오른팔 외 자세 차이는 위치·회전 모두 `0`, 루트 변위도 `0m`였다.
- 원본 `player model/transfer grab from behind.fbx`와 Unity FBX의 SHA-256은 모두 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`로 유지됐다. `Hands_Stow_Back.controller`의 SHA-256도 `C0AFF2A9F8FE7684E9A1E776D2D84CAA212C688C392C4287DF998B64F6ADE0D5`로 유지됐으며 Hands_Stow_Back, 다른 Animator와 대상 루트는 변경하지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsDrawBackLowPalmLeftPose`, `CapturePlayerHandsDrawBackLowPalmLeftPoseReview`, `CapturePlayerHandsDrawBackLowPalmLeftPoseFinal`을 사용했다. 검토 자료는 `docs/validation/player_hands_draw_back_low_palm_left_2026-08-28/`에 저장했고 직접 확인과 보조 확인을 통과한 뒤 검토 접촉 시트를 최종 이미지로 한 번만 확정했다. Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## Hands_Draw_Back 오른팔 바깥쪽 인출 궤적 보정

- 실제 Unity Play Mode에서 등 뒤 최저점부터 최대 전진까지의 인출 구간을 원본과 수정본의 전신·오른팔 확대 정면 및 측면으로 나란히 직접 확인했다. 수정본의 오른팔꿈치는 캐릭터 오른쪽 몸통 실루엣 밖으로 먼저 벌어지고, 오른손은 팔꿈치보다 몸통에 가까운 안쪽 궤적으로 따라오면서 전방으로 이동했다. 어깨–팔꿈치–전완–손목은 역꺾임이나 단절 없이 이어졌고, 몸통 관통·메시 파손·프레임 사이 튐은 보이지 않았다.
- 기존 `Hands_Draw_Back_ForwardAdjusted.anim` 생성 과정에서 원본 손이 가장 뒤에 있는 43프레임부터 최대 전진 66프레임까지만 완만한 왕복 가중치를 적용했다. 인출 중앙 54프레임에서 손의 도달 거리를 잠시 줄이고 팔꿈치 굽힘 기준을 캐릭터 오른쪽으로 이동해 팔꿈치가 먼저 몸통 밖으로 빠질 공간을 만들었다. 43·66프레임의 추가 보정값은 0이므로 인출 시작과 기존 최종 자세는 바뀌지 않는다.
- 직접 확인 뒤 2순위 보조 확인에서 54프레임의 팔꿈치는 원본보다 약 `0.048332m` 더 바깥으로 이동했다. 수정 팔꿈치는 몸통 외곽 기준보다 약 `0.276268m` 바깥, 오른손은 약 `0.056389m` 바깥이며, 손은 팔꿈치보다 약 `0.219879m` 안쪽이었다.
- 최대 전진 프레임은 원본과 수정본 모두 66으로 유지됐다. 해당 프레임의 오른손–명치 높이 차이는 약 `0.00000298m`, 팔꿈치 굽힘은 `29.99812°`, 수평 정면 각도와 손바닥 왼쪽 목표 오차는 각각 `0°`였다. 2.3초·30fps·반복 속도, `Loop=True`, `ApplyRootMotion=False`도 유지했다.
- 69프레임씩 2루프, 총 138프레임에서 수정 클립과 실제 Animator 자세의 최대 위치 차이는 약 `0.000000147m`, 회전 차이는 `0°`였다. 원본과 수정본의 오른팔 외 자세 차이와 루트 변위는 모두 `0`이었다.
- 원본 `player model/transfer grab from behind.fbx`와 Unity FBX의 SHA-256은 모두 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`로 유지됐다. `Hands_Stow_Back.controller`의 SHA-256도 `C0AFF2A9F8FE7684E9A1E776D2D84CAA212C688C392C4287DF998B64F6ADE0D5`로 유지됐으며 Hands_Stow_Back, 다른 Animator와 대상 루트는 변경하지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsDrawBackOuterElbowPath`, `CapturePlayerHandsDrawBackOuterElbowPathReview`, `CapturePlayerHandsDrawBackOuterElbowPathFinal`을 사용했다. 검토 자료는 `docs/validation/player_hands_draw_back_outer_elbow_2026-08-28/`에 저장했고, 직접 확인과 보조 확인을 통과한 뒤 검토 접촉 시트를 최종 이미지로 한 번만 확정했다. Harness, EditMode 테스트, PlayMode 테스트, Windows Build, HTML 완료 상태 변경과 Git 작업은 실행하지 않았다.

## Hands_Draw_Back 오른팔의 몸통·얼굴 실루엣 분리

- 실제 Unity Play Mode에서 반복 전 구간의 12개 시점을 전신 정면, 오른팔 확대 정면과 확대 측면의 같은 위상으로 직접 확인했다. 최대 전진 66프레임을 포함한 모든 정면 표본에서 오른쪽 위팔·팔꿈치·전완·손이 몸통과 헬멧·얼굴 실루엣의 캐릭터 오른쪽 바깥에 보이는 간격을 두고 유지됐다. 확대 측면에서도 어깨–팔꿈치–전완–손목이 자연스럽게 이어졌고 관절 꺾임, 몸통·얼굴 교차와 메시 파손은 보이지 않았다.
- 최초 `22°` 바깥 보정은 수치상 최소 간격이 약 `0.0089m`였지만 직접 확인에서 전완과 손 일부가 몸통 뒤에 가려져 실패로 판정했다. 실제 오른쪽 위팔의 바깥 어깨 위치를 실루엣 기준으로 사용하고 최대 전진 자세의 오른팔 수평 방향을 캐릭터 오른쪽으로 `35°` 옮겼다. 손은 실루엣 밖에 두고 팔꿈치는 손보다 더 바깥에 위치하도록 반복 전 구간의 보정 가중치를 유지했다.
- 최대 전진 자세는 원본과 같은 66프레임이며 오른손의 명치 높이, 약 `30°`의 팔꿈치 굽힘, 손바닥이 캐릭터 왼쪽을 향하는 방향과 동작 타이밍·반복 속도를 유지했다. `Hands_Draw_Back.controller`는 보정된 `Hands_Draw_Back_ForwardAdjusted.anim`을 계속 재생하고 `Loop=True`, `ApplyRootMotion=False`다.
- 직접 확인 뒤 2순위 보조 확인에서 69프레임씩 2루프, 총 138개 자세를 표본화했다. 정면 실루엣 대리 경계와 오른팔의 최소 간격은 약 `0.028796m`, 최대 전진 팔꿈치 굽힘은 `29.999073°`, 오른손–명치 높이 차이는 약 `0.000000477m`, 오른팔 수평 바깥 각도는 `34.999737°`, 손바닥 왼쪽 목표 오차는 `0°`였다. 실제 Animator 자세의 최대 위치 오차는 약 `0.000000147m`, 회전 오차와 루트 변위는 `0`이었고 오른팔 외 자세 차이도 `0`이었다.
- 원본 `player model/transfer grab from behind.fbx`와 Unity FBX의 SHA-256은 모두 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`로 유지됐다. `Hands_Stow_Back.controller`의 SHA-256은 `C0AFF2A9F8FE7684E9A1E776D2D84CAA212C688C392C4287DF998B64F6ADE0D5`, 공용 수송자 텍스처의 SHA-256은 `38B5E0881DC9754C314A1347A60873F263EE2D265010CFE7AE4674D8EE246528`로 유지됐으며 Hands_Stow_Back, 다른 Animator와 대상 루트는 변경하지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsDrawBackFrontSilhouetteClearance`, `CapturePlayerHandsDrawBackFrontSilhouetteClearanceReview`, `CapturePlayerHandsDrawBackFrontSilhouetteClearanceFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_draw_back_front_silhouette_clearance_2026-08-28/`에 저장했고, 직접 확인과 보조 확인을 통과한 뒤 최종 이미지를 한 번만 생성했다. 승인된 작업 범위의 남은 확인 항목은 없다.
- 사용자가 명령명과 대상 범위를 직접 지정하지 않아 Harness, EditMode 테스트, PlayMode 테스트와 Windows Build는 실행하지 않았다. 아트 샘플 제작, HTML 완료 상태 변경과 Git 작업도 실행하지 않았다.

## Hands_Draw_Back 오른쪽 가슴 늘어남 보정

- 실제 Unity Play Mode에서 원본 Take와 수정본의 반복 전 구간 12개 동일 위상을 가슴 확대 정면으로 나란히 직접 확인했다. 수정본의 캐릭터 오른쪽 가슴 외곽선·패널 폭·겨드랑이 연결부는 원본 및 왼쪽 가슴과 비슷한 자연스러운 부피를 유지했고, 이전처럼 바깥으로 길게 늘어나는 변형은 보이지 않았다. 전신 정면과 가슴 확대 측면에서도 오른팔은 몸통·얼굴 실루엣 밖에 남았으며 어깨–팔꿈치–전완–손목은 꺾임이나 메시 교차 없이 이어졌다.
- 원인은 최대 전진 자세에서 오른팔을 캐릭터 오른쪽으로 약 `35°` 벌린 이전 보정이 오른쪽 위팔에 연결된 가슴–겨드랑이 스킨을 과도하게 당긴 것이었다. `Hands_Draw_Back_ForwardAdjusted.anim` 생성 시 가슴 안전 바깥 각도를 `28°`로 낮추고, 반복 전 구간의 실루엣 간격 보정은 유지했다. 메시, 스킨 가중치, 원본 FBX와 텍스처는 수정하지 않았다.
- 최대 전진 자세와 타이밍은 66프레임으로 유지됐다. 오른손은 명치 높이, 팔꿈치는 약 `30°`, 손바닥은 캐릭터 왼쪽을 향하며 클립 길이 `2.3초`, 30fps, 반복 속도, `Loop=True`, `ApplyRootMotion=False`도 유지됐다.
- 직접 확인 뒤 2순위 보조 확인에서 69프레임씩 2루프, 총 138개 자세를 표본화했다. 최대 바깥 각도는 `34.999737°`에서 `27.999212°`로 줄었고 정면 최소 실루엣 간격은 약 `0.033862m`였다. 최대 전진 팔꿈치 굽힘은 `30.001709°`, 오른손–명치 높이 차이는 약 `0.000000477m`, 손바닥 왼쪽 목표 오차는 `0°`였다. 실제 Animator 자세의 최대 위치 오차는 약 `0.000000147m`, 회전 오차·오른팔 외 자세 차이·루트 변위는 `0`이었다.
- 원본 `player model/transfer grab from behind.fbx`와 Unity FBX의 SHA-256은 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`로 유지됐다. `Hands_Stow_Back.controller`는 `C0AFF2A9F8FE7684E9A1E776D2D84CAA212C688C392C4287DF998B64F6ADE0D5`, 공용 수송자 텍스처는 `38B5E0881DC9754C314A1347A60873F263EE2D265010CFE7AE4674D8EE246528`로 유지됐다. 수정된 `Hands_Draw_Back_ForwardAdjusted.anim`의 SHA-256은 `479FD4DEA18BA0615757CBE9F3059EC1174C685DC43684067F4675AC97B19494`다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ApplyPlayerHandsDrawBackChestDeformationFix`, `CapturePlayerHandsDrawBackChestDeformationFixReview`, `CapturePlayerHandsDrawBackChestDeformationFixFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_draw_back_chest_deformation_fix_2026-08-28/`에 저장했고 직접 검토를 통과한 접촉 시트를 최종 이미지로 한 번만 확정했다. 승인된 작업 범위의 남은 확인 항목은 없다.
- 사용자가 명령명과 대상 범위를 직접 지정하지 않아 Harness, EditMode 테스트, PlayMode 테스트와 Windows Build는 실행하지 않았다. 아트 샘플 제작, HTML 완료 상태 변경과 Git 작업도 실행하지 않았다.

## Hands_Draw_Back 오른쪽 가슴 잔여 돌출의 상태 전용 메시 교정

- 기존의 바깥 각도 `28°` 애니메이션 보정 뒤에도 남아 있던 오른쪽 가슴의 앞쪽 돌출을 원본 Take와 현재 클립의 베이크 메시로 69프레임 전수 비교했다. 최대 돌출은 13프레임의 437번 정점에서 `0.068359m`였고, `0.002m`를 넘는 영향 정점은 642개였다. 최악 정점은 `RightArm` 가중치가 `0.588218`, 몸통 가중치가 `0.042075`, 기타 가중치가 `0.369707`이어서 오른팔 회전이 가슴–겨드랑이 정점을 앞쪽으로 끌어당기는 것이 원인이었다. 팔 자세만 더 줄이면 승인된 팔 위치를 훼손하므로 애니메이션 각도 조정만으로는 해결할 수 없다고 판정했다.
- 공용 `player.fbx`의 메시와 스킨 가중치는 변경하지 않았다. 원본 메시를 복제한 `Assets/_Project/Art/Player/Generated/Hands_Draw_Back_ChestCorrected.asset`에 `Hands_Draw_Back` 상태 전용 BlendShape 두 개를 추가하고, `Hands_Draw_Back_ForwardAdjusted.anim`의 `char1` 렌더러 경로에 총 140키의 시간 곡선을 연결했다. 1차 곡선은 전체 돌출을 보정하고, 2차 곡선은 1차 보정 결과를 다시 베이크해 남은 포즈별 돌출만 상쇄한다. 생성 메시를 사용하는 렌더러는 `Hands_Draw_Back/char1` 하나뿐이며 다른 수송자는 계속 공용 플레이어 메시를 사용한다.
- 실제 Unity Play Mode의 같은 12개 위상에서 원본, 보정 전과 보정 후를 가슴 확대 정면으로 먼저 직접 비교했다. 보정 전에는 캐릭터 오른쪽 가슴이 오른팔 움직임에 따라 앞쪽으로 부풀었지만 보정 후에는 해당 돌출이 사라지고 중앙 흉판–겨드랑이 연결부가 반대편과 비슷한 두께를 유지했다. 새 함몰, 날카로운 경계와 메시 찢김은 보이지 않았다. 전신 정면과 측면에서도 오른팔은 몸통·얼굴 밖에 남았고 어깨–팔꿈치–손목이 자연스럽게 이어졌다.
- 직접 확인 뒤 2순위 보조 확인에서 최대 앞쪽 돌출은 `0.068359m`에서 `0.003616m`로 줄었고 영향 정점은 642개에서 2개로 감소했다. 69프레임씩 2루프, 총 138프레임에서 몸통–오른팔 최소 실루엣 간격은 `0.033862m`, 팔꿈치 굽힘은 `30.001709°`, 바깥 각도는 `27.999212°`, 오른손–명치 높이 차이는 약 `0.000000477m`, 손바닥 왼쪽 목표 오차는 `0°`, 루트 변위는 `0m`였다. `Loop=True`, `ApplyRootMotion=False`와 기존 동작 타이밍을 유지했다.
- 공용 플레이어 FBX의 SHA-256은 `E025F1CB33BE02D527312A25CCA2820F3F0155FF07BE1ABC7FFD54CC6074DA75`, Draw 원본 FBX는 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`, Stow Controller는 `C0AFF2A9F8FE7684E9A1E776D2D84CAA212C688C392C4287DF998B64F6ADE0D5`, 공용 수송자 텍스처는 `38B5E0881DC9754C314A1347A60873F263EE2D265010CFE7AE4674D8EE246528`로 적용 전후 동일했다. 최종 파생 클립의 SHA-256은 `0683C10EB4322E2EA3FEEC8976CAB1721394663E842E1A83C1A64674384351EA`, 상태 전용 생성 메시의 SHA-256은 `16C03FF92C1F46C6762F93BD760FD27A26F3B1C0E2CD3C5E394201BEB1F41B7C`다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `AnalyzePlayerHandsDrawBackRightChestDeformation`, `ApplyPlayerHandsDrawBackRightChestCorrection`, `CapturePlayerHandsDrawBackRightChestCorrectionReview`, `CapturePlayerHandsDrawBackRightChestCorrectionFinal`을 사용했다. 원인 분석·적용·직접 검토 자료와 한 번만 확정한 최종 이미지는 `docs/validation/player_hands_draw_back_right_chest_correction_2026-08-28/`에 저장했다. 승인된 작업 범위의 남은 확인 항목은 없다.
- 사용자가 명령명과 대상 범위를 직접 지정하지 않아 Harness, EditMode 테스트, PlayMode 테스트와 Windows Build는 실행하지 않았다. 아트 샘플 제작, HTML 완료 상태 변경과 Git 작업도 실행하지 않았다.

## Hands_Draw_Back 지정 FBX 내장 Take 재연결

- 사용자 지시에 따라 `Hands_Draw_Back.controller`에서 `Hands_Draw_Back_ForwardAdjusted.anim` 연결을 해제하고, `player model/transfer grab from behind.fbx`의 Unity 복사본에 내장된 유일한 Take `mixamo.com`을 다시 직접 연결했다. 보정 `.anim` 파일 자체는 삭제하지 않았고 키프레임·곡선·자세를 추론하거나 새로 생성하지 않았다.
- Controller는 단일 `Base Layer`와 단일 `HandsDrawBack` 상태만 사용한다. 상태가 참조하는 FBX sub-asset의 GUID는 `c8b1ceadaee060747b73cd8307955d28`, fileID는 `-203655887218126122`이며 Take는 `2.3초`, 30fps, float curve 60개, 이벤트 0개다. `Speed=1`, `Loop=True`, `ApplyRootMotion=False`, `AlwaysAnimate`로 반복 재생한다.
- 실제 Unity Play Mode에서 원본 Take와 Controller 적용 결과를 정면·측면 8개 동일 위상으로 나란히 직접 확인했다. 위쪽 원본 두 행과 아래쪽 적용 두 행의 자세·팔다리 움직임·실루엣이 전 구간에서 일치했고, 임의 보정이나 별도 생성 동작이 섞이지 않았다. 기존 연보라색 왼팔과 `Hands_Draw_Back_ChestCorrected.asset` 메시 연결도 그대로 유지됐다.
- 직접 확인 뒤 2순위 보조 확인에서 69프레임씩 2루프, 총 138프레임의 원본 대비 최대 위치 차이는 약 `0.000000121m`, 회전 차이와 루트 변위는 `0`이었다. 원본 FBX와 Unity 복사본의 SHA-256은 모두 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`로 적용 전후 동일했다.
- 연결 해제된 `Hands_Draw_Back_ForwardAdjusted.anim`의 SHA-256은 적용 전후 `479FD4DEA18BA0615757CBE9F3059EC1174C685DC43684067F4675AC97B19494`, `Hands_Stow_Back.controller`는 `C0AFF2A9F8FE7684E9A1E776D2D84CAA212C688C392C4287DF998B64F6ADE0D5`로 동일했다. 대상 루트, 대상 메시 경로와 다른 Animator도 변경하지 않았다.
- Unity 반영에는 `./scripts/Refresh-UnityProject.ps1`, `ReconnectPlayerHandsDrawBackExactMixamo`, `CapturePlayerHandsDrawBackExactMixamoReview`, `CapturePlayerHandsDrawBackExactMixamoFinal`을 사용했다. 적용·검토 자료는 `docs/validation/player_hands_draw_back_exact_mixamo_reconnect_2026-08-29/`에 저장했고, 직접 확인과 보조 확인을 통과한 뒤 최종 이미지를 한 번만 확정했다.
- 사용자가 명령명과 대상 범위를 직접 지정하지 않아 Harness, EditMode 테스트, PlayMode 테스트와 Windows Build는 실행하지 않았다. 아트 샘플 제작, HTML 완료 상태 변경과 Git 작업도 실행하지 않았다.

## Hands_Draw_Back 위쪽 지향 동작의 전방 인출 수정 계획 — 미적용

- 사용자 영상 `C:/Users/gus68/Videos/Captures/Bellerophon - CargoRunMvp - Windows, Mac, Linux - Unity 6.3 LTS (6000.3.16f1) _DX12_ 2026-08-29 00-36-52.mp4`의 6.422233초 전체를 4fps 26개 표본과 2.5fps 확대 16개 표본으로 직접 확인했다. 오른손은 머리 옆·등 뒤에서 빠져나온 뒤 명치 앞쪽으로 진행하지 않고, 오른팔꿈치와 손이 함께 머리 위 대각선과 거의 수직 위쪽까지 올라갔다. 두 번째와 세 번째 반복에서도 같은 궤적이 나타났다.
- 현재 `Hands_Draw_Back.controller`는 `Hands_Draw_Back_Mixamo.fbx`의 `mixamo.com` Take를 직접 참조하고 있으며 파생 클립은 연결돼 있지 않다. 그러므로 영상의 위쪽 지향은 Controller 연결 오류가 아니라 지정 FBX 원본 Take 자체의 오른팔 궤적이다. 직접 확인 자료는 `docs/validation/player_hands_draw_back_video_analysis_2026-08-29/`에 기록했다.
- 구현 시 원본 `player model/transfer grab from behind.fbx`와 Unity 복사본은 수정하지 않는다. 원본 2.3초·30fps·69프레임을 그대로 표본화한 `Hands_Draw_Back_ForwardAdjusted.anim` 파생 클립을 다시 생성하고, `RightArm`, `RightForeArm`, `RightHand`의 회전 곡선만 바꾼다. 몸통, 목·머리, 왼팔, 하체, 손가락, 루트와 원본 이벤트는 그대로 유지한다.
- 이전 원본 측정에서 등 뒤 인출 시작은 43프레임, 최대 도달은 66프레임이었으나 구현 직전에 현재 원본 Take에서 같은 특징점인지 다시 측정한다. 일치할 때만 해당 범위를 사용하며, 다르면 추측하지 않고 실제 재측정 프레임을 기준으로 한다.
- 인출 시작부터 최대 도달까지 캐릭터 로컬 공간의 손 목표를 위쪽이 아니라 정면으로 재배치한다. 최대 도달 자세는 이전 사용자 기준인 오른손 명치 높이, 어깨→손 수평 방향의 캐릭터 정면, 약 30도의 팔꿈치 굽힘, 실제 오른손바닥의 캐릭터 왼쪽 방향을 목표로 한다. 팔꿈치는 몸통 오른쪽 실루엣 밖으로 먼저 나오고 손은 팔꿈치보다 덜 바깥에 두어 몸통·얼굴과 겹치지 않게 한다.
- 어깨–팔꿈치–손목의 실제 뼈 길이를 보존하는 2-bone IK로 위팔과 아래팔을 함께 풀고, 팔꿈치 pole은 캐릭터 오른쪽 바깥·약간 아래에 둔다. 손목 방향에 필요한 잔여 회전은 전완과 손에 분산하고 Quaternion 부호 연속성을 유지해 손목 꺾임, 팔꿈치 역꺾임과 프레임 사이 튐을 막는다.
- 반복 경계는 원형 구간으로 취급한다. 보정 가중치는 등 뒤 인출 전에 0에서 완만히 시작해 전방 도달 구간에 도달하고, 클립 끝과 시작이 같은 전방 자세·각속도로 이어진 뒤 원본 복귀 구간에서 완만히 0으로 내려가게 한다. 클립 길이, 30fps, 상태 `Speed=1`, 반복 속도와 `Loop=True`, `ApplyRootMotion=False`는 유지한다.
- 현재 상태 전용 `Hands_Draw_Back_ChestCorrected.asset`과 공용 모델·텍스처는 수정하지 않는다. 팔 자세 변경 뒤 오른쪽 가슴 돌출이 다시 보이면 먼저 팔 목표와 팔꿈치 궤적을 조정하고, 메시·BlendShape 추가 수정은 별도 원인 분석과 사용자 승인 없이는 수행하지 않는다.
- 적용 후 1순위 검증은 실제 Unity Play Mode에서 현재 위쪽 원본과 전방 수정본을 같은 12개 위상으로 전신 정면·측면, 오른팔 확대 정면·측면, 오른쪽 가슴 확대 정면에서 나란히 직접 확인하는 것이다. 손이 등 뒤에서 몸 바깥으로 빠져 명치 앞쪽으로 이동하는지, 몸통·얼굴 교차와 관절 꺾임·가슴 돌출·반복 경계 튐이 없는지를 먼저 판정한다.
- 직접 확인을 통과한 뒤에만 2순위 보조 검증으로 최대 도달의 정면 방향 오차 `5° 이하`, 손–명치 높이 차이 `0.02m 이하`, 팔꿈치 굽힘 `25°~35°`, 오른팔 외 곡선 차이 `0`, 루트 변위 `0m`, 69프레임씩 2루프, `Loop=True`, `ApplyRootMotion=False`를 확인한다. 이 계획은 아직 Unity에 적용하지 않았다.

## 2026-08-29 기준 플레이어 자세·애니메이션 누적 작업 기록

- `Player_Crouch_Diagonal`은 전진·후진·좌우 입력을 조합하는 대각선 Blend Tree로 구성했고, `Player_Crouch_Exit`는 지정 Mixamo 일어서기 Take 뒤에 서 있는 Idle 첫 자세로 이어지는 유지 구간을 적용했다. 두 대상의 원본 FBX와 다른 Animator는 보존하고 직접 비교 및 반복 확인 자료를 `docs/validation/player_crouch_diagonal_exit_2026-08-28/`에 남겼다.
- `Hands_Empty_Idle`, `Hands_Carry_OneHand`, `Hands_Carry_TwoHand`를 플레이어 애니메이션 배치에 연결했다. OneHand는 Empty 몸통을 기준으로 왼팔을 몸통 밖 아래로 내리고 오른팔–전완–손목을 자연스럽게 이어 실제 오른손바닥이 캐릭터 왼쪽을 향하게 했으며, TwoHand와 Empty를 포함한 세 대상은 사용자 확인 뒤 요구사항 HTML에서 완료 표시했다.
- 모든 수송자 모델의 왼팔 미국 국기 영역을 제거하고 연보라색으로 통일했으며 다른 텍스처 영역은 유지했다. 실행 시작 시 `Hands_Empty_Idle`이 정면에 보이도록 플레이어 시작 위치·방향을 맞췄다.
- `Hands_Draw_Back`과 `Hands_Stow_Back`에는 각각 `transfer grab from behind.fbx`, `transfer put back.fbx`의 내장 `mixamo.com` Take를 연결하고 반복 재생했다. 이후 Draw의 오른팔 전방 각도, 명치 높이, 약 30도 팔꿈치, 손바닥 왼쪽, 몸 바깥 인출과 몸통·얼굴 실루엣 분리를 단계별로 조정했다.
- Draw 오른팔 조정 과정에서 오른쪽 가슴–겨드랑이 스킨이 앞으로 늘어나는 원인을 분석했다. 상태 전용 `Hands_Draw_Back_ChestCorrected.asset`을 만들어 공용 수송자 메시와 다른 개체는 보존했으며, 직접 비교에서 남은 돌출을 줄인 기록과 검증 자료를 `docs/validation/player_hands_draw_back_right_chest_correction_2026-08-28/` 및 영상 후속 경로에 남겼다.
- 마지막 사용자 지시에 따라 Draw의 보정 애니메이션 연결을 해제하고 지정 FBX 내장 Take를 정확히 다시 연결했다. 원본과 Unity 복사본 SHA-256은 `A4AD3D660627A34D47A38811E688C2B19416C57146580963279459F6D4EC396B`로 동일하고, 보정 `.anim`, Stow Controller, 대상 메시와 다른 Animator는 삭제하거나 수정하지 않았다.
- 이번 00:36:52 영상 직접 확인으로 정확히 재연결한 원본 Take가 등 뒤 인출 뒤 오른팔을 전방이 아니라 위쪽으로 올린다는 사실을 확인했다. 위 `Hands_Draw_Back 위쪽 지향 동작의 전방 인출 수정 계획`은 다음 구현 승인용 계획이며 현재 씬과 애니메이션에는 아직 적용하지 않았다.
- 지금까지 애니메이션 작업에는 `artSample/`을 만들지 않았다. 사용자가 명령명과 대상 범위를 직접 지정하지 않은 Harness, EditMode 테스트, PlayMode 테스트와 Windows Build도 실행하지 않았다.
