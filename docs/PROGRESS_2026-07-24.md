# 진행 상황 - 2026-07-24

## 완료 - 승인된 니게티프 GLB 외형 Unity 적용

- 사용자가 최종 샘플을 승인해 `artSample/enemies/negatif/glb_appearance_sync/APPROVAL_STATUS.json`을 `APPROVED`, `approved_for_unity=true`로 기록했습니다.
- 프로젝트에 이미 설치된 `com.unity.cloud.gltfast 6.15.1`을 사용했으며 패키지 추가·Unity 재시작·별도 FBX 변환은 하지 않았습니다.
- 승인 산출물 `exports/Negatif_Glb_AppearanceSync.glb`을 `Assets/_Project/Art/Enemies/Negatif/Models/Negatif_Glb_ApprovedAppearance.glb`로 바이트 단위 복사했습니다. 두 파일 SHA-256은 `0E19530D01B15E79DFDF23D9B951C5E5B8C72496C568584CBCCEEA7C6A41AC60`으로 일치합니다.
- 원본 `enemies model/négatif.glb` SHA-256은 `BD27F171F84E212273C841A2BAE44832519CEA0F0201C0D3FBD5707F870CE8E8`로 유지했습니다.
- `CargoRunMvp`의 `Approved Negatif Enemy Placement` 아래 7개 슬롯에서 기존 `Negatif_Model` 자식만 승인 GLB 직접 인스턴스로 교체했습니다. 배치 루트·7개 슬롯 Transform과 다른 씬 루트는 적용 전후 동일합니다.
- 각 모델은 본체 렌더러 1개와 좌우 눈 렌더러 2개, 총 `7749`삼각형, 본체 `46`본, 좌우 눈 각 `676`삼각형이며 승인된 눈 개체 이름과 메시를 그대로 사용합니다.
- 18개 Unity 텍스처는 승인 샘플 텍스처와 각각 SHA-256이 일치해 기존 `.meta`를 보존하고 재사용했습니다.
- 승인 샘플 생성기와 Unity 재질 수치를 최종 대조해 가죽끈 요철 `0.72`, 가죽 색 곱 결과 `(0.7228, 0.6157, 0.59176)`, 눈 기본색 `(1, 0.1, 0.002)`, 눈 발광색 `(1, 0.18, 0.004)`, 발광 강도 `9.0`을 동일하게 반영했습니다.
- `ApplyNegatifApprovedGlbAppearance` 적용 결과와 저장 후 `InspectNegatifApprovedGlbAppearance` 검사는 모두 `PASS`입니다. 검사 결과는 `DirectGlbInstances=7`, `RenderersPerModel=3`, `EyesPerModel=2`, `TrianglesPerModel=7749`, `BonesPerModel=46`, `Materials=6`, `Height=0.500003`입니다.
- Unity 캡처는 `Logs/Negatif_Glb_ApprovedAppearance_Unity.png`에 한 번 생성했으며 씬 상태를 변경하지 않았습니다. 최근 Unity 에디터 로그 500줄에는 새 오류·예외가 없습니다.
- 금지된 Harness/EditMode/PlayMode/Build 및 Smoke/Validate/Ensure 계열 명령, 애니메이션·AI·전투 구현, Git 작업은 실행하지 않았습니다.

## Git 기록 준비 - 니게티프 작업

- 사용자의 요청에 따라 위 니게티프 샘플·Unity 에셋·배치 도구·브리지 명령·진행 문서를 `main` 브랜치에서 커밋하고 `origin/main`으로 푸시하는 범위로 정리했습니다.
- `CargoRunMvp.unity`는 Git LFS 파일이라 니게티프 씬 변경만 부분 스테이징할 수 없습니다. 사용자에게 이 제약을 알렸고, 기존 미커밋 씬 상태를 포함한 현재 씬 파일 전체를 이번 커밋에 포함하도록 명시 승인받았습니다.
- 공유 파일 `UnityEditorValidationBridge.cs`는 니게티프 관련 브리지 명령만 부분 스테이징하며, 작업 트리에 함께 존재하는 돌로레·오스티나토·그라베 관련 일반 파일 변경은 이번 커밋에서 제외합니다.
- 커밋 대상에는 기존 FBX 기반 니게티프 샘플 제작 이력, 최종 GLB 승인 샘플, Unity 니게티프 에셋, 전용 배치·검사 도구, 원본 모델의 FBX→GLB 교체, 관련 샘플 생성 스크립트가 포함됩니다.

## 완료 - 니게티프 GLB 눈 전진 위치 보정

- 작업 대상 샘플은 `artSample/enemies/negatif/glb_appearance_sync`이며 이후 사용자 승인에 따라 위 Unity 적용을 완료했습니다.
- 사용자가 기준 이미지와 비교해 눈이 여전히 뒤에 있다고 지적해, 코끝–눈–귀 상대 위치를 다시 대조하고 앞쪽 얼굴 표면을 측정했습니다.
- 양쪽 눈을 이전 중심 `X=±0.262566, Y=-1.427052, Z=1.211955`에서 `X=±0.265533, Y=-1.553142, Z=1.198604`로 옮겼습니다. 주둥이 방향 추가 이동량은 `0.12609m`입니다.
- 새 위치는 원본 GLB의 좌우 대칭 얼굴 면 `5836/5837` 중심이며, 표면 법선은 `X=±0.883422, Y=-0.058121, Z=0.464960`입니다. 두 면 모두 `Bone_025`가 지배 본입니다.
- 눈 개체는 좌우 한 개씩 총 두 개이며 각 `342`정점·`342`면입니다. 원형 소켓·금속 링·렌즈 크기, 재질, 발광 강도 `9.0`은 변경하지 않았습니다.
- 원본 GLB SHA-256은 `BD27F171F84E212273C841A2BAE44832519CEA0F0201C0D3FBD5707F870CE8E8`로 유지됐습니다. 주 메시 `17771`정점·`6397`면, 보조 메시 `42`정점·`80`면과 각 형상 서명, 리그 `46본`도 전후 일치합니다.
- Blender 샘플의 좌우 대칭 오차는 X 합 `0`, Y 차 `0.00000012`, Z 차 `0`입니다. 내보낸 GLB를 빈 씬에 재수입해도 눈 개체 두 개와 46본 리그가 유지되어 검증 결과는 `PASS`입니다.
- 기준 이미지와 보정 결과 비교 이미지는 `renders/06_reference_eye_comparison.png`입니다.
- Unity 명령, 일반 테스트·빌드, Smoke/Validate/Ensure 계열 명령, 원본 GLB 수정, 애니메이션·AI·전투 구현, Git 커밋·푸시는 실행하지 않았습니다.

## 완료 - 니게티프 대기 눈 발광 반복 모션

- 대상은 `Approved Negatif Enemy Placement/Negatif_01_Idle`이며 기계 유닛이라는 사용자 지시에 따라 호흡 모핑, BlendShape, 몸체 Transform, 루트 이동을 넣지 않았습니다.
- Animator는 `NegatifEyeEmissionPulse.emissionStrength`를 `0초=9.0`, `1.5초=2.0`, `3초=9.0`으로 구동하며, 런타임 컴포넌트가 `MaterialPropertyBlock`을 통해 좌우 눈 렌더러에 실제 발광 강도를 적용합니다.
- 적용 파일은 `NegatifEyeEmissionPulse.cs`, `Negatif_01_Idle_EyeEmission.anim`, `Negatif_01_Idle_EyeEmission.controller`입니다. Animator와 런타임 컴포넌트는 대기 슬롯 루트에만 연결하고 Root Motion을 비활성화했습니다.
- 최초 구현의 다중 재질 `Renderer` 직접 속성 바인딩은 실행 화면에서 밝기 변화가 보이지 않았습니다. 해당 바인딩을 폐기하고 전용 런타임 컴포넌트 방식으로 교체했습니다.
- `ApplyNegatifIdleEyeEmission`으로 수정 상태를 저장한 뒤 `CaptureNegatifIdleEyeEmissionVisualReview`를 실제 Play Mode에서 실행해 같은 카메라·노출의 `0초/1.5초/3초` 패널을 한 장에 생성했습니다. 최종 이미지는 `Logs/Negatif_Idle_EyeEmission_VisualReview.png`입니다.
- 최종 비교 이미지를 직접 확인한 결과 0초와 3초 눈은 같은 밝은 노란색으로 보이고, 1.5초 눈은 명확히 더 어두운 주황빛으로 내려갑니다. 얼굴과 몸체의 위치·형상은 세 패널에서 동일합니다.
- 다른 니게티프 슬롯과 다른 씬 루트는 변경하지 않았습니다. 이동·공격·피격·도주·사망 모션, 일반 테스트·빌드, Smoke/Validate/Ensure 계열 명령, Unity 재시작, Git 작업은 실행하지 않았습니다.

## 완료 - 니게티프 사족보행 이동 모션

- 대상은 `Approved Negatif Enemy Placement/Negatif_02_Move`입니다. 돌로레의 FBX 내장 이동 클립을 실제 Play Mode에서 확인한 결과, 1초 주기로 대각선 다리 쌍이 번갈아 지지하고 25%·75%에서 다리를 접어 교차하는 빠른 사족보행이었습니다.
- 니게티프 리그의 네 다리는 전방 `Bone_030→026`, `Bone_035→031`과 후방 `Bone_006→002`, `Bone_011→007`입니다. 최초 조사에서 후방 다리로 잘못 분류한 `Bone_021→016` 및 `Bone_040/045` 계열은 꼬리 변형 리그로 정정했으며 이동 커브에서 제외했습니다.
- `Negatif_02_Move_Quadruped.anim`은 전방 좌측–후방 우측과 전방 우측–후방 좌측을 반 주기 차이로 구동합니다. 5단 관절 체인에 CCD 방식으로 발 목표를 계산해 리그 회전을 기록했고, 보폭 `0.75`, 발 들림 `0.28`, 몸통 상하폭 `0.05`, 루프 길이 `1초`를 사용했습니다.
- `Negatif_02_Move_Quadruped.controller`를 이동 슬롯의 Animator에만 연결하고 Root Motion을 비활성화했습니다. 다른 니게티프 슬롯, 다른 씬 루트, 모델·메시·텍스처·머티리얼은 변경하지 않았습니다.
- `ApplyNegatifMoveAnimation`으로 클립·컨트롤러·씬 연결 상태를 저장했습니다. `CaptureNegatifMoveVisualReview`는 실제 Play Mode에서 돌로레와 니게티프를 각각 `0.05/0.25/0.5/0.75/0.99초`에 촬영해 `Logs/Negatif_Move_VisualReview.png`에 위·아래 행으로 구성했습니다.
- 최종 이미지를 직접 확인한 결과 니게티프의 네 다리가 돌로레와 같은 대각선 교대 순서로 움직이고, 지지 자세와 다리 교차가 분리되어 보이며, 0.05초와 0.99초 자세가 자연스럽게 이어집니다. 꼬리에는 보행 커브가 없고 루트 위치도 유지됩니다.
- 일반 테스트·빌드, Smoke/Validate/Ensure 계열 명령, Unity 재시작, Git 작업은 실행하지 않았습니다.

## 완료 - 니게티프 후족 기립 교대 할퀴기 공격 모션

- 대상은 `Approved Negatif Enemy Placement/Negatif_03_Claw_Attack`입니다. 사용자 확인에 따라 공격 순서는 네 발 기본 자세 → 후방 두 발 지지 → 몸통·허리 기립 → 왼쪽 앞발 할퀴기 → 오른쪽 앞발 할퀴기 → 네 발 자세 복귀로 구성했습니다.
- 사용자가 제공한 `2026-07-24 14-09-18.mp4`를 직접 확인한 결과, 최초 구현은 `Bone_001`을 `Bone_000`과 상대적으로 이동·회전시켜 몸통 혼합 웨이트와 주둥이·수염 메시가 늘어나고, 후방 다리가 거의 일직선으로 잠기는 문제가 있었습니다.
- 수정된 `Negatif_03_Claw_Attack_Alternating.anim`은 총 `1.9초` 루프입니다. `Bone_001` 위치·회전 커브와 얼굴·수염 커브를 제거하고 `Negatif_Model` 전체를 후방 지지점 주위에서 강체 회전시켜 몸통과 주둥이 실루엣을 보존합니다.
- 기립각은 `56도`에서 `48도`로 낮추고 엉덩이를 몸길이의 `18%`만큼 내려, 전방 좌측 `Bone_030→026`, 전방 우측 `Bone_035→031`, 후방 좌측 `Bone_006→002`, 후방 우측 `Bone_011→007`의 후방 지지 관절이 굽혀지도록 했습니다. 후방 발끝은 CCD로 원래 지면 지지점에 고정하며 꼬리 계열에는 커브를 추가하지 않았습니다.
- 왼발과 오른발은 각각 준비–정면 하향 타격–회수 궤적을 유지하되 한 발당 전체 할퀴기 구간을 `0.5초`에서 `0.25초`로 줄였습니다. 준비점에서 타격점까지의 시간은 `0.15초`에서 `0.075초`로 줄어 기존 대비 정확히 2배 빠릅니다.
- `Negatif_03_Claw_Attack_Alternating.controller`를 공격 슬롯의 Animator에만 연결하고 Root Motion을 비활성화했습니다. 다른 니게티프 슬롯, 모델·메시·텍스처·머티리얼과 다른 씬 루트는 변경하지 않았습니다.
- `ApplyNegatifClawAttackAnimation`으로 클립·컨트롤러·씬 연결 상태를 저장했습니다. `CaptureNegatifClawAttackVisualReview`는 실제 Play Mode에서 `0.05/0.65/0.75/0.825/1.0/1.075/1.4/1.85초`를 촬영해 `Logs/Negatif_ClawAttack_VisualReview.png`로 구성했습니다.
- 최종 이미지를 직접 확인한 결과 후방 관절이 굽혀진 낮은 지지 자세, 왼발과 오른발의 빠른 순차 할퀴기, 네 발 기본 자세 복귀가 분리되어 보입니다. 공격 전후 몸통·주둥이·수염의 길이와 실루엣이 유지되며, 꼬리는 공격 발처럼 움직이지 않고 루트 위치도 유지됩니다.
- 사용자가 제공한 후속 영상 `2026-07-24 14-26-37.mp4`에서는 후방 발목이 몸통 회전을 따라 기울고, 할퀴기 때 몸통 접합부와 수염이 다시 끌리는 문제가 확인됐습니다. 후방 체인은 상위 `Bone_006/005/004`, `Bone_011/010/009`만 접지 보정하고 발목·발 `Bone_003/002`, `Bone_008/007`은 공격 내내 원래 월드 각도와 지면 방향을 유지하도록 변경했습니다.
- 앞다리는 몸통 접합부인 `Bone_030/029`, `Bone_035/034`의 공격 커브를 제거했습니다. 할퀴기는 말단 `Bone_028/027/026`, `Bone_033/032/031`만 사용해 동일한 2배 속도와 왼발→오른발 순서를 유지하며, 몸통·주둥이·수염에 영향을 주는 상위 웨이트를 움직이지 않습니다.
- 최종 측면 사선 Play Mode 캡처를 직접 확인한 결과 후방 발목·발은 기립 전후 같은 각도를 유지하고 상위 다리 관절이 몸통 회전을 보상합니다. 두 타격 프레임에서 가슴 외곽과 주둥이·수염 실루엣은 유지되며, 정면 캡처에서 밝게 보였던 삼각형은 늘어난 몸통이 아니라 회전한 금속 발톱 면으로 확인했습니다.
- 사용자가 제공한 추가 영상 `2026-07-24 14-35-17.mp4`에서는 후방 접지 CCD가 다리를 아래로 당기고, 주둥이 아래에 밝은 삼각 형상이 계속 나타나는 문제가 확인됐습니다. 후방 다리의 CCD와 위치 목표를 완전히 제거하고 `Bone_006/005/004`, `Bone_011/010/009`의 회전만 기록해 모든 본의 로컬 위치와 본 간격을 원본 그대로 유지했습니다.
- 공격 슬롯의 Renderer 구성을 다시 확인한 결과 몸체 SkinnedMeshRenderer 1개와 눈 Renderer 2개뿐이며 별도 수염 오브젝트는 없습니다. 밝은 삼각형은 몸체 스킨드 메시에서 앞다리 두 관절을 CCD로 서로 다르게 접을 때 웨이트 면이 벌어져 생긴 형상이었습니다.
- 앞다리 CCD를 제거하고 좌우 단일 힌지 `Bone_028`, `Bone_033`만 회전하도록 변경했습니다. 각 힌지 아래 발톱 체인은 원래 로컬 회전을 유지한 채 강체처럼 함께 움직이며, `0.65~1.4초` 기립·타격 구간 전체에서 주둥이 아래 삼각 형상이 나타나지 않습니다.
- 최종 측면 Play Mode 캡처를 직접 확인한 결과 후방 다리는 아래로 늘어나지 않고 관절 각도만 바뀌며 발바닥 평행 상태를 유지합니다. 주둥이·수염 실루엣과 몸통 외곽도 유지되고, 왼발→오른발 순서 및 기존 2배 속도는 변경되지 않았습니다.
- 사용자가 제공한 후속 영상 `2026-07-24 14-46-51.mp4`와 사용자 정정을 기준으로 다시 확인한 결과, 주둥이 수염 면은 기본 모델 문제가 아니라 공격 커브가 `Bone_028/Bone_033`을 회전시키면서 해당 본의 혼합 웨이트를 끌어당긴 현상이었습니다. 모델링 파생안은 폐기하고 원본 모델·메시·스킨 웨이트·머티리얼은 수정하지 않았습니다.
- 앞발 공격 커브를 수염 영향 범위에서 더 떨어진 말단 힌지 `Bone_027/Bone_032`로 옮겼습니다. 과도하게 주둥이 쪽으로 접히지 않도록 상단 준비각은 `-16도`, 하향 타격각은 `+74도`로 구성해 상·하 흔들기 범위를 기존 48도에서 90도로 확대했습니다. 좌우 한 발당 `0.25초`, 준비–타격 `0.075초`의 2배 속도와 왼발→오른발 순서는 유지했습니다.
- 정면과 정면 사선 실제 Play Mode 캡처를 직접 확인한 결과 공격 전·왼발 준비/타격·오른발 준비/타격·복귀 프레임에서 수염과 몸통 외곽이 늘어나지 않았고, 앞발의 확대된 하향 타격 폭이 확인됐습니다. 후방 다리의 회전 전용 구성과 발바닥 평행 상태도 유지됩니다.
- 사용자가 제공한 후속 영상 `2026-07-24 15-06-23.mp4`와 정면 근접 Play Mode 캡처를 다시 직접 확인한 결과, 위 `Bone_027/Bone_032` 판정은 잘못됐습니다. 이 계층은 실제 앞발이 아니라 주둥이 수염 `Bone_030→026`, `Bone_035→031`의 일부였으며, 공격 커브가 수염 메시를 삼각 평면처럼 들어 올리고 실제 앞발은 움직이지 않았습니다. 해당 커브와 `-16도/+74도` 구성은 폐기했습니다.
- 승인 GLB의 리그 계층과 모델 전후 위치를 대조해 실제 앞발을 `Bone_006→005→004→003→002`, `Bone_011→010→009→008→007`, 실제 뒷발을 `Bone_040→039→038→037→036`, `Bone_045→044→043→042→041`로 정정했습니다. 수염 끝은 모델 전방 Z `1.341`, 실제 앞발 끝은 Z `-0.169/-0.151`, 실제 뒷발 끝은 Z `-1.039/-1.004`에 있습니다.
- 최종 공격은 실제 뒷발 상위 세 관절만 회전해 낮은 기립 자세를 지지하고 발목·발의 월드 각도를 유지합니다. 실제 앞발 어깨 `Bone_006/Bone_011`은 각각 준비 위치 `0도`에서 몸통 밖 정면 아래 방향 `-60도`까지 회전한 뒤 복귀하며, 왼발 `0.75~0.9초` 다음 오른발 `1.0~1.15초` 순서로 교대합니다. 한 발당 `0.15초`, 준비–최대 타격 `0.075초`의 기존 2배 속도를 유지합니다.
- 수염 계층 `Bone_030→026`, `Bone_035→031`, 몸통 `Bone_001`, 꼬리에는 공격 커브가 없습니다. 원본 모델·메시·스킨 웨이트·머티리얼도 수정하지 않았습니다.
- `CaptureNegatifClawAttackVisualReview`는 실제 Play Mode Animator를 `0.05/0.65/0.7/0.75/0.775/0.8/0.825/0.85/0.875/0.9/0.95/1.0/1.025/1.05/1.075/1.1/1.125/1.15/1.4/1.85초`에 정확히 평가해 `Logs/Negatif_ClawAttack_VisualReview.png`를 생성합니다. 최종 정면 사선 20프레임을 직접 확인한 결과 좌우 앞발의 정면 60도 상하 교대, 수염 삼각 평면 소멸, 몸통·주둥이·수염 메시 무변형, 뒷발 지지와 네 발 복귀가 모두 확인됐습니다.
- 사용자 실행 화면 확인에 따라 메시 평면 없이 앞발이 정상적으로 교대하는 현재 공격 자세를 최종 기준으로 유지했습니다. 원본 모델·메시·리깅·가중치·머티리얼은 수정하지 않았습니다.
- 공격 반복은 기존 왼발→오른발 한 세트의 속도와 60도 범위를 유지한 채 `0.75/1.25/1.75/2.25/2.75초`에 시작하도록 확장했습니다. 총 5세트가 `3.15초`에 끝난 뒤 기립 유지와 하강을 거쳐 `3.9초`에 네 발 자세로 복귀합니다.
- `CaptureNegatifClawAttackVisualReview`는 3.9초 클립을 실제 Play Mode에서 24개 시점으로 평가합니다. 최종 캡처를 직접 확인한 결과 다섯 구간 모두 동일한 왼발→오른발 순서를 유지하고, 뒷발 지지·메시 평면 제거 상태·마지막 네 발 복귀가 유지됩니다.
- 일반 테스트·빌드, Smoke/Validate/Ensure 계열 명령, Unity 재시작, Git 작업은 실행하지 않았습니다.

## 완료 - 니게티프 왼쪽 피격 모션

- 대상은 `Approved Negatif Enemy Placement/Negatif_04_Hit_Reaction`이며, 총 길이는 `0.7초`입니다. `0초` 정자세에서 시작해 `0.14초`에 니게티프 자신의 왼쪽으로 `15도` 기울고 `0.7초`에 정자세로 완전히 복귀합니다.
- 피격 모션은 어떤 본도 움직이지 않고 `Negatif_Model` 전체를 보이는 메시 하단 중심 주위로 강체 회전시킵니다. 슬롯 루트와 Root Motion은 고정되며 몸통·다리·주둥이·수염 메시의 상대 형태는 변형되지 않습니다.
- `Negatif_04_Hit_Reaction_Left.anim`과 `Negatif_04_Hit_Reaction_Left.controller`를 생성해 피격 슬롯의 Animator에만 연결하고 `CargoRunMvp` 장면을 저장했습니다. 다른 니게티프 슬롯과 원본 모델·메시·리그·스킨 웨이트·텍스처·머티리얼은 변경하지 않았습니다.
- `CaptureNegatifHitReactionVisualReview`로 실제 Play Mode Animator를 `0/0.07/0.14/0.28/0.45/0.6/0.7초`에 평가해 `Logs/Negatif_HitReaction_VisualReview.png`를 생성했습니다. 최종 시트를 직접 확인한 결과 정면 기준 화면 오른쪽인 니게티프 자신의 왼쪽으로 짧게 움찔하고, 메시 늘어남이나 수염 분리 없이 원래 위치와 자세로 복귀합니다.
- 일반 테스트·빌드, Smoke/Validate/Ensure 계열 명령, Unity 재시작, Git 작업은 실행하지 않았습니다.

## 완료 - 니게티프 고속 도주 모션

- 대상은 `Approved Negatif Enemy Placement/Negatif_05_Flee`입니다. 이동 모션의 보폭 `0.75`와 발 들림 `0.28`을 유지하면서 보행 주기를 `1초`에서 `0.4초`로 줄여 네 다리를 정확히 `2.5배` 빠르게 구동합니다.
- 최초 도주 구현은 공격 모션 조사 과정의 리그 분류를 잘못 재사용해 `Bone_006/011`과 `Bone_040/045` 체인을 새로 계산했습니다. 이 방식은 승인된 이동 모션의 실제 바인딩과 달라 앞쪽 두 발이 움직이지 않았으므로 폐기했습니다.
- 최종 도주 클립은 `Negatif_02_Move_Quadruped.anim`의 모든 Transform 곡선을 경로와 값 변경 없이 직접 복사합니다. 원본 1초 구간의 시간만 `1/2.5`로 압축해 0.4초마다 반복하므로 앞발과 뒷발의 자세·대각선 교대 순서는 승인된 이동 모션과 같습니다.
- 꼬리 계층 `Bone_021→016`은 루트 `Bone_021`만 월드 수직축 기준으로 회전해 자식 마디의 원래 간격을 유지합니다. 꼬리는 중앙 기준 좌우 각각 `45도`, `3초` 주기로 흔들립니다.
- 다리 `0.4초` 주기와 꼬리 `3초` 주기가 모두 끊김 없이 이어지는 최소 공통 길이인 `6초` 루프로 구성했습니다. 6초 동안 다리는 15주기, 꼬리는 2주기를 완료하며 슬롯 Root Motion과 몸통 본 커브는 사용하지 않습니다.
- `Negatif_05_Flee_QuadrupedTail.anim`과 `Negatif_05_Flee_QuadrupedTail.controller`를 생성해 도주 슬롯의 Animator에만 연결하고 `CargoRunMvp` 장면을 저장했습니다. 다른 니게티프 슬롯과 원본 모델·메시·리그·스킨 웨이트·텍스처·머티리얼은 변경하지 않았습니다.
- `CaptureNegatifFleeVisualReview`로 실제 Play Mode Animator를 `0/0.1/0.2/0.3/0.4/0.75/1.5/2.25/3/3.75/4.5/5.25/6초`에 평가해 `Logs/Negatif_Flee_VisualReview.png`를 생성했습니다. 최종 후방 사선 시트를 직접 확인한 결과 첫 0.4초에 대각선 다리 한 주기가 완료되고, 꼬리는 0.75초와 2.25초에서 반대쪽 최대 방향을 가리킨 뒤 3초에 중앙으로 복귀하며 후반 3초에 같은 동작을 반복합니다. 0초와 6초 자세가 연결되고 다리·꼬리 메시의 길이와 연결 상태도 유지됩니다.
- 사용자 제공 영상 `2026-07-24 16-41-50.mp4`를 직접 확인한 결과 최초 구현에서는 뒤쪽 다리만 반복하고 앞쪽 두 발이 바닥에 고정된 것이 확인됐습니다. 이동 클립 곡선 직접 복사 방식으로 교체한 뒤 같은 측면의 실제 Play Mode 시트를 다시 확인했으며, 첫 0.4초 동안 몸통 앞쪽 두 발과 뒤쪽 두 발이 모두 원본 이동 모션과 같은 순서로 자세를 바꾸는 것을 확인했습니다.
- 일반 테스트·빌드, Smoke/Validate/Ensure 계열 명령, Unity 재시작, Git 작업은 실행하지 않았습니다.

## 완료 - 니게티프 오른쪽 구르기 사망 모션

- 대상은 `Approved Negatif Enemy Placement/Negatif_06_Death`이며 최종 길이는 `2.5초`입니다. 니게티프 자신의 오른쪽으로 한 번 굴러 `1.125초`에 `180도` 회전을 완료하므로, 기존 `2.25초` 구르기보다 정확히 2배 빠릅니다. 이후 배와 다리가 위를 향한 자세를 `2.5초`까지 유지한 뒤 전체 클립을 반복 재생합니다.
- 몸통은 `Negatif_Model` 전체의 위치·회전 7개 커브만 사용합니다. 각 회전 자세에서 현재 모델 경계의 최저점을 원래 바닥 높이에 맞추고 슬롯의 오른쪽 축으로 이동시켜 몸통·머리·다리 메시 변형과 바닥 관통 없이 강체처럼 구릅니다.
- 꼬리 계층 `Bone_021→016`은 루트 `Bone_021`에 회전 4개 커브만 추가했습니다. 몸체의 월드 구르기를 같은 양만큼 역보정해 꼬리의 원래 월드 방향을 유지하므로, 연결 위치는 몸체를 따라가면서 꼬리는 위로 뒤집히지 않고 바닥 옆으로 처집니다. 하위 꼬리 본의 위치·회전과 본 간격은 변경하지 않았습니다.
- `Negatif_06_Death_RightRoll.anim`과 `Negatif_06_Death_RightRoll.controller`를 재생성해 사망 슬롯의 Animator에만 연결하고 Root Motion을 비활성화한 뒤 `CargoRunMvp` 장면을 저장했습니다. 다른 니게티프 6개 슬롯과 다른 장면 루트, 수염을 제거한 공용 GLB 해시 `ABE4A3E315B7D31979BF13A4C346005B92F65949E4F5C6B64A1A73F49F607810`은 유지했습니다.
- `ApplyNegatifDeathAnimation`으로 적용하고 `CaptureNegatifDeathVisualReview`로 실제 Play Mode Animator를 `0/0.275/0.525/0.775/1.025/1.125/1.8/2.45초`에 평가해 `Logs/Negatif_Death_VisualReview.png`를 한 번 생성했습니다. 최종 시트를 직접 확인한 결과 2배 빨라진 오른쪽 단일 회전, 배가 위를 향한 최종 자세, `1.125~2.45초` 자세 유지, 꼬리의 바닥 방향 유지, 메시 무변형과 수염 제거 상태가 모두 확인됐습니다.
- 일반 테스트·빌드, Smoke/Validate/Ensure 계열 명령, Unity 재시작, AI·전투·사망 판정 로직, Git 작업은 실행하지 않았습니다.

## 완료 - 리벨리온 원본 GLB 직접 배치와 애니메이션 목록 확정

- 사용자가 지정한 `enemies model/rébellion.glb`을 별도 `artSample`이나 파생 모델 없이 `Assets/_Project/Art/Enemies/Rebellion/Models/Rebellion.glb`에 바이트 단위로 복사해 직접 사용했습니다. 원본과 Unity 자산의 SHA-256은 `BAF8D47AE39523F5EE7DC366DF4E2110D8280725A387133A3ABDBF687F3211E9`로 일치합니다.
- 원본 GLB는 렌더러 1개와 `Bone_000`~`Bone_028` 리그를 가진 모델입니다. 원본 모델·메시·재질·리깅은 수정하지 않았습니다.
- `CargoRunMvp`에 `Approved Rebellion Enemy Placement` 루트를 생성했습니다. 루트 위치는 `(57.86535, 0, -133.875)`이며 니게티프 루트 `(57.86535, 0, -126.498)`에서 월드 음의 Z 방향으로 롱가 아르마–테르고 간격과 같은 `7.376999m` 떨어져 있습니다.
- 총 7개 슬롯을 X축으로 돌로레 슬롯 간격과 같은 `2.444763m` 간격으로 나열했습니다. 각 슬롯에는 같은 `Rebellion.glb` 직접 인스턴스가 하나씩 있으며 모델 높이는 원본 비율을 유지한 채 기획 수치 `0.7m`에 맞추고 바닥에 정렬했습니다.
- 확정된 슬롯과 애니메이션 목록은 다음과 같습니다.
  1. `Rebellion_00_Static_Review` — 원본 정적 확인
  2. `Rebellion_01_Move` — 이동 모션
  3. `Rebellion_02_Attack_Mode_Transition` — 공격 전환 모션
  4. `Rebellion_03_Forward_Scan` — 전방 스캔
  5. `Rebellion_04_Forward_Burst_Fire` — 정면 연속 사격
  6. `Rebellion_05_Hit_Reaction` — 피격 모션
  7. `Rebellion_06_Death` — 사망 모션
- 이번 단계에서는 슬롯 이름만 확정했으며 실제 애니메이션은 아직 연결하지 않았습니다. 모든 슬롯의 Animator와 Animation은 정적 확인 상태입니다.
- 실행 시작점으로 사용하는 `Player`를 `(65.20091, 0, -142.1098)`로 옮기고 정면인 월드 `+Z` 방향을 바라보도록 저장했습니다. 저장 세션 자동 로드가 꺼져 있고 별도의 시작 위치 덮어쓰기 로직이 없어 장면의 Player Transform이 실행 시작 위치로 유지됩니다.
- `ApplyRebellionPlacement` 적용 결과는 `DirectGlbInstances=7`, `OtherSceneRootsUnchanged=True`, `SceneSaved=True`입니다. Player 카메라 기준 전체 리벨리온 행이 시야 안에 들어오는 것도 확인했습니다.
- 최종 화면은 `Logs/Rebellion_PlayerStartView.png`에 한 번 캡처해 직접 확인했습니다. 배치와 정면 시작 시점은 적용됐지만 원본 GLB의 임베디드 재질은 현재 Unity 렌더 화면에서 보라색으로 표시됩니다. 이번 승인 범위에서 재질 수정이 제외되어 있어 해당 외형은 변경하지 않았습니다.
- Harness/EditMode/PlayMode/Build 및 Smoke/Validate/Ensure 계열 명령, 실제 애니메이션·AI·전투·보호막 구현, Unity 재시작, Git 작업은 실행하지 않았습니다.

## 완료 - 리벨리온 기준 이미지 외형 동기화 아트 샘플

- 대상 샘플은 `artSample/enemies/rebellion/appearance_reference_sync`이며 상태는 `PENDING_USER_REVIEW`, `approved_for_unity=false`입니다. Unity 장면과 현재 배치 GLB에는 적용하지 않았습니다.
- 기준 이미지는 `image/rébellion(리벨리온).png`이며 SHA-256은 `52ED860D11CE73890DEE8C5A42C622D683D5D48D8E32F5B774650FD8492146A1`입니다. 현재 Unity 원본 `Assets/_Project/Art/Enemies/Rebellion/Models/Rebellion.glb`과 샘플 내 무수정 보존 복사본의 SHA-256은 모두 `BAF8D47AE39523F5EE7DC366DF4E2110D8280725A387133A3ABDBF687F3211E9`입니다.
- 기준 이미지의 납작한 회색 원판, 네 거미형 다리, 검은 관절·다리 프레임, 밝은 유압 금속, 검은 정면 패널, 다연장 총구, 작은 붉은 스캔 센서를 분석해 샘플에 반영했습니다.
- 현재 Unity GLB의 스킨드 메시 `18176`정점·`6609`면, 29개 vertex group, `Bone_000`~`Bone_028` 29본과 스킨 가중치를 유지했습니다. 샘플 전후 형상·가중치 서명은 `8BF77AAFA0077ADB30E2D6783DB847EA792E6444CFC77833023148BD80E81E0B`로 일치합니다.
- 기준 이미지에 명확히 보이는 정면 패널·7구 총열·환기구·고정 볼트·센서의 파생 세부 부품 18개를 추가했습니다. 원본 스킨드 메시를 변형하지 않으며 몸통 지배 본 `Bone_017`에 부착해 이후 원판 상승 모션을 따라갈 수 있게 구성했습니다.
- `Rebellion_Worn_Disc_Steel`, `Rebellion_Dark_Leg_Mechanism`, `Rebellion_Hydraulic_Steel`, `Rebellion_Front_Weapon_Black`, `Rebellion_Scan_Optic_Red` 재질을 구성했습니다. 네 금속 재질에는 각각 512×512 알베도·거칠기·노멀 텍스처를 포함하며 금속 얼룩, 긁힘, 산화, 기름때와 미세 요철을 반영했습니다.
- 검토 원본은 `blender/Rebellion_Appearance_ReferenceSync.blend`, 범용 결과물은 `exports/Rebellion_Appearance_ReferenceSync.glb`입니다. 최종 GLB SHA-256은 `0A6FBF7F8B4B5470D3000C7B585522C45F736651E1E791D4E19692CEB4231721`입니다.
- 내보낸 GLB를 Blender 빈 씬에 다시 불러온 결과 메시 개체 21개, 총 `20712`정점·`8089`면, armature 1개, 본 29개와 최대 10개 vertex influence가 유지됐습니다.
- 기준 이미지와 샘플의 직접 비교는 `renders/06_reference_comparison.png`이며, 정면·사선·측면·후면 사선·정면 무장 세부 렌더도 함께 생성했습니다. 직접 확인 결과 회색 원판, 어두운 기계 관절, 밝은 유압부, 검은 무장 패널과 작은 붉은 센서가 기준 이미지와 같은 분포로 분리되어 보입니다.
- Unity 실행·리프레시·재시작·장면 저장, 현재 Unity 모델·재질 교체, 애니메이션·AI·전투 구현, Harness/EditMode/PlayMode/Build 및 Smoke/Validate/Ensure 계열 작업, Git 작업은 실행하지 않았습니다.

## 수정 완료 - 리벨리온 정면 검은 패널 매립형 보정

- 사용자 검토에서 초기 샘플의 검은 정면 패널이 원반 안에 있지 않고 전면 허공에 떠 보이는 문제가 확인됐습니다. 초기 패널 배치는 채택하지 않았습니다.
- 원인은 원반의 실제 중앙 전면 표면이 약 `Y=-1.13`인데 패널을 전역 전방 끝값에 가까운 `Y=-1.292`에 배치해 원반 표면과 약 `0.16m` 떨어뜨린 것이었습니다.
- `Rebellion_Front_Control_Panel`을 얕은 `Rebellion_Front_Inset_Panel`로 교체하고 중심을 `(0, -1.135, 1.43)`, 크기를 `(0.84, 0.008, 0.33)`으로 보정했습니다. 패널 전면은 원반 표면에서 약 `0.009m`만 나오며 금속 테두리는 패널보다 `0.010m` 앞에 있어 검은 판이 원반 측면 띠 안에 들어가 보입니다.
- 총구·센서·환기구·고정 볼트도 새 패널 위치에 맞췄습니다. 원본 모델의 별 모양 전면 돌출물은 삭제하거나 메시를 변형하지 않고 총구 허브 안에 가려지도록 구성했습니다.
- 파생 세부 부품은 테두리 4개가 추가되어 `18개 → 22개`가 됐으며 모두 몸통 지배 본 `Bone_017`에 부착했습니다.
- 정면 비교, 사선, 측면, 정면 무장 확대 렌더를 직접 확인했습니다. 검은 패널과 원반 사이의 빈 틈이 사라졌고 패널이 원반 전면 띠 안에 밀착되며 총구만 앞으로 돌출되는 것을 확인했습니다.
- 원본 Unity GLB SHA-256은 `BAF8D47AE39523F5EE7DC366DF4E2110D8280725A387133A3ABDBF687F3211E9`로 유지됐습니다. 스킨드 메시 `18176`정점·`6609`면, 본 29개, 최대 vertex influence 10개와 형상·가중치 서명도 유지됐습니다.
- 수정본 GLB SHA-256은 `1ABA83E2875D464D355E8BFCD768CC3E140727E75D0CB4D692F333BC7F18B58B`입니다. 재반입 결과 메시 개체 25개, 총 `21576`정점·`8521`면, armature 1개와 본 29개가 확인됐습니다.
- 상태는 계속 `PENDING_USER_REVIEW`, `approved_for_unity=false`입니다. Unity 실행·리프레시·재시작·장면 저장, 현재 Unity 모델·재질 교체, 애니메이션·AI·전투 구현, Harness/EditMode/PlayMode/Build 및 Smoke/Validate/Ensure 계열 작업, Git 작업은 실행하지 않았습니다.
