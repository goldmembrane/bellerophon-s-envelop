# Progress 2026-06-30

## Fuga runtime camera corrected to actual face front

- 작업 범위:
  - 실행 시 바로 Fuga 정면을 볼 수 있도록 `Model Cam` 배치 기준을 다시 정비했습니다.
  - 기존 yaw 기반 추정 대신 Fuga의 주둥이, 입, 눈 오브젝트 위치를 기준으로 실제 시각 정면을 계산하도록 변경했습니다.
- 반영 내용:
  - `FugaCargoRunSceneApplyAndReview`에서 Fuga 정면 계산 기준에 `Fuga2_Broad_Front_Snout_Bulge_With_Wavy_Mouth`, `Fuga2_Subtle_Dark_Wavy_Mouth_Recess`, 좌/우 눈 오브젝트를 사용하도록 했습니다.
  - 얼굴 특징 평균 위치가 렌더러 bounds 중심에서 어느 방향에 있는지 계산해 카메라를 해당 정면 방향 앞에 배치하도록 했습니다.
  - 얼굴 특징을 찾지 못하는 경우에는 기존 yaw 기반 계산으로 되돌아가도록 fallback을 유지했습니다.
- 확인 결과:
  - `FUGA_UNITY_APPLICATION_REVIEW.json` 기준 Fuga 스케일은 `0.25`, yaw는 `180`입니다.
  - 실제 시각 정면 방향은 `0 / 0 / -1`로 확인됐습니다.
  - 실행 카메라 위치는 `57.865 / 1.795 / -36.199`입니다.
  - 카메라 정면 거리는 `5.25m`입니다.
  - Fuga 7개체 배치, X축 간격 `2.45m`, 실제 최소 X축 빈 공간 `0.466m`, Parvum과의 Z축 배치 기준은 유지됐습니다.
  - 적용 로그와 검토 로그 모두 Unity 경고/예외 없이 성공했습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 샘플 자체 리디자인
  - Fuga 모델 메시/텍스처/애니메이션 클립 내용 변경
  - Parvum, 복도, 다른 적대 개체 수정

## Fuga runtime camera aligned to rear-facing front

- 작업 범위:
  - Yaw `180도`로 뒤를 보게 회전된 Fuga의 현재 시각 정면 `+Z` 기준으로 실행/검토 카메라 위치를 다시 정비했습니다.
  - Fuga 모델 메시/텍스처/애니메이션 클립은 변경하지 않았습니다.
- 반영 내용:
  - `FugaCargoRunSceneApplyAndReview`에 Fuga 정면 카메라 최소 거리 `5.25m`, 최대 거리 `7.50m` 기준을 추가했습니다.
  - 기존 bounds 기반 카메라 거리 상한 `3.5m` 제한을 제거하고, 현재 정면 기준으로 더 앞쪽에서 바라보도록 조정했습니다.
  - 검토 단계에서 카메라가 현재 시각 정면 방향에 있는지와 최소 정면 거리 `5.25m`를 만족하는지 확인하도록 했습니다.
  - 검토 기록에 `cameraFrontDistance`를 저장하도록 했습니다.
- 확인 결과:
  - `FUGA_UNITY_APPLICATION_REVIEW.json` 기준 `facingYawDegrees`는 `180`입니다.
  - Fuga 시각 정면 방향은 `0 / 0 / 1`입니다.
  - 카메라 위치는 `57.865 / 1.795 / -25.699`입니다.
  - 카메라 정면 거리는 `5.25m`입니다.
  - Fuga 스케일 `0.25`, X축 간격 `2.45m`, 실제 최소 X축 빈 공간 `0.466m`, Parvum과의 Z축 기준은 유지됐습니다.
  - 적용 로그와 검토 로그 모두 Unity 경고/예외 없이 성공했습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 샘플 자체 재디자인
  - Fuga 모델 메시/텍스처/애니메이션 클립 내용 변경
  - Parvum, 복도, 다른 적대 개체 수정

## Fuga rear-facing rotation reapplied

- 작업 범위:
  - 승인 샘플 FBX 원본 모델 방향은 유지한 상태에서 `CargoRunMvp` 씬의 Fuga 배치 루트 회전만 뒤보기 방향으로 변경했습니다.
  - 스케일 `0.25`, Parvum과의 Z축 배치 기준, 카메라 정면 배치 기준은 유지했습니다.
- 반영 내용:
  - `FugaCargoRunSceneApplyAndReview`의 `FugaFacingYawDegrees`를 `180도`로 변경했습니다.
  - Yaw `180도`에서 렌더러 bounds 기준 X축 여유가 줄어드는 것을 확인해 Fuga X축 배치 간격을 `2.45m`로 조정했습니다.
  - 카메라는 변경된 Fuga 시각 정면 기준으로 다시 배치되도록 기존 로직을 재적용했습니다.
- 확인 결과:
  - `FUGA_UNITY_APPLICATION_REVIEW.json` 기준 `facingYawDegrees`는 `180`입니다.
  - Fuga 시각 정면 방향은 `0 / 0 / 1`입니다.
  - 카메라 위치는 `57.865 / 1.795 / -27.449`입니다.
  - `placementXSpacing`은 `2.45m`, 실제 최소 X축 빈 공간은 `0.466m`입니다.
  - 적용 로그와 최종 검토 로그 모두 Unity 경고/예외 없이 성공했습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 샘플 자체 재디자인
  - Fuga 모델 메시/텍스처/애니메이션 클립 내용 변경
  - Parvum, 복도, 다른 적대 개체 수정

## Fuga Unity model sync to approved Blender sample

- 작업 범위:
  - 승인된 `artSample/enemies/fuga/exports/fuga_sample.fbx`가 Unity에 실제로 들어가 있는지 확인했습니다.
  - Unity 프리팹 생성 과정에서 승인 샘플과 달라지는 원인을 수정했습니다.
- 확인한 원인:
  - Unity 적용 스크립트가 `FugaApproved_Model` 래퍼에 X축 `-90도` 회전을 강제로 넣고 있어 Blender 샘플의 원래 시각 축과 달라질 수 있었습니다.
  - 섭취 모션용 `Fuga_Consume_Open_Mouth_Cavity` 큐브가 샘플에 없는 별도 오브젝트로 프리팹에 추가되고 있었습니다.
  - 머티리얼 재할당이 기존 머티리얼 이름만 보고 처리되어, FBX 임포트 후 오브젝트별 샘플 재질 분포가 충분히 재현되지 않을 수 있었습니다.
- 반영 내용:
  - `FugaApproved_Model`의 래퍼 회전을 `Vector3.zero`로 변경해 승인된 FBX 모델 방향을 그대로 유지하도록 했습니다.
  - 샘플에 없는 `Fuga_Consume_Open_Mouth_Cavity` 보조 큐브 생성과 애니메이션 커브를 제거했습니다.
  - 머티리얼 선택 기준을 렌더러 오브젝트 이름 + 기존 머티리얼 이름 조합으로 변경했습니다.
  - 눈, 동공/어두운 선, 하단 껍질, 내부 날개, 몸통 재질이 샘플 오브젝트 이름에 맞게 분리되도록 재질 분기 순서를 수정했습니다.
- 확인 결과:
  - Unity에 복사된 `Assets/_Project/Art/Enemies/Fuga/Models/fuga_sample.fbx`는 승인 샘플 FBX와 파일 크기/수정 시간이 일치합니다.
  - 재생성된 `FugaApproved.prefab`에서 `FugaApproved_Model` 로컬 회전은 identity입니다.
  - 재생성된 `FugaApproved.prefab`에서 `Fuga_Consume_Open_Mouth_Cavity`는 더 이상 존재하지 않습니다.
  - 프리팹에는 몸통, 눈, 하단 껍질, 내부 날개, 어두운 선 계열 머티리얼 GUID가 분리 적용됐습니다.
  - 적용 로그와 검토 로그 모두 Unity 경고/예외 없이 성공했습니다.
  - Fuga 7개체, 스케일 `0.25`, Yaw `0도`, X축 간격, 카메라 정면 배치, Parvum과의 Z축 기준은 유지됐습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 샘플 자체의 재디자인
  - Parvum, 복도, 다른 적대 개체 수정

## Fuga front-facing runtime camera alignment

- 작업 범위:
  - 현재 회전된 Fuga의 보이는 정면을 기준으로 실행/검토 카메라 위치를 다시 맞췄습니다.
  - Fuga 스케일 `0.25`, Yaw `0도`, X축 간격 `2.15m`, Parvum과의 Z축 배치 기준은 유지했습니다.
- 반영 내용:
  - `FugaCargoRunSceneApplyAndReview`의 카메라 정면 계산을 `focus.forward` 기준에서 Fuga Yaw를 반영한 시각 정면 `Vector3.back` 기준으로 변경했습니다.
  - `ConfigureInitialFugaReviewCamera`가 현재 Fuga 정면 방향으로 카메라를 배치하도록 수정했습니다.
  - `InspectFugaReviewCamera`를 추가해 카메라가 현재 Fuga 정면 방향에 있고 Fuga focus를 바라보는지 검토하도록 했습니다.
  - 검토 기록에 `cameraPositionX/Y/Z`와 `cameraFrontDirectionX/Y/Z`를 저장하도록 했습니다.
- 확인 결과:
  - `FUGA_UNITY_APPLICATION_REVIEW.json` 기준 Fuga 시각 정면 방향은 `0 / 0 / -1`입니다.
  - 카메라 위치는 `57.865 / 1.795 / -33.745`로 기록됐습니다.
  - Fuga 7개체, 스케일 `0.25`, Yaw `0도`, X축 최소 빈 공간 `0.410m`, Parvum과의 Z축 기준은 유지됐습니다.
  - 적용 로그와 검토 로그 모두 Unity 경고/예외 없이 성공했습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 모델/머티리얼/텍스처/애니메이션 클립 내용 변경
  - Parvum, 복도, 다른 적대 개체 수정

## Fuga X-axis spacing adjustment

- 작업 범위:
  - `Approved Fuga Enemy Placement` 아래 Fuga 7개체의 스케일 `0.25`와 Yaw `0도` 회전 상태를 유지했습니다.
  - Fuga 개체들이 X축 기준으로 서로 겹치지 않도록 배치 간격을 넓혔습니다.
- 반영 내용:
  - `FugaCargoRunSceneApplyAndReview`의 Fuga X축 배치 간격을 `2.15m`로 조정했습니다.
  - X축 최소 clearance 기준을 `0.35m`로 추가했습니다.
  - Unity 검토 단계에서 각 Fuga 렌더러 bounds를 X축 기준으로 정렬해 인접 개체 사이의 실제 빈 공간을 검사하도록 했습니다.
  - 검토 기록에 `placementXSpacing`, `minimumXClearance`, `measuredMinimumXGap` 값을 저장하도록 했습니다.
- 확인 결과:
  - `FUGA_UNITY_APPLICATION_REVIEW.json` 기준 `placementXSpacing`은 `2.15m`입니다.
  - 실제 측정된 최소 X축 빈 공간 `measuredMinimumXGap`은 `0.410m`로, 요구 기준 `0.35m`보다 큽니다.
  - Fuga 7개체 수, 스케일 `0.25`, Yaw `0도`, Parvum과의 Z축 배치 기준은 유지됐습니다.
  - 적용 로그와 검토 로그 모두 Unity 경고/예외 없이 성공했습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 모델/머티리얼/텍스처/애니메이션 클립 내용 변경
  - Parvum, 복도, 카메라, 다른 적대 개체 수정

## Fuga artSample individual feather removal and broader single wing

- 작업 범위:
  - 사용자 지시에 따라 날개 쪽 개별 깃털 깃 오브젝트를 제거했다.
  - 제거된 깃털 깃 대신 단일 넓적한 깃의 너비와 실루엣을 키웠다.
- 반영 내용:
  - `artSample/enemies/fuga/tools/build_fuga_sample.py`에서 `Fuga2_*_Open_Pointed_Wing_Feather_*` 생성 루프를 제거했다.
  - 이전 연결 보강용 `Fuga2_*_Open_Feather_Root_Attachment_*` 생성 코드와 `create_feather_root_attachment_mesh`를 제거했다.
  - `create_connected_wing_base_mesh`의 외곽 좌표를 바깥쪽으로 확장해 `Fuga2_*_Single_Connected_Broad_Wing_Base`가 더 넓은 단일 날개 면으로 보이게 했다.
  - 넓어진 날개가 렌더에서 잘리지 않도록 정면/측면/후면/상태 렌더의 카메라 `ortho_scale`을 조정했다.
- 갱신 산출물:
  - `artSample/enemies/fuga/blender/fuga_sample.blend`
  - `artSample/enemies/fuga/exports/fuga_sample.fbx`
  - `artSample/enemies/fuga/exports/fuga_sample.glb`
  - `artSample/enemies/fuga/renders/*.png`
- 확인 결과:
  - Blender 백그라운드 재생성 명령은 종료 코드 `0`으로 완료됐다.
  - `01_front_fuga2_reference_match.png`와 `02_side_fuga2_beside_reference_match.png`에서 개별 깃털 깃이 제거되고 넓어진 단일 넓적한 깃만 남은 것을 확인했다.
  - 내보내기 로그에서 날개 관련 메쉬는 `Fuga2_*_Single_Connected_Broad_Wing_Base`와 `Fuga2_*_Single_Base_Outer_Serrated_Edge`만 남은 것을 확인했다.
  - Git, Unity 실행/리프레시/브리지, Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령은 실행하지 않았다.

## Fuga artSample feather root attachment and backward lean correction

- 작업 범위:
  - 사용자 지시에 따라 측면에서 날개 깃털 깃이 공중에 떠 보이지 않도록 넓적한 깃 표면에 붙는 루트 구조로 수정했다.
  - 정면 기준으로 깃털 깃이 앞쪽에만 떠 있지 않고 약간 뒤로 누워 올라가도록 루트/팁 각도를 조정했다.
- 반영 내용:
  - `artSample/enemies/fuga/tools/build_fuga_sample.py`에 `create_feather_root_attachment_mesh`를 추가해 각 외곽 깃의 루트 아래에 얇은 접합 패치를 생성하게 했다.
  - `Fuga2_*_Open_Feather_Root_Attachment_*`가 넓적한 깃 표면 안쪽 앵커와 `Fuga2_*_Open_Pointed_Wing_Feather_*` 루트를 잇도록 배치했다.
  - 외곽 깃 루트의 `y` 좌표를 넓적한 깃 외곽/표면 좌표에 가깝게 되돌리고, 팁 `y` 좌표는 루트보다 뒤쪽으로 두어 측면에서 뒤로 누운 각도를 만들었다.
  - 루트 폭을 키워 정면에서도 깃털 뿌리가 넓적한 깃에 맞물려 보이게 했다.
- 갱신 산출물:
  - `artSample/enemies/fuga/blender/fuga_sample.blend`
  - `artSample/enemies/fuga/exports/fuga_sample.fbx`
  - `artSample/enemies/fuga/exports/fuga_sample.glb`
  - `artSample/enemies/fuga/renders/*.png`
- 확인 결과:
  - Blender 백그라운드 재생성 명령은 종료 코드 `0`으로 완료됐다.
  - `01_front_fuga2_reference_match.png`에서 깃털 루트가 작은 받침 형태로 넓적한 깃과 맞물리고, 깃 전체가 앞쪽으로만 떠 있지 않도록 정리된 것을 확인했다.
  - `02_side_fuga2_beside_reference_match.png`에서 루트가 넓적한 깃 표면에 닿고 팁이 뒤쪽으로 누워 올라가는 형태를 확인했다.
  - Git, Unity 실행/리프레시/브리지, Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령은 실행하지 않았다.

## Fuga artSample central wing object removal and forward feather placement

- 작업 범위:
  - 사용자 지시에 따라 넓적한 깃 가운데에 세로 오브젝트처럼 보이던 내부 덮깃 묶음을 제거했다.
  - 넓적한 깃 외곽의 날개 깃을 정면 카메라 기준 넓적한 깃 앞쪽에 더 겹치도록 좌표를 조정했다.
- 반영 내용:
  - `artSample/enemies/fuga/tools/build_fuga_sample.py`의 `create_wing`에서 `Fuga2_*_Small_Overlapping_Shoulder_Feather_*` 생성 루프를 제거했다.
  - `Fuga2_*_Open_Pointed_Wing_Feather_*` 루트와 팁 좌표의 `y` 값을 앞쪽으로 이동하고 루트 폭을 키워 넓적한 단일 깃 표면에 붙어 보이게 했다.
  - 외곽 날개 깃의 중간선/가장자리 디테일은 기준 이미지의 깃 질감으로 유지했다.
- 갱신 산출물:
  - `artSample/enemies/fuga/blender/fuga_sample.blend`
  - `artSample/enemies/fuga/exports/fuga_sample.fbx`
  - `artSample/enemies/fuga/exports/fuga_sample.glb`
  - `artSample/enemies/fuga/renders/*.png`
- 확인 결과:
  - Blender 백그라운드 재생성 명령은 종료 코드 `0`으로 완료됐다.
  - `01_front_fuga2_reference_match.png`와 `02_side_fuga2_beside_reference_match.png`에서 내부 덮깃 묶음이 제거되고 외곽 깃이 넓적한 깃 앞에 겹쳐 시작하는 것을 확인했다.
  - 남아 있는 중앙의 어두운 삼각 음영은 별도 세로 오브젝트가 아니라 단일 넓적한 깃 메쉬의 면 분할/조명 음영으로 확인했다.
  - Git, Unity 실행/리프레시/브리지, Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령은 실행하지 않았다.

## Fuga artSample single broad wing base correction

- 작업 범위:
  - 사용자 지시에 따라 넓적한 기반 깃을 여러 오브젝트로 나누지 않고 하나의 길고 넓은 기반 날개 오브젝트로 재구성했다.
  - 긴 날개 깃들의 앞쪽/뿌리가 해당 단일 기반 날개 앞쪽 표면에 붙어 보이도록 좌표를 재조정했다.
- 반영 내용:
  - `create_connected_wing_base_mesh`를 추가해 `Fuga2_*_Single_Connected_Broad_Wing_Base` 단일 메쉬를 생성하게 했다.
  - 기존 `Fuga2_*_Connected_Broad_Base_Feather_*` 여러 기반 깃 생성 루프를 제거했다.
  - 기존 `Fuga2_*_Base_To_Primary_Overlap_Feather_*` 여러 연결 덮깃 생성 루프를 제거했다.
  - `Fuga2_*_Open_Pointed_Wing_Feather_*`의 뿌리 좌표를 단일 기반 날개 표면 쪽으로 이동하고 뿌리 폭을 넓혀, 공중에서 따로 시작하지 않게 했다.
  - 단일 기반 날개에는 외곽선과 리브 선만 보조 디테일로 추가했다.
- 갱신 산출물:
  - `artSample/enemies/fuga/blender/fuga_sample.blend`
  - `artSample/enemies/fuga/exports/fuga_sample.fbx`
  - `artSample/enemies/fuga/exports/fuga_sample.glb`
  - `artSample/enemies/fuga/renders/*.png`
- 확인 결과:
  - Blender 백그라운드 재생성 명령은 종료 코드 `0`으로 완료됐다.
  - `01_front_fuga2_reference_match.png`와 `02_side_fuga2_beside_reference_match.png`에서 기반부가 하나의 넓고 긴 날개 오브젝트로 보이는 것을 확인했다.
  - 긴 깃들의 앞쪽/뿌리가 단일 기반 날개 앞쪽에 겹쳐 시작해, 기존처럼 여러 기반 깃 사이에서 떠 보이는 느낌이 줄어든 것을 확인했다.
  - Git, Unity 실행/리프레시/브리지, Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령은 실행하지 않았다.

## Fuga artSample wing feather attachment cleanup

- 작업 범위:
  - 사용자 지시에 따라 긴 날개 깃이 공중에 떠 보이지 않도록 넓적한 기반 깃 앞쪽과 더 직접적으로 연결되게 수정했다.
  - 날개 부분의 동글동글한 오브젝트를 제거했다.
- 반영 내용:
  - `Fuga2_*_Bumpy_Shoulder_Wing_Ridge` 생성 코드를 제거했다.
  - `Fuga2_*_Bumpy_Leading_Edge_Knuckle_*` 생성 코드를 제거했다.
  - `Fuga2_*_Connected_Broad_Base_Feather_*`의 끝점을 뒤쪽으로 더 늘려 긴 깃 뿌리와 겹치게 했다.
  - `Fuga2_*_Base_To_Primary_Overlap_Feather_*` 연결 덮깃을 추가해 넓적한 기반 깃과 긴 주 깃 사이의 빈틈을 덮었다.
  - `Fuga2_*_Open_Pointed_Wing_Feather_*`의 뿌리 좌표를 기반 깃 위쪽으로 이동하고 뿌리 폭을 넓혀, 깃 앞쪽이 기반 깃과 맞물려 시작하게 했다.
- 갱신 산출물:
  - `artSample/enemies/fuga/blender/fuga_sample.blend`
  - `artSample/enemies/fuga/exports/fuga_sample.fbx`
  - `artSample/enemies/fuga/exports/fuga_sample.glb`
  - `artSample/enemies/fuga/renders/*.png`
- 확인 결과:
  - Blender 백그라운드 재생성 명령은 종료 코드 `0`으로 완료됐다.
  - `01_front_fuga2_reference_match.png`와 `02_side_fuga2_beside_reference_match.png`에서 날개 부분의 동그란 오브젝트가 제거된 것을 확인했다.
  - 전면/측면 기준으로 긴 깃 앞쪽이 넓적한 기반 깃과 겹쳐 시작하며, 이전보다 공중에 떠 보이는 느낌이 줄어든 것을 확인했다.
  - Git, Unity 실행/리프레시/브리지, Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령은 실행하지 않았다.

## Fuga artSample face and wing attachment correction

- 작업 범위:
  - 사용자 지적 사항 2개를 반영했다.
  - 측면 렌더에서 얼굴이 몸통과 떨어져 떠 보이는 문제를 수정했다.
  - 긴 깃들이 공중에서 시작하지 않고, 몸통에 연결된 넓적한 기반 깃 위에 붙어 보이도록 날개 루트를 재구성했다.
- 반영 내용:
  - `Fuga2_Continuous_Face_To_Body_Neck_Bridge`, `Fuga2_Continuous_Dorsal_Head_Back_Join`, `Fuga2_Continuous_Lower_Jaw_To_Chest_Join`을 추가해 얼굴, 턱, 등쪽 머리 뒷부분이 몸통과 겹쳐 이어지게 했다.
  - 단일 다각형 기반 판은 제거하고 `Fuga2_*_Connected_Broad_Base_Feather_*` 기반 깃 여러 장을 몸통에서 펼쳐지게 만들었다.
  - `Fuga2_*_Open_Pointed_Wing_Feather_*`의 뿌리 좌표를 뒤쪽 기반 깃 위로 이동해 긴 깃이 기반 깃 위에서 시작하도록 조정했다.
- 갱신 산출물:
  - `artSample/enemies/fuga/blender/fuga_sample.blend`
  - `artSample/enemies/fuga/exports/fuga_sample.fbx`
  - `artSample/enemies/fuga/exports/fuga_sample.glb`
  - `artSample/enemies/fuga/renders/*.png`
- 확인 결과:
  - Blender 백그라운드 재생성 명령은 종료 코드 `0`으로 완료됐다.
  - `02_side_fuga2_beside_reference_match.png` 기준으로 얼굴이 몸통에 붙어 보이고, 긴 깃이 몸통에서 이어지는 넓은 기반 깃 위에서 시작하는 것을 확인했다.
  - Git, Unity 실행/리프레시/브리지, Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령은 실행하지 않았다.

## Fuga artSample wing silhouette correction

- 작업 범위:
  - 사용자 지시에 따라 `image/fuga2(푸가).png` 및 `image/fuga2-beside.png` 기준으로 `artSample/enemies/fuga/` 샘플의 날개 오브젝트를 수정했다.
  - Git, Unity 실행/리프레시/브리지, Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령은 실행하지 않았다.
- 원인 확인:
  - `artSample/enemies/fuga/blender/fuga_sample.blend` 오브젝트 진단 결과 `Fuga2_*_Bumpy_Shoulder_Wing_Ridge`와 `Fuga2_*_Bumpy_Leading_Edge_Knuckle_*`가 의도한 작은 돌기 크기가 아니라 기본 구체 크기 2.0에 가깝게 저장되어 둥근 날개 덩어리처럼 보이는 문제가 있었다.
  - 원인은 날개 루트에 부모를 연결하기 전 `matrix_world`가 최신 스케일을 반영하지 않아 스케일이 보존되지 않는 흐름으로 판단했다.
- 반영 내용:
  - `artSample/enemies/fuga/tools/build_fuga_sample.py`의 `parent_keep_transform`에 `bpy.context.view_layer.update()`를 추가해 부모 연결 시 스케일을 보존하게 했다.
  - 날개를 둥근 덩어리가 아니라 납작한 긴 깃들이 겹친 형태로 보이도록 `Fuga2_*_Open_Pointed_Wing_Feather_*` 좌표와 폭을 조정했다.
  - 전면/측면/후면 기준 렌더에서 새 날개 끝이 잘리지 않도록 기준 렌더 카메라 `ortho_scale`을 `5.35`로 조정했다.
- 갱신 산출물:
  - `artSample/enemies/fuga/blender/fuga_sample.blend`
  - `artSample/enemies/fuga/exports/fuga_sample.fbx`
  - `artSample/enemies/fuga/exports/fuga_sample.glb`
  - `artSample/enemies/fuga/renders/*.png`
- 확인 결과:
  - Blender 백그라운드 재생성 명령은 종료 코드 `0`으로 완료됐다.
  - 오브젝트 진단에서 `Fuga2_*_Bumpy_Shoulder_Wing_Ridge`는 약 `0.36 x 0.22 x 0.26`, `Fuga2_*_Bumpy_Leading_Edge_Knuckle_*`는 약 `0.13 x 0.09 x 0.10` 수준으로 정상 축소됐다.
  - `01_front_fuga2_reference_match.png`와 `02_side_fuga2_beside_reference_match.png`에서 둥근 날개 덩어리 대신 기준 이미지처럼 뾰족한 깃이 펼쳐진 실루엣을 확인했다.

## 푸가 적대 개체 artSample 제작

- 작업 시각:
  - 2026-06-30 사용자 승인 범위 안에서 `artSample/enemies/fuga/` 샘플을 제작했다.
- 규칙 확인:
  - 사용자가 Git으로 파악하지 말라고 지시했으므로 Git 명령은 실행하지 않았다.
  - 적대 개체 샘플 제작 규칙에 따라 Unity 씬, 프리팹, 런타임 에셋, AI, 피격 판정, UI 흐름에는 연결하지 않았다.
  - 기준 이미지는 `image/fuga2(푸가).png`, `image/fuga2-back.png`, `image/fuga2-beside.png`를 1차 재현 대상으로 사용했다.
  - Unity Refresh/Bridge, Harness/EditMode/PlayMode 테스트, Smoke, Build, Ensure, Validate 계열 명령은 실행하지 않았다.
- 확인한 구현 문서/기획:
  - 최신 진행문서는 `docs/PROGRESS_2026-06-29.md`였고, 2026-06-30 진행문서는 작업 전 존재하지 않았다.
  - `docs/SEED_OBJECT_IMPLEMENTATION_PLAN_2026-06-30.md`에서 푸가가 다음 씨앗체 구현 대상으로 잡혀 있음을 확인했다.
  - `docs/GAME_DESIGN_SOURCE.txt` 기준 푸가는 조류 날개를 가진 소라/두꺼비형 비행 씨앗체이며, 높이 약 60cm, 가로 약 40cm, 세로 약 20cm로 정리했다.
- 반영 내용:
  - `artSample/enemies/fuga/tools/build_fuga_sample.py`를 재작성하고 Blender 백그라운드 생성 루프를 구성했다.
  - 중앙 녹회색 두꺼비형 머리, 황금색 세로 동공, 물결형 입, 좌우 날개, 하단 소라/잎 장식, 젖은 피부 머티리얼, 깃 머티리얼, 장식 머티리얼을 만들었다.
  - 기준 이미지와 비교하기 위한 정면, 측면, 후면 렌더와 런타임 검토용 사선/상태별 렌더를 생성했다.
  - `APPROVAL_STATUS.json`은 `approvalState=검토 필요`, `unityApplicationAllowed=false`, `requiresUserApprovalBeforeUnity=true`로 기록했다.
  - `README.md`, `TEXTURE_ANALYSIS.md`, `PHYSICS_RIG_NOTES.md`, `ASSET_MANIFEST.json`, `index.html`을 한국어 검토 문서로 갱신했다.
  - 스크립트의 이전 문서 생성 함수명은 `write_text_files_legacy_unused`로 분리하고, 실제 실행되는 `write_text_files`는 한국어 검토 문서 생성 함수로 정리했다.
- 생성 산출물:
  - `artSample/enemies/fuga/blender/fuga_sample.blend`
  - `artSample/enemies/fuga/exports/fuga_sample.fbx`
  - `artSample/enemies/fuga/exports/fuga_sample.glb`
  - `artSample/enemies/fuga/textures/fuga2_wet_green_bumpy_body_albedo.png`
  - `artSample/enemies/fuga/textures/fuga2_body_wart_bump.png`
  - `artSample/enemies/fuga/textures/fuga2_olive_feather_albedo.png`
  - `artSample/enemies/fuga/textures/fuga2_inner_brown_olive_feather_albedo.png`
  - `artSample/enemies/fuga/textures/fuga2_lower_shell_leaf_albedo.png`
  - `artSample/enemies/fuga/textures/fuga2_golden_eye_albedo.png`
  - `artSample/enemies/fuga/renders/01_front_fuga2_reference_match.png`
  - `artSample/enemies/fuga/renders/02_side_fuga2_beside_reference_match.png`
  - `artSample/enemies/fuga/renders/03_back_fuga2_back_reference_match.png`
  - `artSample/enemies/fuga/renders/04_three_quarter_runtime_hover_visual.png`
  - `artSample/enemies/fuga/renders/05_idle_hover_pose.png`
  - `artSample/enemies/fuga/renders/06_move_forward_glide_pose.png`
  - `artSample/enemies/fuga/renders/07_attack_wing_slap_pose.png`
  - `artSample/enemies/fuga/renders/08_hit_recoil_altitude_drop_pose.png`
  - `artSample/enemies/fuga/renders/09_death_fall_folded_wings_pose.png`
  - `artSample/enemies/fuga/README.md`
  - `artSample/enemies/fuga/TEXTURE_ANALYSIS.md`
  - `artSample/enemies/fuga/PHYSICS_RIG_NOTES.md`
  - `artSample/enemies/fuga/ASSET_MANIFEST.json`
  - `artSample/enemies/fuga/APPROVAL_STATUS.json`
  - `artSample/enemies/fuga/index.html`
- 실행 명령:
  - `Test-Path docs/PROGRESS_2026-06-30.md`
  - `Get-ChildItem docs -Filter PROGRESS_*.md | Sort-Object Name -Descending | Select-Object -First 1`
  - `Get-Content -Raw AGENTS.md`
  - `Get-Content -Raw docs/GAME_DESIGN_SOURCE.txt`
  - `Get-Content -Raw docs/MVP_IMPLEMENTATION_ORDER.md`
  - `rg -n "푸|Fuga|fuga|fuga2|Fu|fu" docs image artSample`
  - `Get-ChildItem image -Filter "fuga2*"`
  - `C:\Program Files\Blender Foundation\Blender 5.1\blender.exe --background --python .\artSample\enemies\fuga\tools\build_fuga_sample.py`
  - `Get-ChildItem`, `Get-Content`, `rg` 기반 산출물 확인
  - 로컬 이미지 보기 도구로 기준 이미지와 생성 렌더를 확인
- 확인 결과:
  - Blender 생성 명령은 최종 종료 코드 `0`으로 완료됐다.
  - `.blend`, `.fbx`, `.glb`, 렌더 9장, 텍스처 6장, 한국어 검토 문서가 존재함을 확인했다.
  - `README.md`, `index.html`, `TEXTURE_ANALYSIS.md`, `PHYSICS_RIG_NOTES.md`, `APPROVAL_STATUS.json`에서 한국어 문구와 Unity 미적용 상태를 확인했다.
  - `build_fuga_sample.py`에서 실제 실행 함수 `write_text_files`와 `main` 위치를 확인했다.
  - 정면 렌더는 `fuga2(푸가).png`의 큰 눈, 얇은 물결 입, 좌우 날개, 하단 장식을 반영했다.
  - 측면/후면 렌더는 기준 이미지의 날개 방향과 하단 장식 의도를 반영했지만, Unity 적용 전 사용자의 시각 승인과 추가 보정 여부 확인이 필요하다.
- 수행하지 않은 작업:
  - Git 명령
  - Unity 씬/프리팹/런타임 에셋 반영
  - Unity Refresh/Bridge
  - Harness/EditMode/PlayMode 테스트
  - Smoke, Ensure, Validate, Build 계열 명령

## Fuga Unity application with design-size correction

- 작업 범위:
  - 승인된 `artSample/enemies/fuga/` 샘플을 `CargoRunMvp` 씬에 적용했습니다.
  - 원본 기획서 기준 치수 `높이 0.60m / 가로 0.40m / 세로 0.20m` 상한에 맞도록 Unity 씬 스케일을 자동 계산하게 수정했습니다.
  - 정적 비교 상태 1개와 대기, 이동, 공격, 피격, 사망, 섭취 애니메이션 상태 6개를 분리 배치했습니다.
- 반영 파일:
  - `Assets/_Project/Runtime/Enemies/FugaPhysicsMotionDriver.cs`
  - `Assets/_Project/Editor/Validation/FugaCargoRunSceneApplyAndReview.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Art/Enemies/Fuga/`
  - `Assets/_Project/Prefabs/Enemies/Fuga/FugaApproved.prefab`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/validation/fuga_cargo_run_scene/`
- 적용 내용:
  - `Approved Fuga Enemy Placement` 루트 아래 `Fuga_00_Static`부터 `Fuga_06_Consume`까지 7개체를 배치했습니다.
  - Fuga는 `Approved Parvum Enemy Placement`보다 Z축 음수 방향 아래에 배치했습니다.
  - 실행/검토 카메라는 Fuga 전방에서 소형 개체를 볼 수 있는 거리로 재배치했습니다.
  - Rigidbody, Collider, Motion Path target, `FugaPhysicsMotionDriver`를 구성하고 검토용 root motion lock 상태로 두었습니다.
  - 대기는 느린 지속 날갯짓, 이동은 빠르고 큰 날갯짓, 공격은 뒤로 젖힌 뒤 전진하며 날개 끝 타격, 피격은 recoil/squash/wing droop, 사망은 하강/날개 접힘/바닥 근처 정지, 섭취는 입 벌림/전진 bite 모션으로 구성했습니다.
- 크기 확인:
  - 계산된 Unity 씬 스케일은 축별 `X 0.06705959 / Y 0.16991150 / Z 0.07081605`입니다.
  - `Fuga_00_Static` bounds는 `X 0.400m / Y 0.600m / Z 0.200m`로 기록됐습니다.
  - 기획서 기준 `높이 0.60m / 가로 0.40m / 세로 0.20m`에 맞게 들어오는 것을 전용 검사에서 확인했습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 확인 결과:
  - Fuga 적용 로그는 경고와 예외 없이 성공했습니다.
  - Fuga 전용 검사 로그는 `Approved Fuga CargoRunMvp scene state inspected.`로 성공했습니다.
  - 검토 기록은 `docs/validation/fuga_cargo_run_scene/FUGA_UNITY_APPLICATION_REVIEW.json`와 `README.md`에 저장했습니다.
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - 승인된 Fuga `artSample/` 샘플 외형 수정

## Fuga placement closer to Parvum

- 작업 범위:
  - `CargoRunMvp` 씬의 Fuga 배치를 Parvum과 더 가깝게 조정했습니다.
  - Fuga 크기, 모델 외형, 7개 애니메이션 상태는 유지했습니다.
- 반영 내용:
  - `FugaCargoRunSceneApplyAndReview`의 Z축 배치 계산을 Parvum bounds 기준으로 변경했습니다.
  - Fuga 중심 Z 위치는 `Parvum bounds min.z - Fuga half depth - 0.25m` 기준으로 계산합니다.
  - 전체 Fuga 애니메이션 bounds 기준 최소 Z gap은 `0.05m` 이상으로 검사합니다.
  - 검토 기록에 `parvumZGap`과 `parvumZClearance` 값을 저장하도록 했습니다.
- 확인 결과:
  - 최종 `parvumZGap`은 `0.0676m`입니다.
  - 최소 clearance `0.05m`보다 크므로 Parvum과 겹치지 않는 것으로 전용 검사를 통과했습니다.
  - 적용 로그와 검사 로그 모두 Unity 경고/예외 없이 성공했습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 모델 외형/크기/애니메이션 내용 변경
  - Parvum 구현 변경

## Fuga placement matched to corridor-Parvum gap

- 작업 범위:
  - 사용자 지시에 따라 Fuga를 Parvum에 더 가깝게 재배치했습니다.
  - 기준은 복도 bounds와 Parvum bounds 사이의 Z축 간격이며, Parvum과 겹치지 않는 최소 여백을 유지했습니다.
- 반영 내용:
  - `FugaCargoRunSceneApplyAndReview`에 `AdjustPlacementRootZToCorridorParvumGap` 후처리 보정을 추가했습니다.
  - 7개 Fuga 상태의 실제 애니메이션 샘플링 bounds를 계산한 뒤, Parvum bounds와의 Z축 gap이 복도-Parvum gap과 맞도록 root를 다시 이동합니다.
  - 복도-Parvum gap이 `0m`로 계산될 경우 겹침 방지를 위해 최소 `0.01m` gap을 사용합니다.
  - 전용 검사에서 실제 Fuga-Parvum gap이 목표 gap과 일치하는지 확인하도록 했습니다.
- 확인 결과:
  - `corridorParvumZGap`: `0.000m`
  - `desiredFugaParvumZGap`: `0.010m`
  - `parvumZGap`: `0.010m`
  - Fuga는 Parvum과 겹치지 않는 최소 간격으로 더 가깝게 배치됐습니다.
  - Fuga 크기 `X 0.400m / Y 0.600m / Z 0.200m`와 7개 애니메이션 상태는 유지했습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 모델 외형/크기/애니메이션 내용 변경
  - Parvum 구현 변경

## Fuga root placement corrected to visible corridor-Parvum spacing

- 작업 범위:
  - 이전 배치가 renderer bounds 기준으로는 통과했지만 실제 씬 root 위치가 Parvum에서 너무 멀었던 문제를 수정했습니다.
  - Fuga 배치를 renderer bounds가 아니라 root transform Z 위치 기준으로 재계산하게 변경했습니다.
- 반영 내용:
  - `Approved Ship Corridor Segments` root Z와 `Approved Parvum Enemy Placement` root Z의 간격을 계산합니다.
  - Fuga root Z는 `Parvum root Z + directionFromCorridorToParvum * corridorParvumRootGap`으로 배치합니다.
  - 현재 root Z 위치는 Corridor `-15.843`, Parvum `-23.220`, Fuga `-30.597`입니다.
  - 따라서 Corridor-Parvum root gap과 Fuga-Parvum root gap은 모두 `7.377m`입니다.
  - Fuga 크기와 7개 애니메이션 상태는 변경하지 않았습니다.
- 확인 결과:
  - `FUGA_UNITY_APPLICATION_REVIEW.json` 기준 `corridorParvumZGap`: `7.377m`
  - `desiredFugaParvumZGap`: `7.377m`
  - `parvumZGap`: `7.377m`
  - `InspectApprovedFugaCargoRunSceneState`가 성공했습니다.
  - 적용/검사 로그에 Unity 경고나 예외가 없습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 모델 외형/크기/애니메이션 내용 변경
  - Parvum 구현 변경

## Fuga scale 0.25 and reverse facing rotation

- 작업 범위:
  - 사용자 지시에 따라 `Approved Fuga Enemy Placement` 하위 Fuga 7개체의 배치 스케일을 모두 `0.25`로 맞췄습니다.
  - 기존 Y 180도 방향에서 반대 방향을 보도록 Y yaw를 `0`도로 변경했습니다.
- 반영 내용:
  - `FugaCargoRunSceneApplyAndReview`에서 자동 치수 기반 축별 스케일 적용을 중단하고 `Vector3.one * 0.25`를 사용하도록 변경했습니다.
  - Fuga 인스턴스 회전을 `Quaternion.Euler(0, 0, 0)` 기준으로 적용하도록 변경했습니다.
  - 전용 검사에 Fuga 7개체의 uniform scale `0.25`와 yaw `0` 확인을 추가했습니다.
  - 검토 기록에 `requestedUniformScale`과 `facingYawDegrees`를 저장하도록 했습니다.
- 확인 결과:
  - `FUGA_UNITY_APPLICATION_REVIEW.json` 기준 `sceneScaleX/Y/Z`는 모두 `0.25`입니다.
  - `requestedUniformScale`은 `0.25`, `facingYawDegrees`는 `0`입니다.
  - `InspectApprovedFugaCargoRunSceneState`가 성공했습니다.
  - 적용/검사 로그에 Unity 경고나 예외가 없습니다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "ApplyApprovedFugaToCurrentCargoRunScene" -LogPath "Logs\ApplyApprovedFugaToCurrentCargoRunScene.log" -TimeoutSeconds 600`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command "InspectApprovedFugaCargoRunSceneState" -LogPath "Logs\InspectApprovedFugaCargoRunSceneState.log" -TimeoutSeconds 300`
- 실행하지 않은 항목:
  - Git 명령
  - Harness/EditMode/PlayMode/Build/Smoke/Ensure/Validate 계열 명령
  - Fuga 모델 외형/애니메이션 내용 변경
  - Parvum 구현 변경
