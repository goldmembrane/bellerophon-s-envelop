# 진행 기록 2026-06-24

## AR-05 무기실 조종대 armory_shell 샘플 형태 Unity 적용

- 사용자가 현재 무기실 조종대를 `artSample/armory_shell` 샘플에 있는 형태처럼 고치라고 요청했다.
- 적용 기준은 `artSample/armory_shell`의 AR-05 포탑 핸들 샘플로 확인했다.
- 샘플 기준 조종대 형태:
  - 타륜/휠이 아니라 양손으로 잡는 U자형 그립이다.
  - 낮은 베이스 위에 가로 바가 있고, 양쪽에 수직 그립이 있다.
  - 각 수직 그립 상단에는 빨간 엄지 스위치 캡이 있다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command UpdateApprovedArmoryAr05Only -LogPath .\Logs\ApprovedArmoryAr05Only.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateCapture.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - 기존 브리지 명령 `UpdateApprovedArmoryAr05Only`를 사용해 AR-05 조종대만 갱신했다.
  - 적용 후 `CaptureApprovedArmoryShellCurrentState`를 실행해 현재 무기실 상태를 `ApprovedArmoryShellCurrentState.cs` 복구 기준으로 다시 캡처했다.
- 실행하지 않은 작업:
  - `EnsureApprovedArmoryShell` 실행
  - `RestoreApprovedArmoryShellCurrentState` 실행
  - `armory_shell` 전체 재생성
  - `artSample/armory_shell` 파일 수정
  - `APPROVAL_STATUS.json` 승인 상태 변경
  - 코드 파일 수정
  - AR-05 외 무기실 오브젝트 수정
  - 비품실, 동력실, 운송창고, 통제실, 조종실, UI, 매니저 수정
  - Validate/Smoke/Test/Build

## 무기실 전체 현재 편집 상태 스크립트 반영

- 사용자가 Unity에서 직접 편집한 무기실 전체 오브젝트 상태를 스크립트에 반영하라고 요청했다.
- 적용 범위는 `Approved Armory 01 Shell` 루트와 그 하위 전체 Transform/activeSelf 상태로 제한했다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateRestore.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - 현재 Unity 씬의 `Approved Armory 01 Shell` 루트와 하위 전체 Transform/activeSelf 상태를 `ApprovedArmoryShellCurrentState.cs`에 반영했다.
  - 캡처된 상태를 복원 명령으로 다시 적용하고 저장해 무기실의 최종 복구 기준으로 확정했다.
- 실행하지 않은 작업:
  - `EnsureApprovedArmoryShell` 실행
  - 무기실 전체 재생성
  - AR-05 등 특정 오브젝트 재생성 명령 실행
  - 비품실, 동력실, 운송창고, 통제실, 조종실, UI, 매니저 수정
  - `artSample/` 수정
  - 코드 로직 수정
  - Validate/Smoke/Test/Build

## AR-02/AR-03 기둥, 계단, 발판 높이 하향 조정

- 사용자가 무기실의 기둥 높이를 반 이상 줄이고, 계단을 발판 높이에 맞추며, 계단 수도 줄이고, 발판도 낮아진 기둥 높이에 맞춰 옮기라고 요청했다.
- 코드상 대상은 다음으로 구분했다.
  - `AR-02` 중앙 포탑 지지 기둥
  - `AR-03` 후면 계단
  - `AR-04 placeholder top operating platform` 및 `AR-04 placeholder platform safety rim` 발판 계열
- 수정 내용:
  - `AR-02` 기둥 높이를 `1.08`에서 `0.48`로 낮췄다.
  - `AR-03` 계단을 `8`단에서 `5`단으로 줄였다.
  - `AR-03` 계단 최상단 높이를 낮아진 발판 높이 `0.48`에 맞췄다.
  - `AR-04` 발판과 발판 림의 세로 위치를 낮아진 기둥 높이에 맞춰 하향 조정했다.
  - 기존 브리지 명령 `UpdateApprovedArmoryAr02Ar03Only` 경로에 위 조정을 반영했다.
- 수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellCurrentState.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/PROGRESS_2026-06-24.md`
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command UpdateApprovedArmoryAr02Ar03Only -LogPath .\Logs\ApprovedArmoryAr02Ar03Only.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateCapture.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - 적용 후 `CaptureApprovedArmoryShellCurrentState`를 실행해 현재 무기실 상태를 `ApprovedArmoryShellCurrentState.cs` 복구 기준으로 다시 캡처했다.
- 실행하지 않은 작업:
  - `EnsureApprovedArmoryShell` 실행
  - `RestoreApprovedArmoryShellCurrentState` 실행
  - 무기실 전체 재생성
  - AR-02/AR-03/AR-04 발판 계열 외 무기실 오브젝트 수정
  - AR-05 조종대 수정
  - 비품실, 동력실, 운송창고, 통제실, 조종실, UI, 매니저 수정
  - `artSample/` 수정
  - Validate/Smoke/Test/Build

## 무기실 전체 현재 편집 상태 재반영

- 사용자가 Unity에서 직접 편집한 무기실 현재 상태를 스크립트에 반영하라고 요청했다.
- 적용 범위는 `Approved Armory 01 Shell` 루트와 그 하위 전체 Transform/activeSelf 상태로 제한했다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command RestoreApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateRestore.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - 현재 Unity 씬의 `Approved Armory 01 Shell` 루트와 하위 전체 Transform/activeSelf 상태를 `ApprovedArmoryShellCurrentState.cs`에 반영했다.
  - 캡처된 상태를 복원 명령으로 다시 적용하고 저장해 무기실의 최종 복구 기준으로 확정했다.
- 실행하지 않은 작업:
  - `EnsureApprovedArmoryShell` 실행
  - 무기실 전체 재생성
  - 특정 오브젝트 재생성 명령 실행
  - 비품실, 동력실, 운송창고, 통제실, 조종실, UI, 매니저 수정
  - `artSample/` 수정
  - 코드 로직 수정
  - Validate/Smoke/Test/Build

## SR-07 사출대 단말기 승인 ID 정정

- 사용자가 비품실 사출대 단말기 샘플의 승인 ID만 SR-07로 수정하라고 지시했다.
- 수정 내용:
  - `artSample/supply_room_shell/APPROVAL_STATUS.json`의 `objectId`만 `SR-01`에서 `SR-07`로 변경했다.
- 실행하지 않은 작업:
  - Unity Refresh 실행
  - Unity 브리지 명령 실행
  - 비품실 씬 수정
  - 코드 수정
  - `objectId` 외 승인 상태/메모/다른 필드 수정
  - Validate/Smoke/Test/Build

## SR-07 사출대 단말기 HSK 디스플레이 Unity 적용

- 사용자가 비품실 사출대 단말기를 샘플의 디스플레이처럼 수정하라고 요청했다.
- 적용 기준:
  - `artSample/supply_room_shell/renders/10_ejection_terminal_hsk_screen.png`
  - `Assets/Heavy Station Kit/_common/Textures/GUI/HSK_Open_Close.png`
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr07HskScreenOnly -LogPath .\Logs\AddApprovedSupplyRoomSr07HskScreenOnly.log -TimeoutSeconds 360`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - 기존 브리지 명령 `AddApprovedSupplyRoomSr07HskScreenOnly`로 SR-07 사출대 단말기 화면에 HSK Open/Close 디스플레이 텍스처를 적용했다.
  - 적용 후 `CaptureApprovedSupplyRoomShellCurrentState`를 실행해 현재 비품실 상태를 `ApprovedSupplyRoomShellCurrentState.cs` 복구 기준으로 다시 캡처했다.
- 실행하지 않은 작업:
  - 비품실 전체 재생성
  - `RestoreApprovedSupplyRoomShellCurrentState` 실행
  - 사출대 개폐/쿨타임/사출 판정/상호작용 로직 구현
  - SR-07 단말기 디스플레이 외 비품실 오브젝트 수정
  - 무기실, 동력실, 운송창고, 통제실, 조종실, UI, 매니저 수정
  - `artSample/` 추가 수정
  - 코드 로직 수정
  - Validate/Smoke/Test/Build

## SR-07 사출대 단말기 HSK 디스플레이 좌우 반전

- 사용자가 SR-07 HSK 디스플레이의 스케일이나 위치는 변경하지 말고 좌우만 반전하라고 요청했다.
- 수정 내용:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`의 SR-07 HSK 화면 mesh UV X 범위만 반전했다.
  - 디스플레이 위치, 회전, 스케일, 화면 크기, 텍스처 파일은 변경하지 않았다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedSupplyRoomSr07HskScreenOnly -LogPath .\Logs\AddApprovedSupplyRoomSr07HskScreenOnly.log -TimeoutSeconds 360`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
- 결과:
  - 네 명령 모두 종료 코드 `0`으로 완료됐다.
  - SR-07 HSK 디스플레이를 같은 위치와 크기로 다시 생성하되 UV 좌우만 반전했다.
  - 적용 후 `CaptureApprovedSupplyRoomShellCurrentState`를 실행해 현재 비품실 상태를 `ApprovedSupplyRoomShellCurrentState.cs` 복구 기준으로 다시 캡처했다.
- 실행하지 않은 작업:
  - SR-07 디스플레이 위치 변경
  - SR-07 디스플레이 스케일/크기 변경
  - SR-07 디스플레이 텍스처 파일 수정
  - 비품실 전체 재생성
  - `RestoreApprovedSupplyRoomShellCurrentState` 실행
  - 사출대 개폐/쿨타임/사출 판정/상호작용 로직 구현
  - SR-07 HSK 디스플레이 UV 좌우 반전 외 비품실 오브젝트 수정
  - 무기실, 동력실, 운송창고, 통제실, 조종실, UI, 매니저 수정
  - `artSample/` 수정
  - Validate/Smoke/Test/Build

## 비품실 현재 상태 백업 생성

- 사용자가 현재 상태의 비품실 전체 백업본을 하나 만들어 저장하라고 요청했다.
- 백업 전 현재 Unity 씬의 `Approved Supply Room 01 Shell` 상태를 다시 캡처했다.
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
- 결과:
  - 두 명령 모두 종료 코드 `0`으로 완료됐다.
  - 백업 폴더를 생성했다.
  - 경로: `Backups/supply_room/2026-06-24_current-state/`
  - 포함 파일:
    - `CargoRunMvp.unity`
    - `ApprovedSupplyRoomShellCurrentState.cs`
    - `ApprovedSupplyRoomShellCurrentState.cs.meta`
    - `ApprovedSupplyRoomShellCurrentStateCapture.log`
    - `README.md`
- 실행하지 않은 작업:
  - `Ensure` 실행
  - `Restore` 실행
  - 비품실 전체 재생성
  - 비품실 오브젝트 수정
  - 무기실, 동력실, 운송창고, 통제실, 조종실, UI, 매니저 수정
  - `artSample/` 수정
  - 코드 로직 수정
  - Validate/Smoke/Test/Build

## 전체 구역 현재 상태 백업 생성

- 사용자가 모든 구역 오브젝트들의 현재 상태를 날짜까지 기록해 백업본 하나로 저장하라고 요청했다.
- 백업 경로:
  - `Backups/full_object_recovery/2026-06-24_current-state/`
- 실행 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedArmoryShellCurrentState -LogPath .\Logs\ApprovedArmoryShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedSupplyRoomShellCurrentState -LogPath .\Logs\ApprovedSupplyRoomShellCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedControlRoomCurrentState -LogPath .\Logs\ApprovedControlRoomCurrentStateCapture.log -TimeoutSeconds 300`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedControlRoomShellCurrentObjects -LogPath .\Logs\ApprovedControlRoomShellCurrentObjectsCapture.log -TimeoutSeconds 300`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedEngineRoomShellCurrentObjects -LogPath .\Logs\ApprovedEngineRoomShellCurrentObjectsCapture.log -TimeoutSeconds 300`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedEngineRoomHealthScreenCurrentObjects -LogPath .\Logs\ApprovedEngineRoomHealthScreenCurrentObjectsCapture.log -TimeoutSeconds 300`
- 결과:
  - 첫 무기실 캡처 요청은 비품실 캡처와 동시에 실행되어 시간 제한에 걸렸고, 같은 무기실 캡처 명령을 순차 실행해 종료 코드 `0`으로 완료했다.
  - 비품실, 통제실, 동력실 관련 캡처 명령은 종료 코드 `0`으로 완료됐다.
  - 날짜가 포함된 전체 복구 백업 폴더를 생성했다.
  - 백업에는 `CargoRunMvp.unity`, current-state/current-objects 스크립트, 관련 캡처 로그, `artSample/**/editor_current/` 기록, `README.md`를 포함했다.
- 실행하지 않은 작업:
  - `Ensure` 실행
  - `Restore` 실행
  - 구역/오브젝트 재생성
  - 씬 오브젝트 수정
  - 코드 로직 수정
  - `artSample/` 수정
  - Validate/Smoke/Test/Build
  - 기존 백업 폴더 삭제/덮어쓰기

## 운송창고 오브젝트 목록 문서 작성

- 사용자가 원본 기획서를 읽어서 운송창고 관련 오브젝트 목록을 문서로 정리하라고 요청했다.
- 확인한 기준 문서:
  - `docs/GAME_DESIGN_SOURCE.txt`
  - `docs/MVP_IMPLEMENTATION_ORDER.md`
  - `docs/PROGRESS_2026-06-24.md`
- 생성한 문서:
  - `docs/CARGO_HOLD_OBJECTS.md`
- 정리 내용:
  - 운송창고 룸 셸, 중앙 화물, 가장자리 이동 영역, 5개 복도 연결 지점, 방향 표시, 화물 상태 표시 지점을 확정 오브젝트로 정리했다.
  - 운송창고 구역 내구도, 화물 내구도, 화물 손실률, 창고 점수, 개인 화물 운송 가능 여부를 상태 연동 대상으로 분리했다.
  - 원본 기획서와 MVP 구현 순서에 따라 화물 직접 집기/운반/납품 상호작용은 제외 항목으로 명시했다.
  - 원본에서 확정되지 않은 중앙 화물 외형, 연결 지점의 정확한 배치, 상태 표시 지점 형태는 확인 필요 항목으로 남겼다.
- 실행하지 않은 작업:
  - Unity 실행/Refresh/브리지 명령 실행
  - Ensure/Restore/Validate/Smoke/Test/Build
  - 씬, 프리팹, 런타임 자산 수정
  - `artSample/` 생성 또는 수정
  - 코드 파일 수정
  - 원본 기획서에 없는 오브젝트 확정

## 운송창고 셸 artSample 1차 제작

- 사용자가 중앙 화물은 여러 개 의뢰를 받아도 단일 컨테이너로 보이게 할 것이라고 확정하고, 운송창고 샘플 제작을 요청했다.
- 반영한 문서 변경:
  - `docs/CARGO_HOLD_OBJECTS.md`에 중앙 화물 단일 컨테이너 확정 사항을 추가했다.
  - `CH-03` 설명을 여러 의뢰 수와 무관하게 단일 컨테이너로 시각 표시하는 대표 모델로 갱신했다.
  - 중앙 화물 기본 외형 확인 필요 항목을 제거했다.
- 생성/수정한 파일:
  - `scripts/GenerateCargoHoldShellSample.py`
  - `scripts/Run-CargoHoldShellSample.ps1`
  - `artSample/cargo_hold_shell/README.md`
  - `artSample/cargo_hold_shell/index.html`
  - `artSample/cargo_hold_shell/ASSET_MANIFEST.json`
  - `artSample/cargo_hold_shell/APPROVAL_STATUS.json`
  - `artSample/cargo_hold_shell/renders/01_overview_perspective.png`
  - `artSample/cargo_hold_shell/renders/02_floor_plan.png`
  - `artSample/cargo_hold_shell/renders/03_cargo_status_panel.png`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell_layout.json`
- 샘플 구성:
  - 운송창고 룸 셸
  - 중앙 단일 컨테이너 화물
  - 중앙 컨테이너 주변 가장자리 이동 영역
  - 조종실, 동력실, 통제실, 무기실, 비품실 5개 연결 지점
  - 연결 방향 표시
  - 화물 내구도, 손실률, 창고 점수 표시용 상태 패널 자리
- 실행 명령:
  - `.\scripts\Run-CargoHoldShellSample.ps1`
- 결과:
  - 명령은 종료 코드 `0`으로 완료됐다.
  - 외부 이미지 라이브러리 없이 PNG 렌더 3개와 HTML 프리뷰를 생성했다.
  - `APPROVAL_STATUS.json`은 `pending_user_approval` 상태로 저장했다.
- 실행하지 않은 작업:
  - Unity 실행/Refresh/브리지 명령 실행
  - Unity 씬, 프리팹, 런타임 자산 연결
  - Ensure/Restore/Validate/Smoke/Test/Build
  - 화물 직접 집기/운반/납품 구현
  - 크레인, 지게차, 컨베이어, 팔레트 랙, 선반형 적재 시스템 추가
  - 운송창고 자동 포탑 추가
  - 런타임 코드 로직 수정
  - 기존 무기실, 비품실, 동력실, 통제실, 조종실 오브젝트 수정

## 운송창고 셸 artSample 기존 프리뷰 방식 재제작

- 사용자가 `artSample/armory_shell/index.html`을 참고해 운송창고 아트 샘플을 기존 방식대로 다시 제작하라고 요청했다.
- 작업 전 확인한 적용 규칙:
  - 작업 수행 전 해당 작업에 적용되는 규칙을 먼저 파악해야 한다.
  - 이어서 작업이므로 오늘 진행문서 기준을 확인해야 한다.
  - 아트 샘플은 Unity 씬/프리팹/런타임 자산 연결 전 `artSample/`에 검사 가능한 파일로 저장해야 한다.
  - 기존 `artSample`이 기준이면 새 스타일을 만들지 않고 기준 샘플의 프리뷰 구조를 따라야 한다.
  - 사용자 승인 전에는 Unity에 연결하지 않는다.
- 기준으로 확인한 파일:
  - `artSample/armory_shell/index.html`
  - `artSample/armory_shell/README.md`
  - `artSample/armory_shell/ASSET_MANIFEST.json`
  - `artSample/armory_shell/APPROVAL_STATUS.json`
- 재제작 내용:
  - `artSample/cargo_hold_shell/index.html`을 `armory_shell/index.html`과 같은 단일 설명 문단 + 2열 figure grid 구조로 변경했다.
  - manifest를 `sample`, `objectId`, `title`, `approvalState`, `unityApplicationAllowed`, `requiresUserApprovalBeforeUnity`, `sourceBasis`, `generatedFiles`, `includedParts`, `excludedParts` 형식으로 맞췄다.
  - 승인 상태 파일을 `approvalState: 미승인`, `unityApplicationAllowed: false` 형식으로 맞췄다.
  - 렌더 이름을 기존 방식에 맞춰 `01_overview.png`부터 `06_status_display.png`까지 6개 구도로 재생성했다.
  - 기존 섹션형 HTML과 이전 3개 렌더 파일은 새 방식에 맞춰 교체했다.
- 생성/수정한 파일:
  - `scripts/GenerateCargoHoldShellSample.py`
  - `artSample/cargo_hold_shell/README.md`
  - `artSample/cargo_hold_shell/index.html`
  - `artSample/cargo_hold_shell/ASSET_MANIFEST.json`
  - `artSample/cargo_hold_shell/APPROVAL_STATUS.json`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell_layout.json`
  - `artSample/cargo_hold_shell/renders/01_overview.png`
  - `artSample/cargo_hold_shell/renders/02_floor_plan.png`
  - `artSample/cargo_hold_shell/renders/03_single_container.png`
  - `artSample/cargo_hold_shell/renders/04_edge_walkway.png`
  - `artSample/cargo_hold_shell/renders/05_connection_points.png`
  - `artSample/cargo_hold_shell/renders/06_status_display.png`
- 실행 명령:
  - `.\scripts\Run-CargoHoldShellSample.ps1`
- 결과:
  - 명령은 종료 코드 `0`으로 완료됐다.
  - 운송창고 샘플은 중앙 단일 컨테이너, 가장자리 이동 영역, 5개 연결 지점, 방향 표시, CH-11 상태 패널 자리 중심으로 재작성됐다.
- 실행하지 않은 작업:
  - Unity 실행/Refresh/브리지 명령 실행
  - Unity 씬, 프리팹, 런타임 자산, UI 흐름 연결
  - Ensure/Restore/Validate/Smoke/Test/Build
  - `artSample/armory_shell` 수정
  - 무기실, 비품실, 동력실, 통제실, 조종실 오브젝트 수정
  - 런타임 게임 로직 수정
  - 운송창고 샘플 승인 상태를 임의로 `approved`로 변경
  - 원본 기획서에 없는 크레인, 지게차, 컨베이어, 팔레트 랙, 자동 포탑 추가
  - 화물 직접 집기/운반/납품 상호작용 추가

## 운송창고 셸 복도 입구 방향 재배치

- 사용자가 운송창고 샘플의 전체 구조는 원하는 방향으로 나왔지만 복도 입구 방향을 조정해야 한다고 요청했다.
- 최종 사용자 지정 배치:
  - 12시: 조종실
  - 3시: 통제실
  - 9시: 동력실
  - 5시: 무기실
  - 7시: 비품실
- 수정 내용:
  - 기존 북쪽 2개 입구, 좌우 1개씩, 남쪽 1개 입구 배치를 제거했다.
  - `CH-05` 조종실 입구를 12시 방향 중앙으로 배치했다.
  - `CH-07` 통제실 입구를 3시 방향으로 배치했다.
  - `CH-06` 동력실 입구를 9시 방향으로 배치했다.
  - `CH-08` 무기실 입구를 5시 방향 대각선 입구로 배치했다.
  - `CH-09` 비품실 입구를 7시 방향 대각선 입구로 배치했다.
  - 남쪽 양 모서리를 열어 5시/7시 대각선 복도 스텁과 문틀이 실제 모델에서 뚫린 입구처럼 보이도록 했다.
  - README, manifest, HTML 설명 문구도 새 방향 배치 기준으로 갱신했다.
- 생성/수정한 파일:
  - `scripts/GenerateCargoHoldShellSample.py`
  - `artSample/cargo_hold_shell/blender/cargo_hold_shell.blend`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell.fbx`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell.glb`
  - `artSample/cargo_hold_shell/renders/01_overview.png`
  - `artSample/cargo_hold_shell/renders/02_floor_plan.png`
  - `artSample/cargo_hold_shell/renders/03_single_container.png`
  - `artSample/cargo_hold_shell/renders/04_edge_walkway.png`
  - `artSample/cargo_hold_shell/renders/05_connection_points.png`
  - `artSample/cargo_hold_shell/renders/06_status_display.png`
  - `artSample/cargo_hold_shell/index.html`
  - `artSample/cargo_hold_shell/README.md`
  - `artSample/cargo_hold_shell/ASSET_MANIFEST.json`
  - `artSample/cargo_hold_shell/APPROVAL_STATUS.json`
- 실행 명령:
  - `.\scripts\Run-CargoHoldShellSample.ps1`
- 결과:
  - 명령은 종료 코드 `0`으로 완료됐다.
  - 실행 스크립트의 필수 산출물 검사가 통과했다.
  - `APPROVAL_STATUS.json`은 `미승인`, `unityApplicationAllowed: false` 상태로 유지했다.
- 실행하지 않은 작업:
  - Unity 실행/Refresh/브리지 명령 실행
  - Unity 씬, 프리팹, 런타임 자산, UI 흐름 연결
  - Ensure/Restore/Validate/Smoke/Test/Build
  - 운송창고 샘플 승인 상태를 임의로 승인 처리
  - `artSample/armory_shell` 수정
  - 다른 구역 오브젝트 수정
  - 런타임 게임 로직 수정
  - 화물 직접 집기/운반/납품 상호작용 추가

## 운송창고 셸 artSample 모델링 기반 재제작

- 사용자가 `artSample/armory_shell/index.html`처럼 그림이 아니라 실제 모델링 산출물을 만들고, 그 모델을 여러 구도로 렌더해 보여달라고 정정했다.
- 잘못된 이전 산출물 판단:
  - 이전 운송창고 샘플은 2D 그림 PNG 중심이었고, `armory_shell` 방식의 `.blend`, `.fbx`, `.glb` 모델링 샘플이 아니었다.
  - 따라서 이전 완료 보고는 모델링 샘플 기준으로 부정확했다.
- 작업 전 확인한 적용 규칙:
  - 작업 수행 전 해당 작업에 적용되는 규칙을 먼저 파악해야 한다.
  - 이어서 작업이므로 오늘 진행문서 기준을 확인해야 한다.
  - 아트 샘플은 Unity 씬/프리팹/런타임 자산 연결 전 `artSample/`에 검사 가능한 파일로 저장해야 한다.
  - 기존 `artSample/armory_shell` 방식은 Blender 모델 파일과 범용 export, 여러 구도 렌더를 포함한다.
  - 사용자 승인 전에는 Unity에 연결하지 않는다.
- 기준으로 확인한 파일/범위:
  - `artSample/armory_shell/index.html`
  - `artSample/armory_shell/README.md`
  - `artSample/armory_shell/ASSET_MANIFEST.json`
  - `artSample/armory_shell/APPROVAL_STATUS.json`
  - `artSample/armory_shell` 하위 파일 목록
  - `scripts/GenerateArmoryShellSample.py`
  - `scripts/Run-ArmoryShellSample.ps1`
- 재제작 내용:
  - `scripts/GenerateCargoHoldShellSample.py`를 Blender 전용 `bpy` 모델 생성 스크립트로 교체했다.
  - `scripts/Run-CargoHoldShellSample.ps1`을 무기실 샘플 실행 스크립트와 같은 Blender 후보 경로 탐색/필수 산출물 검증 방식으로 교체했다.
  - 운송창고 룸 셸, 중앙 단일 컨테이너, 가장자리 이동 영역, 5개 연결 출입구/복도 스텁, 방향 표시, CH-11 상태 패널을 3D 모델 오브젝트로 생성했다.
  - 렌더는 실제 Blender 모델을 카메라 6개로 촬영해 생성하도록 바꿨다.
- 생성/수정한 파일:
  - `scripts/GenerateCargoHoldShellSample.py`
  - `scripts/Run-CargoHoldShellSample.ps1`
  - `artSample/cargo_hold_shell/blender/cargo_hold_shell.blend`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell.fbx`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell.glb`
  - `artSample/cargo_hold_shell/renders/01_overview.png`
  - `artSample/cargo_hold_shell/renders/02_floor_plan.png`
  - `artSample/cargo_hold_shell/renders/03_single_container.png`
  - `artSample/cargo_hold_shell/renders/04_edge_walkway.png`
  - `artSample/cargo_hold_shell/renders/05_connection_points.png`
  - `artSample/cargo_hold_shell/renders/06_status_display.png`
  - `artSample/cargo_hold_shell/index.html`
  - `artSample/cargo_hold_shell/README.md`
  - `artSample/cargo_hold_shell/ASSET_MANIFEST.json`
  - `artSample/cargo_hold_shell/APPROVAL_STATUS.json`
- 실행 명령:
  - `Get-Command blender`
  - `.\scripts\Run-CargoHoldShellSample.ps1`
- 결과:
  - `Get-Command blender`는 PATH에서 Blender를 찾지 못했다.
  - `Run-CargoHoldShellSample.ps1`은 후보 설치 경로에서 Blender를 찾아 실행했고, 종료 코드 `0`으로 완료됐다.
  - 실행 스크립트의 필수 산출물 검사가 통과했다.
  - `APPROVAL_STATUS.json`은 `미승인`, `unityApplicationAllowed: false` 상태로 유지했다.
- 실행하지 않은 작업:
  - Unity 실행/Refresh/브리지 명령 실행
  - Unity 씬, 프리팹, 런타임 자산, UI 흐름 연결
  - Ensure/Restore/Validate/Smoke/Test/Build
  - `artSample/armory_shell` 수정
  - 무기실, 비품실, 동력실, 통제실, 조종실 오브젝트 수정
  - 런타임 게임 로직 수정
  - 운송창고 샘플 승인 상태를 임의로 `approved`로 변경
  - 원본 기획서에 없는 크레인, 지게차, 컨베이어, 팔레트 랙, 자동 포탑 추가
  - 화물 직접 집기/운반/납품 상호작용 추가

## 운송창고 셸 5시/7시 직선 입구 재배치

- 사용자 요청:
  - 5시와 7시 입구를 대각선으로 뚫지 않고, 아래쪽 벽의 오른쪽 끝과 왼쪽 끝에 각각 직선 입구로 뚫도록 수정한다.
- 최종 배치:
  - 12시: 조종실
  - 3시: 통제실
  - 9시: 동력실
  - 아래쪽 오른쪽 끝, 5시 방향: 무기실
  - 아래쪽 왼쪽 끝, 7시 방향: 비품실
- 수정 내용:
  - `scripts/GenerateCargoHoldShellSample.py`에서 남쪽 대각선 복도 스텁 호출을 제거하고, 남쪽 벽에 비품실/무기실 직선 출입구 2개를 배치했다.
  - 비품실/무기실 복도 스텁을 남쪽 방향 직선 복도로 다시 생성했다.
  - 남쪽 벽 리브가 새 출입구를 가리지 않도록 남쪽 리브 위치를 중앙부로 제한했다.
  - 연결 지점 렌더 구도에서 남쪽 직선 입구 2개가 잘리기 어렵도록 `05_connection_points.png` 카메라 직교 스케일을 조정했다.
- 생성/갱신된 산출물:
  - `artSample/cargo_hold_shell/blender/cargo_hold_shell.blend`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell.fbx`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell.glb`
  - `artSample/cargo_hold_shell/renders/01_overview.png`
  - `artSample/cargo_hold_shell/renders/02_floor_plan.png`
  - `artSample/cargo_hold_shell/renders/03_single_container.png`
  - `artSample/cargo_hold_shell/renders/04_edge_walkway.png`
  - `artSample/cargo_hold_shell/renders/05_connection_points.png`
  - `artSample/cargo_hold_shell/renders/06_status_display.png`
  - `artSample/cargo_hold_shell/index.html`
  - `artSample/cargo_hold_shell/README.md`
  - `artSample/cargo_hold_shell/ASSET_MANIFEST.json`
  - `artSample/cargo_hold_shell/APPROVAL_STATUS.json`
- 실행 명령:
  - `.\scripts\Run-CargoHoldShellSample.ps1`
- 결과:
  - 명령은 종료 코드 `0`으로 완료됐다.
  - 실행 스크립트의 필수 산출물 검사가 통과했다.
  - `APPROVAL_STATUS.json`은 미승인 상태와 `unityApplicationAllowed: false` 상태를 유지했다.
- 실행하지 않은 작업:
  - Unity 실행/Refresh/브리지 명령 실행
  - Unity 씬, 프리팹, 런타임 자산, UI 흐름 연결
  - Ensure/Restore/Validate/Smoke/Test/Build 실행
  - `artSample/armory_shell` 수정
  - 다른 구역 오브젝트 수정
  - 런타임 게임 로직 수정
  - 운송창고 샘플 승인 상태 임의 변경
  - 화물 직접 집기/운반/납품 상호작용 추가

## 운송창고 셸 artSample 조건부 승인

- 사용자 승인:
  - `artSample/cargo_hold_shell` 샘플은 승인한다.
- Unity 반영 조건:
  - 운송창고의 면적 및 전체 크기는 현재 artSample 기준보다 최소 2배 이상 커야 한다.
  - Unity 배치 시 운송창고는 조종실과 X축을 맞춘다.
  - Unity 배치 시 운송창고의 Z축 위치는 무기실보다 아래쪽에 둔다.
  - 현재 artSample은 구조, 연결 방향, 주요 구성 비율의 승인 기준이며 실제 Unity 구현에서는 위 크기와 배치 조건을 우선한다.
- 수정 파일:
  - `artSample/cargo_hold_shell/APPROVAL_STATUS.json`
  - `artSample/cargo_hold_shell/ASSET_MANIFEST.json`
  - `artSample/cargo_hold_shell/README.md`
  - `docs/PROGRESS_2026-06-24.md`
- 실행한 명령:
  - `Get-Content`로 승인된 문서 및 상태 파일 확인
- 실행하지 않은 작업:
  - Unity 실행/Refresh/브리지 명령 실행
  - Unity 씬, 프리팹, 런타임 자산, UI 흐름 연결
  - Ensure/Restore/Validate/Smoke/Test/Build 실행
  - 운송창고 실제 구현 또는 배치 변경
  - `artSample/armory_shell` 및 다른 구역 오브젝트 수정
  - 화물 상호작용/게임 로직 추가

## 운송창고 셸 Unity 구현

- 사용자 요청:
  - 승인된 `artSample/cargo_hold_shell` 샘플을 Unity 씬에 구현한다.
- 작업 전 확인한 적용 규칙:
  - 승인된 artSample은 Unity 구현 시 최대한 그대로 재현한다.
  - Unity 반영 조건으로 운송창고 크기는 artSample 기준 최소 2배 이상, 조종실과 X축 정렬, 무기실보다 Z축 아래 배치를 적용한다.
  - 승인된 단일 작업 범위 밖의 다른 구역 오브젝트, 런타임 로직, Ensure/Validate/Smoke/Test/Build는 수행하지 않는다.
- 반영한 내용:
  - `Approved Cargo Hold 01 Shell` 루트를 `CargoRunMvp` 씬에 생성했다.
  - 루트 스케일을 `2.2`로 적용해 artSample 기준 최소 2배 이상 크기 조건을 반영했다.
  - 조종실 루트와 X축을 맞추고, 무기실 bounds보다 Z축 아래쪽 후보 위치를 찾아 배치했다.
  - 승인 샘플의 5개 출입구 구성을 반영했다.
    - 12시: 조종실
    - 3시: 통제실
    - 9시: 동력실
    - 아래쪽 오른쪽 끝: 무기실
    - 아래쪽 왼쪽 끝: 비품실
  - 중앙 화물은 여러 의뢰 수와 무관하게 단일 컨테이너로 보이도록 `CH-03 single central cargo container` 계열 오브젝트로 구성했다.
  - 바닥 패널, 벽체/문틀, 복도 스텁, 가장자리 이동 영역, 방향 마커, 상태 단말 패널을 프리미티브 기반으로 생성했다.
  - 문서 기준 ID에 맞춰 가장자리 이동 영역은 `CH-04`, 연결 지점은 `CH-05`부터 `CH-09`, 방향 표시는 `CH-10`, 화물 상태 패널은 `CH-11` 이름으로 정리했다.
  - 생성 직후 현재 상태를 `ApprovedCargoHoldShellCurrentState.cs`에 저장했다.
- 수정/생성 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/PROGRESS_2026-06-24.md`
- 실행한 명령:
  - `.\scripts\Refresh-UnityProject.ps1` 2회
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CreateApprovedCargoHoldShell -LogPath .\Logs\CreateApprovedCargoHoldShell.log -TimeoutSeconds 360` 2회
- 확인 결과:
  - 브리지 명령은 종료 코드 `0`으로 완료됐다.
  - 씬 파일에서 `Approved Cargo Hold 01 Shell`, `CH-01 sealed cargo hold deck floor`, `CH-03 single central cargo container body`, `CH-11 cargo status panel wall body`가 확인됐다.
  - `ApprovedCargoHoldShellCurrentState.cs`가 빈 스텁에서 실제 transform 목록으로 갱신됐다.
  - 최종 확인에서 이전 잘못된 `CH-06 single central cargo container body`, `CH-07 cargo manifest terminal wall body` 이름은 씬에 남아 있지 않았다.
- 실행하지 않은 작업:
  - Ensure/Validate/Smoke/Test/Build
  - 다른 구역 오브젝트 수정
  - 화물 직접 집기/운반/납품 상호작용 추가
  - 런타임 게임 로직 추가
## 운송창고 현재 편집 상태 스크립트 반영 및 백업

- 사용자 요청:
  - Unity에서 사용자가 편집한 현재 운송창고 형태를 스크립트에 반영하고 백업본을 만든다.
- 반영한 내용:
  - `CaptureApprovedCargoHoldShellCurrentState` 브리지 명령으로 현재 `Approved Cargo Hold 01 Shell` transform 상태를 캡처했다.
  - 캡처 결과를 `ApprovedCargoHoldShellCurrentState.cs`에 반영했다.
  - 같은 내용을 날짜 포함 백업본으로 복사했다.
- 수정/생성 파일:
  - `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`
  - `docs/backups/ApprovedCargoHoldShellCurrentState_2026-06-24.cs.txt`
  - `docs/PROGRESS_2026-06-24.md`
- 실행한 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedCargoHoldShellCurrentState -LogPath .\Logs\CaptureApprovedCargoHoldShellCurrentState.log -TimeoutSeconds 360`
  - `New-Item -ItemType Directory -Force -Path docs/backups`
  - `Copy-Item -LiteralPath Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs -Destination docs/backups/ApprovedCargoHoldShellCurrentState_2026-06-24.cs.txt`
- 결과:
  - 리프레시와 캡처 브리지 명령은 종료 코드 `0`으로 완료됐다.
  - 백업 디렉터리 생성과 백업 복사는 종료 코드 `0`으로 완료됐다.
- 실행하지 않은 작업:
  - 운송창고 재생성, Restore, Ensure, Validate, Smoke, Test, Build
  - 다른 구역 오브젝트 수정
  - 런타임 게임 로직 추가/수정
  - `artSample/` 수정
## CH-10 운송창고 연결 방향 표시 cargo_hold_shell 통합 샘플 정리

- 사용자 지적:
  - `CH-10`은 별도 샘플로 분리할 필요가 없고, 기존 `artSample/cargo_hold_shell/` 샘플에 추가해서 보여주면 된다고 수정 지시했다.
- 조치:
  - 별도 샘플 폴더 `artSample/cargo_hold_ch10_direction_markers/`를 삭제했다.
  - 별도 샘플 생성/실행 스크립트 `scripts/GenerateCargoHoldCh10DirectionMarkersSample.py`, `scripts/Run-CargoHoldCh10DirectionMarkersSample.ps1`를 삭제했다.
  - 기존 `scripts/GenerateCargoHoldShellSample.py` 안에 `CH-10` 방향 표시를 통합했다.
- 반영한 내용:
  - 기존 운송창고 룸 셸, 중앙 단일 컨테이너, 가장자리 이동 영역, `CH-05`부터 `CH-09` 연결 지점, `CH-11` 상태 패널은 유지했다.
  - 5개 연결 지점 안쪽에 `CH-10` 방향 표시 오브젝트를 추가했다.
    - 12시: `COCKPIT`
    - 3시: `CONTROL`
    - 9시: `ENGINE`
    - 아래쪽 오른쪽 끝: `ARMORY`
    - 아래쪽 왼쪽 끝: `SUPPLY`
  - 각 표시는 컬러 바닥 플레이트, 흰색 진행 화살표, 어두운 라벨 백킹, 돌출 텍스트, 마모 트림으로 구성했다.
  - `ASSET_MANIFEST.json`, `README.md`, `index.html` 설명을 기존 `cargo_hold_shell` 안에 `CH-10`이 포함된 상태로 갱신했다.
  - `APPROVAL_STATUS.json`은 미승인, Unity 반영 불가 상태로 유지했다.
- 수정/갱신 파일:
  - `scripts/GenerateCargoHoldShellSample.py`
  - `artSample/cargo_hold_shell/blender/cargo_hold_shell.blend`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell.fbx`
  - `artSample/cargo_hold_shell/exports/cargo_hold_shell.glb`
  - `artSample/cargo_hold_shell/renders/01_overview.png`
  - `artSample/cargo_hold_shell/renders/02_floor_plan.png`
  - `artSample/cargo_hold_shell/renders/03_single_container.png`
  - `artSample/cargo_hold_shell/renders/04_edge_walkway.png`
  - `artSample/cargo_hold_shell/renders/05_connection_points.png`
  - `artSample/cargo_hold_shell/renders/06_status_display.png`
  - `artSample/cargo_hold_shell/index.html`
  - `artSample/cargo_hold_shell/README.md`
  - `artSample/cargo_hold_shell/ASSET_MANIFEST.json`
  - `artSample/cargo_hold_shell/APPROVAL_STATUS.json`
  - `docs/PROGRESS_2026-06-24.md`
- 삭제 파일:
  - `artSample/cargo_hold_ch10_direction_markers/`
  - `scripts/GenerateCargoHoldCh10DirectionMarkersSample.py`
  - `scripts/Run-CargoHoldCh10DirectionMarkersSample.ps1`
- 실행한 명령:
  - `Remove-Item`으로 삭제 대상이 작업공간 내부인지 확인한 뒤 별도 샘플 폴더와 별도 스크립트 2개 삭제
  - `.\scripts\Run-CargoHoldShellSample.ps1`
  - 산출물 존재 확인
  - UTF-8 기준 `APPROVAL_STATUS.json`, `ASSET_MANIFEST.json` JSON 파싱 확인
  - `02_floor_plan.png`, `05_connection_points.png` 렌더 직접 확인
- 결과:
  - 첫 `Run-CargoHoldShellSample.ps1` 실행은 렌더 시간이 길어 124초 제한에 걸려 중단됐다.
  - 제한 시간을 늘려 같은 명령을 재실행했고 종료 코드 `0`으로 완료됐다.
  - `.blend`, `.fbx`, `.glb`, 6개 렌더 PNG, HTML 미리보기, README, manifest, approval 파일이 기존 `cargo_hold_shell` 경로에 갱신됐다.
  - `02_floor_plan.png`에서 12시/3시/9시/5시/7시의 `CH-10` 방향 표시가 기존 룸 셸 안에 통합된 것을 확인했다.
- 실행하지 않은 작업:
  - Unity 리프레시/브리지/씬/프리팹/런타임 자산 수정
  - 운송창고 현재 상태 스크립트 수정
  - `CH-01`부터 `CH-09`, `CH-11` 형상/배치 수정
  - 조종실, 통제실, 동력실, 무기실, 비품실 오브젝트 수정
  - Restore/Ensure/Validate/Smoke/Test/Build
  - 런타임 게임 로직 추가/수정

## CH-10 운송창고 연결 방향 표시 Unity 단독 반영

- 사용자 승인:
  - 기존 `artSample/cargo_hold_shell/`에 통합된 `CH-10` 방향 표시 샘플을 승인했다.
  - Unity에는 다른 운송창고 오브젝트를 건드리지 말고 `CH-10` 방향 표시만 적용하라고 지시했다.
- 반영한 내용:
  - `artSample/cargo_hold_shell/APPROVAL_STATUS.json`을 승인 및 Unity 반영 가능 상태로 갱신했다.
  - `CreateApprovedCargoHoldShell` 전체 재생성 명령은 사용하지 않고, `CH-10` 접두사 오브젝트만 삭제/재생성하는 `AddApprovedCargoHoldCh10DirectionMarkersOnly` 경로를 추가했다.
  - 기존 단순 `CH-10 ... floor threshold marker` 5개를 상세 방향 표시 그룹 5개로 교체했다.
  - 각 그룹은 컬러 바닥 플레이트, 어두운 라벨 백킹, 흰색 진행 화살표, 좌우 화살표 헤드, 마모 트림, 돌출 텍스트로 구성했다.
  - `ApprovedCargoHoldShellCurrentState.cs`를 현재 씬 상태로 갱신했다.
- 수정 파일:
  - `artSample/cargo_hold_shell/APPROVAL_STATUS.json`
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/PROGRESS_2026-06-24.md`
- 실행한 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedCargoHoldCh10DirectionMarkersOnly -LogPath .\Logs\AddApprovedCargoHoldCh10DirectionMarkersOnly.log -TimeoutSeconds 360`
  - 브리지 로그 확인
  - `ApprovedCargoHoldShellCurrentState.cs`와 `CargoRunMvp.unity`에서 `CH-10` 상세 부품 존재 확인
- 결과:
  - 첫 브리지 실행은 `APPROVAL_STATUS.json`의 한국어 승인값 `승인`을 기존 운송창고 승인 검사 함수가 허용하지 않아 내부 실패했다.
  - 승인 검사 함수에 `승인` 허용 조건을 추가하고 같은 브리지 명령을 재실행했다.
  - 두 번째 브리지 실행은 성공했고 로그에 `Ch10Objects=45`, `NonCh10CargoHoldObjectsUntouched=True`, `ExistingObjectsUntouched=True`가 기록됐다.
  - 씬과 현재 상태 스크립트에서 기존 단순 `CH-10 ... floor threshold marker` 항목은 사라지고, 5개 방향별 상세 `CH-10 ... direction marker` 그룹 및 하위 부품이 확인됐다.
- 실행하지 않은 작업:
  - `CreateApprovedCargoHoldShell`을 통한 운송창고 전체 재생성
  - `RestoreApprovedCargoHoldShellCurrentState`
  - `CH-01`부터 `CH-09`, `CH-11` 형상/배치 수정
  - 조종실, 통제실, 동력실, 무기실, 비품실 오브젝트 수정
  - Validate/Smoke/Test/Build
  - 런타임 게임 로직 추가/수정
