# 2026-06-25 Progress

## CH-10 운송창고 방향 표시 문구 출력 수정

- 사용자 지적:
  - `CH-10` 방향 표시의 문구가 깨진 것이 아니라, Unity에서 회색 판 조각처럼 출력되고 있었다.
  - 문구가 제대로 출력되도록 하고, 라벨 안에 맞는 크기로 조정하라고 요청했다.
- 확인한 원인:
  - 기존 `CH-10` 문구는 단일 `TextMesh`에 일반 라벨 머티리얼을 직접 붙이고 있었다.
  - Unity `TextMesh`는 폰트 텍스처가 있는 머티리얼을 사용해야 하므로, 일반 머티리얼을 붙이면 글자가 아니라 글리프 판처럼 보일 수 있다.
- 반영한 내용:
  - `CH-10` 방향 표시 문구를 `CreateSupplyText` 단일 텍스트 방식에서 `CreateSupplyReadableTextGroup` 기반 글자별 텍스트 방식으로 변경했다.
  - `COCKPIT`, `CONTROL`, `ENGINE`, `ARMORY`, `SUPPLY`가 각 라벨 백킹 안에 들어가도록 `fitWidth`와 `characterSize`를 조정했다.
  - `CH-10` 접두사 오브젝트만 삭제/재생성하는 `AddApprovedCargoHoldCh10DirectionMarkersOnly` 브리지 명령으로 Unity 씬에 다시 적용했다.
  - `ApprovedCargoHoldShellCurrentState.cs`를 재적용 후 현재 씬 상태로 갱신했다.
- 수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/PROGRESS_2026-06-25.md`
- 실행한 명령:
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedCargoHoldCh10DirectionMarkersOnly -LogPath .\Logs\AddApprovedCargoHoldCh10DirectionMarkersOnly.log -TimeoutSeconds 360`
  - 브리지 로그 및 `ApprovedCargoHoldShellCurrentState.cs`의 `CH-10` 문구 오브젝트 확인
- 결과:
  - 브리지 명령은 종료 코드 `0`으로 완료됐다.
  - 로그에 `Ch10Objects=77`, `NonCh10CargoHoldObjectsUntouched=True`, `ExistingObjectsUntouched=True`가 기록됐다.
  - 기존 단일 `CH-10 ... raised floor label` 오브젝트는 사라졌고, 방향별 `CH-10 ... readable floor label` 및 글자별 하위 오브젝트가 생성됐다.
- 실행하지 않은 작업:
  - `CH-10` 외 운송창고 오브젝트 수정
  - 운송창고 전체 재생성
  - 다른 구역 오브젝트 수정
  - Restore/Ensure/Validate/Smoke/Test/Build
  - 런타임 게임 로직 추가/수정

## CH-10 방향 라벨 글자 오브젝트 스케일 조정

- 사용자 지시:
  - 글자 크기 자체를 줄이는 것이 아니라, 서로 겹쳐 보이는 글자 오브젝트의 크기를 줄이라고 지시했다.
- 반영 내용:
  - `CH-10` 방향 라벨의 `TextMesh.characterSize` 값은 유지했다.
  - `CH-10` 방향 라벨 글자 오브젝트의 `Transform.localScale`만 `0.45, 0.45, 0.45`로 축소했다.
  - 공용 텍스트 생성 헬퍼는 기본값 `1.0`을 유지하고, `CH-10` 호출부에만 축소 스케일을 전달했다.
- 수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/PROGRESS_2026-06-25.md`
- 실행한 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedCargoHoldCh10DirectionMarkersOnly -LogPath .\Logs\AddApprovedCargoHoldCh10DirectionMarkersOnly.log -TimeoutSeconds 360`
- 확인 결과:
  - 브리지 로그에 `Ch10Objects=77`, `NonCh10CargoHoldObjectsUntouched=True`, `ExistingObjectsUntouched=True`가 기록됐다.
  - `ApprovedCargoHoldShellCurrentState.cs`의 `CH-10 ... readable floor label letter ...` 오브젝트들이 `new Vector3(0.45f, 0.45f, 0.45f)` 스케일로 저장됐다.
- 수행하지 않은 작업:
  - `CH-10` 외 운송창고 오브젝트 수정
  - 다른 구역 오브젝트 수정
  - 전체 운송창고 재생성
  - Restore/Ensure/Validate/Smoke/Test/Build
  - 런타임 게임 로직 추가/수정

## 사용자 편집 후 운송창고 현재 상태 스크립트 반영

- 사용자 지시:
  - Unity에서 직접 편집한 현재 운송창고 상태를 그대로 스크립트에 반영하라고 지시했다.
- 반영 내용:
  - `CaptureApprovedCargoHoldShellCurrentState` 브리지 명령으로 현재 열린 Unity 씬의 `Approved Cargo Hold 01 Shell` 트랜스폼 계층을 캡처했다.
  - 캡처 결과를 `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`에 저장했다.
  - 이 파일은 `RestoreApprovedCargoHoldShellCurrentState`가 사용하는 현재 운송창고 복구 기준이므로, 이번 사용자 편집 상태를 최종 복구 시점으로 반영했다.
- 수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/PROGRESS_2026-06-25.md`
- 실행한 명령:
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedCargoHoldShellCurrentState -LogPath .\Logs\CaptureApprovedCargoHoldShellCurrentState.log -TimeoutSeconds 360`
- 확인 결과:
  - 브리지 로그에 `Root=Approved Cargo Hold 01 Shell`, `TransformStates=123`, `Output=Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`가 기록됐다.
  - `ApprovedCargoHoldShellCurrentState.cs`의 `CurrentTransformState` 항목 수가 `123`개로 확인됐다.
- 수행하지 않은 작업:
  - 운송창고 형태 재생성
  - 운송창고 오브젝트 추가 수정/정리
  - 다른 구역 오브젝트 수정
  - Restore/Ensure/Validate/Smoke/Test/Build
  - 런타임 게임 로직 추가/수정

## 운송창고 단독 백업 및 전체 오브젝트 백업 생성

- 사용자 지시:
  - 현재 운송창고 상태를 복구할 수 있는 백업본을 먼저 만들고, 그 뒤에 전체 오브젝트를 복구할 수 있는 전체 백업본을 만들라고 지시했다.
- 반영 내용:
  - `CaptureApprovedCargoHoldShellCurrentState`로 현재 운송창고 상태를 다시 `ApprovedCargoHoldShellCurrentState.cs`에 캡처했다.
  - 운송창고 단독 복구용 백업 파일을 생성했다.
  - 전체 구역 오브젝트 복구용 백업 폴더를 생성하고 현재 상태 스크립트 3개와 `CargoRunMvp` 씬 파일 사본을 저장했다.
  - 전체 백업 폴더에 `MANIFEST.md`를 추가해 포함 파일과 항목 수를 기록했다.
- 생성한 백업:
  - `docs/backups/ApprovedCargoHoldShellCurrentState_2026-06-25.cs.txt`
  - `docs/backups/AllZoneObjectCurrentState_2026-06-25/`
- 전체 백업 포함 파일:
  - `ApprovedArmoryShellCurrentState_2026-06-25.cs.txt` (`CurrentTransformState` 151개)
  - `ApprovedSupplyRoomShellCurrentState_2026-06-25.cs.txt` (`CurrentTransformState` 161개)
  - `ApprovedCargoHoldShellCurrentState_2026-06-25.cs.txt` (`CurrentTransformState` 123개)
  - `CargoRunMvp_2026-06-25.unity`
  - `CargoRunMvp_2026-06-25.unity.meta`
  - `MANIFEST.md`
- 실행한 명령:
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'`
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command CaptureApprovedCargoHoldShellCurrentState -LogPath .\Logs\CaptureApprovedCargoHoldShellCurrentState.log -TimeoutSeconds 360`
  - 백업 폴더 생성 및 파일 복사용 `New-Item`, `Copy-Item`
  - 백업 파일 존재 및 항목 수 확인용 `Get-Item`, `Get-ChildItem`, `rg`
- 확인 결과:
  - 운송창고 캡처 로그에 `Root=Approved Cargo Hold 01 Shell`, `TransformStates=123`, `Output=Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`가 기록됐다.
  - 운송창고 단독 백업과 전체 백업 폴더의 파일들이 생성된 것을 확인했다.
- 수행하지 않은 작업:
  - 운송창고 또는 다른 구역 오브젝트 형태 수정
  - Restore/Ensure/Validate/Smoke/Test/Build
  - 전체 오브젝트 재생성
  - 런타임 게임 로직 수정
  - 기존 백업 삭제 또는 덮어쓰기

## Git 커밋 및 푸시 준비

- 사용자 지시:
  - 지금까지 진행한 내용을 25일 진행문서에 기록한 뒤 GitHub에 커밋 및 푸시하라고 지시했다.
  - 이후 커밋 범위에서 `artSample/`은 제외하고, 그 외 변경은 모두 포함하라고 지시했다.
- 반영 방침:
  - `docs/PROGRESS_2026-06-25.md`에 2026-06-25 진행 내용을 기록했다.
  - 커밋 스테이징에서는 `artSample/**` 경로 전체를 제외한다.
  - `artSample/` 밖의 변경 파일, 백업 파일, 문서, 스크립트, Unity 씬/상태 파일은 커밋 대상에 포함한다.
- 수행하지 않을 작업:
  - `artSample/**` 커밋 포함
  - Unity 오브젝트 수정/재생성
  - Restore/Ensure/Validate/Smoke/Test/Build
  - 기존 변경 되돌리기
  - 강제 푸시

## CH-10 방향 라벨 글자 오브젝트 스케일 추가 축소

- 사용자 지시:
  - `0.45` 스케일에서도 글자가 조금씩 겹치므로, 서로 아예 겹치지 않게 오브젝트 크기를 더 줄이라고 지시했다.
- 반영 내용:
  - `CH-10` 방향 라벨의 `TextMesh.characterSize` 값은 유지했다.
  - `CH-10` 방향 라벨 글자 오브젝트의 `Transform.localScale`을 `0.45, 0.45, 0.45`에서 `0.32, 0.32, 0.32`로 추가 축소했다.
  - 라벨 위치, 글자 간격, 라벨판 크기, 방향 표식 구조는 변경하지 않았다.
- 수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
  - `docs/PROGRESS_2026-06-25.md`
- 실행한 명령:
  - `.\scripts\Refresh-UnityProject.ps1`
  - `.\scripts\Invoke-UnityEditorBridge.ps1 -Command AddApprovedCargoHoldCh10DirectionMarkersOnly -LogPath .\Logs\AddApprovedCargoHoldCh10DirectionMarkersOnly.log -TimeoutSeconds 360`
- 확인 결과:
  - 브리지 로그에 `Ch10Objects=77`, `NonCh10CargoHoldObjectsUntouched=True`, `ExistingObjectsUntouched=True`가 기록됐다.
  - `ApprovedCargoHoldShellCurrentState.cs`의 `CH-10 ... readable floor label letter ...` 오브젝트들이 `new Vector3(0.32f, 0.32f, 0.32f)` 스케일로 저장됐다.
- 수행하지 않은 작업:
  - `CH-10` 외 운송창고 오브젝트 수정
  - 다른 구역 오브젝트 수정
  - 전체 운송창고 재생성
  - Restore/Ensure/Validate/Smoke/Test/Build
  - 런타임 게임 로직 추가/수정

## 운송창고 입구 라벨과 바닥색 위치 수정

- 사용자 지시:
  - 조종실을 제외한 운송창고 입구 문구가 잘못 배치되어 있으므로, 동력실/통제실 문구를 서로 바꾸고 비품실/무기실 문구를 서로 바꾸라고 지시했다.
  - 입구 색이 문구와 연관되어 있으면 입구 색도 같은 기준으로 옮기라고 지시했다.
- 반영 내용:
  - `ApprovedArmoryShellBootstrap.cs`의 운송창고 CH-10 방향 표시와 입구 바닥색 배치를 수정했다.
  - Unity 브리지에 운송창고 입구 색상만 갱신하는 명령과 CH-11 디스플레이 전용 갱신 명령을 추가했다.
  - 열린 Unity 에디터에는 `Refresh-UnityProject.ps1`와 브리지 명령을 통해 반영했다.
- 생성한 백업:
  - `docs/backups/ApprovedCargoHoldShellCurrentState_2026-06-25_after_entrance_fix.cs.txt`
- 수정 파일:
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
- 수행하지 않은 작업:
  - 운송창고 전체 재생성
  - 다른 구역 오브젝트 수정
  - Restore/Ensure/Validate/Smoke/Test/Build

## 운송창고 CH-11 패널 디스플레이 샘플 승인 및 Unity 반영

- 사용자 지시:
  - 기존 `cargo_hold_shell` 모델링에 있는 패널에 `B2_Eq2_E.png` 오른쪽 아래 디스플레이 영역만 잘라 넣은 샘플을 만들라고 지시했다.
  - 샘플 승인 후 운송창고의 패널 디스플레이만 샘플대로 수정하고, 나머지 오브젝트는 건드리지 말라고 지시했다.
  - Unity 적용 후 패널 화면을 좌우 반전하라고 지시했다.
- 반영 내용:
  - `scripts/GenerateCargoHoldShellSample.py`에서 `B2_Eq2_E.png` 오른쪽 아래 UI 영역을 크롭해 샘플 텍스처로 저장하도록 수정했다.
  - `artSample/cargo_hold_shell/`에 승인 검토용 렌더, Blender, FBX, GLB, 문서를 생성했다.
  - 승인 후 Unity용 텍스처 `Assets/_Project/Art/Ship/CargoHold/Textures/B2_Eq2_E_bottom_right.png`를 추가했다.
  - `UpdateApprovedCargoHoldCh11DisplayOnly` 브리지 명령으로 CH-11 패널 디스플레이만 교체했다.
  - 이후 UV 좌우 반전만 적용해 Unity에 다시 반영했다.
- 생성한 백업:
  - `docs/backups/ApprovedCargoHoldShellCurrentState_2026-06-25_ch11_display.cs.txt`
- 수정/생성 파일:
  - `scripts/GenerateCargoHoldShellSample.py`
  - `artSample/cargo_hold_shell/`
  - `Assets/_Project/Art/Ship/CargoHold/Textures/B2_Eq2_E_bottom_right.png`
  - `Assets/_Project/Editor/Validation/ApprovedArmoryShellBootstrap.cs`
  - `Assets/_Project/Editor/Validation/ApprovedCargoHoldShellCurrentState.cs`
  - `Assets/_Project/Editor/Validation/UnityEditorValidationBridge.cs`
  - `Assets/_Project/Scenes/CargoRunMvp.unity`
- 확인 결과:
  - CH-11 디스플레이 브리지 로그에서 `NonCh11CargoHoldObjectsUntouched=True`, `ExistingObjectsUntouched=True`를 확인했다.
- 수행하지 않은 작업:
  - CH-11 외 운송창고 오브젝트 수정
  - 다른 구역 오브젝트 수정
  - Restore/Ensure/Validate/Smoke/Test/Build

## 작업 승인 묶음 규칙 추가

- 사용자 지시:
  - 작업 중 필요한 작업을 일일이 승인받지 말고 전체 과정을 파악한 뒤 승인받을 목록을 묶어서 보여주고 검토를 받는 규칙을 `AGENTS.md` 절대규칙으로 기록하라고 지시했다.
- 반영 내용:
  - `AGENTS.md`에 작업 과정 전체를 파악한 뒤 승인 묶음을 제시하고, 승인된 묶음 범위 안에서만 진행한다는 규칙을 추가했다.
- 수정 파일:
  - `AGENTS.md`

## 복도 단품 샘플 제작

- 사용자 지시:
  - 각 구역 연결은 나중에 진행할 예정이므로, 연결 네트워크가 아니라 복도 그 자체의 샘플을 만들라고 지시했다.
  - 이전에 만든 연결형 샘플은 삭제하라고 지시했다.
  - 폐쇄 셔터는 복도 높이를 막는 크기여야 하며, 위에서 아래로 내려오는 형식이어야 한다고 지시했다.
  - 수평 복도 샘플 하나와 경사 복도 샘플 하나, 총 두 개 샘플로 구성하라고 지시했다.
  - 각 샘플을 한 구도만이 아니라 여러 구도로 보여달라고 지시했다.
- 반영 내용:
  - 연결형 샘플 `artSample/ship_corridor_connections/`와 관련 생성 스크립트/실행기를 삭제했다.
  - 새 복도 단품 샘플 생성기 `scripts/GenerateShipCorridorSegmentSample.py`와 실행기 `scripts/Run-ShipCorridorSegmentSample.ps1`를 추가했다.
  - `artSample/ship_corridor_segment/`에 수평 복도 샘플 1개와 경사 복도 샘플 1개만 생성했다.
  - 각 복도 끝에는 상부 하우징에서 아래로 내려와 바닥까지 막는 전체 높이 폐쇄 셔터, 수직 가이드 레일, 경고 스트립을 배치했다.
  - 각 샘플을 전체 사선, 상단, 셔터 정면, 측면 구도로 렌더링해 총 8장 렌더를 생성했다.
- 생성 파일:
  - `scripts/GenerateShipCorridorSegmentSample.py`
  - `scripts/Run-ShipCorridorSegmentSample.ps1`
  - `artSample/ship_corridor_segment/`
- 실행한 명령:
  - `.\scripts\Run-ShipCorridorSegmentSample.ps1`
- 확인 결과:
  - 실행 스크립트가 종료 코드 `0`으로 완료됐다.
  - `artSample/ship_corridor_segment/renders/`에 8개 렌더가 생성됐다.
  - 샘플 상태는 `APPROVAL_STATUS.json`에 `미승인`, `unityApplicationAllowed=false`로 기록했다.
- 수행하지 않은 작업:
  - Unity 씬, 프리팹, 런타임 자산 반영
  - 실제 구역 연결 배치
  - 충돌체, 네비게이션, 폐쇄 로직 구현
  - Restore/Ensure/Validate/Smoke/Test/Build

## 2026-06-25 커밋 및 푸시 범위 정리

- 사용자 지시:
  - 지금까지 진행한 진행상황을 진행문서에 기록하고, 오늘 했던 작업물들을 GitHub에 커밋 및 푸시하라고 지시했다.
- 커밋 포함 방침:
  - 오늘 대화에서 작업한 운송창고 CH-10/CH-11 반영 파일, CH-11 텍스처, 운송창고 및 복도 샘플, 백업, 승인 규칙, 진행문서를 포함한다.
- 커밋 제외 방침:
  - 현재 워킹트리에 남아 있으나 오늘 대화의 작업 범위와 직접 관련이 확인되지 않은 `artSample/armory_*`, `artSample/control_room_*`, `artSample/engine_room_*`, `artSample/supply_room_*` 변경은 포함하지 않는다.
- 수행하지 않을 작업:
  - Unity 오브젝트 추가 수정
  - Restore/Ensure/Validate/Smoke/Test/Build
  - 기존 사용자 변경 되돌리기
  - 강제 푸시
